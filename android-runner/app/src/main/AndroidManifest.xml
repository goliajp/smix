<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The runner's library module.
         No application tag — this is a pure library; the test APK
         (androidTest variant) carries the instrumentation runner. -->
</manifest>
