org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.enableJetifier=false
kotlin.code.style=official

# The coordinate `:probe` publishes under. `:sdk` states it again in its
# own build file because that is the line fact-scan reads; both are in
# fact-scan's coordinate list, so they cannot drift apart in silence.
smixVersion=10.0.0
