# plan-cold/v2.2-android-behavioural-gate.md — Android 侧的断言必须有地方失败

## 为什么要做

07-20 到 07-21 连续三个 Android 缺陷是同一个形状：**读了参数,断在最后一跳的空实现**
(`set_force_key_events` / `set_target_bundle_id`),以及**照抄文档占位符的 view-id 拼法**
(`com.example.app:id/<tag>`,对任何真实应用都不命中)。三个都不让任何东西变红,所以都活了
下来。

根因不在这三处代码,在**没有任何闸门跑 Android 的行为**:

- CI 与 ship.sh 只跑 `:sdk:testDebugUnitTest`。**`:app:testDebugUnitTest` 无人跑** ——
  8 个测试文件(RunnerWire 编解码 / ViewIdCandidates / SessionTable / Popup / Tree),
  包括为修复上述缺陷刚写的那 5 个。
- **instrumentation 套件完全无人跑**:`:app` 的 `RunnerTest.kt` 1241 行,与 `:sdk` 的
  conformance harness(26 个 fixture)+ Spike001 —— **两个模块各有一份 `src/androidTest/`,
  是两个独立的闸门缺口**。它们是唯一会在真设备上执行 runner 的东西。

iOS 侧的同型盲区已在 v2 决策日志记过两次(「ship gate 从不编译分发给用户的运行器主体」、
「stale test 潜伏 15+ 版」)。Android 侧更深一层:不是不编译,是连已经写好的测试都不跑。

## 入口条件

```bash
# 护栏就位 —— 套件跑起来之前就够不到物理手机
bash scripts/dev/adb-guard.test.sh          # 期望:all cases pass
python3 scripts/dev/workflow-scan.py        # 期望:clean
# 无人占用 Android 工具链
pgrep -fl 'emulator|mobilegate'             # 期望:空
( cd android-runner && ./gradlew --status ) # 期望:无 BUSY daemon
```

**不要用 `pgrep gradle` 判占用**:本机常驻一个 IDLE 的 Gradle daemon,它是 wrapper 的
正常产物,那条命令永远非空,照它执行会每次误判成"有人在跑"。

- adb-guard 已 wire 并被 workflow-scan 盯住(2026-07-21 已完成)
- 已知 `:app:testDebugUnitTest` 当前为绿(本机实测),即 C1 是接线而非修测试

## 资源依赖

- 环境:Android SDK + `sim-smix-android-01` emulator(memory: simx_android_emulator_setup);
  JDK + gradle 9.3.1(仓库自带 wrapper)
- **`ANDROID_SERIAL=emulator-NNNN` 必须显式钉住** —— gradle 的 install/connected 任务会
  扇出到每一台连着的设备,adb-guard 会拦下没钉的调用
- CI:instrumentation 需要 emulator runner,GitHub hosted runner 上是否可跑属 C3 的判定项,
  不预设

## 已知风险

- **1241 行的 instrumentation 套件从未被跑过** → 大概率有 stale 断言。对策:C2 只求
  「编译 + 跑通 + 如实记录通过率」,修测试是 C2 内的独立步骤,不与接线混在一起
- **emulator 与用户其它工作抢资源**(memory: 起服务前先 pgrep user build)。对策:每次起
  emulator 前 pgrep;真跑让位活动 batch
- **物理手机在场**(已因此擦过一台 SM-S9010)。对策:入口条件里的 adb-guard 是硬前置
- **CI 上 instrumentation 可能跑不动** → 若如此,必须**明说降级到哪一层**并写进 gate 输出,
  不允许静默只跑单测却让人以为 Android 被覆盖了

## TDD 要点

这一段的被测对象是**闸门本身**,不是产品代码。每个 checkpoint 的验证形态固定:先证明闸门
在缺陷在场时会红(注入或用真实的历史缺陷),再接线,再复跑转绿。C4 是唯一涉及产品行为的
一段 —— 那里走真设备实测,不是红绿。

## Checkpoint 概要列表

- C1:`:app:testDebugUnitTest` 接进 preflight / CI / ship;并扫一遍还有没有别的 gradle
  模块测试任务同样落在闸门之外(不靠记忆,由脚本枚举)
- C2:instrumentation 套件真跑一次 —— 先 `assembleDebugAndroidTest`(无需设备)证明它编译,
  再在钉住的 emulator 上跑,如实记录通过 / 失败 / 跳过,并修掉 stale 断言
- C3:instrumentation 进 ship gate,与 iOS 的 `xcodebuild build-for-testing` 对称;CI 上跑
  不动就明写降级层级
- C4:行为 smoke —— 起 Android runner 跑一条真 yaml flow,逐条验证 07-20/21 的三个修复
  (`--force-key-events` 生效 / 真实包名进得去 / view-id 候选命中真实应用)在设备上确实成立

## 出口验收

```bash
# Android 的测试任务不再有闸门外的
bash scripts/dev/preflight.sh                      # C1 起含 gradle 单测段
grep -c 'androidTest' scripts/release/ship.sh      # ship gate 覆盖 instrumentation
```

注:`preflight.sh` 此前**完全不跑 gradle**,C1 是新增整段而非往已有段里加模块;且该段
无条件跑,不套用 preflight 对 crate 的「按 git diff 收窄」——本段要防的三个缺陷都是
跨语言契约缺陷,改动只落 `crates/` 时按 `android-runner/` 的 diff 收窄会继续放过。
外加:C4 的 smoke flow 在 emulator 上跑通,且三个修复各有一条断言证明其生效 —— 把它们
装回缺陷版本时该断言会红。

## 触发热化的 prompt 模板

按 CLAUDE.md §6 标准模板,附加本段专属 context:

- 必读:`docs/v2.md` 决策日志 2026-07-20 / 07-21 的五条(三个 Android 缺陷 + 契约入库 +
  guard 假阳性),它们是本段存在的理由
- 必查:`pgrep -fl 'gradle|emulator|mobilegate'`、`bash scripts/dev/adb-guard.test.sh`
- 硬约束:任何触碰设备的命令必须显式 `ANDROID_SERIAL=emulator-NNNN`;adb-guard 会拦,
  被拦时**改命令,不改 guard**
