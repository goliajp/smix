# plan-hot — v2.13 到 C8：在别人的仓里,只经 plugin 走完全程

## 目标 checkpoint

C8：一个 smix 仓**之外**的 app 仓,只装本 plugin,不手工起任何 smix 进程,
完成:选设备 → 起 runner → 启动 app → 感知 → 操作 → 确认 app 真的响应了 →
录制成 flow → 收摊。判据是**真实发生的 tool 调用序列**(`--output-format stream-json`),
不是会话对自己的叙述。

## 前置条件

```bash
python3 scripts/dev/plugin-capability-parity.test.py   # C7 闸门在
bash scripts/dev/build-fixture-app.sh                  # 有个可装的 app
claude --help | grep -c "stream-json"                  # 结构化输出可用
```

## 步骤（线性,2 步）

### S1. 判据:读 tool 调用,不读散文

**红（写测试）**
- 文件：`scripts/dev/v2.13-c8-plugin-loop-e2e.sh`
- 断言（解析 stream-json）：
  1. 会话里真实调用过 `smix_devices` / `smix_use` / `smix_release`
  2. 感知与操作各至少一次(`smix_describe`|`smix_tree`|`smix_find` 与 `smix_fill`|`smix_tap`)
  3. **全程没有出现 Bash 调用 `capsule up` / `runner up`** —— 协同闭环若靠手敲兜底就不算闭
  4. 收尾后本机无残留 xcodebuild

**绿（实现）**
- 工作目录是 `mktemp -d` 里的假 app 仓(装好 fixture .app),不是 smix 仓

### S2. 让它真的跑通

**绿（实现）**
- 提示词只描述**目标**不给命令序列(给了就等于我替 agent 想好了,测不出 skill 有没有用)
- 失败时打印实际的 tool 调用序列,便于判断是能力缺失还是提示不足

## Checkpoint C8 验收

```bash
bash scripts/dev/v2.13-c8-plugin-loop-e2e.sh > /tmp/c8.log 2>&1; echo $?; tail -1 /tmp/c8.log
```
期望：退出 0,末行 `C8-PLUGIN-LOOP-PASS`。

## 完成后动作

1. 归档此文件到 `docs/plan-history/v2.13-c8-hot.md`
2. 生成新 `plan-hot.md`（到 C9：§9#2 附件化）
