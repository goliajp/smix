# plan-hot — v0.7 C3：`.github/workflows/ci.yml` 多 runtime matrix（iOS-17-5 / iOS-18-4 / iOS-26-4）字面落地 — `strategy.matrix.runtime` 3 token + `strategy.fail-fast: false` + 26.4 hard-required（`continue-on-error: false`）/ 17.5 + 18.4 在 v0.7 C3 期"5-arg Indigo HID 路径未实装"现实下软 fan-out（`continue-on-error: true`）字面在场 + 既有 5 gate（`bun install --frozen-lockfile` / `bun run typecheck` / `bun x vitest run` 578 / `swift test` ≥102 / `bash scripts/simx-c1-mcp-smoke.sh` all_ok=ok）每条 matrix branch 字面复用 + 新建 `scripts/simx-c3-ci-matrix-validate.sh`（~70 行 / 14 字段 JSON gate 字面校 ci.yml 含 matrix + 3 runtime token + continue-on-error + fail-fast: false + C2 已有 7 token 仍在场）；C2 validator `scripts/simx-c2-ci-workflow-validate.sh` 不动 / 不运行 / 不删（C2 close 已 snapshot 完成；C3 改 ci.yml 后 C2 validator `no_matrix=ok` 字面将翻 fail —— 这是 C2→C3 边界转换的诚实显式表达）

> [3/4 信息层] 当前唯一热计划。**C3 是 v0.7 第 3 个 checkpoint**，cold plan §Checkpoint 概要列表字面"C3 runtime 矩阵：CI matrix iOS 17.5 / 18.4 / 26.x，每条都跑 smoke 全过"。**"每条都跑 smoke 全过" 在 v0.7 C3 真实工程约束下解读为**：26.4 branch hard-required 全过（5 gate exit 0 字面 = C2 已验过的同 5 gate）；17.5 / 18.4 branch **matrix 字面在场 + smoke step 字面在场 + `continue-on-error: true` 字面在场**——理由：iOS 17/18 5-arg `IndigoHIDMessageForMouseNSEvent` ABI 路径 v0.7 C3 真路径**未实装**（`swift-bridge/Sources/SimxIndigoHID/ChannelDispatcher.swift:38` 字面 "iOS 17/18 require the 5-arg `IndigoHIDMessageForMouseNSEvent` ABI" + :59 字面 "(5-arg legacy ABI scheduled for v0.7)" / v1.md 决策日志 2026-05-15 v0.2 C3 末段字面"5-arg iOS 17/18 路径推 v0.7 多 runtime CI 矩阵" / 本机 `xcodebuild -showsdks` 字面单 iOS 26.4 SDK / `xcrun simctl list runtimes -j` 字面仅 com.apple.CoreSimulator.SimRuntime.iOS-26-4 在场）；既有 5 gate 全部 sim 无关（typecheck = 纯 TS / vitest = 纯 TS mock / swift test = SPM macOS target / c1-mcp-smoke = stdio JSON-RPC 无 sim 依赖 / bun install = 纯 npm install） → matrix 3 branch 在 macos-15 上 5 gate 字面**应该全过**（不受 runtime SDK 缺席影响），但 cold plan §C3 字面"runtime 矩阵"意图 = "为 iOS 17/18 HID 真路径 v0.7+ 实装做 CI fan-out 占位 + 26.4 hard gate" → C3 落地 = 占位 + 26.4 hard gate + 17/18 软 fan-out（continue-on-error）。
>
> 通过后世界变成：(a) `.github/workflows/ci.yml` 字面在场 + 含 `strategy.matrix.runtime: [iOS-17-5, iOS-18-4, iOS-26-4]` 3 token 字面 + `fail-fast: false` 字面 + `continue-on-error: ${{ matrix.runtime != 'iOS-26-4' }}` 字面（job-level 字面，硬约束 26.4 / 软 17.5+18.4）+ `name: test (macos-15 / ${{ matrix.runtime }})` 字面动态 job name + 既有 C2 工程字面 token 全部保留 + 行数 ≤ 100 行；(b) `scripts/simx-c3-ci-matrix-validate.sh` 字面在场（~70 行）+ 14 字段 JSON 单行输出 `{exit_code, has_macos_15, has_setup_bun, has_bun_install_frozen, has_typecheck, has_vitest, has_swift_test, has_mcp_smoke, has_matrix, has_runtime_17_5, has_runtime_18_4, has_runtime_26_4, has_fail_fast_false, has_continue_on_error, all_ok}` 字面全 ok + exit 0；(c) 本机 5 gate（与 C2 同 5 命令）逐条 exit 0 不退化：`bun install --frozen-lockfile` / `bun run typecheck` / `bun x vitest run`（`Tests 578 passed` 字面）/ `(cd swift-bridge && swift test)`（`Executed [0-9]+ tests` ≥ 102 字面）/ `bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"'`；(d) `scripts/simx-c2-ci-workflow-validate.sh` 字面不动 / 不运行 / 不删（C2 close 已 snapshot；C3 改 ci.yml 后 C2 validator 期望 `no_matrix=ok` / `single_job=ok` 必翻 fail —— 不是 bug 是边界转换；plan-history/v0.7-c2-hot.md 已字面冻结 C2 时刻状态）；(e) v1.md 决策日志 +1 行字面记 v0.7 C3 close + cold plan 偏差（17/18 真路径推 v0.7+ / 26.4 hard / 17/18 软 fan-out）+ git 工作树仍缺席偏差延续记。
>
> **C3 不在范围内（C4-C7 推后字面拒绝路径）**：
>
> - **C4 `simx doctor --json` 升级 schema 固化**（C4 范围；C3 不动 `src/cli/doctor.ts` 任何字段）
> - **C5 Claude Code plugin manifest `claude-plugin.json`**（C5 范围；C3 不写 plugin manifest）
> - **C6 v1 acceptance script `scripts/v1-acceptance.sh`**（C6 范围；C3 不写聚合 acceptance）
> - **C7 README / docs/ v1.0 发布 + git init / branch protection 人工步骤说明**（C7 范围；C3 不动 README）
> - **真装 iOS 17.5 / 18.4 runtime（`xcversion simulators --install` / `xcrun simctl runtime add --type ios-17-5`）**（cold plan §风险条字面 "用 xcversion 或预 cache" —— C3 期间 17.5 / 18.4 job `continue-on-error: true` 即可，真装命令在 ci.yml 内**不写**；待 v0.7+ 5-arg 真路径实装时一并 graft）
> - **实装 iOS 17/18 5-arg Indigo HID ABI（`swift-bridge/Sources/SimxIndigoHID/` 新增 5-arg call site 或 5-arg dispatcher 文件）**（v0.7+ 范围；C3 仅 CI matrix 占位，不实装 swift 路径）
> - **真起 sim + 长跑 e2e 进 CI（`scripts/long-run-100.ts` 或 `scripts/mcp-smoke.ts` 进 ci.yml）**（依赖 dev-sim.txt CI fixture + `xcrun simctl create` 动态创 dev sim + ~12 分钟时长；推 C6 v1 acceptance 一并处理）
> - **改 `scripts/simx-c2-ci-workflow-validate.sh` 任何字段**（C2 snapshot 冻结；C3 改 ci.yml 后 C2 validator `no_matrix=ok` 会翻 fail，**这是预期边界转换** —— 文件不删不改，仅 C3 acceptance 不调用）
> - **删 / 重写 `.github/workflows/ci.yml`**（C2 47 行字面工程实体在场；C3 = **edit-in-place 增量加 `strategy.matrix` 段** + 改 job `name` 字面引 matrix.runtime + 加 `continue-on-error` 表达式；行数从 47 → ≤ 100 行字面增量）
> - **加 `xcrun simctl runtime add` step 在 ci.yml**（推 v0.7+ 5-arg 实装；C3 期间 17/18 job 跑既有 5 gate 即可 —— 5 gate 全 sim 无关、不依赖 runtime SDK 在场）
> - **加 `workflow_dispatch` / `schedule` / branch filter 改动**（C2 决策 16.A 字面 push + pull_request 2 trigger；C3 沿用 0 改）
> - **加 `actions/cache` / `bun-install --cache` / artifacts upload**（C2 决策 15.A + 17.A 字面拒；C3 沿用）
> - **加 GHA secrets / OIDC / token / codecov**（C2 决策 16.A + 17.B 字面拒；C3 沿用）
> - **加 release / changesets / npm publish workflow**（C2 决策 17.C 字面 — C5 才做）
> - **改 `package.json` `scripts.test` / `scripts.typecheck` / 加 npm dep / 加 dev dep**（C2 决策 2.B 字面 0 dep；C3 沿用 3 prod dep + 既有 dev dep 字面冻结）
> - **加 unit vitest case for matrix 行为或 validator 行为**（C2 决策 10.A 字面 — YAML 文件 + shell wrapper 不挂 vitest；C3 沿用 578 → 578 TS baseline 字面不动）
> - **改 `scripts/simx-c1-mcp-smoke.sh` 任何字段**（C2 决策延续 0 改动；C3 沿用）
> - **改 `src/` 任何文件 / `swift-bridge/` 任何文件 / `examples/*.ts` 任何文件**（v0.7 全期严控 0 改 swift / 0 改 既有 src/sim 6 文件 / 0 改 既有 src/{driver,sdk,core,cli,mcp}）
> - **改 `docs/roadmap.md`**（v0.7 🔥 字面冻结；C7 才翻 ✅）
> - **改 `docs/v1.md` 任何边界字段**（§2 模块表 / §4 Success Criteria [6] 三 runtime / §5 风险表 / §6 工作量 全冻结；C3 close 仅决策日志 +1 行）
> - **改 `docs/plan-cold/v0.7.md`**（cold plan 冻结）
> - **加 `.gitignore` / 改 `engines.bun` / 改 `bun.lock`**（C2 决策延续；C3 不涉）
> - **加 `reusable workflow` / `composite action`**（C2 决策 18.A 字面 — monolithic；C3 单文件继续）
> - **新 acceptance shell（如 `scripts/simx-v07-acceptance.sh`）**（v0.6 acceptance 是 v0.x 末段聚合形态；v0.7 全期不写 v07-acceptance，C6 才聚合 v1 全体 success criteria）
> - **写 dev-sim.txt CI fixture / 在 ci.yml `xcrun simctl create` 创 dev sim**（推 C6 v1 acceptance；C3 5 gate 全部 sim 无关）
>
> **承接（v0.7 C2 close 后实测 baseline，2026-05-16）**：v0.7 C2 ✅ done —— v1.md 决策日志末尾 `2026-05-16 v0.7 C2 close` 字面在场（已读字面在场）+ `.github/workflows/ci.yml` 47 行字面在场 + `scripts/simx-c2-ci-workflow-validate.sh` 字面在场 + 10 字段 JSON gate 字面 `{"all_ok":"ok",...}` 实测在场（已跑 `simx-c2-ci-workflow-validate.sh` 实测 exit 0 + 10 字段全 ok）；TS baseline = **578**（v0.6 close 568 经 dev-lock hot-fix 至 575 / v0.7 C1 +3 supervisor 至 578 / C2 不动 → C3 不动） / 102 swift 不变 / typecheck 0 error / 27 MCP tool expose / 25 vitest 测试文件（v0.7 C1 close note 已记 24→25 graft + C2 close note 已记 plan-hot 偏差 `find src '*.test.ts' | wc -l = 25`）。
>
> **进入 v0.7 C3 前置检字面全 ok**：
>
> 1. **v0.7 C2 工程实体字面在场** — `.github/workflows/ci.yml` 字面 47 行 + 7 step + 9 token 字面 + `scripts/simx-c2-ci-workflow-validate.sh` 字面 + `docs/plan-history/v0.7-c2-hot.md` 字面在场 + v1.md `2026-05-16 v0.7 C2 close` 字面 + 5 gate 本机当前再跑 exit 0 字面（实测当前 `bun x vitest run` `Tests 578 passed` / c2-validate `all_ok=ok`）；
> 2. **v1.md §4 Success Criteria [6] "兼容：iOS 17.5 / 18.4 / 26.x 全部通过冒烟集" 字面在场 + §2.E "CI：GitHub Actions macOS-15，runtime 矩阵 iOS 17.5 / 18.4 / 26.x" 字面在场**（C3 范围工程实体 = v1.md §2.E + §4 [6] 字面）；
> 3. **本机非 git 工作树字面延续** — `test -d .git` 字面 fail（lab15-autofix 仍非 git 工作树）；C3 范围内 GHA workflow 真触发仍不在路径 —— 沿用 C2 等价策略：(a) YAML 文件字面契约 + (b) 5 gate 本机字面执行 + (c) matrix 字面校；
> 4. **iOS 17.5 / 18.4 SDK + runtime 本机仍缺席字面** — `xcodebuild -showsdks` 字面单 iOS 26.4；`xcrun simctl list runtimes -j` 字面单 com.apple.CoreSimulator.SimRuntime.iOS-26-4 在场 + isAvailable=true —— 这与 C3 5 gate 全 sim 无关一致：5 gate 不依赖 iOS SDK / runtime SDK 在场字面；matrix 是 CI 时刻表达，本机不验真 sim；
> 5. **5-arg Indigo HID iOS 17/18 路径未实装字面** — `swift-bridge/Sources/SimxIndigoHID/ChannelDispatcher.swift:38` 字面 "iOS 17/18 require the 5-arg `IndigoHIDMessageForMouseNSEvent` ABI" + :59 字面 "(5-arg legacy ABI scheduled for v0.7)" —— 这是 v0.7 C3 软 fan-out（17/18 `continue-on-error: true`）决策的工程现实依据；
> 6. **C3 红前提**：`scripts/simx-c3-ci-matrix-validate.sh` 字面不存在 + `.github/workflows/ci.yml` 字面无 `strategy:` / `matrix:` token（grep 应 fail）。
>
> **本机探测（C3 落地决策依据，2026-05-16）**：
>
> - `xcodebuild -version` 字面 `Xcode 26.4.1`（C2 baseline 延续；C3 不再起新 xcode 字面）
> - `xcodebuild -showsdks` 字面单 iOS 26.4（决策 1.A 字面 — C3 matrix 26.4 hard / 17/18 软；不在 C3 装 17/18 SDK）
> - `xcrun simctl list runtimes -j | jq '.runtimes[].identifier'` 字面单 `com.apple.CoreSimulator.SimRuntime.iOS-26-4`（决策 1.A 字面延续）
> - `bun --version` 字面 `1.3.14`（CI setup-bun `bun-version: ^1.3.0` 字面延续）
> - `bun x vitest run` 字面 `Tests 578 passed (578)`（C3 baseline 578 字面不动）
> - `(cd swift-bridge && swift test)` 字面 `Executed N tests` ≥ 102（C3 范围 0 改动 swift-bridge/）
> - `bash scripts/simx-c1-mcp-smoke.sh` 字面 `all_ok=ok` + `tools_list_count=27`（C3 范围内 0 改动）
> - `bash scripts/simx-c2-ci-workflow-validate.sh` 字面**当前** exit 0 + `all_ok=ok` + `no_matrix=ok` + `single_job=ok`（**C3 改 ci.yml 后**：`no_matrix=ok` → `no_matrix=fail` + `single_job=ok` → `single_job=fail`；这是 C2→C3 边界转换的诚实表达 —— C2 validator 文件不动 / 不删 / 不运行；C3 acceptance 用 c3-ci-matrix-validate.sh 取代）
> - `wc -l .github/workflows/ci.yml` 字面 47 行（C3 增量后 ≤ 100 行）
> - `.github/workflows/` 目录字面在场（C2 close 已建；C3 复用，不新建）
> - **HID 5-arg iOS 17/18 路径未实装字面**：`grep -rn '5-arg' swift-bridge/Sources/SimxIndigoHID/` 字面注释中多处声明"未实装 / scheduled for v0.7"；无任何 `IndigoHIDDispatcher5Arg.swift` 或 5-arg call site 字面 —— 决策 1.B 字面：C3 期间 17/18 真路径不实装，job 仅占位 + continue-on-error
> - **`.simx/dev-sim.txt` 字面**：本机当前空（`ls /Users/doracawl/workspace/labs/lab15-autofix/.simx 2>/dev/null` 空 / 不存在；用户长跑期间用过 7B88259F UDID 字面记 v0.7 C1 close note）—— C3 5 gate 全部 sim 无关，无需 dev-sim.txt 字面在场
> - **dep 字面 3 个**：`citty` + `@modelcontextprotocol/sdk@1.29.0` + `zod@^3.25.0`（C3 0 dep 增）
> - **`engines.bun` 字面 `>=1.1.0`**（C2 延续）
> - **0 既有 c[1-8] smoke 字段改 / 0 既有 v0[1-6] / runner / axp / hostid smoke 改 / 0 既有 vitest test 改字面**
> - **既有 25 vitest 测试文件字面在场**（不是 24 — v0.7 C1 graft `runner-supervisor.test.ts`；C2 close note 已字面记）
>
> **现状盘点（C3 真生效边界）**：
>
> - **编辑 `.github/workflows/ci.yml`**（47 → ≤ 100 行）：
>   - 保留既有 7 step 字面全部不动
>   - 在 `jobs.test` 块下加 `strategy` 段字面：
>     ```yaml
>     strategy:
>       fail-fast: false
>       matrix:
>         runtime: [iOS-17-5, iOS-18-4, iOS-26-4]
>     ```
>   - 改 job `name:` 字面引 matrix：`name: test (macos-15 / ${{ matrix.runtime }})`
>   - 加 `continue-on-error: ${{ matrix.runtime != 'iOS-26-4' }}` 字面在 job level（26.4 hard / 17.5+18.4 软；决策 1.B 字面）
>   - 加 header comment 字面记 17/18 5-arg 未实装 + cold plan §C3 字面占位意图
>   - **不**加 `xcrun simctl runtime add` / **不**加 `xcversion install` / **不**加 sim 起步 / **不**加 dev-sim.txt fixture step / **不**加 matrix.include 加复杂排除（决策 1.A + 1.B 字面）
> - **新建 `scripts/simx-c3-ci-matrix-validate.sh`**（~70 行）：
>   - mirror `scripts/simx-c2-ci-workflow-validate.sh` 形态字面（决策 5.A 字面）
>   - `set -uo pipefail` + `cd ROOT`
>   - `YML=".github/workflows/ci.yml"` + `test -f "$YML"` 字面 guard
>   - 13 个 grep 字面 token 检查 → 13 个 ok/fail 变量
>   - 14 字段 JSON 单行字面输出 `{exit_code, has_macos_15, has_setup_bun, has_bun_install_frozen, has_typecheck, has_vitest, has_swift_test, has_mcp_smoke, has_matrix, has_runtime_17_5, has_runtime_18_4, has_runtime_26_4, has_fail_fast_false, has_continue_on_error, all_ok}`
>   - `all_ok = ok` iff 上 13 字段全 ok 字面
>   - exit 0 iff `all_ok == ok`
>   - **不**真跑 5 gate 命令（mirror C2 validator 决策 5.B 字面 — validator 仅校 YAML 文件字面契约；5 gate 命令本机 Checkpoint 验收段单独逐条跑）
> - **0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/ 任何文件 / 0 既有 scripts/simx-c1-mcp-smoke.sh / 0 scripts/simx-c2-ci-workflow-validate.sh / 0 scripts/mcp-smoke.ts / 0 scripts/long-run-100.ts / 0 scripts/simx-c1-longrun-100-smoke.sh / 0 scripts/simx-v0[2-6]-acceptance.sh / 0 scripts/simx-runner-* / 0 scripts/simx-axp-* / 0 scripts/simx-hostid-* / 0 既有 25 vitest 测试文件改 / 0 既有 578 vitest case 改 / 0 dep / 0 既有 27 ToolDef 改 / 0 既有 6 mcp smoke 字段改 / 0 examples 改 / 0 roadmap.md 改 / 0 v1.md 边界改（仅决策日志 +1 行）/ 0 cold v0.7.md 改 / 0 README 改 / 0 package.json 改 / 0 tsconfig.json 改 / 0 vitest.config.ts 改**
>
> **C3 真生效 = 二项改动（≤ 5 字面任务约束严守 / 实测 ≤ 2 文件）**：
>
> 1. **编辑 `.github/workflows/ci.yml`**（47 → ≤ 100 行）— 核心 1（GHA YAML matrix 段增量，cold plan §C3 字面"CI matrix iOS 17.5 / 18.4 / 26.x"工程实体）
> 2. **新建 `scripts/simx-c3-ci-matrix-validate.sh`**（~70 行）— 核心 2（本机 lint gate，mirror C2 validator 形态）
>
> **核心改动 = 2 条 ≤ 5 字面满足**。
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / SimctlClient 0 / RunnerClient 0 / RunnerSupervisor 0 / DevSimLockViolation 0 / readDevSimLock 0 / ScreenDescription 0 / SerializedFailure 0 / FailureCode 0 / Role 0 / Rect 0 / ElementSummary 0 / A11yNode 0 / dep 0 / wire payload 0 改 / 既有 27 ToolDef 0 / 既有 server.ts 0 / 既有 tools.ts 0 / 既有 index.ts 0 / 既有 schemas.ts 0 / 既有 examples 0 / 既有 6 mcp smoke 0 字段改 / 既有 5 v0[1-5] acceptance 0 改 / v0.6 acceptance 0 改 / mcp-smoke.ts 0 改 / long-run-100.ts 0 改 / simx-c1-longrun-100-smoke.sh 0 改 / runner-supervisor.ts 0 改 / runner-client.ts 0 改 / dev-lock.ts 0 改 / cell.ts 0 改 / session.ts 0 改 / simctl.ts 0 改 / a11y-tree-source.ts 0 改 / 既有 25 vitest test file 0 改 / 既有 578 vitest case 0 改 / v1.md 边界 0 改 / roadmap.md 0 改 / plan-cold/v0.7.md 0 改 / package.json 0 改 / CLI flag 0 加 / env 变量 0 加 / src/driver/ 0 改 / src/sdk/ 0 改 / src/core/ 0 改 / src/cli/ 0 改 / src/mcp/ 0 改 / swift-bridge/ 0 改 / scripts/mcp-smoke.ts 0 改 / scripts/simx-c[1-8]-*.sh 0 改 / scripts/simx-runner-*.sh 0 改 / scripts/simx-axp-*.sh 0 改 / scripts/simx-hostid-*.sh 0 改 / scripts/simx-v0[1-6]-acceptance.sh 0 改 / scripts/simx-c2-ci-workflow-validate.sh 0 改 / .gitignore 0 改 / engines.bun 0 改 / bun.lock 0 改**。允许改动 / 新建文件范围（**核心 ≤ 5 条**字面任务约束）：
>
> 1. **编辑 `.github/workflows/ci.yml`**（47 → ≤ 100 行）— 核心 1
> 2. **新建 `scripts/simx-c3-ci-matrix-validate.sh`**（~70 行）— 核心 2
>
> **不允许**：(a) 改 `src/` 任何文件；(b) 改 `swift-bridge/`；(c) 改任何 `examples/*.ts`；(d) 改既有 `scripts/simx-c[1-8]-*.sh` / `scripts/simx-v0[1-6]-acceptance.sh` / `scripts/simx-runner-*.sh` / `scripts/simx-axp-*.sh` / `scripts/simx-hostid-*.sh` / `scripts/long-run-100.ts` / `scripts/mcp-smoke.ts`；(e) 引第 4 个 npm dep / 第 4 个 dev dep；(f) 加 CLI flag / env 变量 / `SIMX_RUNTIME` plumbing；(g) 写 plan-cold/v0.7.md / 写 C4-C7 任何代码 / 改 roadmap.md / 改 v1.md 边界 / 改 README；(h) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient` / `RunnerClient` / `RunnerSupervisor` / `DevSimLockViolation` / `readDevSimLock`；(i) 改 ScreenDescription / SerializedFailure / FailureCode / Role / Rect / ElementSummary / A11yNode 字段；(j) 在 ci.yml 跑 sim / runner / xcodebuild test 后台 / dev-sim.txt 依赖 / `xcrun simctl create` / `xcrun simctl runtime add` / `xcversion install`；(k) 加 cache / secrets / artifacts / dispatch / schedule；(l) 加 unit vitest case for workflow file / matrix validator；(m) 改 `scripts/simx-c1-mcp-smoke.sh` 任何字段；(n) 删 / 改 `scripts/simx-c2-ci-workflow-validate.sh`；(o) 实装 iOS 17/18 5-arg Indigo HID swift 路径；(p) 新建 `scripts/simx-v07-acceptance.sh` 聚合脚本。

## 目标 checkpoint

**C3**：通过后世界变成 — (a) `.github/workflows/ci.yml` 字面在场 + 含 C2 既有 9 字面 token（`name: ci` / `runs-on: macos-15` / `actions/checkout@v4` / `oven-sh/setup-bun@v2` / `bun install --frozen-lockfile` / `bun run typecheck` / `bun x vitest run` / `swift test` / `simx-c1-mcp-smoke.sh`）+ `timeout-minutes: 30` 字面 + 新增 6 字面 token（`strategy:` / `matrix:` / `fail-fast: false` / 3 runtime token `iOS-17-5` + `iOS-18-4` + `iOS-26-4` / `continue-on-error:` 含 `matrix.runtime != 'iOS-26-4'` 表达式）+ 行数 ≤ 100 行；(b) `scripts/simx-c3-ci-matrix-validate.sh` 字面 exit 0 + 14 字段 JSON + `all_ok: "ok"` 字面；(c) 本机 5 gate 字面逐条 exit 0：`bun install --frozen-lockfile` / `bun run typecheck` / `bun x vitest run`（`Tests 578 passed` 字面）/ `(cd swift-bridge && swift test)`（`Executed [0-9]+ tests` ≥ 102 字面）/ `bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"'`；(d) 578 TS case baseline 字面不退化（C3 0 case 增）；(e) 27 MCP tool / 102 swift / 35 字段 v0.6 acceptance / mcp-smoke.ts / long-run-100.ts / runner-supervisor / dev-lock / examples 等全不退化（0 改动字面验）；(f) `scripts/simx-c2-ci-workflow-validate.sh` 字面**仍在场**（不删；C3 改 ci.yml 后 C2 validator `no_matrix=ok` + `single_job=ok` 翻 fail = 预期边界转换，C3 acceptance 不调用 C2 validator）。

- `test -f .github/workflows/ci.yml` 字面
- `bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.all_ok == "ok"' > /dev/null` 字面 exit 0
- `bun install --frozen-lockfile` 字面 exit 0
- `bun run typecheck` 字面 exit 0
- `bun x vitest run 2>&1 | grep -qE 'Tests +578 passed'` 字面
- `(cd swift-bridge && swift test 2>&1 | tail -5 | grep -qE 'Test Suite .All tests. passed|Executed +[0-9]+ tests')` 字面（swift test 输出格式锁定 ≥ 102）
- `bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null` 字面 exit 0
- 0 src / 0 swift / 0 既有 scripts/simx-* / 0 既有 25 vitest test file / 0 既有 docs 边界 / 0 dep / 0 既有 mcp smoke 字段改 字面

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.7 C2 close 痕迹存在
test -f .github/workflows/ci.yml
test -f scripts/simx-c2-ci-workflow-validate.sh
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.all_ok == "ok"' > /dev/null

# v0.7 C1 close 痕迹存在
test -f src/sim/runner-supervisor.ts
test -f src/sim/__tests__/runner-supervisor.test.ts
test -f scripts/long-run-100.ts
test -f scripts/simx-c1-longrun-100-smoke.sh

# v0.6 整体 close 痕迹存在
test -f docs/plan-history/v0.6-c8-hot.md
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f scripts/simx-c1-mcp-smoke.sh

# v0.7 C1 + C2 close 决策日志字面在场
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C1 close' docs/v1.md
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C2 close' docs/v1.md

# v1.md C3 边界条款字面在场（未被偷加 / 偷减）
grep -qE 'CI.*GitHub Actions macOS-15.*runtime 矩阵 iOS 17\.5 / 18\.4 / 26\.x' docs/v1.md
grep -qE '^\[6\] 兼容：iOS 17\.5 / 18\.4 / 26\.x 全部通过冒烟集$' docs/v1.md

# roadmap.md v0.7 🔥 字面（未提前翻 ✅）
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# 本机非 git 工作树字面延续验
! test -d .git

# 578 TS baseline 字面（C3 不动 578）
bun x vitest run 2>&1 | grep -qE 'Tests +578 passed'

# typecheck baseline 字面
bun run typecheck > /dev/null

# c1-mcp-smoke 字面 ok（CI gate 形态依赖）
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null

# dep 字面 3 个（不退化）
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'
jq -e '.dependencies["@modelcontextprotocol/sdk"]' package.json > /dev/null
jq -e '.dependencies["citty"]' package.json > /dev/null
jq -e '.dependencies["zod"]' package.json > /dev/null

# C3 红前提：c3-validator 文件**不存在**字面
! test -f scripts/simx-c3-ci-matrix-validate.sh

# C3 红前提：ci.yml 当前无 matrix / strategy 字面
! grep -qE '^[[:space:]]*strategy:' .github/workflows/ci.yml
! grep -qE '^[[:space:]]*matrix:' .github/workflows/ci.yml

# 本机 iOS 26.4 单 runtime 字面（C3 决策 1.A 依据）
xcrun simctl list runtimes -j | jq -e '[.runtimes[] | select(.isAvailable == true) | .identifier] | index("com.apple.CoreSimulator.SimRuntime.iOS-26-4") != null' > /dev/null
xcrun simctl list runtimes -j | jq -e '[.runtimes[] | select(.isAvailable == true) | .identifier] | length == 1' > /dev/null

# 5-arg Indigo HID iOS 17/18 路径未实装字面（C3 决策 1.B 依据）
grep -qE '5-arg legacy ABI scheduled for v0\.7' swift-bridge/Sources/SimxIndigoHID/ChannelDispatcher.swift

# bun >=1.1.0 字面（engines）
test "$(jq -r '.engines.bun' package.json)" = '>=1.1.0'

# bun.lock 字面在场
test -f bun.lock

# C2 ci.yml 行数字面 baseline（C3 后 ≤ 100 行 cap）
test "$(wc -l < .github/workflows/ci.yml)" -le 60
```

期望：以上每条 exit 0。

## 步骤（线性，无分叉；2 个 step）

### S1. 编辑 `.github/workflows/ci.yml` — 增量加 `strategy.matrix.runtime: [iOS-17-5, iOS-18-4, iOS-26-4]` + `strategy.fail-fast: false` + job `name:` 引 `${{ matrix.runtime }}` + job-level `continue-on-error: ${{ matrix.runtime != 'iOS-26-4' }}` + header comment 记 v0.7 C3 占位语义；既有 7 step 字面全部不动；行数 47 → ≤ 100 行

**红（写测试）**

无新 vitest case（C3 是 CI workflow 文件 + shell wrapper validator；578 TS case 字面不增 —— 决策 10.A 字面延续 C2）。

红前提 = `! grep -qE '^[[:space:]]*strategy:' .github/workflows/ci.yml`（前置条件已验）。

**绿（实现）**

编辑 `.github/workflows/ci.yml`（47 → 约 65 行）。最终内容字面：

```yaml
# v0.7 C3 — multi-runtime matrix CI gate (GitHub Actions macos-15).
# Matrix runtime: iOS-17-5 / iOS-18-4 / iOS-26-4. The 5 gates below
# (install --frozen-lockfile / typecheck / vitest 578 / swift test >=102 /
# c1-mcp-smoke stdio JSON-RPC) are sim-agnostic and pass on macos-15
# irrespective of the matrix runtime label.
#
# iOS 26.4: hard-required (continue-on-error: false).
# iOS 17.5 / 18.4: soft fan-out (continue-on-error: true) — the 5-arg
# `IndigoHIDMessageForMouseNSEvent` ABI path that those runtimes need is
# documented in swift-bridge/.../ChannelDispatcher.swift as "scheduled
# for v0.7" but is not yet implemented; the matrix branches act as
# placeholders so the v0.7+ HID work can flip them to hard-required
# without touching ci.yml shape.

name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: test (macos-15 / ${{ matrix.runtime }})
    runs-on: macos-15
    timeout-minutes: 30
    continue-on-error: ${{ matrix.runtime != 'iOS-26-4' }}

    strategy:
      fail-fast: false
      matrix:
        runtime: [iOS-17-5, iOS-18-4, iOS-26-4]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ^1.3.0

      - name: Install dependencies (frozen lockfile)
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Vitest (TS unit tests, expect 578 passed)
        run: bun x vitest run

      - name: Swift test (SimxRunnerCore, expect >=102 passed)
        run: swift test
        working-directory: swift-bridge

      - name: C1 MCP stdio smoke (expect all_ok=ok)
        run: |
          set -euo pipefail
          OUT="$(bash scripts/simx-c1-mcp-smoke.sh)"
          echo "$OUT"
          echo "$OUT" | jq -e '.all_ok == "ok"' > /dev/null
```

字面验证（S1 内 inline）：
```bash
test -f .github/workflows/ci.yml
test "$(wc -l < .github/workflows/ci.yml)" -le 100
# C2 既有 9 token 仍在场字面
grep -qE '^name: ci$' .github/workflows/ci.yml
grep -qE '^[[:space:]]*runs-on: macos-15$' .github/workflows/ci.yml
grep -qE 'actions/checkout@v4' .github/workflows/ci.yml
grep -qE 'oven-sh/setup-bun@v2' .github/workflows/ci.yml
grep -qE 'bun install --frozen-lockfile' .github/workflows/ci.yml
grep -qE 'bun run typecheck' .github/workflows/ci.yml
grep -qE 'bun x vitest run' .github/workflows/ci.yml
grep -qE '(^|[[:space:]])swift test([[:space:]]|$)' .github/workflows/ci.yml
grep -qE 'simx-c1-mcp-smoke\.sh' .github/workflows/ci.yml
grep -qE 'timeout-minutes: 30' .github/workflows/ci.yml
# C3 新增 6 字面 token
grep -qE '^[[:space:]]*strategy:[[:space:]]*$' .github/workflows/ci.yml
grep -qE '^[[:space:]]*matrix:[[:space:]]*$' .github/workflows/ci.yml
grep -qE 'fail-fast:[[:space:]]*false' .github/workflows/ci.yml
grep -qE 'iOS-17-5' .github/workflows/ci.yml
grep -qE 'iOS-18-4' .github/workflows/ci.yml
grep -qE 'iOS-26-4' .github/workflows/ci.yml
grep -qE "continue-on-error:.*matrix\\.runtime.*!=.*'iOS-26-4'" .github/workflows/ci.yml
grep -qE 'matrix\.runtime' .github/workflows/ci.yml
```

期望：每条 exit 0。

**重构**

无。

---

### S2. 新建 `scripts/simx-c3-ci-matrix-validate.sh` — regex 字面校 `.github/workflows/ci.yml` 含 C2 既有 7 token + C3 新增 6 matrix token + 14 字段 JSON gate 输出（mirror C2 validator 形态字面）

**红（写测试）**

无新 vitest case（同 S1）。

红前提 = `! test -f scripts/simx-c3-ci-matrix-validate.sh`（前置条件已验）。

**绿（实现）**

新建 `scripts/simx-c3-ci-matrix-validate.sh`（~70 行）：

```bash
#!/usr/bin/env bash
# v0.7 C3 — GitHub Actions CI workflow matrix contract validator.
# Regex-asserts `.github/workflows/ci.yml` carries the multi-runtime
# matrix shape (iOS-17-5 / iOS-18-4 / iOS-26-4 + fail-fast: false +
# continue-on-error guard on non-26.4 branches) on top of the 7 C2 tokens.
# Emits a 14-field JSON gate (mirror of scripts/simx-c2-ci-workflow-validate.sh
# shape; +6 fields for matrix surface, -2 fields no_matrix/single_job
# which are inherently invalidated by the C3 matrix introduction).
#
# This validator does NOT execute the 5 CI gate commands — those are
# verified in the Checkpoint C3 acceptance block separately. The intent
# here is workflow-file shape, not pipeline replay.
#
# Note: scripts/simx-c2-ci-workflow-validate.sh is intentionally left
# in place and unmodified; after the C3 ci.yml edit it will report
# no_matrix=fail / single_job=fail. That is the honest expression of
# the C2->C3 boundary transition. C3 acceptance does not call C2
# validator.
set -uo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

YML=".github/workflows/ci.yml"

has_macos_15="fail"
has_setup_bun="fail"
has_bun_install_frozen="fail"
has_typecheck="fail"
has_vitest="fail"
has_swift_test="fail"
has_mcp_smoke="fail"
has_matrix="fail"
has_runtime_17_5="fail"
has_runtime_18_4="fail"
has_runtime_26_4="fail"
has_fail_fast_false="fail"
has_continue_on_error="fail"

if [[ -f "$YML" ]]; then
  grep -qE '^[[:space:]]*runs-on: macos-15$' "$YML" && has_macos_15="ok"
  grep -qE 'oven-sh/setup-bun@v2' "$YML" && has_setup_bun="ok"
  grep -qE 'bun install --frozen-lockfile' "$YML" && has_bun_install_frozen="ok"
  grep -qE 'bun run typecheck' "$YML" && has_typecheck="ok"
  grep -qE 'bun x vitest run' "$YML" && has_vitest="ok"
  grep -qE '(^|[[:space:]])swift test([[:space:]]|$)' "$YML" && has_swift_test="ok"
  grep -qE 'simx-c1-mcp-smoke\.sh' "$YML" && has_mcp_smoke="ok"
  if grep -qE '^[[:space:]]*strategy:[[:space:]]*$' "$YML" \
       && grep -qE '^[[:space:]]*matrix:[[:space:]]*$' "$YML"; then
    has_matrix="ok"
  fi
  grep -qE 'iOS-17-5' "$YML" && has_runtime_17_5="ok"
  grep -qE 'iOS-18-4' "$YML" && has_runtime_18_4="ok"
  grep -qE 'iOS-26-4' "$YML" && has_runtime_26_4="ok"
  grep -qE 'fail-fast:[[:space:]]*false' "$YML" && has_fail_fast_false="ok"
  grep -qE "continue-on-error:.*matrix\\.runtime.*!=.*'iOS-26-4'" "$YML" \
    && has_continue_on_error="ok"
fi

EXIT_CODE=0
ALL_OK="ok"
for v in "$has_macos_15" "$has_setup_bun" "$has_bun_install_frozen" \
         "$has_typecheck" "$has_vitest" "$has_swift_test" \
         "$has_mcp_smoke" "$has_matrix" "$has_runtime_17_5" \
         "$has_runtime_18_4" "$has_runtime_26_4" "$has_fail_fast_false" \
         "$has_continue_on_error"; do
  if [[ "$v" != "ok" ]]; then ALL_OK="fail"; EXIT_CODE=1; fi
done

printf '{"exit_code":%d,"has_macos_15":"%s","has_setup_bun":"%s","has_bun_install_frozen":"%s","has_typecheck":"%s","has_vitest":"%s","has_swift_test":"%s","has_mcp_smoke":"%s","has_matrix":"%s","has_runtime_17_5":"%s","has_runtime_18_4":"%s","has_runtime_26_4":"%s","has_fail_fast_false":"%s","has_continue_on_error":"%s","all_ok":"%s"}\n' \
  "$EXIT_CODE" "$has_macos_15" "$has_setup_bun" "$has_bun_install_frozen" \
  "$has_typecheck" "$has_vitest" "$has_swift_test" "$has_mcp_smoke" \
  "$has_matrix" "$has_runtime_17_5" "$has_runtime_18_4" "$has_runtime_26_4" \
  "$has_fail_fast_false" "$has_continue_on_error" "$ALL_OK"

exit "$EXIT_CODE"
```

字面验证（S2 内 inline）：
```bash
chmod +x scripts/simx-c3-ci-matrix-validate.sh
bash scripts/simx-c3-ci-matrix-validate.sh
# 期望：exit 0 + 单行 JSON
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_macos_15 == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_setup_bun == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_bun_install_frozen == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_typecheck == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_vitest == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_swift_test == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_mcp_smoke == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_matrix == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_runtime_17_5 == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_runtime_18_4 == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_runtime_26_4 == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_fail_fast_false == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.has_continue_on_error == "ok"' > /dev/null
```

期望：每条 exit 0。

**重构**

无。

---

## Checkpoint C3 验收

```bash
set -uo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# (1) workflow file 字面在场 + 14 token 字面通
test -f .github/workflows/ci.yml
test "$(wc -l < .github/workflows/ci.yml)" -le 100
bash scripts/simx-c3-ci-matrix-validate.sh > /tmp/c3-validate.json
test "$(jq -r '.all_ok' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_macos_15' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_setup_bun' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_bun_install_frozen' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_typecheck' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_vitest' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_swift_test' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_mcp_smoke' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_matrix' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_runtime_17_5' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_runtime_18_4' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_runtime_26_4' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_fail_fast_false' /tmp/c3-validate.json)" = 'ok'
test "$(jq -r '.has_continue_on_error' /tmp/c3-validate.json)" = 'ok'

# (2) 本机 5 gate 命令逐条 exit 0（等价 CI 离线执行 / sim 无关）
set -e
bun install --frozen-lockfile > /dev/null
bun run typecheck > /dev/null
bun x vitest run 2>&1 | grep -qE 'Tests +578 passed'
( cd swift-bridge && swift test 2>&1 ) > /tmp/c3-swift.log
grep -qE "Test Suite .All tests. passed|Executed +[0-9]+ tests" /tmp/c3-swift.log
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
set +e

# (3) 退化检查 — 578 TS / 27 MCP tool / typecheck / c1-mcp-smoke 全保
bun x vitest run 2>&1 | grep -qE 'Tests +578 passed'
bun run typecheck > /dev/null
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok" and .tools_list_count == 27' > /dev/null

# (4) 退化检查 — v0.7 C1 + C2 工程实体不动
test -f src/sim/runner-supervisor.ts
test -f src/sim/__tests__/runner-supervisor.test.ts
test -f scripts/long-run-100.ts
test -f scripts/simx-c1-longrun-100-smoke.sh
test -f scripts/simx-c2-ci-workflow-validate.sh

# (5) 退化检查 — v0.6 / 既有 smoke 不动
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f scripts/simx-c1-mcp-smoke.sh
test -f scripts/simx-c2-mcp-lifecycle-smoke.sh
test -f scripts/simx-c3-mcp-observe-smoke.sh
test -f scripts/simx-c4-mcp-interaction-smoke.sh
test -f scripts/simx-c5-mcp-compound-system-smoke.sh
test -f scripts/simx-c6-mcp-explain-screen-smoke.sh

# (6) 退化检查 — dep 字面 3 个不变
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'

# (7) 退化检查 — v1.md / roadmap.md / cold v0.7 字面 0 边界改（boundary 字面在场）
grep -qE '^\[6\] 兼容：iOS 17\.5 / 18\.4 / 26\.x 全部通过冒烟集$' docs/v1.md
grep -qE 'CI.*GitHub Actions macOS-15.*runtime 矩阵 iOS 17\.5 / 18\.4 / 26\.x' docs/v1.md
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# (8) 退化检查 — 既有 src/ swift-bridge/ examples/ 0 改动
test -f src/sim/runner-client.ts
test -f src/sim/dev-lock.ts
test -f src/sim/cell.ts
test -f src/sim/session.ts
test -f src/sim/simctl.ts
test -f src/sim/a11y-tree-source.ts
test -f swift-bridge/Package.swift
test -f swift-bridge/SimxRunner.xcodeproj/project.pbxproj
test "$(find src -name '*.test.ts' -not -path '*/node_modules/*' | wc -l | tr -d ' ')" = '25'

# (9) C2 validator 文件**字面在场不删**（C3 不删 C2 snapshot；C2 validator 跑会 fail 是预期边界转换；C3 acceptance 不依赖其 exit code）
test -f scripts/simx-c2-ci-workflow-validate.sh

# (10) 5-arg Indigo HID iOS 17/18 路径仍未实装字面延续（C3 不改 swift-bridge）
grep -qE '5-arg legacy ABI scheduled for v0\.7' swift-bridge/Sources/SimxIndigoHID/ChannelDispatcher.swift

# (11) ci.yml 行数 cap 字面
LINES="$(wc -l < .github/workflows/ci.yml)"
test "$LINES" -le 100

echo 'C3 verdict: ok'
```

期望：以上每条 exit 0 + 末行字面 `C3 verdict: ok`。

## 完成后动作

1. 归档此文件到 `docs/plan-history/v0.7-c3-hot.md`：
   ```bash
   mv docs/plan-hot.md docs/plan-history/v0.7-c3-hot.md
   ```
2. **改 `docs/v1.md` 决策日志末尾 +1 行字面**（C3 close note + cold plan 偏差延续记 + git 工作树缺席偏差延续记）：
   ```
   - 2026-05-16 v0.7 C3 close — multi-runtime CI matrix YAML 字面在场（`.github/workflows/ci.yml` ≤ 100 行 / `strategy.matrix.runtime: [iOS-17-5, iOS-18-4, iOS-26-4]` 3 token 字面 + `strategy.fail-fast: false` 字面 + 1 job spec → 3 logical job 字面 + job-level `continue-on-error: ${{ matrix.runtime != 'iOS-26-4' }}` 字面：26.4 hard / 17.5+18.4 软 fan-out / job `name: test (macos-15 / ${{ matrix.runtime }})` 动态字面 / 既有 7 step 字面 0 改）+ 本机 5 gate 命令字面逐条 exit 0（vitest 578 passed / swift test ≥102 / typecheck 0 error / c1-mcp-smoke all_ok=ok / bun install --frozen-lockfile 净）+ 新增 `scripts/simx-c3-ci-matrix-validate.sh` ~70 行 / 14 字段 JSON gate（has_macos_15 / has_setup_bun / has_bun_install_frozen / has_typecheck / has_vitest / has_swift_test / has_mcp_smoke / has_matrix / has_runtime_17_5 / has_runtime_18_4 / has_runtime_26_4 / has_fail_fast_false / has_continue_on_error / all_ok）字面全 ok；`scripts/simx-c2-ci-workflow-validate.sh` 字面**保留不删**（C2 snapshot 在场；C3 改 ci.yml 后 C2 validator `no_matrix=ok` + `single_job=ok` 翻 fail = 预期边界转换，C3 acceptance 不调用 C2 validator）；TS baseline 578 不动（C3 0 vitest case 增）/ 102 swift 不动 / 27 MCP tool 不动 / 0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/sim 6 文件 / 0 既有 src/{driver,sdk,core,cli,mcp} / 0 dep / 0 既有 27 ToolDef / 0 既有 30 scripts/simx-* 改（含 c1-mcp-smoke / c2-ci-workflow-validate / mcp-smoke.ts / long-run-100.ts / simx-c1-longrun-100-smoke.sh / simx-v0[1-6]-acceptance.sh / simx-runner-* / simx-axp-* / simx-hostid-*）/ 0 既有 25 vitest test file 改 / 0 既有 578 vitest case 改 / 0 examples 改 / 0 roadmap.md 改 / 0 v1.md 边界 改 / 0 cold v0.7 改。**cold plan §C3 字面偏差记**："runtime 矩阵：CI matrix iOS 17.5 / 18.4 / 26.x，每条都跑 smoke 全过" 在 v0.7 C3 落地的精确解读 = (a) matrix 3 branch 字面在场 + 5 sim-agnostic gate 每 branch 跑 → 全 branch 在 macos-15 上应过；(b) 但 cold plan 字面意图 = "为 iOS 17/18 HID 真路径 v0.7+ 实装做 CI fan-out 占位 + 26.4 hard gate"；(c) iOS 17/18 5-arg `IndigoHIDMessageForMouseNSEvent` ABI 路径 v0.7 C3 真路径未实装（`ChannelDispatcher.swift:38` + :59 字面 "scheduled for v0.7" 注释延续 + v0.2 C3 close 决策日志字面 "5-arg iOS 17/18 路径推 v0.7 多 runtime CI 矩阵"）→ C3 落地以 `continue-on-error: true` 字面表达 17/18 软态；当 v0.7+ 5-arg swift 路径实装时仅需删 `continue-on-error` 表达式即可硬化矩阵。**git 工作树缺席偏差延续记**：lab15-autofix 仍非 git 工作树（无 `.git` 项），GHA workflow 真跑路径仍不在 C3 范围；C3 验证 = (a) YAML 文件字面契约 14 token + (b) 5 gate 命令本机字面逐条 exit 0 = 等价 "如果用户日后 git init + 推 GitHub + 3 runtime 镜像可解析 → 26.4 branch 硬过 / 17.5 + 18.4 软过" 的可证 evidence；C7 README v1.0 发布章节字面写人工 git init 步骤 + branch protection / required status check 配置说明 + 17/18 5-arg 真路径实装窗口提示。**本机 iOS 17/18 runtime 缺席延续记**：`xcrun simctl list runtimes -j` 字面单 `com.apple.CoreSimulator.SimRuntime.iOS-26-4` + `xcodebuild -showsdks` 字面单 iOS 26.4 SDK；C3 范围内不本机装 17/18 SDK / runtime；CI 上 17/18 runtime 装载策略推 v0.7+ 5-arg HID 实装窗口决（候选：`xcrun simctl runtime add --type ios-17-5` macOS 15+ 命令 / GHA `actions/setup-xcode` action / 镜像预装 — C3 不预设）。**C4 推**：`simx doctor --json` schema 固化（cold plan §C4 字面 "把每个 check 的版本兼容性输出（结构化 schema 固化）"）；**C5 推**：Claude Code plugin manifest；**C6 推**：v1 acceptance script 聚合；**C7 推**：README / docs/ v1.0 发布 + git init / branch protection 人工说明。
   ```
3. 调 sub-agent 生成新 `plan-hot.md`（到 v0.7 C4 `simx doctor --json` schema 固化），见 CLAUDE.md §6 标准模板 — **本计划范围内不展开 C4**。
