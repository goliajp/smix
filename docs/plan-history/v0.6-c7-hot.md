# plan-hot — v0.6 C7：`ScreenDescription` / `SerializedFailure` zod schema 固化（MCP tool 与 SDK 共用 single source of truth）+ 9 个 ScreenDescription-emitting MCP tool 加 `outputSchema` JSON-Schema 字面 + round-trip 单测覆盖（不改 wire payload byte / 不引 handler runtime parse / 不落盘 .schema.json / 0 新 dep）

> [3/4 信息层] 当前唯一热计划。**C7 是 v0.6 第七个 checkpoint，也是 v0.6 第一次把"AI 看的眼睛"两个核心 wire shape 提升为 zod source-of-truth**——通过后世界变成：`ScreenDescription` / `ElementSummary` / `A11yNode` / `Rect` / `SerializedFailure` / `FailureCode` / `Role` 字面有对应 zod schema 在**新文件 `src/core/schemas.ts`**（决策 1.A——独立文件而非嵌入既有 `src/core/screen.ts` / `src/core/error.ts`——保 type-only 文件纯净 + 单点 source-of-truth；拒嵌入 screen.ts / error.ts —— 现 type alias 文件零 runtime；拒拆 `src/core/schemas/screen.ts` + `schemas/error.ts` —— 单文件 ~120-150 行无需切分），TypeScript `type ScreenDescription` 改为 `z.infer<typeof screenDescriptionSchema>` 派生（决策 1.B——zod-first source-of-truth，type 自动等价；拒手写 type 与 zod 并列两套——drift 风险；`A11yNode` 因 z.lazy infer `unknown` 需手写 recursive type 字面镜像 a11yNodeSchema，schemas.test.ts round-trip 兜底 drift 检测——决策 1.E），既有 `ScreenDescription` / `ElementSummary` / `A11yNode` / `Rect` 外部 type re-export from `src/core/index.ts` 字面 0 改动（决策 1.C——`export type { ScreenDescription } from './screen.js'` 链路保留——`screen.ts` 内部改为 `export type { ScreenDescription } from './schemas.js'`——下游 25+ 处 import 0 改动）；MCP server `tools/list` 字面把 **9 个 ScreenDescription-emitting tool 的 `outputSchema` 字段**填入（决策 2.A——SDK 1.29 `ToolSchema.outputSchema` 字面支持 line 2388 `outputSchema: z.ZodOptional<z.ZodObject<{type:literal'object',properties?,required?},$catchall<unknown>>>`——直接走官方 SDK channel；`tools/list` response 字段每条 tool 加 `outputSchema?` field 与 `inputSchema` 同形态——客户端 AI 自动可读 output shape），9 个 tool = `screen_describe`（SD 字面直出）+ `tap` / `double_tap` / `long_press` / `fill` / `swipe` / `scroll_to` / `key_press`（7 interaction，`withScreenAfter` wrap `{ok:true, screen:ScreenDescription|null, screenError?:string}` 字面）+ `find_and_tap`（同 wrap 形态）—— 9 个 tool 全部 share **2 个 outputSchema literal**（screen-direct vs wrapped——决策 2.B——拒 9 份独立 outputSchema literal —— shape 完全一样 share 字面；拒每 tool inline literal —— 共享 const 引用更紧凑）；`SerializedFailure` 不暴露到 MCP wire（决策 3.A 字面——`SerializedFailure` 当前 0 个 MCP tool 输出此 shape——`ExpectationFailure` throw 路径在 SDK 内消化 `writeFailureJson` 写盘后传给 user 测试 runner，MCP tool 全部走 `{isError:true, content:[{type:'text', text:'<msg>'}]}` 字面 plain text——C7 范围内**不**给 MCP tool 加 SerializedFailure 出参；推 v0.7+ 如果 MCP 客户端真要拿结构化 failure 再加）；runtime parse 在 handler 内**不**接（决策 4.A——`textOk(payload)` 当前 wire shape 0 改动；handler 内 0 `screenDescriptionSchema.parse(...)` runtime cost——handler 是 trusted source、driver 直出已字面满足 schema；拒 handler 内 parse —— 增 latency + 增 throw 路径——只在 SDK 消费侧 / test fixture round-trip 验证用 zod parse——决策 4.B 字面；不切 `structuredContent` wire 形态——决策 4.C 字面 SDK 1.29 line 2601 支持但 C7 范围 textOk + JSON string 字面继续；推 v0.7+）；vitest 加 **`src/core/__tests__/schemas.test.ts`** 单测文件 ~80-100 行覆盖 round-trip（5 case：ScreenDescription valid round-trip / ScreenDescription invalid reject / SerializedFailure valid round-trip / SerializedFailure invalid reject / A11yNode recursive z.lazy round-trip）+ 在 `src/mcp/__tests__/server.test.ts` 加 1 case 验 `tools/list` 9 个 tool 字面有 `outputSchema` field（决策 5.A——MCP 表面 schema-exposure 验证 + core 表面 round-trip 验证双独立——拒在 MCP 里 round-trip——分层职责）；`docs/v1.md` 决策日志 +1 行 v0.6 C7 close note。

> **承接（v0.6 C6 close 后实测 baseline，2026-05-16）**：v0.6 C6 — **27 个 tool 字面 expose**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + C5 7 复合系统 + C6 1 vlm `explain_screen`）+ 测试基线 **127 case in `src/mcp/__tests__/server.test.ts`**（本机 `grep -cE "^  it\(" src/mcp/__tests__/server.test.ts` = 127）+ `ALL_TOOLS` 字面 27 element（`src/mcp/tools.ts` line 1341-1369，本机 `grep -cE "^  [a-z][a-zA-Z]+Tool,$" src/mcp/tools.ts` = 27）+ helper `withScreenAfter` 字面在场（line 500-526）`{ok:true, screen:ScreenDescription|null, screenError?:string}` wire shape + 内部 helper `dispatchSubTool` 在场 line 893-910 + ToolContext shape 0 改动 + `ToolDef` shape 字面 `{name, description, inputSchema, inputJsonSchema, handler}`（line 32-38）— `inputJsonSchema: ToolJsonSchema = {type:'object', properties, required?, additionalProperties:boolean}`（line 25-30）+ `src/mcp/index.ts` barrel re-export 27 tool（line 11-39，含 `explainScreenTool` line 38）+ `src/mcp/server.ts` `toolToListEntry` 字面只 emit `{name, description, inputSchema}`（line 29-39 —— **0 outputSchema emit 当前**）。`docs/plan-history/v0.6-c6-hot.md` 已归档。`scripts/simx-c6-mcp-explain-screen-smoke.sh` 9 字段 JSON gate 全绿。
>
> **本机探测（C7 落地决策依据，2026-05-16）**：
> - SDK 版本 `@modelcontextprotocol/sdk@1.29.0` 字面 `package.json` line 42
> - `node_modules/@modelcontextprotocol/sdk/dist/esm/types.d.ts` `ToolSchema` 字面（line 2380-2410）含 `inputSchema: z.ZodObject<{type:literal'object', properties?, required?}, $catchall<unknown>>` + `outputSchema: z.ZodOptional<z.ZodObject<{type:literal'object', properties?, required?}, $catchall<unknown>>>` + `annotations` + `execution` + `_meta` + `icons` 字面字段 → **SDK 1.29 `outputSchema` 原生支持，wire 字面就在 `tools/list` 出参里**（决策 2.A 落地依据）
> - `node_modules/@modelcontextprotocol/sdk/dist/esm/types.d.ts` line 2601 `CallToolResultSchema` 含 `structuredContent: z.ZodOptional<z.ZodRecord<...>>` 字面字段 → 客户端可接 structuredContent 但 C7 范围内**不**改 textOk → 不走 structuredContent（决策 4.C 字面拒——wire 字面 0 改动 + handler 0 zod runtime cost；推 v0.7+ 如果 AI 客户端真要 structured payload 再切）
> - `src/core/screen.ts` 现状 70 行 type-only：`type Rect = {x,y,w,h}` / `type A11yNode = {rawType, role?, identifier?, label?, value?, text?, bounds:Rect, enabled, selected, hasFocus, visible, children:A11yNode[]}` / `type ElementSummary = {role:Role|'unknown', name?, id?, text?, bounds:Rect, enabled}` / `type ScreenDescription = {screenshot:string, elements:ElementSummary[], frontApp:string, summary:string, capturedAt:number}` + `function summarizeNode` runtime
> - `src/core/error.ts` 现状 127 行：`type FailureCode = 'ELEMENT_NOT_FOUND'|'NOT_VISIBLE'|'NOT_ENABLED'|'AMBIGUOUS'|'TIMEOUT'|'ASSERTION_FAILED'|'APP_NOT_RUNNING'|'SIMULATOR_NOT_BOOTED'|'DRIVER_ERROR'`（9 字面 enum，line 5-14）+ `type FailureInit` + `class ExpectationFailure` + `type SerializedFailure = {ok:false, code, message, selector?, suggestions, visibleElements, hint?, screenshot?}`（line 108-117）+ runtime renderer
> - `src/core/index.ts` re-export 字面：`export type { Rect, A11yNode, ElementSummary, ScreenDescription } from './screen.js'` line 20 + `export type { FailureCode, FailureInit, SerializedFailure } from './error.js'` line 23 → C7 在 `core/index.ts` 加 `export { ... 7 schema } from './schemas.js'` 一行（决策 1.D —— barrel 加 zod export 让 SDK / 测试可 import）+ 既有 type re-export 0 改动
> - 下游 ScreenDescription 消费点 25+ 处：`src/driver/types.ts` line 88 `describe(): Promise<ScreenDescription>` / `src/driver/simctl-driver.ts` line 209 / `src/cli/commands/repl.ts` line 7+323 / `src/sdk/app.ts` line 10+87 / `src/sdk/expect.ts` line 3+19+20 / `src/sdk/matchers.ts` line 5+202+207+240 / `src/cli/commands/run.ts` line 17+493 / `src/sdk/index.ts` line 14 — **type 形态 0 改动**（决策 1.B/C 字面——`type ScreenDescription = z.infer<typeof screenDescriptionSchema>` 与原 type alias 等价；25+ 处 import 0 改动）
> - 下游 SerializedFailure 消费点：`src/sdk/matchers.ts` line 12+14+185-191 `writeFailureJson(sink, pngSeq, payload)` 字面 + `src/cli/commands/run.ts` line 17+46+110 `error?: SerializedFailure | {name, message}` 字面 + `src/sdk/trace-sink.ts` line 3+150+155 `FailurePayload = SerializedFailure & {screenshot_path?: string}` 字面 — **type 形态 0 改动**
> - `src/mcp/tools.ts` `withScreenAfter` line 500-526 字面 wrap `{ok:true, screen}` 形态 + `withScreenAfter` 错误路径返 `{ok:true, screen:null, screenError}` —— 9 个 tool wire shape 字面来源
> - 9 个 ScreenDescription-emitting tool 字面名：本机 `grep -nE "withScreenAfter" src/mcp/tools.ts` 列 8 处调用点（line 555/588/623/658/695/731/789/824）= `tapTool`/`doubleTapTool`/`longPressTool`/`fillTool`/`swipeTool`/`scrollToTool`/`keyPressTool`/`findAndTapTool` + `screenDescribeTool` line 372 字面直返 SD = **共 9 个**
> - `wait_for` 返 `{node: A11yNode}` 非 SD（line 866 `return textOk({ node })`）→ 不在 9 tool list（决策 9.C 字面）
> - `flow_run` 返 `{passed, failed, stepsExecuted, lastError?, lastResult?}` 含 nested ToolCallResult 非 SD（line 971）→ 不在 9 tool list（决策 9.D 字面）
> - `screen_screenshot` 返 `{base64, byteLength}` / `screen_hierarchy` 返 `{tree: A11yNode}` / `element_inspect` 返 `{node: A11yNode}` → 不在 9 tool list（决策 9.C 字面 —— C7 只 SD 主结构 + SF；secondary A11yNode-only / Buffer-only shape 推 v0.7+）
> - `explain_screen` 返 `{description, raw_output}` 非 SD/SF → 不在 9 tool list（决策 9.B 字面）
> - `src/core/role.ts` 待 S1 真探测；本计划假设其纯 type-only union literal 集合，若有 runtime function 保留 runtime + 只搬 type
> - vitest 现状：`bun x vitest run` 全绿 baseline（C6 close 后；spot-check 本机 `src/core/__tests__/` 已有 `expectation-failure.test.ts` / `summarize-node.test.ts` 等；C7 新加 `src/core/__tests__/schemas.test.ts`）
>
> **现状盘点（C7 真生效边界）**：
> - **`src/core/schemas.ts`（新建，~120-160 行）**：7 个 zod schema export + 7 个 `z.infer` type re-export（A11yNode 因 z.lazy infer 限制手写 recursive type 字面，schemas.test.ts case 5 round-trip 兜底——决策 1.E）：`rectSchema` / `roleSchema`（z.enum from `src/core/role.ts` union literal 值集合，S1 写时真探测 role.ts 内容字面镜像）/ `elementSummarySchema`（含 `role: z.union([roleSchema, z.literal('unknown')])` 字面镜像 `ElementSummary.role: Role | 'unknown'`）/ `a11yNodeSchema`（`z.ZodType<unknown> = z.lazy(() => z.object({..., children: z.array(a11yNodeSchema)}).strict())` 字面）/ `screenDescriptionSchema` / `failureCodeSchema`（z.enum 9 字面 mirror error.ts line 5-14）/ `serializedFailureSchema`（含 `selector: z.unknown().optional()` —— 决策 6.A 字面：selector deep zod 已在 `src/mcp/tools.ts` selectorSchema 散落，C7 schemas.ts 字面不重写 —— 推 v0.7+ 跨层共享时统一；type 通过 intersection 与 `import('./selector.js').Selector` 等价）
> - **`src/core/screen.ts` 改动**：~70 行 → ~30 行 —— 删 4 个 `type` 字面（Rect/A11yNode/ElementSummary/ScreenDescription）；保留 `function summarizeNode` 字面不动；改为 `export type { Rect, A11yNode, ElementSummary, ScreenDescription } from './schemas.js'`（决策 7.A）；保留 `import type { Role } from './role.js'` 字面不动
> - **`src/core/error.ts` 改动**：保留 `class ExpectationFailure` + `function renderElement` 字面不动（runtime）；`type FailureCode` / `type SerializedFailure` 改为 `import type { ... } from './schemas.js'` + `export type` re-export（决策 7.A）；保留 `type FailureInit` 手写字面（决策 7.D —— class-only constructor input，不是 wire shape，不进 schemas.ts）
> - **`src/core/role.ts` 改动**：S1 写时 `cat src/core/role.ts` 真探测内容 —— 若纯 `export type Role = 'button' | 'link' | ...` union literal：内容改为 `export type { Role } from './schemas.js'` 字面（决策 7.C —— schemas.ts 单点 SoT）；若含 runtime function：保留 runtime + 仅改 `type Role` 字面行
> - **`src/core/index.ts` 改动**：+1 行 barrel：`export { rectSchema, roleSchema, elementSummarySchema, a11yNodeSchema, screenDescriptionSchema, failureCodeSchema, serializedFailureSchema } from './schemas.js'`（决策 1.D —— 单行 7 schema）；既有 `export type {...}` line 20/23 字面 0 改动（type re-export 链路经 schemas.ts 单点 SoT）
> - **`src/mcp/tools.ts` 改动**：~+30-50 行 / **0 既有 ToolDef handler 行为改动** / **+2 const literal `SCREEN_DESCRIPTION_OUTPUT_SCHEMA` 与 `SCREEN_WRAPPED_OUTPUT_SCHEMA`**（决策 8.A —— hand-written JSON Schema literal mirror zod；拒 `zod-to-json-schema` npm dep 引入 —— 决策 8.B 字面拒 0 新 dep；2 shape 字面 ~30 行 inline literal 字面够）/ ToolDef shape +1 optional 字段 `outputJsonSchema?: ToolJsonSchema`（决策 8.C —— 既有 27 tool 18 个不带此字段保持当前 wire；9 个新带；server.ts toolToListEntry 加 conditional emit）/ 9 个 ToolDef 字面加 `outputJsonSchema: SCREEN_DESCRIPTION_OUTPUT_SCHEMA | SCREEN_WRAPPED_OUTPUT_SCHEMA` 字段。决策 8.D —— `outputJsonSchema` 而非 `outputSchema` 命名 —— 与既有 `inputJsonSchema` 字段命名对称、避免与 zod-level `inputSchema` 混淆
> - **`src/mcp/server.ts` 改动**：~+5 行 —— `toolToListEntry`（line 29-39）加 return type `outputSchema?: ToolDef['outputJsonSchema']` + conditional emit `if (tool.outputJsonSchema !== undefined) entry.outputSchema = tool.outputJsonSchema`（决策 2.A 落地——MCP SDK 1.29 字面 line 2388 `outputSchema` field tag 名；拒 camelCase 偏离 SDK 字面）
> - **`src/core/__tests__/schemas.test.ts`（新建，~80-100 行）**：5 case 字面（详 S1 红段）
> - **`src/mcp/__tests__/server.test.ts` 改动**：+1 case `it('tools/list emits outputSchema for exactly 9 ScreenDescription-emitting tools', ...)` 字面（详 S2 红段）
> - **5 个 MCP smoke 字面 0 改动**（决策 9.A —— C7 tools_count 仍 27、has_explain_screen 等字段全保 —— C7 不引入新 tool 也不改 wire shape；outputSchema 是 tools/list response 新字段不影响既有 has_* 检查；smoke 是 e2e CI gate 不重复字段细节 —— vitest 单测 case 128 覆盖 outputSchema 字面验证）
> - **0 swift / 0 Driver interface / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 SDK matchers 行为 / 0 新 npm dep / 0 ScreenDescription wire payload 改动 / 0 既有 27 tool 行为改动 / 0 ToolContext 改 / 0 selectorSchema 改 / 0 SerializedFailure MCP 暴露 / 0 schemas/*.json 落盘 / 0 自动 zod-to-json-schema 生成 / 0 handler runtime parse / 0 structuredContent wire 切换 / 0 C8 端到端 MCP 客户端 / 0 `_meta` / `annotations` / `execution` tool 字段**
>
> **C7 真生效 = 五项核心源码（type-only 搬迁 + barrel 不计入核心）**：
> 1. **新建 `src/core/schemas.ts`**（~120-160 行 / 7 zod schema + 6 z.infer type re-export + 1 手写 A11yNode recursive type 决策 1.E）
> 2. **改 `src/mcp/tools.ts`**（~+30-50 行 + 2 const literal + ToolDef shape 扩 1 optional + 9 个 ToolDef 加 outputJsonSchema 字段；handler 体 0 改）
> 3. **改 `src/mcp/server.ts`**（~+5 行 toolToListEntry conditional emit + return type 扩 outputSchema?）
> 4. **新建 `src/core/__tests__/schemas.test.ts`**（~80-100 行 / 5 case round-trip + reject）
> 5. **改 `src/mcp/__tests__/server.test.ts`**（+1 case 验 9 tool outputSchema field）
> 6. **改 `src/core/screen.ts`**（type alias → re-export，行为 0 改）— **type-only 搬迁不计入核心源码改动数**（决策 7.A）
> 7. **改 `src/core/error.ts`**（同上 type-only 搬迁）— type-only 不计入
> 8. **改 `src/core/role.ts`**（同上）— type-only 不计入
> 9. **改 `src/core/index.ts`**（+1 barrel 行）— barrel 不计入
> 10. **改 `docs/v1.md`** 决策日志 +1 行 v0.6 C7 close note — docs 不计入
>
> **核心源码改动 = 项 1-5 共 5 条 ≤ 6 字面满足**；项 6-9 type-only 搬迁 + barrel 单行 type re-export 不计入"wire 改动"（决策 7.A/C/D 字面 —— 行为 0 改动 + 下游 25+ 处 import 0 改动 + typecheck 0 退化）；项 10 docs 不计入。
>
> **C7 全部不在范围**（拒绝路径）：
> - 实现 C8 端到端 MCP 客户端脚本（cold plan §C8 字面 + §出口验收 推 C8）
> - 改 `Driver` interface 添加 method / 改 29 method 签名（决策延续 0 改动）
> - 改 `SimctlDriver` 任何 method 实现（决策延续）
> - 改 既有 27 ToolDef handler 行为（决策延续 —— C7 只追加 `outputJsonSchema` 字段、handler 体 0 改）
> - 加 handler 内 zod runtime parse（决策 4.A 字面拒 —— handler 0 latency cost；handler 是 trusted source，driver 直出已字面满足 schema）
> - 切换 wire 形态到 `structuredContent`（决策 4.C 字面拒 —— textOk + JSON string 字面继续；推 v0.7+ 如真有 AI 客户端要 structured 再切）
> - `SerializedFailure` 暴露到 MCP wire 出参（决策 3.A 字面拒 —— 0 个现 MCP tool 输出此 shape；MCP wire 全 plain text isError；推 v0.7+ 如真要 surface 结构化 failure 再加）
> - `explain_screen` `{description, raw_output}` shape 加 outputSchema（决策 9.B 字面拒 —— 非 ScreenDescription/SerializedFailure；C7 cold plan 字面专指此 2 shape；explain_screen output 形态推 v0.7+）
> - `screen_screenshot` / `screen_hierarchy` / `element_inspect` / `wait_for` 加 outputSchema（决策 9.C 字面拒 —— 不返 ScreenDescription；后三虽含 A11yNode 但 wire 形态非 SD；C7 范围 cold plan 字面焦点 SD 主结构 + SF；secondary shape 推 v0.7+ 一起统一 fix）
> - `flow_run` 加 outputSchema（决策 9.D 字面拒 —— 返 `{passed, failed, stepsExecuted, lastError?, lastResult?}` 含 nested ToolCallResult 形态非 SD；推 v0.7+）
> - `simulator_*` / `app_*` lifecycle 7 tool 加 outputSchema（决策 9.E 字面拒 —— 返 `{udid, ok, pid?, ...}` 简单 shape；C7 范围内只 SD 主结构）
> - `pasteboard_*` / `open_url` / `permissions_grant` 4 system tool 加 outputSchema（决策 9.F 字面拒 —— 简单 shape 非 SD）
> - 落盘 `schemas/*.json` JSON Schema 文件（决策 10.A 字面拒 —— zod 是 single SoT；JSON Schema 落盘 stage 推 v1.0 polish 阶段；C7 范围 inline literal 字面够）
> - 引 `zod-to-json-schema` npm dep（决策 8.B 字面拒 —— 手写 inline literal mirror 字面 ~30 行；引 dep 增 surface area + SDK version 兼容风险；v0.6 范围 0 新 dep 字面任务约束）
> - 改 ScreenDescription / SerializedFailure / FailureCode 字段（决策 11.A-E 字面拒 —— wire 形态 byte-stable；加 / 删 / 改字段属新功能推 v0.7+ 或 cold plan 新章节）
> - 加 zod schema 版本字段 `$schema_version`（决策 13.A 字面拒 —— v0.6 wire 形态稳定无需版本字段；版本通过 server `version` field 已传；schema 版本独立字段推 v1.0+ 如真需要 migration）
> - 拆 `core/schemas.ts` 到 `core/schemas/screen.ts` + `core/schemas/error.ts`（决策 14.A 字面拒 —— 单文件 ~120-160 行无需切分；拆分推 schemas 文件超 300 行再做）
> - 改 SDK `matchers.ts` 用 zod parse 验 SerializedFailure 写盘前（决策 15.A 字面拒 —— matchers 是 trusted source；writeFailureJson 内 0 parse cost —— C6 baseline 0 改动）
> - 改 `writeFailureJson` / `appendStep` / `writeStepPng` / `writeFailurePng` 行为（决策 15.B-C 字面拒）
> - 加 SDK 测试 `expect()` 真 parse ScreenDescription with zod（决策 15.D 字面拒 —— SDK 是 trusted source；只 core/schemas.test.ts round-trip 覆盖即可）
> - 加 MCP smoke `jq -e .tools[].outputSchema` 字段验证（决策 16.A 字面拒 —— 既有 5 smoke `tools_count` / `has_*` 字段验已字面够；outputSchema 字段 vitest 单测 case 128 覆盖；smoke 是 e2e CI gate 不重复细节）
> - 改 5 个 MCP smoke（决策 16.B 字面拒 —— 0 字段改 0 改动）
> - 改 `selectorSchema` / `SELECTOR_JSON_SCHEMA`（决策 6.A 字面拒 —— selector deep schema 推 v0.7+；C7 schemas.ts `serializedFailureSchema.selector` 字段用 `z.unknown()` 占位）
> - 改 `withScreenAfter` / `dispatchSubTool` 行为（决策延续 —— helper 0 行为改动；只 outputSchema 文档化既有 wire）
> - 加 `_meta` / `annotations` / `execution` / `icons` tool 字段（决策 12.A 字面拒 —— SDK 1.29 支持但 C7 范围 cold plan 字面只 outputSchema；推 v0.7+）
> - 加 tools/list `_meta` 出参（决策 12.B 字面拒 —— 同 12.A）
> - 写 swift case / 改 swift-bridge（决策延续）
> - 改 `simx-v0[1-5]-acceptance.sh`（v0.5 / v0.4 冻结；决策延续）
> - 建 `simx-v06-acceptance.sh`（推 C8 字面 cold plan §出口验收）
> - 加 HTTP/SSE transport / 加 CLI flag（决策延续 0 flag）
> - 加 `screen_describe` runtime ScreenDescription 出参 zod parse + reject（决策 4.A/15.A 字面拒）
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / acquireCell 0 / SimctlClient 0 / SDK matchers 行为 0 / dep 0 / wire payload byte 0 改 / 既有 27 ToolDef handler 行为 0 / handler runtime parse 0 / ScreenDescription/SerializedFailure/FailureCode 字段 0 改 / C8 tool 0 / structuredContent 切 0 / SerializedFailure MCP 暴露 0 / schemas/*.json 落盘 0 / zod-to-json-schema dep 0 / selectorSchema 改 0 / withScreenAfter+dispatchSubTool 行为 0 / smoke 字段 0 改 / _meta/annotations/execution tool 字段 0 / tools.ts 既有 27 ToolDef handler 体 0**。允许改动文件范围（**核心 ≤ 6 条源码** 字面任务约束）：
> 1. **新建 `src/core/schemas.ts`**（~120-160 行）— 核心
> 2. **改 `src/mcp/tools.ts`**（~+30-50 行 + 9 ToolDef 加 outputJsonSchema 字段 + 2 const + ToolDef shape 扩 1 optional）— 核心
> 3. **改 `src/mcp/server.ts`**（~+5 行 toolToListEntry conditional emit）— 核心
> 4. **新建 `src/core/__tests__/schemas.test.ts`**（~80-100 行 / 5 case）— 核心
> 5. **改 `src/mcp/__tests__/server.test.ts`**（+1 case 验 9 tool outputSchema）— 核心
> 6. **改 `src/core/screen.ts`**（type alias → re-export；行为 0 改）— type-only 不计入核心
> 7. **改 `src/core/error.ts`**（type alias → re-export；行为 0 改）— type-only 不计入
> 8. **改 `src/core/role.ts`**（type alias → re-export 或 type-only 单行改）— type-only 不计入
> 9. **改 `src/core/index.ts`**（+1 barrel 行 schema export）— barrel 不计入
> 10. **改 `docs/v1.md`** 决策日志 +1 行 — docs 不计入
>
> **核心源码改动 = 项 1-5 共 5 条 ≤ 6 字面满足**；项 6-9 type-only re-export 搬迁不计入；项 10 docs 不计入。
>
> **不允许**：(a) 改 `src/driver/` / `src/sim/` 任何文件；(b) 改 `swift-bridge/`；(c) 改其他 CLI 文件 / `src/sdk/` 文件；(d) 改 `scripts/simx-v0[1-5]-acceptance.sh` / 5 个 mcp smoke / C6 smoke；(e) 引第 4 个 npm dep（package.json `dependencies` 仅 `citty` / `@modelcontextprotocol/sdk` / `zod`）；(f) 加 CLI flag；(g) 写 C8 tool；(h) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient`；(i) 改 ScreenDescription / SerializedFailure / FailureCode 字段；(j) 加 zod runtime parse 在 handler；(k) 切 wire 到 structuredContent；(l) 改 `selectorSchema` 或 `SELECTOR_JSON_SCHEMA`；(m) 改 `withScreenAfter` / `dispatchSubTool` 行为；(n) 加 SerializedFailure 到任何 MCP tool 出参；(o) 落盘 `schemas/*.json`；(p) 引 `zod-to-json-schema` dep；(q) 改 SDK matchers / trace-sink 行为；(r) 改既有 27 ToolDef handler 体；(s) 加 `_meta` / `annotations` / `execution` / `icons` tool 字段；(t) 加 schema 版本字段 `$schema_version`；(u) 拆 schemas.ts 到子目录；(v) 给 secondary shape（screen_screenshot/screen_hierarchy/element_inspect/wait_for/explain_screen/flow_run/lifecycle/system 18 tool）加 outputSchema。

## 目标 checkpoint

**C7**：通过后世界变成 — `bun src/cli/index.ts mcp` 字面启动 stdio MCP server expose **27 个 tool**（C1-C6 0 改动）；`tools/list` response 字面 `result.tools[]` 中 **9 个 ScreenDescription-emitting tool 字面带 `outputSchema` field**（决策 2.A），其余 18 个 tool 字面不带（决策 9.B-F）；`src/core/schemas.ts` 字面落地 7 个 zod schema 作为 single source of truth（决策 1.A）；既有 `type ScreenDescription` / `type SerializedFailure` / `type ElementSummary` / `type A11yNode` / `type Rect` / `type FailureCode` / `type Role` 字面通过 `z.infer` 派生或 type re-export（决策 1.B/7.A/7.C）；wire payload byte 0 改动（决策 4.A/11.A-E）；既有 127 case 全保绿 + 新增 **6 case**（5 core round-trip + 1 mcp tools/list outputSchema 字面）= **133 case** 测试基线（决策 5.A 落地）：

- `tools/list` → 返 27 element array，每个 element 字面 `{name, description, inputSchema, outputSchema?}` —— 9 个 SD-emitting tool 字面带 `outputSchema`、18 个 tool 字面不带（决策 2.A / 9.A-F）
- 9 个 tool 字面 outputSchema：
  - `screen_describe` → outputSchema 字面 `SCREEN_DESCRIPTION_OUTPUT_SCHEMA` (`type:'object', properties:{screenshot,elements,frontApp,summary,capturedAt}, required:['screenshot','elements','frontApp','summary','capturedAt'], additionalProperties:false`)
  - `tap` / `double_tap` / `long_press` / `fill` / `swipe` / `scroll_to` / `key_press` / `find_and_tap` → outputSchema 字面 `SCREEN_WRAPPED_OUTPUT_SCHEMA` (`type:'object', properties:{ok:{type:'boolean'}, screen:{oneOf:[SD, {type:'null'}]}, screenError:{type:'string'}}, required:['ok'], additionalProperties:false`)
- `src/core/schemas.ts` 字面 7 zod schema export（`rectSchema` / `roleSchema` / `elementSummarySchema` / `a11yNodeSchema` / `screenDescriptionSchema` / `failureCodeSchema` / `serializedFailureSchema`） + 7 type re-export
- 既有 25+ 处 `ScreenDescription` / `SerializedFailure` import 字面 0 改动（type re-export 链路保留）
- vitest 字面 **133 case** = 127 既有 + 5 core schemas.test.ts + 1 mcp outputSchema case
- `screen_describe` 调用真返 ScreenDescription wire 形态 0 改（`{screenshot, elements, frontApp, summary, capturedAt}`）—— 9 个 tool 真 wire byte-stable 0 改 + 18 个 tool 0 改
- `docs/v1.md` 决策日志 +1 行 v0.6 C7 close note
- 0 新 dep / 0 schemas/*.json 落盘 / 0 swift / 0 Driver 改 / 0 handler runtime parse / 0 structuredContent 切 / 0 SerializedFailure MCP 暴露 / 0 既有 27 handler body 改 / 0 既有 5 smoke 字段改

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.6 C6 close 痕迹存在
test -f docs/plan-history/v0.6-c6-hot.md
test -x scripts/simx-c6-mcp-explain-screen-smoke.sh

# C6 baseline 127 case 字面在场（server.test.ts）
test "$(grep -cE '^  it\(' src/mcp/__tests__/server.test.ts)" = '127'

# C6 baseline ALL_TOOLS 27 element 字面在场
test "$(grep -cE '^  [a-z][a-zA-Z]+Tool,$' src/mcp/tools.ts)" = '27'

# C6 baseline explainScreenTool 在 barrel
grep -qE '^  explainScreenTool,$' src/mcp/index.ts

# typecheck 绿
bun run typecheck > /dev/null

# MCP SDK 1.29 outputSchema 字面支持验
grep -qE 'outputSchema: z\.ZodOptional<z\.ZodObject' node_modules/@modelcontextprotocol/sdk/dist/esm/types.d.ts

# 既有 SD/SF 字面 0 zod schema 验（C7 红前提）
! test -f src/core/schemas.ts

# 9 个 SD-emitting tool 字面在场
grep -qE 'export const screenDescribeTool' src/mcp/tools.ts
grep -qE 'export const tapTool' src/mcp/tools.ts
grep -qE 'export const findAndTapTool' src/mcp/tools.ts
```

期望：以上每条 exit 0。

## 步骤（线性，无分叉；3 个 step）

### S1. 新建 `src/core/schemas.ts` 7 zod schema + 改 `src/core/screen.ts` / `error.ts` / `role.ts` / `index.ts` type re-export（行为 0 改动）+ 新建 `src/core/__tests__/schemas.test.ts` 5 round-trip case

**红（写测试）**

文件：`src/core/__tests__/schemas.test.ts`（新建）

新增 `describe('core zod schemas (v0.6 C7)', ...)` block 含 **5 case**：

1. `it('screenDescriptionSchema parses a valid fixture round-trip', ...)` — fixture `{screenshot:'<base64-bytes>', elements:[{role:'button', bounds:{x:0,y:0,w:100,h:44}, enabled:true, name:'Login'}], frontApp:'com.acme.app', summary:'Login screen visible', capturedAt:1700000000000}`；`const parsed = screenDescriptionSchema.parse(fixture)`；断 `parsed` deep eq `fixture`；断 `parsed.elements[0].role === 'button'` 字面；断 `parsed.elements[0].bounds.w === 100` 字面
2. `it('screenDescriptionSchema rejects invalid fixture (missing required fields)', ...)` — fixture `{screenshot:'x', elements:[]}`（缺 frontApp/summary/capturedAt）；`const result = screenDescriptionSchema.safeParse(fixture)`；断 `result.success === false` 字面；断 `result.error.issues.length >= 3` 字面
3. `it('serializedFailureSchema parses a valid fixture round-trip', ...)` — fixture `{ok:false, code:'ELEMENT_NOT_FOUND', message:'No element matches', suggestions:['try aria-label'], visibleElements:[{role:'button', bounds:{x:0,y:0,w:10,h:10}, enabled:true}]}`；`const parsed = serializedFailureSchema.parse(fixture)`；断 `parsed.code === 'ELEMENT_NOT_FOUND'` 字面；断 `parsed.suggestions[0] === 'try aria-label'` 字面
4. `it('serializedFailureSchema rejects invalid fixture (bad code enum)', ...)` — fixture `{ok:false, code:'BOGUS_CODE', message:'x', suggestions:[], visibleElements:[]}`；`const result = serializedFailureSchema.safeParse(fixture)`；断 `result.success === false` 字面；断 `result.error.issues.some(i => i.path.includes('code'))` 字面
5. `it('a11yNodeSchema parses recursive children via z.lazy', ...)` — fixture `{rawType:'window', bounds:{x:0,y:0,w:390,h:844}, enabled:true, selected:false, hasFocus:false, visible:true, children:[{rawType:'button', label:'OK', bounds:{x:0,y:0,w:50,h:30}, enabled:true, selected:false, hasFocus:false, visible:true, children:[]}]}`；`const parsed = a11yNodeSchema.parse(fixture)`；断 `(parsed as any).children[0].label === 'OK'` 字面；断 `(parsed as any).children[0].children.length === 0` 字面

跑 `bun x vitest run src/core/__tests__/schemas.test.ts`，应 5 个 case 字面**红**（`schemas.ts` 不存在 / `screenDescriptionSchema` undefined）；既有 127 case 全保绿。

**绿（实现）**

a) **写时真探测**：`cat src/core/role.ts` 取其 Role union literal 值列表（每个字符串值），字面填入 `roleSchema = z.enum([...])`；如 role.ts 含 runtime function（如 `roleFromXcuiType` 等），保留 runtime + 只迁 type alias。

b) 新建 `src/core/schemas.ts`（~120-160 行）：

```ts
import { z } from 'zod'

export const rectSchema = z.object({
  x: z.number(),
  y: z.number(),
  w: z.number(),
  h: z.number(),
}).strict()

// Role enum values mirror src/core/role.ts at write-time. schemas.ts is the
// single SoT; role.ts becomes a thin type re-export.
export const roleSchema = z.enum([
  // <write-time: paste Role union members verbatim from src/core/role.ts>
])

export const elementSummarySchema = z.object({
  role: z.union([roleSchema, z.literal('unknown')]),
  name: z.string().optional(),
  id: z.string().optional(),
  text: z.string().optional(),
  bounds: rectSchema,
  enabled: z.boolean(),
}).strict()

export const a11yNodeSchema: z.ZodType<unknown> = z.lazy(() =>
  z.object({
    rawType: z.string(),
    role: roleSchema.optional(),
    identifier: z.string().optional(),
    label: z.string().optional(),
    value: z.string().optional(),
    text: z.string().optional(),
    bounds: rectSchema,
    enabled: z.boolean(),
    selected: z.boolean(),
    hasFocus: z.boolean(),
    visible: z.boolean(),
    children: z.array(a11yNodeSchema),
  }).strict(),
)

export const screenDescriptionSchema = z.object({
  screenshot: z.string(),
  elements: z.array(elementSummarySchema),
  frontApp: z.string(),
  summary: z.string(),
  capturedAt: z.number(),
}).strict()

export const failureCodeSchema = z.enum([
  'ELEMENT_NOT_FOUND',
  'NOT_VISIBLE',
  'NOT_ENABLED',
  'AMBIGUOUS',
  'TIMEOUT',
  'ASSERTION_FAILED',
  'APP_NOT_RUNNING',
  'SIMULATOR_NOT_BOOTED',
  'DRIVER_ERROR',
])

// Selector is opaque at this layer (deep zod lives in src/mcp/tools.ts
// selectorSchema). Use z.unknown() so consumers can typecheck against the
// canonical Selector type from src/core/selector.ts; runtime validation of
// selector internals stays at the MCP boundary where selectorSchema handles
// it. Cross-file type compat via intersection below.
export const serializedFailureSchema = z.object({
  ok: z.literal(false),
  code: failureCodeSchema,
  message: z.string(),
  selector: z.unknown().optional(),
  suggestions: z.array(z.string()),
  visibleElements: z.array(elementSummarySchema),
  hint: z.string().optional(),
  screenshot: z.string().optional(),
}).strict()

// ----- Inferred TypeScript types (single source) -----
export type Rect = z.infer<typeof rectSchema>
export type Role = z.infer<typeof roleSchema>
export type ElementSummary = z.infer<typeof elementSummarySchema>

// A11yNode: z.lazy infers `unknown`, so we hand-mirror the recursive shape.
// schemas.test.ts case 5 round-trip catches drift between schema and type.
export type A11yNode = {
  rawType: string
  role?: Role
  identifier?: string
  label?: string
  value?: string
  text?: string
  bounds: Rect
  enabled: boolean
  selected: boolean
  hasFocus: boolean
  visible: boolean
  children: A11yNode[]
}

export type ScreenDescription = z.infer<typeof screenDescriptionSchema>
export type FailureCode = z.infer<typeof failureCodeSchema>

// Cross-file selector type compat: the wire selector is opaque (unknown)
// but consumers type-check against Selector from src/core/selector.ts.
export type SerializedFailure = Omit<z.infer<typeof serializedFailureSchema>, 'selector'> & {
  readonly selector?: import('./selector.js').Selector
}
```

c) 改 `src/core/screen.ts`：删 4 个 `type` alias（`Rect` / `A11yNode` / `ElementSummary` / `ScreenDescription`），改为 `export type { Rect, A11yNode, ElementSummary, ScreenDescription } from './schemas.js'`；保留 `function summarizeNode` + `import type { Role } from './role.js'` 字面不动。

d) 改 `src/core/error.ts`：保留 `class ExpectationFailure` + `function renderElement` + `type FailureInit`（决策 7.D handwritten）字面不动；`type FailureCode` / `type SerializedFailure` 改为 `import type { FailureCode, SerializedFailure } from './schemas.js'` + `export type { FailureCode, SerializedFailure }` re-export。

e) 改 `src/core/role.ts`：若纯 type-only union literal，全文改为 `export type { Role } from './schemas.js'`；若含 runtime function（如 `roleFromXcuiType`），保留 runtime + 仅 `type Role` 改为 import + re-export from schemas.js。

f) 改 `src/core/index.ts`：+1 行 barrel `export { rectSchema, roleSchema, elementSummarySchema, a11yNodeSchema, screenDescriptionSchema, failureCodeSchema, serializedFailureSchema } from './schemas.js'`；既有 `export type {...}` line 20/23 字面 0 改动。

跑 `bun x vitest run src/core/__tests__/schemas.test.ts`，5 个新 case 应**绿**。

跑 `bun x vitest run`，既有 127 case 全保绿（共 **132 case**）。

跑 `bun run typecheck`，应 exit 0（25+ 处 ScreenDescription/SerializedFailure import 经 type re-export 链路 0 改动 typecheck 字面通过 —— schemas.ts 是新 SoT，screen.ts/error.ts/role.ts 重定向 import）。

**重构**

无。

---

### S2. 改 `src/mcp/tools.ts` 加 2 const `SCREEN_DESCRIPTION_OUTPUT_SCHEMA` / `SCREEN_WRAPPED_OUTPUT_SCHEMA` literal + 扩 `ToolDef` shape +1 optional 字段 `outputJsonSchema?` + 9 个 ToolDef 加此字段；改 `src/mcp/server.ts` `toolToListEntry` conditional emit `outputSchema`

**红（写测试）**

文件：`src/mcp/__tests__/server.test.ts`

新增 1 case（在 `tools/list` 既有 describe block 末尾）：

128. `it('tools/list emits outputSchema for exactly 9 ScreenDescription-emitting tools', ...)` — call `tools/list`、断 `result.tools.length === 27` 字面；定义 `const expectedSdTools = ['screen_describe','tap','double_tap','long_press','fill','swipe','scroll_to','key_press','find_and_tap']`；断 `result.tools.filter(t => (t as any).outputSchema !== undefined).map(t => t.name).sort()` deep eq `[...expectedSdTools].sort()`；断 `(result.tools.find(t => t.name === 'screen_describe') as any).outputSchema.type === 'object'` 字面；断 `(result.tools.find(t => t.name === 'screen_describe') as any).outputSchema.required` 含 `'screenshot'` 字面；断 `(result.tools.find(t => t.name === 'tap') as any).outputSchema.required` deep eq `['ok']` 字面；断 `(result.tools.find(t => t.name === 'explain_screen') as any).outputSchema === undefined` 字面（决策 9.B verification）；断 `(result.tools.find(t => t.name === 'screen_screenshot') as any).outputSchema === undefined` 字面（决策 9.C verification）

跑 `bun x vitest run src/mcp/__tests__/server.test.ts`，case 128 应**红**（`outputJsonSchema` 字段不存在 / `toolToListEntry` 不 emit）。

**绿（实现）**

a) 改 `src/mcp/tools.ts` `ToolDef` type shape（line 32-38）+1 optional field：

```ts
export type ToolDef = {
  readonly name: string
  readonly description: string
  readonly inputSchema: z.ZodTypeAny
  readonly inputJsonSchema: ToolJsonSchema
  readonly outputJsonSchema?: ToolJsonSchema   // NEW (decision 8.C/8.D)
  readonly handler: (args: unknown, ctx: ToolContext) => Promise<ToolCallResult>
}
```

b) 加 2 const literal（在 `selectorSchema` / `SELECTOR_JSON_SCHEMA` 之后、`screenDescribeTool` 之前 —— line ~362）：

```ts
const SCREEN_DESCRIPTION_OUTPUT_SCHEMA: ToolJsonSchema = {
  type: 'object',
  properties: {
    screenshot: { type: 'string', description: 'base64-encoded PNG' },
    elements: {
      type: 'array',
      items: {
        type: 'object',
        properties: {
          role: { type: 'string' },
          name: { type: 'string' },
          id: { type: 'string' },
          text: { type: 'string' },
          bounds: {
            type: 'object',
            properties: {
              x: { type: 'number' },
              y: { type: 'number' },
              w: { type: 'number' },
              h: { type: 'number' },
            },
            required: ['x', 'y', 'w', 'h'],
            additionalProperties: false,
          },
          enabled: { type: 'boolean' },
        },
        required: ['role', 'bounds', 'enabled'],
        additionalProperties: false,
      },
    },
    frontApp: { type: 'string' },
    summary: { type: 'string' },
    capturedAt: { type: 'number' },
  },
  required: ['screenshot', 'elements', 'frontApp', 'summary', 'capturedAt'],
  additionalProperties: false,
}

const SCREEN_WRAPPED_OUTPUT_SCHEMA: ToolJsonSchema = {
  type: 'object',
  properties: {
    ok: { type: 'boolean' },
    screen: { oneOf: [SCREEN_DESCRIPTION_OUTPUT_SCHEMA, { type: 'null' }] },
    screenError: { type: 'string' },
  },
  required: ['ok'],
  additionalProperties: false,
}
```

c) 加 `outputJsonSchema` 字段到 9 个 ToolDef：

- `screenDescribeTool`（line 372）→ `outputJsonSchema: SCREEN_DESCRIPTION_OUTPUT_SCHEMA`
- `tapTool`（line 538）→ `outputJsonSchema: SCREEN_WRAPPED_OUTPUT_SCHEMA`
- `doubleTapTool`（line 571）→ 同上
- `longPressTool`（line 605）→ 同上
- `fillTool`（line 640）→ 同上
- `swipeTool`（line 677）→ 同上
- `scrollToTool`（line ~711）→ 同上（写时定位真实 line via `grep -n "scrollToTool"`）
- `keyPressTool`（line ~750）→ 同上
- `findAndTapTool`（line 806）→ 同上

每个 ToolDef 字面 1-2 行内联追加（决策 8.A）；handler body 0 改动。

d) 改 `src/mcp/server.ts` `toolToListEntry`（line 29-39）：

```ts
function toolToListEntry(tool: ToolDef): {
  name: string
  description: string
  inputSchema: ToolDef['inputJsonSchema']
  outputSchema?: ToolDef['outputJsonSchema']
} {
  const entry: {
    name: string
    description: string
    inputSchema: ToolDef['inputJsonSchema']
    outputSchema?: ToolDef['outputJsonSchema']
  } = {
    name: tool.name,
    description: tool.description,
    inputSchema: tool.inputJsonSchema,
  }
  if (tool.outputJsonSchema !== undefined) {
    entry.outputSchema = tool.outputJsonSchema
  }
  return entry
}
```

跑 `bun x vitest run src/mcp/__tests__/server.test.ts`，case 128 应**绿**；既有 127 case 全保绿（共 128 case）+ S1 新 5 case 合计 **133 case 全绿**。

跑 `bun run typecheck`，应 exit 0。

**重构**

无。

---

### S3. 验收闭环 — vitest 全跑 + typecheck + 6 个 MCP smoke 不退化（0 字段改）+ docs/v1.md 决策日志 +1 行

**红（写测试）**

无新 vitest case（S1 + S2 已 6 新 case 覆盖单元 + 集成；smoke 不引入字段不需要新断言）。

**绿（实现）**

a) 全跑 `bun x vitest run`，期望：133 case 全绿、exit 0。

b) `bun run typecheck`，期望 exit 0。

c) 6 个既有 MCP smoke 不改字段值，本机有 booted sim 时跑（决策 16.A-B 字面 —— outputSchema 是 tools/list response 新字段，既有 smoke 不读此字段、`has_*` / `tools_count` / `pasteboard_roundtrip` / `has_explain_screen` 等字面不变；smoke 字面 0 退化）：

```bash
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "true" and .tools_list_count == "27"' > /dev/null
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "true" and .tools_count == 27' > /dev/null
bash scripts/simx-c3-mcp-observe-smoke.sh | jq -e '.all_ok == "true" and .tools_count == 27' > /dev/null
bash scripts/simx-c4-mcp-interaction-smoke.sh | jq -e '.all_ok == "true" and .tools_count == 27' > /dev/null
bash scripts/simx-c5-mcp-compound-system-smoke.sh | jq -e '.all_ok == "true" and .tools_count == 27' > /dev/null
bash scripts/simx-c6-mcp-explain-screen-smoke.sh | jq -e '.all_ok == "true" and .tools_count == 27 and .has_explain_screen == "true"' > /dev/null
```

期望：6 个 smoke 全 exit 0。

d) 改 `docs/v1.md` 决策日志末尾 +1 行：

```
- 2026-05-16 v0.6 C7 close — ScreenDescription / SerializedFailure / A11yNode / ElementSummary / Rect / FailureCode / Role zod schema 落 src/core/schemas.ts 单点 SoT + 9 个 ScreenDescription-emitting MCP tool tools/list 加 outputSchema (SDK 1.29 原生支持 line 2388) + 6 新 vitest case (5 core round-trip + 1 mcp outputSchema) = 133 case baseline. 理由：AI 客户端可程式化读 wire shape；wire payload byte 0 改动；0 新 dep；0 handler runtime parse cost；SerializedFailure 不暴露 MCP wire 出参 (推 v0.7+ 如需要)。
```

**重构**

无。

---

## Checkpoint C7 验收

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# 1. typecheck 绿
bun run typecheck

# 2. vitest 全绿（127 + 5 schemas + 1 mcp outputSchema = 133 case）
bun x vitest run 2>&1 | tail -5 | grep -qE 'Tests +133 passed'

# 3. src/core/schemas.ts 字面在场 + 7 schema export
test -f src/core/schemas.ts
test "$(grep -cE '^export const [a-z][a-zA-Z]+Schema' src/core/schemas.ts)" = '7'

# 4. type re-export 链路通：既有 ScreenDescription / SerializedFailure / Role / FailureCode 字面通过 core/index.ts 可 import
bun -e "import type { ScreenDescription, SerializedFailure, Role, FailureCode } from './src/core/index.js'; void 0"

# 5. ToolDef shape 字面有 outputJsonSchema 字段
grep -qE 'readonly outputJsonSchema\?:' src/mcp/tools.ts

# 6. ALL_TOOLS 仍 27 element（C6 baseline 不变）
test "$(grep -cE '^  [a-z][a-zA-Z]+Tool,$' src/mcp/tools.ts)" = '27'

# 7. 9 个 ToolDef 字面带 outputJsonSchema 字段
test "$(grep -cE 'outputJsonSchema: (SCREEN_DESCRIPTION_OUTPUT_SCHEMA|SCREEN_WRAPPED_OUTPUT_SCHEMA)' src/mcp/tools.ts)" = '9'

# 8. server.ts toolToListEntry conditional emit outputSchema
grep -qE 'entry\.outputSchema = tool\.outputJsonSchema' src/mcp/server.ts

# 9. C1-C6 smoke 不退化（tools_count == 27 全保 + has_explain_screen == true）
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.tools_list_count == "27"' > /dev/null
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.tools_count == 27' > /dev/null
bash scripts/simx-c3-mcp-observe-smoke.sh | jq -e '.tools_count == 27' > /dev/null
bash scripts/simx-c4-mcp-interaction-smoke.sh | jq -e '.tools_count == 27' > /dev/null
bash scripts/simx-c5-mcp-compound-system-smoke.sh | jq -e '.tools_count == 27' > /dev/null
bash scripts/simx-c6-mcp-explain-screen-smoke.sh | jq -e '.tools_count == 27 and .has_explain_screen == "true"' > /dev/null

# 10. docs/v1.md 决策日志含 C7 close note
grep -qE '2026-05-1[67] v0\.6 C7 close' docs/v1.md
```

期望：每一条 exit 0。

## 完成后动作

1. `mv docs/plan-hot.md docs/plan-history/v0.6-c7-hot.md`
2. 已改 `docs/v1.md` 决策日志 +1 行（S3.d 已落）
3. 调 sub-agent 生成新 `plan-hot.md`（到 C8）按 CLAUDE.md §6 标准模板，附加 C8 特殊 context：v0.6 出口验收端到端 MCP 客户端脚本（`bun scripts/mcp-smoke.ts` 或等价），跑 `examples/login.test.ts` 等价流程经 MCP wire（不复用既有 vitest in-process）—— 读 `examples/` 现状 + `docs/plan-cold/v0.6.md` §C8 + §出口验收 + `src/mcp/__tests__/server.test.ts` 现 133 case baseline + `src/core/schemas.ts` 现 7 schema SoT + 9 tool outputSchema 已生效——C8 可消费 outputSchema 自动验 wire shape
