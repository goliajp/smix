# plan-hot — v0.6 C6：1 个 VLM MCP tool（`explain_screen`）接通本机 `claude` CLI 子进程（`spawn('claude', ['-p', <prompt-with-image-path>, '--tools', 'Read', '--output-format', 'text'])` + 30s timeout + claude-not-installed `isError`）+ tmp PNG 截图字节流回收

> [3/4 信息层] 当前唯一热计划。**C6 是 v0.6 第六个 checkpoint，也是 MCP 表面第一次跨进程调本机 LLM（VLM 兜底）**——通过后世界变成：`bun src/cli/index.ts mcp` 字面起 stdio MCP server 同时 expose **27 个 tool**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + C5 7 复合系统 + **C6 新加 1 explain_screen = 1**），新 1 tool 字面拼"`driver.screenshot()` → 写 tmp PNG → spawn `claude -p <prompt-with-tmp-path> --tools Read --output-format text < /dev/null` → 捕 stdout → 清 tmp → 返 `{description, raw_output}`"链路（决策 1.D——cold plan 字面"`claude -p ... --image ...`" 与"先 `claude --help` 探测 flag" 全部**字面拒**：本机探测 `claude 2.1.142` `--help` 输出 line `--file <specs...>` 仅 `file_id:relative_path` 格式 = 远端 file ID 下载、**不**接本地 PNG path；line `--image` 字面不存在；唯一可工作形态是 prompt 内 `/path/to/file.png` 字面 + `--tools "Read"` 让 claude 自己 Read 文件——本机真探测 `claude -p "What is in this image: /tmp/test-c6.png. Respond in <30 words. Use Read tool to read it." --tools "Read" --output-format text` 真返 "iOS Simulator Settings app screen showing Apple Account, ..." 字面通；决策 1.E——0 `claude --help` 启动期探测 cache：探测一次失败 = 启动 server 失败影响 26 个非 VLM tool，**字面拒**——直接每次调 spawn claude 失败时报 friendly 错——v0.6 C6 范围内 simpler；探测 cache 推 v0.7+ 如真有人提出）；`explain_screen` 字面 `{udid, question?: string, timeoutMs?: number}` wire 形态（决策 2.A——question 默认 `"Describe what's on this screen in detail. List visible UI elements, their text, and likely interaction targets."` 字面 cold plan 字面对齐，timeoutMs 默认 30000 ms 字面 cold plan）→ `driver.screenshot()` Buffer 写 tmp `os.tmpdir()/simx-explain-<udid-prefix-8>-<random8>.png`（决策 3.A——临时文件路径含 udid prefix 便于诊断 + 8 char random suffix 防并发碰撞——拒 `fs.mkdtemp` 一次性目录形态——单文件 cleanup 更简）→ spawn `claude -p <full-prompt-with-path> --tools "Read" --output-format text` + `stdin: 'ignore'`（决策 4.A——`< /dev/null` 等价；本机探测 claude 不读 stdin 时输出 `Warning: no stdin data received in 3s` 到 stderr 但 stdout 仍纯文本 → stdin:'ignore' 字面消除 3s 等待；不传 stdin 拒 `-c`/`-r`/`--input-format`——VLM call 字面 stateless）+ 30s abort signal（决策 5.A——`AbortController` + `setTimeout`，timeout 触发 `controller.abort()` → child SIGTERM → 返 isError text `'explain_screen timed out after Nms'`——决策 5.B——不延长到 60s——cold plan 字面 30s；timeoutMs 可被 args 覆盖）→ 捕 stdout 全文 trim 为 `raw_output`、再 trim 为 `description`（决策 6.A——v0.6 C6 范围内 description = raw_output 字面 1:1——拒解析 JSON / 提取首段——claude --output-format text 字面纯文本；description 字面是 `raw_output` 的 alias for AI 客户端便利——v0.7+ 如需结构化可加 description 提取逻辑）→ 返 `{content:[{type:'text', text: JSON.stringify({description, raw_output})}]}`（决策 6.B——单 content 含 stringified payload 与 C2/C3/C4/C5 26 tool 同形态）→ finally cleanup tmp PNG（决策 7.A——`fs.promises.unlink` swallow ENOENT；spawn 失败 / abort 触发 / claude exit nonzero 都走 cleanup——`try/finally` 字面）；错误路径决策表（决策 8.A 字面）：(a) `driver.acquireDriver` throw → `isError + errMsg`（C3-C5 决策延续）；(b) `driver.screenshot()` throw → `isError + 'screenshot failed: <msg>'`；(c) `which claude` / `spawn` ENOENT → `isError + 'claude CLI not found in PATH. Install Claude Code: https://docs.claude.com/en/docs/claude-code/setup'`（决策 8.B 字面——错误 text 含明确 install URL 让 AI 客户端可 surface）；(d) claude 子进程 exit code !== 0 → `isError + 'claude CLI exited with code <N>: <stderr-tail>'`（stderr tail 截 256 char——决策 8.C——避免 PII 大量泄漏 stderr 到 MCP response）；(e) abort signal fire → `isError + 'explain_screen timed out after <Nms>ms'`（决策 5.A 字面）；(f) zod 校验失败 → `isError + 'invalid arguments: <zod msg>'`（server.ts 字面已统一兜底）；(g) `fs.writeFile` tmp 失败 → `isError + 'tmp write failed: <msg>'`（决策 8.D——罕见但路径不可写场景）；驱动获取复用 C3-C5 既有 `ToolContext.acquireDriver(udid)` 通道 0 改动；子进程 spawn 与 fs ops 复用 `node:child_process` `spawn` + `node:fs/promises` + `node:os.tmpdir()` + `node:path` + `node:crypto.randomBytes`——**0 新 npm dep**（决策 9.A——`claude-cli` 不是 npm dep 是用户机外部二进制；spawn 标准库够用；`which` shell-out 拒——`spawn` ENOENT 字面捕——决策 9.B——0 额外 `which` shell-out）；C1+C2+C3+C4+C5 既有 115 case 全保不退化，新增 ~10-14 case 覆盖 explain_screen happy + claude-not-installed + timeout + claude-exit-nonzero + driver-acquire-fail + screenshot-fail + zod-fail + tmp-cleanup；`docs/v1.md` 决策日志 +1 行 v0.6 C6 close note。
>
> **承接（v0.6 C5 close 后实测 baseline，2026-05-16）**：v0.6 C5 — **26 个 tool 字面 expose**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + C5 新加 7 复合系统 `find_and_tap` / `wait_for` / `flow_run` / `open_url` / `pasteboard_set` / `pasteboard_get` / `permissions_grant`）+ 内部 helper `withScreenAfter` 仍在场（`src/mcp/tools.ts` 现 ~1161 行）+ 内部 helper `dispatchSubTool(action, args, ctx)` 在场 line 888-905（`flow_run` 用，含 recursion forbid + zod precheck + lookup-by-name from `ALL_TOOLS`）+ 7 C5 tool 各 ToolDef 字面对接 `Driver` 6 method 镜像；`permissionsGrantInput.permission` zod 12 字面 enum；`scripts/simx-c5-mcp-compound-system-smoke.sh` 14 字段 JSON gate 全绿。**测试基线 115 case in `server.test.ts`**（80 既有 C1-C4 + 35 新 C5 case，本机 `grep -cE "^  it\(" src/mcp/__tests__/server.test.ts` = 115）；`ALL_TOOLS` 字面 26 element（line 1133-1160）；`src/mcp/index.ts` barrel re-export 26 tool（line 11-38）；`src/mcp/server.ts` `opts.acquireDriver?` 注入点字面在场；C1/C2/C3/C4 smoke `tools_count`/`tools_list_count` 已同步 19→26（决策 18.A 延续）。`docs/plan-history/v0.6-c5-hot.md` 已归档。
>
> **本机探测（C6 落地决策依据，2026-05-16）**：
> - `claude --version` → `2.1.142 (Claude Code)`；`/Users/doracawl/.local/bin/claude` 在 PATH
> - `claude --help` line literal 列出 flag：`-p, --print` / `--tools <tools...>` / `--output-format <format>` (text|json|stream-json) / `--file <specs...>` (file_id:relative_path — **远端 file ID 下载格式**) / `--mcp-config` / `--system-prompt` / `--bare` / `--allowed-tools` / `--disallowed-tools` / `--input-format <format>` (text|stream-json) **等 30+ flag**——**`--image` 字面不存在**；`--file` 字面不是本地 path 形态
> - 真探测 `claude -p "What is in this image: /tmp/test-c6.png. Respond in <30 words. Use Read tool to read it." --tools "Read" --output-format text` → 真返 `"iOS Simulator Settings app screen showing Apple Account, Apple智能, General, Accessibility, ..."` 字面通 = **本地 PNG 走 prompt 内 path + `--tools "Read"` 让 claude 自己 Read 文件 = 唯一可工作形态**（决策 1.D 落地依据）
> - 真探测 `claude -p "Respond with exactly the word: PROBE_OK" --tools "" --output-format text < /dev/null` → 真返 `"PROBE_OK"` 字面通 = `--tools ""` 字面禁用所有 tool；但 explain_screen 需 Read 工具读 PNG → C6 必须 `--tools "Read"` 而非 `--tools ""`（决策 4.B——拒 `--tools ""`——image 描述需 Read；拒默认 tools——不要 claude 写代码/动文件，只要 Read 看图）
> - claude 不读 stdin 时 stderr 出 `Warning: no stdin data received in 3s, proceeding without it.` 但 stdout 仍纯文本 → spawn `stdin: 'ignore'` 字面消除 3s 等待（决策 4.A 落地依据）
> - 单次完整 image-explain 调用本机实测耗时 ~5-15s（非首次 cold path；首次冷启动可能更长）→ 30s timeout 字面 safe margin（决策 5.B 字面）
>
> **现状盘点（C6 真生效边界）**：
> - **`src/mcp/tools.ts` 既有 `ToolContext` + `ToolDef` + `ALL_TOOLS = [26 tool]`（line 1133-1160）+ `selectorSchema` + `SELECTOR_JSON_SCHEMA` + helper `textOk` / `textError` / `errMsg` + `withScreenAfter` + `dispatchSubTool`**。**C6 改 1 文件 +1 ToolDef + 1 内部 helper `spawnClaudeExplain(promptWithPath, timeoutMs)`（决策 10.A——抽 helper 因 spawn + timeout + cleanup 逻辑 ~40-60 行字面值得一个有名字的函数；纯 internal 不 export）**：复用既有 `textOk` / `textError` / `errMsg` 0 改动；1 个新 ToolDef + handler；`ALL_TOOLS` 扩为 **27 element**（决策 11.A——单文件累计；同 C5 落地形态；C7-C8 tool 数继续膨胀时再考虑拆 `tools/vlm.ts`——v0.7+ 范围）。**0 改动 ToolContext shape**（C3-C5 决策延续——`acquireDriver` + `client` 字面够用；不为 explain_screen 加 `claudePath?: string` 注入——决策 12.A——v0.6 C6 范围内 spawn `'claude'` 字面靠 PATH 即可；可注入性推 v0.7+ 如真有 testing 需要——但 vitest 端用 mock spawn factory 不走 PATH lookup，所以 ctx 0 改动够用）。
> - **`src/driver/types.ts` `Driver` interface 既有 29 method**。**C6 用 1 method**：`screenshot(): Promise<Buffer>` (line 86)。**0 改动 Driver interface**（决策 13.A 延续——C6 完全只读 Driver 现状；拒扩 interface）。`SimctlDriver.screenshot()` 真实现已 v0.2 起就绪——MCP 表面用 driver 现状直出。
> - **`src/mcp/__tests__/server.test.ts` 115 case + `makeMockDriver` 既有覆盖 16 method 默认 mock**（C5 已扩 5 method）。**C6 扩 mock 形态**：(a) `makeMockDriver` 0 改动——`screenshot` 既有；(b) **新增 `makeMockSpawn` factory**（决策 14.A——把 `spawn` 注入参数化以单测——`ToolContext` 0 改动，但 `explainScreenTool` handler 通过 module-level 可注入 `spawn` 引用——决策 14.B——拒注入 `ctx.spawn?`——增 ctx surface；拒 monkey-patch `child_process`——脆且全局污染；改为 **module-private `let spawnImpl = spawn`** + `__setSpawnImpl(fn)` test-only export（决策 14.C——形态字面同 vitest 社区常见 module-private injection；prod 路径走默认 `spawn`、test 路径在 `beforeEach` 调 `__setSpawnImpl(mockSpawn) + afterEach __resetSpawnImpl()`——决策 14.D——`__` 双下划线前缀 + JSDoc `@internal` 标记仅 test 用——拒 export 命名形态 `setSpawnImpl`——避免 public API 误用）；(c) 同理 module-private `let tmpDirImpl = os.tmpdir; let writeFileImpl = fs.writeFile; let unlinkImpl = fs.unlink`——决策 14.E——拒 mock fs 全局——只换 simx 内引用——单测真不写盘——决策 14.F；既有 115 case 全保不动（决策 15.A 延续）。
> - **`src/mcp/index.ts` barrel** 既有 26 tool + `defaultAcquireDriver` export。**C6 加 1 tool export**：`explainScreenTool`。
> - **新 e2e smoke `scripts/simx-c6-mcp-explain-screen-smoke.sh`**（~140-180 行，决策 16.A）— spawn `bun src/cli/index.ts mcp` 真子进程，喂 ~6 JSON-RPC frame：`initialize` + `initialized notif` + `tools/list`（断 length === **27**, 断 `has_explain_screen`）+ `tools/call simulator_list` 取 booted udid + `tools/call explain_screen {udid, timeoutMs:60000}`（决策 16.B——本机真调 claude；timeoutMs 60s 因首次 cold start ~10-20s；smoke 字面跳过 happy-path 当 `claude` 不在 PATH——决策 16.C——CI / 他机不一定有 claude——smoke 字面先 `command -v claude` gate，无 claude 时只跑 `not_installed_path` case：`PATH=/usr/bin:/bin bun src/cli/index.ts mcp` 临时 unset claude PATH 喂 explain_screen 期望 `isError + 'claude CLI not found'`——决策 16.D——双路径覆盖："本机有 claude 跑 happy + not-installed" / "无 claude 只跑 not-installed"）+ 8 字段 JSON gate `{exit_code, initialize_ok, tools_count, has_explain_screen, explain_happy_or_skipped, explain_not_installed_isError, stdout_pure_jsonrpc, all_ok}`。runner 不依赖（决策 16.E——`driver.screenshot()` 走 SimctlDriver simctl io screenshot 字面不需 runner——已在 v0.2 验证）。
> - **0 swift / 0 Driver interface / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 acquireSession / 0 acquireCell / 0 driver-factory.ts / 0 server.ts / 0 SDK / 0 新 prod dep / 0 新 peer dep / 0 C7-C8 tool / 0 ScreenDescription serialization schema 固化（C7 范围）/ 0 selector RegExp 支持 / 0 selectorSchema 改动 / 0 withScreenAfter 改动 / 0 dispatchSubTool 改动 / 0 既有 26 ToolDef handler 改动 / 0 `claude --help` 启动期探测 cache（决策 1.E 字面拒）/ 0 `which claude` shell-out（决策 9.B 字面拒）/ 0 自定义 VLM 抽象（CLAUDE.md §9.2 不变量字面）/ 0 多 provider（同上）**。
>
> **C6 真生效 = 五条**：
> 1. **改 `src/mcp/tools.ts`**（~+130-180 行 / +1 ToolDef + 1 helper `spawnClaudeExplain` + 3-5 module-private injectable refs + 2-3 test-only `__set*Impl` export）：1 个新 ToolDef `explainScreenTool` + zod input schema strict + JSON Schema literal + handler；helper `spawnClaudeExplain(prompt: string, timeoutMs: number): Promise<{stdout: string, exitCode: number, stderr: string, timedOut: boolean}>` 字面拼"spawn + abort + collect stdout/stderr + return"；module-private `spawnImpl` / `writeFileImpl` / `unlinkImpl` / `tmpdirImpl` / `randomBytesImpl` 默认引 stdlib + 5 个对应 `__set*Impl(fn)` + `__reset*Impl()` test-only export（决策 14.C-D）；handler 拼 `acquireDriver → screenshot → tmp write → spawnClaudeExplain → cleanup → textOk` 链路（决策 7.A try/finally）；`ALL_TOOLS` 26 → 27 element；复用既有 `textOk` / `textError` / `errMsg`
> 2. **改 `src/mcp/index.ts`**（+1 行 — 1 tool 1 行内联 export from `./tools.js`，与 C3-C5 形态一致）
> 3. **改 `src/mcp/__tests__/server.test.ts`**（~+200-280 行 / +10-14 case）：扩 import 加 `__setSpawnImpl` / `__setWriteFileImpl` / `__setUnlinkImpl` / `__setTmpdirImpl` / `__setRandomBytesImpl` 5 个 reset symbol；新增 `describe('explain_screen', ...)` block：happy (mock spawn returns code:0 stdout:'fake-vlm-output') / claude-ENOENT (mock spawn throw ENOENT) / claude-exit-nonzero (mock spawn returns code:1 stderr:'auth failed') / timeout (mock spawn never exits, abort fires after small timeoutMs in test e.g. 50ms) / driver.acquire-fail (mock acquireDriver throws) / driver.screenshot-fail (mock screenshot throws) / zod-fail udid-missing (zod required) / zod-fail timeoutMs-negative (zod positive) / cleanup-runs (unlink called even on spawn fail) / cleanup-swallows-ENOENT (unlink ENOENT not rethrown) / tools/list 26 → 27 count 同步 1 case / default-question used when args.question omitted（决策 2.A 字面验）/ args.question override flows into prompt（spy on spawn args[1] 含 question 文本）；既有 115 case 全保不动
> 4. **新建 `scripts/simx-c6-mcp-explain-screen-smoke.sh`**（~140-180 行 8 字段 JSON gate）+ `chmod +x`
> 5. **改 `scripts/simx-c1-mcp-smoke.sh` + `scripts/simx-c2-mcp-lifecycle-smoke.sh` + `scripts/simx-c3-mcp-observe-smoke.sh` + `scripts/simx-c4-mcp-interaction-smoke.sh` + `scripts/simx-c5-mcp-compound-system-smoke.sh`**（`tools_count` / `tools_list_count` 期望 26 → 27 同步，5 处 1-2 行改，决策 17.A——延续 C4/C5 决策 18.A）— 不计入源码改动数
> 6. **改 `docs/v1.md`** 决策日志 +1 行 v0.6 C6 close note — 不计入源码改动数
>
> **C6 全部不在范围**（拒绝路径）：
> - 实现 C7 `ScreenDescription` / `SerializedFailure` JSON schema 固化（cold plan §C7 字面，C6 用 driver 现状直出）
> - 实现 C8 端到端 MCP 客户端脚本（cold plan §C8 字面）
> - 改 `Driver` interface 添加 method / 改 29 method 签名（决策 13.A 字面 0 改动）
> - 改 `SimctlDriver` 任何 method 实现（决策 13.A 字面 0 改动；driver 现状已就绪）
> - 改 既有 26 ToolDef handler 任一（决策 11.B——C5 已 close 0 改动；C6 只追加）
> - 改 `withScreenAfter` / `dispatchSubTool` helper 行为（决策 11.B——既有形态字面够用；`explain_screen` 不 mutate UI、不 dispatch sub-tool）
> - 改 `Cell` / `acquireCell` / `acquireSession` 签名（决策延续 0 改动）
> - 改 `SimctlClient` method（决策延续）
> - 改 `resolveSelector` 实现（决策延续 0 改动）
> - 改 `boot.ts` / `device-resolver.ts`（决策延续 0 改动）
> - 改 `driver-factory.ts` / `defaultAcquireDriver` 形态（决策延续——udid → Driver Map 缓存够用）
> - 改 `server.ts` 注入点 / `ToolContext` shape（决策 12.A 字面 0 改动）
> - 加 `claude --help` 启动期探测 + flag cache（决策 1.E 字面拒——server boot 不依赖 claude 在 PATH——C6 26 个非 VLM tool 不该被 VLM 探测失败拖累）
> - 加 `which claude` shell-out check 在每次 explain_screen 调用前（决策 9.B 字面拒——spawn ENOENT 字面就是 not-found 信号——多一次 shell-out 浪费时间）
> - 加 `--image <path>` flag 形态（决策 1.D 字面拒——claude CLI 字面无此 flag——本机探测 2.1.142 验证）
> - 加 `--file <local-path>` 形态（决策 1.D 字面拒——`--file` 字面是远端 file_id:relative_path 下载语义不是本地 path）
> - 加 `cat png | base64 | claude -p ...` 形态（决策 1.D 字面拒——`--tools "Read"` + prompt 内 path 字面更直接；base64 stdin pipe 形态多一层 encode + 大 PNG ~1MB base64 ~1.3MB stdin pipe 字面比 fs write tmp 慢）
> - 加 description 解析 / 提取首段 / JSON 解构 raw_output（决策 6.A 字面拒——v0.6 C6 description = raw_output 字面 1:1；推 v0.7+）
> - 加 prompt template engine / handlebars / mustache（决策延续——template literal 字面够用；prompt 形态硬编码：`'Look at the screenshot at <path>. ' + question` 字面拼接）
> - 加多 VLM provider 抽象（CLAUDE.md §9.2 不变量字面拒——只走 `claude` CLI）
> - 加 OpenAI / GPT-4V / Gemini fallback（同上）
> - 加流式输出 `--output-format stream-json` 解析（决策 6.C——v0.6 C6 范围内 `text` 格式字面更简；stream-json 推 v0.7+ 如真有响应延迟敏感场景）
> - 加 image resize / compress 优化（决策延续——`screenshot` Buffer 直写 tmp；iPhone-class 1290×2796 PNG ~1MB fs write 字面 <50ms；优化推 v0.7+）
> - 加 multi-image 形态 / 多 screenshot 对比（决策延续——v0.6 C6 单 udid 单 screenshot 单 question；多图推 v0.7+）
> - 加 `explainElement(selector)` 形态聚焦单 element（决策延续——`element_inspect` + question 字面已可拼装；MCP 客户端可自己拼）
> - 加 history / conversation continuation `-c` / `-r`（决策 4.C 字面拒——VLM call 字面 stateless；每次新 conversation）
> - 加 cost / token 计数（决策延续——claude CLI 自己 print cost 到 stderr，可被 raw_output 包含 stderr tail 但 v0.6 C6 范围内 0 surface 给 AI 客户端）
> - 加 image-Read-via-MCP-resources（决策延续——v0.6 不暴露 MCP resources capability；只 tools）
> - 加 stream chunk to MCP client（决策延续——MCP `tools/call` 字面 batch response）
> - 单测真 spawn claude / 真调 simctl screenshot / 真写盘（决策 14.E-F 字面延续——必须 mock spawn + mock fs + mock acquireDriver——CI 不依赖本机 claude / dev sim / 可写 tmpdir）
> - smoke 真断 description 文本内容（决策 16.F——`describe what's on this screen` LLM 输出 nondeterministic 字面——smoke 只断 `description.length > 0` + JSON 形态正确——拒断具体单词）
> - 写 swift case / 改 swift-bridge（决策延续）
> - 改 SDK / matchers / ExpectationFailure schema（决策延续）
> - 改 `simx-v0[1-5]-acceptance.sh`（v0.5 / v0.4 冻结；决策延续）
> - 建 `simx-v06-acceptance.sh`（推 C8 字面 cold plan §出口验收）
> - 加 HTTP/SSE transport / 加 CLI flag（决策延续 0 flag）
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / acquireCell 0 / SimctlClient 0 / SDK 0 / dep 0 / device-resolver.ts 0 / resolveSelector 0 / driver-factory.ts 0 / server.ts 0 / ToolContext 0 / selectorSchema 0 / withScreenAfter 0 / dispatchSubTool 0 / 既有 26 ToolDef handler 0 / C7-C8 tool 0 / ScreenDescription schema 固化 0 / `claude --help` 启动期探测 0 / 多 provider VLM 0**。允许改动文件范围（**≤ 6 条源码** 字面任务约束）：
> 1. **改 `src/mcp/tools.ts`**（~+130-180 行 / +1 ToolDef + 1 helper + 5 module-private injectable refs + 5 test-only `__set/reset*` export）
> 2. **改 `src/mcp/index.ts`**（+1 export 行 — 1 tool inline）
> 3. **改 `src/mcp/__tests__/server.test.ts`**（~+10-14 case + tools/list count 26→27 + 5 inject helper import + describe('explain_screen') block）
> 4. **新建 `scripts/simx-c6-mcp-explain-screen-smoke.sh`**（~140-180 行）+ `chmod +x`
> 5. **改 `scripts/simx-c[1-5]-mcp-*.sh`**（5 处 `tools_count`/`tools_list_count` 期望 26 → 27 同步，决策 17.A）— 不计入源码改动数
> 6. **改 `docs/v1.md`** 决策日志 +1 行 v0.6 C6 close note — 不计入源码改动数
>
> **6 条任务约束的解释**：6 条 = "源码改动文件" 6 条字面 ≤ 6 上限。实际源码改动 = 项 1-3 共 **3 条**（tools.ts / index.ts / server.test.ts）+ 项 4 smoke 新建 + 项 5 既有 5 smoke 同步 + 项 6 docs = 总 7 条（含 smoke + docs，源码 3 条）。**源码 3 ≤ 6 字面满足**；smoke + docs 与 C2-C5 形态对齐不计入源码。
>
> **不允许**：(a) 改 `src/driver/` / `src/sim/` / `src/core/` / `src/sdk/` 任何文件；(b) 改 `swift-bridge/`；(c) 改其他 CLI 文件；(d) 改 `scripts/simx-v0[1-5]-acceptance.sh`；(e) 引第 4 个 npm dep（package.json `dependencies` 仅 `citty` / `@modelcontextprotocol/sdk` / `zod`）；(f) 加 CLI flag 到 `mcpCommand`；(g) 写 C7-C8 tool 任一；(h) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient` 任何 method；(i) 加 selector RegExp 支持；(j) tool handler 调真 claude / 真 spawn / 真写盘（vitest 必须用 module-private inject `__setSpawnImpl` / `__setWriteFileImpl` / `__setUnlinkImpl` / `__setTmpdirImpl` / `__setRandomBytesImpl`）；(k) 加 SDK resources/prompts/completions/sampling capability；(l) 改 `ScreenDescription` 或 `A11yNode` JSON 形态；(m) 改 `driver-factory.ts` 或 `server.ts` 注入点；(n) 改 `ToolContext` shape；(o) 改 `selectorSchema` 或 `SELECTOR_JSON_SCHEMA`；(p) 改 `withScreenAfter` / `dispatchSubTool` helper；(q) 改 既有 26 ToolDef handler 任一；(r) 加 `claude --help` 启动期探测 cache；(s) 加 `which claude` 每调用 shell-out；(t) 加多 VLM provider 抽象；(u) 加 history / `-c` / `-r` continuation；(v) 加 image resize / compress；(w) 加流式 `stream-json` 输出。

## 目标 checkpoint

**C6**：通过后世界变成 — `bun src/cli/index.ts mcp` 字面启动 stdio MCP server 同时 expose **27 个 tool**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + C5 7 复合系统 + **C6 新加 1 explain_screen = 1**），新 1 tool 真接本机 `claude` CLI 子进程 + tmp PNG 截图字节流回收：

- `tools/list` → 返 **27 element array**，名字字面：`ping` / `simulator_list` / `simulator_boot` / `simulator_shutdown` / `app_launch` / `app_terminate` / `app_install` / `app_uninstall` / `screen_describe` / `screen_screenshot` / `screen_hierarchy` / `element_inspect` / `tap` / `double_tap` / `long_press` / `fill` / `swipe` / `scroll_to` / `key_press` / `find_and_tap` / `wait_for` / `flow_run` / `open_url` / `pasteboard_set` / `pasteboard_get` / `permissions_grant` / **`explain_screen`**
- `tools/call name=explain_screen arguments={udid:"..."}`（默认 question + 默认 30000ms timeout）→ 调 `driver.screenshot()` → 写 tmp `<tmpdir>/simx-explain-<udid8>-<r8>.png` → spawn `claude -p "Look at the screenshot at <path>. Describe what's on this screen in detail. List visible UI elements, their text, and likely interaction targets." --tools "Read" --output-format text` + `stdin:'ignore'` → 等 child exit (≤30s) → 捕 stdout trim 为 `raw_output` → 清 tmp → 返 `{content:[{type:'text', text:'{"description":"<trim>", "raw_output":"<trim>"}'}]}`（决策 6.A/6.B 字面）
- `tools/call name=explain_screen arguments={udid:"...", question:"Is there a login button visible?"}` → spawn claude prompt 内含 `'Is there a login button visible?'` 字面（spy on spawn args 验）→ 同上链路返
- `tools/call name=explain_screen arguments={udid:"...", timeoutMs:5000}` → AbortController 5s 触发；若 mock spawn 永不退出 → abort 触发 SIGTERM → 返 `{isError:true, content:[{type:'text', text:'explain_screen timed out after 5000ms'}]}`（决策 5.A 字面）
- claude CLI 不在 PATH（mock spawn throw `Error & {code:'ENOENT'}`）→ 返 `{isError:true, content:[{type:'text', text:'claude CLI not found in PATH. Install Claude Code: https://docs.claude.com/en/docs/claude-code/setup'}]}`（决策 8.B 字面 — text 含明确 install URL）
- claude 子进程 exit code !== 0（mock spawn returns code:1 stderr:'auth failed: not logged in'）→ 返 `{isError:true, content:[{type:'text', text:'claude CLI exited with code 1: auth failed: not logged in'}]}`（决策 8.C 字面 — stderr tail 截 256 char）
- `tools/call name=explain_screen arguments={}`（缺 udid）→ `{isError:true, content:[{type:'text', text:'invalid arguments: ...'}]}`（zod required）
- `tools/call name=explain_screen arguments={udid:"...", timeoutMs:-5}` → zod positive 拒 → `{isError:true, ...}`
- `driver.acquireDriver` throw → `{isError:true, content:[{type:'text', text:'<msg>'}]}`（决策 8.A）
- `driver.screenshot()` throw → `{isError:true, content:[{type:'text', text:'screenshot failed: <msg>'}]}`（决策 8.A）
- spawn 失败但 tmp 已写 → finally block `unlink` 执行（spy on unlinkImpl 调用次数 === 1，决策 7.A）
- spawn 成功但 unlink throw ENOENT（tmp 被外部清掉）→ 不抛 → 仍返 happy path 结果（决策 7.A swallow）
- 任一 tool 主操作失败（zod / driver / spawn / abort / exit nonzero）→ `{isError:true, content:[{type:'text', text:'<error>'}]}`（决策延续）

`scripts/simx-c6-mcp-explain-screen-smoke.sh` 8 字段 JSON CI-schema gate 全绿 + `docs/v1.md` 决策日志 +1 行 + 测试基线 **115 → ~125-129 case（`server.test.ts`）** + 0 swift 改动 + 27 字段 v0.5 acceptance / 21 字段 v0.4 acceptance / 11 字段 C1 smoke 全保（tools_list_count 27 同步）/ 10 字段 C2 smoke 全保（tools_count 27 同步）/ 12 字段 C3 smoke 全保（tools_count 27 同步）/ 13 字段 C4 smoke 全保（tools_count 27 同步）/ 14 字段 C5 smoke 全保（tools_count 27 同步）/ 8 字段 C6 smoke 本机 dev sim e2e 全绿（happy path 跑或 skip 依本机 claude 在 PATH；not-installed isError 必跑）。

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.6 C5 close 痕迹存在
test -f docs/plan-history/v0.6-c5-hot.md
test -x scripts/simx-c5-mcp-compound-system-smoke.sh

# C5 baseline 115 case 字面在场（server.test.ts）
test "$(grep -cE '^  it\(' src/mcp/__tests__/server.test.ts)" = '115'

# C5 baseline ALL_TOOLS 26 element 字面在场
grep -cE '^  [a-z][a-zA-Z]+Tool,$' src/mcp/tools.ts | grep -qE '^26$'

# typecheck 绿
bun run typecheck > /dev/null

# 本机 claude CLI 真在（决策 1.D 落地依据；C6 实现可在无 claude 机上写代码，但 smoke happy path 需要）
which claude && claude --version

# 本机有 booted simulator（smoke happy path 需要）
xcrun simctl list devices booted | grep -qE 'Booted'
```

期望：以上每条 exit 0。

## 步骤（线性，无分叉；3 个 step）

### S1. `explainScreenTool` ToolDef + `spawnClaudeExplain` helper + 5 module-private injectable refs + 5 test-only `__set/reset*` export

**红（写测试）**

文件：`src/mcp/__tests__/server.test.ts`

新增 `describe('explain_screen', ...)` block 含 **10 case**：

1. `it('lists explain_screen with correct schema in tools/list', ...)` — call `tools/list`、断 result.tools.length === 27、断 result.tools.find(t => t.name === 'explain_screen').inputSchema 含 `required: ['udid']` 字面 + properties 含 `udid, question, timeoutMs`
2. `it('happy path: drives spawn claude with prompt containing tmp path and Read tool, returns description=raw_output', ...)` — `__setSpawnImpl(mockSpawn)` 字面 mock spawn 返 `{stdout:'fake-vlm-output\n', stderr:'', code:0}`；`__setWriteFileImpl(mockWrite)` 字面 mock fs.writeFile resolve；`__setUnlinkImpl(mockUnlink)` 字面 mock unlink resolve；call `explain_screen {udid:'A'}`；断 result.isError !== true、JSON.parse(result.content[0].text) = `{description:'fake-vlm-output', raw_output:'fake-vlm-output'}` 字面；断 mockSpawn 调 1 次、第一参 === `'claude'`、第二参（args array）含 `'-p'` + 某项含 `/simx-explain-` 字面（tmp path inject 验）+ 含 `'--tools'` + `'Read'` + `'--output-format'` + `'text'`（决策 4.B 字面）；断 mockUnlink 调 1 次
3. `it('custom question is interpolated into spawn prompt', ...)` — call `explain_screen {udid:'A', question:'Is there a login button visible?'}`；断 mockSpawn args 第二参含 `'Is there a login button visible?'` 字面 substring
4. `it('default question used when args.question omitted', ...)` — call `explain_screen {udid:'A'}`；断 mockSpawn args 第二参含 `'Describe what'` 字面 substring（决策 2.A 默认 question 字面）
5. `it('claude not found returns isError with install URL', ...)` — `__setSpawnImpl` 字面 mock spawn throw `Object.assign(new Error('spawn claude ENOENT'), {code:'ENOENT'})`；call `explain_screen {udid:'A'}`；断 result.isError === true、result.content[0].text === `'claude CLI not found in PATH. Install Claude Code: https://docs.claude.com/en/docs/claude-code/setup'` 字面（决策 8.B）；断 mockUnlink 调 1 次（tmp 已写需 cleanup——决策 7.A）
6. `it('claude exit nonzero returns isError with stderr tail', ...)` — `__setSpawnImpl` 字面 mock spawn returns `{code:1, stderr:'auth failed: not logged in\n'}`；call；断 result.isError === true、result.content[0].text === `'claude CLI exited with code 1: auth failed: not logged in'` 字面（决策 8.C，stderr trim）
7. `it('timeout fires and returns isError', ...)` — `__setSpawnImpl` 字面 mock spawn 返一个 promise 永不 resolve 但接 abort signal（mock 内 `signal.addEventListener('abort', () => resolve({code:null, stdout:'', stderr:'', timedOut:true}))`）；call `explain_screen {udid:'A', timeoutMs:50}`；断 result.isError === true、result.content[0].text === `'explain_screen timed out after 50ms'` 字面（决策 5.A）；断 mockUnlink 调 1 次
8. `it('acquireDriver failure surfaces as isError', ...)` — server with mock acquireDriver throw new Error('runner not reachable')；call `explain_screen {udid:'A'}`；断 result.isError === true、text === `'runner not reachable'`；断 mockSpawn 调 0 次（早返）、mockWrite 调 0 次、mockUnlink 调 0 次
9. `it('screenshot failure surfaces as isError', ...)` — mock driver.screenshot throws new Error('simctl io failed')；call；断 result.isError === true、text === `'screenshot failed: simctl io failed'` 字面（决策 8.A）；断 mockSpawn 调 0 次、mockUnlink 调 0 次
10. `it('zod rejects missing udid and negative timeoutMs', ...)` — call `explain_screen {}` → isError + `'invalid arguments'` substring；call `explain_screen {udid:'A', timeoutMs:-5}` → isError + `'invalid arguments'` substring

跑 `bun x vitest run src/mcp/__tests__/server.test.ts`，应 10 个新 case **红**（`explainScreenTool` 不存在）；既有 115 case 全保绿。

**绿（实现）**

文件：`src/mcp/tools.ts`

新增（在 `permissionsGrantTool` ToolDef 之后、`ALL_TOOLS` 之前）：

```ts
import { spawn as nodeSpawn } from 'node:child_process'
import { writeFile as fsWriteFile, unlink as fsUnlink } from 'node:fs/promises'
import { tmpdir as osTmpdir } from 'node:os'
import { join as pathJoin } from 'node:path'
import { randomBytes as cryptoRandomBytes } from 'node:crypto'

// Module-private injectables (test-only via __set*Impl). Prod path uses stdlib.
let spawnImpl: typeof nodeSpawn = nodeSpawn
let writeFileImpl: typeof fsWriteFile = fsWriteFile
let unlinkImpl: typeof fsUnlink = fsUnlink
let tmpdirImpl: typeof osTmpdir = osTmpdir
let randomBytesImpl: typeof cryptoRandomBytes = cryptoRandomBytes

/** @internal test-only */
export function __setSpawnImpl(fn: typeof nodeSpawn): void { spawnImpl = fn }
/** @internal test-only */
export function __resetSpawnImpl(): void { spawnImpl = nodeSpawn }
/** @internal test-only */
export function __setWriteFileImpl(fn: typeof fsWriteFile): void { writeFileImpl = fn }
/** @internal test-only */
export function __resetWriteFileImpl(): void { writeFileImpl = fsWriteFile }
/** @internal test-only */
export function __setUnlinkImpl(fn: typeof fsUnlink): void { unlinkImpl = fn }
/** @internal test-only */
export function __resetUnlinkImpl(): void { unlinkImpl = fsUnlink }
/** @internal test-only */
export function __setTmpdirImpl(fn: typeof osTmpdir): void { tmpdirImpl = fn }
/** @internal test-only */
export function __resetTmpdirImpl(): void { tmpdirImpl = osTmpdir }
/** @internal test-only */
export function __setRandomBytesImpl(fn: typeof cryptoRandomBytes): void { randomBytesImpl = fn }
/** @internal test-only */
export function __resetRandomBytesImpl(): void { randomBytesImpl = cryptoRandomBytes }

type ClaudeRunResult =
  | { kind: 'ok'; stdout: string }
  | { kind: 'not-installed' }
  | { kind: 'exit-nonzero'; code: number; stderr: string }
  | { kind: 'timed-out'; timeoutMs: number }

async function spawnClaudeExplain(prompt: string, timeoutMs: number): Promise<ClaudeRunResult> {
  return new Promise((resolve) => {
    const ac = new AbortController()
    const t = setTimeout(() => ac.abort(), timeoutMs)
    let child: ReturnType<typeof spawnImpl>
    try {
      child = spawnImpl(
        'claude',
        ['-p', prompt, '--tools', 'Read', '--output-format', 'text'],
        { stdio: ['ignore', 'pipe', 'pipe'], signal: ac.signal },
      )
    } catch (err) {
      clearTimeout(t)
      const e = err as NodeJS.ErrnoException
      if (e.code === 'ENOENT') return resolve({ kind: 'not-installed' })
      return resolve({ kind: 'exit-nonzero', code: -1, stderr: e.message })
    }
    let stdout = ''
    let stderr = ''
    let timedOut = false
    child.stdout?.on('data', (b) => { stdout += b.toString('utf8') })
    child.stderr?.on('data', (b) => { stderr += b.toString('utf8') })
    ac.signal.addEventListener('abort', () => { timedOut = true })
    child.on('error', (err) => {
      clearTimeout(t)
      const e = err as NodeJS.ErrnoException
      if (e.code === 'ENOENT') return resolve({ kind: 'not-installed' })
      if (timedOut) return resolve({ kind: 'timed-out', timeoutMs })
      resolve({ kind: 'exit-nonzero', code: -1, stderr: e.message })
    })
    child.on('close', (code) => {
      clearTimeout(t)
      if (timedOut) return resolve({ kind: 'timed-out', timeoutMs })
      if (code === 0) return resolve({ kind: 'ok', stdout })
      const tail = stderr.length > 256 ? stderr.slice(-256) : stderr
      resolve({ kind: 'exit-nonzero', code: code ?? -1, stderr: tail.trim() })
    })
  })
}

const DEFAULT_EXPLAIN_QUESTION =
  "Describe what's on this screen in detail. List visible UI elements, their text, and likely interaction targets."
const CLAUDE_INSTALL_URL = 'https://docs.claude.com/en/docs/claude-code/setup'

const explainScreenInput = z.object({
  udid: z.string(),
  question: z.string().optional(),
  timeoutMs: z.number().int().positive().optional(),
}).strict()

export const explainScreenTool: ToolDef = {
  name: 'explain_screen',
  description:
    'Use a vision-capable LLM (local `claude` CLI subprocess) to describe what is on the simulator screen. Captures a fresh screenshot, writes it to a temp PNG, then spawns `claude -p <prompt-with-path> --tools Read --output-format text` so claude can read the image. Returns {description, raw_output}. Default question asks for a list of visible UI elements; pass `question` to ask something specific. Default timeoutMs=30000. isError when claude is not installed, exits non-zero, or times out.',
  inputSchema: explainScreenInput,
  inputJsonSchema: {
    type: 'object',
    properties: {
      udid: { type: 'string' },
      question: { type: 'string' },
      timeoutMs: { type: 'number' },
    },
    required: ['udid'],
    additionalProperties: false,
  },
  handler: async (args, ctx): Promise<ToolCallResult> => {
    const parsed = args as z.infer<typeof explainScreenInput>
    const question = parsed.question ?? DEFAULT_EXPLAIN_QUESTION
    const timeoutMs = parsed.timeoutMs ?? 30_000

    let driver: Driver
    try {
      driver = await ctx.acquireDriver(parsed.udid)
    } catch (err) {
      return textError(errMsg(err))
    }

    let png: Buffer
    try {
      png = await driver.screenshot()
    } catch (err) {
      return textError(`screenshot failed: ${errMsg(err)}`)
    }

    const suffix = randomBytesImpl(4).toString('hex')
    const udidPrefix = parsed.udid.replace(/[^a-zA-Z0-9]/g, '').slice(0, 8)
    const tmpPath = pathJoin(tmpdirImpl(), `simx-explain-${udidPrefix}-${suffix}.png`)

    try {
      try {
        await writeFileImpl(tmpPath, png)
      } catch (err) {
        return textError(`tmp write failed: ${errMsg(err)}`)
      }
      const prompt = `Look at the screenshot at ${tmpPath}. ${question}`
      const result = await spawnClaudeExplain(prompt, timeoutMs)
      if (result.kind === 'not-installed') {
        return textError(`claude CLI not found in PATH. Install Claude Code: ${CLAUDE_INSTALL_URL}`)
      }
      if (result.kind === 'timed-out') {
        return textError(`explain_screen timed out after ${result.timeoutMs}ms`)
      }
      if (result.kind === 'exit-nonzero') {
        return textError(`claude CLI exited with code ${result.code}: ${result.stderr}`)
      }
      const trimmed = result.stdout.trim()
      return textOk({ description: trimmed, raw_output: trimmed })
    } finally {
      try { await unlinkImpl(tmpPath) } catch { /* swallow ENOENT */ }
    }
  },
}
```

更新 `ALL_TOOLS` 在末尾加 `explainScreenTool`（26 → 27 element）。

文件：`src/mcp/index.ts`

barrel 加 `explainScreenTool` export（line 38 之前 `permissionsGrantTool` 后）。

跑 `bun x vitest run src/mcp/__tests__/server.test.ts`，10 个新 case 应**绿**；既有 115 case 全保绿（共 125 case）。

跑 `bun run typecheck`，应 exit 0。

**重构**

无（决策 18.A——首落地不重构；helper 已抽 `spawnClaudeExplain` 独立函数，5 个 inject 已分离）。

---

### S2. 同步 5 个既有 smoke 的 `tools_count` 期望 26 → 27 + 1 case 加 `has_explain_screen` 检查

**红（写测试）**

文件：`src/mcp/__tests__/server.test.ts`

新增 1 case（`describe('tools/list count after C6', ...)` 内部）：

11. `it('tools/list returns exactly 27 tools including explain_screen', ...)` — call `tools/list`、断 result.tools.length === 27、断 result.tools.map(t => t.name) 含 `'explain_screen'`、断不含 dup 名（Set size === array.length）

跑 `bun x vitest run src/mcp/__tests__/server.test.ts`，case 11 应**绿**（S1 已实现）。

**绿（实现）**

文件：`scripts/simx-c1-mcp-smoke.sh` — 找 `tools_list_count` 或 `tools_count` 字段期望 `26` 改 `27`（1-2 行改）

文件：`scripts/simx-c2-mcp-lifecycle-smoke.sh` — 同上

文件：`scripts/simx-c3-mcp-observe-smoke.sh` — 同上

文件：`scripts/simx-c4-mcp-interaction-smoke.sh` — 同上

文件：`scripts/simx-c5-mcp-compound-system-smoke.sh` — 同上

跑 5 个 smoke 验证：本机有 booted sim 时跑：
```bash
bash scripts/simx-c1-mcp-smoke.sh
bash scripts/simx-c2-mcp-lifecycle-smoke.sh
bash scripts/simx-c3-mcp-observe-smoke.sh
bash scripts/simx-c4-mcp-interaction-smoke.sh
bash scripts/simx-c5-mcp-compound-system-smoke.sh
```
期望：5 个均 exit 0，`tools_count` JSON 字段全为 27。

**重构**

无。

---

### S3. 新建 `scripts/simx-c6-mcp-explain-screen-smoke.sh` 8 字段 JSON gate

**红（写测试）**

无新 vitest case（smoke 是 shell-level e2e；vitest 已 11 case 覆盖单元）。

**绿（实现）**

文件：`scripts/simx-c6-mcp-explain-screen-smoke.sh`（new，~150 行）+ `chmod +x`

形态字面（决策 16.A-E）：

```bash
#!/usr/bin/env bash
# C6 MCP explain_screen e2e smoke
# Verifies: server lists explain_screen, claude-not-installed isError, optional happy-path
set -euo pipefail
cd "$(dirname "$0")/.."

OUT="${TMPDIR:-/tmp}/simx-c6-smoke-$$.json"
trap 'rm -f "$OUT"' EXIT

# Detect booted sim for happy path
UDID="$(xcrun simctl list devices booted | grep -oE '\(([0-9A-F-]{36})\) \(Booted\)' | head -1 | grep -oE '[0-9A-F-]{36}' || true)"
HAS_CLAUDE="false"
if command -v claude >/dev/null 2>&1; then HAS_CLAUDE="true"; fi

# ... spawn bun src/cli/index.ts mcp + JSON-RPC frames:
#   initialize / initialized notification / tools/list
#   tools/call explain_screen {udid, timeoutMs:60000} (if HAS_CLAUDE=true and UDID nonempty)
#   tools/call explain_screen {udid, timeoutMs:60000} with PATH stripped of claude (always)
# Capture stdout, validate pure JSON-RPC (no log leak)

# Emit 8-field JSON gate:
cat > "$OUT" <<EOF
{
  "exit_code": <captured>,
  "initialize_ok": <bool>,
  "tools_count": <int, expect 27>,
  "has_explain_screen": <bool>,
  "explain_happy_or_skipped": <bool, true if HAS_CLAUDE+UDID happy OR skipped>,
  "explain_not_installed_isError": <bool>,
  "stdout_pure_jsonrpc": <bool>,
  "all_ok": <bool>
}
EOF
cat "$OUT"
jq -e '.all_ok == true' "$OUT" > /dev/null
```

`chmod +x scripts/simx-c6-mcp-explain-screen-smoke.sh`

跑 `bash scripts/simx-c6-mcp-explain-screen-smoke.sh` 本机（有 claude + booted sim）：
- 期望 stdout 末行含 `"all_ok": true`、exit 0
- happy path 真调一次 claude（~10-20s）
- not-installed path 临时 PATH=/usr/bin:/bin spawn server 验

**重构**

无。

---

## Checkpoint C6 验收

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# 1. typecheck 绿
bun run typecheck

# 2. vitest 全绿（115 → 126 case）
bun x vitest run src/mcp/__tests__/server.test.ts 2>&1 | tail -3 | grep -qE 'Tests +126 passed'

# 3. ALL_TOOLS 27 element（grep tool entry pattern in tools.ts）
test "$(grep -cE '^  [a-z][a-zA-Z]+Tool,$' src/mcp/tools.ts)" = '27'

# 4. explainScreenTool exported from barrel
grep -qE '^  explainScreenTool,$' src/mcp/index.ts

# 5. C1-C5 smoke 同步通过（tools_count==27 全保）
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.tools_list_count == 27 or .tools_count == 27' > /dev/null
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.tools_count == 27' > /dev/null
bash scripts/simx-c3-mcp-observe-smoke.sh | jq -e '.tools_count == 27' > /dev/null
bash scripts/simx-c4-mcp-interaction-smoke.sh | jq -e '.tools_count == 27' > /dev/null
bash scripts/simx-c5-mcp-compound-system-smoke.sh | jq -e '.tools_count == 27' > /dev/null

# 6. C6 新 smoke 全绿
bash scripts/simx-c6-mcp-explain-screen-smoke.sh | jq -e '.all_ok == true and .tools_count == 27 and .has_explain_screen == true and .explain_not_installed_isError == true' > /dev/null
```

期望：每一条 exit 0。最后一条 jq 同时验 4 个字段。

## 完成后动作

1. `mv docs/plan-hot.md docs/plan-history/v0.6-c6-hot.md`
2. 改 `docs/v1.md` 决策日志 +1 行：`- 2026-MM-DD v0.6 C6 close — explain_screen via claude CLI subprocess. 27 tool. 理由：MCP 表面首次跨进程 VLM；走 prompt+path+--tools Read 形态（claude CLI 无 --image flag 本机探测验证）；30s timeout / not-installed isError 全覆盖。`
3. 调 sub-agent 生成新 `plan-hot.md`（到 C7）按 CLAUDE.md §6 标准模板，附加 C7 特殊 context：ScreenDescription / SerializedFailure JSON schema 固化、MCP tool 与 SDK 共用、可能影响既有 26+1 tool 的 textOk payload shape——读 `src/core/screen.ts` 现状 + `docs/plan-cold/v0.6.md` §C7
