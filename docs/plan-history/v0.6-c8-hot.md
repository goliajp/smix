# plan-hot — v0.6 C8：MCP 客户端脚本 `scripts/mcp-smoke.ts` 跑 `examples/login-tap.test.ts` 等价流程经 MCP wire（passed_tools ≥18）+ v0.6 整体出口 `scripts/simx-v06-acceptance.sh` 35 字段聚合 + v0.6 整体 close（roadmap 🔥→✅ / v0.7 ❄️→🔥 / `docs/v1.md` 决策日志 +2 行）

> [3/4 信息层] 当前唯一热计划。**C8 是 v0.6 最后一个 checkpoint，也是 v0.6 整体出口**——通过后世界变成：(a) `scripts/mcp-smoke.ts` 字面在场 + 真 spawn `bun src/cli/index.ts mcp` stdio 子进程 + SDK `Client` + `StdioClientTransport` 真客户端连接（决策 1.A 字面 — cold plan §出口验收字面 `bun scripts/mcp-smoke.ts`；拒 B shell+jq 喂 frame —— 真客户端形态比 frame echo 更可信，能 round-trip outputSchema/structuredContent 字段；拒 C 只 wrap 既有 6 smoke —— C8 字面"端到端 MCP 客户端跑 examples/login.test.ts 等价流程"，wrapper 不是 client）+ 跑 19 个 tool 依次 call → 每 tool 真 `client.callTool({name, arguments})` → 收 `result.content[0].text` JSON parse → 字面记 `{name, ok}` 入 `passed_tools[]` / `failed_tools[]` → 末尾 `JSON.stringify({passed_tools, failed_tools, total})` 写 stdout（决策 3.A 字面 — `{passed_tools:[{name,ok}], failed_tools:[{name,error}], total}` 字面 schema 匹配 cold plan §出口验收 `jq '.passed_tools | length'` 字面）；(b) 真本机 dev sim e2e — `simulator_list` 拿 udid / `simulator_boot` no-op-if-booted / `app_launch com.apple.Preferences` 真启 Settings / `screen_describe` 真拿 ScreenDescription wire 形态（5 字面 field）/ `screen_screenshot` 真拿 base64 PNG / `screen_hierarchy` 真拿 A11yNode tree / `element_inspect {text:'General'}` 真拿 node / `tap {text:'General'}` 真触发 Settings General sub-page push（runner 起则真跳页，runner 未起则走 host-hid coord fallback —— v0.3 C6 决策延伸）/ `double_tap` / `long_press` `{text:'About'}` 真触发 / `wait_for {text:'About'}` poll 5s 真返 node / `find_and_tap {text:'About'}` 真复合 waitFor+tap / `flow_run` 串 2 step 验 dispatcher / `key_press 'return'` / `open_url 'https://example.com'` 真启 Safari / `pasteboard_set` + `pasteboard_get` 真 round-trip / `permissions_grant` 跳过（无副作用 fixture，决策 5.D 字面 —— skip 不计入 passed/failed） / `app_terminate com.apple.Preferences` 真停 / `app_install` / `app_uninstall` 跳过（无 .app fixture，决策 5.D 字面 —— skip 不计入）= **passed_tools ≥18 字面任务约束达成**（决策 4.A 字面 — 启 runner xcodebuild test 后台覆盖 interaction tools；不启则 ~13 tool 不够 18 —— B 路径被 cold plan 出口字面 ≥18 否决）；(c) `scripts/simx-v06-acceptance.sh` 字面在场 + wrap v0.5 27 字段（Phase A `bash scripts/simx-v05-acceptance.sh` extract）+ 加 8 个 v0.6 字段（Phase B `c1_mcp_protocol` / `c2_mcp_lifecycle` / `c3_mcp_observe` / `c4_mcp_interaction` / `c5_mcp_compound_system` / `c6_explain_screen` / `c7_schemas_fixation` / `c8_e2e`）= 35 字段 single-line JSON + PASS gate（决策 5.A 字面 — 27 superset 形态延 v0.5 → v0.4 → v0.3 → v0.2 一脉相承）；(d) `docs/roadmap.md` 表格 v0.6 状态字面 🔥 → ✅、v0.7 字面 ❄️ → 🔥、"当前所在位置" 段字面更新 v0.6 close marker（决策 6.A 字面 — cold plan §出口验收通过 = v0.6 整体 close）；(e) `docs/v1.md` 决策日志末尾 +2 行 — 1 行 `2026-05-16 v0.6 C8 close` + 1 行 `2026-05-16 v0.6 整体 close`（决策 6.B 字面 — C8 close note 列 35 字段 acceptance 通过 + passed_tools 数；v0.6 整体 close note 列 v1.md §2.D 字面 18 tool 完成态 27 expose / `examples/login-tap.test.ts` MCP wire 等价通 / SDK §2.A.D 完成模块标记）；(f) **plan-hot.md archive 到 `docs/plan-history/v0.6-c8-hot.md`**（决策 6.C 字面 — CLAUDE.md §6 字面完成后动作 (1)）；(g) **plan-cold/v0.7.md 在 C8 close 后由 main convo 下一轮 hot 化 sub-agent 触发生成**（决策 6.D 字面 — CLAUDE.md §6 字面"下一段对应的冷计划入口条件" v0.7 范围由后续 prompt 决定 —— C8 本计划范围**不**写 v0.7 cold plan）。

> **承接（v0.6 C7 close 后实测 baseline，2026-05-16）**：v0.6 C7 — **27 个 tool 字面 expose**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + C5 7 复合系统 + C6 1 vlm `explain_screen`）+ 测试基线 **568 TS case 全绿**（本机 `bun x vitest run` = `Tests 568 passed`；其中 `src/mcp/__tests__/server.test.ts` = 128 case / `src/core/__tests__/schemas.test.ts` = 5 case / 其他 21 file = 435 case）+ `ALL_TOOLS` 字面 27 element + `src/core/schemas.ts` 字面落地 7 zod schema + 9 个 ScreenDescription-emitting tool 字面带 `outputSchema` field（`tools/list` response 字面验证） + `src/mcp/index.ts` barrel 27 tool export + 6 个 MCP smoke 全绿（C1-C6）。`docs/plan-history/v0.6-c7-hot.md` 已归档。
>
> **本机探测（C8 落地决策依据，2026-05-16）**：
> - SDK 版本 `@modelcontextprotocol/sdk@1.29.0` 字面 `package.json` line 42 —— **`Client` + `StdioClientTransport` 字面在 SDK 内可 import**：`node_modules/@modelcontextprotocol/sdk/dist/esm/client/index.d.ts` 字面 `export declare class Client` / `node_modules/@modelcontextprotocol/sdk/dist/esm/client/stdio.d.ts` 字面 `StdioClientTransport`（决策 1.A 落地依据 — 0 新 dep；SDK 已是 prod dep 复用其 client 一面）
> - `examples/login-tap.test.ts` 现状（30 行 / 1 test case：5 步 — tap{text:'General'} / expect.toBeVisible{text:/About/} / waitFor{role:'cell',name:'About'} / tap{id:'About'} / expect.toBeVisible{text:/Version/,inside:{role:'cell'}}）→ C8 MCP wire 等价映射：`tap` / `wait_for` / `element_inspect`（toBeVisible 模拟）/ `tap` / `element_inspect` 字面 5 call → 字面对应 MCP tool 已全在 27 tool 中
> - `examples/_v03-pending/login.test.ts` 含 fill 字段（v0.7+ HID 键盘）→ C8 范围内**不**等价 fill（决策 5.B 字面 — v0.3 C6 决策延伸 fill 推 v0.7+；MCP wire `fill` tool 存在但 driver fill 尚未真实现，调用走 driver throw NotImplemented 走 isError 路径；C8 跳过 fill 不计入 passed_tools，决策 4.B 字面 — 真 e2e flow 不必覆盖每个 tool，只需 ≥18 passed）
> - 现有 6 MCP smoke 全是 raw stdin JSON-RPC frame 喂法（非 SDK Client）→ C8 字面**新形态**用 SDK Client API 真客户端连接 stdio（决策 1.A 字面拒 B 路径 raw frame —— 真客户端 round-trip 比 echo 高一阶；既有 6 smoke 不替换，C8 是补集而非替换 —— 决策 1.D 字面）
> - `simulator_list` / `simulator_boot` 已在 C2 smoke 真跑通；`screen_describe` / `screen_screenshot` / `screen_hierarchy` / `element_inspect` 已在 C3 smoke 真跑通；`tap` / `double_tap` / `long_press` / `swipe` / `scroll_to` / `key_press` / `fill` 已在 C4 smoke 单元 + e2e；`find_and_tap` / `wait_for` / `flow_run` / `open_url` / `pasteboard_*` / `permissions_grant` 已在 C5 smoke；`explain_screen` 已在 C6 smoke → C8 是**串联**这 27 tool 单脚本里 ≥18 真 call（决策 4.A 字面）
> - `bun scripts/mcp-smoke.ts` 字面 cold plan §出口验收 line 51 直引 → 文件名锁定（决策 1.C 字面 — 拒 `scripts/simx-c8-mcp-e2e-smoke.sh` shell 版 —— cold plan §出口验收 line 51 字面 `bun scripts/mcp-smoke.ts` TS 形态；C8 acceptance shell 仍是 `scripts/simx-v06-acceptance.sh` 但 e2e 子进程 spawn `bun scripts/mcp-smoke.ts`）
> - **runner 启动形态**（决策 4.A 落地）：mcp-smoke.ts 内**不**起 runner（决策 4.A.1 字面 — 启 runner 是 xcodebuild test 后台 ~60-90s 启动，violates "1 命令 e2e smoke" 形态；改 degrade tolerance —— runner 起则覆盖所有 interaction tool；runner 未起则部分 interaction tool 走 isError 但 lifecycle/observe/system 走 simctl 路径仍可 ≥14 passed）；acceptance Phase B 由 caller 决定是否本机预先起 runner —— 形态字面 mirror C4 / C5 smoke 的 `runner-unavailable-ok` degrade（决策 4.A.2 字面）
> - **plan-cold/v0.6.md §出口验收字面**（line 44-54）：`bun run test` + `bun src/cli/index.ts mcp &` + `sleep 1` + `bun scripts/mcp-smoke.ts > /tmp/mcp-out.json` + `jq '.passed_tools | length' >= 18` —— **C8 字面任务边界**；`bun src/cli/index.ts mcp &` + `kill $PID` 模式由 mcp-smoke.ts 内部 `StdioClientTransport` 字面 spawn child + `transport.close()` 替代（决策 1.E 字面 — SDK Client API 真客户端形态比 `mcp &; sleep 1` 形态更紧凑、无需 sleep race）；acceptance script Phase B C8 字段直跑 `bun scripts/mcp-smoke.ts` 拿 `passed_tools.length` 验 ≥18（决策 5.C 字面）
> - **v0.6 大版本"18 tool"vs"27 tool 实际 expose"**：v1.md §2.D 字面"18 工具"是 design 目标 floor；v0.6 C1-C6 字面累计 expose 27（C1 ping + 7+4+7+7+1 = 27），其中 ping 是协议层 sample 不计入 18 业务 tool —— 26 业务 tool 真覆盖 v1.md §2.D 18 floor + 8 superset（决策 6.B 字面 — v0.6 整体 close note 记此数差异 + 标记 v1.md §2.D 完成）；C8 mcp-smoke.ts 字面 19 tool call（决策 4.A.3 字面，详 S2 表）选择是基于"e2e flow 真需要"+"runner 依赖最大化"+"避免破坏性 tool（app_install/uninstall/simulator_shutdown/permissions_grant）"
> - **35 字段 acceptance shape**（决策 5.A 落地）：27 字段 v0.5 superset（byte-identical name）+ 8 字段 v0.6 新（顺序 c1→c8 字面，与决策日志时间序一致）= 35 字段 single-line JSON；PASS gate AND-link 35 字段全 ok
> - `docs/roadmap.md` 当前状态：v0.6 🔥 hot / v0.7 ❄️ cold → C8 close 后 v0.6 ✅ done + v0.7 🔥 hot（决策 6.A 字面）
> - `docs/v1.md` 决策日志 line 167 末尾是 C7 close note → C8 + v0.6 整体 close 在末尾 +2 行（决策 6.B 字面）
> - `docs/plan-cold/v0.6.md` line 38 字面"C5 落地修正"已记 cold plan 偏差（pasteboard 拆 2 tool）—— C8 决策日志同形态记"cold plan 字面 ≥18 vs 实际 19 call"偏差（决策 6.B 字面）
> - **scripts/simx-c8-dogfood-smoke.sh 字面在场（v0.4 C8 dogfood 命名 collision 注意）**：v0.4 时期 C8 dogfood smoke 字面 `simx-c8-dogfood-smoke.sh`；v0.6 C8 命名字面**不同名**避 collision —— `mcp-smoke.ts` + `simx-v06-acceptance.sh`（决策 1.C 字面落地 + 决策 1.I 字面 — 0 既有 c8 smoke 文件名复用）
>
> **现状盘点（C8 真生效边界）**：
> - **`scripts/mcp-smoke.ts`（新建，~150-200 行）**：top-level async IIFE / `import { Client } from '@modelcontextprotocol/sdk/client/index.js'` + `import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js'` + `import { spawnSync } from 'node:child_process'`（拿 dev sim udid via `xcrun simctl list devices -j` + `.simx/dev-sim.txt` fallback）+ `transport = new StdioClientTransport({command:'bun', args:['src/cli/index.ts', 'mcp']})` + `client.connect(transport)` 字面 SDK API 走（SDK 1.29 `Client.connect()` 内部含 initialize；不需手 init）+ `tools = await client.listTools()` 验 ≥27 + 串行 for-loop 19 字面 tool call（详 S2 表）+ 每 call `try { await client.callTool({name, arguments}) } catch (e) { failed_tools.push(...) }` + finally `transport.close()` + `console.log(JSON.stringify({passed_tools, failed_tools, total, tools_listed, runner_started, dev_sim_udid}))` + `process.exit(passed_tools.length >= 18 ? 0 : 1)`
> - **`scripts/simx-v06-acceptance.sh`（新建，~180-220 行）**：mirror `simx-v05-acceptance.sh` 形态 — Phase A wrap `simx-v05-acceptance.sh` extract 27 字段；Phase B 8 个 C1-C8 gate（C1-C6 wrap 既有 mcp smoke 验 `.all_ok`；C7 直跑 `bun x vitest run src/core/__tests__/schemas.test.ts` + grep `5 passed` —— 决策 5.E 字面 schemas SoT 验证；C8 真跑 `bun scripts/mcp-smoke.ts > /tmp/mcp-out.json` + jq `.passed_tools | length >= 18`）；Phase C 35 字段 JSON + PASS gate AND-link
> - **改 `docs/roadmap.md`**：3 处 ~3 行改 — line 15 v0.6 状态字段 🔥 hot → ✅ done；line 16 v0.7 状态字段 ❄️ cold → 🔥 hot；line 32-36 "当前所在位置" 段字面 update v0.6 close + plan-cold/v0.7.md 由下轮 sub-agent 生成（决策 6.A 字面）
> - **改 `docs/v1.md`**：决策日志末尾 +2 行 — `2026-05-16 v0.6 C8 close — ...` + `2026-05-16 v0.6 整体 close — ...`（决策 6.B 字面）
> - **plan-hot.md archive**：S3 末尾 `mv docs/plan-hot.md docs/plan-history/v0.6-c8-hot.md` 字面（决策 6.C 字面）
> - **0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 SDK matchers / 0 SDK index / 0 dep / 0 ScreenDescription wire payload 改动 / 0 既有 27 ToolDef handler body 改动 / 0 既有 6 smoke 字段改 / 0 既有 7 zod schema 改 / 0 既有 schemas.ts 改 / 0 既有 server.ts 改 / 0 既有 tools.ts 改 / 0 既有 index.ts 改 / 0 既有 examples 改 / 0 既有 v0[1-5] acceptance 改 / 0 plan-cold/v0.7.md 写 / 0 vitest case 加（C8 是 e2e smoke 非 unit）/ 0 src/ 文件改**
>
> **C8 真生效 = 五项改动（≤6 字面任务约束）**：
> 1. **新建 `scripts/mcp-smoke.ts`**（~150-200 行）— 核心 1（cold plan §出口验收字面文件名）
> 2. **新建 `scripts/simx-v06-acceptance.sh`**（~180-220 行 / 35 字段聚合）— 核心 2
> 3. **改 `docs/roadmap.md`**（3 处 ~3 行 v0.6 🔥→✅ / v0.7 ❄️→🔥 / 当前所在位置 update）— 核心 3
> 4. **改 `docs/v1.md`** 决策日志 +2 行 — 核心 4（C8 close + v0.6 整体 close）
> 5. **mv `docs/plan-hot.md` → `docs/plan-history/v0.6-c8-hot.md`**（archive）— 核心 5
>
> **核心改动 = 5 条 ≤ 6 字面满足**。
>
> **C8 全部不在范围**（拒绝路径）：
> - 写 v0.7 任何代码 / 写 plan-cold/v0.7.md（决策 6.D 字面 — v0.7 hot 化推下轮 sub-agent；C8 范围内**不**起 v0.7）
> - 改 `src/driver/` / `src/sim/` / `src/sdk/` / `src/core/` / `src/cli/` / `src/mcp/` 任何文件（决策延续 0 改动）
> - 改 `swift-bridge/` 任何文件（决策延续 0 改动）
> - 改 既有 27 ToolDef handler 行为 / 改 `src/mcp/server.ts` / 改 `src/mcp/tools.ts` / 改 `src/mcp/index.ts` / 改 `src/core/schemas.ts`（决策延续 0 改动）
> - 改 既有 6 MCP smoke 字段（决策 1.D 字面 — C8 是补集；既有 smoke 不替换）
> - 改 既有 5 v0[1-5] acceptance（v0.5 / v0.4 / v0.3 / v0.2 冻结；决策延续）
> - 加 新 npm dep（决策 1.A 字面 — SDK 已是 prod dep 复用其 Client API；0 新 dep）
> - 加 新 CLI flag（决策延续 0 flag）
> - 改 `examples/login-tap.test.ts` / 等价文件（决策 5.B 字面 — 真 examples 0 改动；mcp-smoke.ts 内嵌 hardcode 调用序列）
> - 写 v0.6 e2e fill 字段（决策 5.B 字面 — fill v0.7+；mcp-smoke.ts 跳过 fill tool，total 字段不含 fill —— 决策 4.A 字面 19 tool 表已排除 fill）
> - 改 `Driver` interface 加 method / 改 29 method 签名（决策延续 0 改动）
> - 写 unit vitest case for mcp-smoke.ts（决策 5.F 字面 — C8 是 e2e smoke 非 unit；568 TS case 不增；只 acceptance shell + smoke ts 落 e2e gate）
> - 改 `package.json` dependencies（决策 1.A 字面 — 既有 `citty` + `@modelcontextprotocol/sdk` + peer `zod` 三 dep 不变）
> - 在 mcp-smoke.ts 跑 `app_install` / `app_uninstall`（决策 5.D 字面 — 无 .app fixture；skip 不计入 passed/failed）
> - 在 mcp-smoke.ts 跑 `simulator_shutdown`（决策 5.E 字面 — 破坏性 tool 会 shutdown dev sim 影响后续；skip 不计入；shutdown handler 单元在 server.test.ts 已覆盖）
> - 在 mcp-smoke.ts 跑 `permissions_grant`（决策 5.K 字面 — 改 dev sim 权限状态副作用；skip 不计入；C5 smoke 已字面覆盖）
> - 在 mcp-smoke.ts 跑 `explain_screen`（决策 5.G 字面 — claude CLI 真调 ~120s 太慢拖长 acceptance；C6 smoke 已字面验证 happy_path；C8 跳过节流；acceptance Phase B `c6_explain_screen` 直 wrap `simx-c6-mcp-explain-screen-smoke.sh.all_ok` —— 决策 5.A 落地）
> - 在 mcp-smoke.ts 跑 `ping`（决策 5.H 字面 — sample tool 不计 e2e flow；C1 smoke 已覆盖；total 19 不含 ping）
> - 加 mcp-smoke.ts CLI flag / env 变量（决策 1.F 字面 — 0 flag 0 env；udid 从 `.simx/dev-sim.txt` 字面读 fallback first-booted；runner_started 自检 `curl /health` 200ms 不强依赖）
> - 不落盘 `/tmp/mcp-out.json`（决策 3.B 字面 — cold plan §出口验收 line 51 字面 `> /tmp/mcp-out.json`；acceptance script 字面 redirect 复制 cold plan 形态 —— 决策 5.C 字面 mirror cold plan）
> - 加 SDK Client retry / reconnect（决策 1.G 字面 — single connect 失败即 fail；acceptance gate 不容忍 connect fail）
> - 改 v1.md §2.D "18 工具" 字面（决策 6.E 字面 — design floor 数字不动；close note 在决策日志侧记"实际 27 expose / 26 业务 + 1 ping"）
> - 加 `simx-c8-mcp-e2e-smoke.sh` shell 版（决策 1.C 字面拒 — cold plan §出口验收 line 51 字面 `bun scripts/mcp-smoke.ts` TS 形态；C8 e2e gate 直 wrap mcp-smoke.ts）
> - 加 mcp-smoke.ts 内部 base64 PNG 写盘验 PNG 头 8 字节（决策 5.I 字面 — C3 smoke `screen_screenshot_base64_len_gt_1000` 已字面验；C8 只验 isError != true）
> - 加 mcp-smoke.ts 内 zod 验 outputSchema（决策 5.J 字面 — C7 已在 server.test.ts 字面验；C8 不重复；推 v0.7+ 如客户端真要 runtime 验）
> - 在 mcp-smoke.ts 内起 runner（决策 4.A.1 字面 — runner xcodebuild test 后台启动 ~60-90s 形态过重；mcp-smoke.ts 假设 caller 预先起或不起 —— degrade tolerance）
> - 改 simx-c8-dogfood-smoke.sh（v0.4 历史 smoke 不动；决策 1.I 字面）
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / SimctlClient 0 / SDK matchers 行为 0 / SDK index 0 / dep 0 / wire payload byte 0 改 / 既有 27 ToolDef handler 体 0 / 既有 server.ts 0 改 / 既有 tools.ts 0 改 / 既有 index.ts 0 改 / 既有 schemas.ts 0 改 / 既有 examples 0 改 / 既有 6 mcp smoke 0 字段改 / 既有 v0[1-5] acceptance 0 改 / vitest case 0 加 / plan-cold/v0.7.md 0 写 / v0.7 任何代码 0 / CLI flag 0 / env 变量 0 加 / package.json deps 0 改 / src/ 任何文件 0 改 / swift-bridge/ 0 改 / scripts/simx-c8-dogfood-smoke.sh 0 改**。允许改动 / 新建文件范围（**核心 ≤ 6 条** 字面任务约束）：
> 1. **新建 `scripts/mcp-smoke.ts`**（~150-200 行）— 核心
> 2. **新建 `scripts/simx-v06-acceptance.sh`**（~180-220 行）— 核心
> 3. **改 `docs/roadmap.md`**（3 处 ~3 行）— 核心
> 4. **改 `docs/v1.md`**（+2 行决策日志）— 核心
> 5. **mv `docs/plan-hot.md` → `docs/plan-history/v0.6-c8-hot.md`** — 核心（mv 算 1 条）
>
> **不允许**：(a) 改 `src/driver/` / `src/sim/` / `src/sdk/` / `src/core/` / `src/cli/` / `src/mcp/` 任何文件；(b) 改 `swift-bridge/`；(c) 改任何 `examples/*.ts`；(d) 改 `scripts/simx-v0[1-5]-acceptance.sh` / 6 个 mcp smoke / 其他 c[1-8] smoke / `simx-c8-dogfood-smoke.sh` v0.4 历史 smoke；(e) 引第 4 个 npm dep（package.json deps 仅 `citty` / `@modelcontextprotocol/sdk` / `zod`）；(f) 加 CLI flag / env 变量；(g) 写 plan-cold/v0.7.md / 起 v0.7；(h) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient`；(i) 改 ScreenDescription / SerializedFailure / FailureCode / Role / Rect / ElementSummary / A11yNode 字段；(j) 加 unit vitest case；(k) 改 `examples/login-tap.test.ts`；(l) 加 `simx-c8-mcp-e2e-smoke.sh` shell 版；(m) 改 v1.md §2.D 字面 18 工具；(n) 在 mcp-smoke.ts 跑 explain_screen / simulator_shutdown / app_install / app_uninstall / fill / ping / permissions_grant。

## 目标 checkpoint

**C8**：通过后世界变成 — `bun scripts/mcp-smoke.ts` 字面 spawn `bun src/cli/index.ts mcp` stdio MCP server 子进程经 SDK `Client` + `StdioClientTransport` 真客户端连接 + `client.listTools()` 真返 27 element + 字面 19 tool 真 call 串行 + 末尾 stdout 单行 JSON `{passed_tools:[{name,ok}], failed_tools:[{name,error}], total:19, tools_listed:27, runner_started:'yes'|'no', dev_sim_udid:'...'}` + `passed_tools.length >= 18` 字面 cold plan §出口验收达成（决策 4.A 字面 — 启 runner 时 19 全 passed；不启 runner 时 ~14-16 passed —— 取决于哪些 tool 走 driver 哪些走 simctl）；`scripts/simx-v06-acceptance.sh` 字面在场 + 35 字段单行 JSON + PASS gate AND-link 35 全 ok + exit 0；`docs/roadmap.md` 字面 v0.6 ✅ done / v0.7 🔥 hot；`docs/v1.md` 决策日志末尾 +2 行 C8 close + v0.6 整体 close；`docs/plan-hot.md` 字面 archive 到 `docs/plan-history/v0.6-c8-hot.md`；568 TS case 字面不退化（C8 不加 vitest case 0 改 src/）；102 swift case 不退化（0 改 swift-bridge/）；6 既有 MCP smoke 不退化（tools_count == 27 全保）；27 字段 v0.5 acceptance 不退化；21 字段 v0.4 acceptance 不退化：

- `bun scripts/mcp-smoke.ts` 字面 exit 0 + 输出 `.passed_tools | length >= 18` 字面
- `bash scripts/simx-v06-acceptance.sh` 字面 exit 0 + 35 字段 JSON 字面全 ok + PASS gate ok
- `docs/roadmap.md` 字面 line 15 `v0.6` 状态字符 `✅ done` + line 16 `v0.7` 状态字符 `🔥 hot`
- `docs/v1.md` 字面末尾含 `2026-05-16 v0.6 C8 close` + `2026-05-16 v0.6 整体 close` 字面 2 行
- `docs/plan-hot.md` 字面**不存在**（已 mv 到 history）
- `docs/plan-history/v0.6-c8-hot.md` 字面存在
- 0 swift / 0 src / 0 examples / 0 dep / 0 既有 smoke 字段改动 / 0 v0[1-5] acceptance 改

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.6 C7 close 痕迹存在
test -f docs/plan-history/v0.6-c7-hot.md
test -f src/core/schemas.ts
test "$(grep -cE '^export const [a-z][a-zA-Z]+Schema' src/core/schemas.ts)" = '7'

# C7 baseline ALL_TOOLS 27 element 字面在场
test "$(grep -cE '^  [a-z][a-zA-Z]+Tool,$' src/mcp/tools.ts)" = '27'

# C7 baseline 568 TS / typecheck 0 error
bun run typecheck > /dev/null
bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +568 passed'

# MCP SDK Client API 字面在场
test -f node_modules/@modelcontextprotocol/sdk/dist/esm/client/index.d.ts
test -f node_modules/@modelcontextprotocol/sdk/dist/esm/client/stdio.d.ts
grep -qE 'export declare class Client' node_modules/@modelcontextprotocol/sdk/dist/esm/client/index.d.ts
grep -qE 'StdioClientTransport' node_modules/@modelcontextprotocol/sdk/dist/esm/client/stdio.d.ts

# v0.5 acceptance 27 字段冷启在场（C8 wrap）
test -x scripts/simx-v05-acceptance.sh

# 6 个 v0.6 MCP smoke 字面在场（C8 acceptance Phase B wrap）
test -x scripts/simx-c1-mcp-smoke.sh
test -x scripts/simx-c2-mcp-lifecycle-smoke.sh
test -x scripts/simx-c3-mcp-observe-smoke.sh
test -x scripts/simx-c4-mcp-interaction-smoke.sh
test -x scripts/simx-c5-mcp-compound-system-smoke.sh
test -x scripts/simx-c6-mcp-explain-screen-smoke.sh

# C8 红前提：mcp-smoke.ts / v06-acceptance.sh 不存在
! test -f scripts/mcp-smoke.ts
! test -f scripts/simx-v06-acceptance.sh

# 有 booted dev sim（C8 真 e2e 必需）
test -f .simx/dev-sim.txt || xcrun simctl list devices -j | jq -e '[.devices[][] | select(.state=="Booted")] | length >= 1' > /dev/null

# roadmap 当前 v0.6 🔥 / v0.7 ❄️（C8 close 后翻转）
grep -qE '^\| \*\*v0\.6\*\* \| 🔥 hot \|' docs/roadmap.md
grep -qE '^\| \*\*v0\.7\*\* \| ❄️ cold \|' docs/roadmap.md
```

期望：以上每条 exit 0。

## 步骤（线性，无分叉；3 个 step）

### S1. 新建 `scripts/mcp-smoke.ts` — SDK Client + StdioClientTransport 真客户端连 stdio MCP server + 19 tool 真 call 串行 + 单行 JSON `{passed_tools, failed_tools, total, ...}` 输出

**红（写测试）**

无新 vitest case（C8 是 e2e smoke gate 非 unit；568 TS case 字面不增 —— 决策 5.F 字面）。

测试形态 = 真跑 `bun scripts/mcp-smoke.ts` 拿 stdout JSON + jq 验 `.passed_tools | length >= 18` 字面 + `.tools_listed == 27` 字面 + `.dev_sim_udid != null` 字面。

红前提 = `! test -f scripts/mcp-smoke.ts`（前置条件最后一段已验）。

**绿（实现）**

a) 新建 `scripts/mcp-smoke.ts`（~150-200 行）：

```ts
#!/usr/bin/env bun
// v0.6 C8 — MCP client e2e smoke. Spawns `bun src/cli/index.ts mcp` as a
// stdio child process, drives 19 tools through the SDK Client API, and emits
// one JSON line to stdout: {passed_tools, failed_tools, total, ...}.

import { Client } from '@modelcontextprotocol/sdk/client/index.js'
import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js'
import { spawnSync } from 'node:child_process'
import { readFileSync, existsSync } from 'node:fs'
import { join } from 'node:path'

type ToolCall = { name: string; arguments: Record<string, unknown> }
type Outcome = { name: string; ok: true } | { name: string; ok: false; error: string }

const ROOT = process.cwd()

function resolveUdid(): string {
  const devFile = join(ROOT, '.simx/dev-sim.txt')
  if (existsSync(devFile)) {
    const v = readFileSync(devFile, 'utf8').trim()
    if (v) return v
  }
  const list = spawnSync('xcrun', ['simctl', 'list', 'devices', '-j'], { encoding: 'utf8' })
  const j = JSON.parse(list.stdout || '{}')
  for (const arr of Object.values(j.devices ?? {}) as Array<Array<{ udid: string; state: string; isAvailable: boolean }>>) {
    for (const d of arr) {
      if (d.isAvailable && d.state === 'Booted') return d.udid
    }
  }
  throw new Error('no booted iOS simulator')
}

function checkRunner(): boolean {
  const r = spawnSync('curl', ['-fsS', '-m', '1', 'http://127.0.0.1:22087/health'], { encoding: 'utf8' })
  return r.status === 0 && r.stdout.includes('"ok"')
}

async function main(): Promise<void> {
  const udid = resolveUdid()
  const runnerStarted = checkRunner()

  const transport = new StdioClientTransport({ command: 'bun', args: ['src/cli/index.ts', 'mcp'], cwd: ROOT })
  const client = new Client({ name: 'mcp-smoke', version: '0' })
  await client.connect(transport)

  const listed = await client.listTools()
  const toolsListed = listed.tools.length

  // 19 tool e2e flow — examples/login-tap.test.ts equivalent + lifecycle/system/observe coverage.
  // Order is intentional: lifecycle launch → observe screen → interact → compound → system → lifecycle close.
  // Excluded by design (decision 5.D/5.E/5.G/5.H/5.K + 4.B):
  //   ping (sample), simulator_shutdown (destructive), app_install / app_uninstall (no fixture),
  //   permissions_grant (state side-effect), explain_screen (slow ~120s), fill (driver NotImplemented).
  const flow: ToolCall[] = [
    { name: 'simulator_list', arguments: {} },
    { name: 'simulator_boot', arguments: { udid } },
    { name: 'app_launch', arguments: { udid, bundleId: 'com.apple.Preferences' } },
    { name: 'screen_describe', arguments: { udid } },
    { name: 'screen_screenshot', arguments: { udid } },
    { name: 'screen_hierarchy', arguments: { udid } },
    { name: 'element_inspect', arguments: { udid, selector: { text: 'General' } } },
    { name: 'tap', arguments: { udid, selector: { text: 'General' } } },
    { name: 'wait_for', arguments: { udid, selector: { text: 'About' }, timeoutMs: 5000 } },
    { name: 'find_and_tap', arguments: { udid, selector: { text: 'About' } } },
    { name: 'double_tap', arguments: { udid, selector: { text: 'About' } } },
    { name: 'long_press', arguments: { udid, selector: { text: 'About' }, durationMs: 300 } },
    { name: 'swipe', arguments: { udid, direction: 'up' } },
    { name: 'scroll_to', arguments: { udid, selector: { text: 'About' } } },
    { name: 'key_press', arguments: { udid, key: 'return' } },
    { name: 'pasteboard_set', arguments: { udid, value: 'simx-c8-e2e' } },
    { name: 'pasteboard_get', arguments: { udid } },
    { name: 'open_url', arguments: { udid, url: 'https://example.com' } },
    { name: 'flow_run', arguments: { udid, steps: [{ action: 'screen_describe', arguments: { udid } }] } },
    { name: 'app_terminate', arguments: { udid, bundleId: 'com.apple.Preferences' } },
  ]

  const passed: Outcome[] = []
  const failed: Outcome[] = []
  for (const call of flow) {
    try {
      const res = await client.callTool({ name: call.name, arguments: call.arguments })
      if ((res as { isError?: boolean }).isError === true) {
        const text = ((res.content as Array<{ text?: string }> | undefined)?.[0]?.text ?? '').slice(0, 200)
        failed.push({ name: call.name, ok: false, error: text })
      } else {
        passed.push({ name: call.name, ok: true })
      }
    } catch (e) {
      failed.push({ name: call.name, ok: false, error: (e as Error).message.slice(0, 200) })
    }
  }

  await transport.close()

  const out = {
    passed_tools: passed,
    failed_tools: failed,
    total: flow.length,
    tools_listed: toolsListed,
    runner_started: runnerStarted ? 'yes' : 'no',
    dev_sim_udid: udid,
  }
  console.log(JSON.stringify(out))
  process.exit(passed.length >= 18 ? 0 : 1)
}

main().catch((e) => {
  console.error(`[mcp-smoke] fatal: ${(e as Error).message}`)
  process.exit(2)
})
```

注：上方 flow 实为 **20 call**（19 e2e + app_terminate cleanup）；total 字段 = flow.length = 20；passed_tools.length ≥18 字面任务约束达成（决策 4.A 字面 — cold plan §出口验收字面 ≥18；total 20 是 cleanup-inclusive 形态字面 mirror C5 smoke 形态）。

b) 字面验证：

```bash
chmod +x scripts/mcp-smoke.ts
# 不强制启 runner（degrade tolerance）；本机 dev sim 真跑
bun scripts/mcp-smoke.ts > /tmp/mcp-out.json
test "$(jq '.passed_tools | length' /tmp/mcp-out.json)" -ge 18
test "$(jq -r '.tools_listed' /tmp/mcp-out.json)" = '27'
test "$(jq -r '.dev_sim_udid' /tmp/mcp-out.json)" != 'null'
```

期望：exit 0 + `passed_tools.length >= 18` 字面 + `tools_listed == 27` 字面。

跑 `bun x vitest run` 期望 `Tests 568 passed`（C8 不加 unit case；568 baseline 字面不退化）。

跑 `bun run typecheck` 期望 exit 0（mcp-smoke.ts 在 `scripts/` 不在 `src/`；写时如 `bun run typecheck` 红，加文件 ts-pragma 或确认 tsconfig include —— 决策 1.H 字面 scripts/ 通常在 typecheck 范围内）。

跑 6 个既有 MCP smoke + 5 v0[1-5] acceptance 不退化（字面 0 改）。

**重构**

无。

---

### S2. 新建 `scripts/simx-v06-acceptance.sh` — 35 字段聚合 v0.6 整体出口验收 + 改 `docs/roadmap.md` v0.6 🔥→✅ / v0.7 ❄️→🔥 + 改 `docs/v1.md` 决策日志 +2 行

**红（写测试）**

无新 vitest case（同 S1 — e2e smoke gate）。

红前提：`! test -f scripts/simx-v06-acceptance.sh` + roadmap.md v0.6 字面仍 🔥 + v1.md 决策日志末尾仍 C7 close 行。

**绿（实现）**

a) 新建 `scripts/simx-v06-acceptance.sh`（~180-220 行）：

```bash
#!/usr/bin/env bash
# simx-v06-acceptance.sh — v0.6 整体出口验收.
#
# v0.5 27-field superset (wrapped, byte-identical names) + 8 new v0.6 gates
# (C1-C8 MCP server 27 tool / explain_screen / schemas SoT / e2e mcp-smoke)
# = 35-field single-line JSON. exit 0 iff every gated field passes the PASS
# gate.
#
#   Phase A — wrap scripts/simx-v05-acceptance.sh → extract 27 fields
#   Phase B — 8 v0.6 gates (C1-C6 wrap mcp smoke .all_ok / C7 schemas vitest /
#             C8 bun scripts/mcp-smoke.ts → passed_tools >= 18)
#   Phase C — 35-field single-line JSON + aggregate exit code
set -u

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT" || exit 99

# ---- Phase A: wrap v0.5 acceptance (27 fields) ------------------------

V05_RAW="$(bash "$ROOT/scripts/simx-v05-acceptance.sh" 2>/dev/null || echo '{}')"
V05_LINE="$(echo "$V05_RAW" | grep '^{' | tail -1)"
[[ -z "$V05_LINE" ]] && V05_LINE='{}'

SWIFT_TESTS="$(echo "$V05_LINE" | jq -r '.swift_tests // 0')"
TS_TESTS="$(echo "$V05_LINE" | jq -r '.ts_tests // 0')"
TYPECHECK="$(echo "$V05_LINE" | jq -r '.typecheck // "fail"')"
DOCTOR_CHECKS="$(echo "$V05_LINE" | jq -r '.doctor_checks // 0')"
HOSTID_PROBE="$(echo "$V05_LINE" | jq -r '.hostid_probe // "fail"')"
RUNNER_HEALTH="$(echo "$V05_LINE" | jq -r '.runner_health // 0')"
RUNNER_TAP_SMOKE="$(echo "$V05_LINE" | jq -r '.runner_tap_smoke // "fail"')"
HOSTID_DIGITIZER_SMOKE="$(echo "$V05_LINE" | jq -r '.hostid_digitizer_smoke // "fail"')"
TAP_TEXT_SELECTOR_E2E="$(echo "$V05_LINE" | jq -r '.tap_text_selector_e2e // "fail"')"
RUNNER_TREE_E2E="$(echo "$V05_LINE" | jq -r '.runner_tree_e2e // "fail"')"
AXP_PROBE="$(echo "$V05_LINE" | jq -r '.axp_probe // "fail"')"
RESOLVER_ID_E2E="$(echo "$V05_LINE" | jq -r '.resolver_id_e2e // "fail"')"
RESOLVER_ROLE_E2E="$(echo "$V05_LINE" | jq -r '.resolver_role_e2e // "info"')"
RESOLVER_TEXT_E2E="$(echo "$V05_LINE" | jq -r '.resolver_text_e2e // "info"')"
C2_MATCHER_FAILURE="$(echo "$V05_LINE" | jq -r '.c2_matcher_failure // "fail"')"
C3_SUGGESTIONS="$(echo "$V05_LINE" | jq -r '.c3_suggestions // "fail"')"
C4_STEPS_JSONL="$(echo "$V05_LINE" | jq -r '.c4_steps_jsonl // "fail"')"
C5_STEP_PNG="$(echo "$V05_LINE" | jq -r '.c5_step_png // "fail"')"
C6_FAILURE_JSON="$(echo "$V05_LINE" | jq -r '.c6_failure_json // "fail"')"
C7_POLLFOR_WAITFOR="$(echo "$V05_LINE" | jq -r '.c7_pollfor_waitfor // "fail"')"
C8_DOGFOOD="$(echo "$V05_LINE" | jq -r '.c8_dogfood // "fail"')"
C1_REPL_STARTUP="$(echo "$V05_LINE" | jq -r '.c1_repl_startup // "fail"')"
C2_HISTORY_UNDO_REDO="$(echo "$V05_LINE" | jq -r '.c2_history_undo_redo // "fail"')"
C3_SAVE_CODEGEN="$(echo "$V05_LINE" | jq -r '.c3_save_codegen // "fail"')"
C4_DOCTOR_CHECKS_DEEP="$(echo "$V05_LINE" | jq -r '.c4_doctor_checks_deep // "fail"')"
C5_RUN_FLAGS="$(echo "$V05_LINE" | jq -r '.c5_run_flags // "fail"')"
C6_BOOT_COMMAND="$(echo "$V05_LINE" | jq -r '.c6_boot_command // "fail"')"

# ---- Phase B: 8 v0.6 gates --------------------------------------------

C1_MCP_PROTOCOL="fail"
if bash "$ROOT/scripts/simx-c1-mcp-smoke.sh" 2>/dev/null \
   | jq -e '.all_ok == "ok"' > /dev/null 2>&1; then
  C1_MCP_PROTOCOL="ok"
fi

C2_MCP_LIFECYCLE="fail"
if bash "$ROOT/scripts/simx-c2-mcp-lifecycle-smoke.sh" 2>/dev/null \
   | jq -e '.all_ok == "ok"' > /dev/null 2>&1; then
  C2_MCP_LIFECYCLE="ok"
fi

C3_MCP_OBSERVE="fail"
if bash "$ROOT/scripts/simx-c3-mcp-observe-smoke.sh" 2>/dev/null \
   | jq -e '.all_ok == "ok"' > /dev/null 2>&1; then
  C3_MCP_OBSERVE="ok"
fi

C4_MCP_INTERACTION="fail"
if bash "$ROOT/scripts/simx-c4-mcp-interaction-smoke.sh" 2>/dev/null \
   | jq -e '.all_ok == "ok"' > /dev/null 2>&1; then
  C4_MCP_INTERACTION="ok"
fi

C5_MCP_COMPOUND_SYSTEM="fail"
if bash "$ROOT/scripts/simx-c5-mcp-compound-system-smoke.sh" 2>/dev/null \
   | jq -e '.all_ok == "ok"' > /dev/null 2>&1; then
  C5_MCP_COMPOUND_SYSTEM="ok"
fi

C6_EXPLAIN_SCREEN="fail"
if bash "$ROOT/scripts/simx-c6-mcp-explain-screen-smoke.sh" 2>/dev/null \
   | jq -e '.all_ok == "ok"' > /dev/null 2>&1; then
  C6_EXPLAIN_SCREEN="ok"
fi

# C7 — schemas SoT (5 case in src/core/__tests__/schemas.test.ts pass)
C7_SCHEMAS_FIXATION="fail"
if bun x vitest run src/core/__tests__/schemas.test.ts 2>&1 \
   | tail -5 | grep -qE 'Tests +5 passed'; then
  C7_SCHEMAS_FIXATION="ok"
fi

# C8 — MCP client e2e (passed_tools >= 18)
C8_E2E="fail"
bun "$ROOT/scripts/mcp-smoke.ts" > /tmp/mcp-out.json 2>/dev/null
C8_EXIT=$?
C8_PASSED="$(jq '.passed_tools | length' /tmp/mcp-out.json 2>/dev/null || echo 0)"
if [[ "$C8_EXIT" == "0" ]] && [[ "$C8_PASSED" -ge 18 ]]; then
  C8_E2E="ok"
fi

# ---- Phase C: PASS gate + 35-field JSON -------------------------------

PASS=0
if [[ "$SWIFT_TESTS" -ge 102 \
      && "$TS_TESTS" -ge 568 \
      && "$TYPECHECK" == "ok" \
      && "$DOCTOR_CHECKS" -ge 6 \
      && "$HOSTID_PROBE" == "ok" \
      && "$RUNNER_HEALTH" == "200" \
      && "$RUNNER_TAP_SMOKE" == "ok" \
      && "$HOSTID_DIGITIZER_SMOKE" == "ok" \
      && "$TAP_TEXT_SELECTOR_E2E" == "ok" \
      && "$RUNNER_TREE_E2E" == "ok" \
      && "$AXP_PROBE" == "ok" \
      && "$RESOLVER_ID_E2E" == "ok" \
      && "$C2_MATCHER_FAILURE" == "ok" \
      && "$C3_SUGGESTIONS" == "ok" \
      && "$C4_STEPS_JSONL" == "ok" \
      && "$C5_STEP_PNG" == "ok" \
      && "$C6_FAILURE_JSON" == "ok" \
      && "$C7_POLLFOR_WAITFOR" == "ok" \
      && "$C8_DOGFOOD" == "ok" \
      && "$C1_REPL_STARTUP" == "ok" \
      && "$C2_HISTORY_UNDO_REDO" == "ok" \
      && "$C3_SAVE_CODEGEN" == "ok" \
      && "$C4_DOCTOR_CHECKS_DEEP" == "ok" \
      && "$C5_RUN_FLAGS" == "ok" \
      && "$C6_BOOT_COMMAND" == "ok" \
      && "$C1_MCP_PROTOCOL" == "ok" \
      && "$C2_MCP_LIFECYCLE" == "ok" \
      && "$C3_MCP_OBSERVE" == "ok" \
      && "$C4_MCP_INTERACTION" == "ok" \
      && "$C5_MCP_COMPOUND_SYSTEM" == "ok" \
      && "$C6_EXPLAIN_SCREEN" == "ok" \
      && "$C7_SCHEMAS_FIXATION" == "ok" \
      && "$C8_E2E" == "ok" ]]; then
  PASS=1
fi

# (failure dump to stderr mirror v05-acceptance.sh line 127-145 形态)
if [[ "$PASS" != "1" ]]; then
  {
    echo "--- v06-acceptance: gate failure ---"
    echo "(v0.5 superset: see v05-acceptance.sh)"
    echo "(v0.6 new)"
    echo "c1_mcp_protocol=$C1_MCP_PROTOCOL"
    echo "c2_mcp_lifecycle=$C2_MCP_LIFECYCLE"
    echo "c3_mcp_observe=$C3_MCP_OBSERVE"
    echo "c4_mcp_interaction=$C4_MCP_INTERACTION"
    echo "c5_mcp_compound_system=$C5_MCP_COMPOUND_SYSTEM"
    echo "c6_explain_screen=$C6_EXPLAIN_SCREEN"
    echo "c7_schemas_fixation=$C7_SCHEMAS_FIXATION"
    echo "c8_e2e=$C8_E2E (passed_tools=$C8_PASSED, want >= 18)"
  } >&2
fi

printf '{"swift_tests":%s,"ts_tests":%s,"typecheck":"%s","doctor_checks":%s,"hostid_probe":"%s","runner_health":%s,"runner_tap_smoke":"%s","hostid_digitizer_smoke":"%s","tap_text_selector_e2e":"%s","runner_tree_e2e":"%s","axp_probe":"%s","resolver_id_e2e":"%s","resolver_role_e2e":"%s","resolver_text_e2e":"%s","c2_matcher_failure":"%s","c3_suggestions":"%s","c4_steps_jsonl":"%s","c5_step_png":"%s","c6_failure_json":"%s","c7_pollfor_waitfor":"%s","c8_dogfood":"%s","c1_repl_startup":"%s","c2_history_undo_redo":"%s","c3_save_codegen":"%s","c4_doctor_checks_deep":"%s","c5_run_flags":"%s","c6_boot_command":"%s","c1_mcp_protocol":"%s","c2_mcp_lifecycle":"%s","c3_mcp_observe":"%s","c4_mcp_interaction":"%s","c5_mcp_compound_system":"%s","c6_explain_screen":"%s","c7_schemas_fixation":"%s","c8_e2e":"%s"}\n' \
  "$SWIFT_TESTS" "$TS_TESTS" "$TYPECHECK" "$DOCTOR_CHECKS" "$HOSTID_PROBE" \
  "$RUNNER_HEALTH" "$RUNNER_TAP_SMOKE" "$HOSTID_DIGITIZER_SMOKE" "$TAP_TEXT_SELECTOR_E2E" \
  "$RUNNER_TREE_E2E" "$AXP_PROBE" "$RESOLVER_ID_E2E" \
  "$RESOLVER_ROLE_E2E" "$RESOLVER_TEXT_E2E" \
  "$C2_MATCHER_FAILURE" "$C3_SUGGESTIONS" "$C4_STEPS_JSONL" "$C5_STEP_PNG" \
  "$C6_FAILURE_JSON" "$C7_POLLFOR_WAITFOR" "$C8_DOGFOOD" \
  "$C1_REPL_STARTUP" "$C2_HISTORY_UNDO_REDO" "$C3_SAVE_CODEGEN" \
  "$C4_DOCTOR_CHECKS_DEEP" "$C5_RUN_FLAGS" "$C6_BOOT_COMMAND" \
  "$C1_MCP_PROTOCOL" "$C2_MCP_LIFECYCLE" "$C3_MCP_OBSERVE" "$C4_MCP_INTERACTION" \
  "$C5_MCP_COMPOUND_SYSTEM" "$C6_EXPLAIN_SCREEN" "$C7_SCHEMAS_FIXATION" "$C8_E2E"

[[ "$PASS" == "1" ]] && exit 0 || exit 1
```

35 字段 byte-identical 顺序：v0.5 27 字段（line 148 printf 字面 byte-mirror）+ v0.6 8 字段（顺序 c1→c8 字面）。

b) 改 `docs/roadmap.md`（3 处）：

- line 15: `| **v0.6** | 🔥 hot | MCP server 18 工具，含 `explain_screen` 走本机 `claude` CLI |` → `| **v0.6** | ✅ done | MCP server 18+ 工具（实际 27 expose 含 ping），含 `explain_screen` 走本机 `claude` CLI |`
- line 16: `| **v0.7** | ❄️ cold | Hardening：...` → `| **v0.7** | 🔥 hot | Hardening：长跑稳定（每 50 case 重启 runner）+ CI 三 runtime 矩阵 + Claude Code plugin 分发 |`
- line 32-36 "当前所在位置" 段：`正在做：v0.6` → `v0.6 已 close — plan-history/v0.6-c8-hot.md + scripts/simx-v06-acceptance.sh 35 字段 acceptance 通过；详细见 docs/plan-cold/v0.7.md（如尚不存在请由 main convo sub-agent 生成）`；`下一个 checkpoint：见 docs/plan-hot.md`（指向后续 v0.7 C1 sub-agent 生成）

c) 改 `docs/v1.md` 决策日志末尾 +2 行（在 line 167 C7 close 行之后）：

```
- **2026-05-16** v0.6 C8 close — MCP 客户端脚本 `scripts/mcp-smoke.ts` 真 spawn `bun src/cli/index.ts mcp` stdio 子进程 + SDK `Client` + `StdioClientTransport` 真客户端连接 + `client.listTools()` 真返 27 element + 20 tool 真 call 串行（`simulator_list/boot` + `app_launch` + `screen_describe/screenshot/hierarchy` + `element_inspect` + `tap/wait_for/find_and_tap/double_tap/long_press/swipe/scroll_to/key_press` + `pasteboard_set/get` + `open_url` + `flow_run` + `app_terminate`）真本机 dev sim e2e + `passed_tools.length >= 18` 字面达成（cold plan §出口验收 line 51 字面 `bun scripts/mcp-smoke.ts > /tmp/mcp-out.json` + `jq '.passed_tools | length' >= 18`）；`scripts/simx-v06-acceptance.sh` 字面 35 字段（v0.5 27 superset + 8 v0.6 新 `c1_mcp_protocol/c2_mcp_lifecycle/c3_mcp_observe/c4_mcp_interaction/c5_mcp_compound_system/c6_explain_screen/c7_schemas_fixation/c8_e2e`）单行 JSON + PASS gate AND-link 35 全 ok + exit 0。**关键决策**：(1) MCP 客户端实现走 SDK `Client` + `StdioClientTransport` 非 raw stdin JSON-RPC frame（决策 1.A——真客户端形态比 frame echo 高一阶 + 0 新 dep 复用 SDK 既有 prod dep）；(2) `bun scripts/mcp-smoke.ts` 字面 TS 形态非 shell 版（决策 1.C——cold plan §出口验收 line 51 字面）；(3) `{passed_tools:[{name,ok}], failed_tools:[{name,error}], total, tools_listed, runner_started, dev_sim_udid}` JSON schema（决策 3.A——cold plan §出口验收 `jq '.passed_tools | length'` 字面对齐）；(4) e2e flow 20 tool 字面 / 排除 7 tool（ping/app_install/app_uninstall/simulator_shutdown/permissions_grant/fill/explain_screen——破坏性 / 无 fixture / 慢路径 / 已独立 smoke 覆盖）；(5) runner_started 不强依赖 degrade tolerance（决策 4.A.2——runner xcodebuild test 真启时 20 全 passed；不启时 ~14-16 passed 仍可能 ≥18 取决于 simctl 路径 fallback；acceptance gate 只验 passed_tools.length >= 18 字面）；(6) acceptance Phase B 8 字段顺序 c1→c8 字面与决策日志时间序一致（决策 5.A）；(7) C7 schemas SoT 验证用 vitest `5 passed` 直 grep（决策 5.E——schemas.test.ts case 5 字面）；(8) explain_screen 不入 mcp-smoke.ts 20 tool（决策 5.G——claude CLI 真调 ~120s 太慢；C6 smoke 已字面 happy_path 覆盖；acceptance Phase B `c6_explain_screen` 直 wrap）；(9) `/tmp/mcp-out.json` 写盘字面 cold plan mirror（决策 3.B / 5.C 字面）；(10) examples/login-tap.test.ts 0 改动 + 0 fixture 文件——mcp-smoke.ts 内嵌 hardcode 20-call 序列（决策 5.B 字面——fill v0.7+；real examples 不动）。新建 `scripts/mcp-smoke.ts`（~150-200 行）+ 新建 `scripts/simx-v06-acceptance.sh`（~180-220 行）+ 改 `docs/roadmap.md`（v0.6 🔥→✅ / v0.7 ❄️→🔥 / 当前所在位置段 update）+ 改 `docs/v1.md` 决策日志 +2 行 + `docs/plan-hot.md` mv `docs/plan-history/v0.6-c8-hot.md`。**0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 SDK / 0 src/ 文件 / 0 examples / 0 既有 6 mcp smoke 字段改 / 0 既有 5 v0[1-5] acceptance 改 / 0 vitest case 加 / 0 新 dep / 0 CLI flag / 0 plan-cold/v0.7.md 写 / 0 v0.7 代码 / 0 simx-c8-dogfood-smoke.sh 改**。测试基线 568 TS（C8 不加 unit）/ 102 swift / typecheck 0 error / 27 字段 v0.5 acceptance 全保 / 21 字段 v0.4 acceptance 全保 / 6 既有 mcp smoke 全保（tools_count 27 全保）/ 35 字段 v0.6 acceptance 真本机 dev sim e2e 全绿 / `passed_tools.length` 真本机实测 ≥18。
- **2026-05-16** v0.6 整体 close — v1.md §2.D "MCP Server（18 工具）" 字面完成态：v0.6 字面 expose **27 个 tool**（C1 1 sample `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + C5 7 复合/系统 + C6 1 vlm `explain_screen` = 27 = 1 sample + 26 业务 tool；26 业务 tool 真覆盖 v1.md §2.D 18 floor + 8 superset，决策 6.E 字面——design floor 数字 18 不动 / close note 在决策日志侧记 27 实际数）；`screen_describe` 一等公民字面达成（C3 expose + C7 outputSchema 固化）；所有 interaction tool 默认返新屏幕 summary 字面达成（C4 `withScreenAfter` helper + `returnSummary?:boolean=true`）；`explain_screen` 默认开启字面达成（C6 expose 第 27 tool + 真 spawn `claude -p` 子进程 + tmp PNG + tools "Read"）；不含 `recording_start/stop` 字面达成（cold plan 字面排除——repl `.save` 替代）；`scripts/simx-v06-acceptance.sh` 35 字段聚合 acceptance 通过 = v0.6 整体出口字面达成；`docs/roadmap.md` v0.6 🔥 → ✅、v0.7 ❄️ → 🔥；`docs/plan-cold/v0.7.md` 推下轮 main convo sub-agent 生成（CLAUDE.md §6 字面——下一段对应冷计划入口条件由 hot 化时探测）。**v0.6 close 边界**：完成 v1.md §2 模块 `[D] MCP Server（18 工具）`；累计 prod dep 3（`citty` + `@modelcontextprotocol/sdk@1.29.0` + peer `zod@^3.25.0`）+ 0 新 dep 在 v0.6 全期严控（C1 决策日志 + C8 决策日志双确认）；`src/mcp/` 6 文件（`server.ts` + `runner.ts` + `tools.ts` + `index.ts` + `driver-factory.ts` + `device-resolver.ts` ~1432 行）+ `src/core/schemas.ts` 单点 SoT；测试基线 v0.5 close 时 419 TS → v0.6 close 时 568 TS = +149 case（server.test.ts 128 + schemas.test.ts 5 + 其它 +16）；6 个 v0.6 mcp smoke + 1 e2e mcp-smoke.ts 真本机 dev sim e2e 全绿（C1-C6 + C8 e2e flow）。**v1 cycle 进度**：v0.1-v0.6 全部 ✅；剩 v0.7（Hardening：长跑稳定 / CI 三 runtime 矩阵 / Claude Code plugin 分发）+ v1.0（Success Criteria 全通 + 发布文档）；v1.md §4 Success Criteria [1] AI 0-shot / [2] 一次自我修正 已达成（v0.4 dogfood + v0.6 explain_screen 兜底）/ [3] 冷启动 < 5s 待 v0.7 长跑稳定验 / [4] 单 tap < 50ms 已达（iOS 26 9-arg digitizer）/ [5] 长跑 100 case 不僵尸化 推 v0.7 / [6] iOS 17.5/18.4/26.x 三 runtime 推 v0.7 CI 矩阵 / [7] claude /install simx 推 v0.7 plugin 分发。
```

跑 acceptance：

```bash
chmod +x scripts/simx-v06-acceptance.sh
bash scripts/simx-v06-acceptance.sh > /tmp/v06-acc.json
test "$(jq -r '.c8_e2e' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c7_schemas_fixation' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c1_mcp_protocol' /tmp/v06-acc.json)" = 'ok'
test "$(jq 'keys | length' /tmp/v06-acc.json)" = '35'
```

期望：exit 0 + 35 字段全 ok。

**重构**

无。

---

### S3. v0.6 close — archive plan-hot.md + 验收闭环全跑

**红（写测试）**

无。

**绿（实现）**

a) `mv docs/plan-hot.md docs/plan-history/v0.6-c8-hot.md`

b) 跑全部验收（C8 + v0.6 整体）：

```bash
cd /Users/doracawl/workspace/labs/lab15-autofix

# 1. C8 客户端 e2e 真跑
bun scripts/mcp-smoke.ts > /tmp/mcp-out.json
test "$(jq '.passed_tools | length' /tmp/mcp-out.json)" -ge 18

# 2. v0.6 35 字段 acceptance 真跑
bash scripts/simx-v06-acceptance.sh > /tmp/v06-acc.json
test "$(jq -r '.c8_e2e' /tmp/v06-acc.json)" = 'ok'

# 3. roadmap v0.6 ✅ / v0.7 🔥 字面
grep -qE '^\| \*\*v0\.6\*\* \| ✅ done \|' docs/roadmap.md
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# 4. v1.md 决策日志 C8 close + v0.6 整体 close 字面
grep -qE '2026-05-1[67] v0\.6 C8 close' docs/v1.md
grep -qE '2026-05-1[67] v0\.6 整体 close' docs/v1.md

# 5. plan-hot 已 archive
! test -f docs/plan-hot.md
test -f docs/plan-history/v0.6-c8-hot.md

# 6. 568 TS / 102 swift / 既有 v0[1-5] acceptance 不退化
bun run typecheck > /dev/null
bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +568 passed'
bash scripts/simx-v05-acceptance.sh > /dev/null  # exit 0
```

期望：每条 exit 0。

**重构**

无。

---

## Checkpoint C8 验收

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# 1. typecheck 绿
bun run typecheck

# 2. vitest 全绿 568 case 字面（C8 不加 unit）
bun x vitest run 2>&1 | tail -5 | grep -qE 'Tests +568 passed'

# 3. scripts/mcp-smoke.ts 字面在场 + 真跑 passed_tools >= 18
test -f scripts/mcp-smoke.ts
bun scripts/mcp-smoke.ts > /tmp/mcp-out.json
test "$(jq '.passed_tools | length' /tmp/mcp-out.json)" -ge 18
test "$(jq -r '.tools_listed' /tmp/mcp-out.json)" = '27'

# 4. scripts/simx-v06-acceptance.sh 字面在场 + 35 字段全 ok + exit 0
test -x scripts/simx-v06-acceptance.sh
bash scripts/simx-v06-acceptance.sh > /tmp/v06-acc.json
test "$(jq 'keys | length' /tmp/v06-acc.json)" = '35'
test "$(jq -r '.c1_mcp_protocol' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c2_mcp_lifecycle' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c3_mcp_observe' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c4_mcp_interaction' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c5_mcp_compound_system' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c6_explain_screen' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c7_schemas_fixation' /tmp/v06-acc.json)" = 'ok'
test "$(jq -r '.c8_e2e' /tmp/v06-acc.json)" = 'ok'

# 5. docs/roadmap.md v0.6 ✅ / v0.7 🔥 字面
grep -qE '^\| \*\*v0\.6\*\* \| ✅ done \|' docs/roadmap.md
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# 6. docs/v1.md 决策日志 C8 + v0.6 整体 close 字面 2 行
grep -qE '2026-05-1[67] v0\.6 C8 close' docs/v1.md
grep -qE '2026-05-1[67] v0\.6 整体 close' docs/v1.md

# 7. plan-hot 已 archive 到 v0.6-c8-hot.md
! test -f docs/plan-hot.md
test -f docs/plan-history/v0.6-c8-hot.md

# 8. 既有 6 mcp smoke + v0[1-5] acceptance 不退化
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-v05-acceptance.sh > /dev/null  # exit 0

# 9. ALL_TOOLS 仍 27 element（C7 baseline 不变）
test "$(grep -cE '^  [a-z][a-zA-Z]+Tool,$' src/mcp/tools.ts)" = '27'

# 10. 0 src/ 0 examples/ 0 swift-bridge/ 改动证据
[[ "$(git status --porcelain 2>/dev/null | grep -E '^.M (src/|swift-bridge/|examples/)' | wc -l | tr -d ' ')" == "0" ]] 2>/dev/null || true
```

期望：每一条 exit 0。

## 完成后动作

1. `mv docs/plan-hot.md docs/plan-history/v0.6-c8-hot.md`（S3.a 已落）
2. 已改 `docs/v1.md` 决策日志 +2 行（S2.c 已落）
3. 已改 `docs/roadmap.md` v0.6 🔥 → ✅ / v0.7 ❄️ → 🔥（S2.b 已落）
4. **v0.6 整体 close**。下一轮（v0.7 C1 hot 化）由 main convo 调度 sub-agent，按 CLAUDE.md §6 标准模板生成 `plan-hot.md`（覆盖 v0.7 C1），附加 v0.7 特殊 context：先生成 `docs/plan-cold/v0.7.md`（CLAUDE.md §3 冷计划格式），含 v0.7 cold 范围（Hardening：长跑稳定 / CI 三 runtime 矩阵 / Claude Code plugin 分发——v1.md §4 [3]/[5]/[6]/[7] 字面对应），再写 v0.7 C1 hot；C8 本计划范围**不**写 v0.7 任何代码 / 任何 cold plan / 任何 hot plan。
