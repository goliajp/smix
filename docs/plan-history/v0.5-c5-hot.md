# plan-hot — v0.5 C5：`simx run` 增强 — `--device` 行为保 + `--grep <substr>` + `--json` schema CI-parsable + `--bail` 首 fail 立停

> [3/4 信息层] 当前唯一热计划。**C5 是 v0.5 第五个 checkpoint**——通过后世界变成：`bun src/cli/index.ts run <file.test.ts> [--device | --udid | --runtime] [--grep <substr>] [--json] [--bail]` 4 flag 全部真生效。其中 `--device` 在 v0.1 已落地（`pickCell` line 95-122 支持 `select.deviceName`，本 C5 仅写 regression case 锁），**C5 真生效的 3 个 flag** 是：(1) `--grep <substr>` — 字面 substring match case `name` 过滤（决策 1：A 字面 substring 而非 regex / glob，理由：`src/sdk/test.ts` line 56 既有 `RunOptions.grep` 已是 `c.name.includes(opts.grep!)` substring 形态，C5 仅 wire CLI flag → 0 改 SDK 接口）；(2) `--json` — 切到 single-line JSON 输出模式（**替代** 人类 stdout 而非双写；决策 3：A 替代模式，理由：cold plan §出口验收 `jq '.passed'` 字面要求 stdout 是纯 JSON，可被 pipe 进 jq 无 prefix 干扰；非 `--json` 模式仍 byte-identical 既有 `${passed} passed, ${failed} failed\n`）；(3) `--bail` — boolean flag（决策 4：A 0-arg boolean 拒 `--bail N`），首个 fail case 立即停止后续 case 执行，已跑的 case 全部保结果 + 后续 case 全部不跑（注意：单 case 内若多个 `expect` 抛错，只算 1 个 fail——`--bail` 是 case 级别非 expectation 级别）。**`--bail` 与 `--grep` 联用**：先 grep 过滤 → 在过滤后子集内 bail 第 1 fail；`--bail` 与 `--json` 联用：JSON `cases` 数组只含已跑 case（含 fail 那 1 个）+ `bailed: true` 字段；未跑 case 不出现在 cases 数组中（决策 5：A 拒填 skipped status；cases.length < total 隐含已 bail）。
>
> **承接（v0.5 C4 close 后实测 baseline）**：v0.5 C4 close — `simx doctor` JSON CI-schema 锁 + runtimes/claude 2 check deepen + 6 check 顺序锁 [xcode/runtimes/claude/bun/hid/axp]。最近形态参考 `docs/plan-history/v0.5-c4-hot.md` (555 行)。测试基线 **407 TS / 102 swift / 21 字段 v0.4 acceptance 全保**。
>
> **现状盘点（C5 真生效边界）**：
> - **`src/cli/index.ts` line 31-55 `runCommand`**已实现 4 args：`file` (positional, required) / `udid` / `device` / `runtime`（3 个 device-select args 全 string optional）；**C5 加 3 个 args**：`grep: { type: 'string' }` / `json: { type: 'boolean', default: false }` / `bail: { type: 'boolean', default: false }`——citty `defineCommand` 签名扩展（与 doctor command `--json` 形态对齐 line 63）。改 `runRunCommand` 调用：`grep`/`json`/`bail` 透传到 `RunRunDeps`（**3 行新增**，0 删除）。
> - **`src/cli/commands/run.ts` line 30-37 `RunRunDeps`**当前 7 字段：`client`/`file`/`select`/`out`/`err`/`cwd`；**C5 扩 3 字段**：`grep?: string` / `json?: boolean` / `bail?: boolean`（**3 行新增**）。`runRunCommand` 主体 line 39-93 改 ~25 行：(a) 调 `runRegistry` 时把 `grep` 透传到 `RunOptions.grep`（既有 SDK 接口 line 30）；(b) 实现 `--bail` 逻辑——因 `runRegistry` 当前 line 59-73 不支持 bail，**改 SDK** `src/sdk/test.ts` `RunOptions` 加 `bail?: boolean` + 主循环 line 59-73 在 catch 块内 `if (opts.bail) break`（**~3-5 行 SDK 改动**，决策 6：A 在 SDK 加 bail 拒 CLI 层包 wrapper—— wrapper 需要复制 onCaseStart/onCaseEnd 钩子 + try/catch 复杂度，SDK 内加 1 行 break 简洁）；(c) 输出分流——`if (deps.json) { JSON.stringify(...) + '\n' }` 单行 JSON 替代既有 `${passed} passed, ${failed} failed` 人类文本（**~15 行新增**）；(d) JSON 输出 stderr `FAIL ${name}` 那段 line 83-88：在 `--json` 模式下 stderr 也静默（决策 3.c：A JSON 模式 stderr 0 字节使 CI consumer 100% 纯净，理由：用户调 `--json | jq` 时 stderr noise 不入 pipe 但也不该被 console 染色；fail 详情已在 JSON `cases[i].error` 字段——单一事实源）。
> - **`src/sdk/test.ts` line 28-36 `RunOptions`**当前 4 字段：`driver` / `grep` / `onCaseStart` / `onCaseEnd`（**`grep` 已存在 line 32-33 `filter by case name substring`** —— v0.1/v0.2 时就已 wire，CLI 仅未连 flag）；**C5 加 1 字段** `bail?: boolean`（**1 行新增**）+ 改主循环 line 59-73：catch 块尾在 `result.failures.push` 之后加 `if (opts.bail) break`（**~2-3 行新增**，**0 删除现有行为**）。**注意**：SDK `run` 既有 grep filter line 55-57 已字面工作，仅需 CLI 接通——非"实现 grep"，是"接通 grep"。
> - **`src/cli/__tests__/run.test.ts` 14 case**（line 104 `describe('runRunCommand', ...)` 唯一 describe，包 13 `it(...)`——既有 happy/2 case/fail/udid/default-pick/matcher-fail/jsonl/throw/readonly/step-png-3 case/file-not-found 12+1+1 = 13 case；现状盘点字面）；C5 新增 `describe('C5: --grep + --json + --bail (v0.5 C5)', ...)` block ~7-8 case（见 §步骤 S1 红段）；**保既有 13 case 0 改动**（决策 7：A 0 改既有拒并行写 + 拒删——既有 13 case 是 v0.1-v0.4 累积 anchor，C5 不动；新 block 字面追加），合计 ~20-22 case。
> - **`src/sdk/__tests__/test.test.ts`**（既有 ~10-15 case 测 registry / grep / onCaseStart/End）：C5 加 ~3-4 case 测 `bail` 行为 — `bail: true` + 第 1 fail → 后续 case 不跑 / `bail: false` (default) → 全跑 / `bail` + 0 fail → 全跑（行为与默认等价）/ `bail` + grep 联用（**~3-4 case 新增**）。
> - **新文件 `scripts/simx-c5-run-flags-smoke.sh`** (~60-80 行)：e2e gate — 真跑 `bun src/cli/index.ts run examples/screenshot-only.test.ts --json` (本机 v0.3 C7 落地 examples 字面在场) 解析 JSON → 7 字段 `{exit_code, json_parseable, passed_field, failed_field, total_field, cases_array_present, all_ok}`。**不依赖** `examples/calc-two.test.ts`（决策 8：A 拒——calc-two 是 C3 `.save as <name>` REPL 用户产物，C5 不假设其在场；用 `examples/screenshot-only.test.ts` 字面在场更稳）。cold plan §出口验收字面 `examples/calc-two.test.ts` 仍可手工跑 + cold plan 整体 v0.5 出口在 C6 close 时验证（C1-C6 链路一起）。
> - **`docs/v1.md` 决策日志 +1 行**（C5 close 时记，C1/C2/C3/C4 同形态延续）。
> - **0 swift / 0 Driver interface / 0 Cell / 0 trace-sink 激活 / 0 dep 引入 / 0 SimctlDriver / 0 matchers / 0 ExpectationFailure schema 改动**。`pickCell` line 95-122 **0 改动**（`--device` 行为既有，C5 仅写 regression case 锁定）。
>
> **C5 真生效 = 五条**：
> 1. **`src/cli/index.ts` runCommand args 扩 3 个**（`grep` string / `json` boolean / `bail` boolean）+ 透传到 `runRunCommand` 调用（**~10 行新增 + 0 改既有**）
> 2. **`src/cli/commands/run.ts` `RunRunDeps` 扩 3 字段 + `runRunCommand` 主体改 ~25 行**：grep 透传 SDK `RunOptions` / json 模式输出分流（包 stderr 静默）/ bail 透传 SDK
> 3. **`src/sdk/test.ts` `RunOptions` 加 `bail?: boolean` + 主循环 catch 末加 `if (opts.bail) break`**（**~3-5 行新增**，0 删除既有行为）
> 4. **`src/cli/__tests__/run.test.ts` 追加 `describe('C5: --grep + --json + --bail (v0.5 C5)')` block ~7-8 case** + **`src/sdk/__tests__/test.test.ts` 追加 ~3-4 case** 测 bail 语义
> 5. **新建 `scripts/simx-c5-run-flags-smoke.sh`** (~60-80 行)：JSON CI-schema 真盘 gate
>
> **C6 全部不在 C5 范围**：
> - `simx boot <device>` 独立子命令 (cold plan §C6 字面)：决策 9 拒（C5 严格"run 增强 4 flag"，不加新子命令）
> - 加 `--reporter <name>` flag（默认 vs json 之外的 reporter，如 tap / junit）：决策 10 拒（推 v0.7+；`--json` 单 flag 已满足 cold plan §出口验收）
> - 加 `--bail N` 数值变体（容忍 N fail 后才停）：决策 4 拒（boolean 已 cover 90% TDD 场景；数值变体推 v0.7+）
> - 加 `--grep` regex / glob 变体：决策 1 拒（substring 已字面 cover `c.name.includes(grep)`，与 v0.1 wire 一致；regex/glob 推 v0.7+ 或 v1.1+ watch mode）
> - 加 `--parallel <N>` / `--workers <N>` parallel case 执行：决策 11 拒（v1.1+ Cell L4 并行调度 字面；v0.5 C5 严格 serial）
> - 加 `--watch` watch mode：决策 12 拒（推 v1.1+ watch mode 字面）
> - 加 v0.5 acceptance 字段 / `scripts/simx-v05-acceptance.sh`：v0.5 close 时 C6 一并写（C1/C2/C3/C4 决策同形态延续）
> - 改 `examples/` 文件 / `_v04-tests` / `_v05-saved` 文件：决策 13 拒（C5 是 CLI 表面层；examples reorg 不在 C5 scope）
>
> **范围严格 = swift 0 改动 / Driver interface 0 改动 / Cell 0 改动 / ExpectationFailure schema 0 改动 / matchers public API 0 改动 / SimctlDriver 0 改动 / trace-sink 不激活 / 0 dep 引入 / 0 npm/pnpm；只 bun + node stdlib**。允许改动文件范围（**≤ 5 条**，任务约束字面）：
> 1. `src/cli/index.ts` — runCommand args 扩 3 个 + 透传（**~10 行新增**）
> 2. `src/cli/commands/run.ts` — `RunRunDeps` 扩 3 字段 + 主体改 ~25 行（**~30-40 行改既有 + 新增**）
> 3. `src/sdk/test.ts` — `RunOptions.bail` 字段 + 主循环 break（**~3-5 行新增**）
> 4. `src/cli/__tests__/run.test.ts` — 追加 C5 describe block ~7-8 case（**~120-160 行新增**）+ `src/sdk/__tests__/test.test.ts` — 追加 ~3-4 case bail（**~50-70 行新增**；同 1 文件改动算一条，决策 14：A 1 改动条目 / 拒按测试文件计 2 条——SDK + CLI 测试同 PR 是一组）
> 5. **新建 `scripts/simx-c5-run-flags-smoke.sh`** (~60-80 行)：e2e CI-schema gate
> 6. **C5 close 时 `docs/v1.md` 决策日志 +1 行**（不计 ≤ 5 条 src 改动；docs 不算）
>
> 不允许：(a) 改 `src/driver/` / `src/sim/` / `src/core/` / `src/sdk/app.ts` / `src/sdk/trace-sink.ts` 任何文件（C5 是 CLI + registry 层）；(b) 改 `swift-bridge/` 任何文件（v1.md §9.6 dlsym 决策硬约束）；(c) 改 `src/cli/commands/repl*.ts` / `doctor.ts` / `list.ts`（C5 只动 run.ts）；(d) 改 `scripts/simx-v04-acceptance.sh`（v0.4 acceptance 是冻结痕迹；C5 superset 自然过）；(e) 改 `package.json`（0 dep）；(f) 加 `--bail N` 数值变体；(g) 加 `--parallel` / `--watch` / `--reporter`；(h) 改 `pickCell`（`--device` 行为既有 v0.1，仅写 regression case 锁）；(i) 让 `--json` mode 双写人类 + JSON（决策 3 字面 A 替代）；(j) 让 `--json` mode stderr 染色 FAIL line（决策 3.c 字面 stderr 静默）；(k) 让 `--bail` 影响单 case 内多 expect 行为（bail 是 case 级别非 expect 级别）。
>
> **C5 真生效 = 现状盘点详细补充**：
> - **`--device` 字面满足 cold plan §C5**：v0.1 `pickCell` line 95-122 已支持 `select.deviceName` 透传 `acquireCell({deviceName})` (`src/sim/cell.ts` 既有)；既有 `run.test.ts` line 191-214 case `'device selection: --udid picks exactly that device'` 是 udid 路径；C5 加 1 regression case `'device selection: --device picks by name'` 字面锁 deviceName 路径（决策 2：A 加 regression case 而非新行为；拒——已存在的功能不在 C5 scope 范围内，但需要锁防退化）。
> - **`--grep` SDK 端已字面 wire**：`src/sdk/test.ts` line 30-36 `RunOptions.grep` 字段 + line 55-57 `const cases = opts.grep ? registry.filter((c) => c.name.includes(opts.grep!)) : registry` —— v0.1 时就已字面 substring match；本 C5 仅在 CLI 层加 `--grep <pattern>` flag 透传到 `RunOptions.grep`。无 SDK 接口改动，仅 wire 接通（决策 1：A substring 字面与 SDK wire 一致；拒 regex / glob 因 SDK 已 commit substring 行为）。
> - **`--json` schema 决策**（决策 3）：A 替代模式 + 输出 schema：
>   ```json
>   {
>     "exitCode": 0,
>     "passed": 1,
>     "failed": 0,
>     "total": 1,
>     "durationMs": 1234,
>     "cases": [
>       {"name": "screenshot only", "status": "passed", "durationMs": 1100}
>     ]
>   }
>   ```
>   - **schema 字段**：`exitCode: 0|1` (= `failed === 0 ? 0 : 1`) / `passed: number` / `failed: number` / `total: number` (= `passed + failed`，若 bail 则 `< registry.length` 表已 bail) / `durationMs: number` (整 run 总耗时) / `cases: Array<{name: string, status: 'passed'|'failed', durationMs: number, error?: SerializedFailure | {name: string, message: string}}>` / `bailed?: boolean` (仅 `--bail` 且真触发时填 `true`)
>   - **失败 case 的 `error` 字段**：`ExpectationFailure.toJSON()` 完整 schema (`{code, message, suggestions, visibleElements, hint?, screenshotPath?}` — `src/core/error.ts` line 89-95 既有 `toJSON(includeScreenshot = false)`，C5 字面调 `includeScreenshot: false`)；非 `ExpectationFailure` 的 generic Error 则 `{name: err.name, message: err.message}`（决策 3.b：A 同 schema 区分二态拒——schema 上 union 类型保证 jq consumer `.error.code // .error.name` 都可读）
>   - **顺序固定**：`cases` 数组按执行顺序（与 registry filtered subset 序，bail 时截断尾）
>   - **stdout = single-line JSON + '\n'**：`process.stdout.write(JSON.stringify(report) + '\n')`；**stderr = 0 字节** (决策 3.c)
> - **`--bail` 语义细化**（决策 4）：
>   - boolean flag (0-arg)，default `false`
>   - 单 case fail（throw 任何 error，含 `ExpectationFailure`）→ 立即终止主循环 → 未跑 case 不进 `cases` 数组
>   - 0 fail → bail 不触发 → 全跑 → 行为等价 default
>   - 与 `--grep` 联用 → 先 grep filter → 在 filtered subset 内 bail
>   - JSON 模式下 `bailed: true` 仅在真触发时填（决策 4.b：A 拒填 `bailed: false` 节省字节）
>   - **单 case 内多 expect 不受 bail 影响**：bail 是 case 级别非 expectation 级别（test 内 `expect.toBeVisible` 第 1 throw 已结束该 case，后续 expect 不执行——既有 v0.4 行为）
> - **`runRegistry` SDK 改动最小化**（决策 6）：仅扩 `RunOptions.bail?: boolean` + 主循环 catch 末 `if (opts.bail) break` 共 3-5 行；既有 13 个 grep/onCaseStart/onCaseEnd 用例 0 退化。**关键不变量**：`RunResult` schema `{passed, failed, failures}` 三字段 0 改动——bail 不改 result schema，只改主循环何时终止；`total` 由 CLI 层计算（`registry.length` filtered subset）而非 SDK 层（**决策 6.b：A SDK 不暴露 total，CLI 层从 grep filter 后 subset size 推**——SDK `run` 已字面无 total 字段，C5 不改 SDK 接口表面）。
> - **`--device` 行为锁 regression case**：`run.test.ts` line 191-214 既有 udid case 字面满足"udid 路径锁"；C5 加 1 case `'device selection: --device picks by name'` mockReturn 3 device with names → `select: { deviceName: 'iPhone 16 Pro' }` → 断言 launch udid 是该 name 对应的 udid——直接消费 v0.1 既有 pickCell 行为（决策 2 字面）。
>
> **C5 真生效 = 现状盘点详细补充（schema 例 + CLI args 例）**：
> - **`runCommand` args 扩展形态**（`src/cli/index.ts` line 31-55）：
>   ```ts
>   const runCommand = defineCommand({
>     meta: { name: 'run', description: '...' },
>     args: {
>       file: { type: 'positional', description: 'test file (TS)', required: true },
>       udid: { type: 'string', description: 'select device by udid' },
>       device: { type: 'string', description: 'select device by name' },
>       runtime: { type: 'string', description: 'select device by runtime identifier' },
>       grep: { type: 'string', description: 'filter case name by substring' },
>       json: { type: 'boolean', description: 'output single-line JSON instead of human text', default: false },
>       bail: { type: 'boolean', description: 'stop on first case failure', default: false },
>     },
>     async run({ args }) {
>       const client = new SimctlClient()
>       const res = await runRunCommand({
>         client,
>         file: args.file,
>         select: {
>           udid: args.udid,
>           deviceName: args.device,
>           runtimeIdentifier: args.runtime,
>         },
>         ...(args.grep !== undefined ? { grep: args.grep } : {}),
>         json: args.json === true,
>         bail: args.bail === true,
>         out: process.stdout,
>         err: process.stderr,
>         cwd: process.cwd(),
>       })
>       process.exit(res.exitCode)
>     },
>   })
>   ```
> - **`runRunCommand` JSON 输出 + bail 透传**（`src/cli/commands/run.ts` line 70-89 改）：
>   ```ts
>   const runStart = Date.now()
>   const caseRecords: Array<{ name: string; status: 'passed'|'failed'; durationMs: number; error?: unknown }> = []
>   const caseStartByName = new Map<string, number>()
>   try {
>     const result = await runRegistry({
>       driver: tracer,
>       ...(deps.grep !== undefined ? { grep: deps.grep } : {}),
>       bail: deps.bail === true,
>       onCaseStart: (name) => {
>         caseStartByName.set(name, Date.now())
>         const slug = slugify(name)
>         tracer.beginCase(slug)
>         setTraceSink({ traceRoot, caseSlug: slug })
>       },
>       onCaseEnd: (name, ok) => {
>         clearTraceSink()
>         const startedAt = caseStartByName.get(name) ?? Date.now()
>         caseRecords.push({ name, status: ok ? 'passed' : 'failed', durationMs: Date.now() - startedAt })
>       },
>     })
>     // attach error payloads (only for failed cases)
>     for (const f of result.failures) {
>       const rec = caseRecords.find((c) => c.name === f.name && c.status === 'failed')
>       if (rec !== undefined) {
>         rec.error = f.error instanceof ExpectationFailure
>           ? f.error.toJSON(false)
>           : { name: f.error.name, message: f.error.message }
>       }
>     }
>     const durationMs = Date.now() - runStart
>     const total = result.passed + result.failed
>     const exitCode = result.failed === 0 ? 0 : 1
>     if (deps.json === true) {
>       const report: Record<string, unknown> = {
>         exitCode,
>         passed: result.passed,
>         failed: result.failed,
>         total,
>         durationMs,
>         cases: caseRecords,
>       }
>       if (deps.bail === true && result.failed > 0) {
>         report['bailed'] = true
>       }
>       deps.out.write(JSON.stringify(report) + '\n')
>     } else {
>       deps.out.write(`${result.passed} passed, ${result.failed} failed\n`)
>       for (const f of result.failures) {
>         const rendered = f.error instanceof ExpectationFailure
>           ? f.error.toPrompt()
>           : `${f.error.name}: ${f.error.message}`
>         deps.err.write(`FAIL ${f.name}\n${rendered}\n`)
>       }
>     }
>     return { exitCode }
>   } finally {
>     await releaseCell(cell).catch(() => undefined)
>   }
>   ```
> - **`RunOptions.bail` + SDK 主循环 break**（`src/sdk/test.ts` line 28-75 改）：
>   ```ts
>   export type RunOptions = {
>     driver?: Driver
>     grep?: string
>     bail?: boolean    // <— C5 新增
>     onCaseStart?: (name: string) => void | Promise<void>
>     onCaseEnd?: (name: string, ok: boolean) => void | Promise<void>
>   }
>
>   // 主循环 catch 块末
>   } catch (err) {
>     result.failed++
>     result.failures.push({
>       name: c.name,
>       error: err instanceof Error ? err : new Error(String(err)),
>     })
>     await opts.onCaseEnd?.(c.name, false)
>     if (opts.bail === true) break   // <— C5 新增 1 行
>   }
>   ```
>
> **C1/C2/C3/C4 重要不变量延续（不许改）**：
> - `RunResult` schema `{passed: number, failed: number, failures: Array<{name, error}>}` 3 字段保（C5 不动 SDK result schema）
> - `RunOptions.driver` / `RunOptions.grep` / `RunOptions.onCaseStart` / `RunOptions.onCaseEnd` 4 字段保（C5 仅追加 `bail`）
> - `runRunCommand` 既有 13 case test 全保 byte-identical（包括 file-not-found / udid pick / matcher fail / jsonl / step-png）
> - 非 `--json` 模式输出 `${passed} passed, ${failed} failed\n` byte-identical（regression gate）
> - 非 `--json` 模式 stderr `FAIL ${name}\n${rendered}` byte-identical（既有失败渲染保）
> - `pickCell` 行为 byte-identical（line 95-122 0 改动）
> - `TracingDriver` 行为 byte-identical（0 改动）
> - `setTraceSink` / `clearTraceSink` 调用顺序 byte-identical（C5 仅在 `onCaseEnd` 内额外塞 caseRecords push 不破既有 lifecycle）
> - v0.4 acceptance 21 字段全保（`ts_tests >= 388` superset 自然过）
> - v0.5 C4 doctor schema 锁 0 退化

## 目标 checkpoint

**C5：`bun src/cli/index.ts run examples/screenshot-only.test.ts --json | jq -e '.passed >= 1 and .total >= 1 and (.cases | length) >= 1 and (.cases[0].status == "passed") and (.cases[0].durationMs | type == "number")'` exit 0；`bun src/cli/index.ts run examples/screenshot-only.test.ts --grep "nonexistent" --json | jq -e '.total == 0 and .passed == 0 and .failed == 0'` exit 0；`bun x vitest run src/cli/__tests__/run.test.ts src/sdk/__tests__/test.test.ts` exit 0 含 ~21-22 + ~13-19 = ~34-41 tests passed（既有 13 + ~7-8 新 C5 + 既有 sdk ~10-15 + ~3-4 新 bail）；`bash scripts/simx-c5-run-flags-smoke.sh` exit 0；`bash scripts/simx-v04-acceptance.sh` 21 字段 `ts_tests >= 388` 自然过 superset 不退化。**0 swift / 0 Driver interface / 0 Cell / 0 trace-sink 激活 / 0 dep 引入**。**

通过后世界变成：

- **`simx run` CLI flags 表面**（决策 1/3/4 字面）：

  ```
  Usage: simx run <file> [options]

  Options:
    --udid <udid>          select device by udid
    --device <name>        select device by name
    --runtime <id>         select device by runtime identifier
    --grep <substr>        filter case name by substring (case.name.includes(substr))
    --json                 output single-line JSON to stdout (replaces human text)
    --bail                 stop on first case failure
  ```

- **`--json` mode 输出 schema**（决策 3：A 替代 + 决策 3.b：A 同 schema error union）：

  ```json
  {
    "exitCode": 0,
    "passed": 1,
    "failed": 0,
    "total": 1,
    "durationMs": 1234,
    "cases": [
      {"name": "screenshot only", "status": "passed", "durationMs": 1100}
    ]
  }
  ```

  - 失败 case `error` 字段：`{code: 'ELEMENT_NOT_FOUND', message: '...', suggestions: [...], visibleElements: [...], hint?: '...'}` (ExpectationFailure.toJSON(false))，或 `{name: 'Error', message: '...'}` (generic Error)
  - `bailed: true` 仅在 `--bail` 且真触发时填（决策 4.b）

- **`--bail` 行为**（决策 4 字面 + 4.b 字面）：
  - boolean flag (0-arg)，default `false`
  - 单 case fail → 终止主循环 → 未跑 case 不入 cases 数组（决策 5：cases.length < total 隐含已 bail）
  - 0 fail → bail 不触发 → 全跑 → 行为等价 default

- **非 `--json` mode 输出 byte-identical 既有形态**（regression gate 锁）：

  ```
  1 passed, 0 failed
  ```

  失败：
  ```
  0 passed, 1 failed
  ```
  stderr:
  ```
  FAIL boom
  FAIL [ELEMENT_NOT_FOUND]: x
    hint: add a11y id
  ```

- **`RunOptions` 5 字段**（决策 6.a：A 拒 SDK 暴露 total）：

  ```ts
  export type RunOptions = {
    driver?: Driver
    grep?: string             // 既有，v0.1 wire
    bail?: boolean            // C5 新增
    onCaseStart?: (name: string) => void | Promise<void>
    onCaseEnd?: (name: string, ok: boolean) => void | Promise<void>
  }
  ```

- **`RunRunDeps` 10 字段**：

  ```ts
  export type RunRunDeps = {
    client: SimctlClient
    file: string
    select: DeviceSelect
    grep?: string             // C5 新增
    json?: boolean            // C5 新增
    bail?: boolean            // C5 新增
    out: WritableLike
    err: WritableLike
    cwd: string
  }
  ```

## 前置条件

```bash
# 1. v0.5 C4 已 close (doctor JSON CI-schema 锁落地)
test -f docs/plan-history/v0.5-c4-hot.md
test -x scripts/simx-c4-doctor-smoke.sh
bash scripts/simx-c4-doctor-smoke.sh > /tmp/c4-baseline.json
jq -e '.all_ok == "ok"' /tmp/c4-baseline.json > /dev/null

# 2. examples/screenshot-only.test.ts 字面在场（C5 smoke 依赖）
test -f examples/screenshot-only.test.ts

# 3. SDK runRegistry 既有 grep filter 已字面 wire
grep -qE "c\.name\.includes\(opts\.grep" src/sdk/test.ts

# 4. CLI runCommand 既有 3 device-select args 字面 wire
grep -qE "udid: \{ type: 'string'" src/cli/index.ts
grep -qE "device: \{ type: 'string'" src/cli/index.ts
grep -qE "runtime: \{ type: 'string'" src/cli/index.ts

# 5. ExpectationFailure.toJSON 既有
grep -qE "toJSON\(includeScreenshot" src/core/error.ts

# 6. v0.4 acceptance 21 字段当前全绿 baseline（C5 不能退化）
test -f scripts/simx-v04-acceptance.sh

# 7. 一个 booted dev sim 字面在场（smoke gate 真跑需要）
xcrun simctl list devices booted 2>&1 | grep -qE 'Booted'

# 8. plan-cold/v0.5.md C5 字面对应
grep -qE '^- \*\*C5\*\* `simx run` 增强' docs/plan-cold/v0.5.md
```

期望：每条 exit 0；任何一条非 0 = 拒绝热化 C5，回报上层。

## 步骤（线性，3 个 step）

### S1. SDK `runRegistry` 加 `bail` + CLI run command 加 3 flag + JSON 输出（type-driven，主体改）

**红（写测试）**

- 文件 1：`src/sdk/__tests__/test.test.ts`（既有 ~10-15 case；加 3-4 bail case）
- 追加 1 个新 `describe('C5: bail option (v0.5 C5)', () => { ... })` block ~3-4 case：
  1. `bail: true + first case fails → second case not invoked` — 注册 3 个 test (`'a'` pass, `'b'` throw, `'c'` pass) → run with `bail: true` → result.passed === 1 (just 'a') + result.failed === 1 ('b') + result.failures.length === 1 + 关键：第 3 个 'c' onCaseStart 0 次调用（mock spy 计数）
  2. `bail: false (default) → all cases run` — 同 3 test → run with `bail: false` (或不传) → 全 3 case 都跑 / passed === 2 + failed === 1 + 第 3 个 'c' onCaseStart 1 次调用
  3. `bail: true + 0 failure → all cases run (no-op)` — 注册 2 pass test → bail: true → 全跑 / passed === 2 + failed === 0
  4. `bail + grep 联用` — 注册 4 test (`a`/`b-fail`/`c-fail`/`d`) → run with `grep: 'fail', bail: true` → filtered 2 case → bail 在 'b-fail' (first match fails) 立停 → passed === 0 + failed === 1 + 'c-fail' 不跑（onCaseStart 0 次对 'c-fail'）
- 跑：`bun x vitest run src/sdk/__tests__/test.test.ts`
- 应看到红色：4 新 case 全 fail（`opts.bail` 字段不存在，主循环跑完全部 case）。

- 文件 2：`src/cli/__tests__/run.test.ts`（既有 13 case；加 7-8 C5 case）
- 追加 1 个新 `describe('C5: --grep + --json + --bail (v0.5 C5)', () => { ... })` block ~7-8 case：
  1. `--device picks by name (regression; v0.1 行为锁)` — 3 device `[{udid:'AAA',name:'iPhone 15'},{udid:'BBB',name:'iPhone 16 Pro'},{udid:'CCC',name:'iPad'}]`，select `{deviceName:'iPhone 16 Pro'}` → 断言 `client.__launchCalls[0]?.udid === 'BBB'`
  2. `--grep filters case name by substring` — 2 test (`'foo bar'` / `'baz qux'`) in file → `deps.grep = 'foo'` → 断言 out 含 `'1 passed, 0 failed'` 而非 `'2 passed'`；只有 'foo bar' case 跑（验证 trace dir slug 字面）
  3. `--grep 0 matches → 0 passed 0 failed exit 0` — `deps.grep = 'nonexistent'` → 断言 `res.exitCode === 0` + out 含 `'0 passed, 0 failed'`（no case → no failure）
  4. `--json happy → single-line valid JSON to stdout, stderr empty` — 1 pass case → `deps.json = true` → 断言 (a) `bufs.readErr() === ''`（决策 3.c stderr 静默）+ (b) `bufs.readOut()` 是单行 JSON + (c) `JSON.parse(out)` 字段 `{exitCode:0, passed:1, failed:0, total:1, durationMs: number, cases: [{name:'...', status:'passed', durationMs: number}]}` 全字段在场
  5. `--json fail → cases[0].error contains ExpectationFailure.toJSON schema` — fail with `ExpectationFailure({code:'ELEMENT_NOT_FOUND', ...})` → `deps.json = true` → 断言 (a) `res.exitCode === 1` + (b) `parsed.exitCode === 1` + (c) `parsed.failed === 1` + (d) `parsed.cases[0].error.code === 'ELEMENT_NOT_FOUND'` + (e) `parsed.cases[0].error.message` 是 string + (f) stderr === ''
  6. `--bail stops on first fail` — 3 test (`'pass1'` pass, `'fail-mid'` throw, `'pass2'` pass) → `deps.bail = true` → 断言 (a) `res.exitCode === 1` + (b) 非-json mode：out 含 `'1 passed, 1 failed'`（不是 `'2 passed, 1 failed'`，因为 pass2 未跑）+ (c) trace dir `'pass2'` 不存在（mkdir 不触发因 onCaseStart 不调用）
  7. `--bail + --json + --grep combined` — 4 test (`a` pass, `b-fail` throw, `c-fail` throw, `d` pass) → `deps.grep='fail', deps.bail=true, deps.json=true` → 断言 (a) `parsed.cases.length === 1` (only 'b-fail') + (b) `parsed.bailed === true` + (c) `parsed.total === 1` + (d) `parsed.failed === 1` + (e) `parsed.passed === 0`
  8. `non-json mode byte-identical regression` — 1 pass case → 不传 json → 断言 out **字面 toBe** `'1 passed, 0 failed\n'`（既有形态锁 byte-identical）
- 跑：`bun x vitest run src/cli/__tests__/run.test.ts`
- 应看到红色：~7-8 新 case 全 fail（grep/json/bail flag 未透传 / JSON schema 未实现 / stderr 未静默）。

**绿（实现）**

- 文件 1：`src/sdk/test.ts`
- 改 `RunOptions` 加 `bail?: boolean`（line 28-36 ~1 行新增）：
  ```ts
  export type RunOptions = {
    driver?: Driver
    grep?: string
    bail?: boolean
    onCaseStart?: (name: string) => void | Promise<void>
    onCaseEnd?: (name: string, ok: boolean) => void | Promise<void>
  }
  ```
- 改主循环 catch 末（line 64-72 加 1 行）：
  ```ts
  } catch (err) {
    result.failed++
    result.failures.push({
      name: c.name,
      error: err instanceof Error ? err : new Error(String(err)),
    })
    await opts.onCaseEnd?.(c.name, false)
    if (opts.bail === true) break    // C5 新增
  }
  ```
- 跑：`bun x vitest run src/sdk/__tests__/test.test.ts`
- 应看到绿色：~13-19 tests passed（既有 ~10-15 + ~3-4 新）。

- 文件 2：`src/cli/commands/run.ts`
- 改 `RunRunDeps` 加 3 字段（line 30-37 ~3 行）：
  ```ts
  export type RunRunDeps = {
    client: SimctlClient
    file: string
    select: DeviceSelect
    grep?: string
    json?: boolean
    bail?: boolean
    out: WritableLike
    err: WritableLike
    cwd: string
  }
  ```
- 改 `runRunCommand` 主体（line 70-89 ~25 行）：
  - 加 `runStart = Date.now()` + `caseRecords: Array<{...}>` + `caseStartByName: Map<string, number>` 在 try 块开头
  - 改 `runRegistry` 调用：`...(deps.grep !== undefined ? { grep: deps.grep } : {})` + `bail: deps.bail === true` + `onCaseStart` 加 `caseStartByName.set(name, Date.now())` + `onCaseEnd` 改 `(name, ok) => { clearTraceSink(); caseRecords.push({ name, status: ok ? 'passed' : 'failed', durationMs: Date.now() - caseStartByName.get(name)! }) }`
  - 改 result 处理：先 attach error payload 到对应 caseRecords (find by name + status:failed)；error 转换用 `f.error instanceof ExpectationFailure ? f.error.toJSON(false) : { name: f.error.name, message: f.error.message }`
  - 加 `if (deps.json === true) { ... JSON.stringify(report) + '\n' ... }` 分支替代既有 human stdout + stderr 渲染；非 json 分支保 byte-identical 既有 line 82-88 形态
  - `bailed: true` 仅在 `deps.bail === true && result.failed > 0` 时填
- 跑：`bun x vitest run src/cli/__tests__/run.test.ts`
- 应看到绿色：~20-22 tests passed（既有 13 + ~7-8 新）。

**重构**

- （可选）如 JSON 输出构造 inline 长 >25 行 → 抽 `buildJsonReport(result, caseRecords, total, durationMs, bailedTriggered): Record<string, unknown>` 同文件 helper。本 step 内若 inline 25 行内则跳过。
- （可选）`caseRecords` + `caseStartByName` 两个变量 → 合并为 `caseStateById: Map<string, {start: number, record?: CaseRecord}>`。本 step 内 2 变量可读则跳过。

### S2. CLI `runCommand` args 扩 3 个 + 透传到 `runRunCommand`

**红（写测试）**

- S1 已覆盖 `runRunCommand` 的 grep/json/bail 行为（单元测试 mock client）；S2 是 CLI args wiring，**单测意义低**（mock citty args = 等同 mock 整个 wiring 函数，与 S1 mock deps 等价）—— S3 e2e smoke gate 覆盖真 CLI args parse 路径。
- **S2 红 = 仅类型 check**：跑 `bun run typecheck`，应看到 `runRunCommand` 调用方 `RunRunDeps` 类型变（多 3 optional 字段，但 `args.json`/`args.bail` 在 `index.ts` line 39-54 未透传）—— 实际 optional 字段不传不报错，所以 typecheck 可能 pass。退化版红 = 实际跑 `bun src/cli/index.ts run examples/screenshot-only.test.ts --json` 应仍是人类 mode 输出（args.json 未连）。

**绿（实现）**

- 文件：`src/cli/index.ts`
- 改 `runCommand` args 加 3 字段（line 32-38 ~3 行）：
  ```ts
  args: {
    file: { type: 'positional', description: 'test file (TS)', required: true },
    udid: { type: 'string', description: 'select device by udid' },
    device: { type: 'string', description: 'select device by name' },
    runtime: { type: 'string', description: 'select device by runtime identifier' },
    grep: { type: 'string', description: 'filter case name by substring' },
    json: { type: 'boolean', description: 'output single-line JSON instead of human text', default: false },
    bail: { type: 'boolean', description: 'stop on first case failure', default: false },
  },
  ```
- 改 `runRunCommand` 调用（line 41-52 ~5 行）：
  ```ts
  const res = await runRunCommand({
    client,
    file: args.file,
    select: {
      udid: args.udid,
      deviceName: args.device,
      runtimeIdentifier: args.runtime,
    },
    ...(args.grep !== undefined ? { grep: args.grep } : {}),
    json: args.json === true,
    bail: args.bail === true,
    out: process.stdout,
    err: process.stderr,
    cwd: process.cwd(),
  })
  ```
- 跑：`bun run typecheck`
- 应看到绿色：exit 0，0 error。
- 跑：`bun src/cli/index.ts run examples/screenshot-only.test.ts --json | jq '.passed'`
- 应看到绿色：输出 `1`（或 ≥1 — 端到端 wiring 真通；本机 dev sim booted 前置 7 成立）。

**重构**

- 若 `--device` / `--udid` / `--runtime` 与 `--grep` / `--json` / `--bail` 两组 args 概念混淆 → 拆 `deviceSelectArgs` / `runFlagsArgs` 注释段。本 step 内若 inline 7 字段可读则跳过。

### S3. `scripts/simx-c5-run-flags-smoke.sh` e2e gate

**红（写测试）**

- 文件：`scripts/simx-c5-run-flags-smoke.sh` 不存在 → smoke 缺位 = C5 e2e gate 未落地。
- 跑：`bash scripts/simx-c5-run-flags-smoke.sh`
- 应看到红色：`No such file or directory` (exit ≠ 0)。

**绿（实现）**

- 新建 `scripts/simx-c5-run-flags-smoke.sh` (~60-80 行)：

  ```bash
  #!/usr/bin/env bash
  # simx-c5-run-flags-smoke.sh — v0.5 C5 `simx run --json` JSON CI-schema gate.
  # 真跑 `simx run examples/screenshot-only.test.ts --json` → 解析 JSON → 7 字段.
  set -euo pipefail
  ROOT="$(cd "$(dirname "$0")/.." && pwd)"
  cd "$ROOT"

  FIXTURE="examples/screenshot-only.test.ts"
  test -f "$FIXTURE" || { echo "missing fixture: $FIXTURE"; exit 1; }

  RAW="$(bun src/cli/index.ts run "$FIXTURE" --json 2>/dev/null || echo '{}')"

  # JSON parseable?
  JSON_PARSEABLE="fail"
  if echo "$RAW" | jq -e '.' > /dev/null 2>&1; then
    JSON_PARSEABLE="ok"
  fi

  EXIT_CODE="$(echo "$RAW" | jq -r '.exitCode // -1')"
  PASSED="$(echo "$RAW" | jq -r '.passed // -1')"
  FAILED="$(echo "$RAW" | jq -r '.failed // -1')"
  TOTAL="$(echo "$RAW" | jq -r '.total // -1')"
  CASES_LEN="$(echo "$RAW" | jq -r '.cases | length // -1')"

  PASSED_FIELD="fail"
  [[ "$PASSED" -ge 1 ]] && PASSED_FIELD="ok"

  FAILED_FIELD="fail"
  [[ "$FAILED" -ge 0 ]] && FAILED_FIELD="ok"

  TOTAL_FIELD="fail"
  [[ "$TOTAL" -ge 1 ]] && TOTAL_FIELD="ok"

  CASES_ARRAY_PRESENT="fail"
  [[ "$CASES_LEN" -ge 1 ]] && CASES_ARRAY_PRESENT="ok"

  # Per-case schema: each case has name + status + durationMs
  CASE_SCHEMA="fail"
  BAD="$(echo "$RAW" | jq '[.cases[] | select((.name | type) != "string" or (.status | type) != "string" or (.durationMs | type) != "number")] | length // -1')"
  [[ "$BAD" == "0" ]] && CASE_SCHEMA="ok"

  ALL_OK="fail"
  [[ "$EXIT_CODE" == "0" \
     && "$JSON_PARSEABLE" == "ok" \
     && "$PASSED_FIELD" == "ok" \
     && "$FAILED_FIELD" == "ok" \
     && "$TOTAL_FIELD" == "ok" \
     && "$CASES_ARRAY_PRESENT" == "ok" \
     && "$CASE_SCHEMA" == "ok" ]] && ALL_OK="ok"

  printf '{"exit_code":%s,"json_parseable":"%s","passed_field":"%s","failed_field":"%s","total_field":"%s","cases_array_present":"%s","case_schema":"%s","all_ok":"%s"}\n' \
    "$EXIT_CODE" "$JSON_PARSEABLE" "$PASSED_FIELD" "$FAILED_FIELD" "$TOTAL_FIELD" "$CASES_ARRAY_PRESENT" "$CASE_SCHEMA" "$ALL_OK"

  [[ "$ALL_OK" == "ok" ]] && exit 0 || exit 1
  ```

- `chmod +x scripts/simx-c5-run-flags-smoke.sh`
- 跑：`bash scripts/simx-c5-run-flags-smoke.sh`
- 应看到绿色：exit 0 + 8 字段 JSON `{"exit_code":0,"json_parseable":"ok","passed_field":"ok","failed_field":"ok","total_field":"ok","cases_array_present":"ok","case_schema":"ok","all_ok":"ok"}`。

**重构**

- 若 jq pipeline 长度难读 → 拆 `validate_run_json()` bash function。本 step 内若 60-80 行可读则跳过。

## Checkpoint C5 验收

```bash
# Gate 1：vitest run.test.ts + sdk/test.test.ts 全绿
bun x vitest run src/cli/__tests__/run.test.ts src/sdk/__tests__/test.test.ts
# 期望 exit 0；总数 ~34-41 passed（既有 13 + ~7-8 新 C5 run + 既有 ~10-15 + ~3-4 新 bail）

# Gate 2：typecheck 全绿（RunRunDeps + RunOptions 签名升级 + 调用方全适配）
bun run typecheck
# 期望 exit 0

# Gate 3：全测试套不退化
bun run test 2>&1 | grep -oE 'Tests +[0-9]+ passed' | head -1 | awk '{print $2}' | xargs -I{} test {} -ge 407
# 期望 exit 0（v0.5 C4 close 实测 ts_tests=407；C5 +10-12 case → ~417-419；如低于 407 = 既有退化）

# Gate 4：--json mode JSON CI-schema 真盘（happy path）
bun src/cli/index.ts run examples/screenshot-only.test.ts --json | jq -e '
  .exitCode == 0
  and (.passed >= 1)
  and (.failed == 0)
  and (.total >= 1)
  and (.durationMs | type == "number")
  and (.cases | length >= 1)
  and (.cases | all((.name | type) == "string" and (.status | type) == "string" and (.durationMs | type) == "number"))
  and (.cases[0].status == "passed")
' > /dev/null
# 期望 exit 0：JSON schema 严格合规 + 1 case 真过 + cases 每项 3 字段全 type-strict

# Gate 5：--grep 0 命中 → exit 0 + 0 passed / 0 failed / 0 total
bun src/cli/index.ts run examples/screenshot-only.test.ts --grep "nonexistent-filter" --json | jq -e '
  .exitCode == 0
  and .passed == 0
  and .failed == 0
  and .total == 0
  and (.cases | length == 0)
' > /dev/null

# Gate 6：--grep substring 命中 → 1 case 跑
bun src/cli/index.ts run examples/screenshot-only.test.ts --grep "screenshot" --json | jq -e '.passed >= 1 and (.cases | length >= 1)' > /dev/null

# Gate 7：非 --json mode 输出 byte-identical 既有形态（regression gate）
bun src/cli/index.ts run examples/screenshot-only.test.ts 2>&1 | grep -qE '^[0-9]+ passed, [0-9]+ failed$'

# Gate 8：C5 e2e smoke gate 退 0
bash scripts/simx-c5-run-flags-smoke.sh > /tmp/c5-smoke-out.json
jq -e '.all_ok == "ok" and .json_parseable == "ok" and .case_schema == "ok" and .cases_array_present == "ok"' /tmp/c5-smoke-out.json > /dev/null

# Gate 9：v0.4 acceptance 21 字段不退化（关键 regression gate）
bash scripts/simx-v04-acceptance.sh > /tmp/v04-acc-out 2>&1
jq -e '
  .swift_tests >= 102 and .ts_tests >= 388 and .typecheck == "ok"
  and .doctor_checks >= 6
  and .c2_matcher_failure == "ok" and .c3_suggestions == "ok"
  and .c4_steps_jsonl == "ok" and .c5_step_png == "ok"
  and .c6_failure_json == "ok" and .c7_pollfor_waitfor == "ok"
  and .c8_dogfood == "ok"
' /tmp/v04-acc-out > /dev/null

# Gate 10：v0.5 C1/C2/C3/C4 形态不退化（C5 不破前 4 checkpoint）
test -f docs/plan-history/v0.5-c1-hot.md
test -f docs/plan-history/v0.5-c2-hot.md
test -f docs/plan-history/v0.5-c3-hot.md
test -f docs/plan-history/v0.5-c4-hot.md
bash scripts/simx-c4-doctor-smoke.sh > /tmp/c4-regression.json
jq -e '.all_ok == "ok"' /tmp/c4-regression.json > /dev/null
grep -qE "verb: '\.save'" src/cli/commands/repl-parser.ts
test -f src/cli/commands/repl-codegen.ts

# Gate 11：C5 代码 anchor 落盘
grep -qE "grep\?: string" src/cli/commands/run.ts
grep -qE "json\?: boolean" src/cli/commands/run.ts
grep -qE "bail\?: boolean" src/cli/commands/run.ts
grep -qE "bail\?: boolean" src/sdk/test.ts
grep -qE "if \(opts\.bail === true\) break" src/sdk/test.ts
grep -qE "grep: \{ type: 'string'" src/cli/index.ts
grep -qE "json: \{ type: 'boolean'" src/cli/index.ts
grep -qE "bail: \{ type: 'boolean'" src/cli/index.ts
test -x scripts/simx-c5-run-flags-smoke.sh

# Gate 12：0 dep 引入（package.json deps 未变）
git diff --stat package.json bun.lock 2>/dev/null | grep -Ev '^$|files? changed' | wc -l | grep -q '^0$' || \
  (echo "package.json or bun.lock changed — verify 0 new dep"; git diff package.json bun.lock | head -30)

# Gate 13：0 swift / 0 driver / 0 cell / 0 core 改动（C5 是 CLI + registry 表面层）
git diff --stat swift-bridge/ src/driver/ src/sim/ src/core/ src/sdk/app.ts src/sdk/trace-sink.ts 2>/dev/null | grep -Ev '^$|files? changed' | wc -l | grep -q '^0$'

# Gate 14：plan-hot.md 形态合规（meta gate）
test -f docs/plan-hot.md
grep -qE '^# plan-hot — v0\.5 C5' docs/plan-hot.md
grep -qE '^## 目标 checkpoint' docs/plan-hot.md
grep -qE '^## 前置条件' docs/plan-hot.md
grep -qE '^## 步骤（线性，3 个 step）' docs/plan-hot.md
grep -qE '^## Checkpoint C5 验收' docs/plan-hot.md
grep -qE '^## 完成后动作' docs/plan-hot.md
```

期望：每条 exit 0。任何一条非 0 = C5 未通过，回到对应 step 修。

**Gate 4/5/6/8 注意**：真依赖本机 booted dev sim + examples/screenshot-only.test.ts 字面在场（前置 2/7 已校验）。`screenshot-only.test.ts` 字面 `await app.launch('com.apple.mobilesafari')` + `await app.screenshot()`——本机已 v0.1 起反复跑通；Gate 4 jq 表达式断言 `.cases[0].status == "passed"` —— 这是 C5 `--json` mode 完整 schema 字面证据。Gate 7 是关键 regression gate——非 `--json` mode 输出形态 byte-identical `1 passed, 0 failed\n` 不能破。Gate 9 是关键 regression gate——v0.4 acceptance 21 字段全保不退化是 C5 通过的硬条件。

## 完成后动作

1. **C5 close 时**：`mv docs/plan-hot.md docs/plan-history/v0.5-c5-hot.md`
2. **`docs/v1.md` 决策日志追加 1 行**（C5 close 记）：
   ```
   - **2026-05-1{X}** v0.5 C5：`simx run` 增强 4 flag (`--device` v0.1 既有 + `--grep <substr>` substring filter + `--json` single-line JSON 替代 + `--bail` 首 fail 立停)。**核心决策**：cold plan §C5 字面 4 flag → 现状盘点 (`--device` v0.1 既有 / `--grep` SDK 已 wire substring filter 仅未连 CLI flag / `--json` 缺 / `--bail` 缺) → C5 真生效 = 3 flag wire (`--grep` + `--json` + `--bail`) + 1 flag regression case 锁 (`--device` v0.1)。**14 决策落地**：grep substring (拒 regex/glob—与 SDK 既有 wire 一致) / --device v0.1 行为锁 regression case 拒新行为 / --json 替代 mode 拒双写 (cold plan §出口验收 jq '.passed' 字面要求纯 JSON stdout) / --json mode stderr 静默拒 FAIL 行染色 (CI consumer 100% 纯净) / --bail boolean 拒 `--bail N` 数值变体 (boolean 已 cover 90% TDD 场景) / cases.length < total 隐含已 bail 拒填 skipped status / 既有 13 run case 0 改动拒并行 / 拒删 / SDK 加 bail break 拒 CLI 层包 wrapper / `bailed: true` 仅触发时填拒填 false / RunResult schema 不动 (passed/failed/failures) total 由 CLI 推 (registry filtered subset size) / examples/screenshot-only.test.ts smoke fixture 拒 calc-two (C3 user 产物不在 C5 假设) / 不加新子命令 (boot 推 C6) / 不加 --reporter --parallel --watch --bail N 等扩展 flag / SDK + CLI 测试同 PR 算 1 改动条目。改 `src/cli/index.ts` (runCommand args +3 grep/json/bail + 透传 ~10 行)；改 `src/cli/commands/run.ts` (RunRunDeps +3 字段 + 主体 JSON 输出分流 +bail 透传 ~30-40 行)；改 `src/sdk/test.ts` (RunOptions +bail + 主循环 break ~3-5 行)；改测试 (`src/cli/__tests__/run.test.ts` C5 describe +7-8 case + `src/sdk/__tests__/test.test.ts` C5 bail describe +3-4 case)；新建 `scripts/simx-c5-run-flags-smoke.sh` (~60-80 行 8 字段 JSON CI-schema gate)。**0 swift / 0 Driver interface / 0 Cell / 0 trace-sink 激活 / 0 dep 引入 / 0 SimctlDriver / 0 matchers / 0 ExpectationFailure schema / 0 pickCell 改动**。测试基线 407 → ~417-419 TS / 102 swift / 21 字段 v0.4 acceptance 全保 (ts_tests >= 388 自然过 superset)。
   ```
3. **`docs/roadmap.md`** "当前所在位置"段：`正在做：v0.5（C5 已 close — plan-history/v0.5-c5-hot.md；详细见 docs/plan-cold/v0.5.md）；下一个 checkpoint：C6 (simx boot <device> 单独命令 + v0.5 close + scripts/simx-v05-acceptance.sh)`
4. **触发 §6 标准热化 prompt 生成 `docs/plan-hot.md` 覆盖 v0.5 C6**（main convo 调 sub-agent，附加 context：
   - C6 范围 = `simx boot <device>` 单独子命令 + v0.5 整体 close + `scripts/simx-v05-acceptance.sh` (C1-C6 6 smoke 聚合)
   - C6 现状 = simctl `bootAndWait` 既有 (`src/sim/simctl.ts`)；CLI 缺 `boot` 子命令；v0.5 acceptance script 待写
   - 不变量：swift 0 / Driver 0 / Cell 0 / dep 0 / v0.4 acceptance 21 字段全保
   - 复用：`src/sim/simctl.ts` (既有 `bootAndWait`) + `src/sim/cell.ts` (既有 `acquireCell`)

## 决策表

| # | 决策 | 选 | 拒绝路径 | 理由 |
|---|---|---|---|---|
| 1 | **`--grep` 匹配语义** | **A 字面 substring `case.name.includes(grep)`**（与 SDK `src/sdk/test.ts` line 55-57 既有 wire 一致） | B regex `new RegExp(grep).test(case.name)` (语义更强但破 SDK 既有约定；SDK 已 commit substring 行为 v0.1 起；引入 regex 等于 SDK 接口变 + 错误信息要含 regex parse 错处理) / C glob `minimatch(case.name, grep)` (违反 0 dep；glob 无标准库支持) / D 同时支持 3 种 + flag 切换 (3x 表面积；CLI flag 表面爆炸) | substring 已 cover 90% 用例（`--grep settings` 抓所有含 "settings" 的 case 名）；SDK 已字面 commit；C5 仅 wire CLI 不改 SDK 行为契约 |
| 2 | **`--device` 是新行为还是 regression case** | **A regression case**（v0.1 `pickCell` 已支持 `deviceName` 透传 `acquireCell({deviceName})`） | B 假设没有 + 重新实现 (浪费；既有 v0.1 已落地) / C 删既有 deviceName 路径换新接口 (破坏性 + 0 收益) | cold plan §C5 字面 `--device` 列在"4 flag 全部生效"——"生效"包含"已有但需 regression case 锁"；既有 udid case (line 191-214) 是同形态 anchor，加 1 deviceName case 闭环 |
| 3 | **`--json` mode 输出策略** | **A 替代既有 human stdout**（决策 3.a）+ **同 schema 区分 ExpectationFailure 与 generic Error 的 error 字段**（决策 3.b）+ **stderr 静默**（决策 3.c） | B 双写 (human + JSON to two streams：stdout = human, stderr = JSON — 反直觉 jq consumer 不 pipe stderr) / C JSON 到 `.simx/run-report.json` 不到 stdout (cold plan §出口验收 `bun ... --json \| jq '.passed'` 字面 stdout 需 JSON) / D --json + --quiet 双 flag (复杂度爆炸；--json 单 flag 已字面满足) / E error 字段二态 (`expectation_failure` 与 `error` 二字段) (jq consumer 写 `.cases[i].error.code // .cases[i].error.name` 难；同 schema union 更友好) | cold plan §出口验收 `jq '.passed'` 字面要求 stdout 纯 JSON；替代 mode 是最简单；stderr 静默让 CI consumer pipe 0 干扰 |
| 4 | **`--bail` 数值变体 vs boolean** | **A boolean (0-arg)** + **`bailed: true` 仅在 `--bail` 且真触发时填**（决策 4.b） | B `--bail N` 数值变体 (容忍 N 次 fail 后才停；TDD 场景几乎不用—TDD 是"第 1 fail 立修"；数值变体推 v0.7+) / C `--bail` 默认 1 + 接受数值 override (混合两种语义；CLI parser 复杂) / D 改 `--max-failures N` 替代 `--bail` (命名不直观；与 vitest/jest 既有 `--bail` 习惯冲突) | TDD 场景 90% 用法 = `--bail` boolean；数值变体推 v0.7+ 或 watch mode；`bailed: false` 节省 JSON 字节（cases.length < total 自然指示） |
| 5 | **bail 模式 cases 数组是否含未跑 case (status: 'skipped')** | **A 拒填 skipped；cases 仅含已跑 case**（cases.length < total 隐含已 bail） | B 填 `{name, status: 'skipped'}` 占位 (CI consumer 不关心 skipped 名字；增加 JSON 大小；与"cases 是执行结果"语义冲突——skipped 不是结果) / C 单独 `skipped: number` 字段 (信息冗余；total - cases.length 已自然推) | cases 数组是"已执行 case 的结果"；未跑 case 不应进；`bailed: true` flag 已字面指示 |
| 6 | **bail 实现层 (SDK vs CLI wrapper)** | **A SDK 内加 `RunOptions.bail` + 主循环 break**（**决策 6.a 字面**）+ **SDK 不暴露 total，CLI 层从 grep filtered subset 推**（**决策 6.b 字面**） | B CLI 层包 wrapper：CLI 内自己迭代 listTests + 单 case 单 case 调 runRegistry (需要复制 onCaseStart/onCaseEnd 钩子 + try/catch 复杂度 + grep filter 也要复制；最终是 SDK run 的 reimpl) / C SDK 加 bail + 返 total 字段 (RunResult schema 改；C5 不动 SDK 接口表面；total 由 CLI 推等价信息无成本) | SDK 内加 1 行 break 简洁；既有 SDK 13 个用例 0 退化；CLI 层 wrapper 是重复造轮子 |
| 7 | **既有 13 run case 改还是不改** | **A 0 改既有 13 case + 字面追加 C5 describe block** | B 改既有 case 加 grep/json 断言 (既有 case 是 happy/fail/file-not-found/udid/default-pick/matcher/jsonl/throw/readonly/step-png 等 v0.1-v0.4 累积 anchor；混入 C5 断言污染回归边界) / C 删 + 重写 (违反"既有不退化") / D 只删 trivial case 重写 (脆弱；选哪个 trivial 是主观决策) | 既有 13 case 全保不退化是硬约束；C5 字面追加 7-8 新 case 是 0 风险增量；保 case 总数稳定（既有 13 → 既有 13 + 7-8 = 20-21） |
| 8 | **C5 smoke gate 用 calc-two 还是 screenshot-only** | **A `examples/screenshot-only.test.ts`**（v0.3 C7 字面在场 + 不依赖 dev sim 复杂 UI） | B `examples/calc-two.test.ts` (cold plan §出口验收字面但**该文件不在场**——是 C3 `.save as <name>` REPL 假设用户产物；C5 不能假设其存在) / C 自家写 inline fixture (smoke 脚本 self-contained；不再依赖 examples) / D 用 `examples/login-tap.test.ts` (Settings UI 路径，UI 稳定性 v0.3 C7 已 noted iOS 26.4 sparse-tree regression；smoke 不应被 UI 噪声影响) | screenshot-only 是字面简单 `launch + screenshot` 路径 v0.1 起反复验过；CI-stable；cold plan §出口验收 `examples/calc-two.test.ts` 推 v0.5 close 时 C6 整体 acceptance 手工验（含 C3 + C5 联用） |
| 9 | **是否加 `simx boot <device>` 子命令** | **不加** (C6 范围) | A 加 + 推 C6 (复杂度推前；C5 严格 "run 增强 4 flag"；boot 是独立子命令非 run flag) / B 在 run 加 `--boot` flag 替代 boot 子命令 (语义混乱——boot 是 prep；run 是 execute) | cold plan §C5 严格 4 flag；C6 独立子命令是字面分工 |
| 10 | **是否加 `--reporter <name>` flag** | **不加** (推 v0.7+) | A 加 + 支持 tap / junit / json (--json 单 flag 已字面满足 cold plan §出口验收；多 reporter 是 CI 集成场景推 v0.7+) / B 加 + 仅支持 json (--reporter json 与 --json 等价；冗余) | --json 单 flag 是最小满足；reporter 多元化推 v0.7+ |
| 11 | **是否加 `--parallel <N>` parallel case 执行** | **不加** (v1.1+ Cell L4 并行调度字面) | A 加 (并行需要 Cell allocator + worker pool；v0.5 单 Cell 串行 simctl 串行；并行场景推 v1.1+) | v1.md §132 决策日志字面 "L4 并行调度 + matrix run 推 v1.1"；C5 严格 serial |
| 12 | **是否加 `--watch` watch mode** | **不加** (推 v1.1+) | A 加 + 文件监听 + auto-rerun (watch 需要 fs.watch + rerun queue + grep state 持久；推 v1.1+ watch mode 整体设计) | v1.md roadmap §22 字面 v1.1 = "Watch mode + Cell L4 并行调度"；C5 不预 watch |
| 13 | **是否改 examples/ 文件** | **不改** (C5 是 CLI 表面层) | A 加 `examples/c5-bail-demo.test.ts` 示范 bail 用法 (smoke gate 已字面覆盖；examples 是 user-facing—— C5 内部行为变化不需 user-facing 示范) | examples/ reorg 不在 C5 scope；smoke gate 真盘 cover |
| 14 | **改动文件计数** | **A 测试 2 文件 (`run.test.ts` + `test.test.ts`) 同 PR 算 1 改动条目**（≤ 5 条限制） | B 按测试文件分开计 (2 条 + index.ts + run.ts + test.ts + smoke = 6 条 超限) | 测试 + SDK 测试是一组逻辑变更 (bail SDK 行为 + bail CLI wiring) 同 PR 自然组合；改动条目按 "src 文件 + 同组测试 + 同组 fixture" 一组算 |

## 期望测试数

- 既有 baseline：**102 swift / 407 TS**（v0.5 C4 close 后实测；C4 close note 字面 407）
- C5 新增：
  - `src/sdk/__tests__/test.test.ts` +3-4 case（bail describe block：true/false/0-fail/grep-联用）
  - `src/cli/__tests__/run.test.ts` +7-8 case（C5 describe block：--device regression / --grep 命中 / --grep 0 hit / --json happy / --json fail / --bail / --bail+grep+json / non-json regression byte-identical）
  - **合计 +10-12 case**
- C5 close 时：**102 swift / ~417-419 TS**（既有全保不退化）
- C5 不加 swift case（决策硬约束）
- C5 不加 e2e smoke 字段到 v0.4 acceptance（v0.4 是冻结痕迹，决策 7 形态延续）；v0.5 整体 acceptance 推 C6 收尾

## v0.5 acceptance 字段（如新增）

**C5 不新增 v0.5 acceptance 字段**——`scripts/simx-v05-acceptance.sh` 整个脚本推 v0.5 close 时一并写（C6 close 后）。C1/C2/C3/C4 决策同形态延续。C5 close 时 v0.4 acceptance 21 字段保持不退化（`ts_tests >= 388` → ~417-419 自然过 superset；`doctor_checks >= 6` 自然过 superset 不动）即可。**C5 独立 e2e 痕迹** = `scripts/simx-c5-run-flags-smoke.sh` 8 字段 JSON (`{exit_code, json_parseable, passed_field, failed_field, total_field, cases_array_present, case_schema, all_ok}`)，v0.5 close 时 acceptance 整合该 smoke。

## 是否含 e2e

**含**——Gate 4/5/6/7/8/9 真本机 e2e 路径：
- Gate 4：`bun src/cli/index.ts run examples/screenshot-only.test.ts --json` 真跑 + jq 严格 schema 断言（exitCode/passed/failed/total/durationMs/cases 字段全 type-strict + cases[0].status == "passed"）
- Gate 5：`--grep "nonexistent"` 0 命中 → 0 passed/0 failed/0 total
- Gate 6：`--grep "screenshot"` 命中 → ≥1 passed
- Gate 7：非 `--json` mode byte-identical regression `1 passed, 0 failed\n`
- Gate 8：`scripts/simx-c5-run-flags-smoke.sh` 真跑 + 8 字段 JSON `{all_ok: "ok"}`
- Gate 9：v0.4 acceptance 21 字段 regression gate

理由：C5 涉及真 CLI args parse (citty `defineCommand`) + 真 JSON 输出 stream (`process.stdout.write`) + 真 SDK 主循环 break (`opts.bail`) + 真 dev sim launch/screenshot 路径，单测全 mock client 无法证明真 wiring（决策 6 字面：SDK 内 bail break 改动需 vitest unit 验，但 CLI args parse 走 citty 框架需 e2e 真盘 — citty 行为不应 mock）；e2e gate 是 C5 真生效的最终证明。本机已 v0.1 起反复跑通 `examples/screenshot-only.test.ts`，Gate 4/5/6/8 可真盘过；Gate 7 是关键 regression gate——非 `--json` mode 输出 byte-identical 不能破；Gate 9 是关键 regression gate——v0.4 acceptance 21 字段全保不退化是 C5 通过的硬条件。

## 与 cold plan 偏差

| cold plan 字面 | C5 实际范围 |
|---|---|
| "**simx run 增强**" | 锁定 = `--device` v0.1 regression case 锁 + 3 flag 真 wire (`--grep` SDK 已 substring 字面 + `--json` 替代 mode single-line JSON + `--bail` boolean SDK 主循环 break) |
| "**`--device`**" | 已 v0.1 落地 `pickCell` 支持 `select.deviceName`；C5 加 1 regression case `'device selection: --device picks by name'` 字面锁；0 实现改动 |
| "**`--grep`**" | C5 真生效：CLI flag `--grep <pattern>` 透传到 `RunOptions.grep` (SDK line 55-57 既有 substring filter 已 wire v0.1 起；C5 仅 wire CLI flag → SDK 0 接口改) |
| "**`--json`**" | C5 真生效：`--json` boolean flag → 替代 human stdout 为 single-line JSON `{exitCode, passed, failed, total, durationMs, cases: [{name, status, durationMs, error?}], bailed?}`；stderr 静默；非 `--json` mode byte-identical 既有形态 |
| "**`--bail`**" | C5 真生效：`--bail` boolean flag → 透传 SDK `RunOptions.bail` (新字段) → 主循环 catch 末 `if (opts.bail === true) break`；首 fail 立即停 + JSON `bailed: true` 仅触发时填 |
| "**全部生效**" | 4 flag 全部 wire + smoke gate 真盘；锁 case 总数稳定 (既有 13 + 7-8 = 20-21 run.test.ts，~10-15 + 3-4 = 13-19 test.test.ts) |
| cold plan §出口验收 `bun src/cli/index.ts run examples/calc-two.test.ts --json \| jq '.passed'  # 期望：1` | C5 smoke 用 `examples/screenshot-only.test.ts` 替代 `calc-two.test.ts`（决策 8：calc-two 是 C3 `.save as <name>` REPL 用户产物，C5 不能假设其在场；screenshot-only v0.3 C7 字面在场 CI-stable）；cold plan 整体出口推 v0.5 close 时 C6 整体 acceptance 手工验（含 C3 .save → C5 .json 联用） |
| `simx boot <device>` | C6 范围；C5 不动（决策 9 拒） |
| `--reporter <name>` / `--parallel <N>` / `--watch` / `--bail N` | 决策 10/11/12/4 拒绝（推 v0.7+ / v1.1+）；C5 严格 4 flag |
| watch mode / parallel cells | v1.1+ 字面（v1.md roadmap §22 line 22）；C5 严格 serial |
