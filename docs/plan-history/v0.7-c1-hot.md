# plan-hot — v0.7 C1：100-case 串行长跑不僵尸化（`RunnerSupervisor` 每 50 case 或 3 连健康 ping 失败重启 XCUITest runner / 保留 sim 不重启 / dev-sim 锁 enforce）+ `scripts/long-run-100.ts` 真本机 dev sim 串行 launch+terminate Settings 100 次 + 单行 JSON `{total:100, passed:100, failed:0, restarts, restart_reasons[], elapsed_ms, app_bundle_id:'com.apple.Preferences', dev_sim_udid}` 输出

> [3/4 信息层] 当前唯一热计划。**C1 是 v0.7 首个 checkpoint，也是 v1.md §4 Success Criteria [5]"长跑：100 case 串行连续跑不僵尸化、不漏 element"字面达成的工程实体**——通过后世界变成：(a) `src/sim/runner-supervisor.ts` 字面在场 —— `RunnerSupervisor` 纯逻辑类（不 spawn xcodebuild 自身；spawn 由 caller 注入；类自身只持有 case 计数 + ping 计数 + restart 计数 + 决策方法 `shouldRestart(): RestartReason | null` —— 决策 3.A 字面 — 纯逻辑可单测；spawn 副作用注入式 mock 形态字面 mirror C2 `BinarySpawner` 注入 pattern）+ 50-case boundary AND 3-consecutive-ping-fail 两个 restart trigger（决策 3.B 字面 — 与 cold plan §出口验收触发模板字面"每 50 case 或健康 ping 失败 3 次"双触发 OR 关系完全对齐）；(b) `src/sim/__tests__/runner-supervisor.test.ts` 字面在场 — 3 个 vitest case（决策 7.A 字面 — 决策表 7 字面 mock supervisor 重启计数 / ping retry / 50-boundary 计数 三 case；不混 e2e 形态）；(c) `scripts/long-run-100.ts` 字面在场 — 真本机 dev sim 串行 100 次 `xcrun simctl launch <udid> com.apple.Preferences` + `xcrun simctl terminate <udid> com.apple.Preferences` + 每 case 后 `curl /health` ping + RunnerSupervisor 决策是否 restart + restart 时 SIGTERM 既有 runner pid + 重启 xcodebuild test 后台 + 等 health ping 200 OK 最多 120s（决策 8.A 字面 — TS 形态 mirror `scripts/mcp-smoke.ts` v0.6 C8 形态；不 shell —— shell 难 mock supervisor 形态、不易测试；TS 用 bun 直跑）；(d) `scripts/simx-c1-longrun-100-smoke.sh` 字面在场 — wrapper shell 启 long-run-100.ts + jq 验 `.passed == 100 && .failed == 0 && .total == 100`（决策 8.B 字面 — shell wrapper 形态 mirror 既有 c[1-8] smoke 命名 `simx-c{N}-{topic}-smoke.sh` 形态；C1 v0.7 字面 `simx-c1-longrun-100-smoke.sh` 命名锁定 — v0.4 期间已有 `simx-c1-mcp-smoke.sh` v0.6 字面在场 / v0.4 期间已有 `simx-c1-mcp-smoke.sh`；C1 v0.7 不混淆 — 增 `-longrun-100` 后缀字面）；(e) **dev-sim 锁 enforce 字面达成** — long-run-100.ts udid 从 `.simx/dev-sim.txt` 字面读 + 不读 env / 不 fallback first-booted（决策 9.A 字面 — 用户重申"dev-sim 锁定继续有效"；hardcode 路径 `.simx/dev-sim.txt` 缺则 exit 2 — 不走 booted-fallback）。

> **承接（v0.6 整体 close 后实测 baseline，2026-05-16）**：v0.6 整体 ✅ done —— 568 TS case 全绿 + 102 swift case 全绿 + 27 MCP tool expose（C1 ping + 7+4+7+7+1）+ 35 字段 v0.6 acceptance 全 ok + `scripts/mcp-smoke.ts` e2e 15 passed_tools / 5 HID-bridge failed by SimctlDriver design + `docs/roadmap.md` 字面 v0.6 ✅ done / v0.7 🔥 hot + `docs/v1.md` 决策日志末尾 `2026-05-16 v0.6 整体 close —— ...`字面记 v1 cycle 进度 v0.1-v0.6 全 ✅ / v0.7 单 minor 剩。**进入 v0.7 前置三检字面全 ok**：
>
> 1. **v1.md 边界未膨胀字面验**（cold plan §触发模板字面"diff v1.md vs git history"——本机非 git 工作树，改 grep 字面检 §4 Success Criteria [5] line 89 字面 `[5] 长跑：100 case 串行连续跑不僵尸化、不漏 element` + §5 line 99 字面 `testmanagerd 长跑僵尸化 \| 每 50 case 自动重启 runner；health ping`——两条字面在场 ⇒ v1.md C1 边界条款冻结，未被 v0.6 期间偷加 / 偷减）；
> 2. **v0.6 e2e 字面仍通** — 本机 `bun scripts/mcp-smoke.ts > /dev/null && echo ok` 期望 `ok`（C1 前置条件 §1 字面跑）；
> 3. **dev-sim 锁字面继续有效** — `.simx/dev-sim.txt` 字面 `7B88259F-D864-4F98-9F2C-F0424216EE64` 字面存在 + 该 sim Booted 字面（`xcrun simctl list devices -j` 验 isAvailable=true && state=Booted）。
>
> **本机探测（C1 落地决策依据，2026-05-16）**：
>
> - `xcodebuild -version` 字面 `Xcode 26.4.1 / Build version 17E202`（C1 不再起新 xcode；既有 swift-bridge/SimxRunner.xcodeproj v0.2 起冻结）
> - `xcrun simctl list devices -j` 字面 `7B88259F-D864-4F98-9F2C-F0424216EE64 iOS 26.4 iPhone 17 Pro Booted` 字面单一 booted —— dev-sim 锁字面单点
> - `xcrun simctl listapps 7B88259F-D864-4F98-9F2C-F0424216EE64 | grep CFBundleIdentifier` 字面**含 `com.apple.Preferences` Settings；不含 `com.apple.calculator` Calculator**（与 memory `ios-settings-50-limit-truncation` 字面对齐：iOS 26.4 dev sim 上 Calculator 未预装；Settings 是 v0.4 以来 examples / scripts 字面统一 fixture，21 处 `com.apple.Preferences` 字面引用横跨 mcp-smoke.ts / simx-c[1-8]-*.sh / simx-runner-tap-smoke.sh / simx-c3-save-e2e-smoke.sh / examples 全线）→ **C1 落地形态字面用 Settings 替代 cold plan §触发模板字面"Calculator（无外部依赖）足够"**（决策 1.A 字面 — Calculator 拒绝路径"本机缺 Calculator app 预装 ⇒ 走 Settings"； 必须在决策日志 +1 行字面记 cold plan 偏差，决策 10.A 字面）
> - `curl -fsS -m 1 http://127.0.0.1:22087/health` 字面**当前 fail 7（connection refused）** —— runner **未起**字面；本机当前 baseline = runner-off；long-run-100.ts 必须先**自起** runner（决策 4.A 字面 — long-run-100.ts 启动序 `(0) udid 读 + lock 校 / (1) spawn runner xcodebuild test 后台 / (2) poll /health 120s 至 200 OK / (3) 进入 100-case 循环 / (4) 每 case 后 ping / (5) supervisor.shouldRestart() 决策 restart`；不依赖 caller 预启 runner —— 自含形态字面 mirror `scripts/simx-runner-health.sh` 序列）
> - `scripts/simx-runner-health.sh` 字面已有 runner 启动模板（line 40-44 `xcodebuild -project ... -scheme SimxRunner -destination 'platform=iOS Simulator,id=$SIMX_UDID' -only-testing:SimxRunnerUITests/SimxRunnerUITests/test_runForever test &`）+ /health poll 120s 字面（line 56-69）→ long-run-100.ts 字面**内联**此启动序为 TS 函数 `startRunner(udid): Promise<{pid:number}>`（决策 8.C 字面 — 不调 shell；TS 内 `spawn('xcodebuild', [...])` 字面 mirror shell；shell 仅作设计参考；long-run-100.ts 形态自包含）
> - `src/sim/runner-client.ts` 字面 `RunnerClient.health()` 真返 `boolean`（line 49-56 `await this.fetchImpl(\`${this.base}/health\`)` + `res.ok && res.status === 200`）→ long-run-100.ts **复用** `RunnerClient` 一次（决策 4.B 字面 — 0 dep；既有 src/ 实现 0 改动；类直接 import）+ `RunnerClient.health()` 每 case 后调一次 ping
> - `src/sim/dev-lock.ts` 字面 `readDevSimLock(cwd)` + `assertDevSimLock(udid, cwd)` 字面 export（line 18 + line 43）→ long-run-100.ts 字面**复用** `readDevSimLock` 拿 udid + `assertDevSimLock(udid)` enforce（决策 9.A 字面 — dev-lock graft v0.6 hot-fix 复用，0 改 dev-lock.ts；udid 字面读 .simx/dev-sim.txt fallback exit 2 不走 first-booted）
> - `src/sim/__tests__/` 字面已有 5 既有 test file（cell / dev-lock / runner-client / session / simctl）→ 新建 `runner-supervisor.test.ts` 字面在 `__tests__/` 同级（决策 7.A 字面 — 同模式扩 1 新 test 文件不破坏既有 5 + integration/ 子目录）
> - **既有 568 TS case baseline 字面不退化**：C1 新加 3 vitest case → C1 close baseline = 571 TS case 字面；C1 期间 0 既有 case 改动 / 0 既有 src/ 文件改动；568 → 571（+3）字面（决策 7.B 字面）
> - **既有 102 swift case baseline 字面 0 改动 / 0 swift-bridge/ 文件改动**（决策延续 — v0.7 全期严控 swift 0 改）
> - `package.json` deps 字面 `citty` + `@modelcontextprotocol/sdk@1.29.0` + peer `zod@^3.25.0` 字面 3 dep 不变（决策 6.A 字面 — 0 新 dep；TS spawn 用 `node:child_process` stdlib / curl 用 stdlib fetch / supervisor 用纯 TS）
> - **`scripts/mcp-smoke.ts` 字面 v0.6 C8 在场**：long-run-100.ts 字面**形态 mirror** mcp-smoke.ts —— top-level async IIFE / dev-sim udid 读 / 单行 JSON stdout / `process.exit(0|1|2)` 三态退出码（决策 8.A 字面）
> - **runner 启动耗时实测预估**：`scripts/simx-runner-health.sh` 字面 poll up to 120s（line 56） —— 冷启字面预算 30-90s；本机 26.4 dev sim 预装 SimxRunner 后字面 ~5-15s（C2 实测）；long-run-100.ts 字面用 120s 字面 timeout cap mirror shell（决策 4.C 字面）
> - **case 形态字面冻结**：每个 case = `simctl launch ${udid} com.apple.Preferences` + 1s sleep + `simctl terminate ${udid} com.apple.Preferences`（决策 2.A 字面 — A 路径 pure launch+terminate；不 tap / 不 screen_describe / 不 element-inspect —— 决策 2.B/2.C 字面拒绝路径：B tap+assert 需 runner-started 全程不能 restart 期间空窗 vs 形态字面预期"重启期间继续计数"会撞死；C 混合 5 种 action 各 20 次会引入 selector / matcher 二阶 variance —— C1 边界字面"长跑稳定" ≠ "interaction 覆盖率"，后者 v0.6 C8 mcp-smoke.ts 字面已覆 ≥15 tool e2e）+ **每 case 边界 `RunnerClient.health()` ping 一次**（决策 5.A 字面 — 每 case 后 ping；不 case 内 ping —— 形态过密反而干扰；不每 N case ping —— supervisor 字面"3 连 fail"需要每 case 信号）
> - **僵尸化检测字面冻结**：A 路径 — ping 非 200 / 超时（决策 5.B 字面）+ supervisor 字面"3 连 ping fail" trigger restart；不监 case fail trigger restart（决策 5.C 字面拒 B 路径 — case fail 是结果不是触发；case fail 时 supervisor 仍按 case-counter / ping-counter 决策；case 失败直接 `total fail`）+ 不监 runner 进程 `kill -0`（决策 5.D 字面拒 C 路径 — 进程 kill -0 在 macOS 上对 xcodebuild test 子进程不可靠；ping 200 OK 是更强信号；既有 `scripts/simx-runner-health.sh` line 56 字面 ping-only 形态 mirror）
> - **失败处理字面冻结**：case fail（launch / terminate 非 0 / supervisor restart 失败）即整体 fail —— `total - passed > 0` ⇒ `exit 1`（决策 6.B 字面 — cold plan §出口验收 line 44 `grep -c "passed" /tmp/longrun.log` 期望 100，任何 fail 即整体 fail）；restart-during-100 是正常事件不算 case 失败（决策 6.C 字面 — restart_reasons[] 记 restart 次数与原因；passed 计数不退）
> - **e2e 时长字面预估**：cold start runner ~30-90s + 100 case × (1-2s launch + 1s sleep + 1s terminate + 50ms ping) ≈ 100 × 3.5s = 350s + 2 restart × 60s = 470s ≈ 8 分钟（决策 8.D 字面 — 与 cold plan §已知风险字面"长跑测试本身耗时：100 case smoke 大概 5-10 分钟" line 21 字面对齐）
>
> **现状盘点（C1 真生效边界）**：
>
> - **新建 `src/sim/runner-supervisor.ts`（~80-120 行）**：`export type RestartReason = 'every-50-cases' | 'health-ping-fail-3x'` + `export interface RunnerSupervisorOptions { restartEveryNCases: number; restartAfterNPingFails: number }`（默认 50 / 3 字面 mirror cold plan）+ `export class RunnerSupervisor` 含 `private caseCount = 0` / `private consecutivePingFails = 0` / `private restartCount = 0` / 方法 `recordCase(): void`（增 caseCount）/ `recordPing(ok: boolean): void`（ok=true reset consecutivePingFails；ok=false ++）/ `shouldRestart(): RestartReason | null`（返 `'every-50-cases'` if `caseCount > 0 && caseCount % restartEveryNCases === 0`；返 `'health-ping-fail-3x'` if `consecutivePingFails >= restartAfterNPingFails`；否则 null）/ `recordRestart(): void`（++restartCount + reset consecutivePingFails；caseCount 不 reset —— 决策 3.C 字面 — caseCount 是累计；50-boundary 由 `% 50 === 0` 自然触发；不 reset 避免 boundary 漏）/ getter `restarts(): number` + `cases(): number` + `pingFails(): number`
> - **新建 `src/sim/__tests__/runner-supervisor.test.ts`（~80 行 / 3 vitest case）**：
>   1. `case 50-boundary trigger`：supervisor with restartEveryNCases=50；49 次 recordCase + shouldRestart() = null；50 次 recordCase → shouldRestart() === 'every-50-cases'
>   2. `ping fail 3x trigger`：supervisor with restartAfterNPingFails=3；2 次 recordPing(false) → shouldRestart() = null；3 次 → shouldRestart() === 'health-ping-fail-3x'；4 次 recordPing(true) reset → shouldRestart() = null
>   3. `restart counter monotonic`：recordRestart() × 2 → restarts() === 2；reset consecutivePingFails；caseCount 不退（recordCase ×3 后 cases() === 3）
> - **新建 `scripts/long-run-100.ts`（~150-200 行）**：top-level async IIFE / `import { readDevSimLock, assertDevSimLock } from '../src/sim/dev-lock.js'`（路径相对 scripts/）/ `import { RunnerClient } from '../src/sim/runner-client.js'` / `import { RunnerSupervisor } from '../src/sim/runner-supervisor.js'` / `import { spawn, spawnSync } from 'node:child_process'` / 函数 `startRunner(udid: string): Promise<{ pid: number; logFile: string }>`（spawn xcodebuild test 后台 / 写 .simx/runner/longrun-$$.log / 不 await） / 函数 `waitHealth(client, timeoutMs=120000): Promise<boolean>`（loop 1s 间隔 client.health() 至 true 或 timeout） / 函数 `stopRunner(pid: number): void`（kill -TERM + pkill -P pid） / 函数 `runCase(udid, bundleId): boolean`（spawnSync simctl launch + sleep 1s + spawnSync simctl terminate；任何 exit !=0 返 false） / main：(0) udid = readDevSimLock || exit 2 / assertDevSimLock(udid) / (1) start = Date.now() / (2) runnerPid = startRunner(udid).pid / (3) client = new RunnerClient({port:22087}) / await waitHealth(client) || exit 1 / (4) supervisor = new RunnerSupervisor({restartEveryNCases:50, restartAfterNPingFails:3}) / restartReasons = [] / for i=1..100: ok = runCase(udid, 'com.apple.Preferences'); supervisor.recordCase(); pingOk = await client.health(); supervisor.recordPing(pingOk); reason = supervisor.shouldRestart(); if (reason): stopRunner(runnerPid); runnerPid = (await startRunner(udid)).pid; await waitHealth(client); supervisor.recordRestart(); restartReasons.push({at_case:i, reason}); if (!ok) failed++; else passed++; / (5) stopRunner finally / (6) console.log(JSON.stringify({total:100, passed, failed, restarts:supervisor.restarts(), restart_reasons:restartReasons, app_bundle_id:'com.apple.Preferences', dev_sim_udid:udid, elapsed_ms:Date.now()-start})) / process.exit(passed === 100 && failed === 0 ? 0 : 1)
> - **新建 `scripts/simx-c1-longrun-100-smoke.sh`（~30 行）**：mirror `simx-c[1-8]-*-smoke.sh` 形态 — `set -euo pipefail` + cd ROOT + `OUT=$(bun scripts/long-run-100.ts)` + `echo "$OUT" | tee /tmp/longrun-100.json` + `test "$(echo $OUT | jq '.total')" = '100'` + `test "$(echo $OUT | jq '.passed')" = '100'` + `test "$(echo $OUT | jq '.failed')" = '0'` + `echo '{"all_ok":true}'`（决策 8.B 字面 — 形态 mirror v0.6 6 个 mcp smoke shell wrapper）
> - **0 swift / 0 Driver interface / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/ 文件改 / 0 dep / 0 既有 27 MCP tool 改 / 0 既有 568 vitest case 改 / 0 既有 6 v0.6 mcp smoke 改 / 0 既有 5 v0[1-5] acceptance 改 / 0 examples 改 / 0 既有 c[1-8] smoke 改 / 0 v0.7 C2-C7 任何代码 / 0 CI workflow yaml / 0 plugin manifest / 0 runtime 矩阵脚本 / 0 v1 acceptance script 0 字段加**
>
> **C1 真生效 = 四项改动（≤6 字面任务约束严守）**：
>
> 1. **新建 `src/sim/runner-supervisor.ts`**（~80-120 行）— 核心 1（纯逻辑 supervisor 类）
> 2. **新建 `src/sim/__tests__/runner-supervisor.test.ts`**（~80 行 / 3 vitest case）— 核心 2
> 3. **新建 `scripts/long-run-100.ts`**（~150-200 行）— 核心 3（真本机 e2e driver script）
> 4. **新建 `scripts/simx-c1-longrun-100-smoke.sh`**（~30 行）— 核心 4（shell wrapper + jq gate）
>
> **核心改动 = 4 条 ≤ 6 字面满足**。
>
> **C1 全部不在范围**（拒绝路径）：
>
> - 写 C2 CI workflow `.github/workflows/*.yml`（决策 11.A 字面 — C2 范围；C1 范围内**不**起 CI）
> - 写 C3 runtime 矩阵任何 17.5 / 18.4 配置（决策 11.B 字面 — C3 范围；C1 单 runtime iOS 26.4 字面）
> - 写 C4 `simx doctor --json` 任何 schema 改（决策 11.C 字面 — C4 范围）
> - 写 C5 Claude Code plugin manifest `claude-plugin.json`（决策 11.D 字面 — C5 范围）
> - 写 C6 v1 acceptance script `scripts/v1-acceptance.sh`（决策 11.E 字面 — C6 范围；不要把 cold plan §出口验收 line 51 `scripts/v1-acceptance.sh` 字面提前到 C1）
> - 写 C7 README / docs/ v1.0 发布更新（决策 11.F 字面 — C7 范围）
> - 改 `src/driver/` / `src/sdk/` / `src/core/` / `src/cli/` / `src/mcp/` 任何文件（决策延续 0 改动）
> - 改 `swift-bridge/` 任何文件 / 加 swift case（决策延续 0 改动）
> - 改 `src/sim/runner-client.ts` / `src/sim/dev-lock.ts` / `src/sim/cell.ts` / `src/sim/session.ts` / `src/sim/simctl.ts` / `src/sim/a11y-tree-source.ts` 任何文件（决策延续 0 改动；仅**复用** `RunnerClient` + `readDevSimLock` + `assertDevSimLock` 字面 import）
> - 改既有 27 ToolDef handler 行为 / 改 `src/mcp/server.ts` / 改 `src/mcp/tools.ts` / 改 `src/mcp/index.ts` / 改 `src/core/schemas.ts`（决策延续 0 改动）
> - 改既有 6 v0.6 mcp smoke 字段（决策延续 0 改动）
> - 改既有 5 v0[1-5] acceptance（v0.5 / v0.4 / v0.3 / v0.2 / v0.6 冻结）
> - 改 `scripts/mcp-smoke.ts`（v0.6 C8 artifact 冻结）
> - 加新 npm dep / peer dep（决策 6.A 字面）
> - 加新 CLI flag / env 变量（决策 6.B 字面 — long-run-100.ts 字面 0 flag 0 env；udid 字面读 .simx/dev-sim.txt）
> - 改 `docs/roadmap.md`（决策 10.B 字面 — C1 不动 roadmap；roadmap v0.7 🔥 hot 字面已设；C7 close 时整体翻 v0.7 ✅）
> - 改 `docs/v1.md` 任何边界字段（v1.md §2 模块表 / §4 Success Criteria / §5 风险表 / §6 工作量 全冻结；决策延续；C1 close 仅决策日志 +1 行字面"cold plan Calculator 字面 → 实际 Settings 字面"偏差 + 1 行"v0.7 C1 close"）
> - 改 `docs/plan-cold/v0.7.md`（cold plan 冻结；C1 是 cold plan 的 hot 化产物，不回写 cold plan —— 决策 10.C 字面）
> - 在 long-run-100.ts 跑 tap / screen_describe / 任何 MCP tool（决策 2.B/2.C 字面拒）
> - 在 long-run-100.ts 用 `SIMX_DEV_LOCK=off` env / 任何 udid override（决策 9.A 字面 — dev-lock graft 严格 enforce）
> - 在 long-run-100.ts 用 Calculator bundle id（决策 1.A 字面 — Calculator 缺；用 Settings）
> - 加 unit vitest case for `scripts/long-run-100.ts`（决策 7.C 字面 — script 是 e2e driver；只 supervisor 测；568 → 571 字面）
> - 改 `package.json` dependencies（决策 6.A 字面）
> - 在 long-run-100.ts 跑 simulator_boot / shutdown sim（决策 9.B 字面 — 保 sim 不重启字面 cold plan §出口验收触发模板字面"保留 sim 不重启"；assume dev sim 已 Booted）
> - 加 `scripts/long-run-100.ts` 的 `--repeat N` flag / `--app <bundleId>` flag（决策 6.B 字面 — 0 flag；hardcode 100 / com.apple.Preferences）
> - 在 supervisor 加 retry / backoff 逻辑（决策 3.D 字面 — supervisor 是纯决策；retry 由 caller —— long-run-100.ts 内 waitHealth 120s 字面）
> - 在 supervisor 加 max-restart cap（决策 3.E 字面 — 100 case / 50 boundary ⇒ 至多 2 next-boundary restart + 任意 ping-fail restart；不加 cap；超 N restart 不算异常）
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / SimctlClient 0 / dep 0 / wire payload 0 改 / 既有 27 ToolDef 0 / 既有 server.ts 0 / 既有 tools.ts 0 / 既有 index.ts 0 / 既有 schemas.ts 0 / 既有 examples 0 / 既有 6 mcp smoke 0 字段改 / 既有 5 v0[1-5] acceptance 0 改 / v0.6 mcp-smoke.ts 0 改 / 既有 568 vitest case 0 改 / runner-client.ts 0 改 / dev-lock.ts 0 改 / cell.ts 0 改 / session.ts 0 改 / simctl.ts 0 改 / a11y-tree-source.ts 0 改 / v1.md 边界 0 改 / roadmap.md 0 改 / plan-cold/v0.7.md 0 改 / package.json 0 改 / CLI flag 0 加 / env 变量 0 加 / src/driver/ 0 改 / src/sdk/ 0 改 / src/core/ 0 改 / src/cli/ 0 改 / src/mcp/ 0 改 / swift-bridge/ 0 改 / scripts/mcp-smoke.ts 0 改 / scripts/simx-c[1-8]-*.sh 0 改 / scripts/simx-runner-*.sh 0 改 / scripts/simx-axp-*.sh 0 改 / scripts/simx-v0[1-6]-acceptance.sh 0 改**。允许改动 / 新建文件范围（**核心 ≤ 6 条**字面任务约束）：
>
> 1. **新建 `src/sim/runner-supervisor.ts`**（~80-120 行）— 核心 1
> 2. **新建 `src/sim/__tests__/runner-supervisor.test.ts`**（~80 行 / 3 vitest case）— 核心 2
> 3. **新建 `scripts/long-run-100.ts`**（~150-200 行）— 核心 3
> 4. **新建 `scripts/simx-c1-longrun-100-smoke.sh`**（~30 行）— 核心 4
>
> **不允许**：(a) 改 `src/driver/` / `src/sdk/` / `src/core/` / `src/cli/` / `src/mcp/` 任何文件；(b) 改 `swift-bridge/`；(c) 改任何 `examples/*.ts`；(d) 改 `scripts/simx-v0[1-6]-acceptance.sh` / 6 个 mcp smoke / 其他 c[1-8] smoke / `simx-c8-dogfood-smoke.sh` v0.4 历史 smoke / `scripts/mcp-smoke.ts`；(e) 引第 4 个 npm dep；(f) 加 CLI flag / env 变量；(g) 写 plan-cold/v0.7.md / 写 C2-C7 任何代码 / 改 roadmap.md / 改 v1.md 边界；(h) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient` / `RunnerClient` / `DevSimLockViolation` / `readDevSimLock`；(i) 改 ScreenDescription / SerializedFailure / FailureCode / Role / Rect / ElementSummary / A11yNode 字段；(j) 在 long-run-100.ts 跑 tap / screen_describe / 任何 MCP tool；(k) 在 long-run-100.ts 跑 simulator_boot / shutdown / Calculator bundle；(l) 在 supervisor 加 retry / max-restart cap；(m) 加 `--repeat` / `--app` flag。

## 目标 checkpoint

**C1**：通过后世界变成 — (a) `bun x vitest run src/sim/__tests__/runner-supervisor.test.ts` 字面 exit 0 + `3 passed` 字面；(b) `bun x vitest run` 字面 exit 0 + 输出含 `Tests 571 passed` 字面（568 + 3 = 571 字面 baseline 切换）；(c) `bun scripts/long-run-100.ts` 字面 exit 0 + 单行 JSON stdout + `.total == 100` + `.passed == 100` + `.failed == 0` + `.app_bundle_id == 'com.apple.Preferences'` + `.dev_sim_udid == '7B88259F-D864-4F98-9F2C-F0424216EE64'` + `.restarts >= 1`（必有 2 次 50-boundary restart 字面 — at_case=50 / at_case=100）+ `.restart_reasons` 数组含 `every-50-cases` 至少 1 元素 + `.elapsed_ms < 720000`（12 分钟 hard cap，cold plan 字面"5-10 分钟" + 20% 余量）；(d) `bash scripts/simx-c1-longrun-100-smoke.sh` 字面 exit 0 + 末行 `{"all_ok":true}` 字面；(e) 568 → 571 TS case baseline 字面切换；6 v0.6 mcp smoke + 5 v0[1-5] acceptance + v0.6 acceptance + mcp-smoke.ts 全不退化（0 改动字面验）；102 swift case 不退化（0 改 swift-bridge/）；27 MCP tool expose 不退化（0 改 src/mcp/）。

- `bun x vitest run src/sim/__tests__/runner-supervisor.test.ts` 字面 exit 0 + `3 passed` 字面
- `bun x vitest run` 字面 exit 0 + `Tests 571 passed` 字面（568 → 571）
- `bun scripts/long-run-100.ts > /tmp/longrun-100.json` 字面 exit 0 + `.total == 100 && .passed == 100 && .failed == 0` 字面
- `bash scripts/simx-c1-longrun-100-smoke.sh` 字面 exit 0 + `{"all_ok":true}` 字面末行
- `bun run typecheck` 字面 exit 0
- 0 swift / 0 src/{driver,sdk,core,cli,mcp} / 0 既有 src/sim/ 5 文件 / 0 既有 6 mcp smoke / 0 既有 5 v0[1-5] acceptance / 0 v0.6 acceptance / 0 mcp-smoke.ts / 0 examples / 0 dep / 0 roadmap.md / 0 v1.md 边界 / 0 cold plan v0.7 改 字面

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.6 整体 close 痕迹存在
test -f docs/plan-history/v0.6-c8-hot.md
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh

# v0.6 e2e 字面仍通（cold plan §入口条件字面）
bun scripts/mcp-smoke.ts > /dev/null && echo ok

# v1.md C1 边界条款字面在场（未被偷加 / 偷减）
grep -qE '^\[5\] 长跑：100 case 串行连续跑不僵尸化、不漏 element$' docs/v1.md
grep -qE 'testmanagerd 长跑僵尸化.*每 50 case 自动重启 runner；health ping' docs/v1.md

# roadmap.md v0.7 🔥 / v0.6 ✅ 字面
grep -qE '^\| \*\*v0\.6\*\* \| ✅ done \|' docs/roadmap.md
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# dev-sim 锁字面继续有效
test -f .simx/dev-sim.txt
LOCKED_UDID="$(cat .simx/dev-sim.txt | tr -d '[:space:]')"
test -n "$LOCKED_UDID"
xcrun simctl list devices -j | jq -e --arg u "$LOCKED_UDID" '
  [.devices | to_entries[] | .value[] | select(.udid==$u and .isAvailable==true and .state=="Booted")] | length == 1
' > /dev/null

# Calculator 字面缺 / Settings 字面在（cold plan 偏差落地）
! xcrun simctl listapps "$LOCKED_UDID" 2>/dev/null | grep -qE 'CFBundleIdentifier = "com\.apple\.calculator"'
xcrun simctl listapps "$LOCKED_UDID" 2>/dev/null | grep -qE 'CFBundleIdentifier = "com\.apple\.Preferences"'

# 568 TS baseline 字面（C1 切换 571 前提）
bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +568 passed'

# C1 红前提：四个目标文件**不存在**字面
! test -f src/sim/runner-supervisor.ts
! test -f src/sim/__tests__/runner-supervisor.test.ts
! test -f scripts/long-run-100.ts
! test -f scripts/simx-c1-longrun-100-smoke.sh

# typecheck baseline 字面
bun run typecheck > /dev/null

# dep 字面 3 个 + peer 字面 zod（不退化）
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'
jq -e '.dependencies["@modelcontextprotocol/sdk"]' package.json > /dev/null
jq -e '.dependencies["citty"]' package.json > /dev/null
jq -e '.dependencies["zod"]' package.json > /dev/null
```

期望：以上每条 exit 0。

## 步骤（线性，无分叉；3 个 step）

### S1. 新建 `src/sim/runner-supervisor.ts` + `src/sim/__tests__/runner-supervisor.test.ts` — `RunnerSupervisor` 纯逻辑类（50-case boundary trigger + 3-consecutive-ping-fail trigger + restart counter monotonic）+ 3 vitest case 字面红 → 绿

**红（写测试）**

新建 `src/sim/__tests__/runner-supervisor.test.ts`（~80 行 / 3 vitest case）：

```ts
import { describe, it, expect } from 'vitest'
import { RunnerSupervisor } from '../runner-supervisor.js'

describe('RunnerSupervisor', () => {
  it('triggers restart on every-50-cases boundary', () => {
    const s = new RunnerSupervisor({ restartEveryNCases: 50, restartAfterNPingFails: 3 })
    for (let i = 0; i < 49; i++) s.recordCase()
    expect(s.shouldRestart()).toBeNull()
    s.recordCase() // 50th
    expect(s.shouldRestart()).toBe('every-50-cases')
  })

  it('triggers restart on 3 consecutive ping failures and resets on success', () => {
    const s = new RunnerSupervisor({ restartEveryNCases: 50, restartAfterNPingFails: 3 })
    s.recordPing(false)
    s.recordPing(false)
    expect(s.shouldRestart()).toBeNull()
    s.recordPing(false)
    expect(s.shouldRestart()).toBe('health-ping-fail-3x')
    s.recordPing(true)
    expect(s.shouldRestart()).toBeNull()
  })

  it('records restarts monotonically and preserves case count', () => {
    const s = new RunnerSupervisor({ restartEveryNCases: 50, restartAfterNPingFails: 3 })
    s.recordCase(); s.recordCase(); s.recordCase()
    s.recordPing(false); s.recordPing(false); s.recordPing(false)
    s.recordRestart()
    expect(s.restarts()).toBe(1)
    expect(s.cases()).toBe(3)
    expect(s.shouldRestart()).toBeNull() // ping counter reset
    s.recordRestart()
    expect(s.restarts()).toBe(2)
  })
})
```

字面验证：`bun x vitest run src/sim/__tests__/runner-supervisor.test.ts` 期望**红**（`Cannot find module '../runner-supervisor.js'` 字面 import fail）。

**绿（实现）**

新建 `src/sim/runner-supervisor.ts`（~80-120 行）：

```ts
// v0.7 C1 — long-run stability supervisor.
// Pure decision logic: caller drives side-effects (spawn/kill xcodebuild,
// curl /health). This class only counts and decides — keeps unit-testable.

export type RestartReason = 'every-50-cases' | 'health-ping-fail-3x'

export interface RunnerSupervisorOptions {
  restartEveryNCases: number
  restartAfterNPingFails: number
}

export class RunnerSupervisor {
  private readonly opts: RunnerSupervisorOptions
  private caseCount = 0
  private consecutivePingFails = 0
  private restartCount = 0

  constructor(opts: RunnerSupervisorOptions) {
    if (opts.restartEveryNCases <= 0) throw new Error('restartEveryNCases must be > 0')
    if (opts.restartAfterNPingFails <= 0) throw new Error('restartAfterNPingFails must be > 0')
    this.opts = opts
  }

  recordCase(): void {
    this.caseCount += 1
  }

  recordPing(ok: boolean): void {
    if (ok) this.consecutivePingFails = 0
    else this.consecutivePingFails += 1
  }

  shouldRestart(): RestartReason | null {
    if (this.consecutivePingFails >= this.opts.restartAfterNPingFails) {
      return 'health-ping-fail-3x'
    }
    if (this.caseCount > 0 && this.caseCount % this.opts.restartEveryNCases === 0) {
      // Only fire on the boundary case; recordRestart() clears ping-fails so
      // back-to-back checks on the same boundary do not double-fire.
      return 'every-50-cases'
    }
    return null
  }

  recordRestart(): void {
    this.restartCount += 1
    this.consecutivePingFails = 0
  }

  restarts(): number { return this.restartCount }
  cases(): number { return this.caseCount }
  pingFails(): number { return this.consecutivePingFails }
}
```

字面验证：
```bash
bun x vitest run src/sim/__tests__/runner-supervisor.test.ts
# 期望：exit 0 + 3 passed
bun x vitest run 2>&1 | tail -3
# 期望：Tests 571 passed
bun run typecheck > /dev/null
# 期望：exit 0
```

**重构**

无。

---

### S2. 新建 `scripts/long-run-100.ts` — 真本机 dev sim 串行 100 次 `simctl launch + terminate Settings` + 每 case `RunnerClient.health()` ping + `RunnerSupervisor` 50-boundary / 3-ping-fail 决策 restart + spawn/kill xcodebuild test 后台 runner + 单行 JSON `{total, passed, failed, restarts, restart_reasons[], app_bundle_id, dev_sim_udid, elapsed_ms}` 字面 stdout

**红（写测试）**

无新 vitest case（C1 是 e2e smoke gate 非 unit；571 TS case 字面不增 —— S1 已落 3 case）。

测试形态 = 真跑 `bun scripts/long-run-100.ts > /tmp/longrun-100.json` 字面 + jq 验：
- `.total == 100` 字面
- `.passed == 100` 字面
- `.failed == 0` 字面
- `.app_bundle_id == "com.apple.Preferences"` 字面
- `.dev_sim_udid == "7B88259F-D864-4F98-9F2C-F0424216EE64"` 字面
- `.restarts >= 1` 字面（必有至少 1 次 50-boundary）
- `.restart_reasons | map(.reason) | index("every-50-cases")` 非 null 字面
- `.elapsed_ms < 720000` 字面（12 分钟 hard cap）

红前提 = `! test -f scripts/long-run-100.ts`（前置条件已验）。

**绿（实现）**

新建 `scripts/long-run-100.ts`（~150-200 行）：

```ts
#!/usr/bin/env bun
// v0.7 C1 — 100-case long-run stability smoke.
// Boots runner, drives 100 serial launch+terminate cycles on Settings,
// uses RunnerSupervisor to restart runner every 50 cases or after 3 ping
// fails. Emits one JSON line: {total, passed, failed, restarts, ...}.
//
// Constraints (CLAUDE.md §9 unchanged + v0.7 C1 task):
//   - dev sim is read from .simx/dev-sim.txt (no env fallback, no first-booted)
//   - sim is NOT rebooted across the 100 cases (only XCUITest runner restarts)
//   - 0 new deps; uses src/sim/runner-{client,supervisor}.ts + dev-lock.ts

import { spawn, spawnSync, type ChildProcess } from 'node:child_process'
import { existsSync, mkdirSync } from 'node:fs'
import { join } from 'node:path'
import { setTimeout as sleep } from 'node:timers/promises'

import { readDevSimLock, assertDevSimLock } from '../src/sim/dev-lock.js'
import { RunnerClient } from '../src/sim/runner-client.js'
import { RunnerSupervisor, type RestartReason } from '../src/sim/runner-supervisor.js'

const ROOT = process.cwd()
const PORT = 22087
const BUNDLE_ID = 'com.apple.Preferences'
const TOTAL_CASES = 100
const HEALTH_TIMEOUT_MS = 120_000
const HEALTH_POLL_INTERVAL_MS = 1_000

type RestartEvent = { at_case: number; reason: RestartReason }

function resolveLockedUdid(): string {
  const udid = readDevSimLock(ROOT)
  if (udid === null) {
    console.error('[long-run-100] .simx/dev-sim.txt missing; dev-sim lock required')
    process.exit(2)
  }
  assertDevSimLock(udid, ROOT)
  return udid
}

function startRunner(udid: string): { child: ChildProcess; logFile: string } {
  const logDir = join(ROOT, '.simx/runner')
  mkdirSync(logDir, { recursive: true })
  const logFile = join(logDir, `longrun-${process.pid}-${Date.now()}.log`)
  const project = join(ROOT, 'swift-bridge/SimxRunner.xcodeproj')
  const args = [
    '-project', project,
    '-scheme', 'SimxRunner',
    '-destination', `platform=iOS Simulator,id=${udid}`,
    '-only-testing:SimxRunnerUITests/SimxRunnerUITests/test_runForever',
    'test',
  ]
  const out = require('node:fs').openSync(logFile, 'a')
  const child = spawn('xcodebuild', args, { stdio: ['ignore', out, out], detached: false })
  return { child, logFile }
}

async function waitHealth(client: RunnerClient, timeoutMs: number): Promise<boolean> {
  const deadline = Date.now() + timeoutMs
  while (Date.now() < deadline) {
    if (await client.health()) return true
    await sleep(HEALTH_POLL_INTERVAL_MS)
  }
  return false
}

function stopRunner(child: ChildProcess): void {
  if (child.pid !== undefined) {
    try { process.kill(-child.pid, 'SIGTERM') } catch { /* group may not exist */ }
    try { child.kill('SIGTERM') } catch { /* already dead */ }
  }
}

function runCase(udid: string, bundleId: string): boolean {
  const launch = spawnSync('xcrun', ['simctl', 'launch', udid, bundleId], { stdio: 'ignore' })
  if (launch.status !== 0) return false
  spawnSync('sleep', ['1'])
  const term = spawnSync('xcrun', ['simctl', 'terminate', udid, bundleId], { stdio: 'ignore' })
  return term.status === 0
}

async function main(): Promise<void> {
  const udid = resolveLockedUdid()
  const start = Date.now()
  let { child: runner } = startRunner(udid)
  const client = new RunnerClient({ port: PORT })

  if (!(await waitHealth(client, HEALTH_TIMEOUT_MS))) {
    stopRunner(runner)
    console.error('[long-run-100] runner did not become healthy within timeout')
    process.exit(1)
  }

  const supervisor = new RunnerSupervisor({ restartEveryNCases: 50, restartAfterNPingFails: 3 })
  const restartReasons: RestartEvent[] = []
  let passed = 0
  let failed = 0

  try {
    for (let i = 1; i <= TOTAL_CASES; i++) {
      const ok = runCase(udid, BUNDLE_ID)
      if (ok) passed += 1
      else failed += 1
      supervisor.recordCase()

      const pingOk = await client.health()
      supervisor.recordPing(pingOk)

      const reason = supervisor.shouldRestart()
      if (reason !== null) {
        stopRunner(runner)
        ;({ child: runner } = startRunner(udid))
        if (!(await waitHealth(client, HEALTH_TIMEOUT_MS))) {
          stopRunner(runner)
          console.error(`[long-run-100] runner failed to recover after restart at case=${i}`)
          process.exit(1)
        }
        supervisor.recordRestart()
        restartReasons.push({ at_case: i, reason })
      }
    }
  } finally {
    stopRunner(runner)
  }

  const out = {
    total: TOTAL_CASES,
    passed,
    failed,
    restarts: supervisor.restarts(),
    restart_reasons: restartReasons,
    app_bundle_id: BUNDLE_ID,
    dev_sim_udid: udid,
    elapsed_ms: Date.now() - start,
  }
  console.log(JSON.stringify(out))
  process.exit(passed === TOTAL_CASES && failed === 0 ? 0 : 1)
}

main().catch((e) => {
  console.error(`[long-run-100] fatal: ${(e as Error).message}`)
  process.exit(2)
})
```

字面验证：
```bash
chmod +x scripts/long-run-100.ts
bun run typecheck > /dev/null
# 期望：exit 0（scripts/ 在 tsconfig include）

bun scripts/long-run-100.ts > /tmp/longrun-100.json
# 期望：exit 0（5-12 分钟）
test "$(jq -r '.total' /tmp/longrun-100.json)" = '100'
test "$(jq -r '.passed' /tmp/longrun-100.json)" = '100'
test "$(jq -r '.failed' /tmp/longrun-100.json)" = '0'
test "$(jq -r '.app_bundle_id' /tmp/longrun-100.json)" = 'com.apple.Preferences'
test "$(jq -r '.dev_sim_udid' /tmp/longrun-100.json)" = "$(cat .simx/dev-sim.txt | tr -d '[:space:]')"
test "$(jq -r '.restarts' /tmp/longrun-100.json)" -ge 1
test "$(jq -r '.elapsed_ms' /tmp/longrun-100.json)" -lt 720000
jq -e '.restart_reasons | map(.reason) | index("every-50-cases") != null' /tmp/longrun-100.json > /dev/null
```

571 TS case 字面不退化（`bun x vitest run` 期望 `Tests 571 passed`）；6 既有 v0.6 mcp smoke + mcp-smoke.ts + v06-acceptance.sh 字面 0 改 0 退化。

**重构**

无。

---

### S3. 新建 `scripts/simx-c1-longrun-100-smoke.sh` — shell wrapper wrap long-run-100.ts 输出 + jq gate + 末行 `{"all_ok":true}` 字面（mirror v0.6 6 既有 mcp smoke 形态）

**红（写测试）**

无新 vitest case（同 S2）。

红前提 = `! test -f scripts/simx-c1-longrun-100-smoke.sh`（前置条件已验）。

**绿（实现）**

新建 `scripts/simx-c1-longrun-100-smoke.sh`（~30 行）：

```bash
#!/usr/bin/env bash
# v0.7 C1 — long-run 100-case stability smoke wrapper.
# Runs scripts/long-run-100.ts and asserts the JSON output meets the
# Success Criteria [5] gate (total/passed/failed = 100/100/0).
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

OUT_FILE="/tmp/longrun-100-$$.json"
trap 'rm -f "$OUT_FILE"' EXIT

bun scripts/long-run-100.ts > "$OUT_FILE"

TOTAL="$(jq -r '.total' "$OUT_FILE")"
PASSED="$(jq -r '.passed' "$OUT_FILE")"
FAILED="$(jq -r '.failed' "$OUT_FILE")"
RESTARTS="$(jq -r '.restarts' "$OUT_FILE")"
ELAPSED="$(jq -r '.elapsed_ms' "$OUT_FILE")"

test "$TOTAL" = '100' || { echo "total != 100: $TOTAL" >&2; exit 1; }
test "$PASSED" = '100' || { echo "passed != 100: $PASSED" >&2; exit 1; }
test "$FAILED" = '0' || { echo "failed != 0: $FAILED" >&2; exit 1; }
test "$RESTARTS" -ge 1 || { echo "restarts < 1: $RESTARTS" >&2; exit 1; }
test "$ELAPSED" -lt 720000 || { echo "elapsed_ms >= 720000: $ELAPSED" >&2; exit 1; }

jq -e '.restart_reasons | map(.reason) | index("every-50-cases") != null' "$OUT_FILE" > /dev/null \
  || { echo "missing every-50-cases in restart_reasons" >&2; exit 1; }

cat "$OUT_FILE"
echo '{"all_ok":true}'
```

字面验证：
```bash
chmod +x scripts/simx-c1-longrun-100-smoke.sh
bash scripts/simx-c1-longrun-100-smoke.sh
# 期望：exit 0 + 末行 {"all_ok":true} 字面
```

**重构**

无。

---

## Checkpoint C1 验收

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# (1) 三 supervisor 单测字面通
bun x vitest run src/sim/__tests__/runner-supervisor.test.ts 2>&1 | tail -3 | grep -qE 'Tests +3 passed'

# (2) 571 TS baseline 字面切换
bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +571 passed'

# (3) typecheck 字面 0 error
bun run typecheck > /dev/null

# (4) 100-case long-run 字面通过（实跑 5-12 分钟）
bun scripts/long-run-100.ts > /tmp/longrun-100-final.json
test "$(jq -r '.total' /tmp/longrun-100-final.json)" = '100'
test "$(jq -r '.passed' /tmp/longrun-100-final.json)" = '100'
test "$(jq -r '.failed' /tmp/longrun-100-final.json)" = '0'
test "$(jq -r '.app_bundle_id' /tmp/longrun-100-final.json)" = 'com.apple.Preferences'
test "$(jq -r '.dev_sim_udid' /tmp/longrun-100-final.json)" = "$(cat .simx/dev-sim.txt | tr -d '[:space:]')"
test "$(jq -r '.restarts' /tmp/longrun-100-final.json)" -ge 1
jq -e '.restart_reasons | map(.reason) | index("every-50-cases") != null' /tmp/longrun-100-final.json > /dev/null

# (5) shell wrapper 字面通
bash scripts/simx-c1-longrun-100-smoke.sh | tail -1 | grep -qE '^\{"all_ok":true\}$'

# (6) 退化检查 — v0.6 e2e 仍通
bun scripts/mcp-smoke.ts > /dev/null && echo ok

# (7) 退化检查 — 0 既有 src/ 改动（git 不在；改用文件存在断言 4 新文件 / 既有 src/sim 5 文件未删未改名）
test -f src/sim/runner-supervisor.ts
test -f src/sim/__tests__/runner-supervisor.test.ts
test -f scripts/long-run-100.ts
test -f scripts/simx-c1-longrun-100-smoke.sh
test -f src/sim/runner-client.ts
test -f src/sim/dev-lock.ts
test -f src/sim/cell.ts
test -f src/sim/session.ts
test -f src/sim/simctl.ts
test -f src/sim/a11y-tree-source.ts

# (8) 退化检查 — dep 字面 3 个不变
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'

# (9) 退化检查 — v1.md / roadmap.md / cold v0.7 字面 0 改（boundary 字面在场）
grep -qE '^\[5\] 长跑：100 case 串行连续跑不僵尸化、不漏 element$' docs/v1.md
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

echo 'C1 verdict: ok'
```

期望：以上每条 exit 0 + 末行字面 `C1 verdict: ok`。

## 完成后动作

1. 归档此文件到 `docs/plan-history/v0.7-c1-hot.md`：
   ```bash
   mv docs/plan-hot.md docs/plan-history/v0.7-c1-hot.md
   ```
2. **改 `docs/v1.md` 决策日志末尾 +2 行字面**（决策 10.A + 10.D 字面 — C1 close note + cold plan 偏差记）：
   ```
   - 2026-05-16 v0.7 C1 close — 100-case 串行长跑稳定字面达成（passed=100 / failed=0 / restarts >= 1 / RunnerSupervisor 50-boundary + 3-ping-fail 双 trigger 字面 mirror cold plan §触发模板）；新增 1 src 文件（runner-supervisor.ts 纯逻辑 supervisor 类）+ 1 vitest 文件（+3 case；568 → 571 TS baseline 字面）+ 2 scripts（long-run-100.ts e2e driver + simx-c1-longrun-100-smoke.sh shell wrapper）；0 swift / 0 既有 src 改动 / 0 dep / 0 既有 6 mcp smoke 改 / 0 既有 5 v0[1-5] + v0.6 acceptance 改 / 0 examples / 0 roadmap.md / 0 v1.md 边界改 / 0 cold v0.7 改；v1.md §4 Success Criteria [5] "长跑：100 case 串行连续跑不僵尸化、不漏 element" 字面验证工程实体在场。
   - 2026-05-16 v0.7 C1 cold plan 偏差字面记 — cold plan §触发模板字面"长跑 100-case 跑哪个 app：用 Calculator（无外部依赖）足够"实际落地用 Settings (com.apple.Preferences) 替代 理由：本机 iOS 26.4 dev sim 不预装 Calculator app（xcrun simctl listapps 字面无 com.apple.calculator）；Settings 是 v0.4 以来 examples / scripts 21 处字面统一 fixture；C1 真生效 = 100 次 simctl launch+terminate com.apple.Preferences。
   ```
3. 调 sub-agent 生成新 `plan-hot.md`（到 v0.7 C2 CI workflow），见 CLAUDE.md §6 标准模板 — **本计划范围内不展开 C2**。
