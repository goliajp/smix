# plan-hot — v0.7 C4：`simx doctor --json` 每 check 加 `compatibility: { status, required?, actual?, message? }` 字面结构化字段 + zod schema 固化（新建 `src/cli/commands/doctor-schemas.ts` 7 zod schema SoT + 8 z.infer type re-export）+ smoke 6→9 字段升级 — TS 578 → 586 / 24 → 25 test file / 0 swift / 0 Driver / 0 MCP / 0 既有 src/core/schemas.ts 改 / 0 dep / 0 既有 18 doctor case 改

> [3/4 信息层] 当前唯一热计划。**C4 是 v0.7 第 4 个 checkpoint**，cold plan §Checkpoint 概要列表字面 "C4 `simx doctor --json` 把每个 check 的版本兼容性输出（结构化 schema 固化）"。**"版本兼容性输出 + 结构化 schema 固化" 在 v0.7 C4 真实工程约束下解读为**两个独立但同段落落地的元素：
>
> **元素 1（版本兼容性）**：每个 `DoctorCheck` 字面追加 1 个新顶层 optional 字段 `compatibility?: Compatibility`，shape = `{ status: 'supported' | 'unsupported' | 'partial' | 'unknown', required?: string, actual?: string, message?: string }` 字面复合（决策 1.A 字面——单字段复合而非两字段双轨 / 而非仅 enum / 而非 minVersion+actualVersion 双扁字段）。该字段对每个 check 字面派生（详决策 2.x）：
> - **xcode** → required `'Xcode >=26.0'` 字面 + actual = parsed semver from `value`（如 `'26.4.1'`） + status `'supported'` if `gteSemver(actual, '26.0')` / `'partial'` if 解析成功但低版本 / `'unknown'` if probe reject
> - **runtimes** → required `'iOS 26.x present (17.5 / 18.4 nice-to-have)'` 字面 + actual = `'<n> available: <id>, <id>, ...'` 拼接前 5 个 identifier + status `'supported'` if items 含 `iOS-26` token / `'partial'` if items 非空但缺 26.x / `'unsupported'` if items 空 / `'unknown'` if probe reject（决策 2.B 字面：v1.md §4 [6] 终态 "iOS 17.5 / 18.4 / 26.x 全部通过冒烟集" 是 v1.0 总目标；C4 doctor 阈值 = 26.x 在场 = supported，与 v0.7 C3 ci.yml `continue-on-error: ${{ matrix.runtime != 'iOS-26-4' }}` 硬态对齐 —— 17/18 缺席不降 status；硬化推 v0.7+ 5-arg HID 实装窗口）
> - **claude** → required `'logged-in'` 字面 + actual = `'logged-in'`（loggedIn=true）/ `'not-logged-in'`（path 在但 !loggedIn）/ `'missing'`（path undefined） + status `'supported'` / `'partial'` / `'unsupported'` / `'unknown'`（probe reject）
> - **bun** → required `'>=1.1.0'`（MIN_BUN 常量字面延续）+ actual = `value` 字面（如 `'1.3.14'`）+ status `'supported'` if `gteSemver(actual, MIN_BUN)` / `'partial'` if 版本低 / `'unsupported'` if undefined / `'unknown'` if probe reject
> - **hid** → required `'digitizer + indigo9'` 字面 + actual = `'digitizer=<bool>, indigo9=<bool>'` 字面 + status `'supported'` 两 channel 都 true / `'partial'` 仅一 true / `'unsupported'` 两 false / `'unknown'` probe reject
> - **axp** → required `'framework + 5 classes + 3 selectors + +sharedInstance'` 字面 + actual = `'framework=<bool>, classes=<r>/<r+m>, selectors=<r>/<r+m>, sharedInstance=<bool>'` 字面 + status `'supported'` 全部合格 / `'partial'` framework.loaded 但有缺 / `'unsupported'` !framework.loaded / `'unknown'` probe reject
>
> **元素 2（zod schema 固化）**：新建 **`src/cli/commands/doctor-schemas.ts`**（决策 3.A 字面——独立 CLI-local 文件而非搬入 `src/core/schemas.ts` —— doctor type 是 CLI 内部表面、非 MCP wire / 非 SDK 共享形态；v0.6 C7 `src/core/schemas.ts` 是 MCP wire / SDK SoT，doctor 不走该层；将 doctor schema 注入 core/schemas.ts 会污染 SoT 边界 + 增 7 schema 到 core 表面）字面 **7 个 zod schema export** + **8 个 z.infer type export**（决策 3.B 字面：7 schema = `compatibilitySchema` / `runtimesValueSchema` / `claudeValueSchema` / `hidChannelAvailabilitySchema` / `axpCapabilitySchema` / `doctorCheckSchema` / `doctorReportSchema`；同时另 export `doctorCheckNameSchema` 但不计入主 7 个 —— `doctorCheckSchema` 内含；type re-export = `Compatibility` / `RuntimesValue` / `ClaudeValue` / `HidChannelAvailability` / `AxpCapability` / `DoctorCheckName` / `DoctorCheck` / `DoctorReport` 共 8 个；文件 ~150-180 行）；既有 `src/cli/commands/doctor.ts` 7 个 `export type` 字面（`DoctorCheckName` / `DoctorCheck` / `DoctorReport` / `RuntimesValue` / `ClaudeValue` / `HidChannelAvailability` / `AxpCapability`）改为 `import type { ... } from './doctor-schemas.js'` + `export type { ... }` re-export（决策 3.C 字面——zod-first SoT，type 自动等价；下游 import 0 改 —— `src/cli/index.ts` line 10 `import { runDoctorCommand, type AxpCapability } from './commands/doctor.js'` 字面 0 改 + `src/cli/__tests__/doctor.test.ts` line 2-8 import 字面 0 改）。
>
> 通过后世界变成：(a) `bun src/cli/index.ts doctor --json` 字面输出 6 个 check 每个含 `compatibility` 字段 + status 字面 enum 4 字面值之一 + 真本机跑 `compatibility.required` 字面命中（xcode `'Xcode >=26.0'` / bun `'>=1.1.0'` / claude `'logged-in'`）；(b) `compatibility` 字段通过 `doctorCheckSchema.parse()` zod round-trip；(c) `src/cli/commands/doctor-schemas.ts` 字面在场 + 7 zod schema + 8 z.infer type export；(d) `src/cli/commands/doctor.ts` 字面 7 type alias 改为 re-export from `./doctor-schemas.js` + 加 `deriveCompatibility(...)` helper + 6 check 函数 ok / fail / catch 三路径字面填 compatibility 字段；(e) `src/cli/__tests__/doctor.test.ts` 18 → 21 case（+3 case 在新 describe `'C4: compatibility field (v0.7 C4)'` block）；(f) 新建 `src/cli/__tests__/doctor-schemas.test.ts` ~80-100 行 / 5 case（zod round-trip + reject + optional / enum reject / report-level）；(g) `scripts/simx-c4-doctor-smoke.sh` 6 → 9 字段升级（决策 5.A 字面：加 `has_compatibility_field` / `compat_status_enum_ok` / `runtimes_compat_supported` 3 新字段；既有 5 实义字段 grep/jq 语义 0 改）；(h) `docs/v1.md` 决策日志 +1 行 v0.7 C4 close note 字面。
>
> **承接（v0.7 C3 close 后实测 baseline，2026-05-16）**：v0.7 C3 ✅ done —— v1.md 决策日志末尾 `2026-05-16 v0.7 C3 close` 字面在场（已实测 line 173）+ `.github/workflows/ci.yml` 62 行字面 + `scripts/simx-c3-ci-matrix-validate.sh` ~85 行 + 14 字段 JSON gate 字面 `all_ok=ok` + `scripts/simx-c2-ci-workflow-validate.sh` 保留不删 + `docs/plan-history/v0.7-c3-hot.md` 字面在场（已实测）；TS baseline = **578**（C3 0 vitest case 增 → C4 起点 578）/ **24 test files**（C3 0 file 增 → C4 起点 24）/ 102 swift 不变 / typecheck 0 error / 27 MCP tool / 3 prod dep（citty / @modelcontextprotocol/sdk / zod，已实测）/ `bun --version = 1.3.14`（已实测）/ `Xcode 26.4.1`（已实测）/ `xcrun simctl list runtimes -j` 单 `com.apple.CoreSimulator.SimRuntime.iOS-26-4` 字面（已实测）/ `bun x vitest run` 末尾字面 `Tests 578 passed (578)` + `Test Files 24 passed (24)`（已实测） / `simx-c4-doctor-smoke.sh` 当前 58 行 / 6 字段 / 实测当前 `{"exit_code":0,"checks_len":6,"schema_ok":"ok","runtimes_has_items":"ok","claude_logged_in":"ok","all_ok":"ok"}` 字面（已实测）。
>
> **进入 v0.7 C4 前置检字面全 ok**：
>
> 1. **v0.7 C3 工程实体字面在场** — `.github/workflows/ci.yml` 62 行 + `scripts/simx-c3-ci-matrix-validate.sh` + `scripts/simx-c2-ci-workflow-validate.sh`（保留）+ `docs/plan-history/v0.7-c3-hot.md` 字面 + v1.md `2026-05-16 v0.7 C3 close` 字面（已实测 line 173）；
> 2. **v1.md §4 Success Criteria [6] "兼容：iOS 17.5 / 18.4 / 26.x 全部通过冒烟集" 字面在场 + §2 "simx doctor 检测 Xcode / runtime / 私有符号 / claude CLI 可用性" 字面在场**（已实测 line 24+42+98）—— C4 范围工程实体 = v1.md §2 doctor + §4 [6] + §6 风险条 "iOS 26 9-arg Indigo ABI 私有... `simx doctor` 检测"；
> 3. **doctor v0.5 C4 deep 形态字面在场** — `src/cli/commands/doctor.ts` 292 行字面（已实测）+ 6 check 全 deep（xcode / runtimes-with-items / claude-with-loggedIn / bun-with-MIN_BUN-check / hid / axp）+ `src/cli/__tests__/doctor.test.ts` 526 行字面 **18 case**（已实测 `grep -cE "^  it\(" = 18` —— 任务描述字面 "26 case 现状" 实测为 18，按实际推进 + 偏差记下，决策 17.A 字面）+ `scripts/simx-c4-doctor-smoke.sh` 58 行 6 字段 + 当前 all_ok=ok（已实测）；
> 4. **本机非 git 工作树字面延续** — `test -d .git` 字面 fail；C4 范围工程实体 = src/cli + scripts/simx-c4-doctor-smoke.sh + tests + docs/v1.md，不依赖 git；
> 5. **iOS 17.5 / 18.4 SDK + runtime 本机仍缺席字面**（C3 baseline 延续，C4 不装）—— C4 doctor `runtimes` compatibility status `'supported'`（has iOS-26.x；17/18 缺席不降 status，与 ci.yml hard-required 26.4 同步）；
> 6. **既有 `src/core/schemas.ts` 7 schema 字面在场**（v0.6 C7 SoT；已实测 line 1-148 + `grep -cE '^export const [a-z][a-zA-Z]+Schema' = 7`）—— C4 doctor-schemas.ts 独立 CLI-local，0 改 core/schemas.ts；
> 7. **C4 红前提**：`src/cli/commands/doctor-schemas.ts` 字面**不存在** + `src/cli/__tests__/doctor-schemas.test.ts` 字面**不存在** + `src/cli/commands/doctor.ts` 字面**无** `compatibility` 字段（grep 应 fail）；
> 8. **本机 zod 1 dep 在场**（package.json line 42 字面）—— C4 0 新 dep 增。
>
> **本机探测（C4 落地决策依据，2026-05-16）**：
>
> - `xcodebuild -version` 字面 `Xcode 26.4.1`（C3 baseline 延续；doctor xcode compatibility actual=`'26.4.1'`、required=`'Xcode >=26.0'`、status=`'supported'`）
> - `xcrun simctl list runtimes -j` 字面单 iOS-26-4（C3 baseline 延续；doctor runtimes compatibility actual=`'1 available: com.apple.CoreSimulator.SimRuntime.iOS-26-4'`、required=`'iOS 26.x present (17.5 / 18.4 nice-to-have)'`、status=`'supported'`）
> - `bun --version` 字面 `1.3.14`（doctor bun compatibility actual=`'1.3.14'`、required=`'>=1.1.0'`、status=`'supported'`）
> - `bun x vitest run` 末尾字面 `Tests 578 passed (578)` + `Test Files 24 passed (24)`（C4 起点 578/24；C4 close 后 586/25）
> - `(cd swift-bridge && swift test)` 字面 `Executed N tests` ≥ 102（C4 0 改 swift-bridge/）
> - `bash scripts/simx-c1-mcp-smoke.sh` 字面 `all_ok=ok` + `tools_list_count=27`（C4 0 改 MCP）
> - `bash scripts/simx-c4-doctor-smoke.sh` 当前实测字面 `{"exit_code":0,"checks_len":6,"schema_ok":"ok","runtimes_has_items":"ok","claude_logged_in":"ok","all_ok":"ok"}`（C4 后字面 9 字段 + `all_ok=ok` —— 既有 5 实义字段全保 + 3 新 `has_compatibility_field=ok` + `compat_status_enum_ok=ok` + `runtimes_compat_supported=ok` + `all_ok=ok` —— 总 9 字段）
> - `grep -cE '^  it\(' src/cli/__tests__/doctor.test.ts` 字面 `18`（C4 后顶层 it 仍 18 —— 3 新 case 在 nested describe block 内、`grep -cE '^    it\(' >= 3` 字面 indented）
> - `wc -l src/cli/commands/doctor.ts` 字面 `292`（C4 后 ≤ 380 —— +1 type field via z.infer + 1 import + 7 type re-export + `deriveCompatibility` helper ~60 行 + 6 check 函数 ok/fail/catch 三路径字面追加 compatibility 字段 ~18 行）
> - `wc -l src/core/schemas.ts` 字面 `148`（C4 0 改）
> - dep 字面 3 个（C4 0 dep 增；zod 已在 line 42）
> - **既有 18 doctor case 全保字面**（决策 7.A 字面 —— 现有 18 case 0 修改；新 3 case 仅 covers compatibility 字段，不动既有断言；既有 18 case 验证 ok/value/message wire 形态 0 改 = compatibility 是 additive 字段）
>
> **现状盘点（C4 真生效边界）**：
>
> - **新建 `src/cli/commands/doctor-schemas.ts`**（~150-180 行）：7 zod schema export + 8 z.infer type re-export + 1 `doctorCheckNameSchema` 内部依赖 export（决策 3.A/3.B/3.C 字面）：
>   - `compatibilitySchema → { status: enum 4-literal, required?, actual?, message? }`
>   - `runtimesValueSchema → { count, items: array<{identifier, name, version, isAvailable}> }`
>   - `claudeValueSchema → { path?: string, loggedIn, failureDetail? }`
>   - `hidChannelAvailabilitySchema → { digitizer, indigo9 }`
>   - `axpCapabilitySchema → { framework:{path,loaded}, classes:{resolved[],missing[]}, selectors:{resolved[],missing[]}, sharedInstance:{available} }`
>   - `doctorCheckNameSchema → z.enum(['xcode','runtimes','claude','bun','hid','axp'])`
>   - `doctorCheckSchema → { name: enum, ok, value?: union, message?, compatibility?: compatibilitySchema }`
>   - `doctorReportSchema → { exitCode: 0|1 literal union, checks: array<doctorCheckSchema> }`
> - **编辑 `src/cli/commands/doctor.ts`**（292 → ≤ 380 行）：
>   - 删 line 8-16 `export type RuntimesValue = ...` / line 22-26 `export type ClaudeValue = ...` / line 33-36 `export type HidChannelAvailability = ...` / line 41-46 `export type AxpCapability = ...` / line 49 `export type DoctorCheckName = ...` / line 51-59 `export type DoctorCheck = ...` / line 61-64 `export type DoctorReport = ...` 共 7 个 type alias 字面
>   - 加 1 行 `import type { Compatibility, RuntimesValue, ClaudeValue, HidChannelAvailability, AxpCapability, DoctorCheckName, DoctorCheck, DoctorReport } from './doctor-schemas.js'` + 1 行 `export type { Compatibility, RuntimesValue, ClaudeValue, HidChannelAvailability, AxpCapability, DoctorCheckName, DoctorCheck, DoctorReport }`（决策 3.C 字面 —— 下游 import 链路保留 0 改）
>   - 保留 `export type ProbeXcode` / `ProbeRuntimes` / `ProbeClaude` / `ProbeBun` / `ProbeHidChannels` / `ProbeAxp` 字面 0 改（6 个 Probe 是 callback type，不属 wire shape，留在 doctor.ts 本地）
>   - 保留 `export type RunDoctorDeps` 字面 0 改
>   - 保留 `const MIN_BUN = '1.1.0'` 字面 0 改（决策 16.A 字面）
>   - 加 `function deriveCompatibility(name, ok, value, message, probeRejected): Compatibility` helper（~60 行 / 1 函数 / 6-arm switch on name，决策 4.A 字面 —— 一处集中派生而非散落到 6 check 函数 / 决策 4.B 字面 —— `probeRejected: boolean` 参数区分 catch 路径 vs ok=false 但 probe 成功）
>   - 6 个 check 函数（`checkXcode` / `checkRuntimes` / `checkClaude` / `checkBun` / `checkHid` / `checkAxp`）字面修改 return path：每个函数 ok / fail / catch 三路径字面追加 `compatibility: deriveCompatibility(...)` 字段（决策 4.C 字面 —— in-place 字面追加；handler 行为字面 0 改、ok / message / value 字面 0 改 —— compatibility 是 additive optional 字段、既有 18 case 全保绿）
>   - 保留 `function runDoctorCommand` / `renderTail` / 4 个 humanLine helper / `gteSemver` 字面 0 改（决策 4.D 字面 —— 人读路径不渲染 compatibility，决策 8.A 字面 —— 人读已有 ok/✓/✗ icon + value/message，加 compatibility 行污染输出；compatibility 是 AI-readable JSON-only 字段）
> - **编辑 `src/cli/__tests__/doctor.test.ts`**（526 → ≤ 700 行）：+1 新 nested describe block `'C4: compatibility field (v0.7 C4)'`（紧贴既有末段 `'C4: runtimes list + claude logged-in (v0.5 C4)'` block 之后）含 3 case 字面：
>   - case 字面 19 `it('every check has compatibility field with status enum + optional required/actual', ...)` — happy 6 probe → 断 6 check 字面每条有 `c.compatibility` + status ∈ 4-enum + xcode/bun/claude/hid required 字面具体值
>   - case 字面 20 `it('compatibility status derived correctly for partial / unsupported scenarios', ...)` — 多 sub-case：(a) bun=1.0.30 → partial + actual=1.0.30 / (b) runtimes 0 items → unsupported / (c) claude path=undefined → unsupported / (d) xcode probe reject → unknown
>   - case 字面 21 `it('doctorReportSchema.parse round-trips a full --json output', ...)` — dynamic `import('../commands/doctor-schemas.js')` → `safeParse(JSON.parse(out))` → `result.success === true` + checks length 6
> - **新建 `src/cli/__tests__/doctor-schemas.test.ts`**（~80-100 行）：`describe('doctor zod schemas (v0.7 C4)', ...)` 含 **5 case**（决策 6.B 字面 mirror v0.6 C7 `schemas.test.ts` pattern）：
>   - case 1 `compatibilitySchema parses valid round-trip`
>   - case 2 `compatibilitySchema rejects invalid status enum`
>   - case 3 `doctorCheckSchema parses with optional compatibility`（含有 / 无 compatibility 两个 sub-fixture）
>   - case 4 `doctorReportSchema parses a 6-check report round-trip`
>   - case 5 `doctorReportSchema rejects bad check name enum`
> - **编辑 `scripts/simx-c4-doctor-smoke.sh`**（58 → ~80 行）：6 → 9 字段升级（决策 5.A 字面）：
>   - 既有 5 实义字段 `exit_code` / `checks_len` / `schema_ok` / `runtimes_has_items` / `claude_logged_in` 字面 jq 逻辑 0 改（语义字面延续 v0.5 C4 baseline）
>   - 加 `has_compatibility_field` jq 字面验 6 check 每条字面有非 null `.compatibility` 对象
>   - 加 `compat_status_enum_ok` jq 字面验 6 check 字面 status ∈ `["supported","unsupported","partial","unknown"]`
>   - 加 `runtimes_compat_supported` jq 字面验 `checks[1].compatibility.status === "supported"`（本机/CI 26.x 在场）
>   - `all_ok = ok` 当 8 实义字段（5 既有 + 3 新）全 ok 字面
> - **编辑 `docs/v1.md`**（决策日志末尾 +1 行 v0.7 C4 close note，仿 C3 close note 字面 pattern；不动 §2/§4/§5/§6 边界）
> - **0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/sim 6 文件 / 0 既有 src/sdk / 0 既有 src/driver / 0 既有 src/mcp / 0 既有 src/core 6 文件（schemas / screen / error / role / index / selector 等 0 改） / 0 既有 src/cli/index.ts 改（doctor wiring 0 改 —— compatibility 派生在 doctor.ts 内部、不依赖 probe 字段增） / 0 既有 src/cli/commands/list.ts / run.ts / repl.ts / boot.ts 改 / 0 既有 src/cli/__tests__ 中 doctor.test.ts 之外文件 / 0 dep / 0 既有 27 ToolDef / 0 既有 6 mcp smoke 字段 / 0 既有 v0[1-6] acceptance / 0 既有 17 c[1-8] smoke 中 16 个改（仅 `simx-c4-doctor-smoke.sh` 升级—— C4 是其 v0.7 升级目标） / 0 既有 24 vitest test file 中 23 文件改（doctor.test.ts 仅 +3 case） / 0 既有 examples / 0 既有 scripts/long-run-100.ts / 0 既有 scripts/mcp-smoke.ts / 0 .github/workflows/ci.yml / 0 既有 c1-c3 c5-c8 c8-dogfood smoke 改 / 0 roadmap.md 改 / 0 v1.md 边界改（仅决策日志 +1 行）/ 0 cold v0.7 改 / 0 package.json 改 / 0 tsconfig 改 / 0 vitest.config 改 / 0 bun.lock 改 / 0 engines.bun 改**
>
> **C4 真生效 = 五项核心改动（≤ 6 字面任务约束严守 / 实测 5 文件 src+scripts + 1 docs）**：
>
> 1. **新建 `src/cli/commands/doctor-schemas.ts`**（~150-180 行 / 7 zod schema + 8 z.infer type）— 核心 1
> 2. **编辑 `src/cli/commands/doctor.ts`**（292 → ≤ 380 行 / type 7 个搬迁 + deriveCompatibility helper + 6 check 函数 3 路径 × 6 = 18 处字面追加 compatibility）— 核心 2
> 3. **编辑 `src/cli/__tests__/doctor.test.ts`**（+1 新 describe block / +3 case）— 核心 3
> 4. **新建 `src/cli/__tests__/doctor-schemas.test.ts`**（~80-100 行 / 5 case）— 核心 4
> 5. **编辑 `scripts/simx-c4-doctor-smoke.sh`**（58 → ~80 行 / 6→9 字段）— 核心 5
> 6. **编辑 `docs/v1.md` 决策日志 +1 行** — docs 不计入核心
>
> **核心改动 = 5 条 ≤ 6 字面满足**（决策 18.A 字面 —— src/cli 内部 2 文件 + 1 既有 test + 1 新 test + 1 既有 smoke + 1 docs = 6 件物理改动，4 件物理新建/编辑 src/cli + 1 件编辑 scripts + 1 件编辑 docs）。
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / SimctlClient 0 / RunnerClient 0 / RunnerSupervisor 0 / DevSimLock 0 / MCP 27 tool 0 改 / 既有 src/core/schemas.ts 0 改 / 既有 src/core/ 任何文件 0 改 / 既有 src/sdk/ 0 改 / 既有 src/driver/ 0 改 / 既有 src/mcp/ 0 改 / 既有 src/sim/ 0 改 / 既有 src/cli/index.ts 0 改 / 既有 src/cli/commands 中 doctor.ts 之外 0 改 / dep 0 / 既有 18 doctor case 0 改 / 既有 24 test file 中 23 0 改 / 既有 6 mcp smoke 字段 0 / 既有 17 c[1-8] smoke 中 16 个 0 改 / ci.yml 0 改 / v1.md 边界 0 改 / roadmap.md 0 改 / cold v0.7 0 改 / examples 0 改 / package.json 0 改 / engines.bun 0 改 / bun.lock 0 改**。允许改动 / 新建文件范围（**核心 ≤ 6 条**字面严守）：
>
> 1. **新建 `src/cli/commands/doctor-schemas.ts`** — 核心 1
> 2. **编辑 `src/cli/commands/doctor.ts`** — 核心 2
> 3. **编辑 `src/cli/__tests__/doctor.test.ts`** — 核心 3
> 4. **新建 `src/cli/__tests__/doctor-schemas.test.ts`** — 核心 4
> 5. **编辑 `scripts/simx-c4-doctor-smoke.sh`** — 核心 5
> 6. **编辑 `docs/v1.md` 决策日志 +1 行** — docs
>
> **不允许**：(a) 改 `src/` 任何文件除以上 4 个 src/cli 文件；(b) 改 `src/core/schemas.ts` 注入 doctor schema —— C4 doctor schemas 字面 CLI-local 独立（决策 3.A 字面）；(c) 改 `src/cli/index.ts` probe wiring（compatibility 派生在 doctor.ts 内部，不依赖 probe 字段增）；(d) 改 `swift-bridge/`；(e) 改 `examples/`；(f) 引第 4 个 npm dep（zod 已在；决策 9.B 字面拒 `zod-to-json-schema`）；(g) 加 CLI flag 如 `--with-compat` / `--check <name>`（决策 8.B 字面 —— compatibility 字段无条件附加在 --json 出参 / 决策 11.A 字面 —— v1 范围 doctor 全跑 6 check，单 check 过滤推 v1.1+）；(h) 加 env 变量；(i) 改 既有 MCP / wire / Driver / SimctlDriver / Cell / SimctlClient 任何方法；(j) 改 既有 18 doctor case 任何断言（既有 18 case 字面全保绿是退化检查）；(k) 改 既有 6 MCP smoke 字段；(l) 改 既有 c1 / c2 / c3 / c5 / c6 / c8 smoke 任何字段；(m) 删 / 改 既有 c4-doctor-smoke.sh 5 实义字段语义（仅 +3 字段 + `all_ok` 含 3 新；既有 5 字段 grep/jq 输出语义 0 改）；(n) 加 schema 落盘 `.schema.json`（决策 9.A 字面拒——zod 是 SoT，落盘推 v1.0 polish）；(o) 引 `zod-to-json-schema` dep（决策 9.B 字面拒——v0.6 C7 同 pattern 拒）；(p) 加 schema version 字段 `$schema_version`（决策 9.C 字面拒——v0.6 C7 同 pattern 拒）；(q) 写 C5 plugin manifest / C6 v1 acceptance / C7 README（C5-C7 范围）；(r) 改 `compatibility.status` enum 4 字面值（决策 12.A 字面定 4-enum：`'supported'`=完全合格 / `'partial'`=部分功能可用 1-of-N / `'unsupported'`=完全不可用 / `'unknown'`=probe reject 无法判定）；(s) 改 `compatibility` 字段为 array 或 nested object（决策 12.B 字面拒——单层 4 字段足够）；(t) 加 `compatibility.severity: 'error'|'warn'|'info'`（决策 12.C 字面拒——ok=true/false 已表达 fail-or-pass；compatibility 仅描述兼容性、不重复 ok 维度）；(u) 给 Driver / SimctlDriver / MCP wire 添加 compatibility 字段（决策 13.A 字面拒——compatibility 是 doctor CLI-local 字段，MCP wire 不暴露 doctor data）；(v) 改 既有 v0.5 C4 deep 字段（runtimes.items / claude.loggedIn 等）任何 wire 形态（决策 14.A 字面拒——v0.5 C4 wire 字面冻结；C4 v0.7 是 additive only）；(w) 拒 doctor-schemas.ts 拆分到 `src/cli/commands/doctor-schemas/` 子目录（决策 15.A 字面拒——单文件 ~150-180 行无需拆，v0.6 C7 同 pattern）；(x) 改 既有 `MIN_BUN = '1.1.0'` 常量值（决策 16.A 字面拒——v0.5 C4 baseline 锁；v1 范围）；(y) 改 既有 `gteSemver` helper（决策 16.B 字面拒——既有逻辑无 bug）；(z) 改 `runDoctorCommand` 函数体（决策 16.C 字面拒——compatibility 在 checkXxx 函数内填，不在 runDoctorCommand 包外加）；(aa) 加 `simx doctor` 人读路径渲染 compatibility 行（决策 8.A 字面拒——人读 ok/✓/✗ + value/message 已 readable，加 compatibility 增噪；compatibility 是 AI-readable JSON-only 字段）；(ab) 改 既有 `DoctorCheck.value` union type（决策 14.B 字面拒——v0.5 C4 union 形态 `string | RuntimesValue | ClaudeValue | HidChannelAvailability | AxpCapability` 字面冻结）。
>
> **任务说明偏差字面记**（决策 17.A 字面）：本任务字面 "26 case 现状" 实测 `grep -cE '^  it\(' src/cli/__tests__/doctor.test.ts = 18`，按实际 18 case baseline 推进；本任务字面 "claude logged-in" 已在 v0.5 C4 deep 范围内、已字面在场（决策日志 v0.5 C4 close 字面证）；本任务字面 "runtimes items" 同上 v0.5 C4 已 deep。**`simx-c4-doctor-smoke.sh` 文件名跨 v0.5 C4 ↔ v0.7 C4 同名复用偏差字面记**（决策 17.B 字面）：cold plan v0.5 C4 已建该文件 6 字段形态、cold plan v0.7 C4 同名升级 9 字段；不重命名（决策 17.C 字面 —— 文件名继承 v0.5 C4 已落地的命名 + v0.7 C4 是其增量升级，文件名同 = 工程实体连续；改名会留 v0.5 c4 死链接到不存在的脚本）。**runtimes compatibility 阈值字面 26.x-only 而非 17/18/26.x-all 字面记**（决策 2.B 字面 cold plan §C4 + §出口验收 + v1.md §4 [6] 三处对齐解读）：v1.md §4 [6] 终态目标 = 17.5+18.4+26.x 全过；C4 doctor compatibility = 26.x 在场 = supported（与 v0.7 C3 ci.yml hard-required 26.4 同步；17/18 缺席不降 status —— 否则本机 dev 环境 doctor 永远红，违反 doctor 是 dev-friendly 工具的原则；硬化 17/18 推 v0.7+ 5-arg HID 实装窗口；v1.0 acceptance C6 时若 17/18 也字面在场则 doctor compatibility 仍 supported —— 阈值是 26.x lower bound，17/18 是 nice-to-have，required 字面就字面说明 "iOS 26.x present (17.5 / 18.4 nice-to-have)"）。**git 工作树缺席偏差延续记**：lab15-autofix 仍非 git 工作树（无 `.git`）；C4 范围 0 git 依赖；CI 上 `bash scripts/simx-c4-doctor-smoke.sh` 推 C5+ 进 ci.yml 验（C4 范围 ci.yml 0 改 —— c4 smoke 在本机字面验，CI 跑路径推 C6 v1-acceptance 聚合）。

## 目标 checkpoint

**C4**：通过后世界变成 —

(a) `bun src/cli/index.ts doctor --json` 字面输出 6 个 check 每个含 `compatibility: { status, required?, actual?, message? }` 字段；
(b) 每个 check 的 `compatibility.status` ∈ `['supported','unsupported','partial','unknown']` 字面 enum；
(c) `compatibility` 字段通过 `doctorCheckSchema.parse()` zod round-trip；
(d) `src/cli/commands/doctor-schemas.ts` 字面在场 + 7 zod schema export（`compatibilitySchema` / `runtimesValueSchema` / `claudeValueSchema` / `hidChannelAvailabilitySchema` / `axpCapabilitySchema` / `doctorCheckSchema` / `doctorReportSchema`）+ 8 z.infer type export（`Compatibility` / `RuntimesValue` / `ClaudeValue` / `HidChannelAvailability` / `AxpCapability` / `DoctorCheckName` / `DoctorCheck` / `DoctorReport`）；
(e) `src/cli/commands/doctor.ts` 字面 7 type alias 改为 import-and-re-export from `./doctor-schemas.js` + `deriveCompatibility` helper 在场 + 6 check 函数 ok / fail / catch 三路径字面带 compatibility 字段；
(f) `bun x vitest run` 字面 `Tests 586 passed (586)` + `Test Files 25 passed (25)`（决策 6.A 字面 baseline：578 + 3 doctor C4 + 5 doctor-schemas = 586；24 + 1 new doctor-schemas.test.ts = 25）；
(g) `scripts/simx-c4-doctor-smoke.sh` 字面 9 字段 JSON + `all_ok = "ok"` + exit 0；
(h) 既有 18 doctor case 字面全保绿 + typecheck 0 error + 27 MCP tool / 102 swift / 6 既有 MCP smoke 0 退化；
(i) `docs/v1.md` 决策日志末尾 +1 行 v0.7 C4 close note 字面；
(j) 0 dep / 0 swift / 0 既有 src/core / 0 既有 MCP / 0 既有 Driver / 0 既有 SimctlDriver / 0 既有 examples / 0 既有 ci.yml / 0 既有 c1-3 / c5-8 smoke 改 / 0 v1.md 边界 / 0 roadmap.md / 0 cold v0.7 改 字面退化检查全过。

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.7 C3 close 痕迹存在
test -f .github/workflows/ci.yml
test -f scripts/simx-c3-ci-matrix-validate.sh
test -f scripts/simx-c2-ci-workflow-validate.sh
test -f docs/plan-history/v0.7-c3-hot.md
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C3 close' docs/v1.md

# v0.7 C1 + C2 痕迹延续
test -f src/sim/runner-supervisor.ts
test -f scripts/long-run-100.ts
test -f scripts/simx-c1-longrun-100-smoke.sh

# v0.6 close 痕迹延续
test -f docs/plan-history/v0.6-c8-hot.md
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f src/core/schemas.ts
test "$(grep -cE '^export const [a-z][a-zA-Z]+Schema' src/core/schemas.ts)" = '7'

# doctor v0.5 C4 deep 形态在场字面
test -f src/cli/commands/doctor.ts
test "$(wc -l < src/cli/commands/doctor.ts)" -le 320
grep -qE '^export type DoctorCheck' src/cli/commands/doctor.ts
grep -qE '^export type DoctorReport' src/cli/commands/doctor.ts
grep -qE '^export type RuntimesValue' src/cli/commands/doctor.ts
grep -qE '^export type ClaudeValue' src/cli/commands/doctor.ts
grep -qE '^export type AxpCapability' src/cli/commands/doctor.ts
grep -qE "MIN_BUN = '1\.1\.0'" src/cli/commands/doctor.ts

# doctor.test.ts 18 case 字面在场（实测 baseline；任务描述字面 "26" 偏差已记）
test -f src/cli/__tests__/doctor.test.ts
test "$(grep -cE '^  it\(' src/cli/__tests__/doctor.test.ts)" = '18'

# simx-c4-doctor-smoke.sh 6 字段字面在场 + 当前 all_ok（v0.5 C4 形态）
test -f scripts/simx-c4-doctor-smoke.sh
test "$(wc -l < scripts/simx-c4-doctor-smoke.sh)" -le 60
bash scripts/simx-c4-doctor-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null

# v1.md 边界 [6] + doctor 提及字面在场
grep -qE '^\[6\] 兼容：iOS 17\.5 / 18\.4 / 26\.x 全部通过冒烟集$' docs/v1.md
grep -qE 'simx doctor.*Xcode.*runtime.*claude' docs/v1.md

# roadmap.md v0.7 🔥 字面（未提前翻 ✅）
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# 578 TS baseline + 24 test file 字面（C4 起点）
bun x vitest run 2>&1 | grep -qE 'Tests +578 passed'
bun x vitest run 2>&1 | grep -qE 'Test Files +24 passed'
test "$(find src -name '*.test.ts' -not -path '*/node_modules/*' | wc -l | tr -d ' ')" = '24'

# typecheck baseline 字面
bun run typecheck > /dev/null

# 27 MCP tool baseline 字面
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null

# dep 字面 3 个（不退化）
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'
jq -e '.dependencies["@modelcontextprotocol/sdk"]' package.json > /dev/null
jq -e '.dependencies["citty"]' package.json > /dev/null
jq -e '.dependencies["zod"]' package.json > /dev/null

# C4 红前提：doctor-schemas.ts 文件**不存在**字面
! test -f src/cli/commands/doctor-schemas.ts
! test -f src/cli/__tests__/doctor-schemas.test.ts

# C4 红前提：doctor.ts 字面**无** compatibility 字段
! grep -qE 'compatibility' src/cli/commands/doctor.ts

# 本机 iOS 26.4 单 runtime 字面（C3 baseline 延续）
xcrun simctl list runtimes -j | jq -e '[.runtimes[] | select(.isAvailable == true) | .identifier] | index("com.apple.CoreSimulator.SimRuntime.iOS-26-4") != null' > /dev/null

# Xcode 26.4.x 字面
xcodebuild -version | head -1 | grep -qE '^Xcode 26\.'

# bun >=1.1.0 字面（engines）
test "$(jq -r '.engines.bun' package.json)" = '>=1.1.0'

# 本机非 git 工作树字面延续
! test -d .git
```

期望：以上每条 exit 0。

## 步骤（线性，无分叉；3 个 step）

### S1. 新建 `src/cli/commands/doctor-schemas.ts` — 7 zod schema + 8 z.infer type re-export；改 `src/cli/commands/doctor.ts` 7 个 type alias 改为 import-and-re-export from `./doctor-schemas.js`（行为 0 改、下游 import 0 改）；新建 `src/cli/__tests__/doctor-schemas.test.ts` 5 case round-trip + reject

**红（写测试）**

文件：`src/cli/__tests__/doctor-schemas.test.ts`（新建）

新增 `describe('doctor zod schemas (v0.7 C4)', ...)` block 含 **5 case**：

1. `it('compatibilitySchema parses a valid round-trip', ...)` — fixture `{ status: 'supported', required: 'Xcode >=26.0', actual: '26.4.1' }`；`const parsed = compatibilitySchema.parse(fixture)`；断 `parsed` deep eq fixture 字面；断 `parsed.status === 'supported'` 字面
2. `it('compatibilitySchema rejects invalid status enum', ...)` — fixture `{ status: 'BOGUS' }`；`const result = compatibilitySchema.safeParse(fixture)`；断 `result.success === false` 字面；断 `result.error.issues.some(i => i.path.includes('status'))` 字面
3. `it('doctorCheckSchema parses with optional compatibility', ...)` — fixture-a `{ name: 'bun', ok: true, value: '1.3.14', compatibility: { status: 'supported', required: '>=1.1.0', actual: '1.3.14' }}`；`parsed-a = doctorCheckSchema.parse(fixture-a)`；断 round-trip eq + `parsed-a.compatibility?.status === 'supported'` 字面；fixture-b `{ name: 'xcode', ok: true, value: 'Xcode 26.4.1' }`（无 compatibility）；`parsed-b = doctorCheckSchema.parse(fixture-b)`；断 round-trip eq + `parsed-b.compatibility === undefined` 字面
4. `it('doctorReportSchema parses a 6-check report round-trip', ...)` — fixture `{ exitCode: 0, checks: [<6 check fixtures with compatibility>] }`；`parsed = doctorReportSchema.parse(fixture)`；断 deep eq + `parsed.checks.length === 6` 字面 + `parsed.checks[0].compatibility?.status === 'supported'` 字面
5. `it('doctorReportSchema rejects bad check name enum', ...)` — fixture `{ exitCode: 0, checks: [{name: 'BOGUS', ok: true, value: ''}] }`；`safeParse` → `result.success === false` 字面 + `result.error.issues.some(i => i.path.includes('name'))` 字面

跑 `bun x vitest run src/cli/__tests__/doctor-schemas.test.ts`，5 case 字面**红**（`doctor-schemas.ts` 不存在 → import 失败 / `compatibilitySchema` undefined）；既有 578 case 字面全保绿。

**绿（实现）**

a) 新建 `src/cli/commands/doctor-schemas.ts`（~150-180 行）：

```ts
import { z } from 'zod'

export const compatibilitySchema = z
  .object({
    status: z.enum(['supported', 'unsupported', 'partial', 'unknown']),
    required: z.string().optional(),
    actual: z.string().optional(),
    message: z.string().optional(),
  })
  .strict()

export const runtimesValueSchema = z
  .object({
    count: z.number(),
    items: z.array(
      z
        .object({
          identifier: z.string(),
          name: z.string(),
          version: z.string(),
          isAvailable: z.boolean(),
        })
        .strict(),
    ),
  })
  .strict()

export const claudeValueSchema = z
  .object({
    path: z.string().optional(),
    loggedIn: z.boolean(),
    failureDetail: z.string().optional(),
  })
  .strict()

export const hidChannelAvailabilitySchema = z
  .object({
    digitizer: z.boolean(),
    indigo9: z.boolean(),
  })
  .strict()

export const axpCapabilitySchema = z
  .object({
    framework: z.object({ path: z.string(), loaded: z.boolean() }).strict(),
    classes: z
      .object({ resolved: z.array(z.string()), missing: z.array(z.string()) })
      .strict(),
    selectors: z
      .object({ resolved: z.array(z.string()), missing: z.array(z.string()) })
      .strict(),
    sharedInstance: z.object({ available: z.boolean() }).strict(),
  })
  .strict()

export const doctorCheckNameSchema = z.enum([
  'xcode',
  'runtimes',
  'claude',
  'bun',
  'hid',
  'axp',
])

export const doctorCheckSchema = z
  .object({
    name: doctorCheckNameSchema,
    ok: z.boolean(),
    value: z
      .union([
        z.string(),
        runtimesValueSchema,
        claudeValueSchema,
        hidChannelAvailabilitySchema,
        axpCapabilitySchema,
      ])
      .optional(),
    message: z.string().optional(),
    compatibility: compatibilitySchema.optional(),
  })
  .strict()

export const doctorReportSchema = z
  .object({
    exitCode: z.union([z.literal(0), z.literal(1)]),
    checks: z.array(doctorCheckSchema),
  })
  .strict()

export type Compatibility = z.infer<typeof compatibilitySchema>
export type RuntimesValue = z.infer<typeof runtimesValueSchema>
export type ClaudeValue = z.infer<typeof claudeValueSchema>
export type HidChannelAvailability = z.infer<typeof hidChannelAvailabilitySchema>
export type AxpCapability = z.infer<typeof axpCapabilitySchema>
export type DoctorCheckName = z.infer<typeof doctorCheckNameSchema>
export type DoctorCheck = z.infer<typeof doctorCheckSchema>
export type DoctorReport = z.infer<typeof doctorReportSchema>
```

b) 编辑 `src/cli/commands/doctor.ts`（type 搬迁 only —— 行为字面 0 改、6 check 函数 body 字面 0 改、`MIN_BUN` / `runDoctorCommand` / 4 个 humanLine helper / `gteSemver` 字面 0 改、`renderTail` 字面 0 改）：

- 删 line 8-16（`type RuntimesValue = ...`）
- 删 line 22-26（`type ClaudeValue = ...`）
- 删 line 33-36（`type HidChannelAvailability = ...`）
- 删 line 41-46（`type AxpCapability = ...`）
- 删 line 49（`export type DoctorCheckName = ...`）
- 删 line 51-59（`export type DoctorCheck = ...`）
- 删 line 61-64（`export type DoctorReport = ...`）
- 在文件顶部 import 段加 2 行：
  ```ts
  import type {
    Compatibility,
    RuntimesValue,
    ClaudeValue,
    HidChannelAvailability,
    AxpCapability,
    DoctorCheckName,
    DoctorCheck,
    DoctorReport,
  } from './doctor-schemas.js'
  export type {
    Compatibility,
    RuntimesValue,
    ClaudeValue,
    HidChannelAvailability,
    AxpCapability,
    DoctorCheckName,
    DoctorCheck,
    DoctorReport,
  }
  ```
- 保留 `export type ProbeXcode` / `ProbeRuntimes` / `ProbeClaude` / `ProbeBun` / `ProbeHidChannels` / `ProbeAxp` 字面 0 改（Probe callback types 留在 doctor.ts 本地）
- 保留 `export type RunDoctorDeps` 字面 0 改
- 保留 `const MIN_BUN = '1.1.0'` 字面 0 改

跑 `bun x vitest run src/cli/__tests__/doctor-schemas.test.ts`，5 case 应**绿**。

跑 `bun x vitest run`，既有 578 case + 5 新 = **583 case** / **25 test file** 字面全绿。

跑 `bun run typecheck`，exit 0（type re-export 链路通：`src/cli/index.ts` line 10 `import { runDoctorCommand, type AxpCapability } from './commands/doctor.js'` 字面 0 改 + `src/cli/__tests__/doctor.test.ts` line 2-8 import 字面 0 改 —— doctor.ts re-export 链路保留）。

**重构**

无。

---

### S2. 改 `src/cli/commands/doctor.ts` — 加 `deriveCompatibility(name, ok, value, message, probeRejected): Compatibility` helper + 6 check 函数 ok / fail / catch 三路径字面填 `compatibility` 字段；改 `src/cli/__tests__/doctor.test.ts` +3 case 字面验 compatibility 派生 + zod round-trip

**红（写测试）**

文件：`src/cli/__tests__/doctor.test.ts`（编辑）

在末尾（`describe('runDoctorCommand', () => { ... })` 块内、紧贴既有末段 `describe('C4: runtimes list + claude logged-in (v0.5 C4)', ...)` 块之后）加 1 个新 describe block：

```ts
describe('C4: compatibility field (v0.7 C4)', () => {
  // case 19
  it('every check has compatibility field with status enum + optional required/actual', async () => {
    const { deps, bufs } = okDeps({ json: true })
    const res = await runDoctorCommand(deps)
    expect(res.exitCode).toBe(0)
    const parsed = JSON.parse(bufs.readOut().trim())
    expect(parsed.checks).toHaveLength(6)
    const validStatus = ['supported', 'unsupported', 'partial', 'unknown']
    for (const c of parsed.checks) {
      expect(c.compatibility).toBeDefined()
      expect(typeof c.compatibility.status).toBe('string')
      expect(validStatus).toContain(c.compatibility.status)
    }
    const byName = Object.fromEntries(
      parsed.checks.map(
        (c: { name: string; compatibility: { required?: string } }) => [c.name, c.compatibility],
      ),
    )
    expect(byName.xcode.required).toMatch(/Xcode/)
    expect(byName.bun.required).toBe('>=1.1.0')
    expect(byName.claude.required).toBe('logged-in')
    expect(byName.hid.required).toMatch(/digitizer/)
  })

  // case 20
  it('compatibility status derived correctly for partial / unsupported scenarios', async () => {
    // (a) bun 1.0.30 → partial + actual 1.0.30
    {
      const { deps, bufs } = okDeps({
        probeBun: vi.fn().mockReturnValueOnce('1.0.30'),
        json: true,
      })
      await runDoctorCommand(deps)
      const parsed = JSON.parse(bufs.readOut().trim())
      expect(parsed.checks[3].compatibility.status).toBe('partial')
      expect(parsed.checks[3].compatibility.actual).toBe('1.0.30')
    }
    // (b) runtimes 0 items → unsupported
    {
      const { deps, bufs } = okDeps({
        probeRuntimes: vi.fn().mockResolvedValueOnce({ count: 0, items: [] }),
        json: true,
      })
      await runDoctorCommand(deps)
      const parsed = JSON.parse(bufs.readOut().trim())
      expect(parsed.checks[1].compatibility.status).toBe('unsupported')
    }
    // (c) claude path=undefined → unsupported
    {
      const { deps, bufs } = okDeps({
        probeClaude: vi
          .fn()
          .mockResolvedValueOnce({ path: undefined, loggedIn: false }),
        json: true,
      })
      await runDoctorCommand(deps)
      const parsed = JSON.parse(bufs.readOut().trim())
      expect(parsed.checks[2].compatibility.status).toBe('unsupported')
    }
    // (d) xcode probe reject → unknown
    {
      const { deps, bufs } = okDeps({
        probeXcode: vi.fn().mockRejectedValueOnce(new Error('xcodebuild not found')),
        json: true,
      })
      await runDoctorCommand(deps)
      const parsed = JSON.parse(bufs.readOut().trim())
      expect(parsed.checks[0].compatibility.status).toBe('unknown')
    }
  })

  // case 21
  it('doctorReportSchema.parse round-trips a full --json output', async () => {
    const { doctorReportSchema } = await import('../commands/doctor-schemas.js')
    const { deps, bufs } = okDeps({ json: true })
    await runDoctorCommand(deps)
    const parsed = JSON.parse(bufs.readOut().trim())
    const result = doctorReportSchema.safeParse(parsed)
    expect(result.success).toBe(true)
    if (result.success) {
      expect(result.data.exitCode).toBe(0)
      expect(result.data.checks).toHaveLength(6)
    }
  })
})
```

跑 `bun x vitest run src/cli/__tests__/doctor.test.ts`，3 新 case 应**红**（doctor.ts 字面无 compatibility 字段 → `c.compatibility` undefined → assertion fail）；既有 18 case 字面全保绿。

**绿（实现）**

改 `src/cli/commands/doctor.ts`（决策 4.A/4.B/4.C 字面）：

a) 在 6 个 check 函数之前（line ~165，紧贴既有 `function runDoctorCommand` 之上 / `const MIN_BUN` 之下）加 helper：

```ts
function deriveCompatibility(
  name: DoctorCheckName,
  ok: boolean,
  value: DoctorCheck['value'],
  message: string | undefined,
  probeRejected: boolean,
): Compatibility {
  if (probeRejected) {
    return { status: 'unknown', ...(message !== undefined ? { message } : {}) }
  }
  switch (name) {
    case 'xcode': {
      const required = 'Xcode >=26.0'
      const v = typeof value === 'string' ? value : ''
      const m = /Xcode\s+([0-9]+(?:\.[0-9]+)*)/.exec(v)
      const actual = m?.[1] ?? v
      if (!ok || m === null) return { status: 'partial', required, actual }
      return gteSemver(m[1] ?? '0', '26.0')
        ? { status: 'supported', required, actual }
        : { status: 'partial', required, actual }
    }
    case 'runtimes': {
      const required = 'iOS 26.x present (17.5 / 18.4 nice-to-have)'
      if (typeof value === 'object' && value !== null && 'items' in value) {
        const rv = value as RuntimesValue
        if (rv.items.length === 0) {
          return { status: 'unsupported', required, actual: '0 available' }
        }
        const has26 = rv.items.some((r) => /iOS-26(?:-|$)/.test(r.identifier))
        const head = rv.items.slice(0, 5).map((r) => r.identifier).join(', ')
        const suffix = rv.items.length > 5 ? ', ...' : ''
        const actual = `${rv.items.length} available: ${head}${suffix}`
        return { status: has26 ? 'supported' : 'partial', required, actual }
      }
      return { status: 'unsupported', required, actual: '0 available' }
    }
    case 'claude': {
      const required = 'logged-in'
      if (typeof value === 'object' && value !== null && 'loggedIn' in value) {
        const cv = value as ClaudeValue
        if (cv.path === undefined) {
          return { status: 'unsupported', required, actual: 'missing' }
        }
        return cv.loggedIn
          ? { status: 'supported', required, actual: 'logged-in' }
          : { status: 'partial', required, actual: 'not-logged-in' }
      }
      return { status: 'unsupported', required, actual: 'missing' }
    }
    case 'bun': {
      const required = `>=${MIN_BUN}`
      const v = typeof value === 'string' ? value : undefined
      if (v === undefined) return { status: 'unsupported', required, actual: 'undefined' }
      return gteSemver(v, MIN_BUN)
        ? { status: 'supported', required, actual: v }
        : { status: 'partial', required, actual: v }
    }
    case 'hid': {
      const required = 'digitizer + indigo9'
      if (typeof value === 'object' && value !== null && 'digitizer' in value) {
        const hv = value as HidChannelAvailability
        const actual = `digitizer=${hv.digitizer}, indigo9=${hv.indigo9}`
        if (hv.digitizer && hv.indigo9) return { status: 'supported', required, actual }
        if (hv.digitizer || hv.indigo9) return { status: 'partial', required, actual }
        return { status: 'unsupported', required, actual }
      }
      return { status: 'unsupported', required, actual: 'no channels' }
    }
    case 'axp': {
      const required = 'framework + 5 classes + 3 selectors + +sharedInstance'
      if (typeof value === 'object' && value !== null && 'framework' in value) {
        const av = value as AxpCapability
        const cTot = av.classes.resolved.length + av.classes.missing.length
        const sTot = av.selectors.resolved.length + av.selectors.missing.length
        const actual =
          `framework=${av.framework.loaded}, classes=${av.classes.resolved.length}/${cTot}, ` +
          `selectors=${av.selectors.resolved.length}/${sTot}, ` +
          `sharedInstance=${av.sharedInstance.available}`
        const allGood =
          av.framework.loaded &&
          av.classes.missing.length === 0 &&
          av.selectors.missing.length === 0 &&
          av.sharedInstance.available
        if (allGood) return { status: 'supported', required, actual }
        if (av.framework.loaded) return { status: 'partial', required, actual }
        return { status: 'unsupported', required, actual }
      }
      return { status: 'unsupported', required, actual: 'no axp data' }
    }
  }
}
```

b) 6 个 check 函数 字面追加 `compatibility` 字段（决策 4.C 字面 —— 每个函数 ok / fail / catch 三路径字面追加 in-place；ok / fail 传 `probeRejected=false`、catch 传 `probeRejected=true`；handler 行为字面 0 改）。

示例 `checkXcode`（line 165-176 改为）：
```ts
async function checkXcode(probe: ProbeXcode): Promise<DoctorCheck> {
  try {
    const line = (await probe()).trim()
    if (/^Xcode \d+\./.test(line)) {
      return {
        name: 'xcode',
        ok: true,
        value: line,
        compatibility: deriveCompatibility('xcode', true, line, undefined, false),
      }
    }
    const message = `unexpected output: ${line}`
    return {
      name: 'xcode',
      ok: false,
      value: line,
      message,
      compatibility: deriveCompatibility('xcode', false, line, message, false),
    }
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    const message = `xcodebuild failed: ${msg}`
    return {
      name: 'xcode',
      ok: false,
      message,
      compatibility: deriveCompatibility('xcode', false, undefined, message, true),
    }
  }
}
```

其余 5 个 check 函数（`checkRuntimes` / `checkClaude` / `checkBun` / `checkHid` / `checkAxp`）字面同 pattern in-place 追加（每函数 2-3 路径 × 6 = ~15-18 处字面追加）。

跑 `bun x vitest run src/cli/__tests__/doctor.test.ts`，21 case 字面全绿（既有 18 + 3 新 C4）。

跑 `bun x vitest run`，全跑：578 + 5 schemas + 3 doctor C4 = **586 case** / **25 test files** 字面全绿。

跑 `bun run typecheck`，exit 0。

**重构**

无。

---

### S3. 编辑 `scripts/simx-c4-doctor-smoke.sh` 6 → 9 字段（加 `has_compatibility_field` / `compat_status_enum_ok` / `runtimes_compat_supported`）；本机字面 all_ok=ok；改 `docs/v1.md` 决策日志 +1 行 v0.7 C4 close note

**红（写测试）**

无新 vitest case（S1 + S2 已 5 + 3 case 覆盖；smoke 是 e2e gate，字面契约用 grep / jq）。

红前提：当前 `bash scripts/simx-c4-doctor-smoke.sh` 字面 6 字段（v0.5 C4 形态）；C4 后字面 9 字段（v0.7 C4 升级）。

**绿（实现）**

a) 编辑 `scripts/simx-c4-doctor-smoke.sh`（58 → ~80 行）。最终内容字面：

```bash
#!/usr/bin/env bash
# simx-c4-doctor-smoke.sh — v0.7 C4 doctor JSON schema gate (upgraded
# from v0.5 C4 6-field shape to v0.7 C4 9-field shape including
# compatibility-status coverage).
#
# Real-run `simx doctor --json`, parse the output, and emit a 9-field
# single-line JSON gate result:
#   {exit_code, checks_len, schema_ok, runtimes_has_items,
#    claude_logged_in, has_compatibility_field, compat_status_enum_ok,
#    runtimes_compat_supported, all_ok}
#
# Exit 0 iff every gated field is "ok"; non-zero iff any field fails.
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

RAW="$(bun src/cli/index.ts doctor --json 2>/dev/null || echo '{}')"

EXIT_CODE="$(echo "$RAW" | jq -r '.exitCode // -1')"
CHECKS_LEN="$(echo "$RAW" | jq -r '.checks | length' 2>/dev/null || echo 0)"
CHECKS_LEN="${CHECKS_LEN:-0}"

NAMES="$(echo "$RAW" | jq -c '.checks | map(.name)' 2>/dev/null || echo '[]')"
SCHEMA_OK="fail"
if [[ "$NAMES" == '["xcode","runtimes","claude","bun","hid","axp"]' ]]; then
  BAD="$(echo "$RAW" | jq '[
    .checks[] |
    select(
      (.name | type) != "string"
      or (.ok | type) != "boolean"
      or (.value == null and .message == null)
    )
  ] | length' 2>/dev/null || echo 99)"
  [[ "$BAD" == "0" ]] && SCHEMA_OK="ok"
fi

RUNTIMES_HAS_ITEMS="fail"
ITEMS_LEN="$(echo "$RAW" | jq '.checks[1].value.items | length' 2>/dev/null || echo 0)"
ITEMS_LEN="${ITEMS_LEN:-0}"
[[ "$ITEMS_LEN" -ge 1 ]] && RUNTIMES_HAS_ITEMS="ok"

CLAUDE_LOGGED_IN="fail"
LOGGED_IN="$(echo "$RAW" | jq -r '.checks[2].value.loggedIn // false' 2>/dev/null || echo false)"
[[ "$LOGGED_IN" == "true" ]] && CLAUDE_LOGGED_IN="ok"

# v0.7 C4 — compatibility field present on every check.
HAS_COMPAT="fail"
MISSING_COMPAT="$(echo "$RAW" | jq '[.checks[] | select(.compatibility == null)] | length' 2>/dev/null || echo 99)"
[[ "$MISSING_COMPAT" == "0" ]] && HAS_COMPAT="ok"

# v0.7 C4 — every compatibility.status in locked 4-enum.
COMPAT_STATUS_OK="fail"
BAD_STATUS="$(echo "$RAW" | jq '[
  .checks[]
  | .compatibility.status
  | select(. != "supported" and . != "unsupported" and . != "partial" and . != "unknown")
] | length' 2>/dev/null || echo 99)"
[[ "$BAD_STATUS" == "0" ]] && COMPAT_STATUS_OK="ok"

# v0.7 C4 — runtimes.compatibility.status === "supported" on hosts that
# have at least one iOS-26 runtime installed (CI macos-15 + dev baseline).
RUNTIMES_COMPAT_SUPPORTED="fail"
RT_STATUS="$(echo "$RAW" | jq -r '.checks[1].compatibility.status // "fail"' 2>/dev/null || echo fail)"
[[ "$RT_STATUS" == "supported" ]] && RUNTIMES_COMPAT_SUPPORTED="ok"

ALL_OK="fail"
if [[ "$EXIT_CODE" == "0" \
      && "$CHECKS_LEN" == "6" \
      && "$SCHEMA_OK" == "ok" \
      && "$RUNTIMES_HAS_ITEMS" == "ok" \
      && "$CLAUDE_LOGGED_IN" == "ok" \
      && "$HAS_COMPAT" == "ok" \
      && "$COMPAT_STATUS_OK" == "ok" \
      && "$RUNTIMES_COMPAT_SUPPORTED" == "ok" ]]; then
  ALL_OK="ok"
fi

printf '{"exit_code":%s,"checks_len":%s,"schema_ok":"%s","runtimes_has_items":"%s","claude_logged_in":"%s","has_compatibility_field":"%s","compat_status_enum_ok":"%s","runtimes_compat_supported":"%s","all_ok":"%s"}\n' \
  "$EXIT_CODE" "$CHECKS_LEN" "$SCHEMA_OK" "$RUNTIMES_HAS_ITEMS" "$CLAUDE_LOGGED_IN" \
  "$HAS_COMPAT" "$COMPAT_STATUS_OK" "$RUNTIMES_COMPAT_SUPPORTED" "$ALL_OK"

[[ "$ALL_OK" == "ok" ]] && exit 0 || exit 1
```

字面验证（S3 内 inline）：
```bash
bash scripts/simx-c4-doctor-smoke.sh > /tmp/c4-doctor.json
jq -e '.all_ok == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.has_compatibility_field == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.compat_status_enum_ok == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.runtimes_compat_supported == "ok"' /tmp/c4-doctor.json > /dev/null
# 既有 5 实义字段语义全保
jq -e '.exit_code == 0' /tmp/c4-doctor.json > /dev/null
jq -e '.checks_len == 6' /tmp/c4-doctor.json > /dev/null
jq -e '.schema_ok == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.runtimes_has_items == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.claude_logged_in == "ok"' /tmp/c4-doctor.json > /dev/null
```

期望：每条 exit 0。

b) 改 `docs/v1.md` 决策日志末尾 +1 行 v0.7 C4 close note 字面（仿 C3 close note pattern；不动 §2/§4/§5/§6 边界）：

```
- **2026-05-16** v0.7 C4 close — `simx doctor --json` 每 check 字面追加 `compatibility: { status, required?, actual?, message? }` 字段（status enum 字面 `'supported' | 'unsupported' | 'partial' | 'unknown'`）+ 6 check 派生策略字面：xcode required=`'Xcode >=26.0'` actual=parsed semver / runtimes required=`'iOS 26.x present (17.5 / 18.4 nice-to-have)'` actual=`<n> available: <id>...` / claude required=`'logged-in'` actual=logged-in|not-logged-in|missing / bun required=`'>=1.1.0'` actual=version / hid required=`'digitizer + indigo9'` actual=`digitizer=<bool>, indigo9=<bool>` / axp required=`'framework + 5 classes + 3 selectors + +sharedInstance'` actual=framework+counts+sharedInstance；zod schema 固化字面新建 `src/cli/commands/doctor-schemas.ts` ~150-180 行 / 7 schema export（`compatibilitySchema` / `runtimesValueSchema` / `claudeValueSchema` / `hidChannelAvailabilitySchema` / `axpCapabilitySchema` / `doctorCheckSchema` / `doctorReportSchema` + `doctorCheckNameSchema` 内部依赖）+ 8 z.infer type re-export；既有 `src/cli/commands/doctor.ts` 7 type alias 改为 import-and-re-export from `./doctor-schemas.js`（下游 `src/cli/index.ts` line 10 import 字面 0 改）+ `deriveCompatibility(name, ok, value, message, probeRejected): Compatibility` helper ~60 行 + 6 check 函数 ok/fail/catch 三路径字面追加 compatibility 字段（handler 行为字面 0 改）；既有 18 doctor case 字面全保绿 + 新 +3 doctor case（compat 字段验 + 4 sub-scenarios status 派生 + zod round-trip）+ 新建 `src/cli/__tests__/doctor-schemas.test.ts` 5 case（compat round-trip / status enum reject / doctorCheckSchema optional compatibility / doctorReportSchema 6-check round-trip / bad name enum reject）；TS baseline 578 → **586**（+3 doctor + 5 doctor-schemas）/ 24 → **25 test files**（+ doctor-schemas.test.ts）/ 102 swift 不变 / 27 MCP tool 不变 / typecheck 0 error / 6 既有 MCP smoke 0 退化 / 17 既有 c[1-8]-* smoke 中 16 个 0 改（仅 `scripts/simx-c4-doctor-smoke.sh` 字面 6→9 字段升级：加 `has_compatibility_field` / `compat_status_enum_ok` / `runtimes_compat_supported`，既有 5 实义字段 grep/jq 语义字面 0 改） / 0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/core/schemas.ts 改（doctor schemas CLI-local 独立） / 0 既有 src/sim 6 文件 / 0 既有 src/{driver,sdk,core,mcp} / 0 既有 src/cli/index.ts / 0 既有 src/cli/commands 中 doctor.ts 之外 / 0 dep / 0 既有 27 ToolDef / 0 既有 24 test file 中 23 文件 / 0 ci.yml 改 / 0 v1.md 边界改 / 0 roadmap.md 改 / 0 cold v0.7 改 / 0 examples 改 / 0 既有 scripts/long-run-100.ts / mcp-smoke.ts / runner-supervisor 改。**cold plan §C4 字面解读**："把每个 check 的版本兼容性输出（结构化 schema 固化）" = (a) 版本兼容性 = 每 check 加 compatibility 字段 + 4-enum status + required/actual 字面派生字符串；(b) schema 固化 = doctor-schemas.ts 单点 zod SoT + DoctorCheck/DoctorReport z.infer 派生 + doctorReportSchema 整 --json 出参 round-trip 通；(c) 拒绝路径字面：拒 落盘 `.schema.json`（zod SoT，v0.6 C7 同 pattern）/ 拒 引 `zod-to-json-schema` dep（0 dep 增）/ 拒 给 Driver/MCP wire 加 compatibility（doctor CLI-local）/ 拒 改既有 v0.5 C4 wire 字段（runtimes.items / claude.loggedIn 字面冻结，C4 v0.7 是 additive only）/ 拒 加 `--check <name>` 子参数（推 v1.1+）/ 拒 加 `compatibility.severity` 字段（ok 已表达 fail-or-pass）/ 拒 doctor-schemas 注入 src/core/schemas.ts（CLI-local 独立 SoT 边界）/ 拒 加 `simx doctor` 人读路径渲染 compatibility 行（人读 ok/✓/✗ + value/message 已 readable）。**任务描述 18 vs "26" 偏差字面记**：本任务字面 "26 case 现状" 实测 `grep -cE '^  it\(' src/cli/__tests__/doctor.test.ts = 18`，按实际 18 case baseline 推进；C4 close 后 21 case + 5 doctor-schemas case = 586 总 case。**runtimes compatibility 阈值字面 26.x-only 而非 17/18/26.x-all 字面记**：v1.md §4 [6] 终态目标 = 17.5+18.4+26.x 全过；C4 doctor compatibility = 26.x 在场 = supported（与 v0.7 C3 ci.yml hard-required 26.4 同步；17/18 缺席不降 status —— 否则本机 dev 环境 doctor 永远红，违反 doctor 是 dev-friendly 工具的原则；硬化 17/18 推 v0.7+ 5-arg HID 实装窗口；v1.0 acceptance C6 时若 17/18 也字面在场则 doctor compatibility 仍 supported —— required 字面就字面说明 "iOS 26.x present (17.5 / 18.4 nice-to-have)"）。**`simx-c4-doctor-smoke.sh` 跨版本同名复用字面记**：cold plan v0.5 C4 已建 6 字段形态 + cold plan v0.7 C4 同名升级 9 字段；不重命名（文件名继承 v0.5 C4 已落地命名 + v0.7 C4 是其增量升级；改名留 v0.5 死链接 + v0.5 acceptance 行为不退化）。**git 工作树缺席偏差延续记**：lab15-autofix 仍非 git 工作树；C4 范围 0 git 依赖；CI 上 `bash scripts/simx-c4-doctor-smoke.sh` 推 C5+ 进 ci.yml 验（C4 范围 ci.yml 0 改）。**本机 iOS 17/18 runtime 缺席延续记**：`xcrun simctl list runtimes -j` 字面单 iOS-26-4；C4 范围内不本机装 17/18；doctor runtimes compatibility 在本机 supported 字面（has iOS-26）/ 17-18 缺席不降 status。**C5 推**：Claude Code plugin manifest `claude-plugin.json` + 安装说明；**C6 推**：v1.0 自动化验收 `scripts/v1-acceptance.sh` 聚合 §4 7 success criteria；**C7 推**：README / docs/ v1.0 发布 + git init / branch protection 人工说明 + 17/18 5-arg 真路径实装窗口提示。
```

**重构**

无。

---

## Checkpoint C4 验收

```bash
set -uo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# (1) typecheck 绿
set -e
bun run typecheck

# (2) vitest 全绿 586 case / 25 test file
bun x vitest run 2>&1 | grep -qE 'Tests +586 passed'
bun x vitest run 2>&1 | grep -qE 'Test Files +25 passed'
test "$(find src -name '*.test.ts' -not -path '*/node_modules/*' | wc -l | tr -d ' ')" = '25'

# (3) doctor-schemas.ts 字面在场 + 7 zod schema + 8 type re-export
test -f src/cli/commands/doctor-schemas.ts
test "$(grep -cE '^export const [a-zA-Z]+Schema' src/cli/commands/doctor-schemas.ts)" -ge 7
grep -qE '^export const compatibilitySchema' src/cli/commands/doctor-schemas.ts
grep -qE '^export const doctorReportSchema' src/cli/commands/doctor-schemas.ts
grep -qE '^export const doctorCheckSchema' src/cli/commands/doctor-schemas.ts
grep -qE '^export const runtimesValueSchema' src/cli/commands/doctor-schemas.ts
grep -qE '^export const claudeValueSchema' src/cli/commands/doctor-schemas.ts
grep -qE '^export const hidChannelAvailabilitySchema' src/cli/commands/doctor-schemas.ts
grep -qE '^export const axpCapabilitySchema' src/cli/commands/doctor-schemas.ts
grep -qE '^export type Compatibility' src/cli/commands/doctor-schemas.ts
grep -qE '^export type DoctorReport' src/cli/commands/doctor-schemas.ts
grep -qE '^export type DoctorCheck' src/cli/commands/doctor-schemas.ts

# (4) doctor.ts type alias 改为 re-export from './doctor-schemas.js'
grep -qE "from './doctor-schemas\\.js'" src/cli/commands/doctor.ts
grep -qE "export type \{" src/cli/commands/doctor.ts
! grep -qE '^export type DoctorCheck = \{' src/cli/commands/doctor.ts
! grep -qE '^export type DoctorReport = \{' src/cli/commands/doctor.ts
! grep -qE '^export type RuntimesValue = \{' src/cli/commands/doctor.ts

# (5) deriveCompatibility helper 在场
grep -qE '^function deriveCompatibility' src/cli/commands/doctor.ts

# (6) 6 check 函数 字面带 compatibility（至少 6 处 ok-path 字面追加）
test "$(grep -cE 'compatibility: deriveCompatibility\(' src/cli/commands/doctor.ts)" -ge 6

# (7) doctor.test.ts 字面 18 顶层 it + 3 nested C4 it
test "$(grep -cE '^  it\(' src/cli/__tests__/doctor.test.ts)" = '18'
test "$(grep -cE '^    it\(' src/cli/__tests__/doctor.test.ts)" -ge 3
grep -qE "describe\('C4: compatibility field" src/cli/__tests__/doctor.test.ts

# (8) doctor-schemas.test.ts 5 case 字面
test -f src/cli/__tests__/doctor-schemas.test.ts
test "$(grep -cE '^  it\(' src/cli/__tests__/doctor-schemas.test.ts)" = '5'

# (9) simx-c4-doctor-smoke.sh 9 字段 + all_ok
bash scripts/simx-c4-doctor-smoke.sh > /tmp/c4-doctor.json
jq -e '.all_ok == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.exit_code == 0' /tmp/c4-doctor.json > /dev/null
jq -e '.checks_len == 6' /tmp/c4-doctor.json > /dev/null
jq -e '.schema_ok == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.runtimes_has_items == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.claude_logged_in == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.has_compatibility_field == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.compat_status_enum_ok == "ok"' /tmp/c4-doctor.json > /dev/null
jq -e '.runtimes_compat_supported == "ok"' /tmp/c4-doctor.json > /dev/null

# (10) doctor --json 真实输出含 compatibility 字段（每 6 check）
RAW="$(bun src/cli/index.ts doctor --json 2>/dev/null)"
test "$(echo "$RAW" | jq '[.checks[] | select(.compatibility != null)] | length')" = '6'

# (11) doctor --json 真实输出含具体 required 字面
echo "$RAW" | jq -e '.checks[0].compatibility.required == "Xcode >=26.0"' > /dev/null
echo "$RAW" | jq -e '.checks[3].compatibility.required == ">=1.1.0"' > /dev/null
echo "$RAW" | jq -e '.checks[2].compatibility.required == "logged-in"' > /dev/null
echo "$RAW" | jq -e '.checks[1].compatibility.status == "supported"' > /dev/null
echo "$RAW" | jq -e '.checks[0].compatibility.status == "supported"' > /dev/null

# (12) 退化 — 27 MCP tool / 6 既有 MCP smoke 全保
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok" and .tools_list_count == 27' > /dev/null

# (13) 退化 — v0.7 C1/C2/C3 工程实体 + v0.6 不动
test -f src/sim/runner-supervisor.ts
test -f scripts/long-run-100.ts
test -f scripts/simx-c1-longrun-100-smoke.sh
test -f scripts/simx-c2-ci-workflow-validate.sh
test -f scripts/simx-c3-ci-matrix-validate.sh
test -f .github/workflows/ci.yml
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f src/core/schemas.ts

# (14) 退化 — dep 字面 3 个
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'

# (15) 退化 — v1.md / roadmap.md / cold v0.7 字面 0 边界改
grep -qE '^\[6\] 兼容：iOS 17\.5 / 18\.4 / 26\.x 全部通过冒烟集$' docs/v1.md
grep -qE 'simx doctor.*Xcode.*runtime.*claude' docs/v1.md
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# (16) 退化 — 既有 src/core/schemas.ts 7 schema 不动
test "$(grep -cE '^export const [a-z][a-zA-Z]+Schema' src/core/schemas.ts)" = '7'

# (17) 退化 — 既有 ci.yml / c3 / c2 validator 不动
bash scripts/simx-c3-ci-matrix-validate.sh | jq -e '.all_ok == "ok"' > /dev/null
test "$(wc -l < .github/workflows/ci.yml)" -le 100

# (18) docs/v1.md 决策日志 +1 行 v0.7 C4 close
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C4 close' docs/v1.md

# (19) doctor.ts 行数 cap
test "$(wc -l < src/cli/commands/doctor.ts)" -le 380

# (20) 退化 — 17 c[1-8] smoke 中 16 个文件仍在场（仅 simx-c4-doctor-smoke.sh 升级）
for f in scripts/simx-c1-mcp-smoke.sh scripts/simx-c1-longrun-100-smoke.sh \
         scripts/simx-c2-matcher-failure-smoke.sh scripts/simx-c2-mcp-lifecycle-smoke.sh \
         scripts/simx-c3-mcp-observe-smoke.sh scripts/simx-c3-save-e2e-smoke.sh \
         scripts/simx-c3-suggestions-smoke.sh scripts/simx-c4-mcp-interaction-smoke.sh \
         scripts/simx-c4-steps-jsonl-smoke.sh scripts/simx-c5-mcp-compound-system-smoke.sh \
         scripts/simx-c5-run-flags-smoke.sh scripts/simx-c5-step-png-smoke.sh \
         scripts/simx-c6-boot-smoke.sh scripts/simx-c6-failure-json-smoke.sh \
         scripts/simx-c6-mcp-explain-screen-smoke.sh scripts/simx-c6-resolver-tap-smoke.sh \
         scripts/simx-c8-dogfood-smoke.sh ; do
  test -f "$f"
done

# (21) 退化 — src/cli/index.ts 字面 0 改（doctor wiring 不动）
grep -qE "import \{ runDoctorCommand, type AxpCapability \} from './commands/doctor\\.js'" src/cli/index.ts

echo 'C4 verdict: ok'
```

期望：以上每条 exit 0 + 末行字面 `C4 verdict: ok`。

## 完成后动作

1. 归档此文件到 `docs/plan-history/v0.7-c4-hot.md`：
   ```bash
   mv docs/plan-hot.md docs/plan-history/v0.7-c4-hot.md
   ```
2. **改 `docs/v1.md` 决策日志末尾 +1 行**（S3.b 已落字面 v0.7 C4 close note）
3. 调 sub-agent 生成新 `plan-hot.md`（到 v0.7 C5 Claude Code plugin manifest `claude-plugin.json` + 安装说明），见 CLAUDE.md §6 标准模板 — **本计划范围内不展开 C5**；C5 热化时 sub-agent 须先 WebFetch Claude Code 最新 plugin 文档（cold plan §触发模板字面，不假设记忆 schema）。
