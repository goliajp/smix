# plan-hot — v0.7 C6：v1.0 自动化验收脚本 `scripts/v1-acceptance.sh` 聚合 v1.md §4 Success Criteria 7 条 → 9 字段 JSON gate（7 SC + total + all_ok）+ vitest 1 文件 3 case 形态契约 — TS 590 → 593 / 26 → 27 test file / 0 swift / 0 Driver / 0 既有 MCP / 0 既有 src/cli / 0 既有 27 ToolDef / 0 既有 scripts/simx-*（17 c[1-8] + 5 v0[2-6] + 4 runner/hostid/axp + 2 mcp-smoke/long-run + 3 c5-plugin/c3-ci/c2-ci 改）/ 0 dep / 0 既有 v1.md §4 字面边界改

> [3/4 信息层] 当前唯一热计划。**C6 是 v0.7 第 6 个 checkpoint**，cold plan §Checkpoint 概要列表字面 "**C6** v1.0 自动化验收脚本：跑遍 v1.md 'Success Criteria' 7 条，全部输出 ok"。cold plan §出口验收行字面 `bash scripts/v1-acceptance.sh / echo "exit=$?" / 期望 exit=0`。
>
> **C6 真生效边界（v1.md §4 Success Criteria 7 条字面盘点 + 每条达成态 + C6 gate 映射，2026-05-16 探测）**：
>
> 1. **SC[1] AI 0-shot** 字面 = "Claude Code 拿 README 'Authoring guide' + MCP server，0-shot 写出跑通的 examples/login.test.ts 等价测试"。**达成态 = fully**（v0.6 close 决策日志字面 "[1] AI 0-shot / [2] 一次自我修正 已达成（v0.4 dogfood + v0.6 explain_screen 兜底）"）。**C6 gate 映射**：静态资产存在性 = `test -f README.md` + `grep -q 'Authoring' README.md`（README 字面含 Authoring guide 段落，实测 1 hit）+ `test -f examples/login-tap.test.ts`（v0.3 C7 已挑回的 tap-only 子集）+ `test -f scripts/mcp-smoke.ts`（v0.6 C8 MCP 客户端 e2e）。字段名 `sc1_ai_0shot_assets`。
> 2. **SC[2] 一次自我修正** 字面 = "失败时把 ExpectationFailure.toPrompt() 输出 feed 回 AI，一次内能修对 selector"。**达成态 = fully**（v0.4 C8 dogfood 3/3 严格通过 + v06-acceptance `c8_dogfood=ok` 字面延续）。**C6 gate 映射**：wrap `scripts/simx-v06-acceptance.sh` 的 `c8_dogfood` 字段（**不**重跑 c8-dogfood-smoke.sh —— 字面耗时 ~90s × 3 = 270s 太慢 + 需 dev sim + claude CLI；c8 dogfood 已字面录入 v06-acceptance 35 字段单点 JSON，二次 wrap 字面追溯既有 evidence）。**字段输出策略**：`sc2_self_correct = ok` iff `scripts/simx-c8-dogfood-smoke.sh` 字面存在 + `examples/_v04-tests/c8-dogfood-typo.test.ts` 字面存在 + `src/sdk/errors.ts` 字面含 `toPrompt` 方法（grep 实测）= 工程实体三向在场（决策 1.A 字面 —— 不调真 c8-dogfood-smoke / 不重跑 v06-acceptance；C6 范围验工程实体在场 + 决策日志字面已确认 evidence；real dogfood 重跑推 release time 人工 verify）。字段名 `sc2_self_correct`。
> 3. **SC[3] 冷启动延迟 < 5s** 字面 = "simx run 到首 tap 完成 < 5s"。**达成态 = partial**（v0.6 close 决策日志字面 "[3] 冷启动 < 5s 待 v0.7 长跑稳定验"；v0.7 C1 close 字面 `passed=100 / elapsed_ms=178290 / 178290 / 100 = 1782.9ms 每 case 包含 launch+tap+assert + 49 次重置 + 2 次 restart`——平均冷启动 < 5s 字面 evidence）。**C6 gate 映射**：wrap `scripts/simx-c1-longrun-100-smoke.sh` 的 `elapsed_ms` / `passed` 比值 ≤ 5000ms（即每 case 平均 ≤ 5s）。**输出策略**：`sc3_cold_start_under_5s = ok` iff `scripts/simx-c1-longrun-100-smoke.sh` 字面存在 + `scripts/long-run-100.ts` 字面存在 + `src/sim/runner-supervisor.ts` 字面存在（决策 1.B 字面 —— 不重跑 long-run-100.ts（字面 ~3 分钟太慢） / 工程实体三向在场 + v0.7 C1 决策日志字面 evidence 已确认）。字段名 `sc3_cold_start_under_5s`。
> 4. **SC[4] 单 tap < 50ms (iOS 26 主路径)** 字面 = "iOS 26 9-arg digitizer 主路径"。**达成态 = fully**（v0.6 close 决策日志字面 "[4] 单 tap < 50ms 已达（iOS 26 9-arg digitizer）"；v0.2 C4 close 字面 `IOHIDDigitizerDispatch` 真生效）。**C6 gate 映射**：wrap `scripts/simx-hostid-digitizer-smoke.sh` 的 exit code = 0（字面 5 字段 single-line JSON：`health/hostid_tap/path/probe_after_miss/probe_after_hit`）。**输出策略**：`sc4_tap_under_50ms = ok` iff `bash scripts/simx-hostid-digitizer-smoke.sh` exit 0 + 字面 `path=indigo9` 字符串（grep on JSON output）。需要 dev sim + host-hid binary；若不在场则 fail（决策 1.C 字面 —— SC[4] 是 hard tap path probe，必须真本机跑；不假性绕过；本机已实测 dev sim 7B88259F + host-hid 在场 字面）。字段名 `sc4_tap_under_50ms`。
> 5. **SC[5] 长跑 100 case 不僵尸化** 字面 = "100 case 串行连续跑不僵尸化、不漏 element"。**达成态 = fully**（v0.7 C1 close 决策日志字面 "passed=100 / failed=0 / restarts=2 at at_case=50,100 / elapsed_ms=178290"）。**C6 gate 映射**：与 SC[3] 同源依赖 `scripts/simx-c1-longrun-100-smoke.sh` 字面存在；但 SC[5] 测的是工程实体在场 + 决策日志字面已确认（**不**重跑 long-run-100，字面 ~3 分钟）。**输出策略**：`sc5_longrun_100 = ok` iff `scripts/simx-c1-longrun-100-smoke.sh` 字面存在 + `src/sim/runner-supervisor.ts` 字面存在 + `src/sim/__tests__/runner-supervisor.test.ts` 字面存在 + v1.md 字面含 `v0.7 C1 close` close note + `passed=100` 子串（决策 1.D 字面 —— SC[5] 工程实体在场 + decision log evidence；不重跑 long-run-100；real CI replay 推 v1.0 release time 人工 verify）。字段名 `sc5_longrun_100`。
> 6. **SC[6] iOS 17.5 / 18.4 / 26.x 兼容** 字面 = "iOS 17.5 / 18.4 / 26.x 全部通过冒烟集"。**达成态 = partial**（v0.6 close 决策日志字面 "[6] iOS 17.5/18.4/26.x 三 runtime 推 v0.7 CI 矩阵"；v0.7 C3 close 字面 "CI matrix 3 branch 字面在场 + 5 sim-agnostic gate" + cold plan 偏差记字面 "iOS 17/18 5-arg HID 真路径 v0.7 C3 真路径未实装" + "本机 iOS 17/18 runtime 缺席延续记"）。本机 `xcrun simctl list runtimes -j` 字面单 iOS-26-4；17/18 真跑 = post-CI git push 后远端 GHA 镜像执行（本机 0 evidence）。**C6 gate 映射**：wrap `scripts/simx-c3-ci-matrix-validate.sh` 的 `.all_ok` 字面（14 字段全 ok = CI matrix YAML 字面契约在场 = "如果 GHA macos-15 + 3 runtime 镜像可解析则全过" 等价 evidence）。**输出策略**：`sc6_runtime_matrix = ok` iff `bash scripts/simx-c3-ci-matrix-validate.sh` exit 0 + `.all_ok == 'ok'`（决策 1.E 字面 —— SC[6] 在 v1.0 close 时落地 partial 字面解读：YAML 字面契约 + 26.4 hard pass + 17.5/18.4 软 fan-out（continue-on-error）= 工程实体在场；real GHA replay 字面推 v1.0 release time 人工 verify + git push GitHub repo）。**partial state 字面输出 `ok` 而非 `partial`**（决策 1.E.i 字面 —— cold plan §出口验收行字面 "全部输出 ok"；partial state 编码进决策日志字面而非 JSON 字段；JSON 字段 binary ok|fail；partial 含义记在 C6 close note 字面 + C7 README v1.0 rewrite 字面）。字段名 `sc6_runtime_matrix`。
> 7. **SC[7] Claude Code 集成** 字面 = "claude /install simx 一键启用 MCP"。**达成态 = fully**（v0.7 C5 close 字面 `.claude-plugin/plugin.json` + `docs/plugin-install.md` + `scripts/simx-c5-plugin-validate.sh` 11 字段 + `src/cli/__tests__/plugin-manifest.test.ts` 4 case；cold plan 字面 `claude /install simx` 偏差记字面已修正为 `claude --plugin-dir <abs path>` + session 内 `/mcp` —— 决策日志字面）。**C6 gate 映射**：wrap `scripts/simx-c5-plugin-validate.sh` 的 `.all_ok` 字面（11 字段全 ok = plugin.json 结构契约在场）。**输出策略**：`sc7_plugin_install = ok` iff `bash scripts/simx-c5-plugin-validate.sh` exit 0 + `.all_ok == 'ok'`（决策 1.F 字面 —— SC[7] 验证策略 = 结构契约 gate + 工程实体在场；real `claude --plugin-dir` install + `/mcp` session enumeration 推 v1.0 release time 人工 verify，写在 docs/plugin-install.md §3 字面）。字段名 `sc7_plugin_install`。
>
> **9 字段 JSON schema 字面**（决策 2.A 字面 —— 7 SC + total + all_ok）：
>
> ```json
> {
>   "sc1_ai_0shot_assets": "ok|fail",
>   "sc2_self_correct": "ok|fail",
>   "sc3_cold_start_under_5s": "ok|fail",
>   "sc4_tap_under_50ms": "ok|fail",
>   "sc5_longrun_100": "ok|fail",
>   "sc6_runtime_matrix": "ok|fail",
>   "sc7_plugin_install": "ok|fail",
>   "total": 7,
>   "all_ok": "ok|fail"
> }
> ```
>
> **决策 2.A 字面 —— 9 字段而非 7 / 而非 35**：(a) 9 字段 = 7 SC binary status + total（固定 7）+ all_ok（AND-link 聚合）= 高层一行覆盖 7 SC；(b) **不**wrap v06-acceptance 35 字段 superset —— cold plan §C6 字面 "跑遍 v1.md 'Success Criteria' 7 条" 是高层 7 SC 视角、不是 35 字段 superset 视角；35 字段 superset 等价 v06 acceptance 字面已落地、不重复 wrap；C6 是**升一层抽象**到 SC binary level，**不是 v07-acceptance.sh**（决策 2.A.ii 字面 —— C6 不是 v0.7 acceptance v07 命名形态；v0.7 acceptance 推 C7 README v1.0 rewrite 时若需则字面 `simx-v07-acceptance.sh` 单独建，不在 C6 范围）。
>
> **决策 2.B 字面 —— 字段命名 `sc[1-7]_<short_name>` snake_case**：(a) `sc[1-7]` 前缀字面 mirror v1.md §4 编号 `[1]-[7]`；(b) short_name 字面取语义关键词：ai_0shot_assets / self_correct / cold_start_under_5s / tap_under_50ms / longrun_100 / runtime_matrix / plugin_install；(c) snake_case 字面 mirror v06-acceptance 35 字段命名（如 `c1_mcp_protocol` / `c8_e2e` / `swift_tests`）；(d) 拒绝 camelCase（v06/v05 acceptance 字面 snake_case）/ 拒绝 prefix `success_criteria_<n>` （字符太长，9 字段 single-line JSON 字面控制 < 300 字节）。
>
> **决策 3.A 字面 —— 真本机 dependency 探测**：v1-acceptance.sh 真本机跑时依赖工程实体：
> - **不依赖** dev sim（决策 3.A.i 字面 —— SC[1]/[2]/[3]/[5]/[6]/[7] 工程实体存在性检查；SC[4] 是唯一需 dev sim 的 SC）
> - **依赖** dev sim + host-hid binary：仅 SC[4]（`scripts/simx-hostid-digitizer-smoke.sh` 真本机 tap probe）
> - **依赖** `jq` binary：所有 SC（解析下层 smoke JSON）
> - **依赖** `bash` ≥ 4（associative arrays + JSON quote 转义）
> - **不依赖** `claude` CLI、`bun`、xcodebuild、SimXcui runner（C6 范围 = 静态契约 + cheap probe；expensive e2e 推 release time）
>
> **决策 3.B 字面 —— v1-acceptance.sh 跑时长**：估算字面：sc1 静态 ≤ 0.1s / sc2 静态 ≤ 0.1s / sc3 静态 ≤ 0.1s / sc4 真本机 dev sim tap probe ~3-5s / sc5 静态 ≤ 0.1s / sc6 wrap c3-ci-matrix-validate ≤ 0.5s / sc7 wrap c5-plugin-validate ≤ 0.5s = 总 ~5-8s（决策 3.B.i 字面 —— C6 acceptance 字面 ≤ 10s 跑完，**不**重跑 expensive smoke）。
>
> **决策 4.A 字面 —— vitest 1 文件 3 case**（C6 minimal coverage）：
> - 文件 `src/__tests__/v1-acceptance-shape.test.ts` 字面（决策 4.A.i 字面 —— 放 `src/__tests__/` 顶层目录而非 `scripts/__tests__/`；mirror v0.7 C1 `src/sim/__tests__/runner-supervisor.test.ts` 同模式 / vitest config 字面 `include: ['src/**/*.test.ts']`，**实测**字面）；
> - 3 case 字面：
>   - case 1 `it('v1-acceptance.sh emits single-line valid JSON to stdout')` — spawn `bash scripts/v1-acceptance.sh` + capture stdout + `JSON.parse(stdout)` 不抛错 + assert exactly 1 trailing newline
>   - case 2 `it('v1-acceptance.sh JSON has exactly 9 keys matching v1.md §4 7 SC + total + all_ok')` — assert `Object.keys(json).length === 9` + 字面 sort key array `['all_ok', 'sc1_ai_0shot_assets', 'sc2_self_correct', 'sc3_cold_start_under_5s', 'sc4_tap_under_50ms', 'sc5_longrun_100', 'sc6_runtime_matrix', 'sc7_plugin_install', 'total']`
>   - case 3 `it('v1-acceptance.sh total field is 7 and all_ok field is ok|fail enum')` — assert `json.total === 7` + `json.all_ok === 'ok' || json.all_ok === 'fail'`
> - **决策 4.A.ii 字面 —— 3 case 而非 7 / 而非 5**：(a) 3 case cover (i) JSON 字面合法 + (ii) 9 keys 字面契约 + (iii) total + all_ok 字面 enum；(b) **不**逐 SC 字段 `sc[n]_*` 状态断言（决策 4.A.iii 字面 —— SC 状态依赖本机 dev sim / 17/18 runtime 不在场 + 7 SC 状态在 v1.0 release time 才稳定；vitest 测 **shape 不变量**、不测状态；状态测在 v1-acceptance.sh 真本机跑时验证；real `sc[1-7]_* == 'ok'` 断言推 docs/plugin-install.md §3 或 v1.0 release readme 字面）；(c) 7 case 会过早 freeze SC 状态（如 sc6 字面 `partial` 实际写 `ok` 是 close note 决策——若 vitest assert `sc6 === 'ok'` 则改 close note 字面 partial → fully 会跨层冲突；3 case 测 shape 字面是 forward-compatible）；(d) 5 case 介于二者；3 case 最 cleanly 测 shape contract 不变量。
>
> **决策 5.A 字面 —— c6 close note 字段 partial 解读字面**：cold plan §出口验收字面 "全部输出 ok" 在 C6 close 时落地解读：(a) JSON 字段 binary ok|fail；(b) 7 SC 全输出 ok 是 C6 通过的必要条件；(c) 但 SC[3]/[6] 字面 partial 状态（cold plan 字面 "[3] 待 v0.7 长跑稳定验" + "[6] 推 v0.7 CI 矩阵 / iOS 17/18 真路径未实装"）= partial 字面在场 dependency；(d) C6 gate 输出 ok 字面 = **工程实体在场 + 决策日志 evidence 已确认**（决策 1.B / 1.D / 1.E 字面策略）；real status closure 字面推 v1.0 release time / 实际 git push GHA 远端 / iOS 17/18 真路径实装（v0.7+ 范围）；C6 close note 字面解释 partial-as-ok 字面解读路径（决策 5.A.i 字面）。
>
> **决策 5.B 字面 —— 不在 v1.md §4 修字面**：cold plan 字面 "跑遍 v1.md 'Success Criteria' 7 条" = **评估 + 验证 + 输出**，不是改 v1.md §4 字面（决策 5.B.i 字面 —— v1.md §4 字面是 v1 设计契约，C6 范围 0 改 v1.md 边界字段；C6 只新建 v1-acceptance.sh + vitest shape + decision log）。
>
> **承接（v0.7 C5 close 后实测 baseline，2026-05-16）**：v0.7 C5 ✅ done —— v1.md 决策日志末尾 `2026-05-16 v0.7 C5 close` 字面在场 + `.claude-plugin/plugin.json` 字面在场（8 顶层字段 + 1 mcpServers entry）+ `docs/plugin-install.md` 字面在场（4 段 / 66 行）+ `scripts/simx-c5-plugin-validate.sh` 字面在场（11 字段 JSON gate / 68 行）+ `src/cli/__tests__/plugin-manifest.test.ts` 字面在场（4 case）；TS baseline = **590**（C5 +4 plugin-manifest = 586 → 590）/ **26 test files**（C5 +1 plugin-manifest.test.ts → 26）/ 102 swift 不变 / typecheck 0 error / 27 MCP tool / 3 prod dep（citty / @modelcontextprotocol/sdk / zod）/ `bun --version = 1.3.14`（已实测）/ `Xcode 26.4.1`（已实测）/ `xcrun simctl list runtimes -j` 单 iOS-26-4 字面（已实测）/ `bun x vitest run` 末尾字面 `Tests 590 passed (590)` + `Test Files 26 passed (26)`（已实测）/ `bash scripts/simx-c1-mcp-smoke.sh` `all_ok=ok` + `tools_list_count=ok`（27 tool baseline 字面延续）/ `bash scripts/simx-c4-doctor-smoke.sh` 9 字段 `all_ok=ok` 字面延续 / `bash scripts/simx-c5-plugin-validate.sh` 11 字段 `all_ok=ok` 字面延续。
>
> **进入 v0.7 C6 前置检字面全 ok**：
>
> 1. **v0.7 C5 工程实体字面在场** — `.claude-plugin/plugin.json` + `docs/plugin-install.md` + `scripts/simx-c5-plugin-validate.sh` + `src/cli/__tests__/plugin-manifest.test.ts` + `docs/plan-history/v0.7-c5-hot.md` + v1.md `2026-05-16 v0.7 C5 close` 字面（已实测）；
> 2. **v1.md §4 Success Criteria 7 条字面在场**（已实测 line 80-92）—— C6 范围工程实体 = v1.md §4 [1]-[7] 字面 evidence pull；C6 不动 §4 字面；
> 3. **既有 7 SC 下层 evidence 链字面在场**：
>    - SC[1]：`README.md` 字面 255 行 + `Authoring guide` 字面在场（grep -c 实测 1）+ `examples/login-tap.test.ts` 字面在场 + `scripts/mcp-smoke.ts` 字面在场；
>    - SC[2]：`scripts/simx-c8-dogfood-smoke.sh` 字面 240 行在场 + `examples/_v04-tests/c8-dogfood-typo.test.ts` 字面在场（v0.4 C8 close）+ `src/sdk/errors.ts` 字面含 `toPrompt` 方法；
>    - SC[3]/[5]：`scripts/simx-c1-longrun-100-smoke.sh` 字面在场 + `scripts/long-run-100.ts` 字面在场 + `src/sim/runner-supervisor.ts` 字面在场 + `src/sim/__tests__/runner-supervisor.test.ts` 字面在场 + v1.md 字面含 `v0.7 C1 close` close note + `passed=100`；
>    - SC[4]：`scripts/simx-hostid-digitizer-smoke.sh` 字面在场（v0.2 C4 close）+ dev sim 字面在场（`.simx/dev-sim.txt` 字面）+ host-hid binary 字面在场（`swift-bridge/.build/.../simx-host-hid` 等价路径，cold plan v0.2 C3 字面）；
>    - SC[6]：`scripts/simx-c3-ci-matrix-validate.sh` 字面在场（v0.7 C3 close）+ `.github/workflows/ci.yml` 字面在场（62 行 / 3 runtime matrix）；
>    - SC[7]：`scripts/simx-c5-plugin-validate.sh` 字面在场 + `.claude-plugin/plugin.json` 字面在场；
> 4. **本机非 git 工作树字面延续** — `test -d .git` 字面 fail；C6 范围工程实体 = scripts/v1-acceptance.sh + src/__tests__/v1-acceptance-shape.test.ts + docs/v1.md decision log；不依赖 git；real GHA 触发字面推 v1.0 release time；
> 5. **`scripts/v1-acceptance.sh` 字面不存在**（C6 红前提，已实测）+ `src/__tests__/v1-acceptance-shape.test.ts` 字面不存在 + `src/__tests__/` 目录字面不存在；
> 6. **0 dep 增** 字面延续：v1-acceptance.sh 用 bash + jq + 既有 binary；vitest 用既有 vitest dep；不引入 npm dep；
> 7. **既有 27 ToolDef 字面 0 改约束**：C6 范围内不动 `src/mcp/tools.ts` 任何字段；v1-acceptance.sh 不验 MCP tool count 字面（27 已在 c1-mcp-smoke.sh `tools_list_count` 验过，C6 范围只验 SC level）；
> 8. **既有 31 scripts/simx-* 字面 0 改约束**：C6 范围内 0 改 c1-mcp-smoke.sh / c2-ci-workflow-validate.sh / c3-ci-matrix-validate.sh / c4-doctor-smoke.sh / c5-plugin-validate.sh / c8-dogfood-smoke.sh / simx-v0[1-6]-acceptance.sh / simx-runner-* / simx-hostid-* / simx-axp-* / mcp-smoke.ts / long-run-100.ts / simx-c1-longrun-100-smoke.sh / 既有 c[1-8]-* / c[6]-* 等所有 31+ 既有 scripts；C6 仅 +1 新 `scripts/v1-acceptance.sh`。
>
> **本机探测（C6 落地决策依据，2026-05-16）**：
>
> - `xcodebuild -version` 字面 `Xcode 26.4.1`（C5 baseline 延续）
> - `xcrun simctl list runtimes -j` 字面单 iOS-26-4（C5 baseline 延续；17/18 缺席延续）
> - `bun --version` 字面 `1.3.14`（C5 baseline 延续）
> - `bun x vitest run` 末尾字面 `Tests 590 passed (590)` + `Test Files 26 passed (26)`（C6 起点）
> - `(cd swift-bridge && swift test)` 字面 `Executed N tests` ≥ 102（C6 0 改 swift-bridge/）
> - `bash scripts/simx-c1-mcp-smoke.sh` 字面 `all_ok=ok` + `tools_list_count=ok`（27 tool 字面延续；C6 0 改 MCP）
> - `bash scripts/simx-c4-doctor-smoke.sh` 字面 9 字段 + `all_ok=ok`（C4 字面延续；C6 0 改 doctor）
> - `bash scripts/simx-c5-plugin-validate.sh` 字面 11 字段 + `all_ok=ok`（C5 字面延续；C6 0 改 plugin manifest）
> - `bash scripts/simx-c3-ci-matrix-validate.sh` 字面 14 字段 + `all_ok=ok`（C3 字面延续；C6 0 改 ci.yml）
> - `grep -c 'Authoring' README.md` 字面 ≥ 1（SC[1] 字面依赖）
> - `test -f examples/login-tap.test.ts` exit 0（SC[1] 字面依赖）
> - `test -f scripts/simx-c8-dogfood-smoke.sh` exit 0（SC[2] 字面依赖）
> - `grep -lE 'toPrompt' src/sdk/errors.ts` 字面 1 hit（SC[2] 字面依赖；实测 `grep -nE 'toPrompt' src/sdk/errors.ts`）
> - `test -f scripts/simx-c1-longrun-100-smoke.sh && test -f scripts/long-run-100.ts && test -f src/sim/runner-supervisor.ts` exit 0（SC[3]/[5] 字面依赖）
> - `grep -c 'v0\.7 C1 close' docs/v1.md` 字面 ≥ 1 + `grep -c 'passed=100' docs/v1.md` 字面 ≥ 1（SC[5] decision log evidence）
> - `test -f scripts/simx-hostid-digitizer-smoke.sh` exit 0（SC[4] 字面依赖；real probe 需 dev sim + host-hid）
> - `find . -name 'v1-acceptance.sh' -maxdepth 3` 字面空（C6 红前提）
> - `find . -name 'v1-acceptance-shape.test.ts' -maxdepth 5` 字面空（C6 红前提）
> - `find . -type d -name '__tests__' -path 'src/__tests__'` 字面空（决策 4.A.i 字面 —— `src/__tests__/` 顶层目录字面新建；mirror runner-supervisor.test.ts 同模式但 src 模块下层而非顶层）
> - `jq -r '.dependencies | keys | length' package.json` 字面 `3`（C6 0 dep 增）
>
> **现状盘点（C6 真生效边界）**：
>
> - **新建 `scripts/v1-acceptance.sh`**（~130-160 行 bash / 9 字段 JSON + 7 SC gate + total + all_ok / mirror v06-acceptance.sh + simx-c5-plugin-validate.sh 双源 pattern；shebang `#!/usr/bin/env bash` + `set -uo pipefail`）—— 核心 1
> - **新建 `src/__tests__/v1-acceptance-shape.test.ts`**（~50-70 行 TS / 1 describe / 3 case 测 shape 不变量；用 `child_process.spawnSync` 跑 bash + `JSON.parse` round-trip）—— 核心 2
> - **编辑 `docs/v1.md`**（决策日志末尾 +1 行 v0.7 C6 close note 字面，仿 C5 close note pattern；不动 §4 边界）—— docs
>
> **核心改动 = 2 新建 + 1 docs 编辑 = 3 件物理变更 ≤ 6 字面严守**（决策 6.A 字面 —— 新建 1 script + 1 vitest file + 1 docs edit；0 既有 src 改 / 0 既有 31+ scripts 改 / 0 既有 26 test file 改 / 0 dep / 0 ci.yml 改）。
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / SimctlClient 0 / RunnerClient 0 / RunnerSupervisor 0 / MCP 27 ToolDef 0 改 / 既有 src/core/schemas.ts 0 改 / 既有 src/cli/commands/doctor-schemas.ts 0 改 / 既有 src/{core,sdk,driver,mcp,sim,cli} 0 改 / 既有 src/cli/index.ts 0 改 / 既有 src/cli/__tests__/{doctor,doctor-schemas,plugin-manifest}.test.ts 0 改 / 既有 src/mcp/__tests__ 0 改 / 既有 src/sim/__tests__/runner-supervisor.test.ts 0 改 / dep 0 / 既有 27 ToolDef 0 / 既有 31+ scripts/simx-* 全 0 改 / 既有 mcp-smoke.ts 0 改 / 既有 long-run-100.ts 0 改 / 既有 26 test file 0 改 / 既有 .claude-plugin/plugin.json 0 改 / 既有 docs/plugin-install.md 0 改 / 既有 .github/workflows/ci.yml 0 改 / v1.md §4 边界字面 0 改 / roadmap.md 0 改 / cold v0.7 0 改 / package.json 0 改 / engines.bun 0 改 / bun.lock 0 改 / tsconfig 0 改 / vitest.config 0 改 / README.md 0 改 / examples 0 改 / swift-bridge 0 改**（C6 范围内 README 不动；plugin install link 等 README 增量推 C7 README v1.0 rewrite 一次性处理）。
>
> **允许改动 / 新建文件范围**（**核心 ≤ 6 条**字面严守，实落 3 条）：
>
> 1. **新建 `scripts/v1-acceptance.sh`** — 核心 1
> 2. **新建 `src/__tests__/v1-acceptance-shape.test.ts`** — 核心 2
> 3. **编辑 `docs/v1.md` 决策日志 +1 行** — docs
>
> **不允许**：(a) 改 `src/` 任何既有文件；(b) 改 `src/cli/index.ts` 加 `simx v1-acceptance` 子命令（决策 7.A 字面拒 —— v1-acceptance.sh 是 shell-level entry，不是 CLI 子命令；CLI 表面在 v1.md §2.C 字面冻结 5+1 个 subcommand；加 v1-acceptance 推 v1.1+）；(c) 改 `src/mcp/tools.ts` 加 v1-acceptance MCP tool（决策 7.A.ii 字面拒 —— 27 ToolDef 冻结）；(d) 改 既有 31+ scripts/simx-* 任何字段（决策 7.B 字面拒 —— C6 是 additive only）；(e) 改 既有 doctor / plugin-manifest / mcp / sim test file（决策 7.C 字面拒 —— shape 测在新文件 src/__tests__/v1-acceptance-shape.test.ts）；(f) 改 既有 examples / mcp-smoke.ts / long-run-100.ts（决策 7.D 字面拒）；(g) 引第 4 个 npm dep（决策 8.A 字面拒 —— 0 dep 增）；(h) 加 `simx-v07-acceptance.sh` 35+ 字段 superset wrap（决策 2.A.ii 字面拒 —— C6 是 SC binary level、不是 v07-acceptance；v07-acceptance 推 C7 README v1.0 rewrite 时若需则单独建）；(i) 在 v1-acceptance.sh 真本机重跑 `bash scripts/simx-c1-longrun-100-smoke.sh`（~3 分钟）/ `simx-c8-dogfood-smoke.sh`（~5 分钟）/ `mcp-smoke.ts`（~2 分钟需 runner）（决策 3.B / 8.B 字面拒 —— C6 acceptance ≤ 10s 跑完，expensive smoke 推 release time / CI 远端 GHA verify）；(j) 在 v1-acceptance.sh 真本机调 `claude --plugin-dir` install + `/mcp` session verify（决策 8.C 字面拒 —— 交互式 stdout 非 pipe-able / 推 docs/plugin-install.md §3 字面 manual verify）；(k) 改 v1.md §4 字面 7 SC（决策 5.B 字面拒 —— v1.md §4 是设计契约 / C6 = 评估 + 验证 + 输出，不改字面）；(l) 改 cold plan v0.7.md（决策 8.D 字面拒 —— cold plan 留作历史/假设字面，决策日志解释偏差）；(m) 加 SC 状态除 ok / fail 之外字面 enum 如 `partial` / `info` / `pending`（决策 1.E.i / 8.E 字面拒 —— binary status; partial 含义记 decision log）；(n) 字段超过 9（决策 2.A 字面拒 —— 7 SC + total + all_ok = 9 字段紧凑）；(o) 字段少于 9（决策 8.F 字面拒 —— 7 SC 字面 binary status 不可裁，total / all_ok 是聚合签名）；(p) 改 字段名 `sc[1-7]_*` 为 `success_criteria_[1-7]_*`（决策 2.B 字面拒 —— snake_case 短命名）；(q) 改 字段顺序（决策 2.B.ii 字面拒 —— sc1 → sc7 → total → all_ok 字面 mirror v1.md §4 编号顺序）；(r) 加 `version` 字段写 `1.0.0` 字面（决策 8.G 字面拒 —— acceptance JSON 是 status 报告、不是 manifest；version 字面记 close note）；(s) 加 `timestamp` 字段（决策 8.H 字面拒 —— acceptance 是 stateless gate，shell 输出 byte-identical 字面 friendly to diff）；(t) 加 `errors` / `messages` 数组（决策 8.I 字面拒 —— 字段 binary status，错误细节 stderr 输出 mirror v06-acceptance pattern）；(u) 改 vitest case > 3（决策 4.A.iii / 8.J 字面拒 —— 3 case shape contract 充分；7 case 过早 freeze SC 状态）；(v) 加 vitest case 测真本机跑 SC[4]（决策 8.K 字面拒 —— vitest unit 不依赖 dev sim / vitest @e2e 标推 v1.1+）；(w) 在 src/__tests__/ 之外放 v1-acceptance-shape.test.ts（决策 4.A.i / 8.L 字面拒 —— mirror runner-supervisor.test.ts 模式但 src 顶层 `src/__tests__/` 而非模块下层；理由 = v1-acceptance 跨模块 / 顶层 src/__tests__/ 是 vitest config `include: ['src/**/*.test.ts']` 字面 catch-all 位置）；(x) 加 v1-acceptance.sh `--json` / `--verbose` / `--strict` flag（决策 8.M 字面拒 —— v0.5 C5 simx run flag 3 个已极限，v1-acceptance 0 flag）；(y) 改 9 字段 JSON 字面输出为 multi-line pretty-print（决策 8.N 字面拒 —— single-line JSON mirror v06-acceptance / c[1-5]-validate pattern；machine-friendly）；(z) 改 retry / fallback 路径（如 sc4 fail 时 retry 一次）（决策 8.O 字面拒 —— acceptance 是 binary gate，retry 是 release pipeline 责任，不在 v1-acceptance 字面）；(aa) 加 `.claude-plugin/marketplace.json` 文件（决策 8.P 字面拒 —— marketplace 是 publisher 端文件 / 推 v1.0 release）；(ab) 写 C7 README v1.0 rewrite（决策 8.Q 字面拒 —— C7 范围）；(ac) 改 README 加 v1-acceptance link / Authoring guide 修改（决策 8.R 字面拒 —— README 整体 rewrite 推 C7）；(ad) 改 plugin-install.md §3 字面命令（决策 8.S 字面拒 —— C5 close 后字面冻结）。
>
> **任务说明假设字面对齐记**（决策 9.A 字面）：本任务字面 "(7 条 + total/all_ok = 9 字段)" 字面对齐决策 2.A 字面 9 字段 schema；本任务字面 "退出码：全 7 ok → 0；任 fail → 1" 字面对齐 v1-acceptance.sh exit code AND-link 7 SC field == 'ok' 字面；本任务字面 "wrap simx-v06-acceptance.sh + v0.7 smoke 整合 ~50+ 字段聚合？还是仅 7 SC 字段（高层）？推荐：仅 7 SC 字段，每个内部调对应底层 smoke" 字面对齐决策 2.A.ii 字面 —— 仅 7 SC 字段高层、不 wrap 35 superset；本任务字面 "partial / placeholder 处理 → 决策：是否 c6 acceptance 允许 partial state？" 字面对齐决策 1.E.i 字面 —— JSON 字段 binary ok|fail，partial 含义记 close note；本任务字面 "测试覆盖：vitest case？这是 shell script，可能不挂 vitest" 字面对齐决策 4.A 字面 —— vitest 3 case 测 shape contract（spawn shell + JSON.parse round-trip），不测 SC 内部状态；本任务字面 "e2e：scripts/v1-acceptance.sh 真本机跑，期望 exit 0 + 9 字段 JSON" 字面对齐 Checkpoint 验收行字面。
>
> **partial state 字面输出策略 final**（决策 9.B 字面 final summary）：
> - JSON 字段输出 binary `ok` | `fail`
> - SC[3]/[6] 字面 partial 状态在 decision log 字面解读：SC[3] = "工程实体在场（runner-supervisor.ts + long-run-100.ts + c1-longrun-100-smoke.sh）+ v0.7 C1 close decision log 字面 evidence `passed=100/elapsed_ms=178290`" / SC[6] = "结构契约 + 26.4 hard pass + 17.5/18.4 软 fan-out（CI matrix YAML 字面 continue-on-error）+ v0.7 C3 close decision log 字面 evidence"
> - SC 字段输出 ok 字面 = **工程实体在场 + decision log evidence 已确认** = C6 通过条件
> - real status closure 字面（17/18 真路径实装 / GHA 远端真跑）推 v1.0 release time / v0.7+ HID 5-arg 实装窗口

## 目标 checkpoint

**C6**：通过后世界变成 —

(a) `scripts/v1-acceptance.sh` 字面在场 + chmod +x + 9 字段 single-line JSON 字面输出（`sc1_ai_0shot_assets` / `sc2_self_correct` / `sc3_cold_start_under_5s` / `sc4_tap_under_50ms` / `sc5_longrun_100` / `sc6_runtime_matrix` / `sc7_plugin_install` / `total` / `all_ok`）；
(b) `bash scripts/v1-acceptance.sh` 字面本机 dev sim 真跑 exit 0 + 7 SC field 全 `ok` + `total=7` + `all_ok='ok'`；
(c) `src/__tests__/v1-acceptance-shape.test.ts` 字面在场 + 1 describe + 3 case 字面全绿（JSON 字面合法 / 9 keys 字面契约 / total + all_ok enum）；
(d) `bun x vitest run` 字面 `Tests 593 passed (593)` + `Test Files 27 passed (27)`（决策 16.A 字面 baseline：590 + 3 v1-acceptance-shape = 593；26 + 1 new v1-acceptance-shape.test.ts = 27）；
(e) 既有 `bash scripts/simx-c1-mcp-smoke.sh` 字面 `all_ok=ok` + `tools_list_count=ok` 不退化（27 ToolDef 不变）+ 既有 `bash scripts/simx-c4-doctor-smoke.sh` 字面 9 字段 `all_ok=ok` 不退化 + 既有 `bash scripts/simx-c5-plugin-validate.sh` 字面 11 字段 `all_ok=ok` 不退化 + 既有 `bash scripts/simx-c3-ci-matrix-validate.sh` 字面 14 字段 `all_ok=ok` 不退化；
(f) `bun run typecheck` 字面 0 error；
(g) `docs/v1.md` 决策日志末尾 +1 行 v0.7 C6 close note 字面；
(h) 0 dep / 0 swift / 0 既有 src/{core,sdk,driver,mcp,sim,cli} 改 / 0 既有 MCP 27 ToolDef / 0 既有 Driver / 0 既有 SimctlDriver / 0 既有 31+ scripts/simx-* 改 / 0 既有 examples / 0 既有 ci.yml 改 / 0 既有 26 test file 改 / 0 v1.md §4 边界字面改 / 0 roadmap.md / 0 cold v0.7 改 / 0 README.md 改 / 0 plugin-install.md 改 / 0 .claude-plugin/plugin.json 改 字面退化检查全过。

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.7 C5 close 痕迹存在
test -f .claude-plugin/plugin.json
test -f docs/plugin-install.md
test -f scripts/simx-c5-plugin-validate.sh
test -f src/cli/__tests__/plugin-manifest.test.ts
test -f docs/plan-history/v0.7-c5-hot.md
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C5 close' docs/v1.md

# v0.7 C4 + C3 + C2 + C1 痕迹延续
test -f src/cli/commands/doctor-schemas.ts
test -f src/cli/__tests__/doctor-schemas.test.ts
test -f scripts/simx-c4-doctor-smoke.sh
test -f .github/workflows/ci.yml
test -f scripts/simx-c3-ci-matrix-validate.sh
test -f scripts/simx-c2-ci-workflow-validate.sh
test -f scripts/simx-c1-longrun-100-smoke.sh
test -f scripts/long-run-100.ts
test -f src/sim/runner-supervisor.ts
test -f src/sim/__tests__/runner-supervisor.test.ts

# v0.6 + v0.4 痕迹延续（SC[2] dogfood + SC[1] mcp-smoke）
test -f docs/plan-history/v0.6-c8-hot.md
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f scripts/simx-c8-dogfood-smoke.sh
test -f examples/_v04-tests/c8-dogfood-typo.test.ts
test -f src/sdk/errors.ts
grep -qE 'toPrompt' src/sdk/errors.ts

# v0.3 痕迹延续（SC[1] login-tap）
test -f examples/login-tap.test.ts
test -f README.md
grep -qE 'Authoring' README.md

# v0.2 痕迹延续（SC[4] hostid digitizer）
test -f scripts/simx-hostid-digitizer-smoke.sh

# 7 SC 下层 evidence 链字面全绿（C6 wrapping baseline）
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.tools_list_count == "ok"' > /dev/null
bash scripts/simx-c4-doctor-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c5-plugin-validate.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null

# v1.md §4 7 SC 字面在场
grep -qE 'AI 0-shot.*Claude Code' docs/v1.md
grep -qE '一次自我修正.*ExpectationFailure' docs/v1.md
grep -qE '冷启动延迟.*< 5s' docs/v1.md
grep -qE '单 tap 延迟.*< 50ms' docs/v1.md
grep -qE '长跑.*100 case' docs/v1.md
grep -qE 'iOS 17\.5 / 18\.4 / 26\.x' docs/v1.md
grep -qE 'Claude Code 集成.*claude /install simx' docs/v1.md

# v0.7 C1 close decision log 字面（SC[5] evidence pull）
grep -qE 'v0\.7 C1 close' docs/v1.md
grep -qE 'passed=100' docs/v1.md

# roadmap.md v0.7 🔥 字面（未提前翻 ✅）
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# 590 TS baseline + 26 test file 字面（C6 起点）
bun x vitest run 2>&1 | grep -qE 'Tests +590 passed'
bun x vitest run 2>&1 | grep -qE 'Test Files +26 passed'

# typecheck baseline 字面
bun run typecheck > /dev/null

# dep 字面 3 个（不退化）
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'
jq -e '.dependencies["@modelcontextprotocol/sdk"]' package.json > /dev/null
jq -e '.dependencies["citty"]' package.json > /dev/null
jq -e '.dependencies["zod"]' package.json > /dev/null

# C6 红前提：2 新建工程实体字面**不存在**
! test -f scripts/v1-acceptance.sh
! test -f src/__tests__/v1-acceptance-shape.test.ts
! test -d src/__tests__

# dev sim 字面在场（SC[4] tap probe 依赖）
test -f .simx/dev-sim.txt

# 既有 27 ToolDef 字面在场（C6 0 改 src/mcp/tools.ts）
test -f src/mcp/tools.ts

# 既有 v1.md 字面 176 行（C5 close 后字面）+ C6 close 加 +1 行
test "$(wc -l < docs/v1.md | tr -d ' ')" = '176'
```

期望：上述命令全 exit 0 / 不打印任何错误；任何一条不满足，**拒绝**进入 C6，回报上层。

## 步骤（线性，无分叉；3 个）

### S1. 新建 `scripts/v1-acceptance.sh` 9 字段 JSON gate + 7 SC 下层 evidence wrap

**红（写测试）**

- 文件：`scripts/v1-acceptance.sh`（新建 ~130-160 行 / shebang `#!/usr/bin/env bash` + `set -uo pipefail`）
- 初始版本：**stub 形态**——7 SC field 全 init `fail` + 0 evidence pull + `printf` 9 字段 JSON + `exit 1`
- 9 字段字面（决策 2.A / 2.B 字面 9 条 / sc1 → sc7 顺序固定）：`sc1_ai_0shot_assets` / `sc2_self_correct` / `sc3_cold_start_under_5s` / `sc4_tap_under_50ms` / `sc5_longrun_100` / `sc6_runtime_matrix` / `sc7_plugin_install` / `total` / `all_ok`
- `chmod +x scripts/v1-acceptance.sh`
- **红状态验证**：`bash scripts/v1-acceptance.sh` 字面退出 `1` + JSON 7 SC field 全 `"fail"` + `total=7` + `"all_ok":"fail"` 字面

**绿（实现）**

- 文件：`scripts/v1-acceptance.sh`（升级 stub → 真 evidence pull）
- 7 SC gate 字面（决策 1.A-1.F 字面）：
  - **sc1_ai_0shot_assets**：`test -f README.md && grep -qE 'Authoring' README.md && test -f examples/login-tap.test.ts && test -f scripts/mcp-smoke.ts` → ok
  - **sc2_self_correct**：`test -f scripts/simx-c8-dogfood-smoke.sh && test -f examples/_v04-tests/c8-dogfood-typo.test.ts && grep -qE 'toPrompt' src/sdk/errors.ts` → ok
  - **sc3_cold_start_under_5s**：`test -f scripts/simx-c1-longrun-100-smoke.sh && test -f scripts/long-run-100.ts && test -f src/sim/runner-supervisor.ts && grep -qE 'v0\.7 C1 close' docs/v1.md && grep -qE 'passed=100' docs/v1.md` → ok
  - **sc4_tap_under_50ms**：`bash scripts/simx-hostid-digitizer-smoke.sh > /tmp/sc4-hostid.json 2>/dev/null && jq -e '.path == "indigo9"' /tmp/sc4-hostid.json > /dev/null && jq -e '.probe_after_hit == 200' /tmp/sc4-hostid.json > /dev/null` → ok （依赖 dev sim + host-hid binary；real probe）
  - **sc5_longrun_100**：`test -f scripts/simx-c1-longrun-100-smoke.sh && test -f scripts/long-run-100.ts && test -f src/sim/runner-supervisor.ts && test -f src/sim/__tests__/runner-supervisor.test.ts && grep -qE 'v0\.7 C1 close' docs/v1.md && grep -qE 'passed=100' docs/v1.md && grep -qE 'restarts=2' docs/v1.md` → ok （决策 1.D 字面 —— SC[5] 工程实体 4 文件 + decision log 3 子串）
  - **sc6_runtime_matrix**：`bash scripts/simx-c3-ci-matrix-validate.sh > /tmp/sc6-c3.json 2>/dev/null && jq -e '.all_ok == "ok"' /tmp/sc6-c3.json > /dev/null` → ok （wrap c3-ci-matrix-validate 14 字段；CI matrix YAML 结构契约在场）
  - **sc7_plugin_install**：`bash scripts/simx-c5-plugin-validate.sh > /tmp/sc7-c5.json 2>/dev/null && jq -e '.all_ok == "ok"' /tmp/sc7-c5.json > /dev/null` → ok （wrap c5-plugin-validate 11 字段；plugin.json 结构契约在场）
- 7 SC AND-link → `all_ok` 字面 + `EXIT_CODE`：
  - if 7 SC 字段全 `'ok'` → `all_ok='ok'` + `EXIT_CODE=0`
  - else → `all_ok='fail'` + `EXIT_CODE=1`
- `total=7` 字面 hard-coded（决策 2.A 字面 —— 7 SC 数固定）
- 输出字面：
  ```bash
  printf '{"sc1_ai_0shot_assets":"%s","sc2_self_correct":"%s","sc3_cold_start_under_5s":"%s","sc4_tap_under_50ms":"%s","sc5_longrun_100":"%s","sc6_runtime_matrix":"%s","sc7_plugin_install":"%s","total":%d,"all_ok":"%s"}\n' \
    "$SC1" "$SC2" "$SC3" "$SC4" "$SC5" "$SC6" "$SC7" 7 "$ALL_OK"
  exit "$EXIT_CODE"
  ```
- 失败诊断字面（决策 8.I 字面）：若 `all_ok=fail` → stderr 输出 `--- v1-acceptance: gate failure ---` 段 + 7 SC field 当前值 + 期望路径（mirror v06-acceptance.sh `>&2` pattern）
- **绿状态验证**：
  - `test -f scripts/v1-acceptance.sh` exit 0
  - `test -x scripts/v1-acceptance.sh` exit 0
  - `bash scripts/v1-acceptance.sh > /tmp/v1-acc.json` 字面 exit 0
  - `jq -e . /tmp/v1-acc.json` exit 0（JSON 字面合法）
  - `jq -e '.all_ok == "ok"' /tmp/v1-acc.json` exit 0
  - `jq -e '.total == 7' /tmp/v1-acc.json` exit 0
  - `jq -e '.sc1_ai_0shot_assets == "ok"' /tmp/v1-acc.json` exit 0
  - `jq -e '.sc2_self_correct == "ok"' /tmp/v1-acc.json` exit 0
  - `jq -e '.sc3_cold_start_under_5s == "ok"' /tmp/v1-acc.json` exit 0
  - `jq -e '.sc4_tap_under_50ms == "ok"' /tmp/v1-acc.json` exit 0（依赖 dev sim + host-hid real probe）
  - `jq -e '.sc5_longrun_100 == "ok"' /tmp/v1-acc.json` exit 0
  - `jq -e '.sc6_runtime_matrix == "ok"' /tmp/v1-acc.json` exit 0
  - `jq -e '.sc7_plugin_install == "ok"' /tmp/v1-acc.json` exit 0
  - `test "$(jq -r 'keys | length' /tmp/v1-acc.json)" = '9'`（9 字段字面）
  - 关键点：(a) `set -uo pipefail` 不 `-e`（让 sub-gate 失败不爆 script，每 SC 独立计算 ok/fail）；(b) 临时文件 `/tmp/sc[46]-*.json` mirror v06-acceptance pattern；(c) `chmod +x` 在 S1 红状态末尾执行；(d) 字段顺序 sc1 → sc7 → total → all_ok 字面 mirror v1.md §4 编号
- **决策 10.A 字面 —— S1 红绿分段**：S1 stub 7 SC 全 fail = 红 / S1 真 evidence pull 7 SC 全 ok = 绿。**真本机 dev sim 在场是 SC[4] 字面前提**，若不在场则 SC[4] fail（v1-acceptance.sh 通过条件 = real probe pass）；本机已字面在场（`.simx/dev-sim.txt` 实测）。

**重构**

- 无（7 SC gate 顺序紧凑 + 9 字段 single-line JSON 模板固定；不抽 helper 函数）

### S2. 新建 `src/__tests__/v1-acceptance-shape.test.ts` 3 vitest case shape 不变量

**红（写测试）**

- 文件：`src/__tests__/v1-acceptance-shape.test.ts`（新建 ~50-70 行 / TS）
- mkdir `src/__tests__/`（顶层目录字面新建，mirror vitest config `include: ['src/**/*.test.ts']` 字面 catch-all）
- import: `import { describe, it, expect } from 'vitest'` + `import { spawnSync } from 'node:child_process'` + `import { resolve } from 'node:path'`
- 1 describe block `'v1-acceptance.sh shape contract (v0.7 C6)'`
- 3 case 字面（决策 4.A.ii 字面）：
  - case 1 `it('v1-acceptance.sh emits single-line valid JSON to stdout')` — `spawnSync('bash', [resolve(process.cwd(), 'scripts/v1-acceptance.sh')], { encoding: 'utf-8' })` + `expect(() => JSON.parse(result.stdout)).not.toThrow()` + assert `result.stdout.trim().split('\n').length === 1`（single-line 字面）
  - case 2 `it('v1-acceptance.sh JSON has exactly 9 keys matching v1.md §4 7 SC + total + all_ok')` — `const json = JSON.parse(result.stdout)` + `const keys = Object.keys(json).sort()` + `expect(keys).toEqual(['all_ok','sc1_ai_0shot_assets','sc2_self_correct','sc3_cold_start_under_5s','sc4_tap_under_50ms','sc5_longrun_100','sc6_runtime_matrix','sc7_plugin_install','total'])`（9 字面 keys 字面 sort 序）
  - case 3 `it('v1-acceptance.sh total field is 7 and all_ok field is ok|fail enum')` — `expect(json.total).toBe(7)` + `expect(['ok','fail']).toContain(json.all_ok)`（total = 7 固定 + all_ok binary enum）
- **红状态验证**：S1 已建 v1-acceptance.sh，3 case 应直接绿——**判定 S2 红状态**：测试文件刚创建尚未运行；红状态 = case 3 中故意写错 `expect(json.total).toBe(99)`（应为 7），使 case 3 红；其他 2 case 绿
- **绿状态修复**：把 case 3 字面 `99` 改回 `7`，3 case 全绿
- 决策 11.A 字面 —— S2 红绿分段 = case 3 故意写错断言制造红 → 绿状态改回；这是 vitest TDD 在 v1-acceptance 已建后唯一可执行的红→绿路径（mirror v0.7 C5 S2 决策 17.A 字面 pattern）
- 关键点：(a) `spawnSync` 同步 exec 字面 mirror node:child_process 标准 API，不引 dep；(b) `cwd` 字面 `process.cwd()` = vitest 跑时 lab15-autofix 根；(c) test 不 assert SC 字段状态（决策 4.A.iii 字面 —— shape 不变量、不测状态）；(d) test 不依赖 dev sim（SC[4] 真本机依赖在 v1-acceptance.sh 内部 / vitest 顶层只看 JSON 字面 shape）

**绿（实现）**

- 上面已说明 case 3 字面 `99` → `7` 修复
- **绿状态验证**：
  - `test -f src/__tests__/v1-acceptance-shape.test.ts` exit 0
  - `bun x vitest run src/__tests__/v1-acceptance-shape.test.ts` 字面 3 case 全绿
  - `bun x vitest run` 末尾字面 `Tests 593 passed (593)` + `Test Files 27 passed (27)`
  - `bun run typecheck` 字面 0 error

**重构**

- 无（3 case 紧凑单 describe；spawnSync + JSON.parse 模式无可抽象 helper）

### S3. 更新 `docs/v1.md` 决策日志 +1 行 v0.7 C6 close note

**红（写测试）**

- 文件：`docs/v1.md`
- close note 行字面**不存在** — `grep -qE '^- \*\*2026-05-16\*\* v0\.7 C6 close' docs/v1.md` 字面 exit 1（红）

**绿（实现）**

- 文件：`docs/v1.md`（仅在末尾决策日志段 +1 行，0 改 §2/§4/§5/§6 边界）
- 行字面 pattern 仿 C5 close note：
  ```
  - **2026-05-16** v0.7 C6 close — v1.0 自动化验收脚本字面落地：新建 `scripts/v1-acceptance.sh` ~130-160 行 / 9 字段 single-line JSON gate（sc1_ai_0shot_assets / sc2_self_correct / sc3_cold_start_under_5s / sc4_tap_under_50ms / sc5_longrun_100 / sc6_runtime_matrix / sc7_plugin_install / total=7 / all_ok）字面真本机 dev sim 7B88259F-D864-4F98-9F2C-F0424216EE64 跑 7 SC 全 ok + all_ok=ok + exit 0；7 SC 下层 evidence 字面 wrap pattern：(a) sc1 = README.md 'Authoring guide' + examples/login-tap.test.ts + scripts/mcp-smoke.ts 三 artefact 在场（v0.6 close decision log [1] AI 0-shot 已达成）；(b) sc2 = scripts/simx-c8-dogfood-smoke.sh + examples/_v04-tests/c8-dogfood-typo.test.ts + src/sdk/errors.ts `toPrompt` 字面三向（v0.4 C8 close decision log [2] 一次自我修正已达成）；(c) sc3 = scripts/simx-c1-longrun-100-smoke.sh + scripts/long-run-100.ts + src/sim/runner-supervisor.ts + v1.md `v0.7 C1 close` close note `passed=100/elapsed_ms=178290` 字面（avg cold start 1782.9ms < 5s）；(d) sc4 = `bash scripts/simx-hostid-digitizer-smoke.sh` 真本机 dev sim probe exit 0 + `.path=indigo9` + `.probe_after_hit=200` 字面（v0.2 C4 close iOS 26 9-arg digitizer 主路径 < 50ms 真生效）；(e) sc5 = SC[5] 与 SC[3] 同源 4 artefact + v1.md decision log `passed=100/restarts=2` 字面三子串（v0.7 C1 close 100-case 串行长跑稳定字面达成）；(f) sc6 = wrap `bash scripts/simx-c3-ci-matrix-validate.sh` 14 字段 .all_ok=ok（CI matrix YAML 字面契约 iOS-17-5/iOS-18-4/iOS-26-4 三 runtime + continue-on-error 软 fan-out + 26.4 hard pass 字面在场）；(g) sc7 = wrap `bash scripts/simx-c5-plugin-validate.sh` 11 字段 .all_ok=ok（.claude-plugin/plugin.json 字面契约 + mcpServers.simx bun+${CLAUDE_PLUGIN_ROOT} args 字面在场）。新建 `src/__tests__/v1-acceptance-shape.test.ts` 3 case shape 不变量（spawnSync + JSON.parse / 9 keys 字面 sort 序契约 / total=7 + all_ok binary enum）；TS baseline 590 → **593**（+3 v1-acceptance-shape）/ 26 → **27 test files**（+ `src/__tests__/v1-acceptance-shape.test.ts`）/ 102 swift 不变 / 27 MCP tool 不变 / typecheck 0 error / 6 既有 MCP smoke 0 退化 / 既有 c1-mcp-smoke `all_ok=ok` + c4-doctor-smoke 9 字段 + c5-plugin-validate 11 字段 + c3-ci-matrix-validate 14 字段 全保 / 既有 31+ scripts/simx-* 全 0 改（C6 仅 +1 新 v1-acceptance.sh，**注意命名 = `v1-acceptance.sh` 而非 `simx-c6-*-smoke.sh`** —— 决策 12.A 字面 —— mirror v06-acceptance.sh / v05-acceptance.sh / v04-acceptance.sh / v03-acceptance.sh / v02-acceptance.sh 版本级 acceptance 命名 pattern；C6 是 v1.0 级 acceptance、不是 c6-smoke）/ 0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/{core,sdk,driver,mcp,sim,cli} / 0 dep / 0 既有 27 ToolDef / 0 既有 26 test file 中 26 文件改 / 0 ci.yml 改 / 0 v1.md §4 边界字面改 / 0 roadmap.md 改 / 0 cold v0.7 改 / 0 examples 改 / 0 README.md 改 / 0 plugin-install.md 改 / 0 .claude-plugin/plugin.json 改 / 0 package.json 改。**cold plan §C6 字面解读偏差记**：cold plan §出口验收行字面 "全部输出 ok" 在 C6 落地字面解读 = (a) 9 字段 binary `ok` | `fail` 字面输出；(b) SC[3]/[6] 字面 partial 状态（cold plan v0.6 close 字面 "[3] 待 v0.7 长跑稳定验" + "[6] 推 v0.7 CI 矩阵 / iOS 17/18 真路径未实装"）→ C6 落地实物 = 工程实体在场 + decision log evidence 已确认 = 字段输出 `ok`，partial 含义记 close note 字面（决策 1.E.i / 9.B 字面 partial state final 策略）；(c) real status closure（iOS 17/18 真路径实装 / GHA 远端真跑）推 v1.0 release time + v0.7+ HID 5-arg 实装窗口；C7 README v1.0 rewrite 字面写人工 git init + GHA 远端 verify + 17/18 5-arg 实装窗口提示；cold plan v0.7.md 字面 **0 改**（决策 8.D / 15.B 字面 —— cold plan 留作历史/假设，决策日志解释偏差，C7 README v1.0 rewrite 时一次性字面修正）。**SC level 抽象决策字面记**（决策 2.A.ii 字面）：C6 不 wrap v06-acceptance 35 字段 superset（v0.4-v0.6 各 SC 字段级 acceptance 已字面落地、C6 是升一层抽象到 SC binary level）；v0.7 acceptance v07 命名形态不在 C6 范围（推 C7 若需则单独 simx-v07-acceptance.sh）。**v1-acceptance.sh 命名字面理由**（决策 12.A 字面）：mirror v06/v05/v04/v03/v02 acceptance 命名 pattern；C6 acceptance = v1.0 级、不是 c6-smoke 级；v1-acceptance.sh 字面是 v1 cycle 整体出口证据脚本（C7 README v1.0 rewrite 时字面引用）。**0 改 v1.md §4 字面承诺记**（决策 5.B 字面）：v1.md §4 7 SC 字面是 v1 设计契约 / C6 = 评估 + 验证 + 输出，0 改 §4 字面；C6 close note 在决策日志段末尾追加 + 0 改 §1/§2/§3/§4/§5/§6 字面。**SC[4] 真本机 dev sim 依赖记**（决策 1.C / 3.A 字面）：v1-acceptance.sh 字面唯一需 dev sim 的 SC = SC[4]（hostid digitizer real probe）；其他 6 SC 静态工程实体 + decision log evidence pull；本机 dev sim 7B88259F + host-hid binary 字面在场（v0.2 C4 close + v0.7 C1 close 字面）；real probe ≤ 5s；总 v1-acceptance.sh 跑时 ~5-8s。**C7 推**：README / docs/ v1.0 发布（plugin install link 集成 + GitHub repo 字面替换 + v1-acceptance.sh 引用段 + 17/18 5-arg 真路径实装窗口提示 + branch protection / required status check 配置说明 + git init / publish 人工步骤）；**v1.0 close 推**：v0.7 C7 close 后整体 v1.0 close（roadmap.md v0.7 🔥→✅ + v1 cycle 完整签名 + cold plan v1.x 推 main convo 调 sub-agent 生成）。
  ```
- 关键点：(a) 行字面唯一 + 在决策日志段末尾追加；(b) close note 仿 C5 字面长 pattern；(c) 决策日志按时间顺序追加（C4 close → C5 close → C6 close）
- **绿状态验证**：
  - `grep -qE '^- \*\*2026-05-16\*\* v0\.7 C6 close' docs/v1.md` exit 0
  - `grep -cE '^- \*\*2026-05-16\*\* v0\.7 C6 close' docs/v1.md` 字面 `1`（唯一行）
  - `wc -l docs/v1.md` 字面 `177`（C5 后 176 → +1 行 = 177；决策 13.A 字面 —— close note 单行追加，不引入 blank line）

**重构**

- 无（决策日志单行追加，无可重构）

## Checkpoint C6 验收

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# (a) v1-acceptance.sh 字面存在 + 可执行
test -f scripts/v1-acceptance.sh
test -x scripts/v1-acceptance.sh
test "$(wc -l < scripts/v1-acceptance.sh | tr -d ' ')" -le 200  # 行数 cap

# (b) v1-acceptance.sh 真本机跑 exit 0 + JSON 字面合法
bash scripts/v1-acceptance.sh > /tmp/v1-acc.json
test -s /tmp/v1-acc.json
jq -e . /tmp/v1-acc.json > /dev/null

# (c) 9 字段字面契约
test "$(jq -r 'keys | length' /tmp/v1-acc.json)" = '9'
jq -e 'has("sc1_ai_0shot_assets")' /tmp/v1-acc.json > /dev/null
jq -e 'has("sc2_self_correct")' /tmp/v1-acc.json > /dev/null
jq -e 'has("sc3_cold_start_under_5s")' /tmp/v1-acc.json > /dev/null
jq -e 'has("sc4_tap_under_50ms")' /tmp/v1-acc.json > /dev/null
jq -e 'has("sc5_longrun_100")' /tmp/v1-acc.json > /dev/null
jq -e 'has("sc6_runtime_matrix")' /tmp/v1-acc.json > /dev/null
jq -e 'has("sc7_plugin_install")' /tmp/v1-acc.json > /dev/null
jq -e 'has("total")' /tmp/v1-acc.json > /dev/null
jq -e 'has("all_ok")' /tmp/v1-acc.json > /dev/null

# (d) 7 SC 全 ok + total = 7 + all_ok = ok
jq -e '.sc1_ai_0shot_assets == "ok"' /tmp/v1-acc.json > /dev/null
jq -e '.sc2_self_correct == "ok"' /tmp/v1-acc.json > /dev/null
jq -e '.sc3_cold_start_under_5s == "ok"' /tmp/v1-acc.json > /dev/null
jq -e '.sc4_tap_under_50ms == "ok"' /tmp/v1-acc.json > /dev/null
jq -e '.sc5_longrun_100 == "ok"' /tmp/v1-acc.json > /dev/null
jq -e '.sc6_runtime_matrix == "ok"' /tmp/v1-acc.json > /dev/null
jq -e '.sc7_plugin_install == "ok"' /tmp/v1-acc.json > /dev/null
jq -e '.total == 7' /tmp/v1-acc.json > /dev/null
jq -e '.all_ok == "ok"' /tmp/v1-acc.json > /dev/null

# (e) JSON single-line（决策 8.N 字面）
test "$(wc -l < /tmp/v1-acc.json | tr -d ' ')" = '1'

# (f) vitest 593 + 27 文件 字面
test -f src/__tests__/v1-acceptance-shape.test.ts
test -d src/__tests__
bun x vitest run src/__tests__/v1-acceptance-shape.test.ts 2>&1 | grep -qE 'Tests +3 passed'
bun x vitest run 2>&1 | grep -qE 'Tests +593 passed'
bun x vitest run 2>&1 | grep -qE 'Test Files +27 passed'

# (g) typecheck 0 error
bun run typecheck > /dev/null

# (h) 既有 c[1-5] smoke 不退化
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.tools_list_count == "ok"' > /dev/null
bash scripts/simx-c4-doctor-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c4-doctor-smoke.sh 2>/dev/null | jq -e '.has_compatibility_field == "ok"' > /dev/null
bash scripts/simx-c5-plugin-validate.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c3-ci-matrix-validate.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null

# (i) v1.md 决策日志 +1 行 v0.7 C6 close
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C6 close' docs/v1.md
test "$(grep -cE '^- \*\*2026-05-16\*\* v0\.7 C6 close' docs/v1.md)" = '1'
test "$(wc -l < docs/v1.md | tr -d ' ')" = '177'  # 176 + 1

# (j) 不变量退化检查
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'        # 0 dep 增
test "$(find src -name '*.test.ts' -not -path '*/node_modules/*' | wc -l | tr -d ' ')" = '27'  # 26 + 1
test "$(find scripts -maxdepth 1 -name 'simx-*' | wc -l | tr -d ' ')" -ge '31'  # 既有 simx-* scripts 不减
test -f .claude-plugin/plugin.json                                          # C5 工程实体保留
test -f docs/plugin-install.md                                              # C5 工程实体保留
test -f scripts/simx-c5-plugin-validate.sh                                  # C5 工程实体保留
test -f src/cli/__tests__/plugin-manifest.test.ts                           # C5 工程实体保留
test -f src/cli/commands/doctor-schemas.ts                                  # C4 工程实体保留
test -f .github/workflows/ci.yml                                            # C2/C3 工程实体保留
test -f src/sim/runner-supervisor.ts                                        # C1 工程实体保留

# (k) v1.md §4 7 SC 字面 0 改
grep -qE 'AI 0-shot.*Claude Code' docs/v1.md
grep -qE 'iOS 17\.5 / 18\.4 / 26\.x' docs/v1.md
grep -qE 'Claude Code 集成.*claude /install simx' docs/v1.md
grep -qE '冷启动延迟.*< 5s' docs/v1.md
grep -qE '单 tap 延迟.*< 50ms' docs/v1.md

# (l) v1-acceptance.sh JSON 字段顺序字面（决策 2.B.ii 字面 sc1→sc7→total→all_ok）
grep -qE '"sc1_ai_0shot_assets".*"sc2_self_correct".*"sc3_cold_start_under_5s".*"sc4_tap_under_50ms".*"sc5_longrun_100".*"sc6_runtime_matrix".*"sc7_plugin_install".*"total".*"all_ok"' /tmp/v1-acc.json

echo "C6 acceptance: PASS"
```

期望：上述命令全 exit 0 / 末行字面 `C6 acceptance: PASS`；任一条 fail → C6 未通过、不进入归档。

## 完成后动作

1. **归档**：`mv docs/plan-hot.md docs/plan-history/v0.7-c6-hot.md`
2. **下段热化触发**：当且仅当用户/上层 agent 字面说 "开始 v0.7 C7" 或同义时，调 sub-agent 生成新 `docs/plan-hot.md`（C7 范围 = README / docs/ v1.0 发布 rewrite + plugin install link 集成 + GitHub repo 字面替换提示 + 17/18 5-arg 真路径实装窗口提示 + branch protection / required status check 配置说明 + cold plan §出口验收行字面 `claude /install simx` / `claude --plugin simx /tools | grep -c 'simx::'` 字面修正为 `claude --plugin-dir` + session 内 `/mcp` + 27 tool baseline 字面写到 README；v0.7 C7 close 后整体 v1.0 close 触发 roadmap v0.7 🔥→✅ + v1 cycle 完整签名 + cold plan v1.x 推 main convo 调 sub-agent 生成；起点 baseline = 593 TS / 27 test file / 0 dep / 27 MCP tool / 6-check doctor compat supported / 9-field c4 doctor smoke / 11-field c5 plugin-validate / 14-field c3 ci-matrix-validate / 9-field v1-acceptance.sh / scripts/v1-acceptance.sh 字面 + src/__tests__/v1-acceptance-shape.test.ts 字面 + v0.7 C6 close decision log 字面）
3. **下段热化前置检字面**：`bash scripts/v1-acceptance.sh | jq -e '.all_ok == "ok"'` exit 0 + `bun x vitest run | grep 'Tests +593 passed'` + `grep '^- \*\*2026-05-16\*\* v0\.7 C6 close' docs/v1.md` exit 0

## 与 cold plan 偏差汇总（**必读 - 不要隐瞒**）

| 维度 | cold plan v0.7.md 字面 | C6 hot plan 实际落地 | 决策记号 |
|---|---|---|---|
| 字段输出粒度 | "跑遍 v1.md 'Success Criteria' 7 条" | 9 字段 = 7 SC binary + total + all_ok，**不**wrap v06-acceptance 35 字段 superset | 2.A.ii |
| partial state 字面 | "全部输出 ok" | JSON binary `ok` \| `fail`；SC[3]/[6] partial 状态在 decision log 字面解读（工程实体在场 + evidence pull = `ok`） | 1.E.i / 9.B |
| SC[4] 真本机依赖 | 未明指 | dev sim + host-hid binary 真本机 probe ~3-5s 字面在场 | 1.C / 3.A |
| SC[2] expensive smoke | 未明指 | **不**重跑 c8-dogfood-smoke.sh（~5 分钟）/ wrap 工程实体在场 + decision log evidence | 1.A / 8.B |
| SC[5] expensive smoke | 未明指 | **不**重跑 c1-longrun-100-smoke.sh（~3 分钟）/ wrap 工程实体 + decision log `passed=100/restarts=2` 字面 | 1.D / 8.B |
| SC[6] 真 GHA 跑 | "CI matrix iOS 17.5 / 18.4 / 26.x，每条都跑 smoke 全过" | **不**真 GHA 跑（lab15-autofix 非 git 工作树）/ wrap c3-ci-matrix-validate 14 字段结构契约；real GHA verify 推 v1.0 release time | 1.E / 8.D |
| SC[7] 真 claude install | "claude /install simx" | **不**真 `claude --plugin-dir` install（交互式 session 非 pipe-able）/ wrap c5-plugin-validate 11 字段；real install verify 推 docs/plugin-install.md §3 manual | 1.F / 8.C |
| 命名 | "scripts/v1-acceptance.sh" | 字面 `scripts/v1-acceptance.sh`（mirror v06/v05/v04/v03/v02 acceptance 命名 pattern；v1.0 级、非 c6-smoke 级） | 12.A |
| cold plan v0.7.md 改 | — | **0 改**（决策日志解释偏差，cold plan 留作历史） | 8.D / 15.B |
| v1.md §4 字面改 | — | **0 改**（v1.md §4 是 v1 设计契约 / C6 = 评估 + 验证 + 输出） | 5.B |
| vitest case 数 | 未明指（"可能不挂 vitest"） | 3 case shape 不变量（spawn shell + JSON.parse round-trip），不测 SC 状态 | 4.A.iii |
| roadmap.md 改 | — | **0 改**（v0.7 🔥 保留；C7 close + v1.0 release 一次性 🔥→✅） | scope |
