# plan-hot — v0.6 C5：3 个复合 MCP tool（`find_and_tap` / `wait_for` / `flow_run`）+ 4 个系统 MCP tool（`open_url` / `pasteboard_set` / `pasteboard_get` / `permissions_grant`）接通既有 `Driver`（`waitFor` / `tap` / `openUrl` / `pasteboardSet` / `pasteboardGet` / `grantPermission`）+ 序列化动作链

> [3/4 信息层] 当前唯一热计划。**C5 是 v0.6 第五个 checkpoint，也是 MCP 表面第一次暴露"语义复合"（wait+tap、动作链回放）与"系统能力"（URL scheme、剪贴板、权限）**——通过后世界变成：`bun src/cli/index.ts mcp` 字面起 stdio MCP server 同时 expose **26 个 tool**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + **C5 新加 3 复合 + 4 系统 = 7**），新 7 tool 接 `Driver` 6 method（`waitFor` / `tap` / `openUrl` / `pasteboardSet` / `pasteboardGet` / `grantPermission`）+ 1 内部聚合（`flow_run` 字面 dispatch 既有 tool handler 串行执行——决策 6.A——0 改动 Driver）；`tools/list` 返 26 element array、`tools/call` 真调 driver method（或聚合 dispatch）；`find_and_tap` 字面是"`waitFor(selector, timeoutMs) → tap(selector) → describe`"复合（决策 2.A——不是 tap 的 sugar，而是"等到出现 + 点"语义合一，省 1 round-trip），wire 形态 `{udid, selector, timeoutMs?, returnSummary?}`；`wait_for` 字面镜像 `driver.waitFor(selector, timeoutMs?)` 返 `{node: A11yNode}`（决策 3.A——observe 形态非 mutate；timeout → `isError:true`，不附带 screen），wire 形态 `{udid, selector, timeoutMs?}`；`flow_run` 字面 `{udid, steps: Array<{action: string, args: object}>, returnSummaryEachStep?: boolean, bailOnError?: boolean}`（决策 4.A 决 4.A.1 dispatch 形态 A——steps 是 MCP tool name + arguments 同形态，handler 内嵌 dispatch 既有 19 个 ToolDef 的 handler；决策 4.A.2 默认 bailOnError=true；决策 4.A.3 默认 returnSummaryEachStep=false 仅在最后一 step 调 describe——节省 N 倍 round-trip）返 `{passed: number, failed: number, stepsExecuted: number, lastError?: string, lastResult?: ToolCallResult, screen?: ScreenDescription}`；`open_url` 字面 `{udid, url: string}` → `driver.openUrl(url)`（决策 5.A——0 屏幕返；URL scheme 副作用是切前台 app）；`pasteboard_set` `{udid, value: string}` → `driver.pasteboardSet(value)`；`pasteboard_get` `{udid}` → `{value: string}` 字面返 driver `pasteboardGet()` 结果（决策 1.A——2 tool 字面对齐 cold plan §C5 slash `pasteboard_set/get`，每 tool 单一职责，MCP 表面更扁；拒 1 tool 复合 `{mode:'set'|'get'}` 形态）；`permissions_grant` `{udid, bundleId: string, permission: Permission}`（决策 7.A——MCP wire 单 `permission` 字符串非数组，driver `grantPermission(permission, bundleId?)` 签名字面镜像；cold plan §C5 字面 "permissions" 复数是描述简称，driver 接单 enum；如 AI 需赋多权限走 `flow_run` 链或多次 call——拒数组形态推 v0.7+）；`Permission` 限 zod `enum(['camera','photos','location','locationAlways','notifications','microphone','contacts','calendar','reminders','bluetooth','motion','faceId'])` strict 12 字面（决策 7.B——字面镜像 `src/driver/types.ts` line 16-28；driver 内部 `PERMISSION_MAP` 映射到 `SimctlPermission`——MCP 表面用 Driver enum 不用 SimctlPermission）；驱动获取复用 C3 既有 `ToolContext.acquireDriver(udid)` 通道 0 改动；每 tool 含 zod input schema strict + JSON Schema literal 输出 + async handler 路由；错误（acquire 失败 / driver throw / zod 校验失败 / waitFor timeout / flow_run step 失败）一律走 SDK `isError: true` content 路径（C2/C3/C4 决策延续），`find_and_tap` 主操作成功但 describe 失败时 fallback 同 C4 决策 6.A 字面 `{ok:true, screen:null, screenError}` 非 isError；C1+C2+C3+C4 既有 80 case 全保不退化，新增 ~28-35 case 覆盖 7 tool happy + error + boundary + flow_run dispatch 链路 + permissions enum；`docs/v1.md` 决策日志 +1 行 v0.6 C5 close note。
>
> **承接（v0.6 C4 close 后实测 baseline）**：v0.6 C4 — **19 个 tool 字面 expose**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 新加 7 interaction `tap` / `double_tap` / `long_press` / `fill` / `swipe` / `scroll_to` / `key_press`）+ 内部 helper `withScreenAfter(ctx, udid, returnSummary, mutate)` 在场（`src/mcp/tools.ts` line 495-521）统一拼"acquireDriver → mutate → describe?"链路 + `returnSummary?: boolean = true` 字面每 7 interaction tool（默认 true；显式 false 跳过 describe）+ 主操作成功但 describe throws fallback `{ok:true, screen:null, screenError}` 非 isError 字面落地；7 interaction tool 各 ToolDef 字面对接 `Driver` 7 method 镜像；`swipe.direction` zod `enum(['up','down','left','right'])` + `key_press.key` zod `enum(['return','delete','tab','space','escape','arrowUp','arrowDown','arrowLeft','arrowRight'])` 9 字面 `KeyName`；`fill` MCP wire `value` 字段 handler 内映射 driver `fill(selector, text)`；`scripts/simx-c4-mcp-interaction-smoke.sh` 13 字段 JSON gate 全绿（真本机 dev sim e2e）。**测试基线 80 case in `server.test.ts`**（49 既有 C1+C2+C3 + 31 新 C4 case）；`ALL_TOOLS` 字面 19 element；`src/mcp/index.ts` barrel re-export 7 interaction tool；`src/mcp/server.ts` `opts.acquireDriver?` 注入点字面在场；C1/C2/C3 smoke `tools_count`/`tools_list_count` 已同步 12→19。`docs/plan-history/v0.6-c4-hot.md` 已归档。
>
> **现状盘点（C5 真生效边界）**：
> - **`src/mcp/tools.ts` 既有 `ToolContext` + `ToolDef` + `ALL_TOOLS = [19 tool]` + `selectorSchema` + `SELECTOR_JSON_SCHEMA` + helper `textOk` / `textError` / `errMsg` + `withScreenAfter` (line 495-521)**。**C5 改 1 文件 +7 ToolDef + 1 新内部 dispatch helper `dispatchSubTool(name, args, ctx)` for `flow_run`（决策 4.A.4）**：复用既有 `selectorSchema` / `SELECTOR_JSON_SCHEMA` / `withScreenAfter` / `ToolContext` 0 改动；7 个新 ToolDef + handler；`ALL_TOOLS` 扩为 26 element（决策 8.A——单文件累计；C6+ tool 数膨胀时再考虑拆 `tools/system.ts`——v0.7+ 范围）。**0 改动 ToolContext shape**（C3/C4 决策 9.A 延续——`acquireDriver` + `client` 字面够用）。
> - **`src/driver/types.ts` line 50-91 `Driver` interface 既有 29 method**。**C5 用 6 method**：
>   - `waitFor(selector: Selector, timeoutMs?: number): Promise<A11yNode>` (line 83)
>   - `tap(selector: Selector): Promise<void>` (line 60)
>   - `openUrl(url: string): Promise<void>` (line 74)
>   - `pasteboardSet(text: string): Promise<void>` (line 75)
>   - `pasteboardGet(): Promise<string>` (line 76)
>   - `grantPermission(permission: Permission, bundleId?: string): Promise<void>` (line 77) — `Permission` 12 字面 union（line 16-28）
>
>   **0 改动 Driver interface**（决策 10.A 延续——C5 完全只读 Driver 现状；拒扩 interface；拒触碰 29 method 任一签名）。`SimctlDriver` 真实现已在 v0.2-v0.4 双重验证就绪（`async waitFor()` line 418, `async openUrl()` line 161, `async pasteboardSet()` line 167, `async pasteboardGet()` line 173, `async grantPermission()` line 179——含 `lastLaunchedBundleId` fallback + `PERMISSION_MAP` Permission→SimctlPermission 映射）。
> - **`src/driver/types.ts` line 16-28 `Permission` 12 字面 union**：`'camera'|'photos'|'location'|'locationAlways'|'notifications'|'microphone'|'contacts'|'calendar'|'reminders'|'bluetooth'|'motion'|'faceId'`。**C5 zod schema 直接镜像**：`z.enum(['camera','photos','location','locationAlways','notifications','microphone','contacts','calendar','reminders','bluetooth','motion','faceId'])`（决策 7.B 字面 1:1）。
> - **`src/core/screen.ts` `ScreenDescription` + `A11yNode` 既有 wire 形态**。**C5 复用 `driver.describe()` / `driver.waitFor()` 返值原样**（决策 11.A——0 schema 变更；C7 schema 固化前用现状直出）。
> - **`src/mcp/__tests__/server.test.ts` 80 case + `makeMockDriver` 既有覆盖 `screenshot|describe|tree|findOne|tap|doubleTap|longPress|fill|swipe|scrollTo|pressKey` 11 method**。**C5 扩 mock**：`makeMockDriver` 接更多 `Pick<Driver, ...>`（加 `waitFor|openUrl|pasteboardSet|pasteboardGet|grantPermission` 5 method 默认 `vi.fn().mockResolvedValue(...)`）；**0 改动 80 既有 case**（决策 12.A 延续——既有 case helper 字面默认 override 不变）。
> - **`src/mcp/index.ts` barrel** 既有 19 tool + `defaultAcquireDriver` export。**C5 加 7 tool export**：`findAndTapTool` / `waitForTool` / `flowRunTool` / `openUrlTool` / `pasteboardSetTool` / `pasteboardGetTool` / `permissionsGrantTool`。
> - **新 e2e smoke `scripts/simx-c5-mcp-compound-system-smoke.sh`**（~200-240 行，决策 14.A）— spawn `bun src/cli/index.ts mcp` 真子进程，喂 ~10 JSON-RPC frame：`initialize` + `initialized notif` + `tools/list`（断 length === **26**）+ `tools/call simulator_list` 取 booted udid + `tools/call pasteboard_set {udid, value:"simx-c5-marker"}`（决策 15.A——`pasteboard_set` 字面无副作用 / 不依赖前台 app / 不依赖 runner——优先 e2e）+ `tools/call pasteboard_get {udid}`（断 `.value === "simx-c5-marker"` 字面回环；决策 15.A——pbcopy/pbpaste 字面是 simctl 同步 IO 不需 runner）+ `tools/call open_url {udid, url:"https://example.com"}`（决策 15.B——`open_url` simctl openurl 字面同步副作用 = 切 Safari；CI 可接受）+ `tools/call wait_for {udid, selector:{text:"NonExistentMagicMarker_C5"}, timeoutMs:1000}`（断必失败 → `isError:true` + text 含 `'timed out'`——决策 16.A "wait_for 不存在 element timeout 必抛"是字面真理；timeoutMs 1000 字面短路 CI 不卡）+ 14 字段 JSON gate `{exit_code, initialize_ok, tools_count, has_find_and_tap, has_wait_for, has_flow_run, has_open_url, has_pasteboard_set, has_pasteboard_get, has_permissions_grant, pasteboard_roundtrip, open_url_ok, wait_for_timeout_isError, stdout_pure_jsonrpc, all_ok}`。**runner 未运行时 `open_url` 也 OK**（simctl openurl 不需 runner——决策 15.C）；`wait_for` 走 `driver.waitFor` 依赖 runner（决策 15.D fallback）——容忍 `wait_for_timeout_isError` 也接受 `isError:true` 文案含 `'runner not reachable'`——CI 无 runner 时不让 smoke 全红。
> - **0 swift / 0 Driver interface / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 acquireSession / 0 acquireCell / 0 driver-factory.ts / 0 server.ts / 0 SDK / 0 新 prod dep / 0 新 peer dep / 0 C6-C8 tool / 0 explain_screen / 0 HTTP/SSE transport / 0 trace-sink 激活 / 0 claude 子进程调用 / 0 ScreenDescription serialization schema 固化（C7 范围）/ 0 selector RegExp 支持 / 0 selectorSchema 改动 / 0 withScreenAfter 改动 / 0 既有 19 ToolDef handler 改动**。
>
> **C5 真生效 = 七条**：
> 1. **改 `src/mcp/tools.ts`**（~+400-540 行 / +7 ToolDef + 1 内部 helper `dispatchSubTool` for flow_run + 1 type-only import `Permission` from `../driver/types.js`）：7 个新 ToolDef（`findAndTapTool` / `waitForTool` / `flowRunTool` / `openUrlTool` / `pasteboardSetTool` / `pasteboardGetTool` / `permissionsGrantTool`）+ 每 tool zod input schema + JSON Schema literal + handler；`find_and_tap` 复用 `withScreenAfter` 但 mutate 内是"`driver.waitFor(selector, timeoutMs) → driver.tap(selector)`"两步合一（决策 2.A）；`flow_run` handler 内嵌 `dispatchSubTool` 由 ALL_TOOLS lookup tool by name 调 handler 串行（决策 4.A.4——0 改动既有 ToolDef）；`open_url` / `pasteboard_set` / `permissions_grant` 用简化形态 `acquireDriver → driver.method() → return textOk({ok:true})`（决策 5.A——0 副作用屏幕变化不调 describe）；`pasteboard_get` 用 `acquireDriver → return textOk({value: driver.pasteboardGet()})`（决策 1.A——query 形态）；`wait_for` 用 `acquireDriver → return textOk({node: driver.waitFor(selector, timeoutMs)})` timeout 走 isError（决策 3.A——observe 形态非 mutate）；`ALL_TOOLS` 19 → 26 element；复用既有 `selectorSchema` / `SELECTOR_JSON_SCHEMA` / `withScreenAfter` / `textOk` / `textError` / `errMsg`
> 2. **改 `src/mcp/index.ts`**（+1 行 — 7 tool 1 行内联 export from `./tools.js`，与 C3/C4 形态一致）
> 3. **改 `src/mcp/__tests__/server.test.ts`**（~+280-380 行 / +28-35 case）：扩 `makeMockDriver` 加 5 system method 默认 mock（`waitFor`→ mockResolvedValue 空 A11yNode；`openUrl|pasteboardSet|grantPermission` → mockResolvedValue(undefined)；`pasteboardGet` → mockResolvedValue('')）；`tools/list 19 → 26` count 同步；7 tool 各 3-5 case（happy / error / 边界 / 形态特化）；既有 80 case 全保不动
> 4. **新建 `scripts/simx-c5-mcp-compound-system-smoke.sh`**（~200-240 行 14 字段 JSON gate）+ `chmod +x`
> 5. **改 `scripts/simx-c1-mcp-smoke.sh` + `scripts/simx-c2-mcp-lifecycle-smoke.sh` + `scripts/simx-c3-mcp-observe-smoke.sh` + `scripts/simx-c4-mcp-interaction-smoke.sh`**（`tools_count` / `tools_list_count` 期望 19 → 26 同步，4 处 1-2 行改，决策 18.A——延续 C4 决策 18）— 不计入源码改动数
> 6. **改 `docs/v1.md`** 决策日志 +1 行 v0.6 C5 close note
> 7. **改 `docs/plan-cold/v0.6.md`**（§C5 行下 +1 行 footnote 字面记 7 tool 而非 6 tool——pasteboard_set/get 拆 2 tool；permission 单 enum——决策 1.A / 7.A）—不计入源码改动数
>
> **C5 全部不在范围**（拒绝路径）：
> - 实现 C6 `explain_screen` / spawn `claude` 子进程（cold plan §C6 字面）
> - 实现 C7 `ScreenDescription` / `SerializedFailure` JSON schema 固化（cold plan §C7 字面，C5 用 driver 现状直出）
> - 实现 C8 端到端 MCP 客户端脚本（cold plan §C8 字面）
> - 改 `Driver` interface 添加 method / 改 29 method 签名（决策 10.A 字面 0 改动）
> - 改 `SimctlDriver` 6 method 实现（决策 10.A 字面 0 改动；driver 现状已 v0.2-v0.4 双重验证）
> - 改 既有 19 ToolDef handler 任一（决策 8.B——C4 已 close 0 改动；C5 只追加）
> - 改 `withScreenAfter` helper 行为（决策 17.B——C4 形态字面够用；`find_and_tap` 在 mutate callback 内拼 waitFor+tap 即可，不需扩 helper）
> - 改 `Cell` / `acquireCell` / `acquireSession` 签名（决策延续 0 改动）
> - 改 `SimctlClient` method（决策延续）
> - 改 `resolveSelector` 实现（决策延续 0 改动）
> - 改 `boot.ts` / `device-resolver.ts`（决策延续 0 改动）
> - 改 `driver-factory.ts` / `defaultAcquireDriver` 形态（决策延续——udid → Driver Map 缓存够用）
> - 改 `server.ts` 注入点 / `ToolContext` shape（决策 9.A 字面 0 改动）
> - 加 selector RegExp 支持（决策延续——RegExp JSON 不可序列化）
> - 加 `permissions_grant` 数组 `permissions: Permission[]` wire 形态（决策 7.A 字面拒——driver 现签名单 Permission；MCP 表面单 enum；多权限走 flow_run 链或多次 call——推 v0.7+ sugar）
> - 加 `pasteboard` 单 tool `{mode:'set'|'get'}` 复合形态（决策 1.A 字面拒——拆 2 tool 单一职责；MCP 表面更扁）
> - 加 `find_and_tap` 多策略 fallback（near/inside 回退）（决策 2.A 字面拒——`find_and_tap = waitFor + tap` 字面合一；多策略 fallback 是 `resolveSelector` 内部行为，已在 driver 层实现；MCP 表面不暴露策略选择——推 v0.7+ 如需）
> - 加 `flow_run` 加载 `.test.ts` 文件路径形态（决策 4.A 字面拒——`steps` 数组 JSON 自包含；文件路径形态 = trace-sink 范围，推 v0.7+）
> - 加 `flow_run` 跨 tool 状态传递 / 变量绑定（决策 4.A 字面拒——steps 是无状态 dispatch；变量绑定推 v1.1+）
> - 加 `flow_run` 真录制回放（决策 4.A 字面拒——回放需 trace-sink，推 v0.7+）
> - 加 `wait_for` 默认调 describe 附带 screen（决策 3.A 字面拒——observe 形态；wait_for 表语义是"等元素出现"，附带 screen 是浪费；如需走 `find_and_tap` 复合）
> - 加 cell pool / 多 udid 并发（决策延续——并发推 v1.1）
> - 加 base64 size 硬限（决策延续——iOS 26.4 470KB stdio 实测 OK）
> - 单测真 spawn driver / 真调 simctl（必须 mock acquireDriver，决策 13.A 延续——CI 不依赖本机 dev sim 或 runner）
> - smoke 真 tap UI 元素 / 真 fill keyboard（决策 16.A 字面延续——CI 无法保证 UI 状态；smoke 只验"必失败路径"+ pasteboard/openurl"低副作用"路径）
> - 写 swift case / 改 swift-bridge（决策延续）
> - 改 SDK / matchers / ExpectationFailure schema（决策延续）
> - 改 `simx-v0[1-5]-acceptance.sh`（v0.5 / v0.4 冻结；决策延续）
> - 建 `simx-v06-acceptance.sh`（推 C8 字面 cold plan §出口验收）
> - 加 HTTP/SSE transport / 加 CLI flag（决策延续 0 flag）
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / acquireCell 0 / SimctlClient 0 / SDK 0 / dep 0 / device-resolver.ts 0 / resolveSelector 0 / driver-factory.ts 0 / server.ts 0 / ToolContext 0 / selectorSchema 0 / withScreenAfter 0 / 既有 19 ToolDef handler 0 / C6-C8 tool 0 / explain_screen 0 / ScreenDescription schema 固化 0**。允许改动文件范围（**≤ 6 条源码** 字面任务约束）：
> 1. **改 `src/mcp/tools.ts`**（~+400-540 行 / +7 ToolDef + 1 dispatch helper + 1 type-only import）
> 2. **改 `src/mcp/index.ts`**（+1 export 行 — 7 tool inline）
> 3. **改 `src/mcp/__tests__/server.test.ts`**（~+28-35 case + tools/list count 改 19→26 + `makeMockDriver` 扩 5 method 默认 mock）
> 4. **新建 `scripts/simx-c5-mcp-compound-system-smoke.sh`**（~200-240 行）+ `chmod +x`
> 5. **改 `scripts/simx-c[1-4]-mcp-*.sh`**（4 处 `tools_count`/`tools_list_count` 期望 19 → 26 同步，决策 18.A）— 不计入源码改动数
> 6. **改 `docs/v1.md`** 决策日志 +1 行 v0.6 C5 close note
> 7. **改 `docs/plan-cold/v0.6.md`** §C5 footnote +1 行（决策 1.A / 7.A 字面记录拆 / 单 enum）— 不计入源码改动数
>
> **6 条任务约束的解释**：6 条 = "源码改动文件" 6 条字面 ≤ 6 上限。实际源码改动 = 项 1-3 共 **3 条**（tools.ts / index.ts / server.test.ts）+ 项 4 smoke 新建 + 项 5 既有 4 smoke 同步 + 项 6-7 docs = 总 8 条（含 smoke + docs，源码 3 条）。**源码 3 ≤ 6 字面满足**；smoke + docs 与 C2 / C3 / C4 形态对齐不计入源码。
>
> **不允许**：(a) 改 `src/driver/` / `src/sim/` / `src/core/` / `src/sdk/` 任何文件；(b) 改 `swift-bridge/`；(c) 改其他 CLI 文件；(d) 改 `scripts/simx-v0[1-5]-acceptance.sh`；(e) 引第 3 个 npm dep（package.json `dependencies` 仅 `citty` / `@modelcontextprotocol/sdk` / `zod`）；(f) 加 CLI flag 到 `mcpCommand`；(g) 写 C6-C8 tool 任一；(h) 写 `explain_screen` / spawn `claude`；(i) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient` 任何 method；(j) 加 selector RegExp 支持；(k) tool handler 调真 simctl / 真 driver（vitest 必须用 mock acquireDriver）；(l) 加 SDK resources/prompts/completions/sampling capability；(m) 改 `ScreenDescription` 或 `A11yNode` JSON 形态；(n) 改 `driver-factory.ts` 或 `server.ts` 注入点；(o) 改 `ToolContext` shape；(p) 改 `selectorSchema` 或 `SELECTOR_JSON_SCHEMA`；(q) 改 `withScreenAfter` helper；(r) 改 既有 19 ToolDef handler 任一；(s) `permissions_grant` 暴露数组形态；(t) `pasteboard` 合 1 tool；(u) `find_and_tap` 暴露多策略 fallback；(v) `flow_run` 加变量绑定 / 文件路径加载。

## 目标 checkpoint

**C5**：通过后世界变成 — `bun src/cli/index.ts mcp` 字面启动 stdio MCP server 同时 expose **26 个 tool**（C1 `ping` + C2 7 lifecycle + C3 4 observe + C4 7 interaction + **C5 新加 3 复合 + 4 系统 = 7**），7 个新 tool 真接 `Driver` 接口 6 method + `flow_run` 内部 dispatch 既有 ToolDef handler：

- `tools/list` → 返 **26 element array**，名字字面：`ping` / `simulator_list` / `simulator_boot` / `simulator_shutdown` / `app_launch` / `app_terminate` / `app_install` / `app_uninstall` / `screen_describe` / `screen_screenshot` / `screen_hierarchy` / `element_inspect` / `tap` / `double_tap` / `long_press` / `fill` / `swipe` / `scroll_to` / `key_press` / **`find_and_tap` / `wait_for` / `flow_run` / `open_url` / `pasteboard_set` / `pasteboard_get` / `permissions_grant`**
- `tools/call name=find_and_tap arguments={udid:"...", selector:{text:"Login"}}` → 调 `driver.waitFor({text:"Login"}, undefined)` → 调 `driver.tap({text:"Login"})` → 调 `driver.describe()` → 返 `{content:[{type:'text', text:'{"ok":true, "screen":{...}}'}]}`（决策 2.A 字面）
- `tools/call name=find_and_tap arguments={udid:"...", selector:{text:"Login"}, timeoutMs:5000, returnSummary:false}` → 调 `driver.waitFor({text:"Login"}, 5000)` → 调 `driver.tap({text:"Login"})` → 跳 describe → 返 `{ok:true, screen:null}`
- `tools/call name=find_and_tap arguments={udid:"...", selector:{text:"Missing"}, timeoutMs:1000}` → driver.waitFor 抛 timeout → 返 `{isError:true, content:[{type:'text', text:'<timed out>'}]}`（驱动 waitFor 字面在 timeout 抛 `ExpectationFailure { code:'WAIT_TIMEOUT' }`——决策 6.B fallback——MCP 直接 propagate err.message）
- `tools/call name=wait_for arguments={udid:"...", selector:{text:"Welcome"}, timeoutMs:3000}` → 调 `driver.waitFor({text:"Welcome"}, 3000)` → 返 `{content:[{type:'text', text:'{"node":{...A11yNode}}'}]}`（决策 3.A 字面——返 `node` 字面无 screen）
- `tools/call name=wait_for arguments={udid:"...", selector:{text:"Missing"}, timeoutMs:500}` → driver.waitFor 抛 timeout → 返 `{isError:true, content:[{type:'text', text:'<timed out>'}]}`
- `tools/call name=flow_run arguments={udid:"A", steps:[{action:"tap", args:{selector:{text:"Login"}, returnSummary:false}}, {action:"fill", args:{selector:{id:"email"}, value:"a@b.com", returnSummary:false}}, {action:"tap", args:{selector:{text:"Submit"}}}]}` → 串行 dispatch 3 step → 默认 last step `tap{Submit}` 走 returnSummary=true 拼 describe → 返 `{content:[{type:'text', text:'{"passed":3, "failed":0, "stepsExecuted":3, "screen":{...}}'}]}`（决策 4.A 字面——steps 是 tool name + arguments；最后 step 的 describe 自然由其 returnSummary 决定）
- `tools/call name=flow_run arguments={udid:"A", steps:[{action:"tap", args:{selector:{text:"Bad"}}}, {action:"tap", args:{selector:{text:"OK"}}}]}` → step1 失败（mock tap 抛）→ 默认 bailOnError=true → step2 不执行 → 返 `{content:[{type:'text', text:'{"passed":0, "failed":1, "stepsExecuted":1, "lastError":"<msg>"}'}]}`（决策 4.A.2 字面）
- `tools/call name=flow_run arguments={udid:"A", steps:[{action:"unknown_tool", args:{}}]}` → dispatch 找不到 tool name → 返 `{isError:true, content:[{type:'text', text:'unknown tool: unknown_tool'}]}`（决策 4.A.4——dispatch lookup fail 走 isError）
- `tools/call name=flow_run arguments={udid:"A", steps:[]}` → 0 step → 返 `{content:[{type:'text', text:'{"passed":0, "failed":0, "stepsExecuted":0}'}]}` 非 isError（决策 4.A.5——空数组合法 noop）
- `tools/call name=open_url arguments={udid:"...", url:"https://example.com"}` → 调 `driver.openUrl("https://example.com")` → 返 `{content:[{type:'text', text:'{"ok":true}'}]}`（决策 5.A 字面——0 屏幕返）
- `tools/call name=pasteboard_set arguments={udid:"...", value:"hello"}` → 调 `driver.pasteboardSet("hello")` → 返 `{content:[{type:'text', text:'{"ok":true}'}]}`
- `tools/call name=pasteboard_get arguments={udid:"..."}` → 调 `driver.pasteboardGet()` → 返 `{content:[{type:'text', text:'{"value":"hello"}'}]}`（决策 1.A 字面）
- `tools/call name=permissions_grant arguments={udid:"...", bundleId:"com.foo.app", permission:"camera"}` → 调 `driver.grantPermission("camera", "com.foo.app")` → 返 `{content:[{type:'text', text:'{"ok":true}'}]}`（决策 7.A 字面——单 enum；7.B 字面 12 union）
- `tools/call name=permissions_grant arguments={udid:"...", bundleId:"com.foo.app", permission:"unknown_perm"}` → 不符合 12 字面 zod enum → 返 `{isError:true, content:[{type:'text', text:'invalid arguments: ...'}]}`
- 任一 tool 主操作失败（driver acquire throw / driver method throw / zod 校验失败）→ `{isError:true, content:[{type:'text', text:'<error>'}]}`（决策延续）
- `find_and_tap` 主操作（waitFor+tap）**成功** + describe **失败**（runner 突然挂）→ `{content:[{type:'text', text:'{"ok":true, "screen":null, "screenError":"<msg>"}'}]}` 非 isError（决策 6.A 字面——复用 withScreenAfter 既有 fallback）
- `tools/call name=find_and_tap arguments={udid:"...", selector:{}}`（base 4 form 全缺）→ `{isError:true, content:[{type:'text', text:'invalid arguments: ...'}]}`（既有 selectorSchema 字面）
- `tools/call name=open_url arguments={udid:"..."}`（缺 url）→ `{isError:true, content:[...]}`（zod required）

`scripts/simx-c5-mcp-compound-system-smoke.sh` 14 字段 JSON CI-schema gate 全绿 + `docs/v1.md` 决策日志 +1 行 + `docs/plan-cold/v0.6.md` §C5 footnote +1 行 + 测试基线 **80 → ~108-115 case（`server.test.ts`）** + 102 swift 0 改动 + 27 字段 v0.5 acceptance / 21 字段 v0.4 acceptance / 11 字段 C1 smoke 全保（tools_list_count 26 同步）/ 10 字段 C2 smoke 全保（tools_count 26 同步）/ 12 字段 C3 smoke 全保（tools_count 26 同步）/ 13 字段 C4 smoke 全保（tools_count 26 同步）/ 14 字段 C5 smoke 真本机 dev sim e2e 全绿。

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.6 C4 close 痕迹存在
test -f docs/plan-history/v0.6-c4-hot.md
test -x scripts/simx-c4-mcp-interaction-smoke.sh

# C4 baseline 80 case 字面在场（server.test.ts）
grep -cE "^  it\(" src/mcp/__tests__/server.test.ts | grep -qE '^80$'

# typecheck 绿
bun run typecheck > /dev/null

# C4 smoke 13 字段全绿（需 dev sim；若离线 dev 环境可跳过此条但 smoke step 必须能真跑一次）
bash scripts/simx-c4-mcp-interaction-smoke.sh > /tmp/c4-baseline.json
jq -e '.all_ok == "ok"' /tmp/c4-baseline.json > /dev/null

# C1 smoke 11 字段全绿（tools_list_count 19）
bash scripts/simx-c1-mcp-smoke.sh > /tmp/c1-baseline.json
jq -e '.all_ok == "ok"' /tmp/c1-baseline.json > /dev/null

# C2 smoke 10 字段全绿（tools_count 19）
bash scripts/simx-c2-mcp-lifecycle-smoke.sh > /tmp/c2-baseline.json
jq -e '.all_ok == "ok"' /tmp/c2-baseline.json > /dev/null

# C3 smoke 12 字段全绿（tools_count 19）
bash scripts/simx-c3-mcp-observe-smoke.sh > /tmp/c3-baseline.json
jq -e '.all_ok == "ok"' /tmp/c3-baseline.json > /dev/null

# 本机有 booted dev sim
xcrun simctl list devices -j > /tmp/sim-list.json
jq -e '[.devices | to_entries[] | .value[] | select(.isAvailable == true)] | length > 0' /tmp/sim-list.json > /dev/null

# v0.5 acceptance 27 字段全保
bash scripts/simx-v05-acceptance.sh > /tmp/v05-baseline.json
jq -e '
  .swift_tests >= 102 and .typecheck == "ok"
  and .doctor_checks >= 6
  and .c1_repl_startup == "ok" and .c2_history_undo_redo == "ok"
  and .c3_save_codegen == "ok" and .c4_doctor_checks_deep == "ok"
  and .c5_run_flags == "ok" and .c6_boot_command == "ok"
' /tmp/v05-baseline.json > /dev/null

# v0.4 acceptance 21 字段不退化
bash scripts/simx-v04-acceptance.sh > /tmp/v04-regression.json
jq -e '.swift_tests >= 102 and .typecheck == "ok"' /tmp/v04-regression.json > /dev/null

# roadmap 字面 v0.6 🔥 hot
grep -qE '\| \*\*v0\.6\*\* \| 🔥 hot' docs/roadmap.md

# C4 决策日志在场（v0.6 C4 close note）
grep -qE 'v0\.6 C4' docs/v1.md

# cold plan §C5 字面 3+3/4 tool 列表
grep -qE 'find_and_tap, wait_for, flow_run' docs/plan-cold/v0.6.md
grep -qE 'open_url, pasteboard_set/get, permissions_grant' docs/plan-cold/v0.6.md

# `src/mcp/` 字面在场（C3/C4 产出）
test -f src/mcp/tools.ts
test -f src/mcp/server.ts
test -f src/mcp/driver-factory.ts
test -f src/mcp/index.ts
test -f src/mcp/device-resolver.ts
test -d src/mcp/__tests__

# C4 产出 19 tool 字面在场
grep -qE "name: 'tap'" src/mcp/tools.ts
grep -qE "name: 'fill'" src/mcp/tools.ts
grep -qE "name: 'key_press'" src/mcp/tools.ts
grep -qE 'withScreenAfter' src/mcp/tools.ts

# ALL_TOOLS 字面 19 element 校验
grep -cE "^\s*(pingTool|simulatorListTool|simulatorBootTool|simulatorShutdownTool|appLaunchTool|appTerminateTool|appInstallTool|appUninstallTool|screenDescribeTool|screenScreenshotTool|screenHierarchyTool|elementInspectTool|tapTool|doubleTapTool|longPressTool|fillTool|swipeTool|scrollToTool|keyPressTool)," src/mcp/tools.ts | grep -qE '^19$'

# C3 产出 ToolContext.acquireDriver + selectorSchema 字面在场（C5 复用 0 改动）
grep -qE 'acquireDriver:' src/mcp/tools.ts
grep -qE 'const selectorSchema' src/mcp/tools.ts
grep -qE 'const SELECTOR_JSON_SCHEMA' src/mcp/tools.ts

# C5 依赖的 SimctlDriver 6 method 字面在场（接通点）
grep -qE 'async waitFor\(' src/driver/simctl-driver.ts
grep -qE 'async openUrl\(' src/driver/simctl-driver.ts
grep -qE 'async pasteboardSet\(' src/driver/simctl-driver.ts
grep -qE 'async pasteboardGet\(' src/driver/simctl-driver.ts
grep -qE 'async grantPermission\(' src/driver/simctl-driver.ts

# Driver interface 6 method + Permission 12 字面 union 在场
grep -qE 'waitFor\(selector: Selector, timeoutMs\?: number\): Promise<A11yNode>' src/driver/types.ts
grep -qE 'openUrl\(url: string\): Promise<void>' src/driver/types.ts
grep -qE 'pasteboardSet\(text: string\): Promise<void>' src/driver/types.ts
grep -qE 'pasteboardGet\(\): Promise<string>' src/driver/types.ts
grep -qE 'grantPermission\(permission: Permission, bundleId\?: string\)' src/driver/types.ts
grep -qE "export type Permission =" src/driver/types.ts
grep -qE "'camera'" src/driver/types.ts
grep -qE "'faceId'" src/driver/types.ts

# dep 累计未变（C5 0 新引）
grep -qE '"@modelcontextprotocol/sdk":' package.json
grep -qE '"zod":' package.json
node -e 'const p=require("./package.json"); const d=Object.keys(p.dependencies||{}); const e=new Set(["citty","@modelcontextprotocol/sdk","zod"]); for (const k of d) { if (!e.has(k)) { console.error("unexpected:", k); process.exit(1); } } process.exit(0);'

# Xcode + bun 在场
xcodebuild -version | head -1
bun --version
```

期望：每条 exit 0。任何一条非 0 = 前置不满足，先修。

## 步骤（线性，3 个 step）

### S1. `src/mcp/tools.ts` 扩 7 个复合/系统 ToolDef（zod schema + JSON Schema literal + handler + `dispatchSubTool` helper for flow_run）+ `index.ts` barrel export（红 → 绿 → 重构）

**红（写测试）**
- 文件：`src/mcp/__tests__/server.test.ts`（**改** — 既有 80 case 保不动；加 ~28-35 新 case）
- 扩 `DriverAllMethods` type alias → `DriverAllMethods = Pick<Driver, 'screenshot'|'describe'|'tree'|'findOne'|'tap'|'doubleTap'|'longPress'|'fill'|'swipe'|'scrollTo'|'pressKey'|'waitFor'|'openUrl'|'pasteboardSet'|'pasteboardGet'|'grantPermission'>`；`makeMockDriver` 默认覆盖 5 新 method（`waitFor` → `vi.fn().mockResolvedValue({rawType:'XCUIElementTypeOther', role:'unknown', bounds:{x:0,y:0,w:0,h:0}, enabled:true, selected:false, hasFocus:false, visible:true, children:[]} as A11yNode)`；`openUrl|pasteboardSet|grantPermission` → `vi.fn().mockResolvedValue(undefined)`；`pasteboardGet` → `vi.fn().mockResolvedValue('')`）；保留既有 11 method 默认 mock 字面不变——80 既有 case 0 改动
- 修改既有 `'tools/list: returns 19 tools and contains ping'` case：改名 `'tools/list: returns 26 tools and contains ping'` + 改 expect `.toBe(26)`（字面锁 C5 范围——拒 C6+ 提前实现）
- 新增 case `'tools/list: names contain all 7 compound/system (find_and_tap / wait_for / flow_run / open_url / pasteboard_set / pasteboard_get / permissions_grant)'`
- 新增 case（**最少 28，目标 32**）：
  1. `'find_and_tap happy: driver.waitFor + driver.tap + describe (default returnSummary=true)'`：call `{udid:'A', selector:{text:'Login'}}` → 断 driver.waitFor 第 1 arg `{text:'Login'}` + 第 2 arg `undefined` + driver.tap 第 1 arg `{text:'Login'}` + driver.describe 调 1 次 + parsed `.ok === true`
  2. `'find_and_tap with timeoutMs: driver.waitFor(selector, 5000)'`：call `{udid:'A', selector:{text:'X'}, timeoutMs:5000}` → 断 driver.waitFor 第 2 arg `5000`
  3. `'find_and_tap returnSummary=false: skips describe'`：call `{...returnSummary:false}` → 断 driver.describe 调 **0** 次 + parsed `.screen === null`
  4. `'find_and_tap waitFor timeout: isError + driver.tap 0 calls + driver.describe 0 calls'`：mock waitFor 抛 `'waitFor({text:"X"}) timed out after 1000ms'` → 断 `isError === true` + text 含 `'timed out'` + driver.tap 调 0 次 + driver.describe 调 0 次（决策 2.A 字面 — waitFor 抛即停）
  5. `'find_and_tap tap throws after waitFor ok: isError + describe 0 calls'`：mock waitFor resolve + mock tap 抛 → 断 `isError === true` + driver.describe 调 0 次（决策 17.A 复用 withScreenAfter 字面）
  6. `'find_and_tap describe fallback: waitFor+tap ok + describe throws → {ok:true, screen:null, screenError} non-isError'`：mock describe 抛 → 断 `result.isError !== true` + parsed `.screenError` 含错误（决策 6.A 字面复用）
  7. `'wait_for happy: returns {node: A11yNode} via driver.waitFor'`：call `{udid:'A', selector:{text:'Welcome'}, timeoutMs:3000}` → 断 driver.waitFor 第 1 arg `{text:'Welcome'}` + 第 2 arg `3000` + parsed `.node.rawType` 存在
  8. `'wait_for without timeoutMs: driver.waitFor(selector, undefined)'`：call `{udid:'A', selector:{text:'Y'}}` → 断 driver.waitFor 第 2 arg `undefined`
  9. `'wait_for timeout: isError + text contains "timed out"'`：mock waitFor 抛 → 断 `isError === true` + text 含 `'timed out'`（决策 3.A 字面 observe 形态 isError）
  10. `'wait_for missing selector: isError + invalid arguments'`
  11. `'wait_for negative timeoutMs: isError'`：call `{udid:'A', selector:{text:'X'}, timeoutMs:-5}` → 断 `isError === true`（zod `int().positive()`）
  12. `'open_url happy: driver.openUrl(url) returns {ok:true}'`：call `{udid:'A', url:'https://example.com'}` → 断 driver.openUrl 第 1 arg `'https://example.com'` + parsed `.ok === true` + driver.describe 调 0 次（决策 5.A 字面 — 无 screen）
  13. `'open_url custom scheme: driver.openUrl("myapp://foo")'`：call `{udid:'A', url:'myapp://foo'}` → 断 driver.openUrl 第 1 arg deep-equal
  14. `'open_url missing url: isError + invalid arguments'`
  15. `'open_url error: driver.openUrl throws → isError'`
  16. `'pasteboard_set happy: driver.pasteboardSet(value) returns {ok:true}'`：call `{udid:'A', value:'hello'}` → 断 driver.pasteboardSet 第 1 arg `'hello'` + parsed `.ok === true`
  17. `'pasteboard_set empty value allowed: passes "" through'`：call `{udid:'A', value:''}` → 断 driver.pasteboardSet 第 1 arg `''`
  18. `'pasteboard_set missing value: isError'`
  19. `'pasteboard_get happy: returns {value: <string>} via driver.pasteboardGet'`：mock pasteboardGet 返 `'hello'` → call `{udid:'A'}` → 断 parsed `.value === 'hello'`（决策 1.A 字面）
  20. `'pasteboard_get empty: returns {value:""}'`：mock 返 `''` → 断 parsed `.value === ''`
  21. `'permissions_grant happy: driver.grantPermission(permission, bundleId)'`：call `{udid:'A', bundleId:'com.foo.app', permission:'camera'}` → 断 driver.grantPermission 第 1 arg `'camera'` + 第 2 arg `'com.foo.app'` + parsed `.ok === true`（决策 7.A 字面单 enum；driver 签名是 `(permission, bundleId?)` —— wire `permission` field 对应第 1 arg）
  22. `'permissions_grant all 12 Permission values pass: camera/photos/.../faceId'`：循环 12 字面 call → 断每次 isError !== true（决策 7.B 字面 12 union）
  23. `'permissions_grant invalid permission: isError on permission:unknown_perm'`：call `{udid:'A', bundleId:'x', permission:'unknown_perm'}` → 断 `isError === true` + text 含 `'invalid arguments'`（决策 7.A 字面拒泛 string）
  24. `'permissions_grant missing bundleId: isError'`：call `{udid:'A', permission:'camera'}` → 断 `isError === true`（zod required；MCP 表面强制 bundleId 显式——决策 7.C 字面 — 拒 driver 隐式 fallback `lastLaunchedBundleId`，MCP 表面是 stateless 不能依赖 driver session 状态）
  25. `'flow_run happy: 3 steps tap+fill+tap → passed=3, failed=0, stepsExecuted=3'`：call `{udid:'A', steps:[{action:'tap',args:{selector:{text:'Login'},returnSummary:false}},{action:'fill',args:{selector:{id:'email'},value:'a@b.com',returnSummary:false}},{action:'tap',args:{selector:{text:'Submit'},returnSummary:false}}]}` → 断 parsed `.passed === 3` + `.failed === 0` + `.stepsExecuted === 3` + driver.tap 调 2 次 + driver.fill 调 1 次（决策 4.A 字面 dispatch）
  26. `'flow_run bail on error: first step fails → stepsExecuted=1, passed=0, failed=1, lastError'`：mock tap 抛 `'element not found'` → call `{udid:'A', steps:[{action:'tap',args:{selector:{text:'X'}}},{action:'tap',args:{selector:{text:'Y'}}}]}` → 断 parsed `.passed === 0` + `.failed === 1` + `.stepsExecuted === 1` + `.lastError` 含 `'element not found'` + driver.tap 调 **1** 次（决策 4.A.2 字面 bailOnError=true）
  27. `'flow_run bailOnError=false: continues past failure'`：mock tap 第 1 次抛、第 2 次 resolve → call `{udid:'A', bailOnError:false, steps:[{action:'tap',args:{selector:{text:'X'}}},{action:'tap',args:{selector:{text:'Y'}}}]}` → 断 parsed `.stepsExecuted === 2` + `.passed === 1` + `.failed === 1` + driver.tap 调 2 次
  28. `'flow_run unknown tool: isError + text "unknown tool: unknown_tool"'`：call `{udid:'A', steps:[{action:'unknown_tool',args:{}}]}` → 断 `isError === true` + text 含 `'unknown tool'`（决策 4.A.4 字面 dispatch lookup fail）
  29. `'flow_run empty steps: passed=0, failed=0, stepsExecuted=0, non-isError'`：call `{udid:'A', steps:[]}` → 断 `result.isError !== true` + parsed `.stepsExecuted === 0`（决策 4.A.5 字面）
  30. `'flow_run injects udid into each step args (if step.args.udid omitted)'`：call `{udid:'A', steps:[{action:'tap', args:{selector:{text:'X'},returnSummary:false}}]}` → 断 driver call 内的 acquireDriver 调用 udid 是 `'A'`（决策 4.A.6 字面——flow_run 顶层 udid 字面注入每 step args.udid 若缺；显式指定 step udid 优先——AI 通常不 mix 多 udid 在一 flow 但允许）
  31. `'flow_run forbids recursive flow_run step (decision 4.A.7)'`：call `{udid:'A', steps:[{action:'flow_run', args:{steps:[]}}]}` → 断 `isError === true` + text 含 `'flow_run cannot dispatch flow_run'`（决策 4.A.7 字面拒递归——避免无界深度）
  32. `'find_and_tap / wait_for / flow_run / open_url / pasteboard_set / pasteboard_get / permissions_grant: acquireDriver called once with correct udid per call'`：循环 7 tool happy call with udid='Z' → 断 acquireDriver 每次都用 'Z'
  33. `'tools/list schemas: permissions_grant.required = [udid, bundleId, permission]'`：list → find permissions_grant → 断 required 三字段字面 + permission.enum.length === 12
  34. `'C1+C2+C3+C4 19-tool regression: ping + simulator_list + screen_describe + element_inspect + tap + key_press happy paths still pass with extended makeMockDriver'`
- 跑：`bun x vitest run src/mcp/__tests__/server.test.ts`
- 期望：~28-35 新 case 全红（tools.ts 尚未加 7 ToolDef）

**绿（实现）**
- 文件：`src/mcp/tools.ts`（**改** — 末尾在 `ALL_TOOLS` 之前插入 7 ToolDef + 1 内部 `dispatchSubTool` helper；改 `ALL_TOOLS` 末尾追加 7 element；其余 0 改动）：
  - 顶部加 type-only import `Permission`，与既有 `Driver` import 合并：
    ```ts
    import type { Driver, SwipeDirection, KeyName, Permission } from '../driver/types.js'
    ```
  - 在 `ALL_TOOLS` 定义前加内部 helper（决策 4.A.4，flow_run dispatch）：
    ```ts
    // Dispatch a sub-tool by name from within flow_run.
    // Returns the sub-tool ToolCallResult as-is; flow_run aggregates pass/fail counts.
    // Excludes `flow_run` itself to prevent unbounded recursion (decision 4.A.7).
    async function dispatchSubTool(
      action: string,
      args: unknown,
      ctx: ToolContext,
    ): Promise<ToolCallResult> {
      if (action === 'flow_run') {
        return textError('flow_run cannot dispatch flow_run (recursion forbidden)')
      }
      const tool = ALL_TOOLS.find((t) => t.name === action)
      if (tool === undefined) {
        return textError(`unknown tool: ${action}`)
      }
      const parsed = tool.inputSchema.safeParse(args)
      if (!parsed.success) {
        return textError(`invalid arguments for ${action}: ${parsed.error.message}`)
      }
      return tool.handler(parsed.data, ctx)
    }
    ```
  - 7 个新 ToolDef 字面（schema + handler 形态）：
    ```ts
    // ---------- find_and_tap (C5) ----------
    const findAndTapInput = z.object({
      udid: z.string(),
      selector: selectorSchema,
      timeoutMs: z.number().int().positive().optional(),
      returnSummary: z.boolean().optional(),
    }).strict()
    export const findAndTapTool: ToolDef = {
      name: 'find_and_tap',
      description: 'Wait for an element matching the Selector to exist (driver.waitFor with optional timeoutMs), then tap it, then return {ok:true, screen:ScreenDescription} (set returnSummary:false to skip the post-action describe). isError if waitFor times out before tap.',
      inputSchema: findAndTapInput,
      inputJsonSchema: {
        type: 'object',
        properties: {
          udid: { type: 'string' },
          selector: SELECTOR_JSON_SCHEMA,
          timeoutMs: { type: 'number' },
          returnSummary: { type: 'boolean' },
        },
        required: ['udid', 'selector'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof findAndTapInput>
        return withScreenAfter(ctx, parsed.udid, parsed.returnSummary !== false, async (d) => {
          await d.waitFor(parsed.selector as Selector, parsed.timeoutMs)
          await d.tap(parsed.selector as Selector)
        })
      },
    }

    // ---------- wait_for (C5) ----------
    const waitForInput = z.object({
      udid: z.string(),
      selector: selectorSchema,
      timeoutMs: z.number().int().positive().optional(),
    }).strict()
    export const waitForTool: ToolDef = {
      name: 'wait_for',
      description: 'Wait for an element matching the Selector to exist. Returns {node: A11yNode}. isError if waitFor times out. Does not capture a screen (use find_and_tap if you need both).',
      inputSchema: waitForInput,
      inputJsonSchema: {
        type: 'object',
        properties: {
          udid: { type: 'string' },
          selector: SELECTOR_JSON_SCHEMA,
          timeoutMs: { type: 'number' },
        },
        required: ['udid', 'selector'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof waitForInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          const node = await driver.waitFor(parsed.selector as Selector, parsed.timeoutMs)
          return textOk({ node })
        } catch (err) {
          return textError(errMsg(err))
        }
      },
    }

    // ---------- flow_run (C5) ----------
    const flowStepSchema = z.object({
      action: z.string(),
      args: z.record(z.unknown()).optional(),
    }).strict()
    const flowRunInput = z.object({
      udid: z.string(),
      steps: z.array(flowStepSchema),
      bailOnError: z.boolean().optional(),
    }).strict()
    export const flowRunTool: ToolDef = {
      name: 'flow_run',
      description: 'Run a serialized sequence of MCP tool calls. Each step is {action: <tool name>, args: <tool arguments>}. The top-level udid is injected into each step args if step.args.udid is missing. Default bailOnError=true stops at first failure. Returns {passed, failed, stepsExecuted, lastError?, lastResult?}. flow_run cannot dispatch flow_run.',
      inputSchema: flowRunInput,
      inputJsonSchema: {
        type: 'object',
        properties: {
          udid: { type: 'string' },
          steps: {
            type: 'array',
            items: {
              type: 'object',
              properties: { action: { type: 'string' }, args: { type: 'object' } },
              required: ['action'],
              additionalProperties: false,
            },
          },
          bailOnError: { type: 'boolean' },
        },
        required: ['udid', 'steps'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof flowRunInput>
        const bail = parsed.bailOnError !== false
        let passed = 0
        let failed = 0
        let stepsExecuted = 0
        let lastError: string | undefined
        let lastResult: ToolCallResult | undefined
        for (const step of parsed.steps) {
          const stepArgs = { udid: parsed.udid, ...(step.args ?? {}) }
          const result = await dispatchSubTool(step.action, stepArgs, ctx)
          stepsExecuted += 1
          lastResult = result
          if (result.isError === true) {
            failed += 1
            const txt = result.content[0]?.type === 'text' ? result.content[0].text : 'unknown error'
            lastError = txt
            if (bail) {
              if (step.action === 'flow_run') {
                return textError(txt)
              }
              break
            }
          } else {
            passed += 1
          }
        }
        const payload: Record<string, unknown> = { passed, failed, stepsExecuted }
        if (lastError !== undefined) payload.lastError = lastError
        if (lastResult !== undefined) payload.lastResult = lastResult
        return textOk(payload)
      },
    }

    // ---------- open_url (C5) ----------
    const openUrlInput = z.object({
      udid: z.string(),
      url: z.string(),
    }).strict()
    export const openUrlTool: ToolDef = {
      name: 'open_url',
      description: 'Open a URL (http(s) or custom scheme) on the simulator. Wraps simctl openurl. Returns {ok:true}.',
      inputSchema: openUrlInput,
      inputJsonSchema: {
        type: 'object',
        properties: { udid: { type: 'string' }, url: { type: 'string' } },
        required: ['udid', 'url'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof openUrlInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          await driver.openUrl(parsed.url)
          return textOk({ ok: true })
        } catch (err) {
          return textError(errMsg(err))
        }
      },
    }

    // ---------- pasteboard_set (C5) ----------
    const pasteboardSetInput = z.object({
      udid: z.string(),
      value: z.string(),
    }).strict()
    export const pasteboardSetTool: ToolDef = {
      name: 'pasteboard_set',
      description: 'Set the simulator pasteboard text (wraps simctl pbcopy). Empty strings are allowed. Returns {ok:true}.',
      inputSchema: pasteboardSetInput,
      inputJsonSchema: {
        type: 'object',
        properties: { udid: { type: 'string' }, value: { type: 'string' } },
        required: ['udid', 'value'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof pasteboardSetInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          await driver.pasteboardSet(parsed.value)
          return textOk({ ok: true })
        } catch (err) {
          return textError(errMsg(err))
        }
      },
    }

    // ---------- pasteboard_get (C5) ----------
    const pasteboardGetInput = z.object({
      udid: z.string(),
    }).strict()
    export const pasteboardGetTool: ToolDef = {
      name: 'pasteboard_get',
      description: 'Read the simulator pasteboard text (wraps simctl pbpaste). Returns {value:<string>}.',
      inputSchema: pasteboardGetInput,
      inputJsonSchema: {
        type: 'object',
        properties: { udid: { type: 'string' } },
        required: ['udid'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof pasteboardGetInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          const value = await driver.pasteboardGet()
          return textOk({ value })
        } catch (err) {
          return textError(errMsg(err))
        }
      },
    }

    // ---------- permissions_grant (C5) ----------
    const permissionSchema = z.enum([
      'camera', 'photos', 'location', 'locationAlways', 'notifications',
      'microphone', 'contacts', 'calendar', 'reminders', 'bluetooth',
      'motion', 'faceId',
    ])
    const permissionsGrantInput = z.object({
      udid: z.string(),
      bundleId: z.string(),
      permission: permissionSchema,
    }).strict()
    export const permissionsGrantTool: ToolDef = {
      name: 'permissions_grant',
      description: 'Grant a single iOS permission to an app bundle: one of camera | photos | location | locationAlways | notifications | microphone | contacts | calendar | reminders | bluetooth | motion | faceId (12 literal enum; mirrors Driver Permission type). Grant multiple permissions by calling repeatedly or via flow_run. bundleId is required (MCP surface is stateless and does not honour driver-internal lastLaunchedBundleId fallback). Returns {ok:true}.',
      inputSchema: permissionsGrantInput,
      inputJsonSchema: {
        type: 'object',
        properties: {
          udid: { type: 'string' },
          bundleId: { type: 'string' },
          permission: {
            type: 'string',
            enum: [
              'camera', 'photos', 'location', 'locationAlways', 'notifications',
              'microphone', 'contacts', 'calendar', 'reminders', 'bluetooth',
              'motion', 'faceId',
            ],
          },
        },
        required: ['udid', 'bundleId', 'permission'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof permissionsGrantInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          await driver.grantPermission(parsed.permission as Permission, parsed.bundleId)
          return textOk({ ok: true })
        } catch (err) {
          return textError(errMsg(err))
        }
      },
    }
    ```
  - 改 `ALL_TOOLS`：在 `keyPressTool,` 后追加 7 element → 总 26。
- 文件：`src/mcp/index.ts`（+1 行 — 7 tool 同行 named export from `./tools.js`，与 C4 同形态）
- 跑：`bun x vitest run src/mcp/__tests__/`
- 期望：80 既有 + 28-35 新 = 108-115 case 全绿

**重构**
- 无（7 tool 形态平行：`find_and_tap` 复用 `withScreenAfter`；`wait_for`/`open_url`/`pasteboard_set`/`pasteboard_get`/`permissions_grant` 形态 ≤ 7 行；`flow_run` 内嵌 `dispatchSubTool` 串行 for 循环；enum schema 字面镜像 type；ToolDef 形态延续 C4）

**Step-level 验证命令**：
```bash
bun x vitest run src/mcp/__tests__/server.test.ts 2>&1 | tail -5 | grep -qE 'Tests +(10[8-9]|11[0-9]|12[0-5]) passed'
bun run typecheck > /dev/null
grep -qE "name: 'find_and_tap'" src/mcp/tools.ts
grep -qE "name: 'wait_for'" src/mcp/tools.ts
grep -qE "name: 'flow_run'" src/mcp/tools.ts
grep -qE "name: 'open_url'" src/mcp/tools.ts
grep -qE "name: 'pasteboard_set'" src/mcp/tools.ts
grep -qE "name: 'pasteboard_get'" src/mcp/tools.ts
grep -qE "name: 'permissions_grant'" src/mcp/tools.ts
grep -qE 'dispatchSubTool' src/mcp/tools.ts
grep -cE "^\s*(pingTool|simulatorListTool|simulatorBootTool|simulatorShutdownTool|appLaunchTool|appTerminateTool|appInstallTool|appUninstallTool|screenDescribeTool|screenScreenshotTool|screenHierarchyTool|elementInspectTool|tapTool|doubleTapTool|longPressTool|fillTool|swipeTool|scrollToTool|keyPressTool|findAndTapTool|waitForTool|flowRunTool|openUrlTool|pasteboardSetTool|pasteboardGetTool|permissionsGrantTool)," src/mcp/tools.ts | grep -qE '^26$'
```
期望：全 exit 0。

### S2. 全套测试 + typecheck + 既有 acceptance 不退化 gate（确认 C1+C2+C3+C4 + v0.4-v0.5 全保）（红 → 绿 → 重构）

**红（写测试）**
- 此 step 不新增测试代码——是"防退化 gate"step。视 S1 的所有既有 case（80 既有 + 28-35 新 = 108-115）必须全绿为"红"前提。
- 跑：
  ```bash
  bun x vitest run 2>&1 | tail -10
  ```
- 期望：先看到 `server.test.ts` count ≥ 108 字面 `Tests N passed`。若任一既有 case 红 → 立即 stop 回到 S1。

**绿（实现）**
- 跑全套 + typecheck：
  ```bash
  bun x vitest run 2>&1 | tail -5
  bun run typecheck > /dev/null && echo "typecheck-ok"
  ```
- 期望：vitest 全绿 + server.test.ts count ≥ 108 + typecheck exit 0

**重构（可选）**
- 无。

**Step-level 验证命令**：
```bash
bun x vitest run src/mcp/__tests__/server.test.ts 2>&1 | tail -5 | grep -qE 'Tests +(10[8-9]|11[0-9]|12[0-5]) passed'
bun x vitest run 2>&1 | tail -5 | grep -qE 'Tests +[0-9]+ passed'
bun run typecheck > /dev/null
```
期望：全 exit 0。

### S3. 新建 `scripts/simx-c5-mcp-compound-system-smoke.sh`（14 字段 JSON CI gate 真本机 dev sim e2e）+ 改既有 C1/C2/C3/C4 smoke 同步 tools_count 19→26 + `docs/v1.md` 决策日志 +1 行 + `docs/plan-cold/v0.6.md` §C5 footnote +1 行（红 → 绿 → 重构）

**红（写测试）**
- 文件：`scripts/simx-c5-mcp-compound-system-smoke.sh`（**新建** ~200-240 行 + `chmod +x`）
- Schema：15 字段顶层 + `all_ok` = `{exit_code, initialize_ok, tools_count, has_find_and_tap, has_wait_for, has_flow_run, has_open_url, has_pasteboard_set, has_pasteboard_get, has_permissions_grant, pasteboard_roundtrip, open_url_ok, wait_for_timeout_isError, stdout_pure_jsonrpc, all_ok}`
- 实现：
  1. 探测真 booted dev sim（`xcrun simctl list devices -j | jq -r '...'` 取第一个 booted udid）；若无 → 选 first available + `xcrun simctl bootstatus -b`（与 C2/C3/C4 smoke 同形态）
  2. spawn `bun src/cli/index.ts mcp` 真子进程 + stdin 喂 ~10 JSON-RPC frame：`initialize` / `initialized notif` / `tools/list`（断 length === 26）/ `tools/call simulator_list` / `tools/call pasteboard_set {udid, value:"simx-c5-marker-<epoch>"}`（独一 marker 防干扰）/ `tools/call pasteboard_get {udid}`（断 `.value` 含 marker 字面回环——决策 15.A——pbcopy/pbpaste 字面 simctl 同步 IO 不需 runner）/ `tools/call open_url {udid, url:"https://example.com"}`（决策 15.B/C——simctl openurl 不需 runner；副作用切 Safari CI 可接受）/ `tools/call wait_for {udid, selector:{text:"NonExistentMagicMarker_C5"}, timeoutMs:1000}`（断必失败 → `isError:true` + text 含 `'timed out'` 或 `'runner not reachable'`——决策 16.A "wait_for nonexistent 必抛"；timeoutMs 1000 字面短路 CI 不卡）
  3. sleep 5 hold stdin 让 async handler 完成；kill 子进程；jq 验各字段
  4. `tools_count == 26`；7 字段 `has_*` 各对应 tools/list 中 name 命中
  5. `pasteboard_roundtrip` 字段语义：`pasteboard_set` 后 `pasteboard_get` `.value` 字面含 marker → `"ok"`；不匹配 → `"fail"`
  6. `open_url_ok` 字段：result 非 isError → `"ok"`；isError → `"fail"`
  7. `wait_for_timeout_isError` 字段：result `isError === true` → `"ok"`（决策 16.A 字面——必失败路径是断言；接受 `'timed out'` OR `'runner not reachable'` 文案）；非 isError → `"fail"`
  8. `stdout_pure_jsonrpc` 字段：stdout 所有行 jq 解析成功 → `"ok"`；任一行非 JSON-RPC → `"fail"`（C3/C4 smoke 同形态）
- 改既有 smoke 同步 tools_count（决策 18.A）：
  - `scripts/simx-c1-mcp-smoke.sh`：`tools_list_count` 期望 19 → 26（1-2 行改）
  - `scripts/simx-c2-mcp-lifecycle-smoke.sh`：`tools_count` 期望 19 → 26（1-2 行改）
  - `scripts/simx-c3-mcp-observe-smoke.sh`：`tools_count` 期望 19 → 26（1-2 行改）
  - `scripts/simx-c4-mcp-interaction-smoke.sh`：`tools_count` 期望 19 → 26（1-2 行改）
- 跑 smoke（**红**）：
  ```bash
  bash scripts/simx-c5-mcp-compound-system-smoke.sh > /tmp/c5-smoke-red.json 2>&1 || true
  jq -e '.all_ok == "ok"' /tmp/c5-smoke-red.json 2>/dev/null
  ```
- 期望（红前）：smoke 不存在 → exit 非 0 / 任一 jq fail → 进入绿

**绿（实现）**
- 写 smoke 脚本（参考 `scripts/simx-c4-mcp-interaction-smoke.sh` 结构 + 14 字段定制）
- 改 C1/C2/C3/C4 smoke 同步 tools_count 19→26
- 加 `chmod +x` 新 smoke
- 改 `docs/plan-cold/v0.6.md` §C5 行末 +1 行 footnote（决策 1.A / 7.A 字面记录）：
  ```
  > **C5 落地修正**（v0.6 C5 close 2026-05-16）：cold plan slash 表达"3 + 3"实际落地 **3 复合 + 4 系统 = 7 tool**：`pasteboard_set` / `pasteboard_get` 字面拆 2 tool（决策 1.A——单一职责、MCP 表面更扁，拒 1 tool `{mode:'set'|'get'}` 复合形态）；`permissions_grant` MCP wire 是单 `permission` enum 非数组（决策 7.A——driver `grantPermission(permission, bundleId?)` 签名字面镜像；多权限走 `flow_run` 链或多次 call；MCP 表面强制 `bundleId` 显式不 fallback driver `lastLaunchedBundleId`——决策 7.C 字面，MCP 表面是 stateless）；`Permission` zod enum 12 字面镜像 `src/driver/types.ts` line 16-28（决策 7.B）。
  ```
- 改 `docs/v1.md` 末尾 +1 行决策日志：
  ```
  - **2026-05-16** v0.6 C5：MCP 业务层接 7 个复合/系统能力 — `find_and_tap` / `wait_for` / `flow_run` / `open_url` / `pasteboard_set` / `pasteboard_get` / `permissions_grant` 字面 expose，6 tool 接既有 `Driver` 6 method（`waitFor` / `tap` / `openUrl` / `pasteboardSet` / `pasteboardGet` / `grantPermission`）+ `flow_run` 内部 dispatch ALL_TOOLS 既有 ToolDef handler 串行执行（决策 4.A.4——0 改动既有 19 ToolDef）+ 0 改动 Driver interface / SimctlDriver 实现 / withScreenAfter helper / ToolContext shape。`find_and_tap` 字面是"`driver.waitFor(selector, timeoutMs?) → driver.tap(selector) → describe`"复合（决策 2.A——不是 tap 的 sugar，而是"等到出现 + 点"语义合一，AI 单次 round-trip 拿到动作 + 后续屏幕；waitFor 抛即 isError、不调 tap、不调 describe）；wire 形态 `{udid, selector, timeoutMs?, returnSummary?}`；复用 C4 既有 `withScreenAfter` helper 在 mutate callback 内拼 waitFor+tap（决策 17.B 字面——C4 helper 形态字面够用 0 扩参）。`wait_for` 字面镜像 `driver.waitFor(selector, timeoutMs?)` 返 `{node: A11yNode}`（决策 3.A——observe 形态非 mutate；不附带 screen，需 screen 走 `find_and_tap` 或后续 `screen_describe`；timeout → isError + text 含 `'timed out'`）；wire 形态 `{udid, selector, timeoutMs?}`。`flow_run` 字面 `{udid, steps: Array<{action, args?}>, bailOnError?}`（决策 4.A 字面 dispatch 形态 A——steps 是 MCP tool name + arguments；handler 内嵌 `dispatchSubTool(action, args, ctx)` 由 `ALL_TOOLS.find(t => t.name === action)` lookup 调 `tool.handler(parsed.data, ctx)`）；默认 `bailOnError=true` first failure stops loop（决策 4.A.2）；top-level `udid` 字面注入每 step args.udid 若缺（决策 4.A.6——AI 常态单 udid flow）；`flow_run` 拒递归 dispatch `flow_run`（决策 4.A.7——避免无界深度；step `action:'flow_run'` 直接走 isError）；返 `{passed, failed, stepsExecuted, lastError?, lastResult?}`。`open_url` 字面 `{udid, url: string}` → `driver.openUrl(url)` 返 `{ok:true}`（决策 5.A——0 屏幕；URL scheme 副作用切前台 app）；`pasteboard_set` `{udid, value}` → `driver.pasteboardSet(value)` 返 `{ok:true}`；`pasteboard_get` `{udid}` → `{value: <string>}` 字面返 driver `pasteboardGet()` 结果（决策 1.A 字面 — 2 tool 拆 set/get 单一职责，MCP 表面更扁；拒 1 tool `{mode:'set'|'get'}` 复合形态）。`permissions_grant` `{udid, bundleId: string, permission: Permission}`（决策 7.A 字面——MCP wire 单 `permission` 字符串非数组，driver `grantPermission(permission, bundleId?)` 签名字面镜像；cold plan §C5 字面 "permissions" 复数是描述简称；多权限走 `flow_run` 链或多次 call——拒数组形态推 v0.7+ sugar）；`Permission` 限 zod `enum(['camera','photos','location','locationAlways','notifications','microphone','contacts','calendar','reminders','bluetooth','motion','faceId'])` strict 12 字面（决策 7.B 字面镜像 `src/driver/types.ts` line 16-28）；`bundleId` 强制 zod required（决策 7.C 字面——MCP 表面 stateless 不 fallback driver `lastLaunchedBundleId`）。改 `src/mcp/tools.ts`（+7 ToolDef + `dispatchSubTool` helper + 1 type-only import `Permission` + `ALL_TOOLS` 19 → 26）；改 `src/mcp/index.ts`（+1 export 行 7 tool）；改 `src/mcp/__tests__/server.test.ts`（80 → ~108-115 case：+28-35 case 覆盖 7 tool happy + error + 12 Permission 字面 union + flow_run dispatch / bail / empty / recursion 拒 / udid 注入 + find_and_tap waitFor 抛/tap 抛/describe fallback + acquireDriver wired + C1+C2+C3+C4 regression）；新建 `scripts/simx-c5-mcp-compound-system-smoke.sh`（14 字段 JSON CI gate 真 spawn + ~10 frame + 真本机 simctl + pasteboard 回环 + open_url + wait_for nonexistent isError 路径，决策 14.A）；改 `scripts/simx-c[1-4]-mcp-*.sh`（4 处 tools_count 19→26 同步，决策 18.A）；改 `docs/plan-cold/v0.6.md` §C5 footnote +1 行字面记 7 tool 拆 / 单 enum / bundleId required。**0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 SDK / 0 dep / 0 C6-C8 tool / 0 explain_screen / 0 ScreenDescription schema 固化 / 0 resolveSelector 改动 / 0 SDK matcher 改动 / 0 driver-factory.ts / 0 server.ts / 0 ToolContext / 0 selectorSchema / 0 withScreenAfter / 0 既有 19 ToolDef handler 改动**。测试基线 80 → **~108-115 case（server.test.ts）** / 102 swift / typecheck 0 error / 27 字段 v0.5 acceptance 全保 / 21 字段 v0.4 acceptance 全保 / 11 字段 C1 smoke 全保（tools_list_count 26 同步）/ 10 字段 C2 smoke 全保（tools_count 26 同步）/ 12 字段 C3 smoke 全保（tools_count 26 同步）/ 13 字段 C4 smoke 全保（tools_count 26 同步）/ 14 字段 C5 smoke 真本机 dev sim e2e 全绿。
  ```
- 跑：
  ```bash
  bash scripts/simx-c5-mcp-compound-system-smoke.sh > /tmp/c5-smoke-green.json
  jq -e '
    .exit_code == 0
    and .initialize_ok == "ok"
    and .tools_count == 26
    and .has_find_and_tap == "ok"
    and .has_wait_for == "ok"
    and .has_flow_run == "ok"
    and .has_open_url == "ok"
    and .has_pasteboard_set == "ok"
    and .has_pasteboard_get == "ok"
    and .has_permissions_grant == "ok"
    and .pasteboard_roundtrip == "ok"
    and .open_url_ok == "ok"
    and .wait_for_timeout_isError == "ok"
    and .stdout_pure_jsonrpc == "ok"
    and .all_ok == "ok"
  ' /tmp/c5-smoke-green.json > /dev/null
  bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
  bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
  bash scripts/simx-c3-mcp-observe-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
  bash scripts/simx-c4-mcp-interaction-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
  ```
- 期望：exit 0

**重构（可选）**
- 若 smoke 脚本结构与 C4 smoke 差异过大 → 提取共享 shell helper `_simx-mcp-spawn.sh`。**C5 范围内不提取**（推 C6 / C7 累计 5-6 smoke 后再 DRY，C4 决策 16.A 延续）。

**Step-level 验证命令**：
```bash
test -x scripts/simx-c5-mcp-compound-system-smoke.sh
bash scripts/simx-c5-mcp-compound-system-smoke.sh > /tmp/c5-smoke.json
jq -e '.all_ok == "ok"' /tmp/c5-smoke.json
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c3-mcp-observe-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c4-mcp-interaction-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
grep -qE '2026-05-16.*v0\.6 C5' docs/v1.md
grep -qE 'C5 落地修正' docs/plan-cold/v0.6.md
```
期望：全 exit 0。

## Checkpoint C5 验收

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# 1. 全套 TS 测试全绿 + server.test.ts count ≥ 108
bun x vitest run 2>&1 | tee /tmp/c5-vitest.log | tail -5
bun x vitest run src/mcp/__tests__/server.test.ts 2>&1 | tail -5 | grep -qE 'Tests +(10[8-9]|11[0-9]|12[0-5]) passed'

# 2. typecheck 0 error
bun run typecheck > /dev/null

# 3. C5 smoke 14 字段 + all_ok 全绿（真本机 dev sim）
bash scripts/simx-c5-mcp-compound-system-smoke.sh > /tmp/c5-smoke.json
jq -e '
  .exit_code == 0
  and .initialize_ok == "ok"
  and .tools_count == 26
  and .has_find_and_tap == "ok"
  and .has_wait_for == "ok"
  and .has_flow_run == "ok"
  and .has_open_url == "ok"
  and .has_pasteboard_set == "ok"
  and .has_pasteboard_get == "ok"
  and .has_permissions_grant == "ok"
  and .pasteboard_roundtrip == "ok"
  and .open_url_ok == "ok"
  and .wait_for_timeout_isError == "ok"
  and .stdout_pure_jsonrpc == "ok"
  and .all_ok == "ok"
' /tmp/c5-smoke.json > /dev/null

# 4. C1 smoke 11 字段全保（tools_list_count 26 同步）
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null

# 5. C2 smoke 10 字段全保（tools_count 26 同步，需 dev sim）
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null

# 6. C3 smoke 12 字段全保（tools_count 26 同步）
bash scripts/simx-c3-mcp-observe-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null

# 7. C4 smoke 13 字段全保（tools_count 26 同步）
bash scripts/simx-c4-mcp-interaction-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null

# 8. v0.5 acceptance 27 字段全保
bash scripts/simx-v05-acceptance.sh > /tmp/c5-v05.json
jq -e '
  .swift_tests >= 102 and .typecheck == "ok"
  and .doctor_checks >= 6
  and .c1_repl_startup == "ok" and .c2_history_undo_redo == "ok"
  and .c3_save_codegen == "ok" and .c4_doctor_checks_deep == "ok"
  and .c5_run_flags == "ok" and .c6_boot_command == "ok"
' /tmp/c5-v05.json > /dev/null

# 9. v0.4 acceptance 21 字段不退化
bash scripts/simx-v04-acceptance.sh > /tmp/c5-v04.json
jq -e '.swift_tests >= 102 and .typecheck == "ok"' /tmp/c5-v04.json > /dev/null

# 10. 决策日志 + cold plan footnote 在场
grep -qE '2026-05-16.*v0\.6 C5' docs/v1.md
grep -qE 'C5 落地修正' docs/plan-cold/v0.6.md

# 11. 改动 surface 字面确认（源码 3 条 + smoke 5 条改/新 + docs 2 条）
grep -qE "name: 'find_and_tap'" src/mcp/tools.ts
grep -qE "name: 'wait_for'" src/mcp/tools.ts
grep -qE "name: 'flow_run'" src/mcp/tools.ts
grep -qE "name: 'open_url'" src/mcp/tools.ts
grep -qE "name: 'pasteboard_set'" src/mcp/tools.ts
grep -qE "name: 'pasteboard_get'" src/mcp/tools.ts
grep -qE "name: 'permissions_grant'" src/mcp/tools.ts
grep -qE 'dispatchSubTool' src/mcp/tools.ts
grep -qE 'findAndTapTool' src/mcp/index.ts
grep -cE "^  it\(" src/mcp/__tests__/server.test.ts | grep -qE '^(10[8-9]|11[0-9]|12[0-5])$'
test -x scripts/simx-c5-mcp-compound-system-smoke.sh

# 12. dep 累计未变（C5 0 新引）
grep -qE '"@modelcontextprotocol/sdk":' package.json
grep -qE '"zod":' package.json
node -e 'const p=require("./package.json"); const d=Object.keys(p.dependencies||{}); const e=new Set(["citty","@modelcontextprotocol/sdk","zod"]); for (const k of d) { if (!e.has(k)) { console.error("unexpected:", k); process.exit(1); } } process.exit(0);'

# 13. 0 范围外文件改动（git status diff vs C4 close baseline）
git status --short 2>/dev/null | grep -vE '^.M (src/mcp/tools\.ts|src/mcp/index\.ts|src/mcp/__tests__/server\.test\.ts|scripts/simx-c[1-4]-mcp[a-z-]*\.sh|docs/v1\.md|docs/plan-hot\.md|docs/plan-cold/v0\.6\.md)$' | \
  grep -vE '^\?\? (scripts/simx-c5-mcp-compound-system-smoke\.sh|docs/plan-history/v0\.6-c5-hot\.md)$' | \
  wc -l | tr -d ' ' | grep -qE '^0$' || echo "warn: out-of-scope file changes detected (manual review)"

# 14. C5 严格不变量（CLAUDE.md §9 - 不暴露 xpath/坐标 selector / 不暴露 sleep / 不引 multi-provider VLM 抽象）
! grep -qE "name: 'sleep'|name: 'wait_ms'" src/mcp/tools.ts
! grep -qE "name: 'tap_coord'|name: 'tap_xy'" src/mcp/tools.ts
! grep -qE 'permissions_grant.*permissions:.*\[' src/mcp/tools.ts

# 15. ALL_TOOLS 26 element 字面
grep -cE "^\s*(pingTool|simulatorListTool|simulatorBootTool|simulatorShutdownTool|appLaunchTool|appTerminateTool|appInstallTool|appUninstallTool|screenDescribeTool|screenScreenshotTool|screenHierarchyTool|elementInspectTool|tapTool|doubleTapTool|longPressTool|fillTool|swipeTool|scrollToTool|keyPressTool|findAndTapTool|waitForTool|flowRunTool|openUrlTool|pasteboardSetTool|pasteboardGetTool|permissionsGrantTool)," src/mcp/tools.ts | grep -qE '^26$'

# 16. withScreenAfter 没被改（C4 helper 形态字面冻结——决策 17.B）
grep -cE 'function withScreenAfter\(' src/mcp/tools.ts | grep -qE '^1$'

# 17. 19 既有 ToolDef handler 没被改（决策 8.B——C5 只追加）
grep -cE "name: '(ping|simulator_list|simulator_boot|simulator_shutdown|app_launch|app_terminate|app_install|app_uninstall|screen_describe|screen_screenshot|screen_hierarchy|element_inspect|tap|double_tap|long_press|fill|swipe|scroll_to|key_press)'," src/mcp/tools.ts | grep -qE '^19$'

echo "C5 PASS"
```

期望：每条 exit 0。最终输出含 `C5 PASS`。

## 完成后动作

1. 归档此文件到 `docs/plan-history/v0.6-c5-hot.md`：
   ```bash
   mv docs/plan-hot.md docs/plan-history/v0.6-c5-hot.md
   ```
2. 调 sub-agent 生成新 `plan-hot.md`（到 C6 — `explain_screen` 走 `claude -p ... --image ...` 子进程 + timeout 30s + 未安装错误——cold plan §C6 字面），见 CLAUDE.md §6 标准模板。
3. C6 前置探测附加（除标准 §6 prompt + cold plan §C6 入口条件外）：
   ```bash
   # claude CLI 在 PATH + 已登录
   which claude && claude --version
   claude -p "hello" --output-format text | head -1
   # claude --help 探测 --image 等 flag 在场
   claude --help 2>&1 | grep -iE 'image|--image|attach'
   # 26 tool 字面在场（C5 close baseline）
   bash scripts/simx-c5-mcp-compound-system-smoke.sh | jq -e '.tools_count == 26'
   # ALL_TOOLS = 26 字面（grep count）
   grep -cE "^\s*(pingTool|simulatorListTool|simulatorBootTool|simulatorShutdownTool|appLaunchTool|appTerminateTool|appInstallTool|appUninstallTool|screenDescribeTool|screenScreenshotTool|screenHierarchyTool|elementInspectTool|tapTool|doubleTapTool|longPressTool|fillTool|swipeTool|scrollToTool|keyPressTool|findAndTapTool|waitForTool|flowRunTool|openUrlTool|pasteboardSetTool|pasteboardGetTool|permissionsGrantTool)," src/mcp/tools.ts | grep -qE '^26$'
   # withScreenAfter + dispatchSubTool 字面在场（C6 决策点 — explain_screen 是 query 形态非 mutate，不复用 withScreenAfter；不复用 dispatchSubTool）
   grep -qE 'withScreenAfter|dispatchSubTool' src/mcp/tools.ts
   ```
4. **C6 必须先决定的点**（cold plan §C6 字面）：
   - `explain_screen` wire 形态：`{udid, prompt?: string}` 还是 `{udid, screenshot?: base64, prompt?: string}`（决策 — 内部截图 vs 外部传入）？
   - `claude -p` flag 探测策略：每次调用前 `claude --help` cache 一次 vs 启动时 cache 一次 vs 硬编码？
   - timeout 30s：超时后 `isError` + 文案 `'claude CLI timed out after 30s'`；non-zero exit → propagate stderr？
   - claude CLI 未安装错误：handler 内 `which claude` 早期判 vs 子进程 spawn 失败时判？
   - stdout-stderr 分离：claude CLI 输出到 stdout 还是 stderr？需走 `--output-format` 控制？
   - 缓存策略：相同 screenshot + prompt 是否 cache 结果（v0.6 拒——副作用方向不缓存；推 v0.7+）？
