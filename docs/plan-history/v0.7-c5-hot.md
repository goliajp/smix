# plan-hot — v0.7 C5：Claude Code plugin manifest（`.claude-plugin/plugin.json` + 嵌入式 `mcpServers`）+ `docs/plugin-install.md` 本地 dev 安装说明 + `scripts/simx-c5-plugin-validate.sh` 11 字段结构 gate + vitest 1 manifest schema 文件 4 case — TS 586 → 590 / 25 → 26 test file / 0 swift / 0 Driver / 0 既有 MCP / 0 既有 ToolDef / 0 既有 src/cli 改 / 0 dep / 0 既有 v1.md 边界改

> [3/4 信息层] 当前唯一热计划。**C5 是 v0.7 第 5 个 checkpoint**，cold plan §Checkpoint 概要列表字面 "C5 Claude Code plugin manifest（`claude-plugin.json` 或同名）+ 安装说明，能通过 `claude /install simx` 装上"。**cold plan 字面假设与现实 Claude Code plugin 形态偏差字面记**（决策 0.A 字面，按上层 sub-agent 探测 + 官方文档 `code.claude.com/docs/en/plugins-reference.md` / `plugins.md` / `mcp.md` 校正）：
>
> 1. **manifest 文件路径偏差**：cold plan 字面 `claude-plugin.json`（项目根 dot-prefix-less 单文件）→ 现实 = `.claude-plugin/plugin.json`（项目根 `.claude-plugin/` 子目录内 `plugin.json`，**子目录形态强制**）。该路径承接最新 Claude Code plugin reference 字面（决策 1.A 字面 —— 选 `.claude-plugin/plugin.json` 路径 / 拒绝 `claude-plugin.json` 顶层文件路径 / 拒绝 `plugin.json` 顶层无目录路径）。
> 2. **安装命令偏差**：cold plan 字面 `claude /install simx` → 现实 = (a) 本地 dev = `claude --plugin-dir /path/to/lab15-autofix`（外部进程 flag）+ (b) marketplace = session 内 `/plugin install <name>`（session 内 slash 命令；非 `claude /install <name>` 外部命令）。本地 dev 路径在 C5 范围内字面验证；marketplace publish 推 v1.0+ 范围（决策 1.B 字面 —— C5 验本地 dev 形态，cold plan §出口验收行 `claude /install simx` 字面修正为 `claude --plugin-dir ./` + session 内 `/mcp` 列 `simx` server 名 + tool count；并在 C6/C7 cold plan §出口验收对齐更新 —— 但 cold plan 字面修正推 v1.0 acceptance C6 / C7 README，C5 范围内仅落地正确形态、不动 cold plan 文本 / 决策 1.C 字面 —— cold plan §出口验收行字面留作历史/假设记录，决策日志解释偏差，C6 acceptance 实际命令以 plan-hot C6 时再字面修正）。
> 3. **验证命令偏差**：cold plan 字面 `claude --plugin simx /tools | grep -c 'simx::'` 期望 `18` → 现实 = (a) `claude --plugin` flag 不存在；(b) session 内 `/mcp` 列 server 状态 + tool count（交互式 stdout，非 pipe-able）；(c) `simx::` namespace 字面**未在 simx 现 27 ToolDef 实现**（`grep -rE 'simx::' src/` 实测 0 hit；现 tool name 字面 `ping` / `app_*` / `device_*` / etc. 无 namespace 前缀）；(d) tool count 字面 `18` 偏差 = 现 27（v0.6 C6 字面已 expose `ping + 7 lifecycle + 4 observe + 7 interaction + 3 compound + 4 system + 1 vlm = 27`；cold plan v0.7 §TDD 要点字面 "返回 simx 的 18 个 tool" 是 v1.md §D MCP 18 工具字面 baseline，实际 v0.6 C6 已超额到 27）。决策 1.D 字面 —— C5 验证策略 = **结构契约 grep gate**（mirror v0.7 C3 `simx-c3-ci-matrix-validate.sh` 14 字段 pattern）+ **vitest manifest JSON schema parse / 字段断言**；**不**调真 `claude` CLI、**不**依赖 session 内交互输出。session 内 `/mcp` 字面验证推 C7 README 字面写人工 verification 步骤（决策 1.E 字面）。
>
> **元素 1（manifest 文件形态）**：新建 `.claude-plugin/plugin.json` 单文件（决策 2.A 字面 —— 选**单文件 plugin.json 内嵌 `mcpServers` 字段** / 拒绝 B 候选 `.claude-plugin/plugin.json` + 独立 `.mcp.json` 双文件 split / 拒绝 C 候选 `mcpServers` 在 plugin.json 外的 sibling 目录）。**为什么选嵌入**：(a) plugin reference 字面 "MCP servers can be defined directly in `plugin.json` via `mcpServers`" + (b) simx 仅 1 个 MCP server（自身），不需要 `.mcp.json` 多 server scope；(c) 单文件减分发面积 + grep gate 字段集中。
>
> 字段字面 schema（决策 2.B 字面 8 顶层字段字面 + mcpServers 内嵌 1 entry）：
> ```json
> {
>   "name": "simx",
>   "description": "AI-native iOS Simulator automation MCP server (27 tools: lifecycle/observe/interaction/compound/system/vlm)",
>   "version": "0.7.0",
>   "author": { "name": "simx contributors" },
>   "homepage": "https://github.com/anthropic-experimental/simx",
>   "repository": "https://github.com/anthropic-experimental/simx",
>   "license": "MIT",
>   "mcpServers": {
>     "simx": {
>       "command": "bun",
>       "args": ["${CLAUDE_PLUGIN_ROOT}/src/cli/index.ts", "mcp"]
>     }
>   }
> }
> ```
>
> 字段决策展开（决策 3.x 字面）：
> - **3.A** `name: "simx"` 字面 —— 与 `package.json.name` / mcp server `serverInfo.name = "simx"` 字面同步（已实测 `src/cli/index.ts` line 278）。
> - **3.B** `description` 字面短描述含 tool count 27 字面 + 6 channel 分类字面，方便 Claude Code plugin marketplace 列展示 + 与 v0.6 C6 实测 27 tool baseline 对齐（非 cold plan 字面 18，决策 1.D 字面记）。
> - **3.C** `version: "0.7.0"` 字面 —— 与 v0.7 当前活跃 minor 版本对齐（决策 3.C.i 字面 —— 选 `0.7.0` semver 形态 / 拒绝 `0.0.0` 与 `package.json.version` 同步 / 拒绝 `1.0.0-alpha` 形态）。`package.json.version` 当前字面 `0.0.0`（pre-release / private workspace），plugin.json 字面 `0.7.0` 是**对外可见版本号**（决策 3.C.ii 字面 —— plugin manifest 是分发面 / package.json 是内部 dev workspace；两者解耦，v1.0 release 时同步推 1.0.0）。
> - **3.D** `author.name: "simx contributors"` 字面 —— 通用占位 / 不锁单人；`author.email` 字面**不**填（决策 3.D 字面拒 —— lab15-autofix 非 git 工作树、无 maintainer 锁定 + privacy 不暴露 email）。
> - **3.E** `homepage` / `repository` 字面 `https://github.com/anthropic-experimental/simx` 占位 URL（决策 3.E.i 字面 —— 选**统一假定 GitHub URL 占位** / 拒绝 `null` / 拒绝完全 omit field / 拒绝 `repository` 字面留空字符串）。**为什么占位 URL 而非 omit**：plugin reference 字面 `homepage` / `repository` 是 optional 但 marketplace 渲染需要；填 placeholder URL 比 omit 更前向兼容；当 v1.0 真正推 GitHub 时仅需替换 URL 字面（决策 3.E.ii 字面 —— v1.0 acceptance C7 README 写人工 publish-time 字符串替换步骤）。
> - **3.F** `license: "MIT"` 字面 —— 与 simx 自身闭源候选定位有冲突字面记（CLAUDE.md §8.7 "不引入 GPL / AGPL（避免传染到 simx 这个闭源候选）"），但 plugin.json `license` 字段是**对外声明字段**，MIT 是 plugin 分发友好默认；真正 simx 项目 license 决策推 v1.0 release（决策 3.F.i 字面 —— MIT 占位 / 决策 3.F.ii 字面 —— 与 CLAUDE.md "闭源候选" 字面记冲突点：plugin license 是分发声明、不是仓库 license；v1.0 release 时若 simx 仓库选 proprietary license，plugin.json `license` 字段 omit 或改 "UNLICENSED"；C5 范围内 MIT 占位）。
> - **3.G** `mcpServers.simx.command: "bun"` + `args: ["${CLAUDE_PLUGIN_ROOT}/src/cli/index.ts", "mcp"]` 字面（决策 3.G.i 字面 —— 选 **B 候选直接 `bun` + script path args** / 拒绝 A 候选 `bin/simx-mcp` 软链 shim / 拒绝 C 候选 wrapper shell script `bin/simx-mcp.sh`）。**为什么 B 候选**：(a) `${CLAUDE_PLUGIN_ROOT}` 是 plugin reference 字面文档化模板变量，指 plugin 根目录、跨用户 home 路径 portable；(b) `bun` 命令需用户已装 bun ≥ 1.1.0（与 `package.json.engines.bun` 字面同步），通过 plugin install 前置 doctor check 验证（推 C6 v1 acceptance）；(c) shim 软链方案在 lab15-autofix 非 git 工作树 + 用户安装路径不确定下不可移植；(d) wrapper shell script 增 1 文件 + 增 shebang/PATH 复杂度。决策 3.G.ii 字面 —— args 第一项 `${CLAUDE_PLUGIN_ROOT}/src/cli/index.ts` 字面绝对路径模板，**不**用相对路径 `./src/cli/index.ts`（plugin install 后 working dir 不一定是 plugin 根）；args 第二项 `mcp` 字面是 simx subCommand 名（已实测 `src/cli/index.ts:270-285` `mcpCommand`）。
> - **3.H** **mcpServers 内嵌 vs 独立 .mcp.json 决策** = **A 候选嵌入**（决策 2.A 字面已锁）。
>
> **元素 2（安装说明文档）**：新建 `docs/plugin-install.md`（决策 4.A 字面 —— 选独立 `docs/plugin-install.md` 文件 / 拒绝 B 候选写到 `README.md` 新章节 / 拒绝 C 候选写到 `docs/design.md`）。**为什么独立文件**：(a) README.md 当前 207 行 / v1.0 发布章节推 C7 整体 rewrite，C5 范围内不污染 README；(b) `docs/plugin-install.md` 边界单一 = plugin install 步骤说明、独立到 v0.7 C5 物理交付物；(c) README 中加一条 link 指向 `docs/plugin-install.md` 字面 1 行 + ~50 行新文件 = ≤ 6 文件约束严守。
>
> `docs/plugin-install.md` 字面 ~60-80 行内容字面（决策 4.B 字面 章节顺序固定 4 段）：
> 1. **§1 前置要求**字面 4 条：(a) macOS ≥ 15；(b) Xcode ≥ 26.0 + iOS 26.x runtime；(c) `bun --version ≥ 1.1.0`；(d) `claude` CLI 已登录（与 `simx doctor` 字面对齐）。
> 2. **§2 本地 dev 安装**字面命令 `claude --plugin-dir /absolute/path/to/lab15-autofix`（决策 4.C.i 字面 —— C5 范围验本地 dev 形态；marketplace publish 推 v1.0+）+ `claude` 退出 + 重启后 session 内 `/mcp` 列出 `simx` server 字面（决策 4.C.ii 字面 —— session 验证步骤人工）。
> 3. **§3 验证**字面命令：(a) session 内 `/mcp` 应显示 `simx` server + 27 tools 字面 + status `connected`；(b) `bun src/cli/index.ts mcp < /dev/null` 字面本地 MCP server 可启动（与 `simx-c1-mcp-smoke.sh` 字面同源验证）；(c) `bash scripts/simx-c5-plugin-validate.sh` 字面输出 `all_ok=ok`（结构契约 gate）；(d) `bun src/cli/index.ts doctor --json` 字面 `compatibility.status='supported'` 6 check 全过（C4 字面延续）。
> 4. **§4 marketplace publish（未来）**字面**占位段**：plugin 推 v1.0 release 后通过 Claude Code plugin marketplace 发布；当前 v0.7 C5 不在范围（决策 4.D 字面 —— 写 placeholder 段 + 推 v1.0+ 文档化路径，cold plan §C7 README v1.0 rewrite 时字面补完）。
>
> **元素 3（结构契约 validator）**：新建 `scripts/simx-c5-plugin-validate.sh` ~80-100 行（决策 5.A 字面 —— mirror v0.7 C3 `simx-c3-ci-matrix-validate.sh` 14 字段 jq+grep gate pattern）。**11 字段 JSON gate**（决策 5.B 字面）：
>
> | 字段 | 验证内容 |
> |---|---|
> | `manifest_exists` | `test -f .claude-plugin/plugin.json` |
> | `valid_json` | `jq -e . .claude-plugin/plugin.json` parse 成功 |
> | `has_name` | `jq -e '.name == "simx"' ...` |
> | `has_description` | `jq -e '.description \| type == "string" and length >= 20' ...` |
> | `has_version` | `jq -e '.version \| test("^\\d+\\.\\d+\\.\\d+$")' ...`（semver 形态） |
> | `has_mcp_servers` | `jq -e '.mcpServers.simx \| type == "object"' ...` |
> | `mcp_command_bun` | `jq -e '.mcpServers.simx.command == "bun"' ...` |
> | `mcp_args_has_cli` | `jq -e '.mcpServers.simx.args[0] \| contains("src/cli/index.ts")' ...` |
> | `mcp_args_has_mcp_sub` | `jq -e '.mcpServers.simx.args[1] == "mcp"' ...` |
> | `mcp_args_uses_plugin_root` | `jq -e '.mcpServers.simx.args[0] \| contains("${CLAUDE_PLUGIN_ROOT}")' ...` |
> | `all_ok` | 上 10 字段全 ok |
>
> 字段决策：**11 字段而非 8 / 而非 15** —— 8 字段不足 cover `mcpServers` 内嵌 + 3 args 子字段；15 字段会下到 license/homepage 等 optional 字段、增脆性（决策 5.C 字面）。**不**验 `has_license` / `has_homepage` / `has_repository` / `has_author` —— optional + URL 占位会随 v1.0 release 替换，不锁字面 URL（决策 5.D 字面）。
>
> **元素 4（vitest 1 文件 4 case 字面）**：新建 `src/cli/__tests__/plugin-manifest.test.ts` ~70-90 行（决策 6.A 字面 —— 放 `src/cli/__tests__/` 与 `doctor.test.ts` / `doctor-schemas.test.ts` 同目录 / 拒绝 `src/mcp/__tests__/` —— manifest 是 CLI/分发表面、不是 MCP wire / 拒绝 `tests/` 顶层目录 —— v0.6 C7 起 `src/{module}/__tests__/` pattern 字面固定）。
>
> **4 case 字面**（决策 6.B 字面）：
> 1. `it('plugin.json exists at .claude-plugin/plugin.json and is valid JSON')` — 用 `fs.readFileSync` + `JSON.parse` round-trip；断 `name === 'simx'` + `version` matches semver regex
> 2. `it('plugin manifest embeds mcpServers.simx with bun + ${CLAUDE_PLUGIN_ROOT} args pattern')` — 断 `.mcpServers.simx.command === 'bun'` + `args[0]` contains `${CLAUDE_PLUGIN_ROOT}` literal + contains `src/cli/index.ts` + `args[1] === 'mcp'`
> 3. `it('plugin manifest description mentions 27 tools to match v0.6 C6 baseline')` — 断 `.description` contains `'27 tools'` + length ≥ 20
> 4. `it('plugin manifest name matches package.json name and src/cli/index.ts mcp serverInfo name')` — 三向同步：`pluginManifest.name === 'simx'` + `packageJson.name === 'simx'` + `src/cli/index.ts` line 278 字面 `name: 'simx'`（用 string read + regex match 替代 dynamic import，避免实例化 MCP server）
>
> 决策 6.C 字面 —— 4 case 而非 6 / 而非 3 —— 4 case cover (a) manifest 字面存在 + JSON 合法；(b) mcpServers 内嵌正确性；(c) description 与 27 tool baseline 同步；(d) 三向 name 同步。3 case 会漏 description / name 同步；6 case 会重复 validator 已 cover 的字段断言（决策 6.D 字面 —— vitest 测**语义不变量**，validator 测**字面契约存在**，两层互补不重复）。
>
> **承接（v0.7 C4 close 后实测 baseline，2026-05-16）**：v0.7 C4 ✅ done —— v1.md 决策日志末尾 `2026-05-16 v0.7 C4 close` 字面在场 + `src/cli/commands/doctor-schemas.ts` 字面在场（7 zod schema + 8 z.infer type）+ `src/cli/__tests__/doctor-schemas.test.ts` 字面在场 + `src/cli/commands/doctor.ts` 字面 compatibility 字段在 6 check 全填 + `scripts/simx-c4-doctor-smoke.sh` 实测 9 字段 + `{"exit_code":0,"checks_len":6,"schema_ok":"ok","runtimes_has_items":"ok","claude_logged_in":"ok","has_compatibility_field":"ok","compat_status_enum_ok":"ok","runtimes_compat_supported":"ok","all_ok":"ok"}` 字面（已实测）；TS baseline = **586**（C4 +3 doctor + 5 doctor-schemas = 8 → C5 起点 586）/ **25 test files**（C4 +1 doctor-schemas.test.ts → C5 起点 25）/ 102 swift 不变 / typecheck 0 error / 27 MCP tool / 3 prod dep（citty / @modelcontextprotocol/sdk / zod，已实测 package.json line 41-45）/ `bun --version = 1.3.14`（已实测）/ `Xcode 26.4.1`（已实测）/ `xcrun simctl list runtimes -j` 单 iOS-26-4 字面（已实测）/ `bun x vitest run` 末尾字面 `Tests 586 passed (586)` + `Test Files 25 passed (25)`（已实测）/ `bash scripts/simx-c1-mcp-smoke.sh` `all_ok=ok` + `tools_list_count=ok`（27 tool baseline 字面，已实测）。
>
> **进入 v0.7 C5 前置检字面全 ok**：
>
> 1. **v0.7 C4 工程实体字面在场** — `src/cli/commands/doctor-schemas.ts` + `src/cli/__tests__/doctor-schemas.test.ts` + `scripts/simx-c4-doctor-smoke.sh` 9 字段 + `docs/plan-history/v0.7-c4-hot.md` 字面 + v1.md `2026-05-16 v0.7 C4 close` 字面（已实测）；
> 2. **v1.md §4 Success Criteria [7] "Claude Code 集成：claude /install simx 一键启用 MCP" 字面在场**（已实测 line 84）+ §2 D "MCP Server（18 工具）/ stdio 协议 + Claude Code plugin 形态分发" 字面在场（已实测 line 45-49）—— C5 范围工程实体 = v1.md §2 D + §4 [7] + cold plan v0.7 §C5；
> 3. **既有 MCP 27 ToolDef 字面在场** — `bash scripts/simx-c1-mcp-smoke.sh` 字面 `tools_list_count=ok`（hardcoded 27 字面，已实测）；
> 4. **本机非 git 工作树字面延续** — `test -d .git` 字面 fail；C5 范围工程实体 = .claude-plugin/plugin.json + docs/ + scripts/ + src/cli/__tests__/ + docs/v1.md，不依赖 git；plugin marketplace 发布推 v1.0+；
> 5. **`.claude-plugin/` 目录字面不存在**（已实测）+ `.claude-plugin/plugin.json` 字面不存在 + `.mcp.json` 字面不存在 + `docs/plugin-install.md` 字面不存在 + `scripts/simx-c5-plugin-validate.sh` 字面不存在 + `src/cli/__tests__/plugin-manifest.test.ts` 字面不存在（C5 红前提）；
> 6. **`bun` ≥ 1.1.0 字面在场** + `claude` CLI 字面已登录（doctor compatibility 6 check supported 字面延续 C4）；C5 范围内 plugin install command 字面引用 `bun` 命令是 doctor 已验通过路径；
> 7. **`src/cli/index.ts` `mcpCommand` 字面在场**（line 270-285，已实测）+ `serverInfo.name = 'simx'` 字面（line 278）+ `version: '0.0.0'` 字面（v0.6 C1 baseline；C5 不改）—— plugin.json args 字面指 `src/cli/index.ts mcp` 子命令；
> 8. **既有 27 ToolDef 字面 0 改约束**：C5 范围内不动 `src/mcp/tools.ts` 任何字段；plugin.json description 字面 "27 tools" 与现状同步。
>
> **本机探测（C5 落地决策依据，2026-05-16）**：
>
> - `xcodebuild -version` 字面 `Xcode 26.4.1`（C4 baseline 延续）
> - `xcrun simctl list runtimes -j` 字面单 iOS-26-4（C4 baseline 延续）
> - `bun --version` 字面 `1.3.14`（plugin.json `mcpServers.simx.command='bun'` 字面依赖项）
> - `bun x vitest run` 末尾字面 `Tests 586 passed (586)` + `Test Files 25 passed (25)`（C5 起点；C5 close 后 590/26）
> - `(cd swift-bridge && swift test)` 字面 `Executed N tests` ≥ 102（C5 0 改 swift-bridge/）
> - `bash scripts/simx-c1-mcp-smoke.sh` 字面 `all_ok=ok` + `tools_list_count=ok`（27 tool 字面延续；C5 0 改 MCP）
> - `bash scripts/simx-c4-doctor-smoke.sh` 字面 9 字段 + `all_ok=ok`（C4 字面延续；C5 0 改 doctor）
> - `grep -rE 'simx::' src/ | wc -l` 字面 `0`（决策 1.D.i 字面证 —— `simx::` namespace 未实装，cold plan §出口验收 `grep -c 'simx::'` 期望 18 字面为假设，C5 不引入 namespace 改名）
> - `find . -name '.claude-plugin' -maxdepth 2` 字面空（C5 红前提）
> - `jq -r '.dependencies | keys | length' package.json` 字面 `3`（C5 0 dep 增）
>
> **现状盘点（C5 真生效边界）**：
>
> - **新建 `.claude-plugin/plugin.json`**（~20-25 行 JSON / 8 顶层字段 + 1 mcpServers entry，决策 2.A/2.B 字面字段集合）—— 核心 1
> - **新建 `docs/plugin-install.md`**（~60-80 行 / 4 段：前置 / 本地 dev 安装 / 验证 / marketplace 占位，决策 4.A/4.B 字面）—— 核心 2
> - **新建 `scripts/simx-c5-plugin-validate.sh`**（~80-100 行 / 11 字段 JSON gate，决策 5.A/5.B 字面）—— 核心 3
> - **新建 `src/cli/__tests__/plugin-manifest.test.ts`**（~70-90 行 / 1 describe / 4 case，决策 6.A/6.B 字面）—— 核心 4
> - **编辑 `docs/v1.md`**（决策日志末尾 +1 行 v0.7 C5 close note 字面，仿 C4 close note pattern；不动 §2/§4/§5/§6 边界） —— docs
>
> **核心改动 = 4 新建 + 1 docs 编辑 = 5 件物理变更 ≤ 6 字面满足**（决策 7.A 字面 —— 新建 3 src/dist/scripts artefact + 1 vitest file + 1 docs edit；0 既有 src 改 / 0 既有 scripts 改 / 0 既有 docs 范围改 / 0 dep / 0 ci.yml 改）。
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / SimctlClient 0 / RunnerClient 0 / RunnerSupervisor 0 / MCP 27 ToolDef 0 改 / 既有 src/core/schemas.ts 0 改 / 既有 src/{core,sdk,driver,mcp,sim} 0 改 / 既有 src/cli/index.ts 0 改（plugin.json args 字面引用既有 mcpCommand 字面接口，0 改 src/cli/index.ts）/ 既有 src/cli/commands 任何文件 0 改 / 既有 src/cli/__tests__ 中 doctor.test.ts / doctor-schemas.test.ts / 已有 *.test.ts 0 改 / 既有 src/mcp/__tests__ 0 改 / dep 0 / 既有 27 ToolDef 0 / 既有 6 既有 MCP smoke 字段 0 / 既有 17 c[1-8] smoke 中 17 全 0 改（C5 新增 1 个 c5-plugin-validate，不动既有 c5-mcp-compound-system / c5-run-flags / c5-step-png）/ 既有 25 test file 0 改（C5 仅 +1 plugin-manifest.test.ts）/ ci.yml 0 改 / v1.md 边界 0 改 / roadmap.md 0 改 / cold v0.7 0 改 / package.json 0 改 / engines.bun 0 改 / bun.lock 0 改 / tsconfig 0 改 / vitest.config 0 改 / README.md 0 改**（C5 范围内 README 不动；plugin install link 添加推 C7 README v1.0 rewrite 时一次性处理）。
>
> **允许改动 / 新建文件范围**（**核心 ≤ 6 条**字面严守）：
>
> 1. **新建 `.claude-plugin/plugin.json`** — 核心 1
> 2. **新建 `docs/plugin-install.md`** — 核心 2
> 3. **新建 `scripts/simx-c5-plugin-validate.sh`** — 核心 3
> 4. **新建 `src/cli/__tests__/plugin-manifest.test.ts`** — 核心 4
> 5. **编辑 `docs/v1.md` 决策日志 +1 行** — docs
>
> **不允许**：(a) 改 `src/` 任何既有文件（plugin manifest 字面引用既有 mcpCommand 字面接口、0 改）；(b) 改 `src/cli/index.ts` 加 plugin-aware path；(c) 加 `simx::` namespace 到 27 ToolDef（决策 1.D 字面拒——cold plan §出口验收行 `simx::` 是假设非现状，namespace 改名推 v1.0+ 范围 + 涉及 26 wire shape break）；(d) 改 27 ToolDef 任何字段；(e) 加 ToolDef 到 28+（v1.md §D 字面 18 工具上限的扩展窗口已在 v0.6 用满 27、不再扩）；(f) 改 既有 doctor 任何字段；(g) 改 `package.json` 加 `bin` entry / 加 `mcpServer` script（plugin args 直接 `bun + src/cli/index.ts mcp`，0 改 package.json）；(h) 改 `package.json.version` 与 plugin.json version 同步（决策 3.C.ii 字面解耦）；(i) 引第 4 个 npm dep（决策 8.A 字面拒——0 dep 增）；(j) 加 `bin/simx-mcp` shim 软链或 `bin/simx-mcp.sh` wrapper script（决策 3.G.i 字面拒——直接 bun 命令足够）；(k) 加 env 变量；(l) 加 CLI flag 如 `simx plugin-manifest` 子命令（决策 8.B 字面拒——manifest 是 plugin 分发面静态文件，不是 runtime 命令）；(m) 加 `.mcp.json` 独立文件（决策 2.A 字面拒——单文件 plugin.json 内嵌 mcpServers）；(n) 加 marketplace publish 脚本 / npm publish 配置（决策 4.D 字面拒——v1.0+ 范围）；(o) 改 README.md 加 plugin install 章节（决策 9.A 字面拒——C5 范围内 README 不动，链接 + 章节推 C7 README v1.0 rewrite）；(p) 引 `claude` CLI 真调验证（决策 1.D 字面拒——交互式 stdout 不 pipe-able，C5 验证 = 静态契约 gate + vitest manifest schema test）；(q) 用 zod 给 plugin.json schema 加 zod schema 在 `src/cli/commands/doctor-schemas.ts` 或新 `src/cli/commands/plugin-schemas.ts`（决策 10.A 字面拒——plugin.json 是 Claude Code 端 schema，不归 simx 项目所有，simx 端 vitest 用 `JSON.parse` + ad-hoc 断言足够；zod schema 推 v1.1+ 若 plugin.json schema 稳定后落地）；(r) 给 manifest 加 `commands` / `agents` / `skills` / `hooks` 字段（决策 11.A 字面拒——v0.7 C5 范围仅 MCP server 分发，其他 plugin extension 字段推 v1.1+ 范围 + 当前 simx 不提供 commands/agents/skills/hooks 实体）；(s) 改 既有 v0.5 C4 doctor `compatibility` 字段加 `plugin_manifest` 第 7 个 check（决策 12.A 字面拒——doctor 6 check 字面冻结，plugin manifest 检查是分发面、不是环境探测面）；(t) 改 `simx-c1-mcp-smoke.sh` 加 plugin.json 字段验证（决策 12.B 字面拒——c1 smoke 是 MCP 协议 smoke，plugin manifest 验证另文件 c5-plugin-validate.sh）；(u) 改 既有 17 c[1-8] smoke 任何字段（决策 12.C 字面拒——C5 是 additive 仅 +1 新 smoke）；(v) 加 `scripts/simx-c5-plugin-validate.sh` 字段超过 11（决策 5.C 字面拒——11 字段 cover 顶层 + mcpServers + 3 args 子字段，license/homepage/repository optional 不入 gate）；(w) 改 plugin.json `name` 字段为 `"simx-mcp"` 或 `"@anthropic/simx"`（决策 13.A 字面拒——`name='simx'` 字面与 mcp server `serverInfo.name='simx'` + `package.json.name='simx'` 三向同步，改名会 break 已发布 marketplace identity 假设）；(x) 改 `mcpServers.simx` key 为 `simx-mcp` 或 `ios-simulator`（决策 13.B 字面拒——key 字面是用户 session 内 `/mcp` 列展示 server 名 / 同 `name` 字面同步原则）；(y) 加 `mcpServers.simx.env` 字段或 `cwd` 字段（决策 13.C 字面拒——`${CLAUDE_PLUGIN_ROOT}` 已 cover working dir 隐式语义，env 字段非必需 / 推 v1.0+ 若发现 simx 启动需 env）；(z) 写 C6 v1 acceptance / C7 README 整体 rewrite（C6-C7 范围）；(aa) 把 plugin.json 字段语义改为 npm `package.json` 字段（如 `dependencies` / `scripts`）（决策 14.A 字面拒——plugin.json 是 Claude Code 端 schema，不是 npm package；混淆会破坏 plugin reference 字面契约）；(ab) 加 `.claude-plugin/marketplace.json` 文件（决策 14.B 字面拒——marketplace.json 是 marketplace publisher 端文件，不是 plugin 自身文件 / 推 v1.0+ release time）；(ac) 改 `homepage` / `repository` 字面 URL 为真 GitHub repo（决策 3.E.i 字面 —— 占位 URL，v1.0 release 时替换）；(ad) 减字段（如 omit `description` / `version`）—— description/version 是 marketplace 必备渲染字段；(ae) 改 `version: "0.7.0"` 字面为 `"0.0.0"` 或与 package.json 同步（决策 3.C.ii 字面解耦）。
>
> **任务说明假设字面修正记**（决策 15.A 字面）：本任务字面 "能通过 `claude /install simx` 装上" 实际 = `claude --plugin-dir ./` + session 内 `/mcp` 列；本任务字面 "`claude --plugin simx /tools` | `grep -c 'simx::'` 期望 18" 实际 = `/mcp` 交互式 stdout 显示 27 tool / `simx::` namespace 未实装。本 C5 hot plan 落地实物形态 = (a) `.claude-plugin/plugin.json` 字面在场契合 Claude Code plugin reference 字面要求 + (b) 本地 dev `claude --plugin-dir <abs path>` 字面可加载（人工验证步骤写在 `docs/plugin-install.md`）+ (c) 结构契约 gate + vitest schema test 等价 "如果用户跑 `claude --plugin-dir ./` 命令 + 重启 session + `/mcp` 命令 → 27 tool 应 enumerable" 的可证 evidence；(d) cold plan §出口验收行字面 `claude /install simx` + `claude --plugin simx /tools` 字面修正推 C6 v1 acceptance / C7 README 时一次性纠正，C5 范围内不动 cold plan 文本（决策 15.B 字面 —— cold plan v0.7.md 字面 0 改，决策日志解释偏差）。**git 工作树缺席偏差延续记**：lab15-autofix 仍非 git 工作树（无 `.git`），marketplace publish 推 v1.0+ + GitHub repo 推 release time；C5 范围内 0 git 依赖、homepage/repository 字段填占位 URL。**plugin.json `version` 与 `package.json.version` 解耦字面记**（决策 3.C.ii 字面）：plugin.json `0.7.0` 字面 / package.json `0.0.0` 字面；两者解耦理由 = plugin 是对外可见分发面、package 是内部 dev workspace；v1.0 release 时 plugin.json 推 `1.0.0` + package.json 同步 `1.0.0`，C5 范围内仅 plugin.json 写 `0.7.0`。**`simx::` namespace 未实装字面记**：cold plan §出口验收行 `claude --plugin simx /tools | grep -c 'simx::'` 期望 18 字面是 namespace 改名 + tool count 双假设；现状 27 tool 无 namespace 前缀；C5 范围内不引入 namespace 改名（涉及 wire shape break + 既有 27 ToolDef + 7 lifecycle + 4 observe + 7 interaction + 3 compound + 4 system + 1 vlm + 1 ping 同时改）；推 v1.1+ 若 marketplace 需 namespace。

## 目标 checkpoint

**C5**：通过后世界变成 —

(a) `.claude-plugin/plugin.json` 字面在场 + 8 顶层字段（`name='simx'` / `description` 含 `'27 tools'` 子串 / `version='0.7.0'` semver / `author.name='simx contributors'` / `homepage` + `repository` 占位 URL / `license='MIT'`）+ 1 `mcpServers.simx` entry（`command='bun'` + `args=[${CLAUDE_PLUGIN_ROOT}/src/cli/index.ts, mcp]`）；
(b) `docs/plugin-install.md` 字面在场 + 4 段（前置 / 本地 dev 安装 `claude --plugin-dir` / 验证 / marketplace 占位）；
(c) `scripts/simx-c5-plugin-validate.sh` 字面在场 + 11 字段 JSON gate + `all_ok='ok'` + exit 0；
(d) `src/cli/__tests__/plugin-manifest.test.ts` 字面在场 + 1 describe + 4 case 字面全绿；
(e) `bun x vitest run` 字面 `Tests 590 passed (590)` + `Test Files 26 passed (26)`（决策 16.A 字面 baseline：586 + 4 plugin-manifest = 590；25 + 1 new plugin-manifest.test.ts = 26）；
(f) 既有 `bash scripts/simx-c1-mcp-smoke.sh` 字面 `all_ok=ok` + `tools_list_count=ok` 不退化（27 ToolDef 不变）+ 既有 `bash scripts/simx-c4-doctor-smoke.sh` 字面 9 字段 `all_ok=ok` 不退化；
(g) `bun run typecheck` 字面 0 error；
(h) `docs/v1.md` 决策日志末尾 +1 行 v0.7 C5 close note 字面；
(i) 0 dep / 0 swift / 0 既有 src/core / 0 既有 MCP 27 ToolDef / 0 既有 Driver / 0 既有 SimctlDriver / 0 既有 examples / 0 既有 ci.yml / 0 既有 17 c[1-8] smoke 改 / 0 既有 25 test file 改 / 0 v1.md 边界 / 0 roadmap.md / 0 cold v0.7 改 / 0 README.md 改 字面退化检查全过。

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.7 C4 close 痕迹存在
test -f src/cli/commands/doctor-schemas.ts
test -f src/cli/__tests__/doctor-schemas.test.ts
test -f docs/plan-history/v0.7-c4-hot.md
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C4 close' docs/v1.md

# v0.7 C3 + C2 + C1 痕迹延续
test -f .github/workflows/ci.yml
test -f scripts/simx-c3-ci-matrix-validate.sh
test -f scripts/simx-c2-ci-workflow-validate.sh
test -f scripts/simx-c1-longrun-100-smoke.sh
test -f scripts/long-run-100.ts

# v0.6 close 痕迹延续（27 MCP tool baseline）
test -f docs/plan-history/v0.6-c8-hot.md
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f src/core/schemas.ts

# 27 MCP tool baseline 字面（hardcoded in c1 smoke）
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.tools_list_count == "ok"' > /dev/null

# 9-field doctor smoke baseline 字面（C4 close 字面延续）
bash scripts/simx-c4-doctor-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c4-doctor-smoke.sh 2>/dev/null | jq -e '.has_compatibility_field == "ok"' > /dev/null

# v1.md §4 [7] + §2 D 字面在场
grep -qE 'Claude Code 集成.*claude /install simx' docs/v1.md
grep -qE 'MCP Server.*18 工具' docs/v1.md
grep -qE 'Claude Code plugin 形态分发' docs/v1.md

# roadmap.md v0.7 🔥 字面（未提前翻 ✅）
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# 586 TS baseline + 25 test file 字面（C5 起点）
bun x vitest run 2>&1 | grep -qE 'Tests +586 passed'
bun x vitest run 2>&1 | grep -qE 'Test Files +25 passed'

# typecheck baseline 字面
bun run typecheck > /dev/null

# dep 字面 3 个（不退化）
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'
jq -e '.dependencies["@modelcontextprotocol/sdk"]' package.json > /dev/null
jq -e '.dependencies["citty"]' package.json > /dev/null
jq -e '.dependencies["zod"]' package.json > /dev/null

# src/cli/index.ts mcpCommand 字面在场（C5 plugin.json args 引用接口）
grep -qE "name: 'mcp'" src/cli/index.ts
grep -qE "createSimxMcpServer" src/cli/index.ts
grep -qE "name: 'simx'" src/cli/index.ts

# package.json name = 'simx' 字面（plugin.json name 同步源）
test "$(jq -r '.name' package.json)" = 'simx'

# C5 红前提：4 新建工程实体字面**不存在**
! test -d .claude-plugin
! test -f .claude-plugin/plugin.json
! test -f docs/plugin-install.md
! test -f scripts/simx-c5-plugin-validate.sh
! test -f src/cli/__tests__/plugin-manifest.test.ts

# C5 红前提：`simx::` namespace 未实装字面（决策 1.D.i 字面证）
test "$(grep -rE 'simx::' src/ 2>/dev/null | wc -l | tr -d ' ')" = '0'

# 既有 27 ToolDef 字面在场（C5 0 改 src/mcp/tools.ts）
test -f src/mcp/tools.ts

# .mcp.json 不存在（决策 2.A 字面 —— 不双文件 split）
! test -f .mcp.json
```

期望：上述命令全 exit 0 / 不打印任何错误；任何一条不满足，**拒绝**进入 C5，回报上层。

## 步骤（线性，无分叉；3 个）

### S1. 新建 `.claude-plugin/plugin.json` + `scripts/simx-c5-plugin-validate.sh` 11 字段结构 gate

**红（写测试）**

- 文件：`scripts/simx-c5-plugin-validate.sh`（新建 ~80-100 行 / shebang `#!/usr/bin/env bash` + `set -uo pipefail`）
- 11 字段全 init `fail` + `jq -e` 逐字段验 + `printf '{"exit_code":...}\n'` 单行 JSON 输出 + `exit "$EXIT_CODE"`
- 字段字面（决策 5.B 字面 11 条）：`manifest_exists` / `valid_json` / `has_name` / `has_description` / `has_version` / `has_mcp_servers` / `mcp_command_bun` / `mcp_args_has_cli` / `mcp_args_has_mcp_sub` / `mcp_args_uses_plugin_root` / `all_ok`
- `chmod +x scripts/simx-c5-plugin-validate.sh`
- **红状态验证**：`bash scripts/simx-c5-plugin-validate.sh` 字面退出 `1` + JSON `"manifest_exists":"fail"` + `"all_ok":"fail"` 字面（`.claude-plugin/plugin.json` 不存在）

**绿（实现）**

- 文件：`.claude-plugin/plugin.json`（新建 ~20-25 行 JSON）
- 字段字面（决策 2.B 字面）：
  ```json
  {
    "name": "simx",
    "description": "AI-native iOS Simulator automation MCP server (27 tools: lifecycle/observe/interaction/compound/system/vlm)",
    "version": "0.7.0",
    "author": { "name": "simx contributors" },
    "homepage": "https://github.com/anthropic-experimental/simx",
    "repository": "https://github.com/anthropic-experimental/simx",
    "license": "MIT",
    "mcpServers": {
      "simx": {
        "command": "bun",
        "args": ["${CLAUDE_PLUGIN_ROOT}/src/cli/index.ts", "mcp"]
      }
    }
  }
  ```
- **绿状态验证**：
  - `test -f .claude-plugin/plugin.json` exit 0
  - `jq -e '.name == "simx"' .claude-plugin/plugin.json` exit 0
  - `jq -e '.mcpServers.simx.args[0] | contains("${CLAUDE_PLUGIN_ROOT}")' .claude-plugin/plugin.json` exit 0
  - `bash scripts/simx-c5-plugin-validate.sh` 字面 exit 0 + JSON 11 字段全 `"ok"` + `"all_ok":"ok"`
  - 关键点：(a) `${CLAUDE_PLUGIN_ROOT}` 字面 raw 字符串（不被 shell 展开 / 不用单引号外包装 / JSON 内为 plain string token，Claude Code plugin loader 端展开）；(b) `args[1]='mcp'` 字面 = simx mcpCommand 名；(c) `command='bun'` 字面 = doctor `bun` check supported 字面延续

**重构**

- 无（11 字段 jq pattern 已紧凑 mirror v0.7 C3 validator pattern；不引入 helper function）

### S2. 新建 `src/cli/__tests__/plugin-manifest.test.ts` 4 vitest case + `docs/plugin-install.md`

**红（写测试）**

- 文件：`src/cli/__tests__/plugin-manifest.test.ts`（新建 ~70-90 行 / TS）
- import: `import { describe, it, expect } from 'vitest'` + `import { readFileSync } from 'node:fs'` + `import { resolve } from 'node:path'`
- 1 describe block `'plugin manifest (.claude-plugin/plugin.json) (v0.7 C5)'`
- 4 case 字面（决策 6.B 字面）：
  - case 1 `it('plugin.json exists at .claude-plugin/plugin.json and is valid JSON')` — `const raw = readFileSync(resolve(process.cwd(), '.claude-plugin/plugin.json'), 'utf-8')` + `const m = JSON.parse(raw)` + `expect(m.name).toBe('simx')` + `expect(m.version).toMatch(/^\d+\.\d+\.\d+$/)`
  - case 2 `it('plugin manifest embeds mcpServers.simx with bun + ${CLAUDE_PLUGIN_ROOT} args pattern')` — 断 `m.mcpServers.simx.command === 'bun'` + `m.mcpServers.simx.args[0]` includes `'${CLAUDE_PLUGIN_ROOT}'` 字面 + `m.mcpServers.simx.args[0]` includes `'src/cli/index.ts'` + `m.mcpServers.simx.args[1] === 'mcp'`
  - case 3 `it('plugin manifest description mentions 27 tools to match v0.6 C6 baseline')` — 断 `m.description.includes('27 tools')` + `m.description.length >= 20`
  - case 4 `it('plugin manifest name matches package.json name and src/cli/index.ts mcp serverInfo name')` — 读 `package.json` + `src/cli/index.ts` 字面 grep `name: 'simx'`，断三向 `===`
- **红状态验证**：`bun x vitest run src/cli/__tests__/plugin-manifest.test.ts` 字面 4 case fail（理由：S1 已建 plugin.json，case 4 应直接绿；case 1-3 也应直接绿——**判定 S2 红状态实际 = 测试在文件未建前不存在；测试文件刚创建时尚未运行**；红状态 = 文件存在但 case 4 中读取 `src/cli/index.ts` 字面后断言**初始故意写错** `expect(serverInfoName).toBe('SIMX_PLACEHOLDER')`，使 case 4 红；其他 3 case 绿）
- **绿状态修复**：把 case 4 字面 `'SIMX_PLACEHOLDER'` 改回 `'simx'`，4 case 全绿
- 决策 17.A 字面 —— S2 红绿分段 = case 4 故意写错断言制造红 → 绿状态改回。这是 vitest TDD 在 manifest 已建后唯一可执行的红→绿路径

**绿（实现）**

- 文件：`docs/plugin-install.md`（新建 ~60-80 行 / markdown）
- 4 段字面（决策 4.B 字面）：
  - `# simx Claude Code plugin — 安装与验证`
  - `## 1. 前置要求` — 4 条：macOS ≥ 15 / Xcode ≥ 26.0 + iOS 26.x runtime / `bun --version ≥ 1.1.0` / `claude` CLI 已登录
  - `## 2. 本地 dev 安装` — 命令 `claude --plugin-dir /absolute/path/to/lab15-autofix` + 重启 session
  - `## 3. 验证` — 4 条：(a) session 内 `/mcp` 显示 `simx` server + 27 tools + status connected；(b) `bun src/cli/index.ts mcp < /dev/null` 本地 MCP server 启动正常；(c) `bash scripts/simx-c5-plugin-validate.sh` 字面 `all_ok=ok`；(d) `bun src/cli/index.ts doctor --json` 字面 `compatibility.status='supported'`（C4 字面延续）
  - `## 4. Marketplace publish（v1.0+）` — 占位段，注明推 v1.0 release 后通过 Claude Code plugin marketplace 发布
- 关键点：(a) 文档使用 imperative 命令字面 + 等宽块；(b) §3 验证条 (a) 注明 "session 内交互式验证"（人工）；(c) §4 字面 placeholder，不写真实 marketplace 命令；(d) 不引用 README.md / 不破 README 现状
- **绿状态验证**：
  - `test -f docs/plugin-install.md` exit 0
  - `grep -qE '^## 1\. 前置要求' docs/plugin-install.md` exit 0
  - `grep -qE 'claude --plugin-dir' docs/plugin-install.md` exit 0
  - `grep -qE 'simx-c5-plugin-validate' docs/plugin-install.md` exit 0
  - `bun x vitest run src/cli/__tests__/plugin-manifest.test.ts` 字面 4 case 全绿（case 4 断言修回 `'simx'`）
  - `bun x vitest run` 末尾字面 `Tests 590 passed (590)` + `Test Files 26 passed (26)`

**重构**

- 无（vitest case 4 个紧凑，单 describe；plugin-install.md 4 段固定模板，无可抽象 helper）

### S3. 更新 `docs/v1.md` 决策日志 +1 行 v0.7 C5 close note

**红（写测试）**

- 文件：`docs/v1.md`
- close note 行字面**不存在** — `grep -qE '^- \*\*2026-05-16\*\* v0\.7 C5 close' docs/v1.md` 字面 exit 1（红）

**绿（实现）**

- 文件：`docs/v1.md`（仅在末尾决策日志段 +1 行，0 改 §2/§4/§5/§6 边界）
- 行字面 pattern 仿 C4 close note：
  ```
  - **2026-05-16** v0.7 C5 close — Claude Code plugin 分发面字面落地：新建 `.claude-plugin/plugin.json` 字面 8 顶层字段（`name='simx'` / `description='AI-native iOS Simulator automation MCP server (27 tools: lifecycle/observe/interaction/compound/system/vlm)'` / `version='0.7.0'` / `author.name='simx contributors'` / `homepage`+`repository` placeholder GitHub URL / `license='MIT'`）+ 1 `mcpServers.simx` entry（`command='bun'` + `args=[${CLAUDE_PLUGIN_ROOT}/src/cli/index.ts, mcp]`，与既有 `src/cli/index.ts:270-285` mcpCommand 接口对齐 / 0 改 src/cli/index.ts / 0 改 27 ToolDef / 0 改 既有 6 MCP smoke）；新建 `docs/plugin-install.md` 4 段（前置 / 本地 dev `claude --plugin-dir` / 验证 / marketplace v1.0+ 占位）；新建 `scripts/simx-c5-plugin-validate.sh` ~80-100 行 / 11 字段 JSON gate（mirror v0.7 C3 `simx-c3-ci-matrix-validate.sh` 14 字段 pattern：manifest_exists / valid_json / has_name / has_description / has_version / has_mcp_servers / mcp_command_bun / mcp_args_has_cli / mcp_args_has_mcp_sub / mcp_args_uses_plugin_root / all_ok）字面 `all_ok=ok` + exit 0；新建 `src/cli/__tests__/plugin-manifest.test.ts` 4 case（manifest 字面+合法 JSON / mcpServers.simx 内嵌 bun+`${CLAUDE_PLUGIN_ROOT}` args / description '27 tools' 子串 / 三向 name 同步 plugin↔package↔serverInfo）。TS baseline 586 → **590**（+4 plugin-manifest）/ 25 → **26 test files**（+ plugin-manifest.test.ts）/ 102 swift 不变 / 27 MCP tool 不变 / typecheck 0 error / 6 既有 MCP smoke 0 退化 / 18 既有 c[1-4] / c5-mcp-compound-system / c5-run-flags / c5-step-png / c6-* / c8-* smoke 中 18 全 0 改（C5 仅 +1 新 c5-plugin-validate）/ 0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 既有 src/{core,sdk,driver,mcp,sim,cli/index.ts,cli/commands/*} / 0 dep / 0 既有 27 ToolDef / 0 既有 25 test file 中 25 文件改 / 0 ci.yml 改 / 0 v1.md 边界改 / 0 roadmap.md 改 / 0 cold v0.7 改 / 0 examples 改 / 0 README.md 改 / 0 package.json 改 / 0 .mcp.json（决策 2.A 字面单文件嵌入策略）。**cold plan §C5 字面解读偏差记**：cold plan 字面 "通过 `claude /install simx` 装上" + `claude --plugin simx /tools` + `grep -c 'simx::'` 期望 18 = 3 处假设字面与现实 Claude Code plugin 形态不符 — 现实安装命令 `claude --plugin-dir <abs path>` + 验证经 session 内 `/mcp`（交互式 stdout 非 pipe-able）+ `simx::` namespace 未实装（grep 实测 0 hit）+ tool count 现 27（v0.6 C6 已超 v1.md §D 字面 18 baseline）；C5 落地实物 = (a) `.claude-plugin/plugin.json` 字面契合 plugin reference 最新文档（`code.claude.com/docs/en/plugins-reference.md`）+ (b) 结构契约 gate 11 字段 + (c) vitest 4 case 测语义不变量 + (d) `docs/plugin-install.md` 写本地 dev 字面命令 + 占位 marketplace v1.0+ 段；cold plan v0.7.md 字面**0 改**（决策 15.B 字面 —— cold plan 留作历史/假设，决策日志解释偏差，C6 v1 acceptance + C7 README v1.0 rewrite 时一次性字面修正）。**plugin.json `version=0.7.0` 与 package.json `version=0.0.0` 解耦字面记**（决策 3.C.ii 字面）：plugin 是对外可见分发面 / package 是内部 dev workspace；v1.0 release 时两者同步 `1.0.0`。**`license='MIT'` 与 CLAUDE.md §8.7 "闭源候选" 字面记冲突解读**（决策 3.F.ii 字面）：plugin.json `license` 是 plugin 分发声明、不是仓库 license；v1.0 release 时若 simx 仓库选 proprietary，plugin.json 字段 omit 或改 "UNLICENSED"，C5 占位 MIT。**git 工作树缺席偏差延续记**：lab15-autofix 仍非 git，homepage/repository 字段填 `https://github.com/anthropic-experimental/simx` 占位 URL，v1.0 release 时人工字符串替换。**`simx::` namespace 未实装字面记**：现 27 ToolDef 0 namespace 前缀；引入 namespace 涉及 wire shape break 7 lifecycle + 4 observe + 7 interaction + 3 compound + 4 system + 1 vlm + 1 ping 同时改 → 推 v1.1+ 范围。**C6 推**：v1 acceptance script 聚合（plugin install + v1.md 7 success criteria gates）；**C7 推**：README v1.0 rewrite（plugin install link 集成 + GitHub repo 字面替换 + 17/18 5-arg 真路径实装窗口提示 + branch protection / required status check 配置说明）。
  ```
- 关键点：(a) 行字面唯一 + 在决策日志段末尾追加；(b) close note 仿 C4 字面长 pattern；(c) 决策日志按时间倒序还是顺序？实测 v1.md 末尾决策日志按**时间顺序追加**（C3 close → C4 close → C5 close）
- **绿状态验证**：
  - `grep -qE '^- \*\*2026-05-16\*\* v0\.7 C5 close' docs/v1.md` exit 0
  - `grep -cE '^- \*\*2026-05-16\*\* v0\.7 C5 close' docs/v1.md` 字面 `1`（唯一行）
  - `wc -l docs/v1.md` 字面 `176`（C4 后 175 → +1 行 = 176；决策 18.A 字面 —— close note 单行追加，不引入 blank line）

**重构**

- 无（决策日志单行追加，无可重构）

## Checkpoint C5 验收

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# (a) plugin.json 字面存在 + 字段集合
test -f .claude-plugin/plugin.json
jq -e '.name == "simx"' .claude-plugin/plugin.json > /dev/null
jq -e '.description | type == "string" and (. | length >= 20) and contains("27 tools")' .claude-plugin/plugin.json > /dev/null
jq -e '.version | test("^[0-9]+\\.[0-9]+\\.[0-9]+$")' .claude-plugin/plugin.json > /dev/null
jq -e '.version == "0.7.0"' .claude-plugin/plugin.json > /dev/null
jq -e '.author.name == "simx contributors"' .claude-plugin/plugin.json > /dev/null
jq -e '.homepage | type == "string" and startswith("https://")' .claude-plugin/plugin.json > /dev/null
jq -e '.repository | type == "string" and startswith("https://")' .claude-plugin/plugin.json > /dev/null
jq -e '.license == "MIT"' .claude-plugin/plugin.json > /dev/null

# (b) mcpServers.simx 内嵌正确
jq -e '.mcpServers.simx | type == "object"' .claude-plugin/plugin.json > /dev/null
jq -e '.mcpServers.simx.command == "bun"' .claude-plugin/plugin.json > /dev/null
jq -e '.mcpServers.simx.args | length == 2' .claude-plugin/plugin.json > /dev/null
jq -e '.mcpServers.simx.args[0] | contains("${CLAUDE_PLUGIN_ROOT}")' .claude-plugin/plugin.json > /dev/null
jq -e '.mcpServers.simx.args[0] | contains("src/cli/index.ts")' .claude-plugin/plugin.json > /dev/null
jq -e '.mcpServers.simx.args[1] == "mcp"' .claude-plugin/plugin.json > /dev/null

# (c) plugin-install.md 4 段字面
test -f docs/plugin-install.md
grep -qE '^## 1\. 前置要求' docs/plugin-install.md
grep -qE '^## 2\. 本地 dev 安装' docs/plugin-install.md
grep -qE '^## 3\. 验证' docs/plugin-install.md
grep -qE '^## 4\.' docs/plugin-install.md
grep -qE 'claude --plugin-dir' docs/plugin-install.md
grep -qE 'simx-c5-plugin-validate' docs/plugin-install.md

# (d) 结构契约 validator 字面 11 字段 all_ok
test -f scripts/simx-c5-plugin-validate.sh
test -x scripts/simx-c5-plugin-validate.sh
bash scripts/simx-c5-plugin-validate.sh > /tmp/c5-plugin-validate.json
jq -e '.all_ok == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.manifest_exists == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.valid_json == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.has_name == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.has_description == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.has_version == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.has_mcp_servers == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.mcp_command_bun == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.mcp_args_has_cli == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.mcp_args_has_mcp_sub == "ok"' /tmp/c5-plugin-validate.json > /dev/null
jq -e '.mcp_args_uses_plugin_root == "ok"' /tmp/c5-plugin-validate.json > /dev/null
test "$(jq -r 'keys | length' /tmp/c5-plugin-validate.json)" = '12'  # 11 字段 + exit_code

# (e) vitest 590 + 26 文件 字面
test -f src/cli/__tests__/plugin-manifest.test.ts
bun x vitest run src/cli/__tests__/plugin-manifest.test.ts 2>&1 | grep -qE 'Tests +4 passed'
bun x vitest run 2>&1 | grep -qE 'Tests +590 passed'
bun x vitest run 2>&1 | grep -qE 'Test Files +26 passed'

# (f) typecheck 0 error
bun run typecheck > /dev/null

# (g) 既有 MCP / doctor smoke 不退化
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.tools_list_count == "ok"' > /dev/null
bash scripts/simx-c4-doctor-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c4-doctor-smoke.sh 2>/dev/null | jq -e '.has_compatibility_field == "ok"' > /dev/null

# (h) v1.md 决策日志 +1 行 v0.7 C5 close
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C5 close' docs/v1.md
test "$(grep -cE '^- \*\*2026-05-16\*\* v0\.7 C5 close' docs/v1.md)" = '1'

# (i) 不变量退化检查
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'        # 0 dep 增
test "$(find src -name '*.test.ts' -not -path '*/node_modules/*' | wc -l | tr -d ' ')" = '26'  # 25 + 1
! grep -qE 'simx::' src/mcp/tools.ts                                       # namespace 未引入
! test -f .mcp.json                                                        # 单文件策略
test "$(wc -l < .claude-plugin/plugin.json | tr -d ' ')" -le 30           # JSON 紧凑
test "$(wc -l < scripts/simx-c5-plugin-validate.sh | tr -d ' ')" -le 110  # validator ≤ 110 行
test "$(wc -l < docs/plugin-install.md | tr -d ' ')" -le 90               # install doc ≤ 90 行
test "$(wc -l < src/cli/__tests__/plugin-manifest.test.ts | tr -d ' ')" -le 100  # vitest ≤ 100 行

# (j) 既有 17 c[1-8] smoke 中 17 全 0 改（C5 仅 +1 新 c5-plugin-validate）
test -f scripts/simx-c1-mcp-smoke.sh
test -f scripts/simx-c2-mcp-lifecycle-smoke.sh
test -f scripts/simx-c3-mcp-observe-smoke.sh
test -f scripts/simx-c4-mcp-interaction-smoke.sh
test -f scripts/simx-c5-mcp-compound-system-smoke.sh
test -f scripts/simx-c6-mcp-explain-screen-smoke.sh
test -f scripts/simx-c1-longrun-100-smoke.sh
test -f scripts/simx-c2-ci-workflow-validate.sh
test -f scripts/simx-c3-ci-matrix-validate.sh
test -f scripts/simx-c4-doctor-smoke.sh

# (k) 既有 src/cli/index.ts 0 改 + 27 ToolDef 0 改
grep -qE "name: 'simx'" src/cli/index.ts
grep -qE "name: 'mcp'" src/cli/index.ts
test "$(grep -cE "name: 'mcp'" src/cli/index.ts)" -ge 1

echo "C5 acceptance: PASS"
```

期望：上述命令全 exit 0 / 末行字面 `C5 acceptance: PASS`；任一条 fail → C5 未通过、不进入归档。

## 完成后动作

1. **归档**：`mv docs/plan-hot.md docs/plan-history/v0.7-c5-hot.md`
2. **下段热化触发**：当且仅当用户/上层 agent 字面说 "开始 v0.7 C6" 或同义时，调 sub-agent 生成新 `docs/plan-hot.md`（C6 范围 = v1.0 自动化验收脚本，跑遍 v1.md "Success Criteria" 7 条 + 修正 cold plan §出口验收行 `claude /install simx` / `claude --plugin simx /tools | grep -c 'simx::'` 字面为正确形态 `claude --plugin-dir` + session 内 `/mcp` 字面 + 27 tool baseline；起点 baseline = 590 TS / 26 test file / 0 dep / 27 MCP tool / 27 doctor compat supported / 9-field c4 doctor smoke / 11-field c5 plugin-validate / `.claude-plugin/plugin.json` 字面契约 + `docs/plugin-install.md` 4 段 + 1 vitest plugin-manifest.test.ts 4 case）
3. **下段热化前置检字面**：`bash scripts/simx-c5-plugin-validate.sh | jq -e '.all_ok == "ok"'` exit 0 + `bun x vitest run | grep 'Tests +590 passed'` + `grep '^- \*\*2026-05-16\*\* v0\.7 C5 close' docs/v1.md` exit 0

## 与 cold plan 偏差汇总（**必读 - 不要隐瞒**）

| 维度 | cold plan v0.7.md 字面 | C5 hot plan 实际落地 | 决策记号 |
|---|---|---|---|
| 安装命令 | `claude /install simx` | `claude --plugin-dir /abs/path` 本地 dev + marketplace 推 v1.0+ | 1.B |
| 验证命令 | `claude --plugin simx /tools \| grep -c 'simx::'` 期望 18 | (a) 结构契约 `simx-c5-plugin-validate.sh` 11 字段 + (b) vitest 4 case + (c) 人工 session 内 `/mcp` 验 27 tool（写在 plugin-install.md §3） | 1.D |
| manifest 路径 | `claude-plugin.json` 项目根 | `.claude-plugin/plugin.json` 子目录形态 | 1.A |
| Tool count baseline | 18 (v1.md §D 字面) | 27（v0.6 C6 已超 v1.md baseline） | 1.D.ii |
| `simx::` namespace | grep -c 'simx::' 期望 18 | 现 27 ToolDef 0 namespace 前缀；引入推 v1.1+ | 1.D.i |
| TDD 要点行 | "用 claude CLI 加载 manifest 后 tools/list 返回 18 个 tool" | 改为结构契约 gate + vitest 不变量测；交互式 claude CLI 验证写人工步骤 | 1.E |

**cold plan v0.7.md 文本 0 改**（决策 15.B 字面）—— 决策日志解释偏差，cold plan 留作历史/假设。C6 v1 acceptance / C7 README v1.0 rewrite 时一次性字面修正。
