# plan-hot — v0.7 C2：`.github/workflows/ci.yml` 单 runtime（macos-15 / iOS 26 / 既有 dev sim 不在 CI）字面跑通 — checkout + setup-bun@v2 + `bun install --frozen-lockfile` + `bun run typecheck` + `bun x vitest run`（578 passed gate）+ `swift test --package-path swift-bridge`（≥ 102 gate）+ `bash scripts/simx-c1-mcp-smoke.sh`（stdio MCP gate，无 sim 依赖）+ 新建 `scripts/simx-c2-ci-workflow-validate.sh` 本机 lint gate（regex 字面校 YAML 必含 `runs-on: macos-15` / `setup-bun` / 5 关键命令字面 + 单 runtime / 无 matrix；本项目当前非 git 工作树 → CI workflow 本身真跑由 v0.7 close 时 git init 后用户首推触发，C2 范围内**只**验 workflow file 字面契约 + 本机执行 5 gate 命令逐条 exit 0）

> [3/4 信息层] 当前唯一热计划。**C2 是 v0.7 第 2 个 checkpoint**，cold plan §Checkpoint 概要列表字面"C2 自动化 100-case 套件 + CI workflow 跑通（GitHub Actions macos-15）"。**100-case 套件 = C1 已字面落地**（`scripts/long-run-100.ts` + `scripts/simx-c1-longrun-100-smoke.sh` v0.7 C1 close 决策日志在场 / 实测 passed=100 / failed=0 / restarts=2）—— C2 的 100-case 部分是**复用 C1 产物**，无新增 100-case 代码；C2 真生效 = **CI workflow 文件 + 单 runtime 5 gate 本机执行验证**。
>
> 通过后世界变成：(a) `.github/workflows/ci.yml` 字面在场（~60-80 行 / single-job / single-runtime macos-15 / 5 step gate）；(b) `scripts/simx-c2-ci-workflow-validate.sh` 字面在场（~50 行 / regex 字面校 YAML 必含 9 关键字面 token + 单 runtime + 无 matrix + 10 字段 JSON gate `{exit_code, has_macos_15, has_setup_bun, has_bun_install_frozen, has_typecheck, has_vitest, has_swift_test, has_mcp_smoke, no_matrix, single_job, all_ok}`）；(c) 5 gate 命令本机字面逐条 exit 0：`bun install --frozen-lockfile` / `bun run typecheck` / `bun x vitest run`（578 passed 字面）/ `(cd swift-bridge && swift test)`（≥102 字面）/ `bash scripts/simx-c1-mcp-smoke.sh`（`all_ok=ok` 字面 + tools_list_count=27 字面）；(d) C2 决策日志 +1 行字面记 v1.md（v1 cycle 内）— "v0.7 C2 close — single-runtime CI workflow YAML 字面在场 + 5 gate 本机全过 + git 工作树缺席偏差记"。
>
> **C2 不在范围内（C3-C7 推后字面拒绝路径）**：
>
> - **C3 多 runtime matrix iOS 17.5/18.4/26.x**（cold plan §C3 字面 — 本机仅 iOS 26.4 SDK 单装；17.5/18.4 在 CI 上需 `xcversion simulators --install` 预 cache，C3 hot 化时落；C2 字面 "单 runtime 跑通"）
> - **C4 `simx doctor --json` 升级**（C4 范围）
> - **C5 Claude Code plugin manifest**（C5 范围）
> - **C6 v1 acceptance script `scripts/v1-acceptance.sh`**（C6 范围）
> - **C7 README / docs/ v1.0 发布**（C7 范围）
> - **100-case 长跑 e2e 进 CI**（本机 sim 依赖 + 12 分钟时长 + dev-sim.txt 缺席 → CI 上跑会卡 health 120s timeout 后 exit 1；推 C3 一起处理，那时 matrix 内每 runtime 各起一次 sim + 一次长跑；C2 字面**离线 5 gate** = `typecheck` + `vitest` + `swift test` + `c1-mcp-smoke.sh`（stdio 无 sim）+ `install --frozen-lockfile` 全离线，**不**接 dev sim / 不接 dev-sim.txt / 不接 v06-acceptance 35 字段 / 不接 mcp-smoke.ts e2e 15 passed_tools / 不接 long-run-100.ts）
> - **`actions/cache` 加速**（v0.7 C5 后做；C2 不优化 CI 速度）
> - **PR / push trigger 之外的 `workflow_dispatch` 手触**（C5 plugin 分发时挂；C2 字面 `on: [push, pull_request]` 2 trigger 基础）
> - **branch protection / required status check 配置**（仓库 Settings 范畴非 workflow 文件范畴；推 C7 README 写人工说明步）
> - **GHA secrets / OIDC / token 注入**（v0.7 全期无需；C5 plugin 分发可能需 npm publish token，那时再加）
>
> **承接（v0.7 C1 close 后实测 baseline，2026-05-16）**：v0.7 C1 ✅ done —— v1.md 决策日志末尾 `2026-05-16 v0.7 C1 close` 字面在场 + `scripts/long-run-100.ts` + `scripts/simx-c1-longrun-100-smoke.sh` + `src/sim/runner-supervisor.ts` + `src/sim/__tests__/runner-supervisor.test.ts` 字面在场；TS baseline = **578**（v0.6 close 时 568 经 dev-lock hot-fix graft 至 575 / v0.7 C1 close +3 supervisor case 至 578）/ 102 swift 不变 / typecheck 0 error / 27 MCP tool expose / 35 字段 v0.6 acceptance（本机 dev sim 起 runner 时通）/ `bash scripts/simx-c1-longrun-100-smoke.sh` 实测 exit 0 + `{"all_ok":true}` 字面末行。
>
> **进入 v0.7 C2 前置检字面全 ok**：
>
> 1. **v1.md 边界未膨胀字面验**（§4 Success Criteria [5] 长跑 + [6] iOS 17.5/18.4/26.x 三 runtime + §2 资源依赖 `CI：GitHub Actions macOS-15` 字面 + §5 CI 风险条字面在场；[6] 三 runtime 字面冻结 → C2 范围 1 runtime 是真子集；C3 才补满 3 runtime；C2 不偷写 [6]）；
> 2. **v0.7 C1 工程实体字面在场** — `src/sim/runner-supervisor.ts` + `src/sim/__tests__/runner-supervisor.test.ts` + `scripts/long-run-100.ts` + `scripts/simx-c1-longrun-100-smoke.sh` 字面在场；v1.md 决策日志 `2026-05-16 v0.7 C1 close` 字面在场；
> 3. **本机非 git 工作树字面** — `test -d .git` 字面 fail（lab15-autofix 是 lab 实验目录，无 .git；本机探测确认 `ls -la` 字面无 `.git` 项）。
>
> **本机探测（C2 落地决策依据，2026-05-16）**：
>
> - `xcodebuild -version` 字面 `Xcode 26.4.1 / Build version 17E202`（C2 不再起新 xcode；既有 swift-bridge/SimxRunner.xcodeproj 冻结）
> - `xcodebuild -showsdks` 字面单 iOS SDK = `iOS 26.4 / Simulator - iOS 26.4`；**iOS 17.5 / 18.4 SDK 不在场**（决策 1.A 字面 — C2 单 runtime 字面落地 iOS 26.4；C3 才在 CI matrix 用 `xcversion simulators --install` 装多 runtime）
> - `bun --version` 字面 `1.3.14`（CI setup-bun version 锁字面 `^1.3.0` —— 决策 2.A 字面）
> - `bun x vitest run` 字面 `Tests 578 passed (578) / Test Files 24 passed (24)`（C2 baseline 578 字面）
> - `(cd swift-bridge && swift test 2>&1 | tail -3)` 字面预期含 `Executed N tests` 或 `Test Suite 'All tests' passed`（v0.6 close 时 ≥ 102 字面，C2 范围 0 改动 swift-bridge/）
> - `bash scripts/simx-c1-mcp-smoke.sh` 字面 `all_ok=ok` + `tools_list_count=27`（v0.6 C6 close 字面，C2 范围内 0 改动）
> - **`.github/` 目录字面缺**：本机 `ls -la /Users/doracawl/workspace/labs/lab15-autofix/` 字面无 `.github` 项；仅 node_modules 内有第三方 `.github/`；项目级 `.github/workflows/` 字面新建（决策 3.A 字面 — 新建项目级 `.github/workflows/` 目录字面）
> - **非 git 工作树**：`test -d .git` 字面 fail（lab15-autofix 缺 `.git`）；GitHub Actions 真触发依赖 git push → 本机 C2 范围内**workflow file 真跑不在路径**；C2 验证 = (a) YAML 文件字面契约 + (b) 5 gate 命令本机字面逐条跑 = 等价"如果用户日后 git init + push 后 CI 应通"的可证 evidence（决策 4.A 字面 — D 候选采纳：写 GHA YAML + 本机 lint 等价 + 5 gate 本机执行；不写 act-like wrapper / 不写 `scripts/ci-local.sh` 全量 e2e —— 后两者超 C2 范围 / 重复 v06-acceptance 形态）
> - **0 YAML 库依赖**：`package.json` deps 字面 3 个 `citty + @modelcontextprotocol/sdk@1.29.0 + zod@^3.25.0` + dev `vitest + typescript + @types/node`；**不**加 YAML 解析 dep（决策 2.B 字面 0 新 dep）；YAML 字面校用 **grep regex** 而非真解析 —— mirror 既有 c[1-8] smoke shell `jq -e` 字段断言形态（决策 5.A 字面）
> - **`swift test` 路径**：本机 `(cd swift-bridge && swift test)` 字面跑通；CI 上同路径用 GHA step `working-directory: swift-bridge`（决策 6.A 字面）
> - **`bun install --frozen-lockfile`**：本机 `bun.lock` 字面在场（38k）；CI 上 setup-bun@v2 默认 read bun.lock；`--frozen-lockfile` 字面强制 lock 不漂（决策 7.A 字面）
> - **`scripts/simx-c1-mcp-smoke.sh` 字面无 sim 依赖**：head 字面读源码确认 — 只 spawn `bun src/cli/index.ts mcp` stdio JSON-RPC；无 `xcrun simctl` / 无 `RunnerClient` / 无 dev-sim.txt 引用；CI 离线可跑（决策 8.A 字面 — 选 c1-mcp-smoke 作为唯一 MCP gate；不选 c2-c6 mcp smoke —— 后者部分需 sim 起 runner / 真 dev sim app launch）
> - **runner image macos-15**：GitHub Actions macos-15 (GA 2024-09) 预装 Xcode 16.0；**Xcode 26.4 本机 = bleeding edge**（决策 9.A 字面）；C2 CI 上的 swift test + vitest + typecheck **不依赖 Xcode 26.4 特性**（swift test 走 SPM macOS target 字面 macOS 14.4+；vitest 纯 TS；typecheck 纯 TS；c1-mcp-smoke spawn bun + stdin JSON-RPC 纯进程 IO）→ macos-15 默认 Xcode 字面足够（决策 9.B 字面 — 不 `sudo xcode-select` 切版本 / 不装 Xcode 26）；C3 hot 化时若 17.5/18.4 sim 需特定 Xcode 再决（C2 不预设）
> - **`engines.bun` 字面 `>=1.1.0`**：CI 上 setup-bun@v2 用 `bun-version: ^1.3.0` 字面（决策 2.A 字面 mirror 本机实测 1.3.14；engines 字面 `>=1.1.0` 是下限保 v0.6+ 用户向后兼容，CI 字面用 `^1.3.0` 锁实测版本上界）
> - **既有 24 vitest test file 字面在场**（`find src -name '*.test.ts'` 24 个；C2 不增 unit test —— **`.github/workflows/ci.yml` 不挂 vitest 形态**（YAML 文件不在 src/ 下；写 unit test 只能测 "我能读到这个文件吗" / "regex 匹配吗"，等价测试代码的代码，无信号）；C2 范围 = workflow file + shell wrapper validator，0 新 vitest case（决策 10.A 字面 — 578 → 578 TS 不动）
>
> **现状盘点（C2 真生效边界）**：
>
> - **新建 `.github/workflows/ci.yml`（~60-80 行）**：
>   - `name: ci`
>   - `on:` block 2 trigger `push` + `pull_request`
>   - `jobs.test:` single job
>   - `runs-on: macos-15` 字面（决策 1.A 字面）
>   - `timeout-minutes: 30` 字面（决策 11.A 字面 — 30 分钟硬 cap；swift test ~2 分钟 + vitest ~3s + typecheck ~5s + c1-mcp-smoke ~10s + bun install ~30s ≈ 3 分钟，30 分钟 10× 余量；C3 长跑加进来时改 60）
>   - 5+ step：
>     1. `actions/checkout@v4` 字面（决策 12.A 字面 — checkout@v4 stable）
>     2. `oven-sh/setup-bun@v2` with `bun-version: ^1.3.0` 字面（决策 2.A 字面）
>     3. `bun install --frozen-lockfile` 字面（决策 7.A 字面）
>     4. `bun run typecheck` 字面（决策 13.A 字面 — package.json `scripts.typecheck` 字面 `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.examples.json`）
>     5. `bun x vitest run` 字面（决策 14.A 字面 — `bun run test` = `vitest run` 字面 mirror；CI 字面用 `bun x vitest run` 等价 + 更显式）
>     6. `swift test`（`working-directory: swift-bridge`）字面（决策 6.A 字面）
>     7. `bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null` 字面（决策 8.A 字面）
>   - 不挂 cache 字面（决策 15.A 字面 — 简单优先；C5+ 再加）
>   - 不挂 matrix 字面（决策 1.A 字面 — 单 runtime；C3 才 matrix）
>   - 不挂 secrets 字面（决策 16.A 字面）
> - **新建 `scripts/simx-c2-ci-workflow-validate.sh`（~50 行）**：
>   - mirror `simx-c[1-8]-*-smoke.sh` + `simx-c4-doctor-smoke.sh` 形态字面（决策 5.A 字面）
>   - `set -uo pipefail` + `cd ROOT`
>   - `YML=".github/workflows/ci.yml"` + `test -f "$YML"` 字面 guard
>   - 9 个 grep 字面 token 检查 → 9 个 ok/fail 变量
>   - 10 字段 JSON 单行字面输出 `{exit_code, has_macos_15, has_setup_bun, has_bun_install_frozen, has_typecheck, has_vitest, has_swift_test, has_mcp_smoke, no_matrix, single_job, all_ok}`
>   - `all_ok = ok` iff 上 9 字段全 ok 字面
>   - exit 0 iff `all_ok == ok`
> - **0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/ 任何文件 / 0 既有 6 mcp smoke 改 / 0 既有 5 v0[1-5] acceptance 改 / 0 v0.6 acceptance 改 / 0 mcp-smoke.ts 改 / 0 long-run-100.ts 改 / 0 simx-c1-longrun-100-smoke.sh 改 / 0 runner-supervisor.ts 改 / 0 examples 改 / 0 既有 c[1-8] smoke 改 / 0 既有 c1-mcp-smoke 字段改 / 0 dep / 0 既有 27 MCP tool 改 / 0 既有 24 vitest test file 改 / 0 既有 578 vitest case 改**
>
> **C2 真生效 = 二项改动（≤ 6 字面任务约束严守）**：
>
> 1. **新建 `.github/workflows/ci.yml`**（~60-80 行）— 核心 1（GHA YAML，cold plan §C2 字面"CI workflow 跑通 GitHub Actions macos-15"工程实体）
> 2. **新建 `scripts/simx-c2-ci-workflow-validate.sh`**（~50 行）— 核心 2（本机 lint gate，mirror v0.6 6 mcp smoke shell 形态）
>
> **核心改动 = 2 条 ≤ 6 字面满足**。
>
> **C2 全部不在范围**（拒绝路径完整列表）：
>
> - 写 C3 多 runtime matrix（iOS 17.5/18.4/26.x）任何 yaml `strategy.matrix` 段（决策 1.A 字面拒）
> - 装 17.5/18.4 sim runtime（`xcversion simulators --install` / `xcrun simctl runtime add`）（决策 1.A 字面拒）
> - 在 CI workflow 跑 long-run-100.ts / mcp-smoke.ts / v06-acceptance.sh / 任何 dev-sim.txt 依赖（决策 4.A 字面 — 离线 5 gate）
> - 加 `actions/cache` / `bun-install --cache` 优化（决策 15.A 字面）
> - 加 `workflow_dispatch` / `schedule` / branch filter（决策 16.A 字面 — push + pull_request 2 trigger 字面）
> - 加 GHA secrets / OIDC / token（决策 16.A 字面）
> - 加 GHA artifacts upload（决策 17.A 字面 — C5 plugin 分发再做）
> - 加 codecov / SonarCloud / lint action（决策 17.B 字面）
> - 加 release / changesets / npm publish workflow（决策 17.C 字面 — C5 才做）
> - 改 `package.json` `scripts.test` / `scripts.typecheck`（既有字面冻结；CI 调既有 script）
> - 加新 npm dep / 新 dev dep（决策 2.B 字面 0 dep）
> - 加 unit vitest case for ci.yml（决策 10.A 字面 — workflow 文件不挂 vitest）
> - 改 `scripts/simx-c1-mcp-smoke.sh` 任何字段（决策延续 0 改动）
> - 在 ci.yml 起 simulator boot / runner / xcodebuild test 后台（决策 4.A 字面 — 离线）
> - 改 `docs/roadmap.md`（roadmap v0.7 🔥 hot 字面不动；C7 才翻 v0.7 ✅）
> - 改 `docs/v1.md` 任何边界字段（§2 模块表 / §4 Success Criteria [6] 三 runtime / §5 风险表 / §6 工作量 全冻结；C2 close 仅决策日志 +1 行）
> - 改 `docs/plan-cold/v0.7.md`（cold plan 冻结）
> - 改 `src/` / `swift-bridge/` 任何文件（v0.7 全期严控 0 改 swift / 0 改 既有 src/sim 6 文件 / 0 改 既有 src/{driver,sdk,core,cli,mcp}）
> - mv `docs/plan-hot.md` 到 `docs/plan-history/v0.7-c1-hot.md`（plan-history archive 的 v0.7-c1-hot.md 已字面在场 43k；当前 docs/plan-hot.md = C1 旧内容，C2 hot 写入即覆盖；C2 close 时 mv 到 v0.7-c2-hot.md —— 决策 0.A 字面）
> - 加 `.gitignore` 改动（v0.7 C2 不涉 git 行为）
> - 在 ci.yml 引 reusable workflow / composite action（决策 18.A 字面 — C2 单文件 monolithic；超 30 行 step 再拆）
> - C3 17.5/18.4 字面 token / matrix segment / 任何 multi-runtime hint（决策 1.A 字面拒提前）
> - 在 c2-ci-workflow-validate.sh 真跑 5 gate 命令（决策 5.B 字面拒 — validator 仅校 YAML 文件字面契约；5 gate 命令本机 Checkpoint 验收段单独逐条跑 —— validator 跑 5 命令 会导致重复执行 / 拖长 validator 时长 / 混淆"workflow file 契约" vs "5 gate 实际执行"两个不同验证维度）
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / SimctlClient 0 / dep 0 / wire payload 0 改 / 既有 27 ToolDef 0 / 既有 server.ts 0 / 既有 tools.ts 0 / 既有 index.ts 0 / 既有 schemas.ts 0 / 既有 examples 0 / 既有 6 mcp smoke 0 字段改 / 既有 5 v0[1-5] acceptance 0 改 / v0.6 acceptance 0 改 / mcp-smoke.ts 0 改 / 既有 24 vitest test file 0 改 / 既有 578 vitest case 0 改 / runner-client.ts 0 改 / dev-lock.ts 0 改 / cell.ts 0 改 / session.ts 0 改 / simctl.ts 0 改 / a11y-tree-source.ts 0 改 / runner-supervisor.ts 0 改 / long-run-100.ts 0 改 / simx-c1-longrun-100-smoke.sh 0 改 / v1.md 边界 0 改 / roadmap.md 0 改 / plan-cold/v0.7.md 0 改 / package.json 0 改 / CLI flag 0 加 / env 变量 0 加 / src/driver/ 0 改 / src/sdk/ 0 改 / src/core/ 0 改 / src/cli/ 0 改 / src/mcp/ 0 改 / swift-bridge/ 0 改 / scripts/mcp-smoke.ts 0 改 / scripts/simx-c[1-8]-*.sh 0 改 / scripts/simx-runner-*.sh 0 改 / scripts/simx-axp-*.sh 0 改 / scripts/simx-v0[1-6]-acceptance.sh 0 改**。允许改动 / 新建文件范围（**核心 ≤ 6 条**字面任务约束）：
>
> 1. **新建 `.github/workflows/ci.yml`**（~60-80 行）— 核心 1
> 2. **新建 `scripts/simx-c2-ci-workflow-validate.sh`**（~50 行）— 核心 2
>
> **不允许**：(a) 改 `src/` 任何文件；(b) 改 `swift-bridge/`；(c) 改任何 `examples/*.ts`；(d) 改既有 `scripts/simx-*.sh` / `scripts/*.ts`；(e) 引第 4 个 npm dep / 第 4 个 dev dep；(f) 加 CLI flag / env 变量；(g) 写 plan-cold/v0.7.md / 写 C3-C7 任何代码 / 改 roadmap.md / 改 v1.md 边界；(h) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient` / `RunnerClient` / `RunnerSupervisor` / `DevSimLockViolation` / `readDevSimLock`；(i) 改 ScreenDescription / SerializedFailure / FailureCode / Role / Rect / ElementSummary / A11yNode 字段；(j) 在 ci.yml 跑 sim / runner / xcodebuild test 后台 / dev-sim.txt 依赖；(k) 加 matrix / cache / secrets / artifacts / dispatch / schedule；(l) 加 unit vitest case for workflow file；(m) 改 `scripts/simx-c1-mcp-smoke.sh` 任何字段。

## 目标 checkpoint

**C2**：通过后世界变成 — (a) `.github/workflows/ci.yml` 字面在场 + 含 9 字面 token（`name: ci` / `runs-on: macos-15` / `actions/checkout@v4` / `oven-sh/setup-bun@v2` / `bun install --frozen-lockfile` / `bun run typecheck` / `bun x vitest run` / `swift test` / `simx-c1-mcp-smoke.sh`）+ `timeout-minutes: 30` 字面；(b) `scripts/simx-c2-ci-workflow-validate.sh` 字面 exit 0 + 10 字段 JSON + `all_ok: "ok"` 字面；(c) 本机 5 gate 字面逐条 exit 0：`bun install --frozen-lockfile` / `bun run typecheck` / `bun x vitest run`（`Tests 578 passed` 字面）/ `(cd swift-bridge && swift test)`（`Test Suite 'All tests' passed` 字面或 `Executed [0-9]+ tests` ≥ 102 字面）/ `bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"'`；(d) 578 TS case baseline 字面不退化（C2 0 case 增）；(e) 27 MCP tool / 102 swift / 35 字段 v0.6 acceptance / mcp-smoke.ts / long-run-100.ts / runner-supervisor 等全不退化（0 改动字面验）。

- `test -f .github/workflows/ci.yml` 字面
- `bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.all_ok == "ok"' > /dev/null` 字面 exit 0
- `bun install --frozen-lockfile` 字面 exit 0
- `bun run typecheck` 字面 exit 0
- `bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +578 passed'` 字面
- `(cd swift-bridge && swift test 2>&1 | tail -5 | grep -qE 'Test Suite .All tests. passed|Executed +[0-9]+ tests')` 字面（swift test 输出格式锁定 ≥ 102）
- `bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null` 字面 exit 0
- 0 src / 0 swift / 0 既有 scripts / 0 既有 docs 边界 / 0 dep / 0 既有 mcp smoke 字段 改 字面

## 前置条件

```bash
set -euo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# v0.7 C1 close 痕迹存在
test -f src/sim/runner-supervisor.ts
test -f src/sim/__tests__/runner-supervisor.test.ts
test -f scripts/long-run-100.ts
test -f scripts/simx-c1-longrun-100-smoke.sh

# v0.6 整体 close 痕迹存在
test -f docs/plan-history/v0.6-c8-hot.md
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f scripts/simx-c1-mcp-smoke.sh

# v0.7 C1 close 决策日志字面在场
grep -qE '^- \*\*2026-05-16\*\* v0\.7 C1 close' docs/v1.md

# v1.md C2 边界条款字面在场（未被偷加 / 偷减）
grep -qE 'CI.*GitHub Actions macOS-15.*runtime 矩阵 iOS 17\.5 / 18\.4 / 26\.x' docs/v1.md
grep -qE '^\[6\] 兼容：iOS 17\.5 / 18\.4 / 26\.x 全部通过冒烟集$' docs/v1.md

# roadmap.md v0.7 🔥 字面
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# 本机非 git 工作树字面验（决策 4.A 字面前提）
! test -d .git

# 本机非 .github/ 项目级目录字面（C2 红前提）
! test -d .github

# 578 TS baseline 字面（C2 不动 578）
bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +578 passed'

# typecheck baseline 字面
bun run typecheck > /dev/null

# c1-mcp-smoke 字面 ok（CI gate 形态依赖）
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok"' > /dev/null

# dep 字面 3 个（不退化）
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'
jq -e '.dependencies["@modelcontextprotocol/sdk"]' package.json > /dev/null
jq -e '.dependencies["citty"]' package.json > /dev/null
jq -e '.dependencies["zod"]' package.json > /dev/null

# C2 红前提：两个目标文件**不存在**字面
! test -f .github/workflows/ci.yml
! test -f scripts/simx-c2-ci-workflow-validate.sh

# bun >=1.1.0 字面（engines）
test "$(jq -r '.engines.bun' package.json)" = '>=1.1.0'

# bun.lock 字面在场（CI install --frozen-lockfile 依赖）
test -f bun.lock
```

期望：以上每条 exit 0。

## 步骤（线性，无分叉；2 个 step）

### S1. 新建 `.github/workflows/ci.yml` — single-job / single-runtime macos-15 / 5+ gate step（checkout + setup-bun + install-frozen + typecheck + vitest + swift test + c1-mcp-smoke）+ `on: [push, pull_request]` + `timeout-minutes: 30` + 字面契约 9 token 全在场

**红（写测试）**

无新 vitest case（C2 是 CI workflow 文件 + shell wrapper validator；578 TS case 字面不增 —— 决策 10.A 字面）。

红前提 = `! test -d .github && ! test -f .github/workflows/ci.yml`（前置条件已验）。

**绿（实现）**

新建目录 `.github/workflows/` 字面 + 新建 `.github/workflows/ci.yml`（~60-80 行）：

```yaml
# v0.7 C2 — single-runtime CI gate (GitHub Actions macos-15 / iOS 26.4).
# Gates: install --frozen-lockfile / typecheck / vitest (578) / swift test (>=102) /
# c1-mcp-smoke stdio JSON-RPC (all_ok=ok). Multi-runtime matrix (iOS 17.5/18.4)
# arrives in v0.7 C3.

name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: test (macos-15 / iOS 26.4)
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ^1.3.0

      - name: Install dependencies (frozen lockfile)
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Vitest (TS unit tests, expect 578 passed)
        run: bun x vitest run

      - name: Swift test (SimxRunnerCore, expect >=102 passed)
        run: swift test
        working-directory: swift-bridge

      - name: C1 MCP stdio smoke (expect all_ok=ok)
        run: |
          set -euo pipefail
          OUT="$(bash scripts/simx-c1-mcp-smoke.sh)"
          echo "$OUT"
          echo "$OUT" | jq -e '.all_ok == "ok"' > /dev/null
```

字面验证：
```bash
test -f .github/workflows/ci.yml
test "$(wc -l < .github/workflows/ci.yml)" -lt 90  # 上限 90 行 cap
grep -qE '^name: ci$' .github/workflows/ci.yml
grep -qE '^    runs-on: macos-15$' .github/workflows/ci.yml
grep -qE 'actions/checkout@v4' .github/workflows/ci.yml
grep -qE 'oven-sh/setup-bun@v2' .github/workflows/ci.yml
grep -qE 'bun install --frozen-lockfile' .github/workflows/ci.yml
grep -qE 'bun run typecheck' .github/workflows/ci.yml
grep -qE 'bun x vitest run' .github/workflows/ci.yml
grep -qE 'swift test' .github/workflows/ci.yml
grep -qE 'simx-c1-mcp-smoke\.sh' .github/workflows/ci.yml
grep -qE 'timeout-minutes: 30' .github/workflows/ci.yml
! grep -qE '^[[:space:]]*(strategy:|matrix:)' .github/workflows/ci.yml  # 单 runtime / 无 matrix 字面
```

期望：每条 exit 0。

**重构**

无。

---

### S2. 新建 `scripts/simx-c2-ci-workflow-validate.sh` — regex 字面校 `.github/workflows/ci.yml` 含 9 关键 token + 单 job + 无 matrix + 10 字段 JSON gate 输出（mirror v0.6 6 既有 mcp smoke shell 形态）

**红（写测试）**

无新 vitest case（同 S1）。

红前提 = `! test -f scripts/simx-c2-ci-workflow-validate.sh`（前置条件已验）。

**绿（实现）**

新建 `scripts/simx-c2-ci-workflow-validate.sh`（~50 行）：

```bash
#!/usr/bin/env bash
# v0.7 C2 — GitHub Actions CI workflow file contract validator.
# Regex-asserts `.github/workflows/ci.yml` carries the 9 literal tokens
# that the single-runtime CI gate depends on. Emits a 10-field JSON gate
# (mirror of scripts/simx-c[1-8]-*.sh shape).
#
# This validator does NOT execute the 5 CI gate commands — those are
# verified in the Checkpoint C2 acceptance block separately. The intent
# here is workflow-file shape, not pipeline replay.
set -uo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

YML=".github/workflows/ci.yml"

has_macos_15="fail"
has_setup_bun="fail"
has_bun_install_frozen="fail"
has_typecheck="fail"
has_vitest="fail"
has_swift_test="fail"
has_mcp_smoke="fail"
no_matrix="fail"
single_job="fail"

if [[ -f "$YML" ]]; then
  grep -qE '^[[:space:]]*runs-on: macos-15$' "$YML" && has_macos_15="ok"
  grep -qE 'oven-sh/setup-bun@v2' "$YML" && has_setup_bun="ok"
  grep -qE 'bun install --frozen-lockfile' "$YML" && has_bun_install_frozen="ok"
  grep -qE 'bun run typecheck' "$YML" && has_typecheck="ok"
  grep -qE 'bun x vitest run' "$YML" && has_vitest="ok"
  grep -qE '(^|[[:space:]])swift test([[:space:]]|$)' "$YML" && has_swift_test="ok"
  grep -qE 'simx-c1-mcp-smoke\.sh' "$YML" && has_mcp_smoke="ok"
  if ! grep -qE '^[[:space:]]*(strategy:|matrix:)' "$YML"; then no_matrix="ok"; fi
  # single_job = exactly one top-level key under `jobs:` (line `  <key>:` at 2-space indent under jobs).
  JOB_COUNT="$(awk '/^jobs:/{f=1;next} f && /^[a-zA-Z]/ {f=0} f && /^  [a-zA-Z0-9_-]+:[[:space:]]*$/ {c++} END{print c+0}' "$YML")"
  test "$JOB_COUNT" = "1" && single_job="ok"
fi

EXIT_CODE=0
ALL_OK="ok"
for v in "$has_macos_15" "$has_setup_bun" "$has_bun_install_frozen" \
         "$has_typecheck" "$has_vitest" "$has_swift_test" \
         "$has_mcp_smoke" "$no_matrix" "$single_job"; do
  if [[ "$v" != "ok" ]]; then ALL_OK="fail"; EXIT_CODE=1; fi
done

printf '{"exit_code":%d,"has_macos_15":"%s","has_setup_bun":"%s","has_bun_install_frozen":"%s","has_typecheck":"%s","has_vitest":"%s","has_swift_test":"%s","has_mcp_smoke":"%s","no_matrix":"%s","single_job":"%s","all_ok":"%s"}\n' \
  "$EXIT_CODE" "$has_macos_15" "$has_setup_bun" "$has_bun_install_frozen" \
  "$has_typecheck" "$has_vitest" "$has_swift_test" "$has_mcp_smoke" \
  "$no_matrix" "$single_job" "$ALL_OK"

exit "$EXIT_CODE"
```

字面验证：
```bash
chmod +x scripts/simx-c2-ci-workflow-validate.sh
bash scripts/simx-c2-ci-workflow-validate.sh
# 期望：exit 0 + 单行 JSON
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.has_macos_15 == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.has_setup_bun == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.has_bun_install_frozen == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.has_typecheck == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.has_vitest == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.has_swift_test == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.has_mcp_smoke == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.no_matrix == "ok"' > /dev/null
bash scripts/simx-c2-ci-workflow-validate.sh | jq -e '.single_job == "ok"' > /dev/null
```

期望：每条 exit 0。

**重构**

无。

---

## Checkpoint C2 验收

```bash
set -uo pipefail
cd /Users/doracawl/workspace/labs/lab15-autofix

# (1) workflow file 字面在场 + 9 token 字面通
test -f .github/workflows/ci.yml
bash scripts/simx-c2-ci-workflow-validate.sh > /tmp/c2-validate.json
test "$(jq -r '.all_ok' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.has_macos_15' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.has_setup_bun' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.has_bun_install_frozen' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.has_typecheck' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.has_vitest' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.has_swift_test' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.has_mcp_smoke' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.no_matrix' /tmp/c2-validate.json)" = 'ok'
test "$(jq -r '.single_job' /tmp/c2-validate.json)" = 'ok'

# (2) 本机 5 gate 命令逐条 exit 0（等价 CI 离线执行）
set -e
bun install --frozen-lockfile > /dev/null
bun run typecheck > /dev/null
bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +578 passed'
( cd swift-bridge && swift test 2>&1 ) > /tmp/c2-swift.log
grep -qE "Test Suite .All tests. passed|Executed +[0-9]+ tests" /tmp/c2-swift.log
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
set +e

# (3) 退化检查 — 578 TS / 27 MCP tool / 102 swift / typecheck / c1-mcp-smoke 全保
bun x vitest run 2>&1 | tail -3 | grep -qE 'Tests +578 passed'
bun run typecheck > /dev/null
bash scripts/simx-c1-mcp-smoke.sh 2>/dev/null | jq -e '.all_ok == "ok" and .tools_list_count == 27' > /dev/null

# (4) 退化检查 — v0.7 C1 工程实体不动
test -f src/sim/runner-supervisor.ts
test -f src/sim/__tests__/runner-supervisor.test.ts
test -f scripts/long-run-100.ts
test -f scripts/simx-c1-longrun-100-smoke.sh

# (5) 退化检查 — v0.6 / 既有 smoke 不动
test -f scripts/mcp-smoke.ts
test -f scripts/simx-v06-acceptance.sh
test -f scripts/simx-c1-mcp-smoke.sh
test -f scripts/simx-c2-mcp-lifecycle-smoke.sh
test -f scripts/simx-c3-mcp-observe-smoke.sh
test -f scripts/simx-c4-mcp-interaction-smoke.sh
test -f scripts/simx-c5-mcp-compound-system-smoke.sh
test -f scripts/simx-c6-mcp-explain-screen-smoke.sh

# (6) 退化检查 — dep 字面 3 个不变
test "$(jq -r '.dependencies | keys | length' package.json)" = '3'

# (7) 退化检查 — v1.md / roadmap.md / cold v0.7 字面 0 边界改（boundary 字面在场）
grep -qE '^\[6\] 兼容：iOS 17\.5 / 18\.4 / 26\.x 全部通过冒烟集$' docs/v1.md
grep -qE 'CI.*GitHub Actions macOS-15.*runtime 矩阵 iOS 17\.5 / 18\.4 / 26\.x' docs/v1.md
grep -qE '^\| \*\*v0\.7\*\* \| 🔥 hot \|' docs/roadmap.md

# (8) 退化检查 — 既有 src/ swift-bridge/ examples/ 0 改动（断言 6 既有 src/sim + 24 vitest test file 在场）
test -f src/sim/runner-client.ts
test -f src/sim/dev-lock.ts
test -f src/sim/cell.ts
test -f src/sim/session.ts
test -f src/sim/simctl.ts
test -f src/sim/a11y-tree-source.ts
test -f swift-bridge/Package.swift
test -f swift-bridge/SimxRunner.xcodeproj/project.pbxproj
test "$(find src -name '*.test.ts' -not -path '*/node_modules/*' | wc -l | tr -d ' ')" = '24'

echo 'C2 verdict: ok'
```

期望：以上每条 exit 0 + 末行字面 `C2 verdict: ok`。

## 完成后动作

1. 归档此文件到 `docs/plan-history/v0.7-c2-hot.md`：
   ```bash
   mv docs/plan-hot.md docs/plan-history/v0.7-c2-hot.md
   ```
   （备注：v0.7 C1 hot 已字面在 `docs/plan-history/v0.7-c1-hot.md` 43k 在场；当前 `docs/plan-hot.md` 在 C2 hot 写入前是 C1 旧内容副本，C2 hot 写入即覆盖；C2 close 时 mv 到 v0.7-c2-hot.md）
2. **改 `docs/v1.md` 决策日志末尾 +1 行字面**（决策 19.A 字面 — C2 close note + git 工作树缺席偏差记）：
   ```
   - 2026-05-16 v0.7 C2 close — single-runtime CI workflow YAML 字面在场（`.github/workflows/ci.yml` ~60-80 行 / runs-on: macos-15 / 5+ gate step：checkout@v4 + setup-bun@v2 (^1.3.0) + bun install --frozen-lockfile + bun run typecheck + bun x vitest run + swift test (working-directory: swift-bridge) + bash scripts/simx-c1-mcp-smoke.sh）+ 本机 5 gate 命令字面逐条 exit 0（vitest 578 passed / swift test ≥102 / typecheck 0 error / c1-mcp-smoke all_ok=ok / bun install --frozen-lockfile 净 lockfile 不漂）+ 新增 `scripts/simx-c2-ci-workflow-validate.sh` 10 字段 JSON gate（has_macos_15 / has_setup_bun / has_bun_install_frozen / has_typecheck / has_vitest / has_swift_test / has_mcp_smoke / no_matrix / single_job / all_ok）字面全 ok；TS baseline 578 不动（C2 0 vitest case 增）/ 102 swift 不动 / 27 MCP tool 不动 / 0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 既有 src/sim 6 文件 / 0 既有 src/{driver,sdk,core,cli,mcp} / 0 dep / 0 既有 27 ToolDef / 0 既有 6 mcp smoke 字段改 / 0 既有 5 v0[1-5] acceptance 改 / 0 v0.6 acceptance 改 / 0 mcp-smoke.ts 改 / 0 long-run-100.ts 改 / 0 simx-c1-longrun-100-smoke.sh 改 / 0 runner-supervisor.ts 改 / 0 examples / 0 roadmap.md / 0 v1.md 边界 / 0 cold v0.7 改；v1.md §2 资源依赖"CI：GitHub Actions macOS-15"字面工程实体在场（单 runtime；多 runtime matrix 推 C3）。**git 工作树缺席偏差字面记**：lab15-autofix 当前非 git 工作树（无 `.git` 项），GHA workflow 文件真触发依赖 git push → C2 范围内 workflow file 真跑路径不在场；C2 验证 = (a) YAML 文件字面契约 9 token + (b) 5 gate 命令本机逐条字面 exit 0 = 等价"如果用户日后 git init + 推 GitHub 后 CI 应通"的可证 evidence；C7 README v1.0 发布章节写人工 git init 步骤 + branch protection / required status check 配置说明。**C3 推**：iOS 17.5 / 18.4 sim runtime 在本机 SDK 不在场（`xcodebuild -showsdks` 字面单 iOS 26.4），多 runtime matrix 走 C3 + CI 上 `xcversion simulators --install` 预 cache；长跑 e2e 进 CI 走 C3（需 dev-sim.txt CI fixture + xcrun simctl create dev sim）。
   ```
3. 调 sub-agent 生成新 `plan-hot.md`（到 v0.7 C3 多 runtime matrix iOS 17.5/18.4/26.x），见 CLAUDE.md §6 标准模板 — **本计划范围内不展开 C3**。
