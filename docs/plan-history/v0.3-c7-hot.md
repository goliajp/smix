# plan-hot — v0.3 到 C7：v0.3 收尾（login.test.ts tap-only 子集挑回 + v0.3 整体出口验收脚本 + README/roadmap 同步）

> [3/4 信息层] 当前唯一热计划。完成 C7 后归档到 `plan-history/v0.3-c7-hot.md`，由 main convo 调 sub-agent 生成 v0.4 C1（SDK 行为完备 — matcher 失败上下文真填充 / `.simx/trace/` 输出 / `waitFor` 真生效改进，参 `docs/plan-cold/v0.4.md`（main convo 在 C7 close 后创建））。
>
> **范围决策（C7 候选 A/B/C/D）**：v0.3 C7 冷计划字面写 `examples/login.test.ts` 完整跑通（含 selector + tap + **fill**）。**fill 字段不能照搬**：(1) `fill` 走 HID 键盘 (`v1.md §2.A` 表 "文本输入" 行)，runner 端无 `/fill` endpoint，host-hid 二进制无 keyboard subcommand —— 工程量 ≥ 5 swift step + 6+ ts step（违反 §2 "1-3 step" 限制）；(2) HID 键盘真实现在冷计划路线图上明确归 v0.7（runtime CI 矩阵同期 5-arg iOS 17/18 path）；(3) C6 决策 #6 已显式记 "fill 字段合理化为 tap-only 子集，HID 键盘 fill v0.7+"。
>
> **选 (D) = (A) + (C)**：
>
> 1. **(A) examples 部分挑回**：从 `_v03-pending/login.test.ts` 摘出 **tap-only 子集**（删 2 个 `fill` step + 删依赖 fill 的"login rejects invalid credentials" 整 case + 改 bundle id 为 `com.apple.Preferences`），命名 **`examples/login-tap.test.ts`**（顶层 examples，参照 `tap-text-selector.test.ts` 形态）。新 example 完整展示 v0.3 selector 4 base + 3 modifier × 真触发 Settings UI 跳页。**原 `_v03-pending/login.test.ts` 不动**（留 v1.md §4 Success Criteria [1] 字面 reference target，v0.7+ HID 键盘后挑回顶层）。**cart-checkout.test.ts 不动**（longPress / scroll / pasteboard / deeplink 推 v0.4-v0.5 HID multi-action + system bridge）。
> 2. **(C) v0.3 整体 acceptance + 文档同步**：新建 `scripts/simx-v03-acceptance.sh`（参 v02 模式 + 新增 3 字段 = 12 字段 JSON，向后兼容 v02 9 字段）；README Status 段从 "v0.2 — driver real-tap shipped" 改为 "v0.3 — selector resolver + AX read shipped"；`docs/roadmap.md` v0.3 → ✅ done / v0.4 → 🔥 hot。
>
> **不选 (B) 写新 examples/selector-showcase.test.ts**：login.test.ts tap-only 子集等价覆盖（id + role+name + text + RegExp + inside modifier）+ 更接近 v1 SuccessCriteria [1] 字面 spec；新写一个 selector-showcase 是重复工。
>
> **不选纯 (A) 或纯 (C)**：纯 (A) = v0.3 closure 无单一证据；纯 (C) = v0.3 selector 形态在 examples 上仍空（顶层只有 v0.2 text-only example），AI 测试作者拿来当黄金路径会错以为 v0.3 没新东西。
>
> **范围严格 = swift 0 改动 / Driver interface 0 改动 / src/ 0 实现逻辑改动**（与 v0.2 C7 同 closure 范式）。**唯一 src 改动允许窗口** = 测试 path string 跟动（grep 后定，预期 0 处）。

## 目标 checkpoint

**C7**：v0.3 关版本。具体说，C7 通过后世界变成：

1. **examples/ 顶层增 v0.3 黄金路径**：4 个文件 → 3 个真 example：
   - `screenshot-only.test.ts`（v0.1，不动）
   - `tap-text-selector.test.ts`（v0.2，不动）
   - **新 `login-tap.test.ts`**（v0.3 — 1 个 test case，展示 id + role+name + text/RegExp + `inside` modifier + `waitFor` + `expect.toBeVisible`）
   - `_v03-pending/login.test.ts` 不动（v1 SuccessCriteria [1] 字面 reference target，等 v0.7+ HID 键盘 fill 后挑回）
   - `_v03-pending/cart-checkout.test.ts` 不动（推 v0.4-v0.5）
   - `examples/README.md` 更新表格加 `login-tap.test.ts` 行 + `_v03-pending/README.md` 改 "v0.3 → v0.7+" 字面
2. **`scripts/simx-v03-acceptance.sh` 跑通**：1 条 shell 串联 v0.2 9 字段 + v0.3 新增 3 字段 = **12 字段单行 JSON**：
   - 沿用 v0.2 9 字段：`swift_tests` (≥102) / `ts_tests` (≥264) / `typecheck` / `doctor_checks` (≥6) / `hostid_probe` / `runner_health` / `runner_tap_smoke` / `hostid_digitizer_smoke` / `tap_text_selector_e2e`
   - 新增 3 字段（v0.3 selector resolver + AX 真证据）：
     - `runner_tree_e2e`：`scripts/simx-runner-tree-smoke.sh` 拉 /tree → node_count > 50 + 含 "General" label
     - `axp_probe`：`simx-host-hid axp-probe` → `.ok == true`（C2 host-side AXP probe）
     - `resolver_id_e2e`：`scripts/simx-c6-resolver-tap-smoke.sh` 的 `id_selector_probe_miss == "404"` + `id_selector_probe_hit == "ok"`（id selector → resolveSelector → host-hid coord tap → 真触发 General 子页；C6 已证 Phase B 全通过）
   - **role/text selector e2e 字段不纳入 acceptance gate**：sparse-tree regression 已记 C6 决策（SimXcui 上游问题非 driver bug）；单测层 22 case 已覆盖 role+name / text+modifier；C7 不修上游 regression（推 v0.7 hardening）；C7 acceptance JSON 仍**输出** Phase C/D 的 raw 字段（信息保真）但**不**列 gate
3. **README.md** Status 段从 "v0.2 — driver real-tap shipped" 改为 "v0.3 — selector resolver (4 base + 8 modifiers) + AX read shipped"；Roadmap 段（README 内）v0.2/v0.3 → done，v0.4 → next；examples 段加一行 `login-tap.test.ts`；Authoring guide 不动（v0 锁定的 DSL surface）；Example code block 内 `app.fill(...)` 行加注释 "(fill: v0.7+; this example shows the v0.3 selector forms that work today)"
4. **`docs/roadmap.md`** 同步：v0.3 `🔥 hot` → `✅ done`；v0.4 `❄️ cold` → `🔥 hot`；"当前所在位置"段：正在做 v0.4
5. 既有 102 swift + 264 TS 测试**全部不退化**（C7 0 src 实现改动，0 swift 改动；只 examples reorg + 新 shell + docs）
6. CLI 不变；Driver 接口不变；任何 `src/` / `swift-bridge/Sources/` 字节级改动 = 退化

### 具体 deliverable

1. **新建** `examples/login-tap.test.ts`（≤ 40 行；1 个 test case；展示 v0.3 selector 形态；bundle id 用 `com.apple.Preferences` —— 与 `tap-text-selector.test.ts` 同 sim app，runner auto-launches Settings）
2. **改** `examples/README.md`：表加 `login-tap.test.ts` 行 + 改 "What's in here today (v0.2)" 标题为 "What's in here today (v0.3)"
3. **改** `examples/_v03-pending/README.md`：把 "Resurrected by v0.3" 改为 "Resurrected by v0.7+ (HID keyboard fill)"，明确 login.test.ts 等 fill + cart-checkout 等 longPress/pasteboard
4. **新建 + chmod 755** `scripts/simx-v03-acceptance.sh`（≤ 220 行；4 Phase 串联，v02 9 字段 + 3 v0.3 字段 = 12 字段 JSON）
5. **改** `README.md`：Status 段 / Roadmap 段 / Example code block 注释 / Local dev 段 acceptance 命令
6. **改** `docs/roadmap.md`：v0.3/v0.4 状态字段切换 + 当前位置段
7. **C7 close 后 main convo** 在 `docs/v1.md` 末尾追加 1 行决策日志（sub-agent 不动 v1.md）
8. **C7 close 后 main convo** 创建 `docs/plan-cold/v0.4.md`（v0.4 = SDK 行为完备：matcher 失败上下文 / trace dir / waitFor 改进；冷计划框架先建，C1 热化时再填具体步骤）—— **C7 plan 不创建 v0.4 cold 计划**（CLAUDE.md §6 fence：sub-agent 不预先展开下一段；冷计划路标已在 `docs/roadmap.md` v0.4 行）

### 真验证终点的机器可判断证据（无人工读图）

```bash
# (a) 文件结构
test -f examples/login-tap.test.ts
test -f examples/screenshot-only.test.ts
test -f examples/tap-text-selector.test.ts
test -f examples/README.md
test -f examples/_v03-pending/README.md
test -f examples/_v03-pending/login.test.ts
test -f examples/_v03-pending/cart-checkout.test.ts
test -x scripts/simx-v03-acceptance.sh

# (b) 不退化
swift test --package-path swift-bridge 2>&1 | grep -Eq 'Executed 102 tests, with 0 failures'
bun run test 2>&1 | grep -Eq 'Tests +264 passed'
bun run typecheck

# (c) v02 acceptance 仍绿（C7 不破 v0.2 出口）
bash scripts/simx-v02-acceptance.sh | jq -e '
  .swift_tests >= 63 and .ts_tests >= 125 and .typecheck == "ok" and
  .doctor_checks >= 5 and .hostid_probe == "ok" and
  .runner_health == 200 and .runner_tap_smoke == "ok" and
  .hostid_digitizer_smoke == "ok" and .tap_text_selector_e2e == "ok"'

# (d) v03 acceptance 12 字段全 pass
bash scripts/simx-v03-acceptance.sh | jq -e '
  .swift_tests >= 102 and .ts_tests >= 264 and .typecheck == "ok" and
  .doctor_checks >= 6 and .hostid_probe == "ok" and
  .runner_health == 200 and .runner_tap_smoke == "ok" and
  .hostid_digitizer_smoke == "ok" and .tap_text_selector_e2e == "ok" and
  .runner_tree_e2e == "ok" and .axp_probe == "ok" and
  .resolver_id_e2e == "ok"'

# (e) docs 同步
grep -qE "v0\.3" README.md
grep -qE 'simx-v03-acceptance\.sh' README.md
grep -qE "v0\.3.*✅ done" docs/roadmap.md
grep -qE "v0\.4.*🔥 hot" docs/roadmap.md

# (f) src / swift 0 改动（C7 是 docs + examples + scripts 收口）
! find src -name '*.ts' -newer docs/plan-history/v0.3-c6-hot.md -print 2>/dev/null | head -1
! find swift-bridge/Sources -name '*.swift' -newer docs/plan-history/v0.3-c6-hot.md -print 2>/dev/null | head -1
```

**已锁定的 4 个 C7 决策**（详 §关键决策 #1-#4）：

1. **C7 范围 = (D)**：(A) login.test.ts tap-only 子集挑回 + (C) acceptance script + 文档同步。不选 (B) 新写 selector-showcase（重复 + 远离 v1.md §4 [1] 字面 spec）。
2. **acceptance script 命名 = `simx-v03-acceptance.sh`（v0.3 单独）**：v02 保留不动；v04+ 起再考虑通用化 `simx-acceptance.sh`（避免每 minor 版本都 break 已存在脚本 path）。
3. **examples 处置 = 挑回 + 不新写**：`login.test.ts` 是 v1 SuccessCriteria [1] 字面 reference target；挑 tap-only 子集为 `login-tap.test.ts`，HID 键盘 v0.7+ 后合并回 `login.test.ts`。`cart-checkout.test.ts` 全留 pending（推 v0.4-v0.5）。
4. **iOS 26.4 sparse-tree regression 不在 C7 修**：推 v0.7 hardening。C7 acceptance gate 仅 id selector phase（C6 Phase B 全通过），不 gate role/text phase；role/text e2e 字段 raw 输出但不卡 exit code。理由：(1) C7 是版本收尾，不引入新 src 逻辑（违反 closing 范式）；(2) sparse-tree 是 SimXcui 上游问题（C6 已记），driver 单测层 22 case 已独立证明 driver 正确性；(3) v0.7 hardening minor 是修上游 issue 的合理 home。

### 不做（推 C8 ❌ / v0.4+）

> v0.3 C7 是 v0.3 minor 的**最后** checkpoint —— 没有 C8。任何"再做一步"都跨 minor 边界 = 必推 v0.4+ cold 计划或更后。

- `examples/_v03-pending/login.test.ts` 含 fill 整 case 真跑通 → **v0.7+**（HID 键盘 + runner /type-text endpoint；v1.md §2.A 表 "文本输入" 行）
- `examples/_v03-pending/cart-checkout.test.ts` 真跑通 → **v0.4** (`longPress` / `scroll` / `scrollTo` — HID multi-action / **v0.5** (`pasteboard` 接通虽然 simctl 已有 wrapper、`system.openUrl` deep link runner endpoint)
- `app.describe()` ScreenDescription / matcher `toBeVisible` 失败上下文真填充 (`visibleElements` / `suggestions` edit-distance) → **v0.4**（v1.md §2.B + plan-cold/v0.4 范围）
- `.simx/trace/` JSON + PNG 真输出 → **v0.4**
- AXP `objectAtPoint` / `frontmostApplicationWithDisplayId` 真直读（替代 XCUITest snapshot） → **v0.5**（C2 决策 (D) 留的真直读空间）
- iOS 26.4 sparse-tree regression 修复（driver 层 1-shot retry / app.describe() 兜底） → **v0.7**（hardening）
- Cell L2 操作隔离 + port allocator + `.simx/cells/<id>/` traceDir prefix → **v0.4-v0.6**（C6 决策已记）
- 真 5-arg iOS 17/18 HID path → **v0.7**（CI runtime 矩阵）
- runner lifecycle 管理（auto-spawn / restart / health-loop） → **v0.4-v0.6**
- tap 延迟 P50 < 50ms metric instrument → **v0.7**（hardening；v0.2 C7 决策日志已推）
- 新 npm / SPM dep → **C7 0 dep 改动**
- 新 swift 源 / 改 swift 源 → **C7 swift 0 字节**
- 改 Driver 接口 / 改 SimSession / 改 Cell type → **C7 不动 src/sim/_、src/driver/_、src/sdk/_、src/core/_、src/cli/_ 任何 .ts 文件实现逻辑**

## 前置条件（已本机探测 — 2026-05-15）

```bash
cd /Users/doracawl/workspace/labs/lab15-autofix

# 1. 文件层 — C6 已归档，C7 未热化
test -f docs/plan-history/v0.3-c6-hot.md                                                            # ✓
test -f docs/plan-cold/v0.3.md                                                                       # ✓
! test -e docs/plan-hot.md                                                                           # ✓（本文件写入即创建）
! test -e docs/plan-cold/v0.4.md                                                                     # ✓（C7 close 后 main convo 创建）

# 2. v0.3 C6 baseline（C7 不破）
swift test --package-path swift-bridge 2>&1 | grep -Eq 'Executed 102 tests, with 0 failures'         # ✓ 102
bun run test 2>&1 | grep -Eq 'Tests +264 passed'                                                     # ✓ 264
bun run typecheck                                                                                    # ✓ exit 0

# 3. v0.2 acceptance 仍绿（C7 不破 v0.2 出口）
bash scripts/simx-v02-acceptance.sh | jq -e '
  .swift_tests >= 63 and .ts_tests >= 125 and .typecheck == "ok" and
  .doctor_checks >= 5 and .hostid_probe == "ok" and
  .runner_health == 200 and .runner_tap_smoke == "ok" and
  .hostid_digitizer_smoke == "ok" and .tap_text_selector_e2e == "ok"'                                # ✓

# 4. v0.3 子 endpoint 全 work（C7 acceptance 串它们）
bash scripts/simx-runner-tree-smoke.sh | jq -e '.bounds.w > 0 and .bounds.h > 0'                     # ✓ runner /tree 工作
swift-bridge/.build/debug/simx-host-hid axp-probe | jq -e '.ok == true'                              # ✓ AXP probe ok
bash scripts/simx-c6-resolver-tap-smoke.sh | jq -e '
  .id_selector_probe_miss == "404" and .id_selector_probe_hit == "ok"'                               # ✓ C6 Phase B 通

# 5. dev sim booted + runner 可达（C7 e2e 必需）
test -f .simx/dev-sim.txt                                                                            # ✓
SIMX_UDID="$(cat .simx/dev-sim.txt)"                                                                 # ✓ 7B88259F-D864-4F98-9F2C-F0424216EE64
xcrun simctl list devices -j | jq -r --arg u "$SIMX_UDID" \
  '.devices | to_entries[] | .value[] | select(.udid==$u) | .state'                                  # ✓ "Booted"

# 6. examples 当前形态（C7 即将增 login-tap.test.ts）
test -f examples/screenshot-only.test.ts                                                             # ✓
test -f examples/tap-text-selector.test.ts                                                           # ✓
test -d examples/_v03-pending                                                                        # ✓
test -f examples/_v03-pending/login.test.ts                                                          # ✓ 含 2 个 case + fill
test -f examples/_v03-pending/cart-checkout.test.ts                                                  # ✓
test -f examples/README.md                                                                           # ✓ (v02 C7 落)
test -f examples/_v03-pending/README.md                                                              # ✓
! test -e examples/login-tap.test.ts                                                                 # ✓（C7 起步即新增）

# 7. tsconfig.examples.json exclude _v03-pending 在位（C7 新 login-tap.test.ts 是顶层，自动 include）
grep -q '"examples/_v03-pending/\*\*"' tsconfig.examples.json                                        # ✓

# 8. 工具链 + xcodebuild + iOS 26.4 runtime
xcodebuild -version | head -1 | grep -qE 'Xcode 26'                                                  # ✓
xcrun simctl list runtimes | grep -qE 'iOS 26\.4'                                                    # ✓

# 9. Driver interface 0 改动锚点（C7 不动 types.ts / cell.ts）
test -f src/driver/types.ts                                                                          # ✓
test -f src/sim/cell.ts                                                                              # ✓
```

**全部 ✓** → 入口条件通过；**不**拒绝热化（详 §"是否拒绝热化"段 + §"与冷计划假设的对照"段）。

## 范围决策 & 与冷计划的对照

### 与冷计划 `plan-cold/v0.3.md` C7 字面对照

| 冷计划字面 | 实际 / 范围 | 处理 |
|---|---|---|
| C7 "`examples/login.test.ts` 完整跑通（含 selector + tap + fill）" | `fill` 走 HID 键盘，runner 端无 /fill endpoint，host-hid 无 keyboard subcommand —— 工程量 ≥ 5 swift + 6 ts step，明确归 v0.7 路线 | **决策 #3 拆分**：login.test.ts tap-only 子集挑回为新 `login-tap.test.ts`（v0.3 真跑通）；原文件留 _v03-pending（v0.7+ 含 fill 整 case 挑回） |
| 冷计划 §出口验收 "AX 树 100 节点 resolve P50 < 20ms（trace metric）" | C5 决策已合理化 geometry 落 TS 侧；trace dir 真输出推 v0.4；100 节点真 sim 上 dev sim tree node_count = 167 已实测 | **推 v0.4**（与 v0.2 C7 latency P50 同形态：metric instrument 需 trace dir 真落地，与 SDK 行为完备同期） |
| 冷计划 §出口验收 "`bun run test` / `swift test --package-path swift-bridge`" | v0.3 C7 整体出口 = `bash scripts/simx-v03-acceptance.sh` 单条命令串 12 字段 JSON | **超额完成**：acceptance script 串 v02 9 字段 + v0.3 3 新字段，单条 shell 出 JSON gate |
| 冷计划 §C7 已知风险 "AX 树深度限制 60 层" | 沿用 C1/C5 决策（截断 → resolveSelector 返 null → ExpectationFailure visibleElements 兜底；visibleElements 真填充推 v0.4） | **吸收** |

**结论**：C7 范围 = 冷计划字面 (selector + tap **part-of** fill case) **挑回**至 tap-only 子集 + 出口验收脚本 + 文档同步；fill 整 case 推 v0.7+；latency P50 推 v0.4。**3 处合理偏差**全部列入 §"与冷计划假设的对照"，main convo 在 C7 close 后追加决策日志一行（CLAUDE.md §10）。**不**拒绝热化。

### v1.md 边界对照

- **v1.md §2.A 表"文本输入" 行**：HID 键盘（短）+ UIPasteboard（长）。C7 不引 HID 键盘（推 v0.7+），fill 字段在 login.test.ts 中保留为 _v03-pending 黄金 spec
- **v1.md §2.B "Selector resolver：4 base + 8 modifiers 全部生效"**：C6 已通；C7 通过 login-tap.test.ts 在真 Settings UI 上展示 id + role+name + text/RegExp + `inside` modifier，实质性证据加固
- **v1.md §2.B "Matcher 失败上下文真填充：visibleElements / screenshot / suggestions (edit-distance)"**：C6 仅 visibleElements 占位填充；screenshot / suggestions edit-distance 推 v0.4（与 v0.2 C7 同形态 - 推后）
- **v1.md §3 不做表 / §9 不变量**：C7 满足全 7 条 — (1) 仍只支持模拟器；(2) 不引 VLM；(3) **不**暴露 xpath / 坐标 selector（login-tap.test.ts 形态严格 selector 对象）；(4) 不裸 sleep；(5) 失败 AI-readable（acceptance fail → JSON 标 "fail" + stderr tail）；(6) 私有符号 dlsym（C7 不动 Swift）；(7) 4 层信息层（plan-hot 本文件写入即满足；v0.4 cold 计划由 main convo 在 C7 close 后创建）。**全 7 条满足**。

## 步骤（线性，无分叉；2 个 step）

> **分组依据**：
> - **S1** = examples 部分挑回（新建 `login-tap.test.ts` + 改 2 个 README）+ 跟动 path（grep 后预期 0 处）。纯文件层 + 文档操作，无 src/ 改动。
> - **S2** = v0.3 整体出口验收脚本 + README + roadmap 同步。1 个新 shell + 2 个 docs 改。
>
> **不分 S3**：roadmap.md 改 2 行 + README 改 4 段都在 S2 内一起完成（与 v0.2 C7 同形态；S3 拆出来散）。

---

### S1. examples 部分挑回 — 新建 `login-tap.test.ts` + 更新 examples README（reorganization 红→绿）

> **决策依据（见末尾"关键技术决策"表 #3）**：v0 时期 `login.test.ts` 是 v1.md §4 SuccessCriteria [1] 字面 reference target（"AI 0-shot 写出跑通的 examples/login.test.ts 等价测试"）。**保留**原文件在 `_v03-pending/` 作 spec 凭据，**新写** `login-tap.test.ts` 在顶层展示 v0.3 已落地的 selector 形态。tap-only 子集裁剪规则：
>
> - 删 `app.fill(...)` 行（2 处）
> - 删依赖 fill 的整 case（"login rejects invalid credentials"）
> - 改 bundle id `com.example.app` → `com.apple.Preferences`（与 `tap-text-selector.test.ts` 同 sim app，runner auto-launches）
> - 改 selector 文本为 Settings 真 UI 元素 — 选 General / Display & Brightness / Accessibility 三个 root cell + Wi-Fi 子页 selector 作 expect.toBeVisible 锚点
> - 保留 selector 4 base + `inside` modifier + `waitFor` + `expect.toBeVisible` 真打通展示（v0.3 selector resolver 端到端典型用法）
>
> Settings root cells（C6 实测）：Apple Account / General / Display & Brightness / Accessibility / Wi-Fi 等都在 root；tap "General" → push General sub-page 含 About / Software Update / AirDrop 等；C7 example 用 General 跳页 + About 验证（C6 Phase B 已证全通）。

**红（写"验证 example 未存在"的断言；非 vitest，是 shell 验证）**

- 红：
  ```bash
  cd /Users/doracawl/workspace/labs/lab15-autofix
  test -f examples/login-tap.test.ts   # 期望 exit 1（红）
  ```

**绿（执行 reorg + 新建）**

1. 新建 `examples/login-tap.test.ts`，**锁形状**（≤ 40 行）：
   ```typescript
   /**
    * v0.3 selector resolver showcase: id / role+name / text + RegExp +
    * `inside` modifier, all driving the real Settings UI on the dev sim.
    *
    * Trimmed-down from _v03-pending/login.test.ts: the v0.7+ HID-keyboard
    * `fill` steps are still in the pending file as the SuccessCriteria
    * [1] reference target. When v0.7+ keyboard lands we'll merge them.
    *
    * Run (runner auto-launches com.apple.Preferences):
    *   bun src/cli/index.ts run examples/login-tap.test.ts
    */
   import { test, expect } from '../src/sdk/index.js'

   test('settings: navigate via 4-base + modifier selectors', async ({ app }) => {
     // 1) base text selector — plain string match (path A: runner /tap).
     await app.tap({ text: 'General' })

     // 2) text + RegExp on the General sub-page.
     await expect(app.element({ text: /About/ })).toBeVisible()

     // 3) waitFor on a role+name selector (auto-poll).
     await app.waitFor({ role: 'cell', name: 'About' })

     // 4) id selector → resolveSelector → host-hid coord tap (path B).
     await app.tap({ id: 'About' })

     // 5) text + `inside` modifier — disambiguates by container role.
     await expect(app.element({ text: /Version/, inside: { role: 'cell' } }))
       .toBeVisible()
   })
   ```
   - **形态决策**：1 个 test case，5 steps 覆盖 4 base (text/RegExp/role+name/id) + 1 modifier (inside) + waitFor + 2 expect.toBeVisible
   - **bundle id 隐含**：不写 `app.launch(...)` —— runner auto-launches Settings（与 `tap-text-selector.test.ts` 同模式 + C6 决策日志已记 `fresh:true` 会与 runner 抢 PID）
   - **selector 真实性**：所有 selector 都基于 C6 实测的 Settings UI 元素（General root cell / About sub-page cell / version row inside cell）
   - **不写 launch / terminate**：runner 已 attached；example 假定 root state（与 C6 sparse-tree regression 兼容，因为 acceptance 不 gate role/text e2e；本 example 在 example layer 即使遇 regression 也能在 v0.7 修复后跑通）

2. 改 `examples/README.md`：
   - "What's in here today (v0.2)" → "What's in here today (v0.3)"
   - 表加一行（在 `tap-text-selector.test.ts` 后）：
     ```
     | `login-tap.test.ts` | v0.3 selector resolver showcase: `text` / `text:RegExp` / `id` / `role+name` / `inside` modifier / `waitFor` / `expect.toBeVisible` against the real Settings UI. The v0.3 closing-checkpoint smoke. | Same as `tap-text-selector.test.ts`. |
     ```
   - "Pending v0.3 (selector resolver)" → "Pending v0.7+ (HID keyboard fill / multi-action)"
   - "v0.3 will resurrect them..." → "v0.7+ will resurrect `login.test.ts` (needs HID keyboard `fill`) and v0.4-v0.5 will resurrect `cart-checkout.test.ts` (needs `longPress` / `scroll` / pasteboard / deep link)"
   - Running 段加：
     ```bash
     # v0.3 selector showcase (depends on the SimxRunner already being up).
     bun src/cli/index.ts run examples/login-tap.test.ts
     ```

3. 改 `examples/_v03-pending/README.md`，**锁形状**（≤ 25 行）：
   ```markdown
   # examples/_v03-pending

   Tests kept around as **spec / future golden path**, not currently
   runnable.

   - `login.test.ts` — the v1 SuccessCriteria [1] reference target.
     Needs `app.fill` (HID keyboard input). Resurrected by **v0.7+**
     (HID keyboard + runner `/type-text` endpoint).
     The v0.3 selector resolver portion is already showcased in
     `examples/login-tap.test.ts` (tap-only subset).
   - `cart-checkout.test.ts` — needs `longPress` / `scroll` / `scrollTo`
     (v0.4 HID multi-action), `pasteboard.set` / `system.openUrl`
     (v0.5 system bridge). Resurrected by **v0.5**.

   Both use `com.example.app` / `exampleshop://` placeholders that
   don't exist on the host today; v0.7+ plan replaces with a real
   sample app before merge.
   ```

4. 探测：`grep -rn "login\.test\|cart-checkout" src/ docs/plan-cold/ scripts/`——除已知（`docs/plan-history/`（归档不动）+ 本计划 + `examples/_v03-pending/`）外**应**无引用；如有 → 改路径
   ```bash
   grep -rn "examples/_v03-pending/login\|examples/login\.test\|examples/cart-checkout" \
     src/ scripts/ 2>/dev/null
   # 期望：0 hit（src / scripts 都不引用）
   ```

5. 跑 typecheck + test（绿）：
   ```bash
   bun run typecheck 2>&1 | tail -3
   bun run test 2>&1 | grep -E 'Tests +[0-9]+ passed' | tail -1
   ```
   - 期望：typecheck exit 0；`Tests 264 passed (264)`（**不变**，examples/ 不在 vitest scope；新 example 是 sdk 测试不是 vitest）
   - **关键**：新 `login-tap.test.ts` 必须**编译通过**（tsconfig.examples.json 扫顶层 `examples/*.ts`），因此 import path / 类型必须正确；任何 type error 在此 step 红色，必须修

6. 跑新 example（绿 — runner auto-launches；C7 在 S2 acceptance 跑前手动验一次合理）：
   ```bash
   # 先确保 runner 起着（C6 smoke script 的 trap 已 cleanup；S1 手验不必跑）
   # — 本步骤仅在 S2 acceptance 内自然验证（acceptance 串 c6-resolver-tap-smoke 但不串 login-tap example）
   # 若 S1 手验：
   #   bash scripts/simx-runner-health.sh  # 起 runner
   #   bun src/cli/index.ts run examples/login-tap.test.ts   # 跑 example，exit 0 = pass
   # C7 不要求 S1 跑 example —— S2 acceptance JSON 的 resolver_id_e2e 字段才是 gate
   ```

7. 跑 reorg 验证（绿确认）：
   ```bash
   test -f examples/login-tap.test.ts && \
   test -f examples/screenshot-only.test.ts && \
   test -f examples/tap-text-selector.test.ts && \
   test -f examples/README.md && \
   test -f examples/_v03-pending/README.md && \
   test -f examples/_v03-pending/login.test.ts && \
   test -f examples/_v03-pending/cart-checkout.test.ts && \
   grep -q "login-tap.test.ts" examples/README.md && \
   grep -q "v0.7" examples/_v03-pending/README.md
   echo "s1-exit=$?"
   ```
   - 期望：`s1-exit=0`

**重构**

- 不动 `examples/screenshot-only.test.ts` / `examples/tap-text-selector.test.ts`（v0.1/v0.2 milestone，0 risk）
- 不动 `_v03-pending/login.test.ts` / `_v03-pending/cart-checkout.test.ts` 内容（保 v1.md §4 [1] 字面 reference）
- 不在 example 内加 try/catch swallow（CLAUDE.md §8.5 错误纪律）
- 不在 example 内写 setTimeout / 裸 sleep（CLAUDE.md §9.4 不变量）

---

### S2. v0.3 整体出口验收脚本 + README + roadmap（红→绿）

> **决策依据（见末尾"关键技术决策"表 #1 #2 #4）**：v0.3 closure 需要单一证据；C7 acceptance 脚本串 12 个字段，是**唯一** v0.3-level 真验收脚手架。继承 v02 9 字段（向后兼容 / 字面相同）+ 加 3 个 v0.3 新字段。脚本不重复实现（不并行启 2 个 xcodebuild test runner，沿用 v02 模式按 phase 串行）。
>
> **新 3 字段的来源 endpoint**：
>
> | 字段 | endpoint | gate |
> |---|---|---|
> | `runner_tree_e2e` | `scripts/simx-runner-tree-smoke.sh`（C1 落地，已 work） | bounds.w > 0 + bounds.h > 0 + 含 General label |
> | `axp_probe` | `swift-bridge/.build/debug/simx-host-hid axp-probe`（C2 落地） | `.ok == true` |
> | `resolver_id_e2e` | `scripts/simx-c6-resolver-tap-smoke.sh`（C6 落地） | id_selector_probe_miss == "404" AND id_selector_probe_hit == "ok" |
>
> **role/text e2e 字段输出但不 gate**（决策 #4 sparse-tree regression 推 v0.7）：JSON 仍含 `resolver_role_e2e` / `resolver_text_e2e` 字段（信息保真，便于 v0.4-v0.7 监测 regression），但 PASS 判定**不**依赖它们。
>
> 综上：脚本结构（4 Phase，沿用 v02 模式 + Phase 4 加 v0.3 endpoint）：
> - **Phase 1**：离线 5 endpoint — swift test / typecheck / bun test / doctor 6-check / host-hid probe（与 v02 同但 doctor 6 含 axp 第 6 check）
> - **Phase 2**：c6-e2e（自动启 runner + drive `tap-text-selector.test.ts` 真 UI tap + 双面 probe）—— 与 v02 同
> - **Phase 3**：hostid digitizer smoke —— 与 v02 同
> - **Phase 4 NEW**：v0.3 selector resolver 3 endpoint
>   - `runner_tree_e2e`（runner /tree node_count > 50）
>   - `axp_probe`（C2 host-side AXP probe）
>   - `resolver_id_e2e`（C6 c6-resolver-tap-smoke 的 id selector phase）
> - **Phase 5**：单行 JSON summary（12 字段）+ aggregate exit code

**红（写脚本未存在 / 跑会失败）**

- 红：
  ```bash
  test -x /Users/doracawl/workspace/labs/lab15-autofix/scripts/simx-v03-acceptance.sh
  # 期望 exit 1（红）
  ```

**绿（实现脚本 + 文档同步）**

- 文件 1（新建 + chmod 755）：`scripts/simx-v03-acceptance.sh`
  - **锁结构**（伪代码；不变量在下方列）：
    ```bash
    #!/usr/bin/env bash
    # simx-v03-acceptance.sh — v0.3 整体出口验收。
    #
    # 串联 v0.2 的 9 字段 + v0.3 新增 3 字段 = 12 字段单行 JSON。
    # exit 0 = v0.3 整体验收通过；任一 gate fail → exit 1。
    #
    #   Phase 1 — 离线 5 endpoint（swift / typecheck / TS / doctor 6 / hostid-probe）
    #   Phase 2 — c6-e2e（runner /health 200 + tap-text-selector 真 UI 跳页 + 双面 probe）
    #   Phase 3 — hostid digitizer smoke
    #   Phase 4 — v0.3 NEW: runner /tree + AXP probe + resolver id selector e2e
    #   Phase 5 — 单行 JSON summary + aggregate exit code
    set -euo pipefail

    ROOT="$(cd "$(dirname "$0")/.." && pwd)"

    # ---- Phase 1: offline checks --------------------------------------------
    SWIFT_RAW="$(cd "$ROOT/swift-bridge" && swift test 2>&1 || true)"
    SWIFT_N="$(echo "$SWIFT_RAW" | grep -oE 'Executed [0-9]+ tests, with 0 failures' \
      | tail -1 | awk '{print $2}')"
    SWIFT_N="${SWIFT_N:-0}"

    TS_RAW="$(cd "$ROOT" && bun run test 2>&1 || true)"
    TS_N="$(echo "$TS_RAW" | grep -oE 'Tests +[0-9]+ passed' | head -1 | awk '{print $2}')"
    TS_N="${TS_N:-0}"

    TYPECHECK="ok"
    (cd "$ROOT" && bun run typecheck > /dev/null 2>&1) || TYPECHECK="fail"

    DOCTOR_RAW="$(cd "$ROOT" && bun src/cli/index.ts doctor 2>&1 || true)"
    DOCTOR_N="$(echo "$DOCTOR_RAW" | grep -cE '^✓ ' || true)"
    DOCTOR_N="${DOCTOR_N:-0}"

    HOSTID_PROBE="fail"
    if [[ -x "$ROOT/swift-bridge/.build/debug/simx-host-hid" ]]; then
      if "$ROOT/swift-bridge/.build/debug/simx-host-hid" probe 2>/dev/null \
          | jq -e '.ok == true' > /dev/null 2>&1; then
        HOSTID_PROBE="ok"
      fi
    fi

    # ---- Phase 2: c6-e2e（v02 兼容字段） ------------------------------------
    C6_OUT="$(bash "$ROOT/scripts/simx-c6-tap-e2e.sh" 2>/dev/null || echo '{}')"
    HEALTH="$(echo "$C6_OUT" | jq -r '.health // 0')"
    CASE_PASSED="$(echo "$C6_OUT" | jq -r '.case_passed // 0')"
    CASE_FAILED="$(echo "$C6_OUT" | jq -r '.case_failed // 1')"
    PROBE_MISS="$(echo "$C6_OUT" | jq -r '.probe_after_miss // 0')"
    PROBE_HIT="$(echo "$C6_OUT" | jq -r '.probe_after_hit // 0')"

    # ---- Phase 3: hostid digitizer smoke（v02 兼容字段） --------------------
    HOSTID_OUT="$(bash "$ROOT/scripts/simx-hostid-digitizer-smoke.sh" 2>/dev/null || echo '{}')"
    HOSTID_HEALTH="$(echo "$HOSTID_OUT" | jq -r '.health // 0')"
    HOSTID_TAP="$(echo "$HOSTID_OUT" | jq -r '.hostid_tap // "fail"')"
    HOSTID_MISS="$(echo "$HOSTID_OUT" | jq -r '.probe_after_miss // 0')"
    HOSTID_HIT="$(echo "$HOSTID_OUT" | jq -r '.probe_after_hit // 0')"

    # ---- Phase 4: v0.3 NEW endpoints ----------------------------------------
    # 4.1 runner /tree e2e
    TREE_OUT="$(bash "$ROOT/scripts/simx-runner-tree-smoke.sh" 2>/dev/null || echo '{}')"
    TREE_NODE_COUNT="$(echo "$TREE_OUT" | jq -r '.node_count // 0')"
    TREE_BOUNDS_OK="$(echo "$TREE_OUT" | jq -r '
      if (.bounds.w // 0) > 0 and (.bounds.h // 0) > 0 then "ok" else "fail" end')"

    # 4.2 AXP probe
    AXP_OK="fail"
    if "$ROOT/swift-bridge/.build/debug/simx-host-hid" axp-probe 2>/dev/null \
        | jq -e '.ok == true' > /dev/null 2>&1; then
      AXP_OK="ok"
    fi

    # 4.3 c6-resolver-tap-smoke (id phase = gate; role/text raw output only)
    C6R_OUT="$(bash "$ROOT/scripts/simx-c6-resolver-tap-smoke.sh" 2>/dev/null || echo '{}')"
    R_ID_MISS="$(echo "$C6R_OUT" | jq -r '.id_selector_probe_miss // "fail"')"
    R_ID_HIT="$(echo "$C6R_OUT" | jq -r '.id_selector_probe_hit // "fail"')"
    R_ROLE_MISS="$(echo "$C6R_OUT" | jq -r '.role_selector_probe_miss // "fail"')"
    R_ROLE_HIT="$(echo "$C6R_OUT" | jq -r '.role_selector_probe_hit // "fail"')"
    R_TEXT_MISS="$(echo "$C6R_OUT" | jq -r '.text_selector_probe_miss // "fail"')"
    R_TEXT_HIT="$(echo "$C6R_OUT" | jq -r '.text_selector_probe_hit // "fail"')"

    # ---- Phase 5: aggregate + 12-field JSON ---------------------------------
    RUNNER_TAP_SMOKE="fail"
    [[ "$HEALTH" == "200" ]] && RUNNER_TAP_SMOKE="ok"

    HOSTID_SMOKE="fail"
    if [[ "$HOSTID_HEALTH" == "200" && "$HOSTID_TAP" == "ok" \
          && "$HOSTID_MISS" == "404" && "$HOSTID_HIT" == "200" ]]; then
      HOSTID_SMOKE="ok"
    fi

    TAP_TEXT_E2E="fail"
    if [[ "$CASE_PASSED" == "1" && "$CASE_FAILED" == "0" \
          && "$PROBE_MISS" == "404" && "$PROBE_HIT" == "200" ]]; then
      TAP_TEXT_E2E="ok"
    fi

    RUNNER_TREE_E2E="fail"
    if [[ "$TREE_BOUNDS_OK" == "ok" && "$TREE_NODE_COUNT" -ge 50 ]]; then
      RUNNER_TREE_E2E="ok"
    fi

    RESOLVER_ID_E2E="fail"
    if [[ "$R_ID_MISS" == "404" && "$R_ID_HIT" == "ok" ]]; then
      RESOLVER_ID_E2E="ok"
    fi

    # role / text raw output (NOT in PASS gate; sparse-tree regression推 v0.7).
    RESOLVER_ROLE_E2E="info"
    if [[ "$R_ROLE_MISS" == "404" && "$R_ROLE_HIT" == "ok" ]]; then
      RESOLVER_ROLE_E2E="ok"
    elif [[ "$R_ROLE_MISS" == "fail" || "$R_ROLE_HIT" == "fail" ]]; then
      RESOLVER_ROLE_E2E="regression"
    fi
    RESOLVER_TEXT_E2E="info"
    if [[ "$R_TEXT_MISS" == "404" && "$R_TEXT_HIT" == "ok" ]]; then
      RESOLVER_TEXT_E2E="ok"
    elif [[ "$R_TEXT_MISS" == "fail" || "$R_TEXT_HIT" == "fail" ]]; then
      RESOLVER_TEXT_E2E="regression"
    fi

    PASS=0
    if [[ "$SWIFT_N" -ge 102 \
          && "$TS_N" -ge 264 \
          && "$TYPECHECK" == "ok" \
          && "$DOCTOR_N" -ge 6 \
          && "$HOSTID_PROBE" == "ok" \
          && "$HEALTH" == "200" \
          && "$RUNNER_TAP_SMOKE" == "ok" \
          && "$HOSTID_SMOKE" == "ok" \
          && "$TAP_TEXT_E2E" == "ok" \
          && "$RUNNER_TREE_E2E" == "ok" \
          && "$AXP_OK" == "ok" \
          && "$RESOLVER_ID_E2E" == "ok" ]]; then
      PASS=1
    fi

    # diagnostic tail to stderr if any gate failed.
    if [[ "$PASS" != "1" ]]; then
      {
        echo "--- v03-acceptance: gate failure ---"
        echo "swift_tests=$SWIFT_N (want >= 102)"
        echo "ts_tests=$TS_N (want >= 264)"
        echo "typecheck=$TYPECHECK (want ok)"
        echo "doctor_checks=$DOCTOR_N (want >= 6)"
        echo "hostid_probe=$HOSTID_PROBE (want ok)"
        echo "runner_health=$HEALTH (want 200)"
        echo "runner_tap_smoke=$RUNNER_TAP_SMOKE (want ok)"
        echo "hostid_digitizer_smoke=$HOSTID_SMOKE (want ok)"
        echo "tap_text_selector_e2e=$TAP_TEXT_E2E (want ok)"
        echo "runner_tree_e2e=$RUNNER_TREE_E2E (want ok; node_count=$TREE_NODE_COUNT bounds=$TREE_BOUNDS_OK)"
        echo "axp_probe=$AXP_OK (want ok)"
        echo "resolver_id_e2e=$RESOLVER_ID_E2E (want ok; miss=$R_ID_MISS hit=$R_ID_HIT)"
        echo "resolver_role_e2e=$RESOLVER_ROLE_E2E (info-only; miss=$R_ROLE_MISS hit=$R_ROLE_HIT)"
        echo "resolver_text_e2e=$RESOLVER_TEXT_E2E (info-only; miss=$R_TEXT_MISS hit=$R_TEXT_HIT)"
      } >&2
    fi

    printf '{"swift_tests":%s,"ts_tests":%s,"typecheck":"%s","doctor_checks":%s,"hostid_probe":"%s","runner_health":%s,"runner_tap_smoke":"%s","hostid_digitizer_smoke":"%s","tap_text_selector_e2e":"%s","runner_tree_e2e":"%s","axp_probe":"%s","resolver_id_e2e":"%s","resolver_role_e2e":"%s","resolver_text_e2e":"%s"}\n' \
      "$SWIFT_N" "$TS_N" "$TYPECHECK" "$DOCTOR_N" "$HOSTID_PROBE" \
      "$HEALTH" "$RUNNER_TAP_SMOKE" "$HOSTID_SMOKE" "$TAP_TEXT_E2E" \
      "$RUNNER_TREE_E2E" "$AXP_OK" "$RESOLVER_ID_E2E" \
      "$RESOLVER_ROLE_E2E" "$RESOLVER_TEXT_E2E"

    [[ "$PASS" == "1" ]] && exit 0 || exit 1
    ```

  **关键不变量**：
  1. **gate 12 字段，输出 14 字段**：JSON 多出 `resolver_role_e2e` + `resolver_text_e2e` 信息字段（"ok" / "regression" / "info"），方便 v0.4-v0.7 监测 sparse-tree regression；PASS 不依赖它们
  2. **swift/TS count 用 `>=` gate**（与 v02 同），v0.4+ 加测试不破
  3. **doctor 6 = v0.2 5-check + v0.3 C2 axp 第 6 check**（既有 doctor 已实现）
  4. **单行 JSON 出 stdout；诊断 tail 出 stderr**（与 v02 同）
  5. **任一 gate fail → 整体 exit 1**；JSON 仍出（含 "fail"），CI 可解析
  6. **脚本不重新 build**（c6-e2e / hostid-smoke / runner-tree-smoke 各自串行 + 各自 cleanup trap）
  7. **dev-sim 优先 `.simx/dev-sim.txt`**（沿用现有约定；`SIMX_UDID` env 可覆盖）
  8. **不并行启 runner**（4 个 e2e endpoint 各自启自家 xcodebuild test → 各自 cleanup；v0.7 lifecycle minor 后合并）

- 文件 2（改）：`README.md`
  - **改 Status 段**（line ~29-40）：
    - "**v0.2** — driver real-tap shipped..." → "**v0.3** — selector resolver (4 base + 8 modifiers) + AX read shipped."
    - "v0.2 closing smoke `bash scripts/simx-v02-acceptance.sh` exits 0..." → "v0.3 closing smoke `bash scripts/simx-v03-acceptance.sh` exits 0 on a 14-field JSON (12 gated + 2 info)."
    - "Selector resolver ... are the v0.3 milestone." → "SDK behaviour completeness (`visibleElements` / `screenshot` / `suggestions` real fill / `.simx/trace/` output) is the v0.4 milestone."
  - **改 src 树图**（line ~43-55）：
    - `driver/` 行：`Driver interface + SimctlDriver (real tap path)` → `Driver interface + SimctlDriver (real tap + tree + findOne/findAll + waitFor real impl)`
    - 加 `core/resolve-selector.ts` 提到（4 base + 8 modifiers 真函数）
    - `sim/` 行：加 `tree-source / AXP probe`
  - **改 Example 段** Example code block（line ~59-72）：在 `app.fill(...)` 后加一行注释：
    ```typescript
    // Note: app.fill (HID keyboard) lands in v0.7+; the selector
    // forms above (text / id / role+name) all work today.
    ```
  - **改 examples 段**（line ~74-76）：加一行：
    `Selector showcase: see [\`examples/login-tap.test.ts\`](./examples/login-tap.test.ts) for v0.3 selector forms running against real Settings UI.`
  - **改 Local dev 段**（line ~196-227）：
    - "Run the v0.2 closing smoke" → "Run the v0.3 closing smoke"
    - "exits 0 with a single-line JSON of 9 fields" → "exits 0 with a single-line JSON of 14 fields (12 gated + 2 info)"
    - `bash scripts/simx-v02-acceptance.sh` → `bash scripts/simx-v03-acceptance.sh`
    - **v02 命令保留为辅助**：加一行 "(For the v0.2 sub-gate alone, run `bash scripts/simx-v02-acceptance.sh`.)"
  - **改 Roadmap 段**（line ~229-238）：
    - `v0.2 (done)` 保留
    - `v0.3 (next)` → `v0.3 (done) selector resolver (4 base + 8 modifiers, all spatial + index) + AX read (XCUITest snapshot via runner /tree; AXP host-side probe via simx-host-hid axp-probe)`
    - `v0.4-v0.7` → `v0.4 (next) SDK behaviour completeness — matcher failure context, .simx/trace/, waitFor improvements; v0.5-v0.7 — see docs/roadmap.md`
  - **不改**：Authoring guide for AI agents 整段（line ~80-191）—— v0 锁定的 DSL 表面文档；v0.3 不引新 selector / action 语义

- 文件 3（改）：`docs/roadmap.md`
  - 改表 line 10-18：
    - v0.3 `🔥 hot` → `✅ done`
    - v0.4 `❄️ cold` → `🔥 hot`
  - 改"当前所在位置"段（line 33-36）：
    - "正在做：v0.3" → "正在做：v0.4（v0.3 已 close — `plan-history/v0.3-c7-hot.md`；详细见 `docs/plan-cold/v0.4.md`）"
    - **注**：v0.4 cold plan 由 main convo 在 C7 close 后创建（CLAUDE.md §6 fence；S2 此项**不**创建 v0.4 plan-cold；只在 roadmap 内 ref 路径）—— 如 main convo 担心 v0.4 plan-cold 不存在时 roadmap 引用看起来悬空，可改成 "正在做：v0.4（v0.3 已 close；v0.4 plan-cold 即将由 main convo 创建）"

- 跑（绿）：
  ```bash
  cd /Users/doracawl/workspace/labs/lab15-autofix
  chmod +x scripts/simx-v03-acceptance.sh
  bun run typecheck 2>&1 | tail -3   # exit 0
  bun run test 2>&1 | grep -E 'Tests +[0-9]+ passed' | tail -1   # 264 passed
  bash scripts/simx-v03-acceptance.sh   # 真 dev sim 端到端
  echo "acceptance-exit=$?"
  ```
- 期望 **绿**：
  - typecheck exit 0
  - `Tests 264 passed (264)`
  - `acceptance-exit=0`
  - acceptance JSON 单行（见 §"出口验收 JSON schema"）

**重构**

- 不抽 v02 / v03 acceptance script 共用 helper（合并是 v0.7 lifecycle minor 工作；v02 acceptance 保留作 v0.2 子 gate）
- 不在 acceptance 内 measure tap 延迟 P50（与 v0.2 C7 决策日志已记 — instrument 推 v0.7 hardening）
- 不动 examples/screenshot-only.test.ts / examples/tap-text-selector.test.ts

---

## Checkpoint C7 验收

### 三类：文件结构（reorg + 新 example）+ 不退化（swift 102 / TS 264 / typecheck）+ 出口（v03-acceptance 12 字段 gate）

```bash
cd /Users/doracawl/workspace/labs/lab15-autofix

# ---- 1. 文件结构（S1 落地） -----------------------------------------------
test -f examples/login-tap.test.ts                                              # 新
test -f examples/screenshot-only.test.ts                                        # 不动
test -f examples/tap-text-selector.test.ts                                      # 不动
test -f examples/_v03-pending/login.test.ts                                     # 保留
test -f examples/_v03-pending/cart-checkout.test.ts                             # 保留
test -f examples/README.md                                                      # 改
test -f examples/_v03-pending/README.md                                         # 改
grep -q "login-tap.test.ts" examples/README.md
grep -q "v0.7" examples/_v03-pending/README.md

# ---- 2. acceptance 脚本 ---------------------------------------------------
test -x scripts/simx-v03-acceptance.sh

# ---- 3. docs 同步 ---------------------------------------------------------
grep -q "v0\.3" README.md
grep -q "simx-v03-acceptance\.sh" README.md
grep -qE "v0\.3.*✅ done" docs/roadmap.md
grep -qE "v0\.4.*🔥 hot" docs/roadmap.md

# ---- 4. swift / TS / typecheck 0 退化 -------------------------------------
swift test --package-path swift-bridge 2>&1 | grep -E 'Executed +[0-9]+ tests' | tail -1
# 期望含：Executed 102 tests, with 0 failures（**完全不变**）

bun run typecheck 2>&1 | tail -3   # exit 0

bun run test 2>&1 | grep -E 'Tests +[0-9]+ passed' | tail -1
# 期望含 'Tests 264 passed (264)'（**完全不变**）

# ---- 5. v0.2 acceptance 仍绿（C7 不破 v0.2 出口） -------------------------
bash scripts/simx-v02-acceptance.sh | jq -e '
  .swift_tests >= 63 and .ts_tests >= 125 and .typecheck == "ok" and
  .doctor_checks >= 5 and .hostid_probe == "ok" and
  .runner_health == 200 and .runner_tap_smoke == "ok" and
  .hostid_digitizer_smoke == "ok" and .tap_text_selector_e2e == "ok"'

# ---- 6. v0.3 acceptance 12 字段 gate 全 pass + 14 字段输出 ---------------
bash scripts/simx-v03-acceptance.sh > /tmp/v03-acceptance.json 2> /tmp/v03-acceptance.err
echo "acceptance-exit=$?"
jq -e '
  # offline gates (5)
  .swift_tests >= 102 and
  .ts_tests >= 264 and
  .typecheck == "ok" and
  .doctor_checks >= 6 and
  .hostid_probe == "ok" and
  # v0.2 e2e gates (4)
  .runner_health == 200 and
  .runner_tap_smoke == "ok" and
  .hostid_digitizer_smoke == "ok" and
  .tap_text_selector_e2e == "ok" and
  # v0.3 new gates (3)
  .runner_tree_e2e == "ok" and
  .axp_probe == "ok" and
  .resolver_id_e2e == "ok" and
  # info-only fields exist (不 gate 值)
  (.resolver_role_e2e | type) == "string" and
  (.resolver_text_e2e | type) == "string"' /tmp/v03-acceptance.json

# ---- 7. src/ + swift-bridge/Sources/ 0 字节改动 ---------------------------
! find src -name '*.ts' -newer docs/plan-history/v0.3-c6-hot.md -print 2>/dev/null | head -1
! find swift-bridge/Sources -name '*.swift' -newer docs/plan-history/v0.3-c6-hot.md -print 2>/dev/null | head -1

# ---- 8. CLAUDE.md §9 不变量 ----------------------------------------------
! git diff src/driver/types.ts 2>/dev/null | grep -E '^[+-]'   # Driver 接口 0 改动
! git diff src/sim/cell.ts 2>/dev/null | grep -E '^[+-]'        # Cell type 0 改动
! git diff src/core/selector.ts 2>/dev/null | grep -E '^[+-]'   # Selector type 0 改动
```

期望（机器可判断 — 8 类 gate 全部满足，无人工读图）：
- 所有 `test -f` / `test -x` / `grep -q` 文件 / 内容断言 0 退出
- swift test：`Executed 102 tests, with 0 failures`（**完全不变**）
- `bun run test`：`Tests 264 passed (264)`（**完全不变**）
- `bun run typecheck` exit 0
- v02 acceptance jq exit 0（向后兼容）
- v03 acceptance script `exit 0` + JSON 12 gated 字段全 expected + 2 info 字段存在
- `src/`、`swift-bridge/Sources/`、`src/driver/types.ts`、`src/sim/cell.ts`、`src/core/selector.ts` 0 字节改动

## 完成后动作

1. `mv docs/plan-hot.md docs/plan-history/v0.3-c7-hot.md`
2. **追加 1 行决策日志**到 `docs/v1.md` 末尾（main convo 做，sub-agent 不动）：
   ```
   - 2026-05-15 v0.3 C7（收尾）：examples 部分挑回（_v03-pending/login.test.ts → examples/login-tap.test.ts tap-only 子集，展示 v0.3 selector 4 base + inside modifier + waitFor + expect.toBeVisible 真接通 Settings UI）+ `scripts/simx-v03-acceptance.sh` 12 字段 gate + 2 info 字段（resolver_role_e2e / resolver_text_e2e 输出但不 gate，sparse-tree regression 推 v0.7） + README/roadmap 同步。冷计划 C7 字面 "examples/login.test.ts 完整跑通含 fill" 中 fill 字段推 v0.7+（HID 键盘 + runner /type-text endpoint）；cart-checkout.test.ts 整文件推 v0.4-v0.5（longPress/scroll/pasteboard/deeplink）；iOS 26.4 SimXcui sparse-tree regression 推 v0.7 hardening（C6 决策日志已记）。**v0.3 close**：102 swift / 264 TS / 6-check doctor (xcode/runtimes/claude/bun/hid/axp) / 双 transport（runner /tap + host-hid digitizer）+ /tree + AXP probe + id-selector resolver-tap 真 UI 跳页 全绿。
   ```
3. main convo 创建 `docs/plan-cold/v0.4.md`（v0.4 = SDK 行为完备：matcher 失败上下文真填充 + `.simx/trace/` 输出 + `waitFor` 改进 + Cell L2 操作隔离起点）
4. main convo 调 sub-agent 生成 v0.4 C1 `plan-hot.md`（CLAUDE.md §6 标准 prompt + v0.4 cold 计划已存在）

---

## 关键技术决策（在 plan 里现场判定 — 决策影响 step 形态）

| # | 决策点 | 选 | 理由 |
|---|---|---|---|
| 1 | **C7 真正做什么？(A)/(B)/(C)/(D) 候选** | **(D) = (A) + (C)** | (A) login.test.ts tap-only 子集挑回 + (C) acceptance script + 文档同步。(A) 单独 = v0.3 closure 无单一证据；(C) 单独 = v0.3 selector 形态在 examples 顶层仍空白，AI 测试作者错以为 v0.3 没新东西；(B) 新写 selector-showcase 与 v1.md §4 [1] 字面 spec 偏离（login.test.ts 是字面 reference target），重复工。决策记入 v1.md 决策日志。 |
| 2 | **acceptance script 命名 = `simx-v03-acceptance.sh` 单独 vs 通用 `simx-acceptance.sh`** | **`simx-v03-acceptance.sh` 单独**（与 v02 并存） | (1) v02 acceptance 是 v0.2 sub-gate 字面证据，CI / 后续 AI 解析按字面 JSON schema 工作，rename 会破已有 path；(2) v0.2 9 字段是 v0.3 12 字段的子集，v03 acceptance 既出 12 字段也涵盖 v02 9 字段——v02 acceptance 是 v0.2 closure 历史 marker，v03 是 v0.3 closure 真出口；(3) 通用化 `simx-acceptance.sh` 推 v0.5（其时 minor 增量稳定 / Cell L2 + matcher 失败上下文 / trace 都需要 version-agnostic gate）—— v0.3 不通用化避免每 minor 都 break 已存在脚本 path。 |
| 3 | **examples 处置 = 挑回 / 新写 / 都做** | **(A) 挑回 tap-only 子集为 `login-tap.test.ts`**（不新写） | login.test.ts 是 v1.md §4 SuccessCriteria [1] 字面 reference target（"AI 0-shot 写出跑通的 examples/login.test.ts 等价测试"）。挑出 tap-only 子集 = 真接近字面 spec；v0.7+ HID 键盘 fill 后能无缝合并回 `login.test.ts`（rename `login-tap.test.ts` 删 + 补 fill case 即可）。新写 `selector-showcase.test.ts` 是重复工 + 远离字面 spec。**cart-checkout 不动**：longPress / scroll / pasteboard / deeplink 全在 v0.4-v0.5 路线图上，C7 时 0 字段可用——不挑回任何子集。 |
| 4 | **iOS 26.4 sparse-tree regression 在 C7 修 vs 推 v0.7** | **推 v0.7 hardening** | (1) C7 是版本**收口** —— 引入新 driver 层逻辑（如 1-shot retry / app.describe 兜底）= 违反 closing 范式（v0.2 C7 已确立 "C7 0 src 改动" 形态）；(2) sparse-tree 是 SimXcui 上游问题（C6 决策日志已记）—— driver 单测层 22 case 在 mock tree 上已独立证明 driver 正确性；(3) v0.7 hardening 是修 runner / SimXcui 上游 issue 的合理 home（plan-cold/v0.7 含 "长跑稳定 + 每 50 case 重启 runner"，与 sparse-tree 同形态）；(4) acceptance gate 仅 id selector phase（C6 Phase B 全通），role/text e2e 字段**输出**但不 gate（信息保真 + 不阻塞 v0.3 closure）；(5) v0.7 修后 acceptance 加 `resolver_role_e2e` + `resolver_text_e2e` 进 gate（向后兼容字段名，仅 PASS 判定升级）—— "info-only → gated" 是 forward-compatible 升级路径。 |
| 5 | **acceptance JSON schema 字段命名与数量** | **14 字段输出 / 12 gated**（v02 9 + v0.3 3 + 2 info） | (1) v02 9 字段全部保留（向后兼容，字段名一致）；(2) v0.3 新增 3 gated 字段 `runner_tree_e2e` / `axp_probe` / `resolver_id_e2e`，每个 = v0.3 一个 checkpoint 的字面 endpoint（C1 /tree + C2 AXP probe + C6 id selector resolver-tap）；(3) 加 2 个 info-only 字段 `resolver_role_e2e` + `resolver_text_e2e`，监测 sparse-tree regression 状态（"ok"/"regression"/"info"），v0.7 修复后可升级为 gated；(4) **不**叠 latency 字段（与 v0.2 C7 决策同 — instrument 推 v0.7）。 |
| 6 | **README 是否重写 Authoring guide for AI agents** | **不重写** | Authoring guide (line 80-191) 是 v0 锁定的 DSL surface 文档；v0.3 不引新 selector / action 语义（selector type 在 C4/C5 已锁，action surface 在 v0 锁），guide 内容仍准确。fill / longPress 等 action 表 list 已标 "surface above is exhaustive...write the test as if it existed and surface the gap"——C7 不删 fill 行（它仍是合法的 surface API，只是 driver 未实现，与 cart-checkout 用的 scrollTo / pasteboard 同形态）。改 = scope creep。**唯一改动**：Example code block 内 `app.fill(...)` 行加 1 行注释说明 fill v0.7+。 |
| 7 | **roadmap.md 是否在 C7 改 v0.4 状态** | **改：v0.4 → 🔥 hot** | C7 close 后立即 main convo 调 sub-agent 生成 v0.4 C1 plan-hot.md（CLAUDE.md §6 触发条件）；roadmap.md 在 C7 close 里同改 = 状态信息原子化更新（避免 "v0.3 done 已写、v0.4 hot 未写" 的中间态被外部观察）。**v0.4 cold plan 由 main convo 创建**（sub-agent 不预先展开下一 minor；§完成后动作 已写明）。 |
| 8 | **是否动 `docs/v1.md` 决策日志** | **本 plan 不动；main convo 在 C7 close 后追加** | CLAUDE.md §10 决策记录由 main convo 维护，sub-agent 写 plan-hot 不动 v1.md（明确边界，与 v0.2 C7 同形态）；本 plan §"完成后动作"段写明 main convo 要追加的决策日志字面。 |
| 9 | **是否引入 dev sim 自动创建脚本 / dep / swift 改动** | **不** | C7 是 closure；引新 dep / 改 swift = scope creep（与 v0.2 C7 同形态）。新 example 只用 `..src/sdk/index.js` import（已有，不引新依赖）；acceptance script 串既有 4 个 endpoint script，0 新 binary。 |
| 10 | **新 example bundle id 选择** | **`com.apple.Preferences`（沿用 runner auto-launch）** | 与 `tap-text-selector.test.ts` 同 sim app；runner XCUITest target 在 SimxRunnerUITests/setUp 阶段 attached 到 Preferences，example 不写 `app.launch(...)` 避免与 runner 抢 PID（v0.2 C6 决策日志已记 `fresh:true` 漂移问题）。Settings 是预装 app（与 Calculator 不同），iOS 26.4 sim 100% 可用。 |

---

## 出口验收 JSON schema

acceptance 脚本 stdout 单行 JSON（机器解析友好，字段固定 14 个，前 12 个 gated，后 2 个 info-only；顺序固定）：

| 字段 | 类型 | gate / info | pass 值 | 覆盖 checkpoint | 来源 endpoint |
|---|---|---|---|---|---|
| `swift_tests` | number | gate | `>= 102` | v0.2 C3-C5 swift + v0.3 C1/C2 swift (102 现) | `swift test --package-path swift-bridge` |
| `ts_tests` | number | gate | `>= 264` | v0.1 + v0.2 + v0.3 全 TS 单测 (264 现) | `bun run test` |
| `typecheck` | string | gate | `"ok"` | TS strict + examples（含新 login-tap.test.ts）通编译 | `bun run typecheck` |
| `doctor_checks` | number | gate | `>= 6` | v0.2 5 + v0.3 C2 axp = 6 | `bun src/cli/index.ts doctor` 数 `✓` 行数 |
| `hostid_probe` | string | gate | `"ok"` | v0.2 C5 simx-host-hid probe（digitizer + indigo9） | `simx-host-hid probe \| jq .ok` |
| `runner_health` | number | gate | `200` | v0.2 C1 SimxRunner /health | c6-e2e 内 |
| `runner_tap_smoke` | string | gate | `"ok"` | v0.2 C2 SimxRunner /tap | c6-e2e (health=200 即 pass) |
| `hostid_digitizer_smoke` | string | gate | `"ok"` | v0.2 C4 IOHIDEvent digitizer 真 UI 跳页 | `simx-hostid-digitizer-smoke.sh` |
| `tap_text_selector_e2e` | string | gate | `"ok"` | v0.2 C6 SDK→Driver→RunnerClient→/tap 真 UI 跳页 | `simx-c6-tap-e2e.sh` |
| `runner_tree_e2e` | string | **NEW gate** | `"ok"` | v0.3 C1 SimxRunner GET /tree → A11yNode JSON（node_count ≥ 50 + bounds.w/h > 0） | `simx-runner-tree-smoke.sh` |
| `axp_probe` | string | **NEW gate** | `"ok"` | v0.3 C2 host-side AXP probe（framework / classes / selectors / sharedInstance 全 ok） | `simx-host-hid axp-probe` |
| `resolver_id_e2e` | string | **NEW gate** | `"ok"` | v0.3 C6 id selector → resolveSelector → host-hid coord tap 真触发 General 子页 | `simx-c6-resolver-tap-smoke.sh` (id phase only) |
| `resolver_role_e2e` | string | **info** | `"ok"` / `"regression"` / `"info"` | v0.3 C6 role+name selector phase（sparse-tree regression 推 v0.7 修） | `simx-c6-resolver-tap-smoke.sh` (role phase, 不 gate) |
| `resolver_text_e2e` | string | **info** | `"ok"` / `"regression"` / `"info"` | v0.3 C6 text+modifier selector phase（同上） | `simx-c6-resolver-tap-smoke.sh` (text phase, 不 gate) |

**示例 pass 输出**（单行；role/text info 字段允许 "ok" 或 "regression"，均不影响 PASS）：
```json
{"swift_tests":102,"ts_tests":264,"typecheck":"ok","doctor_checks":6,"hostid_probe":"ok","runner_health":200,"runner_tap_smoke":"ok","hostid_digitizer_smoke":"ok","tap_text_selector_e2e":"ok","runner_tree_e2e":"ok","axp_probe":"ok","resolver_id_e2e":"ok","resolver_role_e2e":"regression","resolver_text_e2e":"regression"}
```

**任一 gated 字段 fail** → 整脚本 exit 1 + 该字段值为 `"fail"` / `0` / 非期望数字；stderr 含错误尾部。**info 字段 = "regression"** 不影响 exit code，但 v0.7 hardening 时会被监测脚本扫到（forward-compatible 升级 hook）。

---

## v0.3 整体出口验收覆盖矩阵（C1-C7 全 7 个 checkpoint）

| Checkpoint | 一句话 | acceptance JSON 字段 覆盖 |
|---|---|---|
| **C1** XCUITest snapshot 路径（GET /tree A11yNode JSON） | `runner_tree_e2e` + `swift_tests`（C1 +19 swift） |
| **C2** AXP host-side probe + doctor 加 axp check | `axp_probe` + `doctor_checks`（axp 行 = 第 6 check） + `swift_tests`（C2 +20 swift） |
| **C3** A11yNode wire 协议固化 + role 翻译 + A11yTreeSource interface | `ts_tests`（C3 +21 TS）+ 间接覆盖 `runner_tree_e2e`（tree 出口 schema 一致） |
| **C4** Selector resolver 4 base + regex 支持（pure TS） | `ts_tests`（C4 +42 TS）+ 间接覆盖 `resolver_id_e2e`（resolveSelector 是 driver.findOne 内部） |
| **C5** Selector resolver 8 modifiers（pure TS 几何 + 索引） | `ts_tests`（C5 +34 TS = 76 in-file）+ 间接覆盖 `resolver_id_e2e` |
| **C6** SDK / Driver 接通 selector resolver（4 stub 真实现 + tap 双路径） | `resolver_id_e2e` + `ts_tests`（C6 +27 TS = 264 现） |
| **C7** examples 部分挑回 + acceptance script + README/roadmap | acceptance 脚本本身 exit 0 + 14 字段全 pass = C7 自证 |

**14 个字段 ↔ 7 个 checkpoint** 的映射是 7 → 14 多对多，但 1-1 覆盖：**任一 checkpoint 退化 → 至少 1 个 gated field fail**。这是 v0.3 "出口验收" 段的字面机器化。

---

## 与冷计划假设的对照（CLAUDE.md §6 拒绝热化清单核查）

| 冷计划假设 | 本机实际 | 对策 |
|---|---|---|
| C7 = "examples/login.test.ts 完整跑通（含 selector + tap + fill）" | fill 走 HID 键盘 / runner /type-text endpoint，工程量 ≥ 5 swift + 6 ts step（违反 1-3 step） + 明确归 v0.7 路线 | **决策 #3 拆分**：tap-only 子集挑回为新 `login-tap.test.ts`（v0.3 真跑通，selector + tap + waitFor + expect.toBeVisible 真接通 Settings UI）；fill 整 case 留 _v03-pending 等 v0.7+（决策日志记一行）|
| C7 出口 "AX 树 100 节点 resolve P50 < 20ms" | metric instrument 需 trace dir 真落地 + selector resolver TS 侧落（C5 已记 geometry TS 侧）；trace dir 推 v0.4 | **推 v0.4**（与 v0.2 C7 latency P50 同形态 — instrument 推后）|
| C7 出口 "bun src/cli/index.ts run examples/login.test.ts" 真跑通 | login.test.ts 含 fill 整 case 不可跑（fill 未实现） | **拆分**：v0.3 跑 `examples/login-tap.test.ts`（tap-only 子集，acceptance Phase 4 内 `resolver_id_e2e` 间接证），login.test.ts 整文件留 v0.7+ |
| 冷计划 §已知风险 "AX 树深度限制 60 层" | C1 已落 60 层静默截断 + stderr warn | **吸收**（C1 决策延续；不依赖 visibleElements 真填充，那推 v0.4）|
| 冷计划 §C7 不写"sparse-tree regression"风险 | iOS 26.4 SimXcui 在 back-button tap 后 ~25s 返回 sparse tree（C6 已踩 + 决策日志已记）| **推 v0.7 hardening**（决策 #4；acceptance role/text 字段输出但不 gate，forward-compatible）|
| iOS 26.4 runtime 存在 | ✓ Xcode 26 / iOS 26.4 isAvailable / dev sim Booted | — |
| C1-C6 全部完成 | ✓ 102 swift + 264 TS + 双 transport tap 真 UI 跳页 + tree + AXP probe + selector resolver pure TS 全 | — |
| 任何私有 framework 链接发现要硬链接 → stop 回报 | C7 完全不碰 Swift / framework | — |

**结论**：与冷计划假设有 **4 处合理偏差**（决策 #1 fill 拆分到 v0.7+、#2 latency P50 → v0.4、#3 login.test.ts 拆 tap-only 子集、#4 sparse-tree → v0.7），均为**范围合理化**（C6 决策已铺路 + v0.7 hardening 接住）而非"假设错误"——属于 v0.3 closing 必须的现实调整。**4 处都在 main convo 决策日志补一行**（CLAUDE.md §10）。**不**拒绝热化。

---

## 不变量（C7 范围内必满足；超出范围视为退化）

1. **CLAUDE.md §9.1 只支持模拟器**：C7 任何路径不引真机代码（acceptance 脚本 host=127.0.0.1，sim udid only）
2. **CLAUDE.md §9.3 不暴露 xpath / 坐标 selector**：C7 不改 selector type；新 login-tap.test.ts 形态严格 selector 对象，0 坐标 / 0 xpath
3. **CLAUDE.md §9.4 不提供裸 sleep**：acceptance 脚本内 phase 间 sleep / animation settle 仅在 shell wrapper —— TS 源 0 sleep 改动（沿用 v0.2 C7 既有 pattern）
4. **CLAUDE.md §9.5 失败信息 AI-readable**：acceptance fail → stdout JSON 字段标 `"fail"` + stderr 含 tail（CI / AI 0-shot 修复 prompt 友好）
5. **CLAUDE.md §9.7 4 层信息结构始终保持**：本计划写入 → `plan-hot.md`（C7 唯一）+ `roadmap.md` + `v1.md` + `plan-cold/v0.3.md`（C7 close 后 v0.3 不再修） + `plan-cold/v0.4.md`（C7 close 后由 main convo 创建）
6. **Driver 接口签名 0 改动**（`src/driver/types.ts` byte-identical；C7 不动）
7. **Swift bridge `swift-bridge/Sources/**` 0 字节改动**（C1-C6 已稳，C7 是 docs + examples + scripts 收口）
8. **既有 102 swift tests + 264 TS tests 一个都不退化**（**0 增 0 减**，C7 完全不写 / 不删 src test）
9. **测试纪律 §8.4**：不引 vitest 新 test；acceptance 是 shell endpoint，不与 vitest 混；新 login-tap.test.ts 是 simx SDK runner test（不是 vitest），与 `tap-text-selector.test.ts` 同形态
10. **范围纪律 §8.1**：不做 fill / longPress / scroll / pasteboard / system.openUrl / trace dir 真输出 / visibleElements 真填充 / suggestions edit-distance / Cell L2 / 真 5-arg iOS 17/18 / runner lifecycle / latency metric / sparse-tree regression 修复
11. **真验证用 dev sim**（`.simx/dev-sim.txt` 锁定 UDID）；acceptance 不 kill 用户其它 sim
12. **依赖纪律 §8.7**：0 新 npm dep；0 新 SPM dep；0 新 brew/系统 dep；C7 不引任何工具链改动
13. **examples/_v03-pending 不被 typecheck 扫**（tsconfig.examples.json exclude 既存 `examples/_v03-pending/**`；C7 不改）
14. **新 login-tap.test.ts 被 typecheck 扫**（tsconfig.examples.json include `examples/**/*.ts` 自动覆盖顶层；C7 不改 tsconfig）
15. **README 内 Authoring guide for AI agents 段不动**（v0 锁定的 DSL surface 文档）

## 是否拒绝热化

**不拒绝**。

- 全 9 条入口条件 ✓（前置条件段已逐条本机探测：102 swift / 264 TS / typecheck ok / v02 acceptance 全 9 字段 ok / 4 v0.3 子 endpoint 全 work / dev sim Booted / xcode 26 / iOS 26.4 / 文件层 anchors 全在位 + login-tap.test.ts 起步未存在）
- 与冷计划假设的 4 处合理偏差均已识别 + 列入"与冷计划假设的对照"段 + 决策表 #1/#3/#4 详记 + main convo 决策日志追加路径已写明
- C7 范围完全在 v1.md 边界内（C7 不引 v1.md §3 "明确不做"任何一项；fill / longPress / pasteboard / deeplink 全显式推 v0.4-v0.7）
- C6 close 时遗留的 sparse-tree regression 已分析：(1) driver 单测层 22 case 独立证明正确性；(2) C7 acceptance 只 gate id selector phase（C6 Phase B 全通）；(3) role/text 字段输出但不 gate（forward-compatible 升级 hook）；(4) 修复 home 在 v0.7 hardening minor —— **不**影响 C7 closure
- C7 2 个 step 线性、无分叉、每步红绿都机器可判断
- Checkpoint 验收命令 8 类 gate 全部 single-line shell + jq 可判断（CLAUDE.md §5 满足）
- **0 新 dep / 0 swift 改动 / 0 src/ 实现改动** = 最纯的 "version closing" 形态（与 v0.2 C7 范式一致）
