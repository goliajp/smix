# plan-hot — v0.6 C3：4 个观察 MCP tool（`screen_describe` / `screen_screenshot` / `screen_hierarchy` / `element_inspect`）接通既有 `SimctlDriver`（`describe` / `screenshot` / `tree` / `findOne`）

> [3/4 信息层] 当前唯一热计划。**C3 是 v0.6 第三个 checkpoint，也是 MCP 表面第一次暴露 a11y / 截图能力**——通过后世界变成：`bun src/cli/index.ts mcp` 字面起 stdio MCP server 同时 expose **12 个 tool**（C1 `ping` + C2 7 lifecycle + C3 新加 4 observe），4 个新 tool 接到既有 `Driver` 接口的 4 method（`screenshot` / `describe` / `tree` / `findOne`），通过 `tools/list` 返 12 element array、`tools/call` 真调 driver 返结构化 JSON 结果；驱动获取通过 `ToolContext` 扩 `acquireDriver(udid): Promise<Driver>` lazy factory（决策 1.B）+ 进程内 udid → Driver 缓存（决策 2.A）；每 tool 含 zod input schema strict + JSON Schema literal 输出（C2 既有形态延续）+ async handler 路由到注入式 driver；错误（driver acquire 失败 / driver method throw / selector 解析失败）一律走 SDK `isError: true` content 路径（C2 决策 10.A 延续）；`element_inspect` 元素未找到 = `{node: null}`（不视为 error，是合法查询结果——决策 6.A）；`screen_describe` 输出复用既有 `ScreenDescription` 序列化形态（screenshot base64 + elements + frontApp + summary + capturedAt——C7 schema 固化前用 driver 现状直出，决策 8.A）；C1+C2 既有 29 case 全保不退化，新增 ~16-20 case 覆盖 4 tool happy + error 路径；`docs/v1.md` 决策日志 +1 行 v0.6 C3 close note。
>
> **承接（v0.6 C2 close 后实测 baseline）**：v0.6 C2 — 8 个 tool 字面 expose（`ping` / `simulator_list` / `simulator_boot` / `simulator_shutdown` / `app_launch` / `app_terminate` / `app_install` / `app_uninstall`）+ `ToolDef.inputJsonSchema` per-tool literal + `ToolContext = { client: SimctlClient }` 注入 + `src/mcp/device-resolver.ts` 共享 helper + `simulator_boot` 支持 udid/name 互斥；`scripts/simx-c2-mcp-lifecycle-smoke.sh` 10 字段 JSON gate 全绿。**测试基线 464 TS / 102 swift / typecheck 0 error / 27 字段 v0.5 acceptance 全保 / 21 字段 v0.4 acceptance 全保 / 11 字段 C1 smoke 全保（has_ping 用 index 改写）/ 10 字段 C2 smoke 真本机 dev sim e2e 全绿**（决策日志 2026-05-16 v0.6 C2 close 字面 464 TS）。`docs/plan-history/v0.6-c2-hot.md` 已归档。
>
> **现状盘点（C3 真生效边界）**：
> - **`src/mcp/tools.ts` line 11-13 `ToolContext`** 既有 `{ client: SimctlClient }`。**C3 扩 1 字段**：`acquireDriver: (udid: string) => Promise<Driver>`（决策 1.B——lazy factory；拒 A `driver?: Driver` 单实例绑定 udid 形态——MCP server 必须支持多 udid 同进程切换；拒 C 每 tool 自己 acquire 重复代码；拒 D 推 cell pool——v1.1 范围、远超 C3）。`ToolContext` 改为 `{ client: SimctlClient; acquireDriver: (udid: string) => Promise<Driver> }`。既有 8 tool（C1+C2）handler 0 改动（只读 `ctx.client`），新 4 tool 读 `ctx.acquireDriver`。
> - **`src/mcp/server.ts` line 38-50 `createSimxMcpServer`** 既有 client 注入 + ctx 构造。**C3 扩 1 工厂**：`createSimxMcpServer({ ..., acquireDriver?: (udid) => Promise<Driver> })` 注入点（决策 3.A——可选注入用于测试 mock；默认走 `defaultAcquireDriver(client)` 工厂），默认 factory 内部 `acquireCell({ udid }, { client })` + `new SimctlDriver(cell)` + udid → Driver Map 缓存（决策 2.A）。`createSimxMcpServer` 内 `const acquireDriver = opts.acquireDriver ?? defaultAcquireDriver(client)` + ctx 构造扩 `acquireDriver` 字段；`defaultAcquireDriver` 抽到 `src/mcp/driver-factory.ts` 新文件（决策 4.A——独立文件 ~30-50 行，承载 udid → Driver 缓存 + acquireCell wrap；拒嵌入 `server.ts` 30 行膨胀）。
> - **`src/driver/types.ts` line 50-91 `Driver` interface** 既有 29 method。**C3 用 4 method**：`screenshot()` / `describe()` / `tree()` / `findOne(selector, opts?)`。**0 改动 Driver interface**（决策 5.A——C3 完全只读 Driver 现状；拒扩 interface 添加 `inspect()` 等便利 method；拒触碰 29 method 任一签名）。`SimctlDriver` 真实现已就绪：`describe()` 返 `ScreenDescription`（base64 + 50 elements DFS pre-order）；`screenshot()` 返 `Buffer`；`tree()` 调 treeSource.getTree() 真返 A11yNode；`findOne()` 调 `resolveSelector(tree, selector)` 真返 A11yNode|null。
> - **`src/core/screen.ts` line 14-30 `A11yNode`** 既有 wire 形态 — `{rawType, role?, identifier?, label?, value?, text?, bounds, enabled, selected, hasFocus, visible, children}` 全 JSON-serializable（含 children 递归数组）。**C3 不改 A11yNode**（决策 8.A——直接 `JSON.stringify` 返；C7 schema 固化前用现状）。`ScreenDescription` 同（`{screenshot: base64, elements: ElementSummary[], frontApp, summary, capturedAt}`）。
> - **`src/core/selector.ts` `Selector` 4 base + 8 modifier** 既有。**C3 `element_inspect` 接受 selector input**：zod schema 4 base oneOf（text|id|label|role+name）+ 8 modifier optional + 限制 `text` / `name` 只支持 `string`（拒 RegExp——RegExp JSON 不可序列化；决策 7.A——v0.6 MCP 表面只 string，RegExp 推 v0.7+ envelope `{$regex: "..."}` 若需要）；递归 modifier `near` / `below` / `above` / `leftOf` / `rightOf` / `inside` 接 sub-selector 同 schema。zod 用 `z.lazy(() => selectorSchema)` 表达递归。
> - **`src/mcp/__tests__/server.test.ts` line 1-495 29 case** 既有 — `makeLinkedClient(opts?)` + `makeMockClient(overrides?)` helper。**C3 加 ~16-20 case**：复用 helper + 注入 mock `acquireDriver`（返 mock Driver 部分 method stub）；每 tool ≥ 3-4 case（happy + error + 边界）。
> - **`src/mcp/index.ts` barrel re-export** 既有 8 tool。**C3 加 4 tool export + 1 工厂**：`screenDescribeTool` / `screenScreenshotTool` / `screenHierarchyTool` / `elementInspectTool` + `defaultAcquireDriver` from `./driver-factory.js`。
> - **新 e2e smoke `scripts/simx-c3-mcp-observe-smoke.sh`**（~150-200 行，决策 11.A）— spawn `bun src/cli/index.ts mcp` 真子进程，喂 7 个 JSON-RPC frame：`initialize` + `initialized notif` + `tools/list`（断 length === 12）+ `tools/call simulator_list arguments={}`（取真 booted udid）+ `tools/call screen_screenshot arguments={udid}`（断 `JSON.parse(content[0].text).base64.length > 1000`）+ `tools/call screen_describe arguments={udid}`（断 parsed `.elements` 是 array + `.screenshot` 是 base64 string）+ `tools/call element_inspect arguments={udid, selector:{text:'Settings'}}`（断 `.node === null || .node.rawType === 'string'`——容忍 element 不存在）；输出 12 字段 JSON CI gate。**`screen_hierarchy` 不进 smoke**（runner 未运行时 driver.tree() 抛 ExpectationFailure；CI 无 runner，加进去就一定 error——决策 12.A 改走 unit 覆盖）。
> - **0 swift / 0 Driver interface / 0 SimctlDriver 改动 / 0 Cell 改动 / 0 SimctlClient 改动 / 0 acquireSession 改动 / 0 SDK 改动 / 0 新 prod dep / 0 新 peer dep / 0 C4-C5 交互/复合 tool / 0 explain_screen / 0 HTTP/SSE transport / 0 trace-sink 激活 / 0 claude 子进程调用 / 0 ScreenDescription serialization schema 固化（C7 范围）**。
>
> **C3 真生效 = 五条**：
> 1. **新建 `src/mcp/driver-factory.ts`**（~30-50 行）：`defaultAcquireDriver(client: SimctlClient): (udid: string) => Promise<Driver>` 工厂——内部 udid → Driver Map 缓存 + `acquireCell({ udid }, { client })` + `new SimctlDriver(cell)`
> 2. **改 `src/mcp/tools.ts`**（~+220-320 行 / +4 ToolDef + ToolContext shape 扩 1 字段 + 1 zod 递归 selectorSchema 定义）：`ToolContext` 加 `acquireDriver: (udid: string) => Promise<Driver>` + 4 个新 ToolDef（`screenDescribeTool` / `screenScreenshotTool` / `screenHierarchyTool` / `elementInspectTool`）+ 每 tool zod input schema + JSON Schema literal + handler（screen_describe 限 limit 默认 50 / screen_hierarchy 默认无 max_depth 限制 / element_inspect 接 selector 递归 schema）；`ALL_TOOLS` 扩为 12 element
> 3. **改 `src/mcp/server.ts`**（~+10-15 行）：`CreateSimxMcpServerOptions` 扩 `acquireDriver?: (udid: string) => Promise<Driver>`；`createSimxMcpServer` 内 `const acquireDriver = opts.acquireDriver ?? defaultAcquireDriver(client)`；ctx 构造扩 `acquireDriver` 字段
> 4. **改 `src/mcp/index.ts`**（+2 export 行）：barrel re-export `screenDescribeTool` / `screenScreenshotTool` / `screenHierarchyTool` / `elementInspectTool` + `defaultAcquireDriver` from `./driver-factory.js`
> 5. **改 `src/mcp/__tests__/server.test.ts`**（~+220-300 行 / +16-20 case）：每 tool ≥ 3-4 case（happy + error + 边界）+ tools/list count 从 8 改 12 + 既有 29 case 全保
> 6. **新建 `scripts/simx-c3-mcp-observe-smoke.sh`**（~150-200 行 12 字段 JSON gate）+ `chmod +x` + 改 `scripts/simx-c1-mcp-smoke.sh` + `scripts/simx-c2-mcp-lifecycle-smoke.sh`（`tools_list_count` / `tools_count` 期望 8 → 12 同步，决策 15）+ **`docs/v1.md`** 决策日志 +1 行
>
> **C3 全部不在范围**（拒绝路径）：
> - 实现 C4 7 交互 tool 任一（`tap` / `double_tap` / `long_press` / `fill` / `swipe` / `scroll_to` / `key_press`——cold plan §C4 字面）
> - 实现 C5 6 复合/系统 tool 任一（`find_and_tap` / `wait_for` / `flow_run` / `open_url` / `pasteboard_*` / `permissions_grant`——cold plan §C5 字面）
> - 实现 `explain_screen` / spawn `claude` 子进程（cold plan §C6 字面）
> - 固化 `ScreenDescription` / `SerializedFailure` JSON schema（cold plan §C7 字面，C3 用 driver 现状直出）
> - 改 `Driver` interface 添加 method / 改 29 method 签名（决策 5.A 字面 0 改动）
> - 改 `SimctlDriver` describe/screenshot/tree/findOne 实现（决策 5.A 字面 0 改动；driver 现状已 v0.4-v0.5 双重验证）
> - 改 `Cell` / `acquireCell` / `acquireSession` 签名（决策 2.A 字面 0 改动）
> - 改 `SimctlClient` method（C2 决策 6.A 延续）
> - 改 `resolveSelector` 实现（决策 8.A 字面 0 改动）
> - 改 `boot.ts` / `device-resolver.ts`（C2 决策延续 0 改动）
> - 加 selector RegExp 支持（决策 7.A 字面拒——RegExp JSON 不可序列化）
> - 加 cell pool / 多 udid 并发（决策 2.A 字面 udid → Driver 单 Map 缓存即够；并发推 v1.1）
> - 加 max_depth tree trim 实现（决策 9.A 字面 v0.6 C3 不剪 — driver.tree 返多大就多大；C7 schema 固化时再决定）
> - 加 base64 size 硬限（决策 10.A 字面无 cap — iOS 26.4 1290×2796 PNG base64 ~470KB，MCP stdio JSON 单 line 470KB 已实测 OK；超大走 C4 trace-sink 推 v0.7+ HTTP 引用形态）
> - 单测真 spawn driver / 真调 simctl（必须 mock acquireDriver，决策 13.A——CI 不依赖本机 dev sim 或 runner）
> - smoke 测 `screen_hierarchy`（决策 12.A 字面 runner 未运行 driver.tree 必抛；smoke 跳过此 tool 走 unit 覆盖）
> - 写 swift case / 改 swift-bridge（C1-C2 决策延续）
> - 改 SDK / matchers / ExpectationFailure schema（C1-C2 决策延续）
> - 改 `simx-v0[1-5]-acceptance.sh`（v0.5 / v0.4 冻结；C1-C2 决策延续）
> - 建 `simx-v06-acceptance.sh`（推 C8 字面 cold plan §出口验收）
> - 加 HTTP/SSE transport / 加 CLI flag（C1 决策延续 0 flag）
>
> **范围严格 = swift 0 / Driver interface 0 / SimctlDriver 0 / Cell 0 / acquireCell 0 / SimctlClient 0 / SDK 0 / dep 0 / device-resolver.ts 0 / resolveSelector 0 / C4-C5 tool 0 / explain_screen 0 / ScreenDescription schema 固化 0**。允许改动文件范围（**≤ 6 条源码** 字面任务约束）：
> 1. **新建 `src/mcp/driver-factory.ts`**（~30-50 行）
> 2. **改 `src/mcp/tools.ts`**（~+220-320 行 / +4 ToolDef + ToolContext shape 扩 1 字段 + zod 递归 selectorSchema）
> 3. **改 `src/mcp/server.ts`**（~+10-15 行 / 扩 1 opts 字段 + ctx 加 acquireDriver）
> 4. **改 `src/mcp/index.ts`**（+2 export 行）
> 5. **改 `src/mcp/__tests__/server.test.ts`**（~+16-20 case + tools/list count 改 8→12）
> 6. **新建 `scripts/simx-c3-mcp-observe-smoke.sh`**（~150-200 行）+ `chmod +x`
> 7. **改 `scripts/simx-c1-mcp-smoke.sh` + `scripts/simx-c2-mcp-lifecycle-smoke.sh`**（`tools_list_count` / `tools_count` 期望 8 → 12 同步，1-2 行改）— 不计入源码 6 条，与 C2 时改 C1 smoke 同先例（决策 15）
> 8. **改 `docs/v1.md`** 决策日志 +1 行 v0.6 C3 close note
>
> **6 条任务约束的解释**：6 条 = "源码改动文件" 6 条（项 1-5 是源码 5 条；项 6 smoke + 项 7 既有 smoke 同步 + 项 8 docs 不计入源码改动数，与 C2 形态对齐——C2 字面 "≤ 6 条" 同样不计 smoke + docs）。如严格解读为 "≤ 6 条总改动"，则去掉 `src/mcp/index.ts` 2 行 export（决策 4.B fallback——4 个 ToolDef 仅 mcp 内部用 + `defaultAcquireDriver` 也只 server.ts 内部用，不必从 barrel re-export）→ 总 6 条（含 smoke + docs，源码 4 条）。
>
> **不允许**：(a) 改 `src/driver/` / `src/sim/` / `src/core/` / `src/sdk/` 任何文件；(b) 改 `swift-bridge/`；(c) 改其他 CLI 文件；(d) 改 `scripts/simx-v0[1-5]-acceptance.sh`；(e) 引第 3 个 npm dep；(f) 加 CLI flag 到 `mcpCommand`；(g) 写 C4-C5 13 tool 任一；(h) 写 `explain_screen` / spawn `claude`；(i) 改 `Driver` / `SimctlDriver` / `Cell` / `SimctlClient` 任何 method；(j) 加 selector RegExp 支持；(k) tool handler 调真 simctl / 真 driver（vitest 必须用 mock acquireDriver）；(l) 加 SDK resources/prompts/completions/sampling capability；(m) 改 `ScreenDescription` 或 `A11yNode` JSON 形态。

## 目标 checkpoint

**C3**：通过后世界变成 — `bun src/cli/index.ts mcp` 字面启动 stdio MCP server 同时 expose **12 个 tool**（C1 `ping` + C2 7 lifecycle + C3 新加 4 observe），4 个新 tool 真接 `Driver` 接口：

- `tools/list` → 返 **12 element array**，名字字面：`ping` / `simulator_list` / `simulator_boot` / `simulator_shutdown` / `app_launch` / `app_terminate` / `app_install` / `app_uninstall` / `screen_describe` / `screen_screenshot` / `screen_hierarchy` / `element_inspect`
- `tools/call name=screen_describe arguments={udid:"..."}` → 调 `driver.describe()` → 返 `{content:[{type:'text', text:'{"screenshot":"<base64>", "elements":[{role, name?, id?, text?, bounds, enabled}, ...], "frontApp":"...", "summary":"", "capturedAt":N}'}]}`
- `tools/call name=screen_describe arguments={udid:"...", limit:10}` → 同上但 elements 长度 ≤ 10（**注**：driver.describe() 内部硬编码 50，limit 在 handler 层后截断 — 决策 14.A）
- `tools/call name=screen_screenshot arguments={udid:"..."}` → 调 `driver.screenshot()` → 返 `{content:[{type:'text', text:'{"base64":"<png-base64>", "byteLength":N}'}]}`
- `tools/call name=screen_hierarchy arguments={udid:"..."}` → 调 `driver.tree()` → 返 `{content:[{type:'text', text:'{"tree":{rawType, role?, identifier?, label?, ..., children:[...]}}'}]}` 全 a11y 树原样
- `tools/call name=element_inspect arguments={udid:"...", selector:{text:"Settings"}}` → 调 `driver.findOne({text:"Settings"})` → 返 `{content:[{type:'text', text:'{"node":{rawType, role?, ..., bounds, children:[...]}}'}]}` 或 `{content:[{type:'text', text:'{"node":null}'}]}`（element 不存在不是 error，决策 6.A）
- `tools/call name=element_inspect arguments={udid:"...", selector:{role:"button", name:"OK", below:{text:"Confirm?"}}}` → 同上，支持 4 base + 8 modifier 字面（决策 7.A——string only）
- 任一 observe tool 失败（driver acquire throw / driver.method throw / selector 无效 zod 校验失败）→ `{isError:true, content:[{type:'text', text:'<error message>'}]}`（C2 决策 10.A 延续）
- `tools/call name=screen_describe arguments={}`（缺 udid）→ `{isError:true, content:[{type:'text', text:'invalid arguments: ...'}]}`（zod required + SDK 默认 isError 路径）
- `tools/call name=element_inspect arguments={udid, selector:{}}`（base 4 form 全缺）→ `{isError:true, content:[{type:'text', text:'invalid arguments: ...'}]}`（zod refine 至少 1 base 字段）

`scripts/simx-c3-mcp-observe-smoke.sh` 12 字段 JSON CI-schema gate 全绿 + `docs/v1.md` 决策日志 +1 行 + 测试基线 **464 → ~480-486 TS**（server +16-20 case）+ 102 swift 0 改动 + 27 字段 v0.5 acceptance / 21 字段 v0.4 acceptance / 11 字段 C1 smoke / 10 字段 C2 smoke 全保不退化。

## 前置条件

```bash
# v0.6 C2 close 痕迹存在
test -f docs/plan-history/v0.6-c2-hot.md
test -x scripts/simx-c2-mcp-lifecycle-smoke.sh

# C2 baseline 464 TS 全保
bun x vitest run 2>&1 | tail -5 | grep -qE 'Tests +464 passed'

# typecheck 绿
bun run typecheck > /dev/null

# C1 smoke 11 字段全绿
bash scripts/simx-c1-mcp-smoke.sh > /tmp/c1-baseline.json
jq -e '.all_ok == "ok"' /tmp/c1-baseline.json > /dev/null

# 本机有 dev sim（C3 smoke 真本机 e2e 前提；探测不强制 exit 0，但若无则 C3 smoke 不可跑）
xcrun simctl list devices -j > /tmp/sim-list.json
jq -e '[.devices | to_entries[] | .value[] | select(.isAvailable == true)] | length > 0' /tmp/sim-list.json > /dev/null

# v0.5 acceptance 27 字段全保
bash scripts/simx-v05-acceptance.sh > /tmp/v05-baseline.json
jq -e '
  .swift_tests >= 102 and .ts_tests >= 464 and .typecheck == "ok"
  and .doctor_checks >= 6
  and .c1_repl_startup == "ok" and .c2_history_undo_redo == "ok"
  and .c3_save_codegen == "ok" and .c4_doctor_checks_deep == "ok"
  and .c5_run_flags == "ok" and .c6_boot_command == "ok"
' /tmp/v05-baseline.json > /dev/null

# v0.4 acceptance 21 字段不退化
bash scripts/simx-v04-acceptance.sh > /tmp/v04-regression.json
jq -e '.swift_tests >= 102 and .ts_tests >= 464 and .typecheck == "ok"' /tmp/v04-regression.json > /dev/null

# roadmap 字面 v0.6 🔥 hot
grep -qE '\| \*\*v0\.6\*\* \| 🔥 hot' docs/roadmap.md

# C2 决策日志在场（v0.6 C2 close note）
grep -qE '2026-05-16.*v0\.6 C2' docs/v1.md

# cold plan §C3 字面 4 tool 列表
grep -qE 'screen_describe' docs/plan-cold/v0.6.md
grep -qE 'screen_screenshot' docs/plan-cold/v0.6.md
grep -qE 'screen_hierarchy' docs/plan-cold/v0.6.md
grep -qE 'element_inspect' docs/plan-cold/v0.6.md

# `src/mcp/` 6 文件字面在场（C2 产出）
test -f src/mcp/tools.ts
test -f src/mcp/server.ts
test -f src/mcp/runner.ts
test -f src/mcp/index.ts
test -f src/mcp/device-resolver.ts
test -d src/mcp/__tests__

# C3 依赖的 Driver / Cell / 工具字面在场
grep -qE 'screenshot\(\)' src/driver/types.ts
grep -qE 'describe\(\)' src/driver/types.ts
grep -qE 'tree\(\)' src/driver/types.ts
grep -qE 'findOne\(' src/driver/types.ts
grep -qE 'async describe\(' src/driver/simctl-driver.ts
grep -qE 'async screenshot\(' src/driver/simctl-driver.ts
grep -qE 'async tree\(' src/driver/simctl-driver.ts
grep -qE 'async findOne\(' src/driver/simctl-driver.ts
grep -qE 'export async function acquireCell' src/sim/cell.ts
grep -qE 'export const DEFAULT_RUNNER_PORT' src/sim/cell.ts

# `src/core/` selector + screen wire 字面在场
grep -qE 'export type Selector' src/core/selector.ts
grep -qE 'export type A11yNode' src/core/screen.ts
grep -qE 'export type ScreenDescription' src/core/screen.ts

# 既有 server.test.ts case count = 29（C2 close 后）
grep -cE "^  it\(" src/mcp/__tests__/server.test.ts | grep -qE '^29$'

# dep 累计未变（C3 0 新引）
grep -qE '"@modelcontextprotocol/sdk": "1\.29\.0"' package.json
grep -qE '"zod": "\^3\.' package.json
node -e 'const p=require("./package.json"); const d=Object.keys(p.dependencies||{}); const e=new Set(["citty","@modelcontextprotocol/sdk","zod"]); for (const k of d) { if (!e.has(k)) { console.error("unexpected:", k); process.exit(1); } } process.exit(0);'

# Xcode + bun 在场
xcodebuild -version | head -1
bun --version
```

期望：每条 exit 0。任何一条非 0 = 前置不满足，先修。

## 步骤（线性，3 个 step）

### S1. `src/mcp/driver-factory.ts` 新建（`defaultAcquireDriver` + udid → Driver 缓存）+ `tools.ts` 扩 `ToolContext.acquireDriver` 字段 + 4 个新 ToolDef（zod schema + JSON Schema literal + handler）+ `server.ts` 接通 acquireDriver 注入 + `index.ts` barrel export（红 → 绿 → 重构）

**红（写测试）**
- 文件：`src/mcp/__tests__/server.test.ts`（**改** — 既有 29 case 保不动；加 ~16-20 新 case）
- 顶部新增 helper `makeMockDriver(overrides?: Partial<Driver>)`：返回 `Pick<Driver, 'screenshot'|'describe'|'tree'|'findOne'>` 形态 mock；`screenshot` 默认 `vi.fn().mockResolvedValue(Buffer.from('fake-png-bytes'))`；`describe` 默认 `vi.fn().mockResolvedValue({screenshot:'YmFzZTY0', elements:[], frontApp:'', summary:'', capturedAt: 1234})`；`tree` 默认 `vi.fn().mockResolvedValue({rawType:'application', bounds:{x:0,y:0,w:100,h:200}, enabled:true, selected:false, hasFocus:true, visible:true, children:[]})`；`findOne` 默认 `vi.fn().mockResolvedValue(null)`
- 顶部新增 helper `makeMockAcquireDriver(driverOverrides?)`：返回 `(udid: string) => Promise<Driver>` + 内部记录 udid 调用列表 + 返同一 mock driver 实例（便于断 udid 命中）
- 修改 `makeLinkedClient` 接 `opts?.acquireDriver` 透传到 `createSimxMcpServer`
- 修改既有 `'tools/list: returns 8 tools and contains ping'` case：改名为 `'tools/list: returns 12 tools and contains ping'` + 改 expect `.toBe(12)`（**字面锁 C3 范围**——拒 C4+ 提前实现）
- 新增 case（**最少 16，目标 20**）：
  1. `'tools/list: names contain all 4 observe (screen_describe / screen_screenshot / screen_hierarchy / element_inspect)'`
  2. `'screen_describe happy: returns ScreenDescription via driver.describe — elements + screenshot base64 in JSON content[0].text'`：mock describe 返 `{screenshot:'YmFzZTY0', elements:[{role:'button', name:'OK', bounds:{x:0,y:0,w:10,h:10}, enabled:true}], frontApp:'com.foo', summary:'', capturedAt:42}` → call `{udid:'A'}` → 断 acquireDriver 调 1 次 with 'A' + driver.describe 调 1 次 + parsed `.elements.length === 1` + parsed `.screenshot === 'YmFzZTY0'` + parsed `.frontApp === 'com.foo'`
  3. `'screen_describe limit: truncates elements client-side to N when limit < driver default 50'`：mock describe 返 30 elements → call `{udid:'A', limit:5}` → 断 parsed `.elements.length === 5`（决策 14.A 字面 — handler 后截断）
  4. `'screen_describe error: driver.describe throws → isError content'`：mock describe 抛 `new Error('describe boom')` → 断 `result.isError === true` + text 含 `'describe boom'`
  5. `'screen_describe missing udid: isError + invalid arguments'`：call `{}` → 断 `result.isError === true` + text 含 `'invalid arguments'`
  6. `'screen_screenshot happy: returns base64 + byteLength via driver.screenshot'`：mock screenshot 返 `Buffer.from('hello-png-bytes')` → call `{udid:'A'}` → 断 parsed `.base64 === Buffer.from('hello-png-bytes').toString('base64')` + parsed `.byteLength === 15`
  7. `'screen_screenshot error: driver.screenshot throws → isError'`：mock 抛 → 断 `isError === true`
  8. `'screen_screenshot missing udid: isError + invalid arguments'`
  9. `'screen_hierarchy happy: returns tree A11yNode via driver.tree'`：mock tree 返根节点带 1 child → call `{udid:'A'}` → 断 parsed `.tree.rawType === 'application'` + parsed `.tree.children.length === 1`
  10. `'screen_hierarchy error: driver.tree throws (e.g. runner not reachable) → isError'`：mock 抛 → 断 `isError === true` + text 含 error
  11. `'element_inspect found: returns A11yNode via driver.findOne'`：mock findOne 返 `{rawType:'button', label:'OK', bounds:{x:0,y:0,w:50,h:30}, enabled:true, selected:false, hasFocus:false, visible:true, children:[]}` → call `{udid:'A', selector:{text:'OK'}}` → 断 driver.findOne 第 1 arg === `{text:'OK'}` + parsed `.node.rawType === 'button'`
  12. `'element_inspect not found: returns {node: null} (not isError)'`：mock findOne 返 `null` → call `{udid:'A', selector:{text:'X'}}` → 断 `result.isError !== true` + parsed `.node === null`（决策 6.A 字面）
  13. `'element_inspect selector modifier: passes near/below modifier through to driver.findOne'`：mock findOne returning anything → call `{udid:'A', selector:{role:'button', name:'OK', below:{text:'Confirm?'}}}` → 断 driver.findOne 第 1 arg deep-equal 输入 selector
  14. `'element_inspect missing base form: isError + invalid arguments'`：call `{udid:'A', selector:{}}` → 断 `isError === true` + text 含 `'invalid arguments'`
  15. `'element_inspect missing selector: isError + invalid arguments'`：call `{udid:'A'}`（缺 selector）→ 断 `isError === true`
  16. `'element_inspect error: driver.findOne throws → isError'`：mock findOne 抛 → 断 `isError === true`
  17. `'observe tools: acquireDriver called with correct udid (DI: ToolContext.acquireDriver wired through ctx)'`：两次 call screen_describe with udid 'A' 然后 'B' → 断 acquireDriver 调用列表 === ['A', 'B']
  18. `'observe tools: acquireDriver throws → isError content (not throw to JSON-RPC)'`：注入 `acquireDriver = vi.fn().mockRejectedValue(new Error('cell acquire failed'))` → call → 断 `isError === true` + text 含 `'cell acquire failed'`
  19. `'C2 8-tool regression: ping + simulator_list + app_launch happy paths still pass with ToolContext.acquireDriver present but unused'`：注入 mock acquireDriver（vi.fn 永不调）+ mock client → call ping/simulator_list/app_launch → 断 happy 路径 + 断 acquireDriver mock 调 0 次（C2 8 tool 不依赖 driver——验证 ctx 扩展未污染既有 tool）
  20. `'tools/list: schema for element_inspect has required udid + selector + selector is object'`：list → find element_inspect → 断 `inputSchema.required` 含 `'udid'` + `'selector'` + `inputSchema.properties.selector.type === 'object'`
- 跑：`bun x vitest run src/mcp/__tests__/server.test.ts`
- 期望：~16-20 新 case 全红（tools.ts 尚未加 4 ToolDef + ToolContext 字段未扩 + driver-factory.ts 未建）

**绿（实现）**
- 文件：`src/mcp/driver-factory.ts`（**新建** ~30-50 行）：
  ```ts
  import type { Driver } from '../driver/types.js'
  import { SimctlDriver } from '../driver/simctl-driver.js'
  import { acquireCell } from '../sim/cell.js'
  import type { SimctlClient } from '../sim/simctl.js'

  // Build a per-process udid → Driver factory backed by acquireCell + SimctlDriver.
  // Caches Driver instances by udid so repeated tool calls reuse the same runner
  // binding. The cache never evicts in v0.6 (MCP server lifetime is bound to the
  // Claude Code session); eviction / multi-cell pooling is a v1.1 concern.
  export function defaultAcquireDriver(
    client: SimctlClient,
  ): (udid: string) => Promise<Driver> {
    const cache = new Map<string, Promise<Driver>>()
    return (udid: string): Promise<Driver> => {
      const cached = cache.get(udid)
      if (cached !== undefined) return cached
      const p = acquireCell({ udid }, { client }).then(
        (cell) => new SimctlDriver(cell) as Driver,
      )
      cache.set(udid, p)
      return p
    }
  }
  ```
- 文件：`src/mcp/tools.ts`（**改** — ToolContext shape 扩 + zod 递归 selector + 4 ToolDef）：
  - `ToolContext` 改为：
    ```ts
    export type ToolContext = {
      client: SimctlClient
      acquireDriver: (udid: string) => Promise<Driver>
    }
    ```
  - 顶部加 `import type { Driver } from '../driver/types.js'`
  - 顶部加 zod 递归 selector schema（决策 7.A——string only，无 RegExp）：
    ```ts
    const baseSelectorSchema: z.ZodType<unknown> = z.lazy(() =>
      z.union([
        z.object({ text: z.string() }).passthrough(),
        z.object({ id: z.string() }).passthrough(),
        z.object({ label: z.string() }).passthrough(),
        z.object({ role: z.string(), name: z.string().optional() }).passthrough(),
      ]),
    )
    const selectorSchema: z.ZodType<unknown> = z.lazy(() =>
      z.intersection(
        baseSelectorSchema,
        z.object({
          near: z.lazy(() => selectorSchema).optional(),
          below: z.lazy(() => selectorSchema).optional(),
          above: z.lazy(() => selectorSchema).optional(),
          leftOf: z.lazy(() => selectorSchema).optional(),
          rightOf: z.lazy(() => selectorSchema).optional(),
          inside: z.lazy(() => selectorSchema).optional(),
          nth: z.number().int().optional(),
          first: z.boolean().optional(),
          last: z.boolean().optional(),
        }),
      ),
    )
    // JSON Schema literal exposes only top-level shape (no recursion in JSON Schema).
    // MCP clients still pass nested modifiers — zod validates them at runtime.
    const SELECTOR_JSON_SCHEMA: Record<string, unknown> = {
      type: 'object',
      description: 'Selector: one base form (text|id|label|role+name; strings only) plus optional modifiers (near|below|above|leftOf|rightOf|inside|nth|first|last). Modifiers nest the same shape recursively.',
    }
    ```
  - 4 个新 ToolDef（每个 ~35-55 行；以 `elementInspectTool` 为最复杂示例）：
    ```ts
    const screenDescribeInput = z.object({ udid: z.string(), limit: z.number().int().positive().optional() }).strict()
    export const screenDescribeTool: ToolDef = {
      name: 'screen_describe',
      description: 'Describe the current screen: returns ScreenDescription { screenshot (base64 PNG), elements (top N visible+enabled summaries in DFS pre-order), frontApp, summary, capturedAt }. Optional `limit` truncates elements client-side (driver returns up to 50).',
      inputSchema: screenDescribeInput,
      inputJsonSchema: {
        type: 'object',
        properties: { udid: { type: 'string' }, limit: { type: 'number' } },
        required: ['udid'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof screenDescribeInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          const desc = await driver.describe()
          const elements = parsed.limit !== undefined ? desc.elements.slice(0, parsed.limit) : desc.elements
          return { content: [{ type: 'text', text: JSON.stringify({ ...desc, elements }) }] }
        } catch (err) {
          return { content: [{ type: 'text', text: err instanceof Error ? err.message : String(err) }], isError: true }
        }
      },
    }

    const screenScreenshotInput = z.object({ udid: z.string() }).strict()
    export const screenScreenshotTool: ToolDef = {
      name: 'screen_screenshot',
      description: 'Capture a PNG screenshot of the simulator screen. Returns { base64, byteLength }. base64 may be ~470KB for iPhone-class 1290×2796 frames.',
      inputSchema: screenScreenshotInput,
      inputJsonSchema: { type: 'object', properties: { udid: { type: 'string' } }, required: ['udid'], additionalProperties: false },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof screenScreenshotInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          const png = await driver.screenshot()
          return { content: [{ type: 'text', text: JSON.stringify({ base64: png.toString('base64'), byteLength: png.byteLength }) }] }
        } catch (err) {
          return { content: [{ type: 'text', text: err instanceof Error ? err.message : String(err) }], isError: true }
        }
      },
    }

    const screenHierarchyInput = z.object({ udid: z.string() }).strict()
    export const screenHierarchyTool: ToolDef = {
      name: 'screen_hierarchy',
      description: 'Return the full accessibility tree (A11yNode { rawType, role?, identifier?, label?, value?, text?, bounds, enabled, selected, hasFocus, visible, children: A11yNode[] }) of the frontmost app. Requires a running SimxRunner XCUITest server bound to the runner port.',
      inputSchema: screenHierarchyInput,
      inputJsonSchema: { type: 'object', properties: { udid: { type: 'string' } }, required: ['udid'], additionalProperties: false },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof screenHierarchyInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          const tree = await driver.tree()
          return { content: [{ type: 'text', text: JSON.stringify({ tree }) }] }
        } catch (err) {
          return { content: [{ type: 'text', text: err instanceof Error ? err.message : String(err) }], isError: true }
        }
      },
    }

    const elementInspectInput = z.object({ udid: z.string(), selector: selectorSchema }).strict()
    export const elementInspectTool: ToolDef = {
      name: 'element_inspect',
      description: 'Resolve a single element by Selector and return its A11yNode (or { node: null } when no match). Selector supports 4 base forms (text|id|label|role+name; strings only — RegExp deferred to v0.7+) and 8 modifiers (near|below|above|leftOf|rightOf|inside|nth|first|last; modifiers nest the same shape recursively).',
      inputSchema: elementInspectInput,
      inputJsonSchema: {
        type: 'object',
        properties: { udid: { type: 'string' }, selector: SELECTOR_JSON_SCHEMA },
        required: ['udid', 'selector'],
        additionalProperties: false,
      },
      handler: async (args, ctx) => {
        const parsed = args as z.infer<typeof elementInspectInput>
        try {
          const driver = await ctx.acquireDriver(parsed.udid)
          // selectorSchema validates 4-base form via union; modifier-only objects fail the union
          // and trigger zod's default isError path before reaching this handler.
          const node = await driver.findOne(parsed.selector as Parameters<Driver['findOne']>[0])
          return { content: [{ type: 'text', text: JSON.stringify({ node }) }] }
        } catch (err) {
          return { content: [{ type: 'text', text: err instanceof Error ? err.message : String(err) }], isError: true }
        }
      },
    }
    ```
  - `ALL_TOOLS = [pingTool, simulatorListTool, simulatorBootTool, simulatorShutdownTool, appLaunchTool, appTerminateTool, appInstallTool, appUninstallTool, screenDescribeTool, screenScreenshotTool, screenHierarchyTool, elementInspectTool] as const`
- 文件：`src/mcp/server.ts`（**改**）：
  - 加 import `Driver`（type-only）+ `defaultAcquireDriver`
  - `CreateSimxMcpServerOptions` 扩 `acquireDriver?: (udid: string) => Promise<Driver>`
  - `createSimxMcpServer` 内 `const acquireDriver = opts.acquireDriver ?? defaultAcquireDriver(client)` + `const ctx: ToolContext = { client, acquireDriver }`
- 文件：`src/mcp/index.ts`（+2 行 export 4 tool + `defaultAcquireDriver`）
- 跑：`bun x vitest run src/mcp/__tests__/`
- 期望：29 既有 + 16-20 新 = 45-49 case 全绿

**重构**
- 无（4 tool 形态平行 + handler try/catch 统一 + acquireDriver 抽 helper；selector zod 递归一次到位）

**Step-level 验证命令**：
```bash
bun x vitest run src/mcp/__tests__/server.test.ts 2>&1 | tail -5 | grep -qE 'Tests +(4[5-9]|5[0-1]) passed'
bun run typecheck > /dev/null
test -f src/mcp/driver-factory.ts
grep -qE "name: 'screen_describe'" src/mcp/tools.ts
grep -qE "name: 'screen_screenshot'" src/mcp/tools.ts
grep -qE "name: 'screen_hierarchy'" src/mcp/tools.ts
grep -qE "name: 'element_inspect'" src/mcp/tools.ts
grep -qE 'acquireDriver:' src/mcp/tools.ts
```
期望：全 exit 0。

### S2. 全套测试 + typecheck + 既有 acceptance 不退化 gate（确认 C1+C2 + v0.4-v0.5 全保）（红 → 绿 → 重构）

**红（写测试）**
- 此 step 不新增测试代码——是"防退化 gate"step。视 S1 的所有既有 case（13 C1 + 16 C2 + 16-20 C3 = 45-49）必须全绿为"红"前提。
- 跑：
  ```bash
  bun x vitest run 2>&1 | tail -10
  ```
- 期望：先看到 `Tests N passed` 其中 N ≥ 464 + 16 = 480。若任一既有 case 红 → 立即 stop 回到 S1。

**绿（实现）**
- 跑全套：
  ```bash
  bun x vitest run 2>&1 | tail -5
  bun run typecheck > /dev/null && echo "typecheck-ok"
  ```
- 期望：vitest 全绿 + count ≥ 480 + typecheck exit 0

**重构（可选）**
- 无。

**Step-level 验证命令**：
```bash
bun x vitest run 2>&1 | tail -5 | grep -qE 'Tests +(4[8-9][0-9]|50[0-9]) passed'
bun run typecheck > /dev/null
```
期望：全 exit 0。

### S3. 新建 `scripts/simx-c3-mcp-observe-smoke.sh`（12 字段 JSON CI gate 真本机 dev sim e2e）+ 改既有 C1/C2 smoke 同步 tools_count + `docs/v1.md` 决策日志 +1 行（红 → 绿 → 重构）

**红（写测试）**
- 文件：`scripts/simx-c3-mcp-observe-smoke.sh`（**新建** ~150-200 行 + `chmod +x`）
- Schema：12 字段 `{exit_code, initialize_ok, tools_count, has_screen_describe, has_screen_screenshot, has_screen_hierarchy, has_element_inspect, screen_screenshot_base64_len_gt_1000, screen_describe_elements_is_array, element_inspect_node_resolvable, stdout_pure_jsonrpc, all_ok}`
- 实现：
  1. 探测真 booted dev sim（`xcrun simctl list devices -j | jq -r '...'` 取第一个 booted udid）；若无 → 选 first available + `simctl bootstatus -b`（与 C2 smoke 同形态）
  2. spawn `bun src/cli/index.ts mcp` 真子进程 + stdin 喂 7 JSON-RPC frame：`initialize` / `initialized notif` / `tools/list` / `tools/call simulator_list` / `tools/call screen_screenshot {udid}` / `tools/call screen_describe {udid}` / `tools/call element_inspect {udid, selector:{text:"Settings"}}`
  3. sleep 5 hold stdin 让 async handler 完成；kill 子进程；jq 验各字段
  4. **不调 `screen_hierarchy`**（决策 12.A——CI 无 runner driver.tree 必抛；走 unit 覆盖；smoke 只验 `has_screen_hierarchy` 在 tools/list 中即可）
  5. `element_inspect_node_resolvable` 字段语义：`.node === null || .node.rawType` 是 string——容忍 element 不存在（Settings 文字未必命中前台 app）；isError 也算 `ok`（runner 未运行时 findOne 会抛）
- 改既有 smoke 同步 tools_count（决策 15）：
  - `scripts/simx-c1-mcp-smoke.sh`：`tools_list_count` 期望 8 → 12（1 行改）
  - `scripts/simx-c2-mcp-lifecycle-smoke.sh`：`tools_count` 期望 8 → 12（1 行改）
- 跑 smoke（**红**）：
  ```bash
  bash scripts/simx-c3-mcp-observe-smoke.sh > /tmp/c3-smoke-red.json 2>&1 || true
  jq -e '.all_ok == "ok"' /tmp/c3-smoke-red.json 2>/dev/null
  ```
- 期望（红前）：smoke 不存在 → exit 非 0 / 任一 jq fail → 进入绿

**绿（实现）**
- 写 smoke 脚本（参考 `scripts/simx-c2-mcp-lifecycle-smoke.sh` 结构 + 12 字段定制）
- 改 C1 / C2 smoke 同步 tools_count
- 加 `chmod +x` 新 smoke
- 改 `docs/v1.md` 末尾 +1 行决策日志：
  ```
  - **2026-05-16** v0.6 C3：MCP 业务层接 a11y / 截图能力 — 4 observe tool（`screen_describe` / `screen_screenshot` / `screen_hierarchy` / `element_inspect`）字面 expose，每 tool 接既有 `Driver` 4 method（`describe` / `screenshot` / `tree` / `findOne`）+ 0 改动 Driver interface / SimctlDriver 实现；新建 `src/mcp/driver-factory.ts`（`defaultAcquireDriver(client)` 工厂 + udid → Driver Map 缓存 + acquireCell + new SimctlDriver wrap，决策 4.A）；`ToolContext` 扩 1 字段 `acquireDriver: (udid) => Promise<Driver>` lazy factory（决策 1.B 拒单实例 / 拒 cell pool）；`createSimxMcpServer({acquireDriver?})` 注入点（决策 3.A 默认 defaultAcquireDriver(client)）；`element_inspect` 元素未找到 = `{node: null}` 非 isError（决策 6.A 合法查询结果）；selector zod 递归 schema 支持 4 base + 8 modifier（决策 7.A string-only 拒 RegExp）；`ScreenDescription` / `A11yNode` 直 JSON.stringify 现状直出（决策 8.A 拒 C7 schema 固化提前）；`screen_describe` handler 限 limit 客户端截断（决策 14.A driver 内部 50 不改）；`screen_hierarchy` 不剪 max_depth 全树返（决策 9.A）；base64 无 size cap（决策 10.A iOS 26.4 470KB stdio 实测 OK）。新建 `src/mcp/driver-factory.ts`（~40 行）；改 `src/mcp/tools.ts`（+4 ToolDef + ToolContext 字段 + zod 递归 selectorSchema）；改 `src/mcp/server.ts`（opts.acquireDriver + ctx 加 acquireDriver）；改 `src/mcp/index.ts`（+2 export）；改 `src/mcp/__tests__/server.test.ts`（29 → ~45-49 case：+16-20 case 覆盖 4 observe tool happy + error + selector modifier + acquireDriver wired + C2 regression）；新建 `scripts/simx-c3-mcp-observe-smoke.sh`（12 字段 JSON CI gate 真 spawn + 7 frame + 真本机 simctl + screen_describe + screen_screenshot + element_inspect 全链路）；改 `scripts/simx-c1-mcp-smoke.sh` + `scripts/simx-c2-mcp-lifecycle-smoke.sh`（tools_count 期望 8 → 12 同步，决策 15）。**0 swift / 0 Driver / 0 SimctlDriver / 0 Cell / 0 SimctlClient / 0 SDK / 0 dep / 0 C4-C5 tool / 0 explain_screen / 0 ScreenDescription schema 固化 / 0 resolveSelector 改动 / 0 SDK matcher 改动**。测试基线 464 → **~480-486 TS**（server +16-20）/ 102 swift / typecheck 0 error / 27 字段 v0.5 acceptance 全保 / 21 字段 v0.4 acceptance 全保 / 11 字段 C1 smoke 全保（tools_list_count 12 同步）/ 10 字段 C2 smoke 全保（tools_count 12 同步）/ 12 字段 C3 smoke 真本机 dev sim e2e 全绿。
  ```
- 跑：
  ```bash
  bash scripts/simx-c3-mcp-observe-smoke.sh > /tmp/c3-smoke-green.json
  jq -e '
    .exit_code == 0
    and .initialize_ok == "ok"
    and .tools_count == 12
    and .has_screen_describe == "ok"
    and .has_screen_screenshot == "ok"
    and .has_screen_hierarchy == "ok"
    and .has_element_inspect == "ok"
    and .screen_screenshot_base64_len_gt_1000 == "yes"
    and .screen_describe_elements_is_array == "ok"
    and .element_inspect_node_resolvable == "ok"
    and .stdout_pure_jsonrpc == "ok"
    and .all_ok == "ok"
  ' /tmp/c3-smoke-green.json > /dev/null
  bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
  bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
  ```
- 期望：exit 0

**重构（可选）**
- 若 smoke 脚本结构与 C2 smoke 差异过大 → 提取共享 shell helper `_simx-mcp-spawn.sh`。**C3 范围内不提取**（推 C5 / C6 累计 3 smoke 后再 DRY，决策 16.A）。

**Step-level 验证命令**：
```bash
test -x scripts/simx-c3-mcp-observe-smoke.sh
bash scripts/simx-c3-mcp-observe-smoke.sh > /tmp/c3-smoke.json
jq -e '.all_ok == "ok"' /tmp/c3-smoke.json
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null
grep -qE '2026-05-16.*v0\.6 C3' docs/v1.md
```
期望：全 exit 0。

## Checkpoint C3 验收

```bash
set -euo pipefail

# 1. 全套 TS 测试全绿 + count ≥ 480
bun x vitest run 2>&1 | tee /tmp/c3-vitest.log | tail -5
grep -qE 'Tests +(4[8-9][0-9]|50[0-9]) passed' /tmp/c3-vitest.log

# 2. typecheck 0 error
bun run typecheck > /dev/null

# 3. C3 smoke 12 字段全绿（真本机 dev sim）
bash scripts/simx-c3-mcp-observe-smoke.sh > /tmp/c3-smoke.json
jq -e '
  .exit_code == 0
  and .initialize_ok == "ok"
  and .tools_count == 12
  and .has_screen_describe == "ok"
  and .has_screen_screenshot == "ok"
  and .has_screen_hierarchy == "ok"
  and .has_element_inspect == "ok"
  and .screen_screenshot_base64_len_gt_1000 == "yes"
  and .screen_describe_elements_is_array == "ok"
  and .element_inspect_node_resolvable == "ok"
  and .stdout_pure_jsonrpc == "ok"
  and .all_ok == "ok"
' /tmp/c3-smoke.json > /dev/null

# 4. C1 smoke 11 字段全保（tools_list_count 同步改 12）
bash scripts/simx-c1-mcp-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null

# 5. C2 smoke 10 字段全保（tools_count 同步改 12，需 dev sim）
bash scripts/simx-c2-mcp-lifecycle-smoke.sh | jq -e '.all_ok == "ok"' > /dev/null

# 6. v0.5 acceptance 27 字段全保
bash scripts/simx-v05-acceptance.sh > /tmp/c3-v05.json
jq -e '
  .swift_tests >= 102 and .ts_tests >= 480 and .typecheck == "ok"
  and .doctor_checks >= 6
  and .c1_repl_startup == "ok" and .c2_history_undo_redo == "ok"
  and .c3_save_codegen == "ok" and .c4_doctor_checks_deep == "ok"
  and .c5_run_flags == "ok" and .c6_boot_command == "ok"
' /tmp/c3-v05.json > /dev/null

# 7. v0.4 acceptance 21 字段不退化
bash scripts/simx-v04-acceptance.sh > /tmp/c3-v04.json
jq -e '.swift_tests >= 102 and .ts_tests >= 480 and .typecheck == "ok"' /tmp/c3-v04.json > /dev/null

# 8. 决策日志在场
grep -qE '2026-05-16.*v0\.6 C3' docs/v1.md

# 9. 改动 surface 字面确认（源码 5 条 + smoke 3 条改/新 + docs 1 条）
test -f src/mcp/driver-factory.ts
grep -qE "name: 'screen_describe'" src/mcp/tools.ts
grep -qE "name: 'screen_screenshot'" src/mcp/tools.ts
grep -qE "name: 'screen_hierarchy'" src/mcp/tools.ts
grep -qE "name: 'element_inspect'" src/mcp/tools.ts
grep -qE 'acquireDriver:' src/mcp/tools.ts
grep -qE 'acquireDriver' src/mcp/server.ts
grep -qE 'screenDescribeTool' src/mcp/index.ts
grep -cE "^  it\(" src/mcp/__tests__/server.test.ts | grep -qE '^(4[5-9]|5[0-1])$'
test -x scripts/simx-c3-mcp-observe-smoke.sh

# 10. dep 累计未变（C3 0 新引）
grep -qE '"@modelcontextprotocol/sdk": "1\.29\.0"' package.json
grep -qE '"zod": "\^3\.' package.json
node -e 'const p=require("./package.json"); const d=Object.keys(p.dependencies||{}); const e=new Set(["citty","@modelcontextprotocol/sdk","zod"]); for (const k of d) { if (!e.has(k)) { console.error("unexpected:", k); process.exit(1); } } process.exit(0);'

# 11. 0 范围外文件改动（git status diff vs C2 close baseline）
git status --short 2>/dev/null | grep -vE '^.M (src/mcp/tools\.ts|src/mcp/server\.ts|src/mcp/index\.ts|src/mcp/__tests__/server\.test\.ts|scripts/simx-c[12]-mcp[a-z-]*\.sh|docs/v1\.md|docs/plan-hot\.md)$' | \
  grep -vE '^\?\? (src/mcp/driver-factory\.ts|scripts/simx-c3-mcp-observe-smoke\.sh|docs/plan-history/v0\.6-c3-hot\.md)$' | \
  wc -l | tr -d ' ' | grep -qE '^0$' || echo "warn: out-of-scope file changes detected (manual review)"

echo "C3 PASS"
```

期望：每条 exit 0。最终输出含 `C3 PASS`。

## 完成后动作

1. 归档此文件到 `docs/plan-history/v0.6-c3-hot.md`：
   ```bash
   mv docs/plan-hot.md docs/plan-history/v0.6-c3-hot.md
   ```
2. 调 sub-agent 生成新 `plan-hot.md`（到 C4 — 7 个交互 tool `tap` / `double_tap` / `long_press` / `fill` / `swipe` / `scroll_to` / `key_press` + cold plan §C4 字面 "默认返回新屏幕 summary"），见 CLAUDE.md §6 标准模板。
3. C4 前置探测附加（除标准 §6 prompt + cold plan §C4 入口条件外）：
   ```bash
   # SimctlDriver 交互 method 字面在场（C4 接通点）
   grep -qE 'async tap\(' src/driver/simctl-driver.ts
   grep -qE 'tapViaRunner|tapViaResolver' src/driver/simctl-driver.ts
   # ToolContext.acquireDriver 字面在场（C3 产出）
   grep -qE 'acquireDriver:' src/mcp/tools.ts
   # 12 tool 字面在场（C3 close baseline）
   bash scripts/simx-c3-mcp-observe-smoke.sh | jq -e '.tools_count == 12'
   ```
4. **C4 必须先决定的点**（cold plan §C4 字面 "+ 默认返回新屏幕 summary"）：交互 tool handler 调完 driver method 后是否调 `driver.describe()` 拼到 result content？
   - 选项 A：每个交互 tool 默认调 describe → 结果 `{ok:true, screen:ScreenDescription}`（cold plan 字面，省 round-trip）
   - 选项 B：交互 tool 不返 screen，AI 显式 call screen_describe（违反 cold plan 字面）
   - → C4 sub-agent 必须选 **A** 并在 plan 前提里字面声明
