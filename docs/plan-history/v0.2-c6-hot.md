# plan-hot — v0.2 到 C6：TS SDK `app.tap({text})` 通过 Cell + SimxRunner HTTP `/tap` 真触发 iOS UI 跳页

> [3/4 信息层] 当前唯一热计划。完成 C6 后归档到 `plan-history/v0.2-c6-hot.md`，由 main convo 调 sub-agent 生成 C7（候选：`examples/calc-tap.test.ts` 含 tap+fill 跑通 + tap 延迟 P50 < 50ms metric — 参 `plan-cold/v0.2.md` C7 概要）。
>
> **范围决策**：C6 把 v0.1 `SimctlDriver.tap()` 18 个 `notSupported` stub 之一（**只**改 `tap`，其它 17 个不动）替换为 **真实现**——走 C2 SimxRunner XCUITest HTTP `POST /tap`（text selector → NSPredicate `label==% OR identifier==%` → `XCUIElement.tap()`）。**不**走 C4 `simx-host-hid`（host-side HID）—— host-hid 是坐标 tap，C6 没有 text→coord 解析能力（v0.3 selector resolver 工作），保留为已通基础设施。**同时**引入 Cell 抽象 L1 最小落地：`Cell` 类型 + `acquireCell()` + Driver 接 `Cell`（向后兼容：Driver 仍可接 `SimSession` 直传，CLI run.ts 改为先 acquireCell 再传给 Driver）。

## 目标 checkpoint

**C6**：

1. `SimctlDriver.tap({text:"General"})` 在 dev sim 真触发 iOS 26.4 Settings 跳到 General 子页（与 C2 smoke + C4 smoke 验证模式一致：tap 后 `Display & Brightness` 消失 / `About` 出现，作为机器可判断的"UI 真变化"证据）。
2. TS 用户代码 `await app.tap({text: 'General'})` 真链路打通 SDK → App → Driver → RunnerClient → HTTP `POST /tap` → XCUITest tap → 真 iOS UI。
3. 引入 `Cell` 类型（`src/sim/cell.ts`）：`{ id, udid, runnerPort, traceDir, session }`；`acquireCell({udid?, deviceName?, runtimeIdentifier?})` 返回 Cell（v1 单 Cell，`id='cell-0001'` 硬编码，`runnerPort=22087` 硬编码）。`SimctlDriver` ctor 接 `Cell` **或** `SimSession`（重载，向后兼容既有测试构造 `new SimctlDriver(session)`）。
4. `src/sim/runner-client.ts`：HTTP client 调 `GET /health`、`POST /tap`；构造时接 `{port, host?}`，默认 `host='127.0.0.1'`；用 Bun 原生 `fetch`（**不**引 ky/axios，CLAUDE.md §8.7）。
5. **不**做 runner xcodebuild 进程的内部启动 / lifecycle 管理 —— C6 runner 仍由外部脚本启动（参 `scripts/simx-runner-tap-smoke.sh` 模式）；Driver / RunnerClient 假定 runner 已经在 `:22087` listen，**首次** tap 前做一次 `GET /health` 探测，失败抛 `DRIVER_ERROR` with hint 指向脚本。runner 进程托管推 C7+。
6. 既有 63 swift tests + 106 TS tests **全部不退化**；C6 新增 TS tests 覆盖 RunnerClient + Cell + 改写后的 Driver.tap unit 路径。swift 侧 **零改动**（CLAUDE.md §9.6 不变量延伸 — Swift bridge C2-C5 已稳）。
7. 真 e2e：`examples/c6-tap.test.ts` 通过 `simx run` 在 dev sim 上 launch Settings + tap "General" + 验证跳页（用 SDK 调一次 RunnerClient `/tap` 做 probe assertion，与 C4 smoke 双面验证同模式）。

具体 deliverable：

1. **新增 `src/sim/cell.ts`** — `Cell` 类型 + `acquireCell(opts, deps)` + `releaseCell(cell)`
2. **新增 `src/sim/runner-client.ts`** — `RunnerClient` 类（`health()` / `tap(selector)`）+ 类型
3. **新增 `src/sim/__tests__/cell.test.ts`** — 5 case 覆盖 acquireCell delegate to acquireSession + Cell 形态 + releaseCell
4. **新增 `src/sim/__tests__/runner-client.test.ts`** — 8 case 覆盖 health / tap happy / 404 / 5xx / non-text-selector reject / fetch reject / JSON parse fail / port custom
5. **改 `src/driver/simctl-driver.ts`** — ctor 重载接 `Cell | SimSession`；`tap()` stub 改为真实现（**只**改 `tap`，17 个其它 stub 不动）；构造时注入 `RunnerClient`（可选，缺省 lazy 构造）
6. **改 `src/driver/__tests__/simctl-driver.test.ts`** — `unimplemented methods` 表删 `tap` 一行（17 个保留）；新增 `describe('tap')` 6 case：happy text → POST 调 / non-text selector → reject DRIVER_ERROR / runner 404 → ELEMENT_NOT_FOUND / runner 5xx → DRIVER_ERROR / fetch reject → DRIVER_ERROR with health hint / pre-flight health fail → DRIVER_ERROR
7. **改 `src/cli/commands/run.ts`** — 把 `pickSession` → `pickCell`（包既有 acquireSession 逻辑），`SimctlDriver` ctor 改传 cell；TracingDriver 不动
8. **改 `src/cli/__tests__/run.test.ts`**（如存在）— 调整 fixture 适配 cell；如不存在则跳过本项（探测后定）
9. **新增 `examples/c6-tap.test.ts`** — `launch com.apple.Preferences` + `tap({text:'General'})` + `tap({text:'About'})`（probe 假定跳到 General 后 About 出现；若 About 已 tap 不破 test，关键是 tap 不抛）
10. **新增 `scripts/simx-c6-tap-e2e.sh`** — 启 runner + 跑 `bun src/cli/index.ts run examples/c6-tap.test.ts` + 验证 exit 0；参 `simx-hostid-digitizer-smoke.sh` 模式

**真验证终点的机器可判断证据**（无人工读图，CLAUDE.md §5）：

- `bash scripts/simx-c6-tap-e2e.sh` exit 0 + stdout 单行 JSON 含 `{"health":200, "case_passed":1, "case_failed":0, "probe_after_miss":404, "probe_after_hit":200}`（脚本内启 runner → `simx run` → 完后用同 runner /tap probe 双面验证 Settings 已跳到 General 子页）
- `bun run test` ≥ **119 passed**（106 既有 + 6 driver tap + 5 cell + 8 runner-client = +19）
- `swift test --package-path swift-bridge` 仍 `Executed 63 tests, with 0 failures`（swift 侧零改动）

**不**做（推 C7+ / 后续 minor）：

- runner xcodebuild 子进程托管 / 启停 / health ping / 重启 / 僵尸检测 —— **C7+ / v0.7**（决策 #5）
- selector `id` / `label` / `role+name` / 任何 modifier (near/below/inside/nth/...) —— **v0.3 selector resolver**（C6 仅 text base form；其它 base 抛 `DRIVER_ERROR` with hint `"v0.3 selector resolver"`）
- `simx-host-hid` 接入 Driver —— **v0.3+**（决策 #1）
- 多 Cell / runner port allocator / `.simx/cells/<id>/` trace dir retrofit —— **v0.4-v0.5**（决策 #2）
- `doubleTap` / `longPress` / `fill` / `swipe` / `scroll` / `pressKey` —— C6 **不动** 17 个其它 stub；推 C7+
- runner `/health` 失败时的自动启动 —— **C7+**（C6 抛 DRIVER_ERROR with hint 指向手动启 runner 的脚本）
- tap 延迟 P50 metric —— **C7 e2e 出口验收**（参 plan-cold v0.2 出口验收）
- `pickCell` 在 main CLI 多个命令复用（list/doctor 暂不需要 cell）—— C6 只在 run.ts 内 inline

## 前置条件（已本机验证 — 2026-05-15）

```bash
# 1. 文件层 — C5 已归档，C6 未热化
test -f /Users/doracawl/workspace/labs/lab15-autofix/docs/plan-history/v0.2-c5-hot.md   # ✓
! test -e /Users/doracawl/workspace/labs/lab15-autofix/docs/plan-hot.md                  # ✓（本文件写入即创建）

# 2. C5 产出基线（必须仍绿；C6 不退化）
cd /Users/doracawl/workspace/labs/lab15-autofix && bun run test 2>&1 | grep -Eq 'Tests +106 passed'                            # ✓ 106
cd /Users/doracawl/workspace/labs/lab15-autofix/swift-bridge && swift test 2>&1 | grep -Eq 'Executed 63 tests, with 0 failures'  # ✓ 63
test -x /Users/doracawl/workspace/labs/lab15-autofix/swift-bridge/.build/debug/simx-host-hid                                   # ✓
test -f /Users/doracawl/workspace/labs/lab15-autofix/swift-bridge/Sources/SimxRunnerCore/SimxRunnerServer.swift                # ✓ C2 runner

# 3. v0.1 既有：SimctlDriver tap stub 仍在
grep -q "this.notSupported('tap', 'HID bridge')" /Users/doracawl/workspace/labs/lab15-autofix/src/driver/simctl-driver.ts      # ✓

# 4. dev-sim 锁定 + booted
cat /Users/doracawl/workspace/labs/lab15-autofix/.simx/dev-sim.txt   # ✓ 7B88259F-D864-4F98-9F2C-F0424216EE64
xcrun simctl list devices -j | jq -r --arg u "$(cat /Users/doracawl/workspace/labs/lab15-autofix/.simx/dev-sim.txt)" \
  '.devices | to_entries[] | .value[] | select(.udid==$u) | .state'   # ✓ "Booted"

# 5. C5 probe 真值（HID 基础设施仍 work，C6 不依赖但要确认环境健康）
swift-bridge/.build/debug/simx-host-hid probe | jq -e '.ok == true'   # ✓ true

# 6. C1 runner 仍可起（C6 e2e 依赖）
bash scripts/simx-runner-health.sh | jq -e '.status == 200'   # ✓ 200

# 7. C2 runner /tap 仍工作（C6 复用同 endpoint）
bash scripts/simx-runner-tap-smoke.sh | jq -e '.health == 200 and .tap_hit == 200 and .tap_miss == 404'   # ✓

# 8. 工具链
xcodebuild -version | head -1 | grep -Eq '^Xcode 26\.'                # ✓ Xcode 26.4.1
bun --version | grep -Eq '^[12]\.'                                     # ✓
```

**全部 ✓** → 入口条件通过，**不**拒绝热化。

## 步骤（线性，无分叉；3 个 step）

> **分组依据**：
> - **S1** = Cell 抽象（`Cell` 类型 + `acquireCell` + `releaseCell`）单测纯单元，零网络。**不**改 Driver / CLI。
> - **S2** = `RunnerClient` HTTP wrapper 单测（注入 fetch fake），**不**碰真 socket；之后改 `SimctlDriver.tap` 调 RunnerClient（注入 fake 测） + 改 `run.ts` 用 Cell。
> - **S3** = 端到端真链路：`examples/c6-tap.test.ts` + `scripts/simx-c6-tap-e2e.sh` + 真 dev sim 跑通双面验证。

---

### S1. `Cell` 类型 + `acquireCell` + `releaseCell`（单元测试 红→绿）

> **决策依据（见末尾"关键技术决策"表 #2）**：v1 单 Cell 跑，Cell 抽象本 checkpoint 落 L1 最小一步——`Cell` 类型形态 + `acquireCell` 包 `acquireSession` + 默认 `runnerPort=22087` 硬编码 + 默认 `traceDir='.simx/trace'`（v0.1 既有路径，**不** retrofit 到 `.simx/cells/<id>/`，决策 #2-延伸）；保留 retrofit 接口供 v0.4 改。Driver ctor 重载接 Cell 或 SimSession 兼容既有测试。

**红（写测试）**

- 文件 1（新建）：`src/sim/__tests__/cell.test.ts`
  - **5 个 test case**：
    1. `'acquireCell defaults: udid passthrough, runnerPort=22087, traceDir=.simx/trace, id=cell-0001'` — fake client + `acquireCell({udid:'X'}, {client})` → 返回的 cell 字段断言
    2. `'acquireCell errors propagate from acquireSession (DEVICE_NOT_FOUND)'` — fake client listDevices 返 `[]` → `acquireCell` rejects DeviceNotFoundError
    3. `'acquireCell with custom runnerPort'` — `acquireCell({udid:'X', runnerPort: 33000}, {client})` → `cell.runnerPort === 33000`
    4. `'acquireCell cell.session is SimSession instance with same udid'` — 验证 `cell.session instanceof SimSession && cell.session.udid === 'X'`
    5. `'releaseCell delegates to session.release()'` — spy session.release 调用一次

- 跑（红）：
  ```bash
  cd /Users/doracawl/workspace/labs/lab15-autofix
  bun x vitest run src/sim/__tests__/cell.test.ts 2>&1 | tail -10
  ```
- 期望 **红**：`Cannot find module '../cell'` 或 `acquireCell is not a function`

**绿（实现）**

- 文件 2（新建）：`src/sim/cell.ts`
  - **锁形状（代码作者自写函数体；以下是 API + 不变量）**：
    ```ts
    import type { SimctlClient } from './simctl.js'
    import { acquireSession, type AcquireOptions, type SimSession } from './session.js'

    /**
     * v1 runtime container: 1 booted simulator + 1 runner port + 1 trace dir.
     * v0.4-v0.6 will retrofit traceDir to `.simx/cells/<id>/`; v0.6+ adds
     * runner port allocator. C6 keeps single-cell defaults.
     */
    export type Cell = {
      readonly id: string             // v1 single cell: 'cell-0001'
      readonly udid: string
      readonly runnerPort: number     // v1 default 22087
      readonly traceDir: string       // v1 keeps '.simx/trace' (no per-cell prefix)
      readonly session: SimSession    // raw SimSession kept addressable for simctl ops
    }

    export type AcquireCellOptions = AcquireOptions & {
      runnerPort?: number             // override (test fixtures); default 22087
      traceDir?: string               // override; default '.simx/trace'
      id?: string                     // override; default 'cell-0001'
    }

    export type AcquireCellDeps = {
      client: SimctlClient
    }

    export const DEFAULT_RUNNER_PORT = 22087
    export const DEFAULT_TRACE_DIR = '.simx/trace'
    export const DEFAULT_CELL_ID = 'cell-0001'

    export async function acquireCell(
      opts: AcquireCellOptions,
      deps: AcquireCellDeps,
    ): Promise<Cell> {
      const { runnerPort, traceDir, id, ...sessionOpts } = opts
      const session = await acquireSession(sessionOpts, { client: deps.client })
      return {
        id: id ?? DEFAULT_CELL_ID,
        udid: session.udid,
        runnerPort: runnerPort ?? DEFAULT_RUNNER_PORT,
        traceDir: traceDir ?? DEFAULT_TRACE_DIR,
        session,
      }
    }

    export async function releaseCell(
      cell: Cell,
      opts: { shutdown?: boolean } = {},
    ): Promise<void> {
      await cell.session.release(opts)
    }
    ```

    **关键不变量**：
    1. `Cell` 是 `readonly` 字段——pass by reference，无 setter（避免 v0.4 multi-cell 时 mutation race）
    2. `acquireCell` 是**纯 delegation**——所有"sim 找 + boot"逻辑走既有 `acquireSession`；C6 不复制粘贴
    3. `traceDir` 默认 `'.simx/trace'`（v0.1 形态）— v0.4 retrofit 到 `'.simx/cells/<id>/'`；C6 **不**预早动 trace 文件结构（CLAUDE.md §8.1 范围纪律）
    4. `runnerPort` 默认 22087——与 C1 server hardcoded port 一致（v0.6+ 改 allocator）
    5. `id` 默认 `'cell-0001'`——v1 单 Cell；v0.4+ 启用 counter / hash

- 跑（绿）：
  ```bash
  cd /Users/doracawl/workspace/labs/lab15-autofix
  bun run typecheck 2>&1 | tail -3   # exit 0
  bun x vitest run src/sim/__tests__/cell.test.ts 2>&1 | tail -10
  ```
- 期望 **绿**：5 case 全过；typecheck exit 0

**重构**

- 不动 `src/sim/session.ts`（保持 acquireSession 向后兼容）
- 不动 `src/sim/simctl.ts`
- 不引新 dep

---

### S2. `RunnerClient` HTTP + `SimctlDriver.tap` 真实现 + `run.ts` 改用 Cell（单元测试 红→绿）

> **决策依据（见决策 #1 #3 #4）**：选 **runner-only** transport（决策 #1：选 (A) 仅 HTTP runner /tap）。RunnerClient 用 Bun 原生 `fetch`（决策 #4），构造时接 port + host（默认 127.0.0.1）。`SimctlDriver` ctor 加重载：`new SimctlDriver(session)` 旧 + `new SimctlDriver(cell)` 新 + `new SimctlDriver(cell, {runnerClient})` 测试注入；缺省 lazy 构造 `RunnerClient` from `cell.runnerPort`。tap 实现：(1) selector 必须是 base text form（无 modifier）—— 否则抛 DRIVER_ERROR with hint `"v0.3 selector resolver"`；(2) 首次 tap pre-flight `runnerClient.health()` 失败 → DRIVER_ERROR with hint 指向 `scripts/simx-runner-tap-smoke.sh` 启动方式（决策 #5）；(3) POST `/tap` body `{selector:{text:<string>}}` —— 与 C2 server 已 freeze 的 wire 形状字节一致；(4) 404 → `ELEMENT_NOT_FOUND`；(5) 其它非 200 / fetch reject → `DRIVER_ERROR`。

**红（写测试）**

- 文件 3（新建）：`src/sim/__tests__/runner-client.test.ts`
  - **8 个 test case**：
    1. `'health: GET /health returns ok'` — fake fetch returning `{ok:true,status:200,json:()=>({ok:true})}` → `await client.health() === true`
    2. `'health: 500 returns false'` — fake fetch returning 500 → `client.health() === false`
    3. `'health: fetch rejects → returns false'` — fake fetch throws → `client.health() === false`（**不**抛——caller 决定怎么处理）
    4. `'tap text happy: 200 → resolves; correct URL + body'` — fake fetch returning `{status:200,ok:true,json:()=>({ok:true,matched:{label:'General'}})}` → URL=`http://127.0.0.1:22087/tap`，method POST，header Content-Type=application/json，body=`{"selector":{"text":"General"}}`
    5. `'tap 404: throws TapNotFoundError with selector'` — fake fetch returning 404 → throws `TapNotFoundError`（自定义错误类，包 selector + body.error）
    6. `'tap 5xx: throws RunnerTransportError with body snippet'` — 500 → throws `RunnerTransportError`
    7. `'tap fetch reject: throws RunnerTransportError wrapping cause'` — fetch throws ECONNREFUSED → throws `RunnerTransportError` with message containing 'fetch failed'/'ECONNREFUSED'
    8. `'tap with custom port + host: URL reflects both'` — `new RunnerClient({port:33000, host:'localhost'})` → URL=`http://localhost:33000/tap`

- 文件 4（改）：`src/driver/__tests__/simctl-driver.test.ts`
  - `unimplemented methods` 表删 `tap` 一行（保留 17 个：doubleTap / longPress / fill / clear / swipe / scroll / scrollTo / pressKey / hideKeyboard / tree / findOne / findAll / waitFor / background / foreground / setLocale / setNetwork）
  - 新增 `describe('tap')` block — **6 个 case**：
    1. `'tap text happy: posts to runner with correct body'` — fake RunnerClient.tap resolves; driver.tap({text:'General'}) resolves; verify RunnerClient.tap called once with selector arg
    2. `'tap pre-flight health fail: throws DRIVER_ERROR with hint pointing to runner script'` — fake RunnerClient.health → false (first call); driver.tap rejects ExpectationFailure with code DRIVER_ERROR + hint includes 'simx-runner' (script name fragment)
    3. `'tap non-text base selector ({id:...}): throws DRIVER_ERROR with hint "v0.3 selector resolver"'`
    4. `'tap text + modifier (near): throws DRIVER_ERROR with hint "v0.3 selector resolver"'`
    5. `'tap runner returns 404: throws ELEMENT_NOT_FOUND with selector echoed'` — fake RunnerClient.tap throws `TapNotFoundError`; driver.tap rejects ExpectationFailure code='ELEMENT_NOT_FOUND' selector set
    6. `'tap RunnerTransportError: throws DRIVER_ERROR with hint hint mentioning runner port'` — fake RunnerClient.tap throws `RunnerTransportError`; driver.tap rejects ExpectationFailure code='DRIVER_ERROR'

- **改测试 fixture**：`makeDriver()` 内（既有）的 `new SimctlDriver(session)` 保持 — 这一对路径必须仍工作（向后兼容，决策 #6）；新 tap test 用一个新工厂 `makeDriverWithRunner()` 注入 fake RunnerClient

- 跑（红）：
  ```bash
  cd /Users/doracawl/workspace/labs/lab15-autofix
  bun x vitest run src/sim/__tests__/runner-client.test.ts 2>&1 | tail -10
  bun x vitest run src/driver/__tests__/simctl-driver.test.ts 2>&1 | tail -10
  ```
- 期望 **红**：`Cannot find module './runner-client'` / `TapNotFoundError is not exported` / `SimctlDriver(cell)` 不接受 Cell type / driver.tap rejects with hint 'HID bridge (v0.2)' 与新 case 断言不符

**绿（实现）**

- 文件 5（新建）：`src/sim/runner-client.ts`
  - **锁形状**：
    ```ts
    import type { Selector } from '../core/index.js'

    export class TapNotFoundError extends Error {
      readonly selector: Selector
      readonly body: string
      constructor(selector: Selector, body: string) {
        super(`tap not_found for selector ${JSON.stringify(selector)}`)
        this.name = 'TapNotFoundError'
        this.selector = selector
        this.body = body
        Object.setPrototypeOf(this, new.target.prototype)
      }
    }

    export class RunnerTransportError extends Error {
      readonly status: number | undefined
      readonly cause: unknown
      constructor(message: string, opts: { status?: number; cause?: unknown } = {}) {
        super(message)
        this.name = 'RunnerTransportError'
        this.status = opts.status
        this.cause = opts.cause
        Object.setPrototypeOf(this, new.target.prototype)
      }
    }

    export type RunnerClientOptions = {
      port: number
      host?: string                  // default '127.0.0.1'
      fetchImpl?: typeof fetch       // test injection; default globalThis.fetch
    }

    /**
     * Thin HTTP client over the SimxRunner XCUITest server (C2). C6 uses
     * only GET /health + POST /tap; future checkpoints add /screenshot,
     * /locate, etc. against this same client.
     */
    export class RunnerClient {
      private readonly base: string
      private readonly fetchImpl: typeof fetch

      constructor(opts: RunnerClientOptions) {
        const host = opts.host ?? '127.0.0.1'
        this.base = `http://${host}:${opts.port}`
        this.fetchImpl = opts.fetchImpl ?? fetch
      }

      async health(): Promise<boolean> {
        try {
          const res = await this.fetchImpl(`${this.base}/health`)
          return res.ok && res.status === 200
        } catch {
          return false
        }
      }

      /**
       * Only `{ text: string }` base selectors are supported in C6.
       * Caller (SimctlDriver) is responsible for rejecting other forms;
       * RunnerClient stays narrow so it doesn't grow a selector compiler.
       */
      async tap(selector: { text: string }): Promise<void> {
        const body = JSON.stringify({ selector })   // wire-frozen with C2
        let res: Response
        try {
          res = await this.fetchImpl(`${this.base}/tap`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body,
          })
        } catch (e) {
          const msg = e instanceof Error ? e.message : String(e)
          throw new RunnerTransportError(`runner /tap fetch failed: ${msg}`, { cause: e })
        }
        if (res.status === 404) {
          const text = await res.text().catch(() => '')
          throw new TapNotFoundError(selector, text)
        }
        if (!res.ok) {
          const text = await res.text().catch(() => '')
          throw new RunnerTransportError(
            `runner /tap returned status ${res.status}: ${text.slice(0, 200)}`,
            { status: res.status },
          )
        }
      }
    }
    ```

    **关键不变量**：
    1. `fetchImpl` 注入点是**唯一**测试 hook —— 单测不 `vi.mock('node:http')` 不 spy fetch（CLAUDE.md §8 测试纪律）
    2. `tap()` 入参类型**收窄到** `{text: string}` —— RegExp / 其它 selector form 在 Driver 层拒绝；wire 上永远只发字符串（与 C2 server 假设一致）
    3. `health()` **不**抛 —— 区分"工具坏"vs"网络问题"由 caller 通过 boolean 决定怎么处理
    4. `tap()` **不**重试 —— 重试推 C7+

- 文件 6（改）：`src/driver/simctl-driver.ts`
  - 加 import：
    ```ts
    import type { Cell } from '../sim/cell.js'
    import { RunnerClient, TapNotFoundError, RunnerTransportError } from '../sim/runner-client.js'
    import { isTextSelector, describeSelector } from '../core/index.js'
    ```
  - ctor **重载**：
    ```ts
    type SimctlDriverInput = Cell | SimSession
    type SimctlDriverDeps = { runnerClient?: RunnerClient }

    // Public ctor takes either:
    //   new SimctlDriver(session)               // v0.1 form, kept for tests
    //   new SimctlDriver(cell)                  // v0.2 C6 form (Cell)
    //   new SimctlDriver(cell, { runnerClient: fakeRunner })  // test injection
    constructor(input: SimctlDriverInput, deps?: SimctlDriverDeps) {
      if (isCell(input)) {
        this.session = input.session
        this.cell = input
        this.runnerClient = deps?.runnerClient
          ?? new RunnerClient({ port: input.runnerPort })
      } else {
        // SimSession path — no runner; tap will fail-fast with helpful hint.
        this.session = input
        this.cell = undefined
        this.runnerClient = deps?.runnerClient    // may be undefined → tap throws
      }
      this.lastLaunchedBundleId = undefined
      this.healthChecked = false
    }

    private readonly cell: Cell | undefined
    private readonly runnerClient: RunnerClient | undefined
    private healthChecked: boolean
    ```
    `isCell` helper：`function isCell(x: SimctlDriverInput): x is Cell { return 'session' in x && 'runnerPort' in x }`

  - `tap()` **实现**（替换既有 stub）：
    ```ts
    async tap(selector: Selector): Promise<void> {
      if (!this.isPlainTextSelector(selector)) {
        throw new ExpectationFailure({
          code: 'DRIVER_ERROR',
          message: `Driver.tap() does not support this selector in v0.2: ${describeSelector(selector)}`,
          selector,
          hint: 'only base { text: string } is supported in v0.2; id/role/label and modifiers (near/below/...) need the v0.3 selector resolver',
        })
      }
      if (this.runnerClient === undefined) {
        throw new ExpectationFailure({
          code: 'DRIVER_ERROR',
          message: 'Driver.tap() requires a RunnerClient; construct SimctlDriver with a Cell',
          selector,
          hint: 'use acquireCell(...) instead of acquireSession(...) when building the driver',
        })
      }
      if (!this.healthChecked) {
        const alive = await this.runnerClient.health()
        if (!alive) {
          throw new ExpectationFailure({
            code: 'DRIVER_ERROR',
            message: `SimxRunner not reachable on port ${this.cell?.runnerPort ?? '?'}`,
            selector,
            hint: 'start the runner first: bash scripts/simx-runner-tap-smoke.sh (or simx-runner-health.sh)',
          })
        }
        this.healthChecked = true
      }
      try {
        await this.runnerClient.tap({ text: (selector as { text: string }).text })
      } catch (e) {
        if (e instanceof TapNotFoundError) {
          throw new ExpectationFailure({
            code: 'ELEMENT_NOT_FOUND',
            message: `element not found by runner: ${describeSelector(selector)}`,
            selector,
            hint: 'verify the text label matches an XCUIElement label or identifier (NSPredicate label==% OR identifier==%)',
          })
        }
        if (e instanceof RunnerTransportError) {
          throw new ExpectationFailure({
            code: 'DRIVER_ERROR',
            message: `Driver.tap() transport failure: ${e.message}`,
            selector,
            hint: `runner port=${this.cell?.runnerPort ?? '?'} unreachable or returned non-2xx; check xcodebuild test log`,
          })
        }
        throw e
      }
    }

    private isPlainTextSelector(s: Selector): s is { text: string } {
      if (!isTextSelector(s)) return false
      if (typeof s.text !== 'string') return false   // RegExp推 v0.3
      const mods: ReadonlyArray<keyof Selector> = ['near','below','above','leftOf','rightOf','inside','nth','first','last']
      return mods.every((k) => (s as Record<string, unknown>)[k] === undefined)
    }
    ```

    **关键不变量**：
    1. `tap` 是**唯一**改动的方法；17 个其它 `notSupported` stub 字节不变（CLAUDE.md §9 驱动接口不变量延伸）
    2. `healthChecked` 是 instance state —— 同 driver 后续 tap **不**重复 health probe（性能）
    3. RegExp text 推 v0.3 —— C6 显式拒绝（错误类型 DRIVER_ERROR with hint "v0.3 selector resolver"）
    4. `SimctlDriver(session)` 旧路径**保留** —— tap 调时若 runnerClient undefined 给 actionable hint

- 文件 7（改）：`src/cli/commands/run.ts`
  - import：`import { acquireCell, releaseCell } from '../../sim/cell.js'`
  - 把 `pickSession` 重命名为 `pickCell`，内部包既有 `acquireSession` 逻辑——但用 `acquireCell` 调；返回 Cell
  - `runRunCommand` body：
    ```ts
    let cell: Cell
    try {
      cell = await pickCell(deps.client, deps.select)
    } catch (err) { ... }

    const base = new SimctlDriver(cell)
    const tracer = new TracingDriver(base, join(deps.cwd, '.simx', 'trace'))
    // ... unchanged

    try {
      const result = await runRegistry({ driver: tracer, onCaseStart: ... })
      ...
    } finally {
      await releaseCell(cell).catch(() => undefined)
    }
    ```
  - `pickSession` → `pickCell` 重命名 + return type `Promise<Cell>`；body 把 4 个 `acquireSession({...}, {client})` 调用都改为 `acquireCell({...}, {client})`

- 文件 8（改 — 若存在则改，否则跳过）：检查 `src/cli/__tests__/run.test.ts` 是否存在。**注**：探测显示 `src/cli/__tests__/` 下当前只有 `doctor.test.ts`，**run.ts 暂无 unit 测试**（v0.1 已确认）—— 故 **跳过本项**，CLI run 行为靠 e2e（S3）验证

- 文件 9（改）：`src/sim/__tests__/session.test.ts`（如果有任何已存在的 import session 的测试）—— **不改**（session.ts 字节不变）

- 跑（绿）：
  ```bash
  cd /Users/doracawl/workspace/labs/lab15-autofix
  bun run typecheck 2>&1 | tail -3   # exit 0
  bun x vitest run src/sim/__tests__/runner-client.test.ts 2>&1 | tail -10
  bun x vitest run src/driver/__tests__/simctl-driver.test.ts 2>&1 | tail -10
  bun run test 2>&1 | grep -E 'Tests +[0-9]+ passed' | tail -1
  ```
- 期望 **绿**：
  - typecheck exit 0
  - runner-client 8 case 全过
  - driver 既有 case + 新 6 case 全过（既有 `unimplemented methods` 17 case 全跑）
  - `Tests 125 passed (125)` — 106 既有 - 1 删除 tap stub case + 6 tap 新 case + 5 cell + 8 runner-client = 124 (起码) 实际 ≥ 119（含些许波动空间）

**重构**

- 不在 `simctl-driver.ts` 内 inline `RunnerClient` constructor —— 留 deps inject 路径
- 不动 17 个 `notSupported` stub 一行（含 `tap` 移走后 `UNSUPPORTED_HINTS.HID bridge` 是否还被引用——还被 doubleTap/longPress/.../hideKeyboard 等 9 个用，**保留**）
- 不引新 dep

---

### S3. `examples/c6-tap.test.ts` + e2e 脚本 + 真 dev sim 验证（红→绿 + e2e 绿）

> **决策依据**：e2e 脚本沿用 C4 `simx-hostid-digitizer-smoke.sh` 的 5-Phase 模式 — 启 runner → wait health → 跑 `simx run` → 用同 runner port 做 probe 双面验证（Settings 跳到 General 后："Display & Brightness" /tap → 404；"About" /tap → 200）。脚本 stdout 输出单行 JSON 给 CI 判断。

**红（写 example + 脚本）**

- 文件 10（新建）：`examples/c6-tap.test.ts`
  - **1 个 test case**：
    ```ts
    import { test } from '../src/sdk/index.js'

    test('settings: tap General navigates to sub-page', async ({ app }) => {
      await app.launch('com.apple.Preferences', {
        args: ['-AppleLanguages', '(en)', '-AppleLocale', 'en_US'],
        fresh: true,
      })
      // C6 wire path: SDK → App → SimctlDriver.tap → RunnerClient → POST /tap
      // → SimxRunner XCUITest NSPredicate match → XCUIElement.tap() → real iOS UI.
      await app.tap({ text: 'General' })
      // No assertion needed — the e2e shell wrapper probes the runner's
      // /tap endpoint after this test exits to verify the screen really
      // pushed (double-sided probe, mirrors C4 smoke shape).
    })
    ```
  - **WHY 不在 TS test 内做 probe assertion**：probe 调 `/tap` 改 UI state（POST /tap 404 也可能改 sim state）。让 shell wrapper 在 simx run 退出后做 double-sided probe，与 C4 smoke 一致；TS test 保持"用户视角最小代码"。

- 文件 11（新建）：`scripts/simx-c6-tap-e2e.sh`
  - **结构**（参 `simx-hostid-digitizer-smoke.sh` 4 phase，改为 5 phase）：
    - Phase 0：环境检查（dev-sim 存在 + booted + simx-host-hid optional + port free + simx-host-hid 已 build optional skip——C6 不依赖 host-hid，但 build 检查仍可 reuse）
    - Phase A：启 xcodebuild test backgrounded（与 C4 smoke 同），trap cleanup
    - Phase B：poll `GET /health` up to 150s 直到 200
    - Phase C：`bun src/cli/index.ts run examples/c6-tap.test.ts` —— 期望 exit 0 + stdout 含 `1 passed, 0 failed`
    - Phase D：probe `POST /tap` `{selector:{text:"Display & Brightness"}}` → 期望 404 (`.ok==false and .error=="not_found"`)
    - Phase E：probe `POST /tap` `{selector:{text:"About"}}` → 期望 200 (`.ok==true`)
    - Phase F：stdout 单行 summary JSON：
      ```bash
      printf '{"health":%s,"case_passed":%s,"case_failed":%s,"probe_after_miss":%s,"probe_after_hit":%s}\n' \
             "$HEALTH_STATUS" "$CASE_PASSED" "$CASE_FAILED" "$MISS_STATUS" "$HIT_STATUS"
      ```
  - exit 0 if 全部 phase 通过，否则 1/2 + stderr 含 xcodebuild log tail

- 跑（红）：
  - `examples/c6-tap.test.ts` 不在 `bun run test` 范围（被 `examples/` 路径排除 — 探测 `vitest.config` / 默认）；用 `bun src/cli/index.ts run examples/c6-tap.test.ts` 调；当前 driver tap 已实现，但若 runner 未启 → 期望明确的 DRIVER_ERROR with hint 字符串包 'simx-runner'
  - 先**不启 runner**跑一次 → 期望 exit 1 + stderr 含 `FAIL settings: tap General` + hint `simx-runner`

**绿（启 runner，跑 e2e）**

- 跑（绿）：
  ```bash
  cd /Users/doracawl/workspace/labs/lab15-autofix
  bash scripts/simx-c6-tap-e2e.sh
  echo "exit=$?"
  ```
- 期望 **绿**：
  - exit 0
  - stdout 单行 JSON 含 `{"health":200,"case_passed":1,"case_failed":0,"probe_after_miss":404,"probe_after_hit":200}`

**重构**

- 不抽 e2e 脚本 helper（C4 / C2 / C6 三脚本短期都重复 launch xcodebuild / wait health / cleanup pattern）—— v0.7 出口验收时再合并
- 不改 `examples/screenshot-only.test.ts` / `examples/login.test.ts`（后者仍是 v1 黄金路径文档样本，未完成跑通——v0.3+ 工作）

---

## Checkpoint C6 验收

### 三类：unit（swift 不退化 + TS 增多）+ build（typecheck + simx-host-hid 不需要重 build）+ e2e（runner 启动 + simx run + double-sided probe）

```bash
cd /Users/doracawl/workspace/labs/lab15-autofix

# ---- 1. 文件存在性 ----
test -f src/sim/cell.ts                                                   # 新
test -f src/sim/runner-client.ts                                          # 新
test -f src/sim/__tests__/cell.test.ts                                    # 新
test -f src/sim/__tests__/runner-client.test.ts                           # 新
test -f examples/c6-tap.test.ts                                           # 新
test -x scripts/simx-c6-tap-e2e.sh                                        # 新
test -f src/driver/simctl-driver.ts                                       # 改
test -f src/driver/__tests__/simctl-driver.test.ts                        # 改
test -f src/cli/commands/run.ts                                           # 改

# Swift bridge 0 改动（不退化）
test -f swift-bridge/Sources/SimxRunnerCore/SimxRunnerServer.swift
test -f swift-bridge/Sources/SimxIndigoHID/IOHIDDigitizerTap.swift
test -f swift-bridge/Sources/SimxIndigoHID/InputChannel.swift

# ---- 2. Swift tests 不退化（swift 侧 0 改动）----
swift test --package-path swift-bridge 2>&1 | grep -E 'Executed +[0-9]+ tests' | tail -1
# 期望含：Executed 63 tests, with 0 failures

# ---- 3. TS typecheck + tests（既有 + 新增 = ≥ 119 passed）----
bun run typecheck 2>&1 | tail -3
# 期望 exit 0

bun run test 2>&1 | grep -E 'Tests +[0-9]+ passed' | tail -1
# 期望含 'Tests 119 passed (119)' 或更多（106 + 19 = 125 上限；下限 119 容差 6）

# ---- 4. SimctlDriver tap stub 已被替换（grep 反向验证）----
! grep -q "this.notSupported('tap', 'HID bridge')" src/driver/simctl-driver.ts
# 期望 exit 0（grep 没找到 → tap stub 已删）

! grep -qE "'tap', hintFragment: 'HID bridge'" src/driver/__tests__/simctl-driver.test.ts
# 期望 exit 0（既有"tap throws DRIVER_ERROR HID bridge"行已删）

# ---- 5. 既有 v0.1 单测（driver / sim / cli）pattern 行为仍存（向后兼容）----
grep -q "background.*hintFragment.*HID/system bridge" src/driver/__tests__/simctl-driver.test.ts
# 期望 exit 0（17 个 unimplemented 一个 background 留作哨兵）

# ---- 6. e2e — 端到端真链路 ----
bash scripts/simx-c6-tap-e2e.sh > /tmp/c6-e2e-summary.json 2> /tmp/c6-e2e-stderr.log
echo "e2e-exit=$?"
jq -e '.health == 200 and .case_passed == 1 and .case_failed == 0 and .probe_after_miss == 404 and .probe_after_hit == 200' /tmp/c6-e2e-summary.json
# 期望 e2e-exit=0 + jq exit 0

# ---- 7. driver.tap 真链路在 cli run 失败模式（runner 未启）也 actionable ----
# 端口未启的 fail-fast 测试（手工，非 CI）：
# bash -c 'lsof -iTCP:22087 -sTCP:LISTEN | head -1'    # 期望无输出
# bun src/cli/index.ts run examples/c6-tap.test.ts 2>&1 | grep -E 'simx-runner|DRIVER_ERROR'   # 期望 stderr 含 hint
# 此 checkpoint 验收命令不强制跑（依赖端口状态），但 e2e 脚本 Phase A 已隐含覆盖

# ---- 8. CLAUDE.md §9 不变量 ----
# Driver 接口签名未变
! git diff src/driver/types.ts 2>/dev/null | grep -E '^[+-]'
# 期望无输出（types.ts 0 改动）

# Swift bridge 源 0 改动
! find swift-bridge/Sources -name '*.swift' -newer docs/plan-history/v0.2-c5-hot.md -print | head -1
# 期望无输出
```

期望（机器可判断 — 8 个条件全部满足，无人工读图）：
- 所有 `test -f` / `test -x` 文件存在断言 0 退出
- swift test：`Executed 63 tests, with 0 failures`（**完全不退化**）
- `bun run test` ≥ 119 passed（106 + 13 下限 / 上限 125）
- `bun run typecheck` exit 0
- `grep -q "notSupported('tap'" src/driver/simctl-driver.ts` exit **非 0**（stub 已删）
- e2e 脚本 exit 0 + JSON 5 个字段全部 expected 值
- `src/driver/types.ts` 0 改动
- `swift-bridge/Sources/**` 0 改动

## 完成后动作

1. `mv docs/plan-hot.md docs/plan-history/v0.2-c6-hot.md`
2. main convo 调 sub-agent 生成 v0.2 C7 `plan-hot.md`（候选：tap latency P50 < 50ms metric + `examples/calc-tap.test.ts` 跑通 + runner lifecycle 内部托管 — 参 plan-cold v0.2 C7 概要）

---

## 关键技术决策（在 plan 里现场判定 — 决策影响 step 形态）

| # | 决策点 | 选 | 理由 |
|---|---|---|---|
| 1 | **C6 tap transport 选哪个？** | **(A) 仅 HTTP runner /tap** | (B) host-hid 是坐标 tap，C6 没有 text→coord 解析（v0.3 工作）；(C) /locate endpoint runner 内未实现；(D) host-hid fallback 路径需要 runner /locate；最简且 ROI 最高是 (A)——C2 endpoint 已 freeze，C6 接通即可让用户 `app.tap({text:'General'})` 真跳页。host-hid 留作"v0.3 接 selector resolver 时一起接 Driver"——届时 runner 给 a11y frame coord，host-hid 真 tap（C4 路径），延迟从~数十ms 压回~ms。**v1.md §2.A 表"主路径 9-arg/digitizer，兜底 XCUITest"在 C6 暂时反过来——XCUITest 是 C6 默认；这是阶段性临时态，C6 hint 内已记录推 v0.3。** |
| 2 | **Cell 抽象 C6 scope** | **L1 最小落地：Cell type + acquireCell + Driver 接 Cell** | design.md §Cell 模式说 v0.4-v0.6 必须 cell-aware；v1.md §2.B 说 v1 "API 必须 cell-aware 避免 retrofit"。C6 是 v0.2 最后一个 checkpoint（之后 v0.3 进 selector resolver），现在不引 Cell 会让 v0.3 又要 retrofit driver ctor 一次。**但**完整 multi-cell + port allocator + traceDir prefix retrofit 都推 v0.4-v0.6（CLAUDE.md §8.1 范围纪律）。C6 落 Cell type + acquireCell pure delegation + Driver ctor 接 Cell（向后兼容 SimSession） + traceDir/runnerPort 默认 v0.1 形态——这是"接口现在 cell-aware，实现 v0.4 再深化"的折中。 |
| 3 | **runner 生命周期怎么管？** | **C6 不管 — 假定外部脚本启动** | xcodebuild test 是 ~30s cold start 后台进程；C6 driver 启它会导致：(a) 单测难注入（必须 mock child_process —— 违反测试纪律 CLAUDE.md §8 不直接 import node:child_process 进单测），(b) 测试时 spawn 真 xcodebuild = 测试不可重复；(c) cleanup race（kill xcodebuild 系列 PID tree 需要专门 helper）。**C6 折中**：Driver 假定 runner 已 listen；首 tap 前 health probe 失败 → DRIVER_ERROR with hint 指向 `scripts/simx-runner-tap-smoke.sh`（用户/CI 手动启）。Runner 内部托管推 **C7+**（届时已知"启停 race 问题"是真问题再做）。 |
| 4 | **HTTP client 用什么？** | **Bun 原生 `fetch`（globalThis.fetch）** | CLAUDE.md §8.7 不引新 dep；fetch 在 bun 是原生稳定 API；node 22.x+ 也原生 fetch；测试 fixture 通过 `fetchImpl` 注入点解耦，不 mock global —— 测试纪律 §8 满足。ky/axios 都引入 transitive 依赖（CommonJS/types/...），无收益。 |
| 5 | **runner 健康 / 重启** | **C6 仅做首 tap 前 1 次 health probe，失败 fail-fast** | plan-cold v0.7 已记 runner 长跑僵尸化 + 每 50 case 重启 health ping。C6 跑通即可，僵尸检测 v0.7。fail-fast 形态：actionable hint 字符串 `'start the runner first: bash scripts/simx-runner-tap-smoke.sh'`——AI / 用户能 0-step 修复。|
| 6 | **SimctlDriver ctor 接 Cell vs SimSession** | **重载 — 同时支持** | 既有 18 个 unimplemented 测试都用 `new SimctlDriver(session)`；改全部 fixture 是不必要 churn。重载形态：runtime check `'session' in input && 'runnerPort' in input` 判 Cell；SimSession 路径 runnerClient 为 undefined，tap 调时给 actionable hint 教用户用 acquireCell。 |
| 7 | **selector 非 text base form 怎么报错** | **同一类 DRIVER_ERROR，hint 包 "v0.3 selector resolver"** | 既有 `notSupported('tap','HID bridge')` 是阶段 stub；C6 拆成两类错误：(a) selector 是 text base form → 真去 runner；(b) 其它（id/role/label/有 modifier/RegExp）→ DRIVER_ERROR hint `'only base { text: string } is supported in v0.2; id/role/label and modifiers need the v0.3 selector resolver'`。这是 AI-readable 失败信息（CLAUDE.md §9.5）。 |
| 8 | **是否在 C6 接 doubleTap / fill / longPress / swipe？** | **不接** | plan-cold v0.2 C6 概要写 "tap/doubleTap/longPress/swipe/fill 接通"——但 (a) C2 runner 当前**只**实现 `/tap`；接 fill 要 runner 加 `/type-text` endpoint + Swift 侧改动（违反 C6"swift 侧 0 改动"约束），(b) swipe/scroll 需要 runner gesture API（不存在），(c) fill 长短文本分流要 pasteboard，但 pasteboardSet 已在 v0.1 work，fill 是 pasteboard.set + tap + cmd-v 序列——属于 v0.3 工作。**C6 砍范围**到只接 tap；其它 4 个交互方法继续 stub，hint 改为 `'requires runner /xxx endpoint (v0.3)'`。这是 plan-cold 与 hot 化的合理偏差，列入 v1.md 决策日志在 C6 close 时补一行。 |
| 9 | **traceDir 是否在 C6 retrofit 到 `.simx/cells/<id>/`?** | **不** retrofit | design.md §Cell 模式末尾 "v0.1 / v0.2 的 .simx/trace/<case>/ 会在 v0.4 retrofit 到 cell 结构"已明示。C6 Cell.traceDir 默认 `.simx/trace`（v0.1 形态），保留 retrofit 接口（v0.4 改默认即可）。 |
| 10 | **examples/c6-tap.test.ts 是否做 assertion？** | **不在 TS 内 assert，shell wrapper 做 double-sided probe** | probe 调 /tap 会改 UI state；让 shell 在 simx run 退出后 probe（mirrors C4 smoke）。TS test 保最小用户视角代码——"`launch + tap` 不抛 = pass"，与 v1.md §4 "AI 0-shot 写测试"概念一致。 |

---

## 与冷计划假设的对照（CLAUDE.md §6 拒绝热化清单核查）

| 冷计划假设 | 本机实际 | 对策 |
|---|---|---|
| v0.2 C6 = "SDK tap/doubleTap/longPress/swipe/fill 接通 InputChannel" | C2 runner 只有 `/tap`；接全 5 个交互方法需 runner 加 4 个 endpoint —— 违反"swift 侧 0 改动" + 超出 C6 ROI | **决策 #8**：C6 只接 `tap`（text base form）；其它 4 方法 stub 改 hint 指向 v0.3；plan-cold v0.2 C6 概要语义性偏差列入 v1.md 决策日志 |
| C5 SDK `tap` 接通走 **InputChannel** | InputChannel 抽象在 host-hid 侧；C6 选 transport (A) 仅 runner /tap → 暂不走 InputChannel | **决策 #1**：v0.3 selector resolver 工作时，coord 从 runner /locate 拿，真 tap 走 host-hid InputChannel（届时 v1.md §2.A "主路径 9-arg/digitizer" 才真激活）；C6 是阶段性走 XCUITest 兜底——v1.md 不变量未破（v1.md §2.A 表写 "主路径 / 兜底" 表明兜底是合法路径） |
| iOS 26 runtime 存在 | ✓ iOS-26-4 26.4.1 isAvailable=true 且 dev sim Booted | 走 runner /tap |
| Swift bridge 由 sub-agent 在 worktree 中开发 | C6 swift 0 改动；不开 worktree | — |
| 9-arg Indigo 实现强烈参考 baguette repo | C3/C4 已落，C6 无需碰 | — |
| C2 XCUITest 兜底是不变量 | C6 把"兜底"用作主路径 | v1.md §2.A 表合法；阶段性偏差有时间窗（v0.3 资源化主路径） |
| 任一私有 framework 链接发现要硬链接 → stop 回报 | C6 不碰 Swift / 不碰 framework | — |
| 出口验收期望 tap 延迟 P50 < 50ms (iOS 26) / < 100ms (iOS 17/18) | C6 走 runner /tap = XCUITest snapshot ~数十 ms → 不在"主路径 ms 级红利"区间 | P50 metric 推 C7（plan-cold v0.2 出口验收对应）；v0.3 selector resolver + host-hid 接 Driver 后再达 ms 级 |

**结论**：与冷计划假设有 **3 处偏差**（决策 #1 transport 选 runner、决策 #8 砍 doubleTap/fill/...、决策 #9 traceDir 不 retrofit）；3 处都是**范围合理收窄**而非"假设错误"——属于 plan-cold 概要在 hot 化时的细化决策。**不**拒绝热化。

---

## 不变量（C6 范围内必满足；超出范围视为退化）

1. **CLAUDE.md §9.1 只支持模拟器**：C6 任何路径不引真机代码（runner host=127.0.0.1，sim udid only）
2. **CLAUDE.md §9.3 不暴露 xpath / 坐标 selector**：C6 RunnerClient.tap **类型签名**只接 `{text:string}`；Driver.tap 入口拒绝其它 base form
3. **CLAUDE.md §9.4 不提供裸 sleep**：C6 不引 `setTimeout(sleep)` 进 driver / runner-client / cell（健康轮询全在 shell 脚本 e2e wrapper 内，不污染 TS）
4. **CLAUDE.md §9.5 失败信息 AI-readable**：4 类 tap 失败（non-text selector / runner未启 / 404 / 5xx / fetch reject）的 hint 都是可执行修复指引
5. **CLAUDE.md §9.7 4 层信息结构始终保持**：`plan-hot.md`（C6 唯一）+ `roadmap.md` + `v1.md` + `plan-cold/v0.2.md` 四层完好
6. **Driver 接口签名 0 改动**（`src/driver/types.ts` byte-identical）—— 仅实现层（`simctl-driver.ts`）改 ctor 重载（重载是实现细节，TS 编译时 interface Driver 不变）
7. **Swift bridge `swift-bridge/Sources/**` 0 字节改动**（C2-C5 已稳，C6 是 TS-only 工作）
8. **既有 63 swift tests + 106 TS tests 一个都不退化**（106 中只有 1 个被删 — driver tap stub assertion；105 + 19 新增 = 124 floor，下限断 119 是兼容性容差）
9. **测试纪律 §8.4**：单测不 `vi.mock('node:http')` / 不 `vi.mock('node:child_process')` / 不 import `node:child_process` 进 driver 单测（fetch 通过 fetchImpl 注入；runner client 通过 deps 注入）
10. **范围纪律 §8.1**：不做 doubleTap/fill/swipe/scroll/clear/pressKey/hideKeyboard 等 9 个 HID bridge stub（保留既有 notSupported 行为）；不做 traceDir retrofit；不做 runner 内部托管；不做延迟 metric
11. **真验证用 dev sim**（`.simx/dev-sim.txt` 锁定 UDID）；e2e 脚本不 kill 用户其它 sim 进程
12. **依赖纪律 §8.7**：0 新 npm dep；0 新 SPM dep；0 新 brew/系统 dep

## 是否拒绝热化

**不拒绝**。所有入口条件 ✓；与冷计划假设有 3 处合理范围收窄（决策表 #1 #8 #9 已列出 + 与冷计划对照表已记）；C6 close 时需在 `docs/v1.md` 决策日志补一行记 transport 临时选 (A)、doubleTap/fill 推 C7+、traceDir retrofit 推 v0.4。
