# Generated by `scripts/dev/gen-llms.py`, copied here at build time by
# `web/scripts/sync-llms.sh` so the site serves the same text the repo
# does. A committed second copy would be a second answer to "what does
# smix tell an agent", and the copy is the one that drifts.
llms.txt
llms-full.txt
