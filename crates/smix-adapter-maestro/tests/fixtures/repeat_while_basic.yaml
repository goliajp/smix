appId: com.test.parity
---
# "false" is falsy, so the loop body never runs: this fixture only has to
# parse and execute. Seeded output is covered by
# mock_run_repeat_while_with_output.
- repeat:
    while: "false"
    commands:
      - tapOn:
          id: "would-not-run"
