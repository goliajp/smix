# napi build outputs — regenerated by `napi build`; the per-triple .node
# and the platform loader shims are distribution artifacts (prebuild matrix
# lands in v2.9-C4), not source. target/ and node_modules/ are covered by the
# repo-root .gitignore.
*.node
/index.js
/index.d.ts

# napi create-npm-dirs output — per-platform subpackages, regenerated at
# build/publish time, not source.
/npm/
