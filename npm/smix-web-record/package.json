{
  "name": "@goliapkg/smix-web-record",
  "version": "4.0.0",
  "description": "smix web recording bridge — captures browser interactions via Playwright and maps them to the platform-neutral smix IRAction, the web capture leg of the cross-platform recorder.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0 OR MIT",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "playwright": "^1.48.0"
  }
}
